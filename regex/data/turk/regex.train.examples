(example (utterance "items with a letter and a numeral .") (targetValue (name "?{and(<num>,<let>)}")))
(example (utterance "lines with zero or more of a letter and lower-case letter") (targetValue (name "?{contain(<let>),star(<low>)}")))
(example (utterance "lines containing words having a vowel or a character , 5 times or more") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines that have 1 of these: a character , an upper-case letter , or a small letter") (targetValue (name "?{or(or(<cap>,<low>),<any>)}")))
(example (utterance "lines with words without string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a character") (targetValue (name "?{or(or(<!>,<@>),<any>)}")))
(example (utterance "lines starting with words with string leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and letter before string leftquoatation @ rightquoatation") (targetValue (name "?{contain(and(<!>,<let>)),<@>}")))
(example (utterance "lines with over 2 of the string dog before vowel") (targetValue (name "?{<!>,not(repeatatleast(<vow>,2))}")))
(example (utterance "lines containing a character followed by a lower-case letter") (targetValue (name "?{contain(concat(<any>,<low>))}")))
(example (utterance "lines with a vowel before a letter before number") (targetValue (name "?{<vow>,concat(<let>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation  , a letter , or a lower-case letter") (targetValue (name "?{<!>,or(or(<@>,<let>),<low>)}")))
(example (utterance "lines having words with a capital letter , string leftquoatation ! rightquoatation , and with or without string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<cap>),<@>)}")))
(example (utterance "lines with a number , zero or more times followed by a lower-case letter") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "lines with a letter followed by a vowel") (targetValue (name "?{<let>,contain(<vow>)}")))
(example (utterance "items with an upper case letter preceding leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{concat(<cap>,<!>),<any>}")))
(example (utterance "items with a numeral or a letter at least 4 times .") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "leftquoatation ! rightquoatation and leftquoatation @ rightquoatation appears before a vowel in the lines") (targetValue (name "?{contain(<!>),concat(<@>,<vow>)}")))
(example (utterance "items without leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines with a letter or vowel at least 2 times") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines which contain a vowel at least 7 or more times before a lower-case letter") (targetValue (name "?{repeatatleast(<vow>,7),<low>}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and a vowel") (targetValue (name "?{concat(<let>,<!>),and(<@>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter and character") (targetValue (name "?{concat(<!>,<low>),star(<any>)}")))
(example (utterance "lines that end with words that have a number followed by a character") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines without a capital letter or vowel") (targetValue (name "?{or(<cap>,<vow>)}")))
(example (utterance "lines that have a small letter and then the leftquoatation ! rightquoatation string") (targetValue (name "?{contain(<low>),<!>}")))
(example (utterance "lines with words ending in 4 or more of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,4)}")))
(example (utterance "lines which are containing a capital letter before leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{contain(concat(<cap>,<!>))}")))
(example (utterance "lines containing a lower-case letter followed by 3 or more characters") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines that contain a vowel followed by a number and a lower-case letter , at least once") (targetValue (name "?{repeatatleast(<vow>,1),concat(<num>,<low>)}")))
(example (utterance "lines with letter before string leftquoatation ! rightquoatation and vowel") (targetValue (name "?{<let>,and(<!>,<vow>)}")))
(example (utterance "lines containing a capital letter after leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{<!>,repeatatleast(<cap>,6)}")))
(example (utterance "lines with a vowel words followed by string leftquoatation ! rightquoatation") (targetValue (name "?{<!>,<vow>}")))
(example (utterance "lines containing words having a character at least once or leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,1),<!>}")))
(example (utterance "lines where leftquoatation ! rightquoatation or a vowel come after a number") (targetValue (name "?{contain(<num>),or(<!>,<vow>)}")))
(example (utterance "lines with words that contain only leftquoatation ! rightquoatation , 2 or more times in them") (targetValue (name "?{<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or lower-case or capital letter") (targetValue (name "?{or(or(<!>,<low>),<cap>)}")))
(example (utterance "lines containing lower-case letter or character at least once") (targetValue (name "?{contain(<low>),repeatatleast(<any>,1)}")))
(example (utterance "lines with lower-case letter before character and string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,and(<any>,<!>)}")))
(example (utterance "lines which have leftquoatation ! rightquoatation before either a letter , a number , or the leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<let>,<num>),<@>)}")))
(example (utterance "lines with a lower-case letter or character") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines without a vowel before a letter") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines with zero or more of a vowel before number") (targetValue (name "?{<vow>,star(contain(<num>))}")))
(example (utterance "lines with a lower-case letter or with capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,1),concat(<cap>,<!>)}")))
(example (utterance "lines with words a long with leftquoatation ! rightquoatation at least 6 times or being leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,6),<@>}")))
(example (utterance "lines with a capital letter , a character or a lower case letter , a vowel ,  zero times or more") (targetValue (name "?{or(and(and(<cap>,<vow>),<any>),<low>)}")))
(example (utterance "all lines") (targetValue (name "?{endwith(or(<!>,<@>))}")))
(example (utterance "lines not starting in 3 or more characters") (targetValue (name "?{startwith(repeatatleast(<any>,3))}")))
(example (utterance "lines with a letter , a number , and a vowel , in order") (targetValue (name "?{<let>,and(<num>,<vow>)}")))
(example (utterance "lines ending over 7 times with leftquoatation ! rightquoatation or a lower-case") (targetValue (name "?{repeatatleast(or(<!>,<low>),7)}")))
(example (utterance "lines with a vowel , character , and lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<any>),<low>),<!>}")))
(example (utterance "lines with 4 or more of a vowel before a character") (targetValue (name "?{<vow>,repeatatleast(<any>,4)}")))
(example (utterance "lines starting in a character or ending in string leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,endwith(<!>))}")))
(example (utterance "the lines are starting with only a lower-case letter or leftquoatation ! rightquoatation .") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines that have a combination starting with the word leftquoatation ! rightquoatation and containing a letter preceding a lower-case letter") (targetValue (name "?{concat(<let>,<low>),startwith(<!>)}")))
(example (utterance "lines containing  lower-case letter , 2 or more times") (targetValue (name "?{endwith(repeatatleast(<low>,2))}")))
(example (utterance "lines containing a lower-case letter , followed by a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{<low>,and(<vow>,<!>)}")))
(example (utterance "lines with a character ending in vowel") (targetValue (name "?{<any>,endwith(<vow>)}")))
(example (utterance "items beginning with a character preceding leftquoatation ! rightquoatation .") (targetValue (name "?{startwith(<any>),star(<!>)}")))
(example (utterance "lines with a character or a number followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<num>),<!>}")))
(example (utterance "lines with a lower-case , at least 6 times and having a character") (targetValue (name "?{repeatatleast(<low>,6),contain(<any>)}")))
(example (utterance "lines containing vowel before lower-case letter and letter") (targetValue (name "?{and(concat(<vow>,<low>),<let>)}")))
(example (utterance "lines with words with either leftquoatation ! rightquoatation , a letter , or a character in them") (targetValue (name "?{or(<!>,<let>),<any>}")))
(example (utterance "items with an upper case letter preceding a character at least thrice .") (targetValue (name "?{<cap>,repeatatleast(<any>,3)}")))
(example (utterance "lines with words and a number before capital letter") (targetValue (name "?{<num>,<cap>}")))
(example (utterance "lines with a capital letter after leftquoatation ! rightquoatation , a lower-case letter , or a number") (targetValue (name "?{or(or(<!>,<low>),<num>),<cap>}")))
(example (utterance "lines with a capital letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with 2 or more characters before a capital letter") (targetValue (name "?{repeatatleast(<any>,2),<cap>}")))
(example (utterance "lines with a letter and a character , or a capital letter") (targetValue (name "?{and(<let>,<any>),<cap>}")))
(example (utterance "lines wherein an uppercase letter is followed by a vowel 3 or more times") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation and leftquoatation # rightquoatation and a letter") (targetValue (name "?{and(and(<let>,<@>),<#>),<!>}")))
(example (utterance "lines with 7 or more of a character before a lower-case letter") (targetValue (name "?{<any>,repeatatleast(<low>,6)}")))
(example (utterance "lines with words that contain only string leftquoatation ! rightquoatation and lower-case letter") (targetValue (name "?{<!>,<low>}")))
(example (utterance "lines containing a lower-case letter , a character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<any>),<!>)}")))
(example (utterance "lines with a lower-case before capital letter then letter") (targetValue (name "?{<low>,concat(<cap>,<let>)}")))
(example (utterance "lines containing a vowel , a number , then a capital letter") (targetValue (name "?{concat(<vow>,<num>),<cap>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation before character , string leftquoatation @ rightquoatation , and string leftquoatation # rightquoatation") (targetValue (name "?{endwith(<!>),and(and(<any>,<@>),<#>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation followed by a vowel 1 or more times") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{<low>,star(contain(<!>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a letter and a character") (targetValue (name "?{and(concat(<!>,<let>),<any>)}")))
(example (utterance "lines containing 0 or more capital letters followed by a letter") (targetValue (name "?{star(<cap>),<let>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before a vowel , zero or more times in it") (targetValue (name "?{endwith(<!>),star(<vow>)}")))
(example (utterance "lines with a letter after a capital letter before a number") (targetValue (name "?{concat(<cap>,<let>),<num>}")))
(example (utterance "lines with a vowel or a character , 4 or more times in them") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a number , leftquoatation @ rightquoatation , and lower case letter") (targetValue (name "?{contain(<!>),and(and(<num>,<@>),<low>)}")))
(example (utterance "lines containing a character before a lower-case letter , 2 or more times") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines containing character or ending with a capital") (targetValue (name "?{or(<any>,endwith(<cap>))}")))
(example (utterance "lines with words having leftquoatation ! rightquoatation") (targetValue (name "?{star(<!>),<@>}")))
(example (utterance "lines with a letter then a vowel then a capital") (targetValue (name "?{<let>,concat(<vow>,<cap>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation and words") (targetValue (name "?{<num>,<!>}")))
(example (utterance "lines with a number before non letter") (targetValue (name "?{<num>,not(<let>)}")))
(example (utterance "lines which include 1 or more instances of a character and a number .") (targetValue (name "?{contain(<any>),repeatatleast(<num>,1)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation followed by the word leftquoatation @ rightquoatation , as well as a number and a letter") (targetValue (name "?{<!>,and(and(<@>,<num>),<let>)}")))
(example (utterance "lines with a character before either leftquoatation ! rightquoatation , a vowel , or leftquoatation @ rightquoatation") (targetValue (name "?{<any>,or(or(<@>,<vow>),<!>)}")))
(example (utterance "lines with a lower-case letter before a vowel which is followed by a capital letter") (targetValue (name "?{<low>,concat(<vow>,<cap>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a number") (targetValue (name "?{<any>,and(and(<!>,<@>),<num>)}")))
(example (utterance "lines with a letter followed by leftquoatation ! rightquoatation before a non lower-case letter") (targetValue (name "?{concat(<let>,<!>),not(<low>)}")))
(example (utterance "items with a numeral preceding leftquoatation ! rightquoatation preceding a small letter .") (targetValue (name "?{<num>,concat(<!>,<low>)}")))
(example (utterance "lines which are not having a lower-case letter , a capital letter , or a vowel") (targetValue (name "?{or(or(<low>,<cap>),<vow>)}")))
(example (utterance "items with a letter preceding an upper case letter preceding a small letter .") (targetValue (name "?{concat(<let>,<cap>),<low>}")))
(example (utterance "lines containing a number after a character") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before either leftquoatation @ rightquoatation , a lower-case letter , or leftquoatation # rightquoatation in them") (targetValue (name "?{<!>,or(or(<@>,<low>),<#>)}")))
(example (utterance "lines with a letter or character and a lower-case letter") (targetValue (name "?{contain(<let>),and(<any>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation and leftquoatation # rightquoatation and a number") (targetValue (name "?{repeatatleast(<!>,1),and(and(<@>,<num>),<#>)}")))
(example (utterance "lines with character or string leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lives containing5or more combinations of a number before a vowel") (targetValue (name "?{<num>,repeatatleast(<vow>,5)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or vowel") (targetValue (name "?{<!>,<vow>}")))
(example (utterance "lines that have at least 1 character followed by a lower-case letter") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel and a number") (targetValue (name "?{and(<num>,<!>),<vow>}")))
(example (utterance "lines having a letter , greater than or equal to zero times before having a capital letter") (targetValue (name "?{star(<let>),<cap>}")))
(example (utterance "lines that end with a lower-case letter and a letter , zero times or more") (targetValue (name "?{endwith(<low>),star(<let>)}")))
(example (utterance "lines ending with a vowel , at least 4 times") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines with 7 or more letters , or a character before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,7),concat(<any>,<!>)}")))
(example (utterance "lines containing a vowel") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines with letters not before a number .") (targetValue (name "?{contain(concat(<let>,<num>))}")))
(example (utterance "lines that begin with a number") (targetValue (name "?{<num>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , followed by a vowel and a character") (targetValue (name "?{<!>,and(<vow>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a letter , or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<let>),<@>}")))
(example (utterance "lines containing lower-case letter , vowel , and number") (targetValue (name "?{and(and(<low>,<vow>),<num>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation or end with a character , at least once") (targetValue (name "?{or(<!>,endwith(<any>))}")))
(example (utterance "lines with letter , 3 or more times and containing a number") (targetValue (name "?{repeatatleast(<let>,3),contain(<num>)}")))
(example (utterance "items with a letter preceding leftquoatation ! rightquoatation or an upper case letter .") (targetValue (name "?{<let>,or(<!>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a letter") (targetValue (name "?{<let>,<!>}")))
(example (utterance "lines ending in a vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(concat(<vow>,<!>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation after a capital letter , a lower-case letter , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<cap>,<low>),<@>),<!>}")))
(example (utterance "lines with a character and string leftquoatation ! rightquoatation before lower-case letter , zero or more times") (targetValue (name "?{and(<any>,<!>),star(<low>)}")))
(example (utterance "lines with a number before capital letter , vowel , and character") (targetValue (name "?{<num>,and(and(<cap>,<vow>),<any>)}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{or(concat(<let>,<!>),<cap>)}")))
(example (utterance "lines which are ending with a number before a letter") (targetValue (name "?{endwith(<num>),<let>}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation") (targetValue (name "?{<num>,contain(<!>)}")))
(example (utterance "lines ending with a letter") (targetValue (name "?{star(<let>)}")))
(example (utterance "lines that contain6or more vowels or leftquoatation ! rightquoatation") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines with words and a letter , the leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(<let>,<!>),<@>}")))
(example (utterance "lines that end with end with leftquoatation ! rightquoatation preceded by a number") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines with lower-case letter before character , 6 or more times") (targetValue (name "?{<low>,repeatatleast(<any>,6)}")))
(example (utterance "lines with words and containing a number") (targetValue (name "?{or(<num>,<let>),<vow>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before letter and ending with vowel") (targetValue (name "?{concat(<!>,<let>),endwith(<vow>)}")))
(example (utterance "lines containing words with a letter and leftquoatation ! rightquoatation") (targetValue (name "?{<let>,<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation then a vowel or character") (targetValue (name "?{<!>,or(<vow>,<any>)}")))
(example (utterance "lines that end in a vowel or number , 1 or more times") (targetValue (name "?{endwith(<vow>),repeatatleast(<num>,1)}")))
(example (utterance "lines that have a lower-case letter after a vowel") (targetValue (name "?{<vow>,<low>}")))
(example (utterance "lines that ends with a lower-case or capital letter") (targetValue (name "?{or(<low>,<cap>)}")))
(example (utterance "lines starting in number and 3 or more string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<num>),repeatatleast(<!>,3)}")))
(example (utterance "lines with a character after a capital letter or a vowel") (targetValue (name "?{or(<cap>,<vow>),<any>}")))
(example (utterance "lines should have a capital letter or character") (targetValue (name "?{<any>,<cap>}")))
(example (utterance "lines with a lower case letter after a number") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines containing the leftquoatation ! rightquoatation , leftquoatation @ rightquoatation  and a capital letter before 'ring") (targetValue (name "?{contain(and(and(<!>,<@>),<cap>)),<#>}")))
(example (utterance "lines with 4 or more lower-case letters or leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines that have either a lower-case letter , a leftquoatation ! rightquoatation string , or leftquoatation @ rightquoatation appearing before a character") (targetValue (name "?{or(or(<@>,<low>),<!>),<any>}")))
(example (utterance "lines with letter , zero or more times and ending with number") (targetValue (name "?{star(<let>),endwith(<num>)}")))
(example (utterance "lines that contain words with a number , letter , and a capital letter .") (targetValue (name "?{and(<num>,<let>),<cap>}")))
(example (utterance "lines with a character , 7 or more times before a capital") (targetValue (name "?{repeatatleast(<any>,7),<cap>}")))
(example (utterance "lines with words ending in a capital letter before a vowel") (targetValue (name "?{endwith(<cap>),<vow>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before a capital , a letter , and vowel") (targetValue (name "?{<!>,and(and(<cap>,<let>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter before a letter") (targetValue (name "?{<!>,concat(<low>,<let>)}")))
(example (utterance "lines without a capital letter , a lower-case letter or any letter") (targetValue (name "?{or(or(<low>,<cap>),<let>)}")))
(example (utterance "lines with a vowel at least once not being a number") (targetValue (name "?{repeatatleast(<vow>,1),not(<num>)}")))
(example (utterance "items with any letter or numeral .") (targetValue (name "?{and(and(<let>,<num>),<cap>)}")))
(example (utterance "lines containing a character and a vowel") (targetValue (name "?{<any>,<vow>}")))
(example (utterance "leftquoatation ! rightquoatation or a lower-case letter appears before a character in the lines") (targetValue (name "?{or(<!>,<low>),<any>}")))
(example (utterance "lines ending with a capital letter , zero or more times") (targetValue (name "?{star(<cap>)}")))
(example (utterance "lines containing words with either capital or lower-case letters") (targetValue (name "?{or(<cap>,<let>),<low>}")))
(example (utterance "lines with a letter or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<cap>),<!>),<@>}")))
(example (utterance "items with leftquoatation ! rightquoatation , a character , and an upper case letter .") (targetValue (name "?{and(and(<!>,<cap>),<any>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation or 'truck or a character") (targetValue (name "?{<num>,or(or(<!>,<@>),<any>)}")))
(example (utterance "lines containing either lower-case letter , vowel , or capital letter") (targetValue (name "?{or(or(<low>,<vow>),<cap>)}")))
(example (utterance "line with words containing a vowel or lower-case letter") (targetValue (name "?{<vow>,<low>}")))
(example (utterance "lines that have a vowel , leftquoatation ! rightquoatation , or a capital letter following leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<@>),<cap>}")))
(example (utterance "lines with a 6 or more of a character before a letter") (targetValue (name "?{or(concat(<any>,<let>),<low>)}")))
(example (utterance "lines stating with 2 or more of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,2)}")))
(example (utterance "items with an uppe case letter and character preceding leftquoatation ! rightquoatation .") (targetValue (name "?{and(<cap>,<any>),<!>}")))
(example (utterance "lines with less than 3 characters") (targetValue (name "?{contain(repeatatleast(<any>,3))}")))
(example (utterance "lines containing the words with leftquoatation ! rightquoatation at least 6 times") (targetValue (name "?{<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a lower-case letter") (targetValue (name "?{startwith(<!>),contain(<low>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation or a lower-case letter then leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,concat(<low>,<@>))}")))
(example (utterance "lines with zero or more of a letter , character , and string leftquoatation ! rightquoatation") (targetValue (name "?{contain(and(and(<let>,<any>),<!>))}")))
(example (utterance "leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation or a vowel appears zero or more times") (targetValue (name "?{or(concat(<!>,<@>),<vow>)}")))
(example (utterance "lines with letter or a number") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a lower case letter") (targetValue (name "?{<low>,<!>}")))
(example (utterance "lines with vowels preceding a character") (targetValue (name "?{<any>,<vow>}")))
(example (utterance "lines containing containing only string leftquoatation ! rightquoatation before capital letter") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines without a vowel , that don't contain leftquoatation ! rightquoatation nor leftquoatation @ rightquoatation") (targetValue (name "?{not(and(and(<vow>,<!>),<@>))}")))
(example (utterance "lines ending with number followed by vowel , 5 or more times") (targetValue (name "?{endwith(concat(<num>,<vow>))}")))
(example (utterance "lines with a lower-case letter before number") (targetValue (name "?{and(<let>,<low>),<num>}")))
(example (utterance "lines containing a letter before a vowel preceding a lower-case letter") (targetValue (name "?{<let>,concat(<vow>,<low>)}")))
(example (utterance "lines whiich are not having a capital letter , 3 or more times or the string 'dog") (targetValue (name "?{or(repeatatleast(<cap>,3),<!>)}")))
(example (utterance "lines with at least 1 lower-case letter followed by a letter") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines starting with a small letter 5x or more , or leftquoatation ! rightquoatation") (targetValue (name "?{or(repeatatleast(<low>,5),<!>)}")))
(example (utterance "lines with words with a character or string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation") (targetValue (name "?{<any>,concat(<!>,<@>)}")))
(example (utterance "lines not ending in leftquoatation ! rightquoatation or vowel") (targetValue (name "?{or(not(<!>),<vow>)}")))
(example (utterance "lines with a capital letter before a character , number or leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,or(or(<any>,<num>),<!>)}")))
(example (utterance "lines without a character and not starting in string leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,startwith(<!>))}")))
(example (utterance "lines that conclude with a vowel no less than 1 time at the minimum") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines containing zero or more vowels") (targetValue (name "?{<vow>}")))
(example (utterance "lines without number , zero or more times before character") (targetValue (name "?{concat(star(<num>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or 3 or more of leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "leftquoatation ! rightquoatation , leftquoatation @ rightquoatation and a letter or leftquoatation # rightquoatation appears in the beginning of the lines") (targetValue (name "?{or(and(and(<!>,<@>),<let>),<#>)}")))
(example (utterance "items with a character at least 4 times preceding a number .") (targetValue (name "?{repeatatleast(<any>,4),<num>}")))
(example (utterance "lines without only 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,3)}")))
(example (utterance "lines containing 0 or more of leftquoatation ! rightquoatation , before a number or leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),or(<num>,<@>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by capital letter before number") (targetValue (name "?{concat(<!>,<cap>),<num>}")))
(example (utterance "lines with character and string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation at least once") (targetValue (name "?{and(<any>,<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines with a vowel capital letter followed by a number") (targetValue (name "?{concat(<cap>,<num>),<vow>}")))
(example (utterance "lines that have words with a number before 6 or more lower-case letters") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines which are not having leftquoatation ! rightquoatation and not starting with a letter") (targetValue (name "?{and(<!>,startwith(<let>))}")))
(example (utterance "lines that don't have leftquoatation ! rightquoatation and contain a number") (targetValue (name "?{not(<!>),contain(<num>)}")))
(example (utterance "items with a numeral , leftquoatation ! rightquoatation and a letter .") (targetValue (name "?{and(and(<num>,<!>),<let>)}")))
(example (utterance "lines with a lower-case letter followed by a number then a capital letter") (targetValue (name "?{concat(<low>,<num>),<cap>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation which is followed by a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with a number before ending in string leftquoatation ! rightquoatation or a letter") (targetValue (name "?{<num>,endwith(or(<!>,<let>))}")))
(example (utterance "lines with words , letter , zero , more times") (targetValue (name "?{<let>}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation then number") (targetValue (name "?{<cap>,concat(<!>,<num>)}")))
(example (utterance "lines with a letter before a capital letter then a vowel") (targetValue (name "?{<let>,concat(<cap>,<vow>)}")))
(example (utterance "lines which are containing a character and ending with leftquoatation ! rightquoatation") (targetValue (name "?{contain(<any>),endwith(<!>)}")))
(example (utterance "lines containing a capital letter followed by a number which precedes leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,concat(<num>,<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , an upper case letter , or a letter preceding a character .") (targetValue (name "?{or(or(<!>,<cap>),<let>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or letter and a character") (targetValue (name "?{contain(<!>),and(<let>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and a letter") (targetValue (name "?{concat(<!>,<@>),star(<let>)}")))
(example (utterance "lines with a lower-case letter or vowel") (targetValue (name "?{or(contain(<low>),<vow>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation or ending in leftquoatation @ rightquoatation") (targetValue (name "?{concat(<any>,<!>),endwith(<@>)}")))
(example (utterance "lines which have a number or a vowel before a letter") (targetValue (name "?{or(<num>,<vow>),<let>}")))
(example (utterance "items with a vowel preceding a letter at least 4 times .") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines with words with either a character ,  string leftquoatation ! rightquoatation , or string leftquoatation @ rightquoatation") (targetValue (name "?{or(<any>,<!>),<@>}")))
(example (utterance "lines containing a lower-case letter or string leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{contain(or(<low>,<!>))}")))
(example (utterance "lines with a number and a character followed by a lower-case letter or a letter") (targetValue (name "?{and(<num>,<any>),or(<low>,<let>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation or with a letter") (targetValue (name "?{or(star(<!>),<let>)}")))
(example (utterance "lines containing a character followed by a vowel , leftquoatation ! rightquoatation , as well as leftquoatation @ rightquoatation") (targetValue (name "?{<any>,and(and(<vow>,<!>),<@>)}")))
(example (utterance "lines with 4 or more of a character before lower-case letter") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines with a character and string leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{and(<any>,<!>),contain(<low>)}")))
(example (utterance "lines not having words that contain only a number") (targetValue (name "?{<num>}")))
(example (utterance "a vowel or a character appears 5 or more times in the line") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines with neither leftquoatation ! rightquoatation , a lower-case letter , or a vowel") (targetValue (name "?{or(or(<!>,<low>),<vow>)}")))
(example (utterance "lines with a letter and leftquoatation ! rightquoatation coming before a number") (targetValue (name "?{and(<let>,<!>),<num>}")))
(example (utterance "items with words containing a character , a small letter , or a capitla letter .") (targetValue (name "?{or(<cap>,<low>),<any>}")))
(example (utterance "items with a capital letter or character at least thrice .") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "lines with a lower-case letter or ending in a capital letter") (targetValue (name "?{or(<low>,endwith(<cap>))}")))
(example (utterance "lines containing a letter which then have the leftquoatation ! rightquoatation string prior to a vowel") (targetValue (name "?{concat(<let>,<!>),<vow>}")))
(example (utterance "lines with a letter or vowel once") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines with a lower-case letter 7 or more times") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines with words ending with number before lower-case letter") (targetValue (name "?{endwith(<num>),<low>}")))
(example (utterance "items with a character preceding a letter .") (targetValue (name "?{<any>,contain(<let>)}")))
(example (utterance "lines that have no characters in them") (targetValue (name "?{<any>}")))
(example (utterance "lines with at least 5 words ending with a capital letter") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation then number") (targetValue (name "?{concat(<any>,<!>),<num>}")))
(example (utterance "lines containing a lower-case letter or leftquoatation ! rightquoatation at least 7 times") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines without leftquoatation dog' , an uppercase letter , or a number") (targetValue (name "?{or(or(<!>,<cap>),<num>)}")))
(example (utterance "lines with letters before a number or a capital") (targetValue (name "?{<let>,or(<num>,<cap>)}")))
(example (utterance "lines having a vowel followed by either a lower-case letter , a capital letter , or a character") (targetValue (name "?{<vow>,or(or(<low>,<cap>),<any>)}")))
(example (utterance "the lines that end with a number followed by at least 1 capital letter") (targetValue (name "?{endwith(concat(<num>,<cap>))}")))
(example (utterance "lines with 6 or more of a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter then non string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<cap>),not(<@>)}")))
(example (utterance "lines starting in capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with 1 or more capital letter before a character") (targetValue (name "?{repeatatleast(<cap>,1),<any>}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation not a character") (targetValue (name "?{or(<!>,<cap>),not(<any>)}")))
(example (utterance "items with a letter preceding a capital letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{<let>,concat(<cap>,<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 3 or more times or not vowel") (targetValue (name "?{repeatatleast(<!>,3),not(<vow>)}")))
(example (utterance "lines that start with a letter at least 5 times") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "lines containing words with either vowel , character , or number") (targetValue (name "?{or(or(<vow>,<any>),<num>)}")))
(example (utterance "lines ending in words with a number") (targetValue (name "?{contain(<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation a lower-case letter zero or more times") (targetValue (name "?{star(<!>),concat(<low>,<any>)}")))
(example (utterance "lines with a number and letter and character") (targetValue (name "?{and(and(<let>,<num>),<any>)}")))
(example (utterance "lines that do not end with leftquoatation ! rightquoatation followed by a vowel") (targetValue (name "?{concat(endwith(<!>),<vow>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "items without any letter or character .") (targetValue (name "?{or(or(<let>,<any>),<cap>)}")))
(example (utterance "lines which are not containing leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{contain(or(<!>,<@>))}")))
(example (utterance "lines starting with letter and string leftquoatation ! rightquoatation or vowel") (targetValue (name "?{startwith(<let>),or(<!>,<vow>)}")))
(example (utterance "lines that don't have a lower-case letter , or leftquoatation ! rightquoatation and no letter") (targetValue (name "?{and(and(<low>,<!>),<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation at least thrice .") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with a letter which is followed by a vowel at least once") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines containing either lower-case letter , string leftquoatation ! rightquoatation , or letter") (targetValue (name "?{or(or(<low>,<!>),<let>)}")))
(example (utterance "items beginning with a small letter , at least 4 times .") (targetValue (name "?{repeatatleast(<low>,4)}")))
(example (utterance "lines with a vowel at least once before a letter in them") (targetValue (name "?{repeatatleast(<vow>,1),<let>}")))
(example (utterance "lines do not contain a character before a number") (targetValue (name "?{not(<any>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a letter at least once") (targetValue (name "?{repeatatleast(<let>,1),<!>}")))
(example (utterance "lines that have a letter after a character at least once") (targetValue (name "?{<any>,repeatatleast(<num>,1)}")))
(example (utterance "lines where leftquoatation ! rightquoatation is not followed by a number") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines without a letter") (targetValue (name "?{and(and(<let>,<low>),<cap>)}")))
(example (utterance "lines that have5or more lower-case letters") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines with a capital letter after leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{<!>,concat(<cap>,<@>)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation or end with leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),endwith(<@>)}")))
(example (utterance "lines having a letter , leftquoatation ! rightquoatation , and a number followed by a character") (targetValue (name "?{and(and(<let>,<!>),<num>),<any>}")))
(example (utterance "lines with a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{<let>,<!>}")))
(example (utterance "lines having words containing a number , a character , as well as a capital letter") (targetValue (name "?{and(<num>,<any>),<cap>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation , lower-case letter , and number") (targetValue (name "?{and(<!>,<low>),<num>}")))
(example (utterance "items with a vowel preceding a character at least 1 time .") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines ending with strings leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation , leftquoatation # rightquoatation , or leftquoatation $ rightquoatation .") (targetValue (name "?{endwith(<!>),or(or(<@>,<#>),<$>)}")))
(example (utterance "lines with numbers before leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{<num>,or(<!>,<low>)}")))
(example (utterance "lines with words containing letters  either lower case or capital or leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<cap>),<!>}")))
(example (utterance "there must a vowel coming after a character in the lines") (targetValue (name "?{<any>,<vow>}")))
(example (utterance "lines with a lower-case or capital letter before a vowel") (targetValue (name "?{contain(<low>),concat(<cap>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a small letter") (targetValue (name "?{<!>,not(<cap>)}")))
(example (utterance "lines with a vowel and a letter at least zero or more times") (targetValue (name "?{star(<vow>),contain(<let>)}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation followed by a lower-case letter , a character , and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<low>,<@>),<any>)}")))
(example (utterance "lines with a vowel before a lower-case letter , 5 or more times in them") (targetValue (name "?{<vow>,repeatatleast(<low>,5)}")))
(example (utterance "lines ending with a letter or string leftquoatation ! rightquoatation before a character") (targetValue (name "?{endwith(or(<let>,<!>)),<any>}")))
(example (utterance "lines which are ending with leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{endwith(or(<!>,<cap>))}")))
(example (utterance "lines with words and starting in string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{startwith(<!>),<low>}")))
(example (utterance "lines having letters that aren't preceded by a vowel") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines where 5 or more times leftquoatation ! rightquoatation or a vowel occurs") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines with a character , number , capital letter") (targetValue (name "?{or(or(<any>,<num>),<cap>)}")))
(example (utterance "lines containing character or vowel , zero or more times") (targetValue (name "?{contain(<any>),star(<vow>)}")))
(example (utterance "lines which are starting with a vowel or a letter") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines that do no have a lower-case letter after a number") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines which are containing either a lower-case letter , a capital letter , or a number") (targetValue (name "?{or(or(<low>,<cap>),<num>)}")))
(example (utterance "lines with words with either string leftquoatation ! rightquoatation , vowel , or string leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<vow>),<@>}")))
(example (utterance "lines with a number preceding a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{<num>,or(<vow>,<!>)}")))
(example (utterance "lines do not contain 5 or more letters , nor leftquoatation ! rightquoatation") (targetValue (name "?{and(repeatatleast(<let>,5),<!>)}")))
(example (utterance "lines that start with string leftquoatation ! rightquoatation and with 1 or more vowels") (targetValue (name "?{repeatatleast(<vow>,1),startwith(<!>)}")))
(example (utterance "lines with a letter after a capital letter or a character") (targetValue (name "?{or(<cap>,<any>),contain(<let>)}")))
(example (utterance "lines with a letter followed by leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<let>,<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation before at least 1 letter") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "6 or more lines starting with a capital letter") (targetValue (name "?{repeatatleast(<cap>,6)}")))
(example (utterance "lines with only a character at least zero times") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation , 2 or more times") (targetValue (name "?{<!>,repeatatleast(<@>,2)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or leftquoatation # rightquoatation or a lower-case letter") (targetValue (name "?{contain(<!>),or(or(<@>,<#>),<low>)}")))
(example (utterance "lines with a vowel before a lower-case letter , string leftquoatation ! rightquoatation , or letter") (targetValue (name "?{<vow>,or(or(<low>,<!>),<let>)}")))
(example (utterance "lines that have either a letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<@>,<let>),<!>)}")))
(example (utterance "leftquoatation ! rightquoatation appears before a number and a lower-case letter in the lines") (targetValue (name "?{<!>,and(<num>,<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and leftquoatation @ rightquoatation in sequence , and then a lower-case letter .") (targetValue (name "?{concat(<!>,<@>),<low>}")))
(example (utterance "lines with 7 or more leftquoatation ! rightquoatation and a letter before a lower-case letter") (targetValue (name "?{repeatatleast(<!>,7),concat(<let>,<low>)}")))
(example (utterance "items with a vowel preceding an upper case letter or leftquoatation ! rightquoatation .") (targetValue (name "?{<vow>,or(<cap>,<!>)}")))
(example (utterance "lines with a letter before a lower-case letter") (targetValue (name "?{<let>,contain(<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 2 or more times before not a letter") (targetValue (name "?{repeatatleast(<!>,2),not(<let>)}")))
(example (utterance "lines with a lower-case letter , at least 3 times before a vowel") (targetValue (name "?{repeatatleast(<low>,3),<vow>}")))
(example (utterance "lines with a letter or string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{contain(<let>),concat(<!>,<vow>)}")))
(example (utterance "lines with words with 5 or more of a vowel and capital letter") (targetValue (name "?{and(<vow>,<cap>)}")))
(example (utterance "lines with letters or characters before a vowel") (targetValue (name "?{or(<let>,<any>),<vow>}")))
(example (utterance "lines containing letter before leftquoatation ! rightquoatation or a number") (targetValue (name "?{<let>,or(<!>,<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , a vowel , and letter") (targetValue (name "?{and(and(<!>,<vow>),<let>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a lower-case letter and a vowel") (targetValue (name "?{<!>,concat(<low>,<vow>)}")))
(example (utterance "lines with words with zero or more of a lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines that do not contain a lower-case letter followed by a capital letter") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines that begin with either a lower-case letter or a letter") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , 3 or more times and which end with a character") (targetValue (name "?{repeatatleast(<!>,3),endwith(<any>)}")))
(example (utterance "lines with a number after leftquoatation ! rightquoatation at least 4 times") (targetValue (name "?{<!>,repeatatleast(<num>,4)}")))
(example (utterance "lines wherein 5 or more times it contains words with an uppercase letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines without a number , a capital letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<!>),<cap>)}")))
(example (utterance "lines with 4 or more of a character before a number") (targetValue (name "?{<any>,repeatatleast(<num>,4)}")))
(example (utterance "lines containing a lower-case letter before leftquoatation ! rightquoatation followed by a vowel") (targetValue (name "?{concat(<low>,<!>),<vow>}")))
(example (utterance "lines wherein at least once a number is preceded by a character") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines with zero or more of a string leftquoatation ! rightquoatation and 'truck and a lower-case letter") (targetValue (name "?{contain(and(and(<!>,<low>),<@>))}")))
(example (utterance "lines without leftquoatation ! rightquoatation or number") (targetValue (name "?{or(not(<!>),<num>)}")))
(example (utterance "lines starting with a lower-case letter and ending with string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<low>),endwith(<!>)}")))
(example (utterance "lines containing a letter or a character at least 2 times") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with a lower-case letter before a character") (targetValue (name "?{repeatatleast(<low>,1),<any>}")))
(example (utterance "lines with words that don't leftquoatation ! rightquoatation or leftquoatation @ rightquoatation in them") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines that don't have leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{or(<!>,and(<@>,<#>))}")))
(example (utterance "lines containing at least 1 character , leftquoatation ! rightquoatation , and a letter preceded by a vowel") (targetValue (name "?{and(and(<any>,<!>),<vow>),<let>}")))
(example (utterance "lines ending with lower-case letter followed by letter") (targetValue (name "?{endwith(concat(<low>,<let>))}")))
(example (utterance "lines that do not start with a letter") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation before a letter") (targetValue (name "?{concat(<!>,<@>),repeatatleast(<let>,1)}")))
(example (utterance "lines containing only letter , 2 or more times before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,2),<!>}")))
(example (utterance "lines without a lone character .") (targetValue (name "?{<any>}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , a number preceding leftquoatation # rightquoatation .") (targetValue (name "?{and(and(<num>,<@>),<!>),<#>}")))
(example (utterance "items with a vowel preceding an upper case letter .") (targetValue (name "?{<vow>,<cap>}")))
(example (utterance "lines without a letter before leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{not(<let>),concat(<!>,<num>)}")))
(example (utterance "lines not containing a character , vowel or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<vow>),<!>)}")))
(example (utterance "items without a letter , leftquoatation ! rightquoatation , or a character .") (targetValue (name "?{or(or(<any>,<!>),<let>)}")))
(example (utterance "items with any letter or leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<cap>,<!>),<low>),<@>}")))
(example (utterance "lines with less than 2 of a capital before lower-case letter") (targetValue (name "?{concat(<cap>,repeatatleast(<low>,2))}")))
(example (utterance "lines that contain a letter , 2 times or more before a vowel") (targetValue (name "?{repeatatleast(<let>,2),<vow>}")))
(example (utterance "items with a vowel thrice or more preceding an upper case letter .") (targetValue (name "?{repeatatleast(<vow>,3),<cap>}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(<!>,<vow>),<any>}")))
(example (utterance "items with a numeral preceding leftquoatation ! rightquoatation .") (targetValue (name "?{<num>,<!>}")))
(example (utterance "leftquoatation ! rightquoatation should be included with words in the string .") (targetValue (name "?{<!>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before with or without a vowel or a letter") (targetValue (name "?{or(concat(<!>,<vow>),<let>)}")))
(example (utterance "lines starting in lower-case letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(startwith(<low>),<!>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before a letter") (targetValue (name "?{contain(endwith(<!>)),<let>}")))
(example (utterance "lines with words not being leftquoatation ! rightquoatation") (targetValue (name "?{not(<!>)}")))
(example (utterance "lines with a number before a capital letter") (targetValue (name "?{<num>,not(<low>)}")))
(example (utterance "leftquoatation ! rightquoatation string appears at least once in the lines") (targetValue (name "?{<!>}")))
(example (utterance "the lines containing a character and also leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{and(<any>,<!>),<let>}")))
(example (utterance "lines not having a lower-case letter which is followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines containing 4 or more numbers times before a vowel") (targetValue (name "?{repeatatleast(<num>,4),<vow>}")))
(example (utterance "lines containing neither a number nor a vowel .") (targetValue (name "?{and(contain(<num>),<vow>)}")))
(example (utterance "line with something before a capital letter 3 times") (targetValue (name "?{<any>,repeatatleast(<cap>,3)}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation preceding a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines which are containing a capital letter before a lower-case letter followed by a vowel") (targetValue (name "?{contain(<cap>),concat(<low>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a lower-case letter and then a character") (targetValue (name "?{<!>,concat(<low>,<any>)}")))
(example (utterance "lines that contain a character , letter , or 0 or more instances of leftquoatation ! rightquoatation") (targetValue (name "?{contain(or(or(<any>,<let>),<!>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation 6 times before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,6),endwith(<@>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation then a number") (targetValue (name "?{concat(<low>,<!>),concat(<@>,<num>)}")))
(example (utterance "lines with words with a capital followed by number , zero or more times") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation followed by lower-case letter") (targetValue (name "?{<cap>,concat(<!>,<low>)}")))
(example (utterance "6 or more lines with leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with no vowels") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "lines which are ending with leftquoatation ! rightquoatation , a lower-case letter , and leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{or(and(and(<!>,<low>),<@>),<#>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation after a vowel") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines with a vowel before a number and leftquoatation ! rightquoatation in them") (targetValue (name "?{<vow>,and(<num>,<!>)}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation before a capital letter or number") (targetValue (name "?{<!>,or(<cap>,<num>)}")))
(example (utterance "lines that have at least 7 combinations of a character followed by a capital letter") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a lower-case letter , then leftquoatation @ rightquoatation") (targetValue (name "?{concat(concat(<low>,<@>),<!>)}")))
(example (utterance "lines with number before string leftquoatation ! rightquoatation ,  string leftquoatation @ rightquoatation , and a capital") (targetValue (name "?{<num>,and(and(<!>,<@>),<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel , string leftquoatation @ rightquoatation , or capital letter") (targetValue (name "?{<!>,or(or(<vow>,<@>),<cap>)}")))
(example (utterance "lines with the string") (targetValue (name "?{<!>,contain(<cap>)}")))
(example (utterance "lines without a lower-case or capital letter at the beginning") (targetValue (name "?{or(startwith(<low>),<cap>)}")))
(example (utterance "lines with a caoital letter followed by a character at least once") (targetValue (name "?{repeatatleast(<any>,1),<cap>}")))
(example (utterance "the lines that have a capital letter , followed by a vowel , and then followed by leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,concat(<vow>,<!>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation at least once in them") (targetValue (name "?{startwith(or(<!>,<@>))}")))
(example (utterance "lines no having a capital letter") (targetValue (name "?{not(<cap>),startwith(<let>)}")))
(example (utterance "lines with a character , zero or more times and which are containing a lower-case letter") (targetValue (name "?{star(<any>),contain(<low>)}")))
(example (utterance "lines do not start with leftquoatation ! rightquoatation nor leftquoatation @ rightquoatation") (targetValue (name "?{and(startwith(<!>),<@>)}")))
(example (utterance "lines with vowel before a letter") (targetValue (name "?{<vow>,contain(<let>)}")))
(example (utterance "lines having character , vowel , and string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{and(and(<any>,<vow>),<!>),<low>}")))
(example (utterance "lines containing a number , leftquoatation ! rightquoatation or a lower-case letter in front of a capital letter") (targetValue (name "?{<any>,or(<cap>,<!>)}")))
(example (utterance "lines with a letter , a vowel , and a capital letter") (targetValue (name "?{and(and(<let>,<vow>),<cap>)}")))
(example (utterance "lines with 5 or more a characters followed by a lower-case letters") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines with a character at least 5 times before a number") (targetValue (name "?{repeatatleast(<any>,5),<num>}")))
(example (utterance "lines containing the words with a vowel , a character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<any>),<!>}")))
(example (utterance "lines that have either a letter or a capital letter") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines with a character or leftquoatation ! rightquoatation prior to a number") (targetValue (name "?{or(<any>,<!>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a lower-case and capital letter , before a vowel") (targetValue (name "?{and(and(<!>,<low>),<cap>),<vow>}")))
(example (utterance "lines without leftquoatation ! rightquoatation ending with string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,endwith(<@>))}")))
(example (utterance "lines with character preceding a vowel") (targetValue (name "?{concat(<any>,<vow>),star(<!>)}")))
(example (utterance "lines with words that do not contain a vowel at least 1 time") (targetValue (name "?{repeatatleast(<vow>,1)}")))
(example (utterance "lines containing 7 or more of leftquoatation ! rightquoatation or a character before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,7),concat(<any>,<@>)}")))
(example (utterance "lines with 4 or more of a capital letter before lower-case letter") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines containing a lowercase letter and number preceding a character") (targetValue (name "?{and(<low>,<num>),<any>}")))
(example (utterance "lines without a vowel , character , or capital letter") (targetValue (name "?{and(and(<vow>,<any>),<cap>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation before a vowel at least once in them") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{repeatatleast(<!>,1),<low>}")))
(example (utterance "lines that do not contain a number followed by a character") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines starting with a lower-case") (targetValue (name "?{contain(not(<low>))}")))
(example (utterance "lines starting with lower-case letter and string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<low>),or(<!>,<@>)}")))
(example (utterance "lines that contain a vowel or a lower-case letter , at least 7 times") (targetValue (name "?{repeatatleast(<low>,7),<vow>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation and a vowel must have a capital letter before them") (targetValue (name "?{<cap>,and(<vow>,<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation or an upper case letter or a numeral preceding leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<num>,<cap>),<!>),<@>}")))
(example (utterance "lines with or without a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{contain(and(<vow>,<!>))}")))
(example (utterance "items with words containing an upper case letter and numeral .") (targetValue (name "?{<num>,<cap>}")))
(example (utterance "lines with zero or more letters , not before a number") (targetValue (name "?{star(<let>),not(<num>)}")))
(example (utterance "lines that do not contain a lower-case letter followed by a vowel , at least 6 times") (targetValue (name "?{repeatatleast(concat(<low>,<vow>),6)}")))
(example (utterance "items with words containing a letter preceding a small letter .") (targetValue (name "?{<let>,<low>}")))
(example (utterance "lines with a number before a vowel before capital letter") (targetValue (name "?{concat(<num>,<vow>),<cap>}")))
(example (utterance "lines with a letter") (targetValue (name "?{and(and(<let>,<cap>),<vow>)}")))
(example (utterance "lines with or without leftquoatation ! rightquoatation , before a character or leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),or(<any>,<@>)}")))
(example (utterance "lines with words with zero or more of a lower-case before capital letter") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "the lines which contain a number followed by leftquoatation ! rightquoatation occuring at least once") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "at least 1 line ending in leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines with words starting with 4 or more lower-case letters") (targetValue (name "?{repeatatleast(<low>,4)}")))
(example (utterance "lines with no vowel or capital letter followed by a character") (targetValue (name "?{not(<vow>),concat(<cap>,<any>)}")))
(example (utterance "lines with words which are ending with a capital letter and leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<cap>),<!>}")))
(example (utterance "lines without a character , leftquoatation ! rightquoatation , or a number") (targetValue (name "?{contain(and(and(<any>,<!>),<num>))}")))
(example (utterance "lines that have leftquoatation ! rightquoatation in them") (targetValue (name "?{<!>}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation positioned before a capital letter , a lower-case letter , or leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<cap>,<@>),<low>)}")))
(example (utterance "lines starting in 7 or more of a character before vowel") (targetValue (name "?{startwith(concat(<any>,<vow>))}")))
(example (utterance "lines with a letter before a lower-case letter") (targetValue (name "?{<let>,contain(not(<low>))}")))
(example (utterance "the lines end with a letter and leftquoatation ! rightquoatation .") (targetValue (name "?{contain(and(<let>,<!>))}")))
(example (utterance "lines with an uppercase or lowercase letter or a vowel") (targetValue (name "?{or(or(<cap>,<low>),<vow>)}")))
(example (utterance "lines without a number or letter") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines with words ending with characters") (targetValue (name "?{<any>}")))
(example (utterance "lines without a vowel or ending in string leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,endwith(<!>))}")))
(example (utterance "lines without a letter , 6 or more times") (targetValue (name "?{repeatatleast(<let>,6)}")))
(example (utterance "lines containing either a character , string leftquoatation ! rightquoatation , or string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<any>,<!>),<@>)}")))
(example (utterance "the lines start with a number and contain a vowel .") (targetValue (name "?{startwith(<num>),contain(<vow>)}")))
(example (utterance "items with a character at least 4 times preceding an uppercase letter .") (targetValue (name "?{repeatatleast(<any>,4),<cap>}")))
(example (utterance "lines with number followed by capital letter before lower-case letter") (targetValue (name "?{concat(<num>,<cap>),<low>}")))
(example (utterance "lines that start with words and no character") (targetValue (name "?{not(<any>)}")))
(example (utterance "lines without string leftquoatation ! rightquoatation or vowel") (targetValue (name "?{or(contain(<!>),<vow>)}")))
(example (utterance "lines containing only a number and a character") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines with either leftquoatation ! rightquoatation or leftquoatation @ rightquoatation and any capital letter") (targetValue (name "?{startwith(<!>),and(<@>,<cap>)}")))
(example (utterance "items with a numeral or any letter .") (targetValue (name "?{or(<num>,<low>),<cap>}")))
(example (utterance "lines with words and 2 or more of strings leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{repeatatleast(<!>,2),<cap>}")))
(example (utterance "lines with a vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines with a capital letter and leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{contain(<cap>),or(<!>,<low>)}")))
(example (utterance "lines with either a capital letter or leftquoatation ! rightquoatation followed by a number , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{or(<cap>,<!>),and(and(<num>,<@>),<#>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , lower-case letter , or vowel") (targetValue (name "?{and(and(<!>,<low>),<vow>)}")))
(example (utterance "lines with a character before a non lower-case letter") (targetValue (name "?{<any>,not(<low>)}")))
(example (utterance "lines with 4 or more of th string leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines with number at least once before a capital") (targetValue (name "?{repeatatleast(<num>,1),<cap>}")))
(example (utterance "items without words containing leftquoatation ! rightquoatation .") (targetValue (name "?{<!>}")))
(example (utterance "items with leftquoatation ! rightquoatation , a numeral , and an upper case letter .") (targetValue (name "?{and(and(<cap>,<!>),<num>)}")))
(example (utterance "lines containing 3 or more of leftquoatation ! rightquoatation at the end") (targetValue (name "?{contain(repeatatleast(<!>,3))}")))
(example (utterance "items without at least once a character .") (targetValue (name "?{repeatatleast(<any>,1)}")))
(example (utterance "lines which are starting with leftquoatation ! rightquoatation and a lower-case letter or leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),or(<low>,<@>)}")))
(example (utterance "lines containing a lower case-letter followed by leftquoatation ! rightquoatation , 7 or more times") (targetValue (name "?{<low>,repeatatleast(<!>,7)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , but not 1 or more characters") (targetValue (name "?{or(<!>,not(<any>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a lower-case letter before leftquoatation @ rightquoatation in them") (targetValue (name "?{contain(concat(<!>,<low>)),<@>}")))
(example (utterance "lines containing words without a vowel") (targetValue (name "?{not(<vow>)}")))
(example (utterance "lines with words with a character or string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{<any>,concat(<!>,<@>)}")))
(example (utterance "items with an upper case letter preceding a character preceding a small letter .") (targetValue (name "?{concat(<cap>,<any>),<low>}")))
(example (utterance "the lines must have a vowel and a letter before a number .") (targetValue (name "?{and(<vow>,<let>),<num>}")))
(example (utterance "lines that contain only a number") (targetValue (name "?{<num>}")))
(example (utterance "lines with the string 'dog that end with a letter before it at least 3 times") (targetValue (name "?{endwith(concat(<let>,<!>))}")))
(example (utterance "lines that have 3 things before a vowel: a character , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<any>,<!>),<@>),<vow>}")))
(example (utterance "lines ending in a character before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or number") (targetValue (name "?{endwith(<any>),or(or(<!>,<num>),<@>)}")))
(example (utterance "lines with a vowel before either leftquoatation ! rightquoatation , capital letter and string leftquoatation @ rightquoatation") (targetValue (name "?{<vow>,or(or(<!>,<cap>),<@>)}")))
(example (utterance "zero lines with a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{<low>,star(<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , 2 or more times followed by a letter") (targetValue (name "?{repeatatleast(<!>,2),<let>}")))
(example (utterance "items terminating with leftquoatation ! rightquoatation at least twice .") (targetValue (name "?{repeatatleast(<!>,2)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after leftquoatation @ rightquoatation , leftquoatation # rightquoatation , and a number") (targetValue (name "?{and(and(<@>,<#>),<num>),<!>}")))
(example (utterance "lines starting in leftquoatation ! rightquoatation before leftquoatation @ rightquoatation then lower-case letter") (targetValue (name "?{startwith(<!>),concat(<@>,<low>)}")))
(example (utterance "lines which are having leftquoatation ! rightquoatation , a number , and a capital letter before a character") (targetValue (name "?{and(and(<!>,<num>),<cap>),<any>}")))
(example (utterance "lines containing words with a capital before number") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "items with a small letter and numeral preceding a letter .") (targetValue (name "?{and(<low>,<num>),<let>}")))
(example (utterance "lines with a vowel , then a letter , a capital letter , and then leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,and(and(<let>,<cap>),<!>)}")))
(example (utterance "lines without only leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines having a lower-case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation preceding a character") (targetValue (name "?{or(or(<low>,<!>),<@>),<any>}")))
(example (utterance "lines with words and a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<cap>),<!>)}")))
(example (utterance "lines which are not having a character , not a lower-case letter , and not a letter") (targetValue (name "?{and(and(<any>,<low>),<let>)}")))
(example (utterance "lines with words with 2 or more of leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a number , or a vowel") (targetValue (name "?{<!>,or(or(<@>,<num>),<vow>)}")))
(example (utterance "lines starting in words with a number and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<!>)}")))
(example (utterance "lines with a number before a character at least zero times") (targetValue (name "?{star(<num>),<any>}")))
(example (utterance "lines that don't have  a character 4 or more times") (targetValue (name "?{contain(repeatatleast(<any>,4))}")))
(example (utterance "lines containing capital letter , a number , and a letter") (targetValue (name "?{and(and(<cap>,<num>),<let>)}")))
(example (utterance "lines with a lower-case letter followed by leftquoatation ! rightquoatation then a capital letter") (targetValue (name "?{<low>,concat(<!>,<cap>)}")))
(example (utterance "lines containing a letter followed by a lower-case letter , the string'dog' , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<!>,<!>),<let>),<low>}")))
(example (utterance "lines with a character , capital letter , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<cap>),<!>)}")))
(example (utterance "lines that have a capital letter followed by a lower-case letter , then a number") (targetValue (name "?{<cap>,concat(<low>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and 3 or more letters or characters") (targetValue (name "?{or(and(<!>,<let>),<any>)}")))
(example (utterance "lines with words and a letter and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<cap>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before words ending in string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,endwith(<@>)}")))
(example (utterance "items with a letter or a numeral .") (targetValue (name "?{or(<low>,<let>),<num>}")))
(example (utterance "lines containing a number , a lower-case letter , and a capital letter") (targetValue (name "?{<num>,concat(<low>,<cap>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , a capital , and vowel") (targetValue (name "?{and(and(<!>,<cap>),<vow>)}")))
(example (utterance "lines do not contain a lower-case letter nor a character") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , a vowel , or leftquoatation @ rightquoatation followed by a number") (targetValue (name "?{or(or(<!>,<vow>),<@>),<num>}")))
(example (utterance "lines starting with a capital letter") (targetValue (name "?{<cap>,or(<vow>,<num>)}")))
(example (utterance "lines which are not starting with a number , 3 or more times") (targetValue (name "?{repeatatleast(startwith(<num>),3)}")))
(example (utterance "lines that have at least 6 characters") (targetValue (name "?{<any>}")))
(example (utterance "lines having a lower-case letter , a character and string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<any>),<!>),<let>}")))
(example (utterance "lines beginning at least 1 time with a character") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines that have words ending with a letter before a lower-case letter") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines with a character 4 times or a number") (targetValue (name "?{repeatatleast(<any>,4),<num>}")))
(example (utterance "lines having words with a number , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(<num>,<!>),<@>}")))
(example (utterance "items with words containing a vowel , leftquoatation ! rightquoatation and a character .") (targetValue (name "?{and(<vow>,<!>),<any>}")))
(example (utterance "lines that have either leftquoatation ! rightquoatation , a lower-case letter , or a number") (targetValue (name "?{or(or(<low>,<!>),<num>)}")))
(example (utterance "lines starting with a number before string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<num>),contain(<!>)}")))
(example (utterance "lines containing a lower-case letter at least once") (targetValue (name "?{repeatatleast(concat(<low>,<cap>),1)}")))
(example (utterance "lines that have the leftquoatation ! rightquoatation string or an upper-case letter occurring 5 times or greater") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines that begin with the word leftquoatation ! rightquoatation or with a character") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines that the the word leftquoatation ! rightquoatation followed by anything but a character") (targetValue (name "?{<!>,not(<any>)}")))
(example (utterance "lines with a vowel and string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or a capital letter") (targetValue (name "?{and(<vow>,<!>),or(<@>,<cap>)}")))
(example (utterance "lines without a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<!>),<low>)}")))
(example (utterance "lines without a letter , a capital letter , or the word leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<cap>),<!>)}")))
(example (utterance "not ending with a character at least 2 times") (targetValue (name "?{repeatatleast(endwith(<any>),2)}")))
(example (utterance "lines not containing only a number and not a character") (targetValue (name "?{and(<num>,<any>)}")))
(example (utterance "lines containing a letter followed by leftquoatation ! rightquoatation , a number , as well as leftquoatation @ rightquoatation") (targetValue (name "?{<let>,and(and(<!>,<num>),<@>)}")))
(example (utterance "lines with characters or numbers") (targetValue (name "?{or(<any>,<num>)}")))
(example (utterance "lines containing vowel before a capital or string leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<vow>,<cap>),<!>)}")))
(example (utterance "lines finishing with only a capital letter at least once") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines with a letter , leftquoatation ! rightquoatation , or a lower-case letter") (targetValue (name "?{contain(or(or(<let>,<!>),<low>))}")))
(example (utterance "lines with at least 4 numbers or characters") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding an upper case letter and a letter and a numeral .") (targetValue (name "?{<!>,and(and(<cap>,<num>),<let>)}")))
(example (utterance "lines with a lower-case letter after leftquoatation ! rightquoatation followed by a character") (targetValue (name "?{concat(<!>,<any>),<low>}")))
(example (utterance "sentence: lines containing a lower-case letter and a capital letter at least once") (targetValue (name "?{and(<low>,repeatatleast(<cap>,1))}")))
(example (utterance "lines that end with the word leftquoatation ! rightquoatation or a letter") (targetValue (name "?{or(contain(<!>),<let>)}")))
(example (utterance "lines with words with a vowel and string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a letter") (targetValue (name "?{<vow>,or(or(<!>,<@>),<let>)}")))
(example (utterance "lines with the string dog , truck , and ring before a letter") (targetValue (name "?{<!>,and(and(<let>,<@>),<#>)}")))
(example (utterance "lines containing a letter or a number and a capital letter") (targetValue (name "?{or(<let>,<num>),<cap>}")))
(example (utterance "lines with words and a letter , character , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<any>),<!>}")))
(example (utterance "lines without words having a vowel before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines with letter , 7 or more times before number") (targetValue (name "?{repeatatleast(<let>,7),<num>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation before letter") (targetValue (name "?{not(<!>),concat(<@>,<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation with string leftquoatation @ rightquoatation and leftquoatation # rightquoatation and a letter") (targetValue (name "?{not(<!>),and(and(<@>,<let>),<#>)}")))
(example (utterance "lines with only 7 or more characters") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines with a number followed by leftquoatation ! rightquoatation or leftquoatation @ rightquoatation 5 or more times") (targetValue (name "?{concat(<num>,<!>),repeatatleast(<@>,5)}")))
(example (utterance "items with a letter or a character at least 1 time .") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "items with a letter and leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{and(and(<let>,<low>),<!>),<any>}")))
(example (utterance "lines with a number before a character then capital letter") (targetValue (name "?{concat(<num>,<any>),<cap>}")))
(example (utterance "lines with a vowel before a number , character , or string leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,or(or(<num>,<any>),<!>)}")))
(example (utterance "lines having a letter before leftquoatation ! rightquoatation , at least5times") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or without a vowel") (targetValue (name "?{repeatatleast(<!>,1),not(<vow>)}")))
(example (utterance "lines having a number , lower-case letter , and a vowel") (targetValue (name "?{and(and(<num>,<low>),<vow>),<cap>}")))
(example (utterance "lines not ending with vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(or(<vow>,<!>))}")))
(example (utterance "lines containing at least 7 vowels or characters") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines which only use lower-case letters") (targetValue (name "?{<low>}")))
(example (utterance "lines without zero or more vowels before leftquoatation ! rightquoatation") (targetValue (name "?{concat(star(<vow>),<!>)}")))
(example (utterance "lines not containing only leftquoatation ! rightquoatation at least once in them") (targetValue (name "?{repeatatleast(<!>,1)}")))
(example (utterance "lines which are having either a vowel , leftquoatation ! rightquoatation , or a lower-case letter before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<low>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a leftquoatation @ rightquoatation , capital letter , and number") (targetValue (name "?{<!>,and(and(<@>,<cap>),<num>)}")))
(example (utterance "lines with a vowel , characater , or letter") (targetValue (name "?{or(or(<vow>,<any>),<let>)}")))
(example (utterance "lines with words and a lower-case letter , string leftquoatation ! rightquoatation , and character") (targetValue (name "?{and(<low>,<!>),<any>}")))
(example (utterance "lines ending in words with string leftquoatation ! rightquoatation and a number") (targetValue (name "?{and(<!>,<num>)}")))
(example (utterance "lines ending with a vowel before a letter") (targetValue (name "?{contain(<let>),endwith(<vow>)}")))
(example (utterance "lines beginning with a vowel 1 or more times") (targetValue (name "?{repeatatleast(<vow>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , zero or more times and a lower-case letter followed by a vowel in them") (targetValue (name "?{star(<!>),concat(<low>,<vow>)}")))
(example (utterance "lines beginning with or without a character and a lower-case letter") (targetValue (name "?{startwith(<any>),star(<low>)}")))
(example (utterance "lines containing containing only character , 6 or more times") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation preceded by a letter , a number , and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<let>,<num>),<@>),<!>}")))
(example (utterance "lines ending in words with vowel") (targetValue (name "?{repeatatleast(<vow>,1)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a letter") (targetValue (name "?{or(<!>,<@>),<let>}")))
(example (utterance "lines with a character , string leftquoatation ! rightquoatation , and vowel before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<any>,<!>),<vow>),<@>}")))
(example (utterance "lines that contain a character followed by a capital letter and leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<cap>),<!>}")))
(example (utterance "items with a vowel and a numeral preceding a character .") (targetValue (name "?{and(<num>,<vow>),<any>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation then capital letter") (targetValue (name "?{concat(<num>,<!>),<cap>}")))
(example (utterance "lines with a capital before vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,or(<vow>,<!>)}")))
(example (utterance "items without a small letter , thrice at least .") (targetValue (name "?{repeatatleast(<low>,3)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation prior to leftquoatation @ rightquoatation") (targetValue (name "?{<!>,contain(<@>)}")))
(example (utterance "lines ending with a capital letter") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines starting in a character before a vowel") (targetValue (name "?{startwith(<any>),repeatatleast(<vow>,1)}")))
(example (utterance "lines with a number and letter before character") (targetValue (name "?{and(<num>,<let>),<any>}")))
(example (utterance "lines containing either a number , a vowel , or a capital letter") (targetValue (name "?{or(<num>,<vow>),<cap>}")))
(example (utterance "lines with words and either a letter , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(<let>,<!>),<any>}")))
(example (utterance "lines with words containing a letter") (targetValue (name "?{repeatatleast(<let>,1)}")))
(example (utterance "lines with a vowel or characters before leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<any>),contain(<!>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation before a vowel or letter , and a number") (targetValue (name "?{<!>,and(and(<vow>,<let>),<num>)}")))
(example (utterance "lines that begin with a letter , at least 7 times") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "lines containing words having a character a letter and a number .") (targetValue (name "?{and(<any>,<let>),<num>}")))
(example (utterance "items with a numeral , a letter , or a vowel .") (targetValue (name "?{or(or(<vow>,<let>),<num>)}")))
(example (utterance "lines with vowel , zero or more times before containing string leftquoatation ! rightquoatation") (targetValue (name "?{star(<vow>),contain(<!>)}")))
(example (utterance "lines containing a letter followed by leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{<let>,or(<!>,<low>)}")))
(example (utterance "character after a letter") (targetValue (name "?{<let>,<any>}")))
(example (utterance "lines that have words beginning with the word leftquoatation ! rightquoatation preceding a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with a capital letter before words starting in string leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,startwith(<!>)}")))
(example (utterance "lines that do not have words with the word leftquoatation ! rightquoatation preceding a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with a lower case letter preceded by leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>),<low>}")))
(example (utterance "lines that don't have a number , character , or lower case letters .") (targetValue (name "?{or(or(<low>,<num>),<any>)}")))
(example (utterance "lines with lower-case letter and  letter before a capital") (targetValue (name "?{and(<low>,<let>),<cap>}")))
(example (utterance "lines not having a vowel , a letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<cap>),<!>)}")))
(example (utterance "lines with a letter containing only a capital letter at least once") (targetValue (name "?{<let>,repeatatleast(<cap>,1)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and leftquoatation @ rightquoatation , or before leftquoatation # rightquoatation .") (targetValue (name "?{<!>,or(<#>,<@>)}")))
(example (utterance "items with 'dog , a numeral , and an upper case letter .") (targetValue (name "?{and(and(<num>,<!>),<cap>)}")))
(example (utterance "lines with a number , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation after a lower-case letter") (targetValue (name "?{<low>,and(and(<num>,<!>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a character at least 2 times") (targetValue (name "?{repeatatleast(<any>,2),<!>}")))
(example (utterance "lines that have a character after a fowel or a lower-case letter") (targetValue (name "?{or(<vow>,<low>),<any>}")))
(example (utterance "lines ending with words with character or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "items with a letter preceding a numeral preceding an upper case letter .") (targetValue (name "?{concat(<let>,<num>),<cap>}")))
(example (utterance "items without an upper case letter , not a letter nor a vowel .") (targetValue (name "?{and(and(<cap>,<vow>),<let>)}")))
(example (utterance "lines not ending in string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(endwith(<!>),<@>)}")))
(example (utterance "lines without only leftquoatation ! rightquoatation followed by a character") (targetValue (name "?{not(<!>),<any>}")))
(example (utterance "lines containing words without a vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or a number and leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),and(<num>,<#>)}")))
(example (utterance "the lines that have the words and contain leftquoatation ! rightquoatation at least 5 times") (targetValue (name "?{repeatatleast(<!>,5)}")))
(example (utterance "lines starting in a capital or lower-case letter") (targetValue (name "?{startwith(or(<cap>,<low>))}")))
(example (utterance "lines that contain a character or a capital letter , at least 7 times") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "lines not having letter followed by string leftquoatation ! rightquoatation before capital letter") (targetValue (name "?{concat(concat(<let>,<!>),<cap>)}")))
(example (utterance "items with a small letter preceding a vowel preceding a letter .") (targetValue (name "?{<low>,concat(<vow>,<let>)}")))
(example (utterance "lines not having a lower-case letter , zero or more times before a number in it") (targetValue (name "?{concat(star(<low>),<num>)}")))
(example (utterance "lines with only a lower case letter") (targetValue (name "?{and(<low>,<any>)}")))
(example (utterance "lines with a lower-case letter , zero times") (targetValue (name "?{star(<low>),not(<let>)}")))
(example (utterance "lines with a vowel following a number") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "items with a small letter preceding leftquoatation ! rightquoatation at least 4 tmes .") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines with at least 6 numbers or vowels") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines having words beginning with a lower-case letter coming twice or more") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a number , 5 or more times in it") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines with words and a capital letter before vowel and character") (targetValue (name "?{concat(<cap>,<vow>),<any>}")))
(example (utterance "lines with vowel before character , 6 or more times") (targetValue (name "?{<vow>,repeatatleast(<any>,6)}")))
(example (utterance "lines starting with words and leftquoatation ! rightquoatation with a lower-case letter") (targetValue (name "?{and(<!>,<low>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation or string leftquoatation # rightquoatation") (targetValue (name "?{or(concat(<!>,<@>),<#>)}")))
(example (utterance "lines containing a lower-case letter before 7 or more capital letters") (targetValue (name "?{<low>,repeatatleast(<cap>,7)}")))
(example (utterance "items without a vowel or leftquoatation ! rightquoatation or small letter .") (targetValue (name "?{or(or(<vow>,<!>),<low>)}")))
(example (utterance "items with a character preceding leftquoatation ! rightquoatation preceding a small letter .") (targetValue (name "?{<any>,concat(<!>,<low>)}")))
(example (utterance "lines not containing 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{contain(not(<!>))}")))
(example (utterance "lines end with a capital letter") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines starting with a letter coming before leftquoatation ! rightquoatation and a vowel") (targetValue (name "?{startwith(<let>),and(<!>,<vow>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or lower-case and capital letters and string leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),and(and(<low>,<cap>),<@>)}")))
(example (utterance "lines with a lower-case letter before vowel followed by character") (targetValue (name "?{<low>,concat(<vow>,<any>)}")))
(example (utterance "lines that contain a number , vowel , and the word leftquoatation ! rightquoatation followed by a capital letter") (targetValue (name "?{and(and(<num>,<vow>),<!>),<cap>}")))
(example (utterance "lines not ending with a lower-case letter") (targetValue (name "?{endwith(contain(<low>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation or vowel before a letter") (targetValue (name "?{contain(<!>),concat(<vow>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a vowel or string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),or(<vow>,<@>)}")))
(example (utterance "lines without vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines with 2 or more numbers") (targetValue (name "?{<num>}")))
(example (utterance "lines without leftquoatation ! rightquoatation , vowel , or letter") (targetValue (name "?{or(or(<!>,<vow>),<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{<!>,contain(<any>)}")))
(example (utterance "lines with words with number before character , 2 or more times") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines containing lower-case and a letter") (targetValue (name "?{and(<low>,<let>)}")))
(example (utterance "lines with lower-case letter , character , and number") (targetValue (name "?{and(and(<low>,<any>),<num>)}")))
(example (utterance "items without a small letter , character or vowel .") (targetValue (name "?{and(and(<low>,<any>),<vow>)}")))
(example (utterance "lines starting in a character ending in letter") (targetValue (name "?{startwith(<any>),endwith(<let>)}")))
(example (utterance "lines that have a capital letter followed by a character") (targetValue (name "?{<any>,<cap>}")))
(example (utterance "lines that contain a character or a number and leftquoatation ! rightquoatation , zero times or more") (targetValue (name "?{or(<any>,and(<num>,<!>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a capital over 3 times") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines with 5 or more of words with a vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines containing 3 numbers or 3 letters") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "items with either a letter or a numeral .") (targetValue (name "?{or(or(<let>,<num>),<low>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation and vowel or a capital") (targetValue (name "?{<!>,or(<vow>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation at least once in them") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with 7 or more characters followed by a lower-case") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines without not having a character with string leftquoatation ! rightquoatation") (targetValue (name "?{not(<any>),contain(<!>)}")))
(example (utterance "lines containing a vowel before a number preceding leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,concat(<num>,<!>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation , lower-case letter , and character") (targetValue (name "?{and(<!>,<low>),<any>}")))
(example (utterance "lines with words without string leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{and(<!>,<cap>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , a vowel or a capital letter") (targetValue (name "?{or(or(<cap>,<vow>),<!>),<@>}")))
(example (utterance "lines containing a letter followed by a capital letter and leftquoatation ! rightquoatation") (targetValue (name "?{<let>,and(<cap>,<!>)}")))
(example (utterance "lines ending with vowel or capital letter , 4 or more times") (targetValue (name "?{repeatatleast(or(<vow>,<cap>),4)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before 2 or more characters") (targetValue (name "?{<!>,repeatatleast(<any>,2)}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a letter") (targetValue (name "?{endwith(<!>),concat(<@>,<let>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation or lower-case letter at least once") (targetValue (name "?{repeatatleast(or(<!>,<low>),1)}")))
(example (utterance "lines which are starting with leftquoatation ! rightquoatation and not a letter") (targetValue (name "?{startwith(<!>),not(<let>)}")))
(example (utterance "lines with 2 or more numbers before a lower-case letter or string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,2),or(<low>,<!>)}")))
(example (utterance "lines containing 5 or more of leftquoatation ! rightquoatation followed by a vowel") (targetValue (name "?{repeatatleast(<!>,5),<vow>}")))
(example (utterance "lines with 3 or more character before a number") (targetValue (name "?{repeatatleast(<any>,3),<num>}")))
(example (utterance "lines having a letter , character , or lower-case letter") (targetValue (name "?{or(or(<let>,<any>),<low>)}")))
(example (utterance "lines containing words with a letter , a character , or a lower-case letter") (targetValue (name "?{or(<let>,<any>),<low>}")))
(example (utterance "lines ending in and with only 5 or more vowels") (targetValue (name "?{repeatatleast(<vow>,5)}")))
(example (utterance "lines with a vowel followed by a capital letter , 6 or more times in them") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "items with words containing a small letter preceding a character .") (targetValue (name "?{<low>,<any>}")))
(example (utterance "lines with a character and capital letter before a number") (targetValue (name "?{and(<any>,<cap>),contain(<num>)}")))
(example (utterance "lines with a letter or string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{or(<let>,<!>),<low>}")))
(example (utterance "lines containing a letter after a vowel , a character , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<any>),<!>),<let>}")))
(example (utterance "lines not having leftquoatation ! rightquoatation before either vowel , leftquoatation @ rightquoatation , or a character") (targetValue (name "?{not(<!>),or(or(<vow>,<@>),<any>)}")))
(example (utterance "lines containing only a lower-case or a number") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines with a number after a capital letter at least zero times") (targetValue (name "?{<cap>,star(<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before vowel and lower-case letter or string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<vow>),or(<low>,<@>)}")))
(example (utterance "lines which are starting with a number or a lower-case letter") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a number") (targetValue (name "?{and(and(<@>,<!>),<num>)}")))
(example (utterance "lines that contain words at lease 1 letter and a lower-case letter") (targetValue (name "?{repeatatleast(<let>,1),<low>}")))
(example (utterance "lines without leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation and leftquoatation # rightquoatation and a number") (targetValue (name "?{not(<!>),and(and(<@>,<num>),<#>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{star(concat(<!>,<@>))}")))
(example (utterance "lines with a letter") (targetValue (name "?{repeatatleast(<low>,1),contain(<cap>)}")))
(example (utterance "lines with a number before a lower-case letter") (targetValue (name "?{<num>,contain(<low>)}")))
(example (utterance "lines without having leftquoatation ! rightquoatation or leftquoatation @ rightquoatation and a character") (targetValue (name "?{not(<!>),and(<@>,<any>)}")))
(example (utterance "items with words containing a character and a vowel .") (targetValue (name "?{<vow>,<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation after a capital letter") (targetValue (name "?{<cap>,<!>}")))
(example (utterance "lines with a vowel at the end") (targetValue (name "?{<vow>}")))
(example (utterance "lines which have leftquoatation ! rightquoatation before words with starting with leftquoatation @ rightquoatation") (targetValue (name "?{<!>,startwith(<@>)}")))
(example (utterance "lines containing words but without leftquoatation ! rightquoatation or lower-case letters") (targetValue (name "?{and(<!>,<low>)}")))
(example (utterance "lines not starting with a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(or(<let>,<!>))}")))
(example (utterance "lines with capital letter before leftquoatation ! rightquoatation or a number") (targetValue (name "?{and(<let>,<cap>),or(<!>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a lower-case letter , or a letter after a character") (targetValue (name "?{<any>,or(or(<!>,<low>),<let>)}")))
(example (utterance "items with words containing any letter or a character .") (targetValue (name "?{and(<any>,<low>),<let>}")))
(example (utterance "lines with zero or more of a lower-case letter and number") (targetValue (name "?{contain(<low>),star(<num>)}")))
(example (utterance "lines with 5 or more capital letters before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<cap>,5),<!>}")))
(example (utterance "lines having a capital letter before a number following a lower-case letter") (targetValue (name "?{concat(<cap>,<num>),<low>}")))
(example (utterance "items with a numeral preceding a small letter preceding a vowel .") (targetValue (name "?{<num>,concat(<low>,<vow>)}")))
(example (utterance "lines starting with words and leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines that have a character or leftquoatation ! rightquoatation occurring at least twice") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines with a lower-case letter before vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<low>),or(<vow>,<!>)}")))
(example (utterance "line not having leftquoatation ! rightquoatation or leftquoatation @ rightquoatation zero times") (targetValue (name "?{star(or(<!>,<@>))}")))
(example (utterance "lines not having leftquoatation ! rightquoatation , a capital letter") (targetValue (name "?{and(and(<!>,<cap>),<let>)}")))
(example (utterance "lines that have a character before a lower-case letter") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines which have a lower-case letter or leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or capital letter before a vowel") (targetValue (name "?{or(<!>,<cap>),<vow>}")))
(example (utterance "lines with 5 or more of a number before string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,repeatatleast(<!>,5)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel or string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,repeatatleast(or(<vow>,<@>),1)}")))
(example (utterance "lines not containing a lower case letter , and uppercase latter or a vowel") (targetValue (name "?{and(and(<low>,<cap>),<vow>)}")))
(example (utterance "character lines with words or a number , zero or more times .") (targetValue (name "?{star(<any>),<num>}")))
(example (utterance "items with a character preceding a vowel preceding leftquoatation ! rightquoatation .") (targetValue (name "?{<any>,concat(<vow>,<!>)}")))
(example (utterance "lines with a capital letter or a lower-case letter") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a number , or a vowel preceding leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<num>),<vow>),<@>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , zero or more times and starting with a capital") (targetValue (name "?{star(<!>),startwith(<cap>)}")))
(example (utterance "lines with 2 or more of a string leftquoatation ! rightquoatation ending with a character") (targetValue (name "?{<!>,endwith(repeatatleast(<any>,2))}")))
(example (utterance "lines with leftquoatation ! rightquoatation after leftquoatation @ rightquoatation , a character , or a letter") (targetValue (name "?{or(or(<@>,<any>),<let>),<!>}")))
(example (utterance "lines with a number before a character , leftquoatation ! rightquoatation , or letter") (targetValue (name "?{<num>,and(and(<any>,<!>),<let>)}")))
(example (utterance "lines that must not end with the word leftquoatation ! rightquoatation at least5times") (targetValue (name "?{endwith(repeatatleast(<!>,5))}")))
(example (utterance "lines with words with character or string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with words and zero or more of leftquoatation ! rightquoatation and a letter") (targetValue (name "?{star(<!>),<let>}")))
(example (utterance "lines in which a lower-case letter or leftquoatation ! rightquoatation come after leftquoatation @ rightquoatation") (targetValue (name "?{<@>,or(<low>,<!>)}")))
(example (utterance "lines with a letter or vowel before leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<vow>),<!>}")))
(example (utterance "lines with a lower-case letter and string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<let>),<!>)}")))
(example (utterance "items with a vowel or numeral at least 7 times .") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation at least 5 times") (targetValue (name "?{repeatatleast(<!>,5)}")))
(example (utterance "lines with 7 or more of leftquoatation ! rightquoatation before number") (targetValue (name "?{<!>,repeatatleast(<num>,7)}")))
(example (utterance "lines with 'dog ; then a vowel , then a lower cased letter") (targetValue (name "?{concat(<low>,<!>),<vow>}")))
(example (utterance "lines without zero or more of a letter") (targetValue (name "?{contain(star(<let>))}")))
(example (utterance "lines with a vowel coming after a character at least 5 times") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "lines which have leftquoatation ! rightquoatation and a lower-case letter before ending with a vowel") (targetValue (name "?{and(<!>,<low>),endwith(<vow>)}")))
(example (utterance "lines with zero or more of a letter or character") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines that contain a character , the word leftquoatation ! rightquoatation , and a number preceding a lower-case letter") (targetValue (name "?{and(and(<any>,<!>),<num>),<low>}")))
(example (utterance "lines that begin with a capital letter or a vowel plus a number") (targetValue (name "?{startwith(<cap>),and(<vow>,<num>)}")))
(example (utterance "lines with a vowel , a letter , and a numeral before a lower case character") (targetValue (name "?{and(and(<vow>,<let>),<num>),<low>}")))
(example (utterance "lines without words , a number , a vowel , or a letter .") (targetValue (name "?{or(or(<num>,<vow>),<let>)}")))
(example (utterance "lines ending with containing either string leftquoatation ! rightquoatation , a character , or capital letter") (targetValue (name "?{contain(or(or(<!>,<any>),<cap>))}")))
(example (utterance "lines with letter or string leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines containing 3 or more letters followed by leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,3),<!>}")))
(example (utterance "lines with words that have a vowel before the word leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{concat(<vow>,<!>),<low>}")))
(example (utterance "line with at least 1 vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines containing 4 or more times a vowel before leftquoatation ! rightquoatation , a character and leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<vow>,4),and(and(<!>,<any>),<@>)}")))
(example (utterance "lines with a containing a character zero times") (targetValue (name "?{<cap>,star(contain(<any>))}")))
(example (utterance "lines having a character before a number or string leftquoatation ! rightquoatation at least 3 times") (targetValue (name "?{or(concat(<any>,<num>),<!>)}")))
(example (utterance "lines ending with capital letter or character") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<let>),<!>}")))
(example (utterance "lines with words ending with string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),<@>}")))
(example (utterance "lines which are ending with a lower-case letter or a vowel , 7 or more times") (targetValue (name "?{endwith(or(<low>,<vow>))}")))
(example (utterance "lines with a letter , a lower-case , a capital letter , 2 times") (targetValue (name "?{repeatatleast(<let>,2),concat(<low>,<cap>)}")))
(example (utterance "the lines that have either a number or a vowel") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines without words or characters and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<!>,<any>)}")))
(example (utterance "lines with character or letter , 5 or more times") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation , a vowel , as well as a lower-case letter") (targetValue (name "?{<!>,and(and(<@>,<vow>),<low>)}")))
(example (utterance "lines having a vowel , number , and capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<num>),<cap>),<!>}")))
(example (utterance "lines that do not have a lower case letter or do not end with a number") (targetValue (name "?{not(<low>),endwith(<num>)}")))
(example (utterance "lines not having letter , not leftquoatation ! rightquoatation , and not leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<let>,<!>),<@>)}")))
(example (utterance "lines having either a vowel , string leftquoatation ! rightquoatation , or number before a capital") (targetValue (name "?{or(or(<vow>,<!>),<num>),<cap>}")))
(example (utterance "lines containing a number , leftquoatation ! rightquoatation and a lower case letter before string leftquoatation @ rightquoatation") (targetValue (name "?{<low>,and(and(<@>,<num>),<!>)}")))
(example (utterance "lines with 5 or more numbers before a character") (targetValue (name "?{<num>,repeatatleast(<any>,5)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number , string leftquoatation @ rightquoatation , and capital letter") (targetValue (name "?{<!>,and(and(<num>,<@>),<cap>)}")))
(example (utterance "lines ending in 4 or more of leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{endwith(concat(<!>,<low>))}")))
(example (utterance "lines without leftquoatation ! rightquoatation , a vowel , or capital letter") (targetValue (name "?{not(or(or(<!>,<vow>),<cap>))}")))
(example (utterance "lines starting with words and a capital and lower case-letter") (targetValue (name "?{and(<cap>,<low>)}")))
(example (utterance "lines which contain a number and a lower-case letter followed by a character") (targetValue (name "?{<any>,and(<num>,<low>)}")))
(example (utterance "lines not having vowel , 3 or more times") (targetValue (name "?{repeatatleast(<vow>,3)}")))
(example (utterance "lines that end with a number") (targetValue (name "?{<num>}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a character preceding a vowel .") (targetValue (name "?{and(and(<!>,<@>),<any>),<vow>}")))
(example (utterance "lines with a capital letter , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<cap>,<!>),<@>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or 7 or more of leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines ending with a number after a vowel") (targetValue (name "?{endwith(<vow>),repeatatleast(<num>,1)}")))
(example (utterance "lines that end with a character or lower-case letter") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation or letter before string leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),concat(<let>,<@>)}")))
(example (utterance "lines without a lower-case letter , not being a number , and not leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<num>),<!>)}")))
(example (utterance "lines containing 2 or more vowels") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "character before leftquoatation ! rightquoatation , a number , and a letter") (targetValue (name "?{<any>,and(and(<!>,<num>),<let>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before not string leftquoatation @ rightquoatation and not leftquoatation # rightquoatation") (targetValue (name "?{<!>,not(and(<@>,<#>))}")))
(example (utterance "lines starting in 2 or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),repeatatleast(<@>,2)}")))
(example (utterance "lines with a capital , number , before a letter") (targetValue (name "?{or(<cap>,<num>),<let>}")))
(example (utterance "lines with a capital before string leftquoatation ! rightquoatation or letter , zero or more times") (targetValue (name "?{or(concat(<cap>,<!>),<let>)}")))
(example (utterance "lines containing a character , 2 or more times or leftquoatation ! rightquoatation followed by a vowel") (targetValue (name "?{repeatatleast(<any>,2),concat(<!>,<vow>)}")))
(example (utterance "find lines with words encompassing more than the search string leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , zero or more times or starting with lower-case letter") (targetValue (name "?{star(<!>),startwith(<low>)}")))
(example (utterance "lines with a lower-case letter or zero or more of a capital letter before a vowel") (targetValue (name "?{or(<low>,concat(<cap>,<vow>))}")))
(example (utterance "lines that contain a vowel followed by a character , at least 4 times") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines containing a character before a vowel preceding a lower-case letter") (targetValue (name "?{<any>,concat(<vow>,<low>)}")))
(example (utterance "lines ending in numbers .") (targetValue (name "?{endwith(<let>),<num>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation") (targetValue (name "?{star(<num>),endwith(<!>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation or only a letter") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines ending with a character or leftquoatation ! rightquoatation at least once in them") (targetValue (name "?{or(endwith(<any>),<!>)}")))
(example (utterance "likes with leftquoatation ! rightquoatation at least once before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),not(<@>)}")))
(example (utterance "lines containing words with ending with a character in them") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines that have words with the word leftquoatation ! rightquoatation preceding an ending with a lower-case letter") (targetValue (name "?{<!>,endwith(<low>)}")))
(example (utterance "lines that do not include a capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least 4 or more times before ending with a character") (targetValue (name "?{repeatatleast(<!>,4),endwith(<any>)}")))
(example (utterance "lines with words containing a capital letter after a lower case letter") (targetValue (name "?{<low>,contain(<cap>)}")))
(example (utterance "lines that have a vowel followed by leftquoatation ! rightquoatation and then a lower-case letter") (targetValue (name "?{<vow>,concat(<!>,<low>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation and not leftquoatation @ rightquoatation or string leftquoatation # rightquoatation") (targetValue (name "?{or(and(<!>,<@>),<#>)}")))
(example (utterance "items with an upper case letter , leftquoatation ! rightquoatation or a vowel preceding a numeral .") (targetValue (name "?{or(or(<cap>,<!>),<vow>),<num>}")))
(example (utterance "lines that contain a vowel , a letter , or a lower-case letter") (targetValue (name "?{or(or(<vow>,<let>),<low>)}")))
(example (utterance "items without leftquoatation ! rightquoatation at least 4 times .") (targetValue (name "?{repeatatleast(<!>,4)}")))
(example (utterance "lines with words with a letter then leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines starting with zero or more of a lower-case letter with a letter") (targetValue (name "?{startwith(star(<low>)),<let>}")))
(example (utterance "lines with a number followed by letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<let>),<!>}")))
(example (utterance "lines that have at least 1 character and 1 letter") (targetValue (name "?{and(<any>,<let>)}")))
(example (utterance "lines with words with a character 3 times before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,3),<!>}")))
(example (utterance "lines with or without leftquoatation ! rightquoatation , and with leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),contain(<@>)}")))
(example (utterance "lines without words with a capital letter at least once") (targetValue (name "?{repeatatleast(<cap>,1)}")))
(example (utterance "lines containing only a letter at least once") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines with a character and vowel after a letter") (targetValue (name "?{<let>,and(<any>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a number before leftquoatation @ rightquoatation once") (targetValue (name "?{and(<!>,<num>),repeatatleast(<@>,1)}")))
(example (utterance "lines with words with a number or ending with a character in them") (targetValue (name "?{<num>,endwith(<any>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation , zero or more times") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with a letter before either a lower-case letter , a number , or leftquoatation ! rightquoatation in them") (targetValue (name "?{<let>,or(or(<low>,<num>),<!>)}")))
(example (utterance "lines with a character or leftquoatation ! rightquoatation , 5 times or more") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with 3 or more of a letter before lower-case letter") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a character , but not leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<any>),not(<@>)}")))
(example (utterance "lower-case letter before a character 6 or more times") (targetValue (name "?{repeatatleast(<low>,6),<any>}")))
(example (utterance "at least 1 time the lines need to end with a character") (targetValue (name "?{repeatatleast(<any>,1)}")))
(example (utterance "lines starting with a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,repeatatleast(<!>,1))}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a number") (targetValue (name "?{and(<!>,<num>),<@>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{contain(or(<!>,<low>))}")))
(example (utterance "items with a letter at least6times preceding a vowel .") (targetValue (name "?{repeatatleast(<let>,6),<vow>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation and  leftquoatation @ rightquoatation , 5 or more times") (targetValue (name "?{repeatatleast(concat(<!>,<@>),5)}")))
(example (utterance "lines with no character before lower-case letter") (targetValue (name "?{not(<any>),endwith(<low>)}")))
(example (utterance "lines with a number before only a letter") (targetValue (name "?{<num>,contain(<let>)}")))
(example (utterance "lines with words and a lower-case letter , character , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<any>),<!>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{<!>,<cap>}")))
(example (utterance "lines containing a letter before a lower-case letter , 6 times or more") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation or starting in string leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,startwith(<@>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation followed by a character") (targetValue (name "?{<!>,concat(<@>,<any>)}")))
(example (utterance "lines with strings leftquoatation ! rightquoatation then leftquoatation @ rightquoatation and leftquoatation # rightquoatation") (targetValue (name "?{<!>,and(<@>,<#>)}")))
(example (utterance "the lines with a number before the character and leftquoatation ! rightquoatation .") (targetValue (name "?{and(<!>,<num>),<any>}")))
(example (utterance "lines containing a number , leftquoatation ! rightquoatation , and a character followed by a capital letter") (targetValue (name "?{and(and(<num>,<!>),<any>),<cap>}")))
(example (utterance "lines containing only a character , 3 or more times before capital letter") (targetValue (name "?{repeatatleast(<any>,3),<cap>}")))
(example (utterance "lines containing words with or without leftquoatation ! rightquoatation , followed by leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),<@>}")))
(example (utterance "items terminating with leftquoatation ! rightquoatation at least6times .") (targetValue (name "?{repeatatleast(<!>,6)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , a number , or a letter that's lower-case") (targetValue (name "?{or(or(<!>,<low>),<num>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and 5 or more letters") (targetValue (name "?{and(<!>,<let>)}")))
(example (utterance "lines with a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<let>),not(<!>)}")))
(example (utterance "items beginning with leftquoatation ! rightquoatation preceding an upper case letter .") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{or(repeatatleast(<!>,1),<@>)}")))
(example (utterance "lines ending with a character , zero or more times after leftquoatation ! rightquoatation") (targetValue (name "?{endwith(concat(<!>,<any>))}")))
(example (utterance "lines that contain 1 of the following: leftquoatation ! rightquoatation ; a capital letter ; or a lower-case letter with a number before it") (targetValue (name "?{or(or(<!>,<cap>),<num>),<low>}")))
(example (utterance "lines without string leftquoatation ! rightquoatation , lower-case letter , and a number .") (targetValue (name "?{and(and(<num>,<!>),<low>)}")))
(example (utterance "lines where there is at least 1 instance of leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation and leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),repeatatleast(<#>,1)}")))
(example (utterance "lines stating with 3 or more lower case letters  or leftquoatation ! rightquoatation") (targetValue (name "?{or(repeatatleast(<low>,3),<!>)}")))
(example (utterance "all lines") (targetValue (name "?{and(<let>,<!>)}")))
(example (utterance "lines must end with a number") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines with a letter followed by a vowel before leftquoatation ! rightquoatation in iit") (targetValue (name "?{concat(<let>,<vow>),<!>}")))
(example (utterance "lines with words and starting in 6 or more vowels") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines with only 4 or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with either a number , leftquoatation ! rightquoatation , or a capital letter followed by a lower-case letter") (targetValue (name "?{or(or(<num>,<!>),<cap>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation and leftquoatation # rightquoatation before a capital letter") (targetValue (name "?{and(<!>,<@>),or(<cap>,<#>)}")))
(example (utterance "lines without a character or vowel with string leftquoatation ! rightquoatation") (targetValue (name "?{or(and(<any>,<vow>),<!>)}")))
(example (utterance "lines starting with words ending with a letter") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "items with a character preceding a vowel and upper case letter .") (targetValue (name "?{<any>,and(<cap>,<vow>)}")))
(example (utterance "lines ending with a capital word and a vowel") (targetValue (name "?{and(<cap>,<vow>)}")))
(example (utterance "lines containg vowel , 2 or more times before the string dog") (targetValue (name "?{repeatatleast(<vow>,2),<!>}")))
(example (utterance "lines with or without a character before either leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{or(concat(<any>,<!>),<low>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines not containing string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{contain(not(<!>))}")))
(example (utterance "lines which are containing a character or a letter") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines which have a string which equals leftquoatation ! rightquoatation at least 7 times or greater") (targetValue (name "?{<!>}")))
(example (utterance "lines with a number before a vowel") (targetValue (name "?{<num>,contain(<vow>)}")))
(example (utterance "lines with words and 6 vowels , capital letters , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<cap>),<!>)}")))
(example (utterance "lines with a number before a character or string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,or(<any>,<!>)}")))
(example (utterance "lines with a vowel or number , zero or more times") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines containing a vowel followed by a character and leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,and(<any>,<!>)}")))
(example (utterance "lines with 5 or more of a character before letter") (targetValue (name "?{<any>,repeatatleast(<let>,5)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a lower-case letter followed by a number") (targetValue (name "?{and(<!>,<low>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , any amount of times with leftquoatation @ rightquoatation or a capital") (targetValue (name "?{star(<!>),or(<@>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a vowel or capitalized .  .") (targetValue (name "?{or(<!>,<vow>),<cap>}")))
(example (utterance "lines starting in a letter or with a lower-case leter") (targetValue (name "?{startwith(<let>),contain(<low>)}")))
(example (utterance "lines with number before lower-case letter , vowel , and string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,and(and(<low>,<vow>),<!>)}")))
(example (utterance "lines containing 1 capital letter and 1 letter") (targetValue (name "?{and(<let>,<cap>)}")))
(example (utterance "lines that have a lower case letter followed by a number , vowel , or the word leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<vow>),<!>),<low>}")))
(example (utterance "lines with capital letter at least once before string leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{repeatatleast(<cap>,1),or(<!>,<low>)}")))
(example (utterance "lines with character before a number") (targetValue (name "?{<any>,contain(<num>)}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation and lower-case letter") (targetValue (name "?{endwith(<any>),and(<!>,<low>)}")))
(example (utterance "lines containing a number , leftquoatation ! rightquoatation and a a lower-case letter") (targetValue (name "?{or(or(<num>,<!>),<low>)}")))
(example (utterance "a line that contains a capital letter , letter and a vowel .") (targetValue (name "?{and(and(<vow>,<cap>),<let>)}")))
(example (utterance "lines containing number or lower-case letter , 6 or more times") (targetValue (name "?{contain(or(<num>,<low>))}")))
(example (utterance "lines with words containing a lower-case letter , a vowel , and a character") (targetValue (name "?{and(<low>,<vow>),<any>}")))
(example (utterance "lines with a lower-case letter and only leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<!>)}")))
(example (utterance "items with a numeral preceding an upper case letter .") (targetValue (name "?{<num>,star(<cap>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation lower-case letter , and a capital letter") (targetValue (name "?{and(and(<!>,<low>),<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceding a vowel and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(<vow>,<@>)}")))
(example (utterance "lines containing a lower-case letter or non-number") (targetValue (name "?{contain(<low>),not(<num>)}")))
(example (utterance "items with a vowel or character at least 7 times .") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines containing either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation or a character") (targetValue (name "?{or(or(<!>,<any>),<@>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or character before letter") (targetValue (name "?{<!>,concat(<any>,<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a capital letter , at least 7 times .") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines containing a character that end with a number") (targetValue (name "?{<any>,endwith(<num>)}")))
(example (utterance "lines with 7 or more numbers") (targetValue (name "?{contain(<num>)}")))
(example (utterance "lines containing 2 or more numbers and a character") (targetValue (name "?{repeatatleast(<num>,2),<any>}")))
(example (utterance "lines that do not contain a number or a letter") (targetValue (name "?{and(<num>,<let>)}")))
(example (utterance "lines with a capital before character followed by vowel") (targetValue (name "?{<cap>,concat(<any>,<vow>)}")))
(example (utterance "lines with a vowel 6 times") (targetValue (name "?{repeatatleast(<vow>,6),not(<let>)}")))
(example (utterance "lines having either a letter , lower-case letter , or string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<low>),<!>),<@>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , and a character") (targetValue (name "?{and(and(<!>,<@>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a number 3 times") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , character , and capital letter before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<any>),<cap>),<@>}")))
(example (utterance "lines do not have a capital letter following a letter") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "items with any letter preceding a numeral .") (targetValue (name "?{and(<low>,<cap>),<num>}")))
(example (utterance "lines without a letter , 7 or more times") (targetValue (name "?{repeatatleast(<let>,7)}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,1),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending in a letter") (targetValue (name "?{repeatatleast(<!>,1),endwith(<let>)}")))
(example (utterance "lines that do not have a character at least twice") (targetValue (name "?{repeatatleast(<any>,2)}")))
(example (utterance "lines that begin with at least 1 instance of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,1)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation , zero") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines containing a character that follows a lower-case letter") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , vowel , and character before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<vow>),<any>),<@>}")))
(example (utterance "lines starting in a vowel before 7 or more of leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<vow>),repeatatleast(<!>,7)}")))
(example (utterance "lines with a letter followed by a lower-case letter and leftquoatation ! rightquoatation , 5 or more times") (targetValue (name "?{concat(<let>,<low>),repeatatleast(<!>,5)}")))
(example (utterance "all lines") (targetValue (name "?{startwith(or(<low>,<num>))}")))
(example (utterance "lines that have either the word leftquoatation ! rightquoatation and a capital letter or the word leftquoatation @ rightquoatation followed by the word leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<cap>),<@>),<#>}")))
(example (utterance "items with leftquoatation ! rightquoatation or any letter .") (targetValue (name "?{and(and(<!>,<low>),<cap>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a number in it .") (targetValue (name "?{or(or(<!>,<@>),<num>)}")))
(example (utterance "lines with words , a vowel , and leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(<vow>,<!>),<@>}")))
(example (utterance "lines with words with a lower-case letter") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines with zero or more numbers or ending in character") (targetValue (name "?{star(<num>),endwith(<any>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation or leftquoatation @ rightquoatation at least once") (targetValue (name "?{contain(or(<!>,<@>))}")))
(example (utterance "lines with words with a vowel followed by a capital letter") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines ending in a letter starting with vowelo") (targetValue (name "?{endwith(<let>),startwith(<vow>)}")))
(example (utterance "lines containing a number after leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a character") (targetValue (name "?{and(and(<!>,<@>),<any>),<num>}")))
(example (utterance "lines containing number 4 times .") (targetValue (name "?{<num>}")))
(example (utterance "line with a words followed by leftquoatation ! rightquoatation") (targetValue (name "?{<any>,<!>}")))
(example (utterance "lines with words leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a character and the leftquoatation # rightquoatation") (targetValue (name "?{<!>,and(and(<@>,<any>),<#>)}")))
(example (utterance "lines do not contain leftquoatation ! rightquoatation , a vowel , or a lower-case letter") (targetValue (name "?{or(or(<!>,<vow>),<low>)}")))
(example (utterance "lines ending with a letter followed by lower-case letter") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a vowel , as well as a number followed by leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<vow>),<num>),<@>}")))
(example (utterance "a letter followed by a capital letter before a character appears in the lines") (targetValue (name "?{concat(<let>,<cap>),<any>}")))
(example (utterance "lines with a character or letter before string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<cap>),<let>),<!>}")))
(example (utterance "lines having either string leftquoatation ! rightquoatation , a lower-case letter , or letter before character") (targetValue (name "?{or(or(<!>,<low>),<let>),<any>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation , or a number and leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),and(<num>,<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 7 or more times before a capital letter in them") (targetValue (name "?{repeatatleast(<!>,7),<cap>}")))
(example (utterance "lines starting with character and lower-case letter followed by letter") (targetValue (name "?{startwith(<any>),concat(<low>,<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or with a number") (targetValue (name "?{or(<!>,contain(<num>))}")))
(example (utterance "lines consisting of a capital letter or the word leftquoatation ! rightquoatation preceding a character") (targetValue (name "?{or(<cap>,<!>),<any>}")))
(example (utterance "items with words containing a small letter .") (targetValue (name "?{<low>}")))
(example (utterance "lines that contain 4 or more times leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a number before vowel") (targetValue (name "?{and(<!>,<num>),<vow>}")))
(example (utterance "lines 7 or more numbers before a letter") (targetValue (name "?{repeatatleast(<num>,7),<let>}")))
(example (utterance "lines without a vowel or lower-case letter") (targetValue (name "?{and(<vow>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before ending with a lower-case letter followed by leftquoatation @ rightquoatation in them") (targetValue (name "?{<!>,endwith(concat(<low>,<@>))}")))
(example (utterance "lines containing a letter followed by a character twice or more times .") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation 7 times and leftquoatation @ rightquoatation") (targetValue (name "?{and(repeatatleast(<!>,7),<@>)}")))
(example (utterance "lines with words with a character or leftquoatation ! rightquoatation at least once in them") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with a letter before capital letter") (targetValue (name "?{<let>,not(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number then a letter") (targetValue (name "?{concat(<!>,<num>),<let>}")))
(example (utterance "lines containing words not ending with a lower-case letter") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines with only a number before lower-case letter") (targetValue (name "?{repeatatleast(<num>,1),<low>}")))
(example (utterance "lines with a number , and leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a letter") (targetValue (name "?{and(and(<num>,<!>),<@>),<let>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation once") (targetValue (name "?{repeatatleast(or(<num>,<!>),1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending in a character") (targetValue (name "?{repeatatleast(<!>,1),endwith(<any>)}")))
(example (utterance "lines that do not contain a character but contain a lower-case letter , at least once") (targetValue (name "?{repeatatleast(<low>,1),not(<any>)}")))
(example (utterance "lines with words , lower-case letter , leftquoatation ! rightquoatation , and a number") (targetValue (name "?{and(<low>,<!>),<num>}")))
(example (utterance "lines with words with zero or more of a character , string leftquoatation ! rightquoatation , or number") (targetValue (name "?{or(or(<any>,<!>),<num>)}")))
(example (utterance "lines with a letter and a vowel occuring before a capital letter") (targetValue (name "?{and(<let>,<vow>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before number or with a capital letter") (targetValue (name "?{concat(<!>,<num>),contain(<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , zero or more times or string leftquoatation @ rightquoatation and vowel") (targetValue (name "?{star(<!>),and(<@>,<vow>)}")))
(example (utterance "lines starting with capital letter or containing string leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,contain(<!>))}")))
(example (utterance "lines with a capital letter 2 times or leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<cap>,2),contain(<!>)}")))
(example (utterance "lines starting with words with vowel , 7 or more times") (targetValue (name "?{repeatatleast(<vow>,7)}")))
(example (utterance "lines at least5times ending with a character and leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,5),endwith(<any>)}")))
(example (utterance "lines without words with a character without capital letter or lower-case letter") (targetValue (name "?{and(and(<any>,<cap>),<low>)}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation before a vowel or lower-case letter") (targetValue (name "?{<!>,or(<low>,<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a lower-case letter , zero or more times in them") (targetValue (name "?{star(concat(<!>,<low>))}")))
(example (utterance "lines contaiing a lower-case letter or leftquoatation ! rightquoatation at least zero times") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines which arecontaining leftquoatation ! rightquoatation , 7 or more times before a character") (targetValue (name "?{contain(repeatatleast(<!>,7)),<any>}")))
(example (utterance "lines with a number or leftquoatation ! rightquoatation before capital letter") (targetValue (name "?{or(<num>,<!>),<cap>}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation before a letter and string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<let>),<@>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a character , and leftquoatation # rightquoatation") (targetValue (name "?{<!>,and(and(<@>,<any>),<#>)}")))
(example (utterance "lines with words with a letter followed by string leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation and ending with capital letter") (targetValue (name "?{startwith(<!>),endwith(<cap>)}")))
(example (utterance "lines that end with a word that contains a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "items with a letter preceding a numeral and terminating with leftquoatation ! rightquoatation .") (targetValue (name "?{concat(<let>,<num>),endwith(<!>)}")))
(example (utterance "lines with words that don't have number and character") (targetValue (name "?{not(<num>),<any>}")))
(example (utterance "lines which are containing either a vowel , leftquoatation ! rightquoatation , or a number") (targetValue (name "?{or(or(<vow>,<!>),<num>)}")))
(example (utterance "lines without letters or leftquoatation ! rightquoatation leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{and(<!>,and(and(<@>,<let>),<#>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation and leftquoatation # rightquoatation , leftquoatation $ rightquoatation , and a lower-case letter in them") (targetValue (name "?{concat(<!>,<@>),and(and(<#>,<$>),<low>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation then 4 or more of leftquoatation @ rightquoatation") (targetValue (name "?{<low>,repeatatleast(concat(<!>,<@>),4)}")))
(example (utterance "lines that have words consisting of a lower-case letter , vowel , and a capital letter") (targetValue (name "?{and(<low>,<vow>),<cap>}")))
(example (utterance "lines without a vowel , string leftquoatation ! rightquoatation , or character") (targetValue (name "?{and(and(<vow>,<!>),<any>)}")))
(example (utterance "lines with a number followed by a character , a letter , or leftquoatation ! rightquoatation") (targetValue (name "?{<num>,or(or(<any>,<let>),<!>)}")))
(example (utterance "lines without only a character before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "all lines") (targetValue (name "?{endwith(star(<vow>))}")))
(example (utterance "lines ending with a character , 4 or more times") (targetValue (name "?{contain(repeatatleast(<any>,4))}")))
(example (utterance "lines with a lower-case letter at least 6 times starting with leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,6),startwith(<!>)}")))
(example (utterance "lines with a lower-case letter , vowel , or character") (targetValue (name "?{and(and(<low>,<vow>),<any>)}")))
(example (utterance "lines whiich are starting with words with a letter , 7 or more times") (targetValue (name "?{repeatatleast(<let>,7)}")))
(example (utterance "lines starting with a character before a capital letter , zero or more times in it") (targetValue (name "?{startwith(<any>),star(<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation or a letter preceding a character .") (targetValue (name "?{or(or(<cap>,<!>),<low>),<any>}")))
(example (utterance "lines without a vowel , character , or number") (targetValue (name "?{or(or(<vow>,<any>),<num>)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation or a vowel , zero or more times in them") (targetValue (name "?{concat(<any>,<!>),star(<vow>)}")))
(example (utterance "lines containing capital letters zero or more times before a consonant") (targetValue (name "?{star(<cap>),not(<vow>)}")))
(example (utterance "items with a numeral , a vowel , or a letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{or(or(<let>,<vow>),<num>),<!>}")))
(example (utterance "lines containing capital letter , character , and letter") (targetValue (name "?{and(and(<cap>,<any>),<let>)}")))
(example (utterance "lines that have5or more letters or lower-case letters") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines starting in zero or more lower-case letters or leftquoatation ! rightquoatation") (targetValue (name "?{or(star(<low>),<!>)}")))
(example (utterance "lines with no vowel or capital letter") (targetValue (name "?{or(not(<vow>),<cap>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation at least once before string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(contain(<!>),1),<@>}")))
(example (utterance "lines having words containing vowels which don't precede a lower-case letter") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines containing over 6 letters after a character") (targetValue (name "?{<any>,endwith(repeatatleast(<let>,6))}")))
(example (utterance "lines not containing vowels more than 5 times") (targetValue (name "?{repeatatleast(<vow>,5)}")))
(example (utterance "lines with a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<let>),<!>)}")))
(example (utterance "lines with or without only a capital letter .") (targetValue (name "?{star(<cap>)}")))
(example (utterance "lines with a vowel followed by a letter or leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,or(<let>,<!>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation and end with a vowel , at least once") (targetValue (name "?{repeatatleast(<vow>,1),endwith(<!>)}")))
(example (utterance "lines with 3 or more of leftquoatation ! rightquoatation before strings leftquoatation @ rightquoatation") (targetValue (name "?{<!>,repeatatleast(<@>,3)}")))
(example (utterance "lines with a character that is preceded by an uppercase letter") (targetValue (name "?{<cap>,<any>}")))
(example (utterance "lines with words zero or more times with a character") (targetValue (name "?{<any>,star(<let>)}")))
(example (utterance "lines with 4 or more letters before a capital letter") (targetValue (name "?{repeatatleast(<let>,4),<cap>}")))
(example (utterance "lines with words containing leftquoatation ! rightquoatation , a number , and a character") (targetValue (name "?{and(<!>,<num>),<any>}")))
(example (utterance "lines ending in vowel with string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<vow>),concat(<!>,<@>)}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation or a capital letter .") (targetValue (name "?{<!>,<cap>}")))
(example (utterance "items with an upper case letter , a numeral , or a character .") (targetValue (name "?{or(or(<cap>,<any>),<num>)}")))
(example (utterance "lines that contain 4 or more words starting with a letter") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,contain(repeatatleast(<@>,1))}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation and a character") (targetValue (name "?{and(<!>,<any>)}")))
(example (utterance "lines with numbers before a character , letter , or vowel") (targetValue (name "?{<num>,or(or(<any>,<let>),<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a character , or a vowel preceding leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<vow>,<any>),<!>),<@>}")))
(example (utterance "lines with words with either a character , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation and a lower-case letter in them") (targetValue (name "?{or(or(<any>,<!>),<@>),<low>}")))
(example (utterance "lines ending with a lower-case letter and string leftquoatation ! rightquoatation") (targetValue (name "?{contain(and(<low>,<!>))}")))
(example (utterance "lines with 2 or more vowels with string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<vow>,2),contain(<!>)}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{concat(<cap>,<!>),<num>}")))
(example (utterance "lines with a letter , zero or more times or  string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{star(<let>),concat(<!>,<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before capital letter then a letter") (targetValue (name "?{<!>,concat(<cap>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or number before a capital letter") (targetValue (name "?{or(<!>,<num>),<cap>}")))
(example (utterance "lines that have 4 or more occurrences of capital letter followed by a character") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "string 'dog appears at least once and string leftquoatation ! rightquoatation does not appear in the lines") (targetValue (name "?{repeatatleast(<!>,1),not(<!>)}")))
(example (utterance "lines with an uppercase letter preceded by a number 6 or more times") (targetValue (name "?{repeatatleast(<num>,6),<cap>}")))
(example (utterance "lines with a lowercase letter preceded by either leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(<!>,<num>),<low>}")))
(example (utterance "lines that have a character or lower-cast letter 6 or more times") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines which are ending with a letter followed by a number") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines with with or without leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{star(<!>),or(<@>,<vow>)}")))
(example (utterance "a vowel or number at the end of a line") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<#>)}")))
(example (utterance "lines which have a letter , capital letter , and the string 'dog") (targetValue (name "?{and(and(<cap>,<let>),<!>)}")))
(example (utterance "lines with vowel at least once before either a capital letter , string leftquoatation ! rightquoatation , or letter") (targetValue (name "?{repeatatleast(<vow>,1),or(or(<cap>,<!>),<let>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation , a capital , and string leftquoatation @ rightquoatation") (targetValue (name "?{<num>,and(and(<!>,<cap>),<@>)}")))
(example (utterance "lines having either a lower-case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation before leftquoatation # rightquoatation in them") (targetValue (name "?{or(or(<low>,<!>),<@>),<#>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation and a character , or leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),or(<any>,<#>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , and at least 1 number") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines that have a letter in them either 4 times or more than that") (targetValue (name "?{<let>}")))
(example (utterance "lines starting with either string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , or string leftquoatation # rightquoatation or a number") (targetValue (name "?{or(or(or(<!>,<@>),<#>),<num>)}")))
(example (utterance "lines with zero or more capital letter without a lower-case letter") (targetValue (name "?{star(<cap>),not(<low>)}")))
(example (utterance "lines having either a vowel , string leftquoatation ! rightquoatation , lower-case letter before a capital letter") (targetValue (name "?{or(or(<vow>,<!>),<low>),<cap>}")))
(example (utterance "lines with a character or leftquoatation ! rightquoatation or 'truck before a lower-case letter") (targetValue (name "?{or(or(<any>,<!>),<@>),<low>}")))
(example (utterance "lines a letter before leftquoatation ! rightquoatation and a lower case letter") (targetValue (name "?{<let>,and(<!>,<low>)}")))
(example (utterance "line with a character then a number or small letter") (targetValue (name "?{<any>,or(<num>,<low>)}")))
(example (utterance "lines ending with only a letter at least once") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,repeatatleast(<!>,1))}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by either a character or a letter") (targetValue (name "?{<!>,or(or(<any>,<let>),<vow>)}")))
(example (utterance "lines with words with lower-case letter , zero or more times and number") (targetValue (name "?{star(<low>),<num>}")))
(example (utterance "lines with words and a character , capital and lower case letters") (targetValue (name "?{and(<any>,<cap>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel and string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<vow>),repeatatleast(<@>,1)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding an upper case letter preceding a character .") (targetValue (name "?{<!>,concat(<cap>,<any>)}")))
(example (utterance "lines with a capital or lower case after a letter") (targetValue (name "?{<let>,or(<cap>,<low>)}")))
(example (utterance "lines with 6 or more of a character before a capital letter") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines having words that only contain a vowel , any number of times .") (targetValue (name "?{<vow>}")))
(example (utterance "lines with 6 or mroe of a lower-case before capital letter") (targetValue (name "?{<low>,repeatatleast(<cap>,6)}")))
(example (utterance "lines with a lower-case letter , 6 or more times but not a character") (targetValue (name "?{repeatatleast(<low>,6),not(<any>)}")))
(example (utterance "lines with or without leftquoatation ! rightquoatation , or a number and a character") (targetValue (name "?{star(<!>),and(<num>,<any>)}")))
(example (utterance "lines where the numbers do not precede vowels") (targetValue (name "?{<num>,not(<vow>)}")))
(example (utterance "lines with a vowel before a number or string leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<vow>,<num>),<!>)}")))
(example (utterance "lines with a vowel after a lower-case letter at least 5 times") (targetValue (name "?{repeatatleast(<low>,5),<vow>}")))
(example (utterance "items with a character preceding leftquoatation ! rightquoatation and a vowel .") (targetValue (name "?{<any>,and(<!>,<vow>)}")))
(example (utterance "lines with capital letters before lower-case letters") (targetValue (name "?{star(<cap>),<low>}")))
(example (utterance "items with a letter or an upper case letter preceding a small letter .") (targetValue (name "?{or(<cap>,<let>),<low>}")))
(example (utterance "lines having either leftquoatation ! rightquoatation , a lower case letter , or a vowel then capital") (targetValue (name "?{or(or(<!>,<low>),<vow>),<cap>}")))
(example (utterance "lines that must contain a number , the the word leftquoatation ! rightquoatation and the word leftquoatation @ rightquoatation") (targetValue (name "?{<num>,and(<!>,<@>)}")))
(example (utterance "2 or more sets of lines with capital letters before the string 'dogleftquoatation ") (targetValue (name "?{repeatatleast(<cap>,2),<!>}")))
(example (utterance "items with small letters preceding a letter at least5times .") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation containing either a capital letter , a number") (targetValue (name "?{<!>,contain(or(or(<cap>,<num>),<@>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a letter , a lower-case letter , or leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<let>,<low>),<@>)}")))
(example (utterance "lines containing a lower-case letter or string leftquoatation ! rightquoatation at least once") (targetValue (name "?{repeatatleast(or(<low>,<!>),1)}")))
(example (utterance "lines without words with capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines which are ending with a number before a letter") (targetValue (name "?{contain(endwith(<num>)),<let>}")))
(example (utterance "lines starting with a vowel or not a letter in them") (targetValue (name "?{or(<vow>,not(<let>))}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation , a lower-case letter , or leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<low>),<@>)}")))
(example (utterance "lines without a vowel , string leftquoatation ! rightquoatation , or lower-case letter") (targetValue (name "?{and(and(<vow>,<!>),<low>)}")))
(example (utterance "lines containing number and character , zero or more times") (targetValue (name "?{contain(<num>),star(<any>)}")))
(example (utterance "lines with words with a character , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<vow>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a non-lower-case letter") (targetValue (name "?{<!>,not(<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation + capital before a vowel") (targetValue (name "?{and(<!>,<cap>),<vow>}")))
(example (utterance "lines with a character , leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{or(<any>,<!>),<low>}")))
(example (utterance "lines containing 4 or more vowels before a letter") (targetValue (name "?{repeatatleast(<vow>,4),<let>}")))
(example (utterance "lines with words with a lower-case letter or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<low>,<!>),<@>}")))
(example (utterance "lines with a lower-case letter and string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a number") (targetValue (name "?{and(and(<low>,<!>),<@>),<num>}")))
(example (utterance "lines which are ending with a vowel , 7 or more times before a character") (targetValue (name "?{endwith(repeatatleast(<vow>,7)),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a letter before a number") (targetValue (name "?{and(and(<!>,<@>),<let>),<num>}")))
(example (utterance "lines without a number , string leftquoatation ! rightquoatation , or lower-case letter before string leftquoatation @ rightquoatation") (targetValue (name "?{concat(and(and(<num>,<!>),<low>),<@>)}")))
(example (utterance "lines starting with letter , 7 or more times before a capital") (targetValue (name "?{repeatatleast(startwith(<let>),7),<cap>}")))
(example (utterance "lines with a vowel or without string leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,not(<!>))}")))
(example (utterance "lines with words having leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation , 3 times  or more") (targetValue (name "?{and(and(<!>,<@>),<#>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation .") (targetValue (name "?{<num>,and(and(<!>,<@>),<#>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation then leftquoatation # rightquoatation") (targetValue (name "?{contain(<!>),concat(<@>,<#>)}")))
(example (utterance "lines ending with a letter or a lower-case") (targetValue (name "?{or(contain(<let>),<low>)}")))
(example (utterance "lines with lower-case letter before string leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{<low>,repeatatleast(<!>,2)}")))
(example (utterance "lines that have words containing a letter and a leftquoatation ! rightquoatation string or a leftquoatation @ rightquoatation string") (targetValue (name "?{and(<let>,<!>),<@>}")))
(example (utterance "items with a small letter preceding a number , leftquoatation ! rightquoatation and an upper case letter .") (targetValue (name "?{<low>,and(and(<num>,<!>),<cap>)}")))
(example (utterance "lines with at least once number or a character") (targetValue (name "?{repeatatleast(<num>,1),<any>}")))
(example (utterance "lines without a vowel and doesn't end with the string 'dog") (targetValue (name "?{and(<vow>,endwith(<!>))}")))
(example (utterance "lines without a character or leftquoatation ! rightquoatation") (targetValue (name "?{not(and(<any>,<!>))}")))
(example (utterance "lines with less than 7 of a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,repeatatleast(<!>,7))}")))
(example (utterance "lines with leftquoatation ! rightquoatation") (targetValue (name "?{concat(repeatatleast(<!>,1),<any>)}")))
(example (utterance "lines that have the words leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,<@>}")))
(example (utterance "lines which are containing leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines with zero or more capital letters or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines with words , a lower-case letter , and the string 'dog or a number") (targetValue (name "?{and(<low>,<!>),<num>}")))
(example (utterance "lines with a character , vowel , and capital letter") (targetValue (name "?{and(and(<any>,<vow>),<cap>)}")))
(example (utterance "lines that contain a number , a character or a letter followed by a vowel") (targetValue (name "?{or(or(<num>,<any>),<let>),<vow>}")))
(example (utterance "lines with number and lower-case letter or character at least once") (targetValue (name "?{or(and(<num>,<low>),<any>)}")))
(example (utterance "lines with either a capital letter , a letter , or a character") (targetValue (name "?{or(or(<let>,<cap>),<any>)}")))
(example (utterance "lines with a vowel and character before a letter") (targetValue (name "?{and(<vow>,<any>),<let>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{or(concat(<!>,<@>),<#>)}")))
(example (utterance "lines with a letter , vowel , and string leftquoatation ! rightquoatation before string 'truck ;") (targetValue (name "?{and(and(<let>,<vow>),<!>),<@>}")))
(example (utterance "lines starting with words with 2 or more vowels") (targetValue (name "?{repeatatleast(<vow>,2)}")))
(example (utterance "lines with a letter before something not being a vowel") (targetValue (name "?{<let>,not(<vow>)}")))
(example (utterance "lines with a number or string leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{or(<num>,<!>),<low>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation , lower'case letter , and vowel") (targetValue (name "?{and(<!>,<low>),<vow>}")))
(example (utterance "lines with zero or more of a character before words with string leftquoatation ! rightquoatation") (targetValue (name "?{<any>,star(<!>)}")))
(example (utterance "lines with words that don't have a letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines not ending with a number 5 times") (targetValue (name "?{endwith(repeatatleast(<num>,5))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before zero or more capital letters") (targetValue (name "?{<!>,contain(star(<cap>))}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , a lower case letter , and leftquoatation @ rightquoatation before a number") (targetValue (name "?{and(and(<!>,<low>),<@>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and capital letter or string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),or(<cap>,<@>)}")))
(example (utterance "lines which are ending with containing only a character , 7 or more times") (targetValue (name "?{repeatatleast(<any>,7)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{<!>,not(star(<@>))}")))
(example (utterance "lines with a lower-case letter , a number , and a capital letter") (targetValue (name "?{and(and(<low>,<num>),<cap>)}")))
(example (utterance "lines with number or containing string leftquoatation ! rightquoatation at least once") (targetValue (name "?{or(<num>,contain(<!>))}")))
(example (utterance "lines with words and a letter , vowel , and character") (targetValue (name "?{and(<let>,<vow>),<any>}")))
(example (utterance "lines containing number or leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{star(or(<num>,<!>))}")))
(example (utterance "lines with 4 ore more letter before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,4),<!>}")))
(example (utterance "lines with 3 capital letters or characters") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines not having letter before ending with lower-case letter") (targetValue (name "?{concat(<let>,endwith(<low>))}")))
(example (utterance "lines with words and zero or more of leftquoatation ! rightquoatation before having a vowel") (targetValue (name "?{star(<!>),<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a character , and leftquoatation @ rightquoatation 1 or more times") (targetValue (name "?{and(and(<!>,<any>),<@>)}")))
(example (utterance "lines which are not having a capital letter , not a character , and not a lower-case letter") (targetValue (name "?{and(and(<cap>,<any>),<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a lower-case letter") (targetValue (name "?{concat(<let>,<!>),<low>}")))
(example (utterance "lines containing 3 or more vowels before a lower-case letter") (targetValue (name "?{repeatatleast(<vow>,3),<low>}")))
(example (utterance "lines without a vowel before a number") (targetValue (name "?{concat(not(<vow>),<num>)}")))
(example (utterance "lines with a vowel at least once or containing leftquoatation ! rightquoatation in them") (targetValue (name "?{repeatatleast(<vow>,1),contain(<!>)}")))
(example (utterance "lines with a character , with any amount not being leftquoatation ! rightquoatation") (targetValue (name "?{star(<any>),not(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before containing leftquoatation @ rightquoatation , a vowel , and a letter in them") (targetValue (name "?{<!>,contain(and(and(<@>,<vow>),<let>))}")))
(example (utterance "lines that end with 5 or more capital letters , each followed by a character") (targetValue (name "?{repeatatleast(concat(<cap>,<any>),5)}")))
(example (utterance "lines containing only a character or without leftquoatation ! rightquoatation") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "a lower case letter , a number , a vowel and leftquoatation ! rightquoatation appears in the lines") (targetValue (name "?{contain(<low>),and(and(<num>,<vow>),<!>)}")))
(example (utterance "lines containing 2 or more numbers") (targetValue (name "?{<num>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or starting with string leftquoatation @ rightquoatation , 4 or more times") (targetValue (name "?{or(<!>,startwith(<@>))}")))
(example (utterance "all lines") (targetValue (name "?{<any>,star(endwith(<let>))}")))
(example (utterance "lines without leftquoatation ! rightquoatation or without a letter") (targetValue (name "?{or(star(<!>),<let>)}")))
(example (utterance "the lines that have a letter , leftquoatation ! rightquoatation , and a vowel followed by a number") (targetValue (name "?{and(and(<let>,<!>),<vow>),<num>}")))
(example (utterance "lines with a character") (targetValue (name "?{<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a lower-case letter , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation in them") (targetValue (name "?{and(<!>,and(and(<low>,<@>),<#>))}")))
(example (utterance "lines with lower-case letter before only string leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{<low>,repeatatleast(<!>,3)}")))
(example (utterance "items without an upper case letter at least 1 time .") (targetValue (name "?{repeatatleast(<cap>,1)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , a capital letter , or vowel") (targetValue (name "?{or(or(<!>,<cap>),<vow>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(repeatatleast(<!>,1),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character or leftquoatation @ rightquoatation and a lower-case letter in it") (targetValue (name "?{concat(<!>,<any>),and(<@>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , as well as leftquoatation # rightquoatation followed by a letter") (targetValue (name "?{and(and(<!>,<@>),<#>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and a capital letter followed by leftquoatation # rightquoatation in them") (targetValue (name "?{concat(<!>,<@>),concat(<cap>,<#>)}")))
(example (utterance "lines with words with a capital and string leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a character before a capital letter in them") (targetValue (name "?{and(<!>,<any>),<cap>}")))
(example (utterance "lower case letter or number before a character with the leftquoatation ! rightquoatation string") (targetValue (name "?{or(<low>,<num>),concat(<any>,<!>)}")))
(example (utterance "lines containing the word leftquoatation ! rightquoatation along with a vowel and a lower-case letter") (targetValue (name "?{and(<!>,<vow>),<low>}")))
(example (utterance "lines which are ending with a lower-case letter followed by a letter before leftquoatation ! rightquoatation") (targetValue (name "?{endwith(concat(<low>,<let>)),<!>}")))
(example (utterance "lines with words and without a vowel before number") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines ending with a number , zero or more .") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines with words with zero or more of a letter or number") (targetValue (name "?{<let>,star(<num>)}")))
(example (utterance "lines with a character , leftquoatation ! rightquoatation , or a capital letter , before a vowel .") (targetValue (name "?{or(or(<any>,<!>),<cap>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation , character , and number") (targetValue (name "?{<!>,and(and(<@>,<any>),<num>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation 7 or more times") (targetValue (name "?{repeatatleast(<!>,7),<any>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation or starting with string leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),startwith(<@>)}")))
(example (utterance "lines with words ending with number and string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<num>),<!>}")))
(example (utterance "lines with a lower-case letter after the string dog , a number , or a capital letter") (targetValue (name "?{or(or(<!>,<num>),<cap>),<low>}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation before a number") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation , a lower-case letter , and a capital letter") (targetValue (name "?{and(and(<low>,<!>),<cap>)}")))
(example (utterance "items without a small letter , at least 7 times .") (targetValue (name "?{contain(repeatatleast(<low>,7))}")))
(example (utterance "lines not ending with vowel , zero or more times") (targetValue (name "?{endwith(star(<vow>))}")))
(example (utterance "lines without a lower-case letter or capital letter") (targetValue (name "?{and(<low>,repeatatleast(<cap>,1))}")))
(example (utterance "lines not ending with string leftquoatation ! rightquoatation , 7 or more times") (targetValue (name "?{repeatatleast(endwith(<!>),7)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a vowel and string leftquoatation @ rightquoatation at least 6 times") (targetValue (name "?{concat(<!>,<vow>),repeatatleast(<@>,6)}")))
(example (utterance "lines containing a capital letter , a character , and a number follwed by leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<any>),<num>),<!>}")))
(example (utterance "lines that have a capital letter before a lower case letter .") (targetValue (name "?{repeatatleast(<cap>,1),contain(<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a character") (targetValue (name "?{and(<!>,<@>),<any>}")))
(example (utterance "lines not having capital letter , a lower-case letter , and not a character") (targetValue (name "?{and(and(<cap>,<low>),<any>)}")))
(example (utterance "lines ending in zero or more of a capital letter or string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(or(<cap>,<!>))}")))
(example (utterance "lines having either leftquoatation ! rightquoatation , a capital letter , or a character before a lower-case letter in them") (targetValue (name "?{or(or(<!>,<cap>),<any>),<low>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by 7 or more numbers") (targetValue (name "?{repeatatleast(concat(<!>,<num>),7)}")))
(example (utterance "lines ending with a capital over 6 times") (targetValue (name "?{endwith(contain(<cap>))}")))
(example (utterance "lines that have a vowel followed by a capital letter , then the word leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,concat(<cap>,<!>)}")))
(example (utterance "lines that begin with a character at least zero times") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines not containing a vowel a character") (targetValue (name "?{contain(and(<vow>,<any>))}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation and a capital , 3 or more times") (targetValue (name "?{contain(and(<!>,<cap>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter , vowel , and capital letter") (targetValue (name "?{<!>,and(and(<low>,<vow>),<cap>)}")))
(example (utterance "lines containing a character followed by 0 or more of leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines that contain a number , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation followed by a capital letter") (targetValue (name "?{or(or(<num>,<!>),<@>),<cap>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before leftquoatation @ rightquoatation at least once followed by a number") (targetValue (name "?{repeatatleast(concat(<!>,<@>),1),<num>}")))
(example (utterance "items with a small letter preceding a capital letter .") (targetValue (name "?{star(<low>),<cap>}")))
(example (utterance "lines which have a capital letter or a lower-case letter , zero or more times") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation , vowel , or lower-case letter") (targetValue (name "?{or(<!>,<vow>),<low>}")))
(example (utterance "items with a vowel preceding a letter preceding a small letter .") (targetValue (name "?{<vow>,concat(<let>,<low>)}")))
(example (utterance "lines containing 3 numbers before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,3),<!>}")))
(example (utterance "leftquoatation ! rightquoatation before a number , vowel , and capital letter") (targetValue (name "?{<!>,and(and(<num>,<cap>),<vow>)}")))
(example (utterance "lines with words and a lower-case letter , vowel , and number") (targetValue (name "?{and(<low>,<vow>),<num>}")))
(example (utterance "lines with 2 or more of a lower-case letter or character") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines with a letter not coming before a number") (targetValue (name "?{<let>,not(<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation before not string leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),not(<#>)}")))
(example (utterance "lines with 6 or more capital letters before a lower-case letter") (targetValue (name "?{repeatatleast(<cap>,6),<low>}")))
(example (utterance "lines that contain a lower-case letter , a character , or leftquoatation ! rightquoatation followed by a capital letter") (targetValue (name "?{or(or(<low>,<any>),<!>),<cap>}")))
(example (utterance "lines with a capital letter before a vowel") (targetValue (name "?{concat(<cap>,repeatatleast(<vow>,1))}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a capital letter before vowel") (targetValue (name "?{repeatatleast(<!>,1),concat(<cap>,<vow>)}")))
(example (utterance "lines without only a number or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines with a vowel then a letter") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines with 3 or more of a characters before lower-case letters then a vowel") (targetValue (name "?{repeatatleast(concat(<any>,<low>),3),<vow>}")))
(example (utterance "lines containing a letter , a character , and a vowel") (targetValue (name "?{and(<let>,<any>),<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a letter , and leftquoatation @ rightquoatation") (targetValue (name "?{contain(and(and(<!>,<let>),<@>))}")))
(example (utterance "lines containing a lower-case letter after a number , capital , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<cap>),<!>),<low>}")))
(example (utterance "lines with a capital letter before a number and vowel") (targetValue (name "?{concat(<cap>,<num>),<vow>}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation at least 3 times") (targetValue (name "?{concat(repeatatleast(<let>,3),<!>)}")))
(example (utterance "items with a character preceding leftquoatation ! rightquoatation at least 7 times .") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines where a letter appears 5 or more times followed by a vowel") (targetValue (name "?{repeatatleast(<let>,5),<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , 3 or more times followed by a lower-case letter") (targetValue (name "?{repeatatleast(<!>,3),<low>}")))
(example (utterance "lines with a vowel before a capital , a lower-case , and a character") (targetValue (name "?{<vow>,and(and(<cap>,<low>),<any>)}")))
(example (utterance "lines that contain a lower case letter , a number , and a vowel") (targetValue (name "?{and(and(<low>,<num>),<vow>)}")))
(example (utterance "lines with words a long with a vowel or leftquoatation ! rightquoatation before any lower case letter") (targetValue (name "?{or(<vow>,<!>),<low>}")))
(example (utterance "lines ending with character followed by letter") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines with words without leftquoatation ! rightquoatation") (targetValue (name "?{not(<!>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or either a number , character , or letter") (targetValue (name "?{or(<!>,or(or(<num>,<any>),<let>))}")))
(example (utterance "lines with a letter before a number") (targetValue (name "?{repeatatleast(<let>,1),not(<num>)}")))
(example (utterance "lines with less than 6 characters or with a lower-case letter") (targetValue (name "?{or(repeatatleast(<any>,6),<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation a letter before a number") (targetValue (name "?{or(<!>,<cap>),<num>}")))
(example (utterance "lines with words that contain vowel before a character") (targetValue (name "?{contain(<vow>),<any>}")))
(example (utterance "lines with a letter before a lower-case letter then character") (targetValue (name "?{concat(<let>,<low>),<any>}")))
(example (utterance "lines that have words containing a character , a number or at least 1 capital letter") (targetValue (name "?{or(or(<any>,<num>),<cap>)}")))
(example (utterance "lines with a number or a vowel , at least 3 times") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines with a letter that is before a capital letter , vowel , and leftquoatation ! rightquoatation .") (targetValue (name "?{<let>,and(and(<cap>,<vow>),<!>)}")))
(example (utterance "lines containing either leftquoatation ! rightquoatation , a capital letter , or a number") (targetValue (name "?{or(or(<!>,<cap>),<num>)}")))
(example (utterance "lines with words with a number or string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{<num>,concat(<!>,<low>)}")))
(example (utterance "lines with 3 or more of leftquoatation ! rightquoatation before having a letter or string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,3),or(<let>,<@>)}")))
(example (utterance "items with a character preceding a terminating small letter .") (targetValue (name "?{<any>,endwith(<low>)}")))
(example (utterance "lines with a lower-case letter before a character") (targetValue (name "?{or(<let>,<low>),<any>}")))
(example (utterance "lines ending in a letter before string leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{or(concat(<let>,<!>),<low>)}")))
(example (utterance "lines that don't begin with a capital letter or vowel") (targetValue (name "?{startwith(or(<cap>,<vow>))}")))
(example (utterance "lines with 4 or more of a letter before leftquoatation ! rightquoatation") (targetValue (name "?{<let>,repeatatleast(<!>,4)}")))
(example (utterance "lines with a capital letter or a lower-case letter , 7 or more times in them") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation or a capital") (targetValue (name "?{or(concat(<!>,<@>),<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a capital letter after a vowel") (targetValue (name "?{<vow>,and(<!>,<cap>)}")))
(example (utterance "items without an upper case letter , a vowel , or a numeral .") (targetValue (name "?{or(or(<vow>,<num>),<cap>)}")))
(example (utterance "lines with vowel before letter or number") (targetValue (name "?{<vow>,or(<let>,<num>)}")))
(example (utterance "lines with a capital letter before anything but a vowel") (targetValue (name "?{<cap>,not(<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a lower-case letter") (targetValue (name "?{<!>,<low>}")))
(example (utterance "lines not having a capital letter and without  leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before number without string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<num>),not(<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation with a capital letter before leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<cap>),<#>}")))
(example (utterance "leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),<@>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a vowel .") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines starting with words and a vowel before a lower-case letter") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines not ending with a word containing a vowel") (targetValue (name "?{endwith(contain(<vow>))}")))
(example (utterance "lines do not contain a lower-case letter , nor a capital letter , nor a character") (targetValue (name "?{and(and(<low>,<cap>),<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or leftquoatation @ rightquoatation , or a vowel that comes after a number") (targetValue (name "?{or(or(<!>,<@>),<num>),<vow>}")))
(example (utterance "lines containing a character preceded by either an upper or lowercase letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<low>),<!>),<any>}")))
(example (utterance "lines with words and only a capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines ending with character or vowel followed by string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<any>),concat(<vow>,<!>)}")))
(example (utterance "lines with words , a number , and vowel") (targetValue (name "?{and(<num>,<vow>)}")))
(example (utterance "lines with words with a character before a letter at least once in them") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines without a character , a number , or a lower-case letter") (targetValue (name "?{and(and(<any>,<num>),<low>)}")))
(example (utterance "lines with 7 or more of a letter before a number") (targetValue (name "?{contain(concat(<let>,<num>))}")))
(example (utterance "lines without leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{startwith(or(<!>,<cap>))}")))
(example (utterance "lineswith words with a lower-case letter or a character") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines ending with not leftquoatation ! rightquoatation or number") (targetValue (name "?{or(not(<!>),<num>)}")))
(example (utterance "lines containing 6 more more uppercase letters") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines that don't end with leftquoatation ! rightquoatation more than 3 times") (targetValue (name "?{endwith(repeatatleast(<!>,3))}")))
(example (utterance "lines having either a lower-case letter , string leftquoatation ! rightquoatation , or a capital  before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<low>,<!>),<cap>),<@>}")))
(example (utterance "lines starting in 2 or more of the  string leftquoatation ! rightquoatation or letter") (targetValue (name "?{or(startwith(<!>),<let>)}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation or number at least once") (targetValue (name "?{concat(<vow>,<!>),repeatatleast(<num>,1)}")))
(example (utterance "lines containing a capital letter minimum 1 time or 1 of a number , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<cap>,1),or(or(<num>,<!>),<@>)}")))
(example (utterance "lines where leftquoatation ! rightquoatation is not followed by leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),star(<@>)}")))
(example (utterance "lines having a lower case letter followed by a vowel before a capital letter") (targetValue (name "?{concat(<low>,<vow>),star(<cap>)}")))
(example (utterance "lines starting with a lower-case and capital letter , followed by a number") (targetValue (name "?{startwith(<low>),concat(<cap>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<num>,<@>),<@>)}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{<any>,and(and(<!>,<@>),<#>)}")))
(example (utterance "lines with a number , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<vow>),<!>)}")))
(example (utterance "lines with a capital letter or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{or(or(<cap>,<!>),<@>),<vow>}")))
(example (utterance "line where a number , a letter , and a capital letter come after leftquoatation ! rightquoatation") (targetValue (name "?{<!>,and(and(<let>,<num>),<cap>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation and that starts with a letter") (targetValue (name "?{concat(<low>,<!>),startwith(<let>)}")))
(example (utterance "lines with a character , then a lower-case letter and a capital letter") (targetValue (name "?{<any>,and(<low>,<cap>)}")))
(example (utterance "lines not words with lower-case letter before vowel") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines with words and a number or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines that do not have the word leftquoatation ! rightquoatation , a capital letter or a character") (targetValue (name "?{or(or(<!>,<any>),<cap>)}")))
(example (utterance "lines which have  a vowel before either leftquoatation ! rightquoatation , a capital letter , or a number") (targetValue (name "?{<vow>,or(or(<!>,<cap>),<num>)}")))
(example (utterance "lines with 6 or more of a lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{contain(concat(<low>,<!>))}")))
(example (utterance "lines with a character or leftquoatation ! rightquoatation , after a lower-case letter") (targetValue (name "?{or(concat(<low>,<any>),<!>)}")))
(example (utterance "items with a numeral preceeding a character at least 7 times .") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation , 4 or more times") (targetValue (name "?{repeatatleast(concat(<!>,<@>),4)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a character , or leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<@>,1),concat(<!>,<any>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{and(<!>,<@>),star(<#>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a character , or a letter") (targetValue (name "?{or(or(<!>,<any>),<let>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceding a character") (targetValue (name "?{<!>,<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and a capital letter") (targetValue (name "?{<!>,and(<@>,<cap>)}")))
(example (utterance "lines with a character 3 times") (targetValue (name "?{<vow>,repeatatleast(<any>,3)}")))
(example (utterance "lines with a letter before they end with a character") (targetValue (name "?{<let>,endwith(<any>)}")))
(example (utterance "lines with a number followed by leftquoatation ! rightquoatation before a lower-case letter in them") (targetValue (name "?{concat(<num>,<!>),<low>}")))
(example (utterance "lines containing either a capital , lower-case letter , or number") (targetValue (name "?{or(or(<cap>,<low>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , at least 2 times or a lower-case letter with a character after") (targetValue (name "?{repeatatleast(<!>,2),concat(<low>,<any>)}")))
(example (utterance "lines with a capital letter and leftquoatation ! rightquoatation or at least 1 lower-case letter") (targetValue (name "?{or(and(<cap>,<!>),<low>)}")))
(example (utterance "lines containing a vowel or a capital letter 1 or more times") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "lines without a number , letter , or capital letter") (targetValue (name "?{and(and(<num>,<let>),<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation or numeral .") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines containing a capital and lower case letter with leftquoatation ! rightquoatation") (targetValue (name "?{and(<!>,<cap>),<low>}")))
(example (utterance "lines with only less than 3 of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,4)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{or(concat(<!>,<@>),<#>)}")))
(example (utterance "items with a small letter preceding a terminating numeral .") (targetValue (name "?{<low>,endwith(<num>)}")))
(example (utterance "lines with a vowel followed by a number and a letter") (targetValue (name "?{<vow>,concat(<num>,<let>)}")))
(example (utterance "lines containing words with a character at least 3 times") (targetValue (name "?{<any>}")))
(example (utterance "lines not containing a capital letter nor leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{concat(or(<cap>,<!>),<@>)}")))
(example (utterance "lines with capital letter followed by letter , 6 or more times") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines without a character before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(repeatatleast(<any>,1),<!>)}")))
(example (utterance "lines with no leftquoatation ! rightquoatation or number before a lower-case letter") (targetValue (name "?{concat(or(<!>,<num>),<low>)}")))
(example (utterance "lines that contain5or more combinations consisting of the word leftquoatation ! rightquoatation followed by the word leftquoatation @ rightquoatation") (targetValue (name "?{<!>,repeatatleast(<@>,5)}")))
(example (utterance "lines without a vowel or with a lower-case letter") (targetValue (name "?{not(<vow>),contain(<low>)}")))
(example (utterance "lines not having a vowel , lower-case letter , or number") (targetValue (name "?{or(or(<vow>,<low>),<num>)}")))
(example (utterance "lines with less than 6 vowels") (targetValue (name "?{repeatatleast(<vow>,6)}")))
(example (utterance "items without leftquoatation ! rightquoatation , a character , or a vowel .") (targetValue (name "?{and(and(<!>,<any>),<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a letter .") (targetValue (name "?{and(and(<@>,<let>),<!>)}")))
(example (utterance "lines that either have a letter in lower-case or a character preceded by a letter") (targetValue (name "?{or(<low>,<let>),<any>}")))
(example (utterance "lines with 7 or more of leftquoatation ! rightquoatation or a vowel before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,7),concat(<vow>,<@>)}")))
(example (utterance "items terminating with leftquoatation ! rightquoatation at least 1 time .") (targetValue (name "?{endwith(or(<!>,<any>))}")))
(example (utterance "lines that do not contain a number , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<!>),<vow>)}")))
(example (utterance "lines ending in vowel or capital letter") (targetValue (name "?{contain(or(<vow>,<cap>))}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation preceding a letter .") (targetValue (name "?{<!>,concat(<@>,<let>)}")))
(example (utterance "lines that have only the word leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{repeatatleast(<!>,1),<let>}")))
(example (utterance "lines with 3 or more of a lower-case letter before number") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation before a character or string leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<!>,<any>),<@>)}")))
(example (utterance "lines that begin with a lower-case letter or a capital letter , at least once") (targetValue (name "?{or(repeatatleast(<low>,1),<cap>)}")))
(example (utterance "lines ending in a character and a capital letter") (targetValue (name "?{endwith(<any>),repeatatleast(<cap>,1)}")))
(example (utterance "lines that end with a lower case letter or a number") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines with lower case letter at least once or either leftquoatation ! rightquoatation , a character , or leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<low>,1),or(or(<!>,<any>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number and ending with leftquoatation @ rightquoatation in them") (targetValue (name "?{concat(<!>,<num>),endwith(<@>)}")))
(example (utterance "lines with a number before a letter , leftquoatation ! rightquoatation , and vowel") (targetValue (name "?{<num>,and(and(<let>,<!>),<vow>)}")))
(example (utterance "lines with words having either leftquoatation ! rightquoatation , a number , or a capital letter") (targetValue (name "?{or(<!>,<num>),<cap>}")))
(example (utterance "items terminating with a character .") (targetValue (name "?{contain(<any>)}")))
(example (utterance "items with a small letter followed by any letter .") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines ending with words with number at least once") (targetValue (name "?{repeatatleast(<num>,1)}")))
(example (utterance "lines without a number in them or that end with the leftquoatation ! rightquoatation string") (targetValue (name "?{or(<num>,endwith(<!>))}")))
(example (utterance "items with a letter , a character , or a numeral .") (targetValue (name "?{or(or(<num>,<any>),<let>)}")))
(example (utterance "lines which are not having a capital letter before containing a letter") (targetValue (name "?{not(<cap>),contain(<let>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation or a lower-case letter , at least 6 times") (targetValue (name "?{or(repeatatleast(<low>,6),<!>)}")))
(example (utterance "lines with a letter before a number , or at least 1 leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<let>,<num>),<!>)}")))
(example (utterance "lines with a capital letter or leftquoatation ! rightquoatation 1 or more times") (targetValue (name "?{repeatatleast(or(<cap>,<!>),1)}")))
(example (utterance "lines not ending with a letter") (targetValue (name "?{contain(endwith(<let>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 3 or more times before a character in them") (targetValue (name "?{repeatatleast(<!>,3),<any>}")))
(example (utterance "lines where leftquoatation ! rightquoatation appears before a character") (targetValue (name "?{<!>,and(and(<let>,<any>),<low>)}")))
(example (utterance "lines that do not contain a character , a number , or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<num>),<!>)}")))
(example (utterance "lines with words with more than 1 letter") (targetValue (name "?{<let>}")))
(example (utterance "lines with a capital letter , character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<any>),<!>)}")))
(example (utterance "lines containing a number or vowel , either zero times or more") (targetValue (name "?{contain(<num>),star(<vow>)}")))
(example (utterance "lines with words with a letter , number , and vowel at least once") (targetValue (name "?{and(and(<let>,<num>),<vow>)}")))
(example (utterance "lines with a vowel leftquoatation ! rightquoatation once") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines containing a capital letter , a vowel , and a number") (targetValue (name "?{and(<num>,<vow>),<cap>}")))
(example (utterance "lines containing a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<!>,<low>),<let>)}")))
(example (utterance "lines with letter before , not with a character .") (targetValue (name "?{<let>,not(<any>)}")))
(example (utterance "lines containing either a character , a number , or 'dog , 5 times or more") (targetValue (name "?{contain(or(or(<any>,<num>),<!>))}")))
(example (utterance "lines with lower-case letter followed by capital") (targetValue (name "?{<low>,<cap>}")))
(example (utterance "lines with a capital letter following a letter at least 4 times") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines with a vowel before ending with 5 or more letters") (targetValue (name "?{<vow>,repeatatleast(endwith(<let>),5)}")))
(example (utterance "lines which are having leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a number before a lower-case letter") (targetValue (name "?{and(and(<!>,<@>),<num>),<low>}")))
(example (utterance "a vowel appears 7 or more times before leftquoatation ! rightquoatation in the lines") (targetValue (name "?{<vow>,repeatatleast(<!>,7)}")))
(example (utterance "lines ending in only leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),<@>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before a character or number at least once") (targetValue (name "?{concat(<!>,<any>),repeatatleast(<num>,1)}")))
(example (utterance "lines in which a capital letter appears between a letter and leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<cap>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a lower-case letter , and a capital letter after a character") (targetValue (name "?{<any>,and(and(<!>,<low>),<cap>)}")))
(example (utterance "vowel or a character appears zero or more times in the lines") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines with a letter ending with 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{<let>,repeatatleast(endwith(<!>),3)}")))
(example (utterance "lines that have a letter or leftquoatation ! rightquoatation 7 or more times") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines without a capital letter followed by a character") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines that does not have a letter or that starts with string leftquoatation ! rightquoatation") (targetValue (name "?{not(<let>),startwith(<!>)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation or a capital , zero or more times") (targetValue (name "?{or(endwith(<!>),<cap>)}")))
(example (utterance "lines containing a capital letter and leftquoatation ! rightquoatation zero or more times") (targetValue (name "?{<cap>,star(<!>)}")))
(example (utterance "lines without a character or letter") (targetValue (name "?{or(or(<any>,<cap>),<let>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation followed by a vowel") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "words with only lower-case letter appears 3 or more times in the lines\\") (targetValue (name "?{<low>}")))
(example (utterance "lines containing a lower-case letter or number") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "lines which are containing a number and leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{contain(<num>),star(<!>)}")))
(example (utterance "lines with or without a character , followed by a vowel") (targetValue (name "?{star(contain(<any>)),<vow>}")))
(example (utterance "lines which contain words with leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{<!>,<num>}")))
(example (utterance "lines with a capital letter and character before vowel") (targetValue (name "?{and(<cap>,<any>),<vow>}")))
(example (utterance "lines starting with a number or leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines which are ending with a vowel followed by a number before a letter") (targetValue (name "?{endwith(<any>),star(<let>)}")))
(example (utterance "lines either having a vowel , a lower-case letter or the word leftquoatation ! rightquoatation before a character") (targetValue (name "?{or(or(<vow>,<low>),<!>),<any>}")))
(example (utterance "items with leftquoatation ! rightquoatation following a letter or a character .") (targetValue (name "?{or(or(<cap>,<low>),<any>),<!>}")))
(example (utterance "line with 5 or more numbers") (targetValue (name "?{<num>}")))
(example (utterance "the lines which have a vowel or character followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<any>),<!>}")))
(example (utterance "lines with a character , a lower-case letter , and a capital letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<low>),<cap>),<!>}")))
(example (utterance "lines with at least 6 vowels before a lower-case letter") (targetValue (name "?{<vow>,repeatatleast(<low>,6)}")))
(example (utterance "lines that have a character or end with a vowel , a minimum of 1 time") (targetValue (name "?{or(<any>,endwith(<vow>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before lower-case letter , string leftquoatation @ rightquoatation , and vowel") (targetValue (name "?{<!>,and(and(<low>,<@>),<vow>)}")))
(example (utterance "lines with 5 characters followed by a capital letter") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines with lower-case letter followed by capital letter , 4 or more times") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation followed by a letter occurring zero or more times") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with words and a number , capital letter , and character") (targetValue (name "?{and(<num>,<cap>),<any>}")))
(example (utterance "lies with a letter at least once with a vowel .") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "letters with lines before a number") (targetValue (name "?{<let>,<num>}")))
(example (utterance "lines not containing only a letter before lower-case letter") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "items with a vowel preceding a numeral .") (targetValue (name "?{<vow>,star(<num>)}")))
(example (utterance "lines with a character before capital letter and string leftquoatation ! rightquoatation") (targetValue (name "?{<any>,and(<cap>,<!>)}")))
(example (utterance "lines that have a number then ending with a  lower case letter") (targetValue (name "?{endwith(<low>),<num>}")))
(example (utterance "lines containing a number") (targetValue (name "?{<num>}")))
(example (utterance "lines containing a capital after a letter") (targetValue (name "?{<let>,contain(<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceded by a lower-case letter and a capital letter") (targetValue (name "?{and(<low>,<cap>),<!>}")))
(example (utterance "lines that have a capital letter before some other number or character") (targetValue (name "?{<cap>,not(<let>)}")))
(example (utterance "lines containing a vowel preceding a number") (targetValue (name "?{<vow>,<num>}")))
(example (utterance "items with a numeral , leftquoatation ! rightquoatation , or uppercase letter preceding leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<num>,<!>),<cap>),<@>}")))
(example (utterance "lines containing either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a number followed by a character") (targetValue (name "?{or(or(<!>,<@>),<num>),<any>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before number or either string leftquoatation @ rightquoatation , a letter , or character") (targetValue (name "?{concat(<!>,<num>),or(or(<@>,<let>),<any>)}")))
(example (utterance "lines with a letter or number") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines without a character , vowel , or a letter") (targetValue (name "?{and(and(<any>,<let>),<vow>)}")))
(example (utterance "lines that do not have a capital letter before leftquoatation ! rightquoatation") (targetValue (name "?{contain(concat(<!>,<cap>))}")))
(example (utterance "lines with only leftquoatation ! rightquoatation , 5 times or more") (targetValue (name "?{repeatatleast(<!>,5)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation and not string leftquoatation @ rightquoatation , zero or more times") (targetValue (name "?{and(<!>,star(<@>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before 1 or more of leftquoatation @ rightquoatation") (targetValue (name "?{<!>,repeatatleast(<@>,1)}")))
(example (utterance "items with a numeral or a vowel preceding leftquoatation ! rightquoatation preceding a letter .") (targetValue (name "?{or(or(<num>,<vow>),<!>),<let>}")))
(example (utterance "liens without leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines ending with a vowel , 4 or more times") (targetValue (name "?{endwith(repeatatleast(<vow>,4))}")))
(example (utterance "lines with a number or letter") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a capital letter") (targetValue (name "?{not(star(<!>)),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation after 2 or more numbers .") (targetValue (name "?{repeatatleast(<num>,2),<!>}")))
(example (utterance "lines containing words or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<let>),<!>}")))
(example (utterance "lines starting in string leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{or(repeatatleast(<!>,1),<cap>)}")))
(example (utterance "lines which are containing leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation before a character") (targetValue (name "?{concat(concat(<!>,<@>),<any>)}")))
(example (utterance "lines starting with words with string leftquoatation ! rightquoatation , a capital , and a vowel") (targetValue (name "?{and(and(<!>,<cap>),<vow>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation then a vowel") (targetValue (name "?{<!>,concat(<@>,<vow>)}")))
(example (utterance "lines not having a letter followed by a lower-case letter in them") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines that have a vowel and a character preceding a capital letter") (targetValue (name "?{and(<vow>,<any>),<cap>}")))
(example (utterance "lines not having a capital , letter , or vowel") (targetValue (name "?{or(or(<cap>,<let>),<vow>)}")))
(example (utterance "lines ending with a letter before leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<let>),star(<!>)}")))
(example (utterance "lines with a character before words with leftquoatation ! rightquoatation 7 times") (targetValue (name "?{<any>,repeatatleast(<!>,7)}")))
(example (utterance "lines with only 1 or more of a vowel or lower-case letter") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation and a lower-case letter") (targetValue (name "?{<!>,<low>}")))
(example (utterance "lines within which there is a vowel or a small letter and that small letter shows up at least 4 times or more") (targetValue (name "?{or(repeatatleast(<low>,4),<vow>)}")))
(example (utterance "lines hat have the word leftquoatation ! rightquoatation or a capital letter preceding a character") (targetValue (name "?{contain(<!>),concat(<cap>,<any>)}")))
(example (utterance "lines ending with lower-case letter before containing a capital") (targetValue (name "?{endwith(<low>),contain(<cap>)}")))
(example (utterance "lines starting in capital letter or lower-case letter") (targetValue (name "?{or(repeatatleast(<cap>,1),<low>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation or a character , zero or more times in them") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines not starting with letter and not a capital") (targetValue (name "?{and(startwith(<let>),<cap>)}")))
(example (utterance "lines with at least 6 numbers before a vowel") (targetValue (name "?{repeatatleast(<num>,6),<vow>}")))
(example (utterance "lines the contain either a character or a leftquoatation ! rightquoatation string at least 4 times") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "letters in lines followed by leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{concat(<let>,<!>),<cap>}")))
(example (utterance "lines containing 0 or more lower-case letters or a capital letter followed by a number") (targetValue (name "?{star(<low>),concat(<cap>,<num>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation , a lower-case , or leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{or(or(or(<!>,<low>),<@>),<#>)}")))
(example (utterance "items with words containing either leftquoatation ! rightquoatation or any letter .") (targetValue (name "?{or(<low>,<let>),<!>}")))
(example (utterance "lines with letter followed by string leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter or a lower-case letter in them") (targetValue (name "?{<!>,or(<cap>,<low>)}")))
(example (utterance "lines ending with a number after a vowel") (targetValue (name "?{endwith(<vow>),<num>}")))
(example (utterance "lines not containing 3 or more letters") (targetValue (name "?{not(repeatatleast(<let>,3))}")))
(example (utterance "lines that have a vowel before a lower-case letter and before leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),and(<low>,<!>)}")))
(example (utterance "items with words containing a character or a vowel .") (targetValue (name "?{<vow>,<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending with string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,endwith(contain(<@>))}")))
(example (utterance "lines having leftquoatation ! rightquoatation before a number and a vowel as well") (targetValue (name "?{and(concat(<!>,<num>),<vow>)}")))
(example (utterance "lines that begin with a capitalized letter .") (targetValue (name "?{<cap>}")))
(example (utterance "items with a numeral preceding an upper case letter at least6times .") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines with words with a letter , vowel , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<vow>),<!>}")))
(example (utterance "lines with letter in lower-case or before leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<let>),<!>}")))
(example (utterance "lines with a character , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<vow>),<!>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation , or leftquoatation # rightquoatation at least zero times or more") (targetValue (name "?{star(<#>),concat(<!>,<@>)}")))
(example (utterance "lines containing a character , a lower-case letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<low>),<!>}")))
(example (utterance "lines without zero or more characters") (targetValue (name "?{not(star(<any>))}")))
(example (utterance "items without any letter .") (targetValue (name "?{or(or(<let>,<low>),<cap>)}")))
(example (utterance "lines not words with ending with number") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines that contain a capital letter , a number , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<!>),<num>)}")))
(example (utterance "lines with a lower-case letter followed by a vowel at least once in them") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines which are ending with a character and leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{endwith(<any>),or(<!>,<cap>)}")))
(example (utterance "lines starting in words with a letter") (targetValue (name "?{repeatatleast(<let>,1)}")))
(example (utterance "the lines that end in a lower-case letter immediately following a capital letter") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines having a vowel , string leftquoatation ! rightquoatation , and string leftquoatation @ rightquoatation before string leftquoatation # rightquoatation") (targetValue (name "?{and(and(<vow>,<!>),<@>),<#>}")))
(example (utterance "lines that end a string that has at least 1 vowel .") (targetValue (name "?{endwith(contain(<vow>))}")))
(example (utterance "lines ending with only a character at least zero times") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines not words that contain only a capital") (targetValue (name "?{<cap>}")))
(example (utterance "lines in which a leftquoatation ! rightquoatation string , a letter that's lower-case , or a leftquoatation @ rightquoatation string are preceded by a letter") (targetValue (name "?{<let>,or(or(<!>,<low>),<@>)}")))
(example (utterance "lines woth a letter and a vowel") (targetValue (name "?{and(<let>,<vow>)}")))
(example (utterance "lines wiht leftquoatation ! rightquoatation or a lower-case letter , twice or more times") (targetValue (name "?{repeatatleast(or(<!>,<low>),2)}")))
(example (utterance "lines with a vowel before a capital letter , or leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{<vow>,or(or(<cap>,<!>),<@>)}")))
(example (utterance "lines with a letter followed by leftquoatation ! rightquoatation , 6 or more times in them") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a lower-case letter in them") (targetValue (name "?{and(and(<!>,<@>),<low>)}")))
(example (utterance "lines not beginning with a letter followed by a lower-case letter") (targetValue (name "?{not(startwith(<let>)),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , zero or more times before a letter or leftquoatation @ rightquoatation in them") (targetValue (name "?{star(<!>),or(<let>,<@>)}")))
(example (utterance "lines with 5 or more of leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines with a letter before a vowel or leftquoatation ! rightquoatation , 3 or more times in them") (targetValue (name "?{concat(<let>,<vow>),repeatatleast(<!>,3)}")))
(example (utterance "lines containing a character before a lower case letter with is before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<low>),<!>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a letter at least5times .") (targetValue (name "?{<!>,repeatatleast(<let>,5)}")))
(example (utterance "items without a terminating vowel .") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines with a number or not a letter at least 7 times") (targetValue (name "?{repeatatleast(<num>,7),not(<let>)}")))
(example (utterance "lines with 2 or more numbers followed by a capital letter") (targetValue (name "?{repeatatleast(<num>,2),<cap>}")))
(example (utterance "lines ending in zero or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{endwith(or(<!>,<@>))}")))
(example (utterance "lines that do not have a letter , the word leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(or(<let>,<!>),<any>)}")))
(example (utterance "lines with a number , leftquoatation ! rightquoatation , and a letter preceding leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<num>,<!>),<let>),<@>}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation with a letter") (targetValue (name "?{endwith(<!>),repeatatleast(<let>,1)}")))
(example (utterance "lines where leftquoatation ! rightquoatation has a number after it , if there is a letter before leftquoatation ! rightquoatation") (targetValue (name "?{<let>,concat(<!>,<num>)}")))
(example (utterance "lines containing a number , a capital letter , and a lower-case letter") (targetValue (name "?{and(<num>,<cap>),<low>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation before a character at least 4 times or more") (targetValue (name "?{<!>,repeatatleast(<any>,4)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or character before string leftquoatation @ rightquoatation , 7 or more times") (targetValue (name "?{or(<!>,<any>),repeatatleast(<@>,7)}")))
(example (utterance "items with a terminating charactered preceding an upper case letter .") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),not(<#>)}")))
(example (utterance "line with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and lower-case letter before a character") (targetValue (name "?{and(and(<!>,<@>),<low>),<any>}")))
(example (utterance "lines with 6 or more of a capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,repeatatleast(<!>,6)}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation or letter") (targetValue (name "?{<cap>,or(<!>,<let>)}")))
(example (utterance "lines with number followed by lower-case letter , 2 or more times") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines with a letter followed by 5 or more numbers") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines containing a vowel preceded by a letter .") (targetValue (name "?{startwith(<let>),<vow>}")))
(example (utterance "items with a vowel preceding a terminating character .") (targetValue (name "?{<vow>,endwith(<any>)}")))
(example (utterance "lines not words ending with vowel") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines without a letter , character , or string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<any>),<!>)}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation or vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines with words and starting with 7 or more capital letters") (targetValue (name "?{repeatatleast(<cap>,7)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a lower-case letter , zero or more times before a capital letter in them") (targetValue (name "?{star(or(<!>,<low>)),<cap>}")))
(example (utterance "lines with a number or string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{or(<num>,<!>),<vow>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or number , 4 or more times") (targetValue (name "?{concat(<!>,<@>),repeatatleast(<num>,4)}")))
(example (utterance "lines which are  ending with containing a lower-case letter followed by a character") (targetValue (name "?{contain(concat(<low>,<any>))}")))
(example (utterance "lines with a capital or number before string leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<num>),<!>}")))
(example (utterance "lines where , twice or more , a vowel is preceded by a letter") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines with a character before 2 or more numbers") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines with vowels before leftquoatation ! rightquoatation and a number") (targetValue (name "?{<vow>,and(<!>,<num>)}")))
(example (utterance "lines ending in a character before a lower-case letter") (targetValue (name "?{endwith(<any>),repeatatleast(<low>,1)}")))
(example (utterance "items with a small letter at least6times .") (targetValue (name "?{<low>}")))
(example (utterance "items with a letter preceding an upper case letter and a numeral .") (targetValue (name "?{<let>,and(<cap>,<num>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation or a lower-case letter at least once") (targetValue (name "?{or(endwith(<!>),<low>)}")))
(example (utterance "lines with no capital letter or lower-case letter") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines with a lower-case letter and letter before vowel") (targetValue (name "?{and(<low>,<let>),<vow>}")))
(example (utterance "lines ending , at least 3 times , with either a capital letter or a vowel") (targetValue (name "?{endwith(<cap>),repeatatleast(<vow>,3)}")))
(example (utterance "lines that have words with either leftquoatation ! rightquoatation , a capital letter , or leftquoatation @ rightquoatation") (targetValue (name "?{or(<@>,<!>),<cap>}")))
(example (utterance "lines with a vowel followed by a letter and a capital letter") (targetValue (name "?{<vow>,and(<let>,<cap>)}")))
(example (utterance "lines not having a capital letter followed by a vowel") (targetValue (name "?{concat(<cap>,concat(<any>,<vow>))}")))
(example (utterance ": lies with lower-case letter before string leftquoatation ! rightquoatation or ending with vowel") (targetValue (name "?{concat(<low>,<!>),endwith(<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a character preceding an upper case letter .") (targetValue (name "?{concat(<!>,<any>),contain(<cap>)}")))
(example (utterance "lines with a letter or vowel and lower-case letter") (targetValue (name "?{and(or(<let>,<vow>),<low>)}")))
(example (utterance "items beginning with a character preceding an upper case letter .") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "a capital letter and a number in a line") (targetValue (name "?{<cap>,<num>}")))
(example (utterance "lines in which a letter comes before a capital letter") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines after the string string dog with a lower-case letter and a vowel") (targetValue (name "?{and(<low>,<vow>),<!>}")))
(example (utterance "lines with 7 or more capital letters before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<cap>,7),<!>}")))
(example (utterance "lines that have a letter before a vowel at least once .") (targetValue (name "?{<let>,contain(repeatatleast(<vow>,1))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation after a capital letter") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines containing a lower-case letter or a capital letter") (targetValue (name "?{<low>,<cap>}")))
(example (utterance "lines starting with character before number") (targetValue (name "?{startwith(<any>),<num>}")))
(example (utterance "lines with words without a letter or vowel") (targetValue (name "?{and(<let>,<vow>)}")))
(example (utterance "lines starting in leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),contain(<@>)}")))
(example (utterance "lines that have a character preceding the word leftquoatation ! rightquoatation followed by a capital letter") (targetValue (name "?{<any>,concat(<!>,<cap>)}")))
(example (utterance "lines with zero or less of a vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{not(<vow>),star(<!>)}")))
(example (utterance "lines without lower-case letters") (targetValue (name "?{<low>}")))
(example (utterance "lines having either a capital letter , a number , or a lower-case letter or a letter , 4 or more times in them") (targetValue (name "?{or(or(or(<cap>,<num>),<low>),<let>)}")))
(example (utterance "number or lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<low>),<!>}")))
(example (utterance "lines which have a capital letter before ending with a letter") (targetValue (name "?{<cap>,endwith(<let>)}")))
(example (utterance "lines with capital letter followed by number before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<num>),<!>}")))
(example (utterance "lines containing words with a letter and a character minimum of 1 time") (targetValue (name "?{and(<let>,<any>)}")))
(example (utterance "lines with a capital letter or lower-case letter") (targetValue (name "?{or(or(<cap>,<low>),<let>)}")))
(example (utterance "line with words starting with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),<@>}")))
(example (utterance "lines beginning with a character followed by a lowercase letter") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , and a lower-case letter before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<vow>,<!>),<low>),<@>}")))
(example (utterance "lines containing a capital letter preceding a letter before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<let>),<!>}")))
(example (utterance "lines not having a character , a capital , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<cap>),<!>)}")))
(example (utterance "lines with words with vowel or containing a capital") (targetValue (name "?{<vow>,contain(<cap>)}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation or letter") (targetValue (name "?{<vow>,or(<!>,<let>)}")))
(example (utterance "lines containing words having either a capital letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<vow>),<!>}")))
(example (utterance "items with an upper case letter preceding leftquoatation ! rightquoatation at least6times .") (targetValue (name "?{<low>,repeatatleast(<!>,6)}")))
(example (utterance "lines with words with ending with leftquoatation ! rightquoatation , occurring 3 or more times") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "items with words containing leftquoatation ! rightquoatation , a character , or a numeral .") (targetValue (name "?{or(<num>,<!>),<any>}")))
(example (utterance "lines containing a vowel string leftquoatation ! rightquoatation zero or more times") (targetValue (name "?{contain(or(<vow>,<!>))}")))
(example (utterance "lines finishing hwith leftquoatation ! rightquoatation at least once followed by a vowel") (targetValue (name "?{endwith(repeatatleast(<!>,1)),<vow>}")))
(example (utterance "lines with words without a vowel , capital letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<cap>),<!>)}")))
(example (utterance "lines which are starting with words with not the string 'dog") (targetValue (name "?{not(<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a numeral preceding leftquoatation @ rightquoatation .") (targetValue (name "?{<!>,concat(<num>,<@>)}")))
(example (utterance "lines which are containing a lower-case letter and a character before leftquoatation ! rightquoatation") (targetValue (name "?{concat(and(<low>,<any>),<!>)}")))
(example (utterance "items with a letter preceding leftquoatation ! rightquoatation , a small letter , and leftquoatation @ rightquoatation .") (targetValue (name "?{<let>,and(and(<!>,<low>),<@>)}")))
(example (utterance "lines that have a lower-case letter , and must not have the word leftquoatation ! rightquoatation") (targetValue (name "?{contain(<low>),not(<!>)}")))
(example (utterance "lines with a letter before a capital letter") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines with a number before character then string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(concat(<num>,<any>),1),<!>}")))
(example (utterance "lines with a vowel or a number before leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<vow>),<num>),<!>}")))
(example (utterance "lines which are not having a letter and a number , 2 or more times") (targetValue (name "?{not(<let>),repeatatleast(<num>,2)}")))
(example (utterance "lines having a character , capital letter , and string leftquoatation ! rightquoatation before number") (targetValue (name "?{and(and(<any>,<cap>),<!>),<num>}")))
(example (utterance "lines with a number before a character then string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,concat(<any>,<!>)}")))
(example (utterance "lines which have words with a capital letter and leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<cap>,or(<!>,<@>)}")))
(example (utterance "lines that have 3 words , each with a capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines with a letter , 4 or more times before a vowel in them") (targetValue (name "?{repeatatleast(<let>,4),<vow>}")))
(example (utterance "lines without a character  , leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{or(or(<any>,<!>),<low>)}")))
(example (utterance "items terminating with a numeral or an upper case letter .") (targetValue (name "?{or(<num>,<cap>)}")))
(example (utterance "lines with a lower-case letter , capital letter , and number before a character") (targetValue (name "?{and(and(<low>,<cap>),<num>),<any>}")))
(example (utterance "lines that don't have a letter nor a character") (targetValue (name "?{and(<let>,<any>)}")))
(example (utterance "the lines that have either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a vowel") (targetValue (name "?{or(or(<!>,<@>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel , string leftquoatation @ rightquoatation , or letter") (targetValue (name "?{<!>,or(or(<vow>,<@>),<let>)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation followed by a character which is  then followed by a letter") (targetValue (name "?{concat(<any>,<let>),<!>}")))
(example (utterance "lines that have 3 or more numbers or capital letters") (targetValue (name "?{or(<num>,<cap>)}")))
(example (utterance "lines with a capital letter or ending with a number , zero or more times in them") (targetValue (name "?{or(<cap>,endwith(<num>))}")))
(example (utterance "lines containing words that have either a lower-case letter , a capital letter , or a vowel") (targetValue (name "?{or(<low>,<cap>),<vow>}")))
(example (utterance "capital letters appears 2 or more times in the lines") (targetValue (name "?{<cap>}")))
(example (utterance "lines that have a vowel followed by the word leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{<vow>,or(or(<!>,<@>),<#>)}")))
(example (utterance "lines that don't end with captial letter words") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines ending with a lower-case letter before a vowel") (targetValue (name "?{endwith(<low>),star(<vow>)}")))
(example (utterance "lines with 4 or more of a lower-case letter with string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<low>,4),or(<!>,<@>)}")))
(example (utterance "lines not beginning with a number or leftquoatation ! rightquoatation") (targetValue (name "?{or(not(<num>),<!>)}")))
(example (utterance "lines with a capital letter after a letter at least 7 times") (targetValue (name "?{repeatatleast(<let>,7),<cap>}")))
(example (utterance "lines ending in words with a letter before vowel") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines containing lower-case letter or 4") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines with number before 'dog") (targetValue (name "?{contain(<num>),<!>}")))
(example (utterance "lines with 4 or more letters before a character") (targetValue (name "?{repeatatleast(<let>,4),<any>}")))
(example (utterance "lines with a letter before vowel then string leftquoatation ! rightquoatation") (targetValue (name "?{<let>,concat(<vow>,<!>)}")))
(example (utterance "lines with a capital letter , 5 or more times before a vowel in them") (targetValue (name "?{repeatatleast(<cap>,5),<vow>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation or with a number .") (targetValue (name "?{contain(<num>),startwith(<!>)}")))
(example (utterance "lines that have words containing a vowel and a letter") (targetValue (name "?{<vow>,<let>}")))
(example (utterance "lines with a number , string leftquoatation ! rightquoatation , and letter before a character") (targetValue (name "?{and(and(<num>,<!>),<let>),<any>}")))
(example (utterance "lines that contain only a capital letter followed by leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{star(<cap>),<!>}")))
(example (utterance "lines that have a capital letter , the word leftquoatation ! rightquoatation , and a lower-case letter") (targetValue (name "?{and(and(<cap>,<low>),<!>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{repeatatleast(<!>,6)}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{repeatatleast(or(<!>,<cap>),1)}")))
(example (utterance "lines with an upper-case letter showing up ahead of a character") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines containing words with string leftquoatation ! rightquoatation and character") (targetValue (name "?{and(<!>,<any>)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation and either a vowel , letter , or number") (targetValue (name "?{endwith(<!>),or(or(<vow>,<let>),<num>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation and a lower-case letter") (targetValue (name "?{contain(and(<!>,<low>))}")))
(example (utterance "lines that end with words with a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines having either a character , a number , or a lower-case letter before a vowel in them") (targetValue (name "?{or(or(<any>,<num>),<low>),<vow>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before 1 or more of a capital letter") (targetValue (name "?{<!>,contain(repeatatleast(<let>,1))}")))
(example (utterance "items terminating with a small letter , at least6times .") (targetValue (name "?{repeatatleast(<low>,6)}")))
(example (utterance "lines with a character and the string 'dog that have a vowel before them") (targetValue (name "?{<any>,and(<vow>,<!>)}")))
(example (utterance "lines starting with a number and capital letter leftquoatation ! rightquoatation") (targetValue (name "?{or(and(<num>,<!>),<cap>)}")))
(example (utterance "lines that at least once do not end with the word leftquoatation ! rightquoatation") (targetValue (name "?{endwith(repeatatleast(<!>,1))}")))
(example (utterance "lines with lower-case letter or letter , 6 or more times") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines with words containing a lowercase letter , an uppercase letter and leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<cap>),<!>}")))
(example (utterance "lines which are containing words with not a number") (targetValue (name "?{not(<num>)}")))
(example (utterance "lines with5or more words without characters") (targetValue (name "?{repeatatleast(<any>,5)}")))
(example (utterance "items without a small letter preceding a vowel .") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines starting in a lower-case letter before a number") (targetValue (name "?{repeatatleast(concat(<low>,<num>),1)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation or capital letter , 4 or more times") (targetValue (name "?{repeatatleast(or(<!>,<cap>),4)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , letter , or character before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<let>),<any>),<@>}")))
(example (utterance "lines where no words start with a vowel") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines with a vowel before capital letter or letter") (targetValue (name "?{<vow>,or(<cap>,<let>)}")))
(example (utterance "lines not containing a number or character") (targetValue (name "?{contain(or(<num>,<any>))}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation followed by letter , 7 or more times") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with words that don't have the sting leftquoatation ! rightquoatation before a lower case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or letter at least once") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before a capital letter and a number before string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<cap>),concat(<num>,<@>)}")))
(example (utterance "lines not having a vowel and string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation") (targetValue (name "?{not(<vow>),concat(<!>,<@>)}")))
(example (utterance "lines that have a number and letter followed by a vowel") (targetValue (name "?{and(<num>,<let>),<vow>}")))
(example (utterance "lines with a character , string leftquoatation ! rightquoatation , and a number before a capital") (targetValue (name "?{and(and(<any>,<!>),<num>),<cap>}")))
(example (utterance "items with words containing a small letter , a character , or leftquoatation ! rightquoatation .") (targetValue (name "?{or(<low>,<any>),<!>}")))
(example (utterance "lines having a character , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation before leftquoatation # rightquoatation in them") (targetValue (name "?{and(and(<any>,<!>),<@>),<#>}")))
(example (utterance "lines with only a character") (targetValue (name "?{<any>}")))
(example (utterance "lines with words with lower-case letter , a letter , and number") (targetValue (name "?{and(<low>,<let>),<num>}")))
(example (utterance "lines containing only string leftquoatation ! rightquoatation or letter , 6 or more times") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a letter , and string leftquoatation # rightquoatation") (targetValue (name "?{<!>,and(and(<@>,<let>),<#>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation and a vowel , zero or more times in it") (targetValue (name "?{and(<!>,<vow>)}")))
(example (utterance "lines with a letter or number .") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 'truck or leftquoatation @ rightquoatation before a capital letter") (targetValue (name "?{or(or(<!>,<@>),<@>),<cap>}")))
(example (utterance "items with a small letter , leftquoatation ! rightquoatation , and a character preceding a vowel .") (targetValue (name "?{and(and(<any>,<low>),<!>),<vow>}")))
(example (utterance "lines starting with a letter or a lower-case letter") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "items with a small letter preceding an upper case letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation .") (targetValue (name "?{<low>,and(and(<!>,<cap>),<@>)}")))
(example (utterance "lines that end in lower case twice or more") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines having either a capital , string leftquoatation ! rightquoatation , or string leftquoatation @ rightquoatation before string leftquoatation # rightquoatation") (targetValue (name "?{or(or(<cap>,<!>),<@>),<#>}")))
(example (utterance "items with a small letter preceding leftquoatation ! rightquoatation and a numeral .") (targetValue (name "?{<low>,and(<!>,<num>)}")))
(example (utterance "lines that being with either a letter or character") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines that have a lower-case letter followed by leftquoatation ! rightquoatation , at least 1 time") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines that have both a vowel and a number occur before leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<num>),<!>}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation and 'truck and a number") (targetValue (name "?{<low>,and(and(<!>,<@>),<num>)}")))
(example (utterance "lines ending with a vowel , zero or more times") (targetValue (name "?{star(contain(<vow>))}")))
(example (utterance "lower-case letter or a letter after lines with a number .") (targetValue (name "?{<num>,or(<low>,<let>)}")))
(example (utterance "lines which are ending with words with a capital letter at least once") (targetValue (name "?{repeatatleast(<cap>,1)}")))
(example (utterance "lines with a capital letter after 5 or more of string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,5),<cap>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a capital and lower-case letter") (targetValue (name "?{and(<!>,<cap>),<low>}")))
(example (utterance "lines with words with a letter or a capital letter and leftquoatation ! rightquoatation in them") (targetValue (name "?{or(<let>,<cap>),<!>}")))
(example (utterance "lines with a letter followed by a character or a number at least once in them") (targetValue (name "?{or(concat(<let>,<any>),<num>)}")))
(example (utterance "lines containing words having a lower-case letter once or more with leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,1),<!>}")))
(example (utterance "lines where a number doesn't come before a character") (targetValue (name "?{<num>,not(<any>)}")))
(example (utterance "lines with a vowel before number or lower-case letter") (targetValue (name "?{<vow>,or(<num>,<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a capital letter or leftquoatation @ rightquoatation in them") (targetValue (name "?{and(<!>,or(<cap>,<@>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation then leftquoatation # rightquoatation") (targetValue (name "?{contain(concat(<!>,<@>)),<#>}")))
(example (utterance "lines with a lower-case letter , a number , and a letter") (targetValue (name "?{and(<num>,<let>),<low>}")))
(example (utterance "lines starting with words with letter followed by lower-case letter") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines which are not having leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a letter") (targetValue (name "?{or(or(<!>,<@>),<let>)}")))
(example (utterance "lines that do not contain a capital letter , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(or(<cap>,<!>),<any>)}")))
(example (utterance "lines with words including a vowel , number , and letter") (targetValue (name "?{and(<vow>,<num>),<let>}")))
(example (utterance "lines with words and a vowel , capital letter , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<cap>),<!>}")))
(example (utterance "lines without a lower-case letter , character , or capital letter") (targetValue (name "?{and(and(<low>,<any>),<cap>)}")))
(example (utterance "lines with words , a character , leftquoatation ! rightquoatation and a number") (targetValue (name "?{and(<any>,<!>),<num>}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation or a lower-case letter , a capital letter , and a character in them") (targetValue (name "?{<!>,and(and(<low>,<cap>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and capital letter before a lower-case letter") (targetValue (name "?{and(and(<!>,<cap>),<@>),<low>}")))
(example (utterance "lines ending with capital letter followed by lower-case letter , zero or more times") (targetValue (name "?{star(concat(<cap>,<low>))}")))
(example (utterance "lines that contain a character followed by a lower-case letter or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<any>,or(or(<!>,<@>),<low>)}")))
(example (utterance "items with a small letter and vowel preceding an upper case letter .") (targetValue (name "?{and(<low>,<vow>),<cap>}")))
(example (utterance "items with any letter , at least5times .") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , capital letter , and lower-case letter before number") (targetValue (name "?{and(and(<!>,<cap>),<low>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before character and starting in letter") (targetValue (name "?{concat(<!>,<any>),startwith(<let>)}")))
(example (utterance "a lower-case letter appears at least once before a character in the lines") (targetValue (name "?{concat(repeatatleast(<low>,1),<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation ,5or more times or beginning with a vowel") (targetValue (name "?{repeatatleast(<!>,5),startwith(<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation followed by a number") (targetValue (name "?{and(and(<!>,<@>),<#>),<num>}")))
(example (utterance "lines not having leftquoatation ! rightquoatation at least once") (targetValue (name "?{not(<!>),repeatatleast(<num>,1)}")))
(example (utterance "lines with a character in them , and then a small letter for a total of 6 times or greater") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines having a vowel before a small letter") (targetValue (name "?{<vow>,not(<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or at least 1 of leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{repeatatleast(or(<!>,<@>),1),<#>}")))
(example (utterance "lines ending with lower-case letter or letter") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines ending with a number and leftquoatation ! rightquoatation") (targetValue (name "?{and(endwith(<num>),<!>)}")))
(example (utterance "lines containing a capital , zero or more times or string leftquoatation ! rightquoatation") (targetValue (name "?{or(star(<cap>),<!>)}")))
(example (utterance "lines with a vowel , number , and string leftquoatation ! rightquoatation before capital letter") (targetValue (name "?{and(and(<vow>,<num>),<!>),<cap>}")))
(example (utterance "items with leftquoatation ! rightquoatation , a numeral , and a vowel preceding leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<vow>,<!>),<num>),<@>}")))
(example (utterance "lines with a vowel or character") (targetValue (name "?{or(contain(<vow>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation that start with a letter") (targetValue (name "?{star(<!>),startwith(<let>)}")))
(example (utterance "lines containing number followed by lower-case letter at least once") (targetValue (name "?{contain(concat(<num>,<low>))}")))
(example (utterance "lines with a character coming before a lower-case letter at least once") (targetValue (name "?{<any>,repeatatleast(<low>,1)}")))
(example (utterance "lines with 6 or more vowels followed by characters") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines with words and 3 or more of a capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,repeatatleast(<!>,3)}")))
(example (utterance "lines with a character and lower-case and capital letters") (targetValue (name "?{and(and(<any>,<low>),<cap>)}")))
(example (utterance "lines that contain a vowel followed by leftquoatation ! rightquoatation or leftquoatation @ rightquoatation , zero times or more") (targetValue (name "?{concat(<vow>,<!>),star(<@>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(<!>,contain(<any>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),concat(<@>,<#>)}")))
(example (utterance "lines containing either leftquoatation ! rightquoatation , a character or a vowel before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<any>),<vow>),<@>}")))
(example (utterance "lines with lower-case letter before 3 or more of leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<low>,<!>),repeatatleast(<@>,3)}")))
(example (utterance "lines containing 3 characters or numbers") (targetValue (name "?{or(<any>,<num>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or letter before string leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,concat(<let>,<@>))}")))
(example (utterance "lines containing character and starting with string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<any>),startwith(<!>)}")))
(example (utterance "lines with a vowel then a capital , at least 4 times") (targetValue (name "?{<vow>,repeatatleast(<cap>,4)}")))
(example (utterance "lines ending with letter , zero or more times") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines that have at least 1 occurrence of either a capital letter or a character") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines that have 3 or more combinations of the word leftquoatation ! rightquoatation followed by a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or zero or more of a vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines with letter before lower-case letter , 2 or more times") (targetValue (name "?{<let>,repeatatleast(<low>,2)}")))
(example (utterance "lines that do not have leftquoatation ! rightquoatation followed by a lower-case letter and a non-capital letter") (targetValue (name "?{and(concat(<!>,<low>),<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character or letter") (targetValue (name "?{<!>,or(<any>,<let>)}")))
(example (utterance "lines with a vowel before a letter at least once in them") (targetValue (name "?{<vow>,repeatatleast(<let>,1)}")))
(example (utterance "lines starting with a vowel before a capital , zero or more times") (targetValue (name "?{startwith(<vow>),star(<cap>)}")))
(example (utterance "lines containing containing only a lower-case letter , 5 or more times in them") (targetValue (name "?{repeatatleast(<low>,5)}")))
(example (utterance "lines that do not contain only capital letters or characters") (targetValue (name "?{and(<cap>,<any>)}")))
(example (utterance "lines without words starting in a lower-case letter") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "lines with a lower-case letter followed by leftquoatation ! rightquoatation before a number in them") (targetValue (name "?{concat(<low>,<!>),<num>}")))
(example (utterance "lines starting in words with string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a number") (targetValue (name "?{and(and(<!>,<num>),<@>)}")))
(example (utterance "lines with a vowel preceeded by a character") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "lines with 6 or more of a capital letter before a character") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines with words with either a character , number or vowel") (targetValue (name "?{or(<vow>,<num>),<any>}")))
(example (utterance "lines in which a vowel is preceded by either a number or a letter that's lower-case") (targetValue (name "?{or(<num>,<low>),<vow>}")))
(example (utterance "lines without 4 capital letters before numbers") (targetValue (name "?{concat(repeatatleast(<cap>,4),<num>)}")))
(example (utterance "all lines") (targetValue (name "?{or(<low>,star(<num>))}")))
(example (utterance "lines with a capital letter after a vowel at least 6 times") (targetValue (name "?{<vow>,repeatatleast(<cap>,6)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a lower-case letter .") (targetValue (name "?{concat(<!>,<let>),contain(<low>)}")))
(example (utterance "lines with a vowel and a number after a capital letter") (targetValue (name "?{<cap>,and(<vow>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 5 or more times before leftquoatation @ rightquoatation in them") (targetValue (name "?{repeatatleast(<!>,5),<@>}")))
(example (utterance "lines with words that don't have a letter , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<vow>),<!>)}")))
(example (utterance "lines not having a vowel") (targetValue (name "?{concat(<vow>,star(<let>))}")))
(example (utterance "lines without a number") (targetValue (name "?{contain(<num>)}")))
(example (utterance "lines ending with a capital before string leftquoatation ! rightquoatation and character") (targetValue (name "?{endwith(<cap>),and(<!>,<any>)}")))
(example (utterance "non-word lines with capital letters and zero") (targetValue (name "?{star(<cap>)}")))
(example (utterance "items with an upper case letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{star(concat(<cap>,<!>))}")))
(example (utterance "the lines that do not contain a character prior to a lower-case letter") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation before a letter") (targetValue (name "?{startwith(<!>),<let>}")))
(example (utterance "lines with words with either a lower-case letter , character , or vowel") (targetValue (name "?{or(<low>,<any>),<vow>}")))
(example (utterance "lines with only string leftquoatation ! rightquoatation or letter") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines that do not have a character and not leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<!>)}")))
(example (utterance "lines without a vowel and leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{not(<vow>),concat(<!>,<@>)}")))
(example (utterance "lines with words starting in capital letter") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines having either a vowel , leftquoatation ! rightquoatation , or a lower-case letter coming before a character") (targetValue (name "?{or(or(<vow>,<!>),<low>),<any>}")))
(example (utterance "lines not containing a letter and leftquoatation ! rightquoatation 1 or more times") (targetValue (name "?{not(<let>),repeatatleast(<!>,1)}")))
(example (utterance "lines containing either a vowel , a character , or a number") (targetValue (name "?{or(<vow>,<any>),<num>}")))
(example (utterance "lines starting in a vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(concat(<vow>,<!>),1)}")))
(example (utterance "lines containing a lower-case letter followed by a vowel or a number") (targetValue (name "?{<low>,or(<vow>,<num>)}")))
(example (utterance "lines with a letter and character before lower-case letter") (targetValue (name "?{and(<let>,<any>),<low>}")))
(example (utterance "lines that end with a letter or a vowel") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation , a capital letter") (targetValue (name "?{and(<!>,<cap>),<let>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation and letter , 6 or more times") (targetValue (name "?{not(<!>),repeatatleast(<let>,6)}")))
(example (utterance "lines with words that contain a lower-case letter or a character in them") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines not start with a number") (targetValue (name "?{not(<num>)}")))
(example (utterance "lines starting with words with a number") (targetValue (name "?{repeatatleast(<num>,1)}")))
(example (utterance "lines that end with a character") (targetValue (name "?{<any>}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation or a capital letter in them") (targetValue (name "?{<any>,or(<!>,<cap>)}")))
(example (utterance "lines that start with a lower-case letter and end with an upper-case (or 'capital'\) letter") (targetValue (name "?{startwith(<low>),endwith(<cap>)}")))
(example (utterance "lines with a character followed by a capital letter , then leftquoatation ! rightquoatation , and finally leftquoatation @ rightquoatation") (targetValue (name "?{<any>,and(and(<cap>,<!>),<@>)}")))
(example (utterance "lines ending with a character or a number") (targetValue (name "?{or(repeatatleast(<any>,1),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a letter before a lower-case letter in it") (targetValue (name "?{or(<!>,<let>),<low>}")))
(example (utterance "lines containing the string dog , truck , ring , zero or more times .") (targetValue (name "?{contain(or(or(<!>,<@>),<#>))}")))
(example (utterance "lines containing a capital letter , a lower-case letter , and a vowel") (targetValue (name "?{<cap>,concat(<low>,<vow>)}")))
(example (utterance "lines having either lower case letter , a character , or leftquoatation ! rightquoatation before a number") (targetValue (name "?{or(or(<low>,<any>),<!>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a number at the end") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before either a vowel , letter , or string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<vow>,<let>),<@>)}")))
(example (utterance "lines with words ending in lower-case letter") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines containing a letter followed by a character or leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,or(<any>,<!>)}")))
(example (utterance "lines that contain a vowel followed by anything other than a number") (targetValue (name "?{<vow>,not(<num>)}")))
(example (utterance "the lines that have words containing both a character and a capital letter , at least once") (targetValue (name "?{<any>,star(<cap>)}")))
(example (utterance "lines containing a vowel and leftquoatation ! rightquoatation follwed by either leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{and(<vow>,<!>),or(<@>,<#>)}")))
(example (utterance "lines that have a letter or leftquoatation ! rightquoatation at least 3 times") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation and a number at least once in them") (targetValue (name "?{<!>,repeatatleast(<num>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a capital letter , 6 or more times in them") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines with words with no leftquoatation ! rightquoatation before letter") (targetValue (name "?{not(<!>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a letter before a number in them") (targetValue (name "?{concat(<!>,<let>),<num>}")))
(example (utterance "lines with a character before a capital letter or ending in lower-case letter") (targetValue (name "?{concat(<any>,<cap>),endwith(<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a number , or a letter before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<num>),<let>),<@>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , leftquoatation # rightquoatation , and a character") (targetValue (name "?{and(and(<!>,<@>),<#>),<any>}")))
(example (utterance "lines with the string dog and then the string truck") (targetValue (name "?{<!>,endwith(<@>)}")))
(example (utterance "lines with a letter and strings leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{and(<let>,<!>),contain(<@>)}")))
(example (utterance "lines with words , leftquoatation ! rightquoatation , a capital letter , and vowel") (targetValue (name "?{and(<!>,<cap>),<vow>}")))
(example (utterance "lines not words with lower-case letter , 7 or more times") (targetValue (name "?{repeatatleast(<low>,7)}")))
(example (utterance "lines with  a letter and a lower-case letter after leftquoatation ! rightquoatation") (targetValue (name "?{<!>,and(<let>,<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a letter at least 3 times") (targetValue (name "?{<!>,repeatatleast(<let>,3)}")))
(example (utterance "lines with a number before a vowel") (targetValue (name "?{<num>,star(<vow>)}")))
(example (utterance "lines with a letter at least 4 times before a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,4),or(<cap>,<!>)}")))
(example (utterance "lines with a vowel or letter") (targetValue (name "?{or(or(<low>,<vow>),<let>)}")))
(example (utterance "lines not having lower-case letter , a capital letter , and not leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<cap>),<!>)}")))
(example (utterance "lines with a lower-case letter before a non character") (targetValue (name "?{<low>,not(<any>)}")))
(example (utterance "lines with a character after a vowel , a letter , and a lower-case letter") (targetValue (name "?{and(and(<vow>,<let>),<low>),<any>}")))
(example (utterance "lines with words that don't have a vowel and a letter in them") (targetValue (name "?{not(<vow>),<let>}")))
(example (utterance "lines that contain a number or leftquoatation ! rightquoatation at least once") (targetValue (name "?{or(repeatatleast(<!>,1),<num>)}")))
(example (utterance "lines that contain neither a capital letter , number , nor a character .") (targetValue (name "?{and(and(<cap>,<num>),<any>)}")))
(example (utterance "items with a letter preceding an upper clase letter preceding a vowel .") (targetValue (name "?{concat(<let>,<cap>),<vow>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation or leftquoatation # rightquoatation , and a letter") (targetValue (name "?{or(or(<!>,<@>),<#>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{<!>,and(and(<cap>,<@>),<#>)}")))
(example (utterance "lines consisting of words with a letter as well as a number") (targetValue (name "?{<let>,<num>}")))
(example (utterance "lines containing a letter or character preceding a lowercase letter .") (targetValue (name "?{or(<let>,<any>),<low>}")))
(example (utterance "lines which may or may not contain words starting with a vowel") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines with words and 6 or more numbers") (targetValue (name "?{repeatatleast(<num>,6)}")))
(example (utterance "lines without the word dog or any letters") (targetValue (name "?{or(or(<vow>,<let>),<!>)}")))
(example (utterance "items with words beginning with a vowel at least 1 time .") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines with a capital letter , a letter , as well as leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<let>),<!>)}")))
(example (utterance "lines not having a letter , zero or more times before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(star(<let>),<!>)}")))
(example (utterance "lines with words with a capital letter , number , or vowel") (targetValue (name "?{or(<cap>,<num>),<vow>}")))
(example (utterance "lines with letter or vowel before capital letter") (targetValue (name "?{or(<let>,<vow>),<cap>}")))
(example (utterance "lines that contain words with a lower-case letter , a vowel , or a number") (targetValue (name "?{or(or(<low>,<vow>),<num>)}")))
(example (utterance "lines having a character , lower-case , or leftquoatation ! rightquoatation before a number") (targetValue (name "?{or(or(<any>,<low>),<!>),<num>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation and string leftquoatation # rightquoatation") (targetValue (name "?{endwith(<!>),and(<@>,<#>)}")))
(example (utterance "lines with a letter followed by a lower-case letter at least once in them") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a lower-case letter") (targetValue (name "?{<any>,or(or(<!>,<low>),<@>)}")))
(example (utterance "lines with words with letter before string leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation ,  leftquoatation @ rightquoatation ,  a letter , or leftquoatation # rightquoatation") (targetValue (name "?{and(<!>,and(and(<@>,<let>),<#>))}")))
(example (utterance "lines ending with a lower-case letter before a letter") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{<low>,contain(<!>)}")))
(example (utterance "lines containing a capital letter , leftquoatation ! rightquoatation and leftquoatation @ rightquoatation preceding leftquoatation # rightquoatation") (targetValue (name "?{<@>,and(and(<#>,<cap>),<!>)}")))
(example (utterance "lines with a character , zero or more times or either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation in them") (targetValue (name "?{star(<any>),or(or(<!>,<@>),<#>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation or string leftquoatation # rightquoatation at least once") (targetValue (name "?{or(and(<!>,<@>),<#>)}")))
(example (utterance "lines with more than just leftquoatation ! rightquoatation") (targetValue (name "?{star(<!>)}")))
(example (utterance "lines starting with a lower-case letter with leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<low>),<!>}")))
(example (utterance "lines having a number string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation before character") (targetValue (name "?{and(and(<!>,<num>),<@>),<any>}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation before leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{endwith(concat(<!>,<@>)),<#>}")))
(example (utterance "lines with words a lower-case letter before containing a number") (targetValue (name "?{<low>,contain(<num>)}")))
(example (utterance "lines with vowel and number before capital letter") (targetValue (name "?{and(<vow>,<num>),<cap>}")))
(example (utterance "lines that contain a character and leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{and(<any>,concat(<!>,<let>))}")))
(example (utterance "lines which are containing ending with a letter , zero or more times") (targetValue (name "?{star(endwith(<let>))}")))
(example (utterance "lines with a character or leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{or(<any>,<!>),repeatatleast(<low>,1)}")))
(example (utterance "lines without a lower-case letter then a character") (targetValue (name "?{not(<low>),<any>}")))
(example (utterance "lines with a capital letter preceded at least once by leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),<cap>}")))
(example (utterance "lines with a capital before vowel or character") (targetValue (name "?{<cap>,or(<vow>,<any>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation followed by a vowel not being a capital letter") (targetValue (name "?{concat(<!>,<vow>),not(<cap>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and capital letter") (targetValue (name "?{<low>,and(and(<!>,<@>),<cap>)}")))
(example (utterance "lines with a vowel or a capital letter before containing a character in them") (targetValue (name "?{or(<vow>,<cap>),contain(<any>)}")))
(example (utterance "lines containing a vowel , leftquoatation ! rightquoatation , and a capital letter") (targetValue (name "?{and(<vow>,<!>),<cap>}")))
(example (utterance "lines do not contain 2 or more numbers nor a letter") (targetValue (name "?{and(repeatatleast(<num>,2),<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before either leftquoatation @ rightquoatation , a number , or a capital letter") (targetValue (name "?{<!>,or(or(<@>,<num>),<cap>)}")))
(example (utterance "lines ending with letter followed by a capital") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines starting in 5 or more characters before a capital letter") (targetValue (name "?{or(repeatatleast(<any>,5),<cap>)}")))
(example (utterance "items with a small letter preceding a vowel , at least 7 times .") (targetValue (name "?{<low>,repeatatleast(<vow>,7)}")))
(example (utterance "lines containing a number and leftquoatation ! rightquoatation followed by a lower-case letter") (targetValue (name "?{and(<num>,<!>),<low>}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation at least 7 times") (targetValue (name "?{contain(not(<!>))}")))
(example (utterance "lines not having a character or a letter") (targetValue (name "?{not(<any>),contain(<let>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceded by a vowel") (targetValue (name "?{<vow>,<!>}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , capital letter , and the string 'truck before a lower case letter") (targetValue (name "?{and(and(<!>,<cap>),<@>),<low>}")))
(example (utterance "items with leftquoatation ! rightquoatation , a character , and a vowel preceding a small letter .") (targetValue (name "?{and(and(<vow>,<any>),<!>),<low>}")))
(example (utterance "lines not ending in only capital letter") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "no lines with words and the string 'dog then a vowel") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines which do not contain leftquoatation ! rightquoatation or a vowel or a letter") (targetValue (name "?{and(and(<!>,<vow>),<let>)}")))
(example (utterance "lines without a letter and leftquoatation ! rightquoatation , any number of times") (targetValue (name "?{not(<let>),star(<!>)}")))
(example (utterance "lines with zero or more lower-case letters before a capital letter then string leftquoatation ! rightquoatation") (targetValue (name "?{star(<low>),concat(<cap>,<!>)}")))
(example (utterance "lines with words with either a character , a number , or leftquoatation ! rightquoatation in them'") (targetValue (name "?{or(<any>,<num>),<!>}")))
(example (utterance "lines without at least 1 string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,1)}")))
(example (utterance "lines containing a letter followed by at least 1 lower-case letter") (targetValue (name "?{<let>,repeatatleast(contain(<low>),1)}")))
(example (utterance "lines starting with 2 or more of leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<!>)}")))
(example (utterance "lines that contain a vowel , a lower-case letter , or a letter and a number") (targetValue (name "?{and(or(or(<vow>,<low>),<let>),<num>)}")))
(example (utterance "6 lines with leftquoatation ! rightquoatation and truck'") (targetValue (name "?{<!>,repeatatleast(<@>,6)}")))
(example (utterance "lines without a lower-case letter , or a character") (targetValue (name "?{or(or(<low>,<any>),<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character 5 times") (targetValue (name "?{<!>,repeatatleast(<any>,5)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or capital letter") (targetValue (name "?{or(concat(<!>,<@>),<cap>)}")))
(example (utterance "lines ending in a letter") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines containing words that have a letter , a number , and a character") (targetValue (name "?{and(<let>,<num>),<any>}")))
(example (utterance "lines with an added capital letter") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines containing words wherein leftquoatation ! rightquoatation precedes a letter") (targetValue (name "?{<!>,<let>}")))
(example (utterance "lines with lower-case letter and vowel before number") (targetValue (name "?{and(<low>,<vow>),<num>}")))
(example (utterance "lines with a vowel or ending with string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{or(<vow>,endwith(<!>))}")))
(example (utterance "lines which have a lower-case letter , 6 or more times before a vowel") (targetValue (name "?{repeatatleast(<low>,6),<vow>}")))
(example (utterance "items with a vowel , numeral , or upper case letter .") (targetValue (name "?{or(or(<num>,<cap>),<vow>)}")))
(example (utterance "lines with a letter and a character after leftquoatation ! rightquoatation") (targetValue (name "?{<!>,and(<let>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a number at least zero times") (targetValue (name "?{star(<num>),<!>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation zero times") (targetValue (name "?{star(<!>)}")))
(example (utterance "lines having a lower-case letter , letter , and string leftquoatation ! rightquoatation before number") (targetValue (name "?{and(and(<low>,<let>),<!>),<num>}")))
(example (utterance "lines without a capital letter , 3 or more times or leftquoatation ! rightquoatation") (targetValue (name "?{or(repeatatleast(<cap>,3),<!>)}")))
(example (utterance "lines with a vowel after leftquoatation ! rightquoatation at least 5 times") (targetValue (name "?{<!>,repeatatleast(<vow>,5)}")))
(example (utterance "lines without a number starting in character") (targetValue (name "?{not(<num>),startwith(<any>)}")))
(example (utterance "lines containing 4 or more vowels , and a capital letter") (targetValue (name "?{repeatatleast(<vow>,4),<cap>}")))
(example (utterance "items with words containing an upper case letter preceding a numeral .") (targetValue (name "?{<cap>,<num>}")))
(example (utterance "lines containing a vowel or letter before any number") (targetValue (name "?{or(<vow>,<let>),<num>}")))
(example (utterance "lines which are containing leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation , 4 or more times") (targetValue (name "?{contain(concat(<!>,<@>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation after a character , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{or(or(<any>,<@>),<#>),<!>}")))
(example (utterance "lines not containing a vowel preceding a number") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines with the strong leftquoatation ! rightquoatation at least zero times") (targetValue (name "?{star(<!>)}")))
(example (utterance "lines containing a capital letter before leftquoatation ! rightquoatation at least 4 times") (targetValue (name "?{<cap>,repeatatleast(<!>,4)}")))
(example (utterance "lines without lower cased letters , no characters , or the string'dog'") (targetValue (name "?{and(and(<!>,<low>),<any>)}")))
(example (utterance "lines with a vowel before capital letter") (targetValue (name "?{contain(concat(<vow>,<cap>))}")))
(example (utterance "lines with lower-case letter before a capital or string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,or(<cap>,<!>)}")))
(example (utterance "lines not having a letter , lower-case letter , or a number") (targetValue (name "?{and(and(<let>,<low>),<num>)}")))
(example (utterance "lines which are not starting with a character , 5 or more times") (targetValue (name "?{repeatatleast(startwith(<any>),5)}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{<!>,<num>}")))
(example (utterance "items with a small letter preceding a numeral , a vowel , or leftquoatation ! rightquoatation .") (targetValue (name "?{<low>,or(or(<num>,<vow>),<!>)}")))
(example (utterance "lines with both strings leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a vowel after any number") (targetValue (name "?{<num>,and(and(<vow>,<!>),<@>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , zero or more times and ending with vowel") (targetValue (name "?{star(<!>),endwith(<vow>)}")))
(example (utterance "items with a vowel preceding a letter at least 7 times .") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation and does not contain leftquoatation # rightquoatation") (targetValue (name "?{and(concat(<!>,<@>),<#>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or truck' or a character") (targetValue (name "?{or(or(<!>,<any>),<@>)}")))
(example (utterance "lines with a capital letter or number") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines starting in string leftquoatation ! rightquoatation and number before string leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),concat(<num>,<@>)}")))
(example (utterance "items with a numeral preceding a character .") (targetValue (name "?{<num>,star(<any>)}")))
(example (utterance "lines without either a capital letter or a vowel") (targetValue (name "?{repeatatleast(or(<cap>,<vow>),1)}")))
(example (utterance "lines  without a number contained 2 or more times") (targetValue (name "?{repeatatleast(contain(<num>),2)}")))
(example (utterance "lines with a lowercase letter followed by a vowel") (targetValue (name "?{<low>,<vow>}")))
(example (utterance "items with a character preceding leftquoatation ! rightquoatation at least6times .") (targetValue (name "?{<any>,repeatatleast(<!>,6)}")))
(example (utterance "items with an upper case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation preceding a letter .") (targetValue (name "?{or(or(<cap>,<!>),<@>),<let>}")))
(example (utterance "lines with words and beginning with leftquoatation ! rightquoatation") (targetValue (name "?{star(<!>)}")))
(example (utterance "lines with zero or more lower-case letters or characters .") (targetValue (name "?{contain(or(<any>,<low>))}")))
(example (utterance "lines ending in a number or with string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{endwith(<num>),or(or(<!>,<@>),<#>)}")))
(example (utterance "lines with no letters before the string 'dog") (targetValue (name "?{<let>,star(<!>)}")))
(example (utterance "lines containing lower-case letter and string leftquoatation ! rightquoatation at least once") (targetValue (name "?{and(<low>,repeatatleast(<!>,1))}")))
(example (utterance "lines not having leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation and a character") (targetValue (name "?{and(not(<!>),<any>)}")))
(example (utterance "items with words containing an upper case letter , a numeral , and a character .") (targetValue (name "?{and(<any>,<num>),<cap>}")))
(example (utterance "lines not containing only string leftquoatation ! rightquoatation before vowel") (targetValue (name "?{not(<!>),<vow>}")))
(example (utterance "lines which are ending with a number or not leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<num>),not(<!>)}")))
(example (utterance "lines with words with a lower-case letter , string leftquoatation ! rightquoatation , capital letter , and number") (targetValue (name "?{and(and(<low>,<!>),<cap>),<num>}")))
(example (utterance "lines with words with a number before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation before capital letter at least once") (targetValue (name "?{concat(<!>,repeatatleast(<cap>,1))}")))
(example (utterance "lines with words and no capital letter") (targetValue (name "?{not(<cap>)}")))
(example (utterance "lines with words contain a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),<!>}")))
(example (utterance "a vowel appears before a capital letter , leftquoatation ! rightquoatation and leftquoatation @ rightquoatation in the lines") (targetValue (name "?{<vow>,and(and(<cap>,<!>),<@>)}")))
(example (utterance "items with words with a numeral at least thrice .") (targetValue (name "?{<num>}")))
(example (utterance "lines with a character after a vowel , leftquoatation ! rightquoatation , or a number") (targetValue (name "?{or(or(<vow>,<!>),<num>),<any>}")))
(example (utterance "lines with 6 or more lower-case letters followed by a letter") (targetValue (name "?{repeatatleast(<low>,6),<let>}")))
(example (utterance "lines with a letter , a lower-case letter , as well as leftquoatation ! rightquoatation followed by a capital letter") (targetValue (name "?{and(and(<let>,<low>),<!>),<cap>}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation and letter") (targetValue (name "?{concat(<cap>,<!>),and(<@>,<let>)}")))
(example (utterance "lines with 6 or more of a letter before vowel") (targetValue (name "?{<let>,repeatatleast(<vow>,6)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation followed by the word leftquoatation @ rightquoatation , as well as a lower-case letter") (targetValue (name "?{<!>,and(<@>,<low>)}")))
(example (utterance "items with a vowel preceding a small letter or character .") (targetValue (name "?{<vow>,or(<low>,<any>)}")))
(example (utterance "lines ending 5 or more times with a lower-case letter") (targetValue (name "?{repeatatleast(<low>,5)}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , a character , or a capital letter and string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<any>),<cap>),<@>}")))
(example (utterance "lines containing a vowel followed by a lower case letter , leftquoatation ! rightquoatation , and a character") (targetValue (name "?{<vow>,and(and(<low>,<!>),<any>)}")))
(example (utterance "lines with words ending in string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "items with an upper case letter preceding leftquoatation ! rightquoatation or leftquoatation @ rightquoatation .") (targetValue (name "?{<cap>,or(<!>,<@>)}")))
(example (utterance "lines having a number , a capital letter , string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<num>,<cap>),<!>),<@>}")))
(example (utterance "items with a small letter , a letter , and a character .") (targetValue (name "?{and(and(<any>,<let>),<low>)}")))
(example (utterance "lines that do not contain a number , leftquoatation ! rightquoatation , and a capital letter") (targetValue (name "?{and(and(<num>,<!>),<cap>)}")))
(example (utterance "lines with an upper-case letter within and that also come before the leftquoatation ! rightquoatation string") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a lower-case letter and leftquoatation # rightquoatation in them") (targetValue (name "?{or(<!>,<@>),and(<low>,<#>)}")))
(example (utterance "lines with the  ; dog ; string within and also a number within") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines with words before a character then capital letter") (targetValue (name "?{concat(<vow>,<any>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a character and a lower-case letter") (targetValue (name "?{and(and(<!>,<any>),<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation before a vowel at least zero times") (targetValue (name "?{concat(<!>,<@>),star(<vow>)}")))
(example (utterance "lines with a vowel and string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{and(<vow>,<!>),<@>}")))
(example (utterance "lines not having a number , leftquoatation ! rightquoatation , a capital letter and a character") (targetValue (name "?{or(<num>,or(or(<!>,<cap>),<any>))}")))
(example (utterance "lines containing a vowel before leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a letter .") (targetValue (name "?{<vow>,and(and(<!>,<let>),<@>)}")))
(example (utterance "lines starting with 7 or more characters or vowels") (targetValue (name "?{or(<any>,repeatatleast(<vow>,7))}")))
(example (utterance "lines without a capital letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<vow>),<!>)}")))
(example (utterance "lines containing a vowel 2 or more times") (targetValue (name "?{<vow>}")))
(example (utterance "lines with a letter after a lower-case letter , zero or more times") (targetValue (name "?{star(concat(<low>,<let>))}")))
(example (utterance "lines with a vowel before character , capital letter , and letter") (targetValue (name "?{<vow>,and(and(<any>,<cap>),<let>)}")))
(example (utterance "lines with words and a vowel") (targetValue (name "?{repeatatleast(<vow>,1)}")))
(example (utterance "lines with 3 or more of a lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,repeatatleast(<!>,3)}")))
(example (utterance "lines ending in words without a capital letter") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a vowel before lower-case letter") (targetValue (name "?{and(<!>,<vow>),<low>}")))
(example (utterance "items with either leftquoatation ! rightquoatation or a letter .") (targetValue (name "?{or(or(<let>,<!>),<low>)}")))
(example (utterance "lines ending over 3 times with leftquoatation ! rightquoatation before a number") (targetValue (name "?{repeatatleast(concat(<num>,<!>),3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , zero or more times and a lower-case letter or a capital letter in them") (targetValue (name "?{star(<!>),or(<low>,<cap>)}")))
(example (utterance "lines starting with 4 or more of a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,repeatatleast(<!>,4))}")))
(example (utterance "lines having words with either a number , a character or a letter") (targetValue (name "?{or(<num>,<any>),<let>}")))
(example (utterance "lines with a capital letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,or(or(<let>,<vow>),<!>)}")))
(example (utterance "lines without a number before a capital or lower-case letter") (targetValue (name "?{or(concat(<num>,<cap>),<low>)}")))
(example (utterance "lines that don't have a vowel , number , or letter .") (targetValue (name "?{or(or(<vow>,<num>),<let>)}")))
(example (utterance "lines with number , 2 or more times and containing string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,2),contain(<!>)}")))
(example (utterance "items with an upper case letter preceding leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation .") (targetValue (name "?{<cap>,or(or(<@>,<!>),<#>)}")))
(example (utterance "items without a vowel at least 4 times .") (targetValue (name "?{repeatatleast(<vow>,4)}")))
(example (utterance "lines with a letter followed by string leftquoatation ! rightquoatation at least once") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation and capital letter") (targetValue (name "?{<any>,and(<!>,<cap>)}")))
(example (utterance "lines ending in a letter and vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{or(and(<let>,<vow>),<!>)}")))
(example (utterance "lines with capital letter or not a number , zero or more times") (targetValue (name "?{or(<cap>,not(<num>))}")))
(example (utterance "lines without a letter") (targetValue (name "?{or(or(<low>,<let>),<cap>)}")))
(example (utterance "lines with words that contain a capital letter , the word leftquoatation ! rightquoatation , and a lower-case letter") (targetValue (name "?{and(<cap>,<!>),<low>}")))
(example (utterance "lines with a character before a lower-case letter ending in string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<low>),endwith(<!>)}")))
(example (utterance "use the word leftquoatation ! rightquoatation at least once before a vowel") (targetValue (name "?{<!>,repeatatleast(<vow>,1)}")))
(example (utterance "items with a character preceding a letter and leftquoatation ! rightquoatation .") (targetValue (name "?{<any>,and(<!>,<let>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation and a lower-case letter followed by string leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),concat(<low>,<@>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by either leftquoatation @ rightquoatation , a letter , or a capital letter") (targetValue (name "?{<!>,or(or(<@>,<let>),<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a number or a capital letter and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<num>),and(<cap>,<@>)}")))
(example (utterance "lines with 6 or more of leftquoatation ! rightquoatation or a vowel followed by leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,6),and(<vow>,<@>)}")))
(example (utterance "lines containing words having either a character , a capital letter , or leftquoatation ! rightquoatation , 6 times or more") (targetValue (name "?{or(or(<any>,<cap>),<!>)}")))
(example (utterance "items with a numeral or small letter at least 7 times .") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "lines with 6 or more letters before a number") (targetValue (name "?{repeatatleast(<let>,6),<num>}")))
(example (utterance "items with an upper case letter at least 4 times .") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines without more than 3 characters , or a letter") (targetValue (name "?{and(repeatatleast(<any>,3),<let>)}")))
(example (utterance "lines with a number ending in only a capital letter") (targetValue (name "?{<num>,endwith(<cap>)}")))
(example (utterance "lines starting with a letter at least zero times") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "lines ending in either a vowel or a lower-case letter .") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines that have a character preceding a letter and the word leftquoatation ! rightquoatation") (targetValue (name "?{<any>,and(<let>,<!>)}")))
(example (utterance "the lines that end with either leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines with the 4 or more strings leftquoatation ! rightquoatation and leftquoatation @ rightquoatation or number") (targetValue (name "?{or(and(<!>,<@>),<num>)}")))
(example (utterance "lines not starting in a vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{not(startwith(<vow>)),<!>}")))
(example (utterance "lines without leftquoatation ! rightquoatation before a character") (targetValue (name "?{not(<!>),<any>}")))
(example (utterance "lines with a capital letter before number") (targetValue (name "?{contain(<cap>),<num>}")))
(example (utterance "items with a numeral preceding a letter or small letter .") (targetValue (name "?{<num>,and(<let>,<low>)}")))
(example (utterance "lines having capital letter , lower-case letter , and vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<low>),<vow>),<!>}")))
(example (utterance "lines that have a number after which there is an upper-case letter") (targetValue (name "?{<num>,<cap>}")))
(example (utterance "lines with words having leftquoatation ! rightquoatation a vowel or character") (targetValue (name "?{or(<!>,<vow>),<any>}")))
(example (utterance "lines not having a vowel or lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{not(or(<vow>,<low>)),<!>}")))
(example (utterance "lines not having a number , leftquoatation ! rightquoatation , a lower-case and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(and(<num>,<!>),<low>),<@>)}")))
(example (utterance "lines with a vowel followed by a number coming before a letter") (targetValue (name "?{concat(<vow>,<num>),<let>}")))
(example (utterance "lines with no capital letter before a lower-case letter") (targetValue (name "?{concat(<cap>,endwith(<low>))}")))
(example (utterance "lines having a letter or character or number") (targetValue (name "?{or(or(<let>,<any>),<num>)}")))
(example (utterance "lines with words") (targetValue (name "?{<let>}")))
(example (utterance "line with leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation or any numbers have a letter before them") (targetValue (name "?{startwith(<let>),and(and(<!>,<num>),<@>)}")))
(example (utterance "items with leftquoatation ! rightquoatation or a small letter or leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<!>,<@>),<low>)}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , and a number") (targetValue (name "?{and(<vow>,<!>),<num>}")))
(example (utterance "lines ending in a character before lower-case letter and vowel") (targetValue (name "?{<any>,and(<low>,<vow>)}")))
(example (utterance "items beginning with leftquoatation ! rightquoatation at least 7 times .") (targetValue (name "?{repeatatleast(<!>,7)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a vowel , and leftquoatation @ rightquoatation followed by a letter") (targetValue (name "?{and(and(<!>,<vow>),<@>),<let>}")))
(example (utterance "lines with a zero or more numbers before a lower-case letter") (targetValue (name "?{star(<num>),<low>}")))
(example (utterance "lines including the words leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and at least 6 lower case letters") (targetValue (name "?{contain(and(and(<!>,<@>),<low>))}")))
(example (utterance "lines that have 7 or more lower-case letters") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines with 4 or more capital letters") (targetValue (name "?{<cap>}")))
(example (utterance "lines containing a lower-case letter , capital letter , and character") (targetValue (name "?{and(and(<low>,<cap>),<any>)}")))
(example (utterance "lines with zero or more of a vowel before a lower-case letter") (targetValue (name "?{contain(concat(<vow>,<low>))}")))
(example (utterance "lines ending in capital letter and string leftquoatation ! rightquoatation before letter") (targetValue (name "?{endwith(<cap>),concat(<!>,<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding 'truck") (targetValue (name "?{contain(<!>),and(<@>,<#>)}")))
(example (utterance "lines with a character , vowel , leftquoatation ! rightquoatation and a capital letter") (targetValue (name "?{or(<any>,<vow>),or(or(<let>,<!>),<cap>)}")))
(example (utterance "lines with a lower-case or containing only leftquoatation ! rightquoatation") (targetValue (name "?{<low>,<!>}")))
(example (utterance "lines which are having either a capital letter , a number , or leftquoatation ! rightquoatation before a letter") (targetValue (name "?{or(or(<cap>,<num>),<!>),<let>}")))
(example (utterance "lines with words , vowels , characters , and a letter") (targetValue (name "?{and(<vow>,<any>),<let>}")))
(example (utterance "lines containing an uppercase letter preceded by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines that do not have an uppercase letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines that have an upper-case letter preceding either a number or the leftquoatation ! rightquoatation string no less then once or greater") (targetValue (name "?{concat(<cap>,<num>),repeatatleast(<!>,1)}")))
(example (utterance "lines with a vowel 4 times") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "at least5times , lines with a letter or vowel") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and lower-case letter") (targetValue (name "?{<any>,and(and(<!>,<low>),<@>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation and number , zero or more times") (targetValue (name "?{star(or(<!>,<num>))}")))
(example (utterance "lines with no letter , vowel , or number") (targetValue (name "?{and(and(<let>,<vow>),<num>)}")))
(example (utterance "lines containing a capital and lower-case letter") (targetValue (name "?{and(<cap>,<low>)}")))
(example (utterance "lines with a vowel preceding a letter at least 6 times") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines which end 6 or more times with a vowel .") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation preceded by a number , at least6times") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines ending with words with number , zero or more times") (targetValue (name "?{star(<num>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a vowel , a letter , and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<vow>,<let>),<@>)}")))
(example (utterance "lines with words and 4 or more of leftquoatation ! rightquoatation or number") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines with lower-case letter before either character , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,or(or(<any>,<vow>),<!>)}")))
(example (utterance "lines ending in number or capital letter") (targetValue (name "?{or(repeatatleast(<num>,1),<cap>)}")))
(example (utterance "lines with a capital letter or a letter , 7 or more times in them") (targetValue (name "?{or(<cap>,<let>)}")))
(example (utterance "items with an upper case letter preceding a vowel preceding a character .") (targetValue (name "?{concat(<cap>,<vow>),<any>}")))
(example (utterance "lines containing a vowel") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "lines which are ending with a letter or leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{endwith(or(<let>,<!>))}")))
(example (utterance "lines with 6 or more capital letters before string leftquoatation ! rightquoatation") (targetValue (name "?{contain(repeatatleast(<cap>,6)),<!>}")))
(example (utterance "lines that do not contain a lowercase letter preceded by an uppercase letter") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines with words and 4 or more vowels") (targetValue (name "?{repeatatleast(<vow>,4)}")))
(example (utterance "lines with a capital letter leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<cap>,or(or(<let>,<!>),<@>)}")))
(example (utterance "lines that contain either a vowel or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<vow>),<@>)}")))
(example (utterance "items with a small letter or leftquoatation ! rightquoatation preceding an upper case letter .") (targetValue (name "?{or(<low>,<!>),<cap>}")))
(example (utterance "lines with 2 or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),repeatatleast(<@>,2)}")))
(example (utterance "lines containing words , a letter , or character") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a character .") (targetValue (name "?{and(<any>,<@>),<!>}")))
(example (utterance "lines starting with a vowel before a character") (targetValue (name "?{startwith(concat(<vow>,<any>))}")))
(example (utterance "lines with words with a capital letter , string leftquoatation ! rightquoatation , and vowel") (targetValue (name "?{and(<cap>,<!>),<vow>}")))
(example (utterance "lines in which a capital letter is preceded by leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,<!>}")))
(example (utterance "lines without a number , character , or string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(or(or(<num>,<any>),<!>),<@>)}")))
(example (utterance "lines that do not have a capital letter , a lower-case letter , or a vowel") (targetValue (name "?{or(or(<cap>,<low>),<vow>)}")))
(example (utterance "lines without a letter , number , or character") (targetValue (name "?{or(or(<let>,<num>),<any>)}")))
(example (utterance "lines having either a number , string leftquoatation ! rightquoatation , or a vowel before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<num>,<!>),<vow>),<@>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation , not a vowel , and not string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<vow>),<@>)}")))
(example (utterance "lines with a lower-case letter before a number") (targetValue (name "?{<low>,repeatatleast(<num>,1)}")))
(example (utterance "a vowel , number , and lines with the strong dog") (targetValue (name "?{and(<!>,<num>),<vow>}")))
(example (utterance "lines with a lower-case letter before a number , zero or more times in them") (targetValue (name "?{<low>,star(<num>)}")))
(example (utterance "lines with 2 or more times of a capital letter followed by a lower-case letter") (targetValue (name "?{<cap>,repeatatleast(<low>,2)}")))
(example (utterance "lines have vowel after capital letter") (targetValue (name "?{repeatatleast(<cap>,1),<vow>}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation and a letter") (targetValue (name "?{and(<!>,<let>)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation at least once or lower-case letter") (targetValue (name "?{or(repeatatleast(<!>,1),<low>)}")))
(example (utterance "lines ending with 5 or more vowels") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines which are containing a character followed by a lower-case letter , zero or more times") (targetValue (name "?{star(concat(<any>,<low>))}")))
(example (utterance "lines which are containing a number before ending with a character") (targetValue (name "?{contain(<num>),endwith(<any>)}")))
(example (utterance "lines without capital letters or characters") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines that end with a lower-case letter followed by a character") (targetValue (name "?{endwith(<low>),<any>}")))
(example (utterance "lines containing words and a letter , the word leftquoatation ! rightquoatation and a number") (targetValue (name "?{and(<let>,<!>),<num>}")))
(example (utterance "lines do not have a lower-case letter and a letter") (targetValue (name "?{and(contain(<low>),<let>)}")))
(example (utterance "lines that contain words with leftquoatation ! rightquoatation and which also contain leftquoatation @ rightquoatation") (targetValue (name "?{<!>,contain(<@>)}")))
(example (utterance "lines containing words with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation in them") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines not having letter , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<vow>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before 3 or more lower-case letters") (targetValue (name "?{<!>,repeatatleast(<low>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , number , or lower-case letter") (targetValue (name "?{or(or(<!>,<num>),<low>)}")))
(example (utterance "lines ending with a letter before vowel") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines with words that have a character followed by a lower-case letter") (targetValue (name "?{<any>,<low>}")))
(example (utterance "lines with words having dog or a vowel") (targetValue (name "?{<!>,<vow>}")))
(example (utterance "lines with a lower-case letter , number , and character") (targetValue (name "?{and(and(<low>,<num>),<any>)}")))
(example (utterance "lines that contain a number") (targetValue (name "?{contain(<num>)}")))
(example (utterance "lines ending in a capital letter or vowel before a character") (targetValue (name "?{endwith(or(<cap>,<vow>)),<any>}")))
(example (utterance "lines with vowel or character , 6 or more times") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel , a letter , or a character .") (targetValue (name "?{<!>,or(or(<vow>,<let>),<any>)}")))
(example (utterance "lines with a number or character before vowel") (targetValue (name "?{or(<num>,<any>),<vow>}")))
(example (utterance "lines with words with lower-case letter , vowel , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<vow>),<!>}")))
(example (utterance "lines with a character or a letter") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines containing character , letter , and vowel") (targetValue (name "?{and(and(<any>,<let>),<vow>)}")))
(example (utterance "lines containing either a number or a character") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "lines containing a lower-case letter preceding a capital letter") (targetValue (name "?{<low>,contain(<cap>)}")))
(example (utterance "lines ending in 5 or more characters") (targetValue (name "?{repeatatleast(<any>,5)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before a letter") (targetValue (name "?{repeatatleast(concat(<!>,<let>),1)}")))
(example (utterance "lines ending with a word containing a capital") (targetValue (name "?{star(<cap>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation , a letter , and a character preceding a number") (targetValue (name "?{and(and(<!>,<let>),<any>),<num>}")))
(example (utterance "lines ending in 5 or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),repeatatleast(<@>,5)}")))
(example (utterance "lines not ending with a letter before a capital") (targetValue (name "?{concat(endwith(<let>),<cap>)}")))
(example (utterance "lines containing either tring leftquoatation ! rightquoatation , letter , or lower-case letter") (targetValue (name "?{or(or(<!>,<let>),<low>)}")))
(example (utterance "items with a character preceding a numeral at least 4 times .") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "a lower case number to which a vowel , a number , and leftquoatation ! rightquoatation proceed it") (targetValue (name "?{and(and(<vow>,<!>),<num>),<low>}")))
(example (utterance "lines with elements leftquoatation ! rightquoatation and leftquoatation @ rightquoatation aqd a sequence of leftquoatation # rightquoatation appearing before leftquoatation $ rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<$>),<#>}")))
(example (utterance "lines containing a character , leftquoatation ! rightquoatation , and a capital letter") (targetValue (name "?{and(<any>,<!>),<cap>}")))
(example (utterance "lines containing a vowel followed by leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{<vow>,and(<!>,<@>)}")))
(example (utterance "lines with words , a capital letter , the 2 or more of leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,<!>)}")))
(example (utterance "lines containing a vowel and ending with string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),endwith(<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation 2 or more times before a lower-case letter") (targetValue (name "?{<!>,repeatatleast(<low>,2)}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation or with string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,contain(<@>)}")))
(example (utterance "lines that have a character preceding a capital letter and a number , 1 or more times") (targetValue (name "?{concat(<any>,<cap>),repeatatleast(<num>,1)}")))
(example (utterance "lines containing a letter and leftquoatation ! rightquoatation preceding a number") (targetValue (name "?{contain(<let>),concat(<!>,<num>)}")))
(example (utterance "lines containing a character preceded by an uppercase or lowercase letter") (targetValue (name "?{or(<cap>,<low>),<any>}")))
(example (utterance "lines ending with a character after a number , 6 times or more") (targetValue (name "?{repeatatleast(concat(<num>,<any>),6)}")))
(example (utterance "lines with a small letter followed by a character") (targetValue (name "?{<low>,<any>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation and number") (targetValue (name "?{concat(<!>,<@>),<num>}")))
(example (utterance "lines containing a vowel after a number , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<num>,<!>),<@>),<vow>}")))
(example (utterance "lines which are ending with words with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a capital letter") (targetValue (name "?{and(and(<!>,<@>),<cap>)}")))
(example (utterance "lines containing a lower-case letter before a capital letter") (targetValue (name "?{repeatatleast(<low>,1),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a lower-case letter >= 1 times") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines which have a capital letter or a lower-case letter , 6 or more times") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines with words and a lower-case letter followed by leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<low>,<!>),<let>}")))
(example (utterance "lines with 3 or more of a capital letter before a letter") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a number and a letter") (targetValue (name "?{<!>,concat(<num>,<let>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or character") (targetValue (name "?{<low>,or(or(<!>,<any>),<@>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a vowel before a number") (targetValue (name "?{concat(<!>,<vow>),<num>}")))
(example (utterance "lines not having lower-case letter , capital letter , or number") (targetValue (name "?{or(or(<low>,<cap>),<num>)}")))
(example (utterance "lines with a capital letter , 7 or more times before a number in them") (targetValue (name "?{repeatatleast(<cap>,7),<num>}")))
(example (utterance "items without leftquoatation ! rightquoatation or uppercase letter or leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<!>,<cap>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation without a capital letter") (targetValue (name "?{and(<!>,not(<cap>))}")))
(example (utterance "lines starting in a letter before capital letter then string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<let>),concat(<cap>,<!>)}")))
(example (utterance "lines that have a capital letter followed by 5 or more occurrences of leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "items with words containing a numeral , a vowel , or a character .") (targetValue (name "?{or(<any>,<num>),<vow>}")))
(example (utterance "lines that have a number and then a non-capital letter afterwards") (targetValue (name "?{<num>,<low>}")))
(example (utterance "lines with a capital or lower-case letter , 3 or more times") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "items with a vowel preceding a letter , a small letter , and leftquoatation ! rightquoatation .") (targetValue (name "?{<vow>,and(and(<let>,<!>),<low>)}")))
(example (utterance "lines without zero or more of leftquoatation ! rightquoatation or number") (targetValue (name "?{not(<!>),star(<num>)}")))
(example (utterance "lines without a lower-case letter following a letter") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines with words with lower-case letter , number , and a capital letter") (targetValue (name "?{and(<low>,<num>),<cap>}")))
(example (utterance "lines ending in a number or capital letter") (targetValue (name "?{or(<num>,repeatatleast(<cap>,1))}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a number preceding an uppercase letter") (targetValue (name "?{and(<!>,<num>),<cap>}")))
(example (utterance "lines without a lower-case letter , number , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<num>),<!>)}")))
(example (utterance "lines with either a capital letter or a vowel followed by leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,or(<!>,<cap>)}")))
(example (utterance "lines not starting with a letter") (targetValue (name "?{contain(not(<let>))}")))
(example (utterance "lines with a number , letter , or leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{or(or(<num>,<let>),<!>),<low>}")))
(example (utterance "lines which conclude either with a numeral no times or greater or with a vowel") (targetValue (name "?{or(star(<num>),<vow>)}")))
(example (utterance "lines which are ending with a number or a character and leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<num>),and(<any>,<!>)}")))
(example (utterance "lines with a lower-case letter zero times") (targetValue (name "?{star(<low>),<let>}")))
(example (utterance "items with leftquoatation ! rightquoatation and a vowel preceding a letter .") (targetValue (name "?{and(<!>,<vow>),<let>}")))
(example (utterance "lineswhich are  not having leftquoatation ! rightquoatation or starting with a character") (targetValue (name "?{not(<!>),startwith(<any>)}")))
(example (utterance "lines without a number  , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<num>,<!>),<@>)}")))
(example (utterance "lines that have words which contain a number , a lower-case letter , and a capital letter") (targetValue (name "?{and(<num>,<low>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{repeatatleast(<!>,1),contain(<low>)}")))
(example (utterance "lines ending with a capital at least 1 time") (targetValue (name "?{repeatatleast(<cap>,1)}")))
(example (utterance "lines with words and a vowel or capital letter") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and letters") (targetValue (name "?{and(<!>,<low>),<cap>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation before a letter , zero or more times") (targetValue (name "?{endwith(<!>),star(<let>)}")))
(example (utterance "lines that contain a number or leftquoatation ! rightquoatation followed by a vowel and a letter") (targetValue (name "?{or(<num>,<!>),and(<vow>,<let>)}")))
(example (utterance "lines starting 3 times with character") (targetValue (name "?{repeatatleast(<any>,3)}")))
(example (utterance "lines with words and a capital before lower case letter or string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<low>),<!>}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a letter") (targetValue (name "?{<num>,and(and(<!>,<let>),<@>)}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation , letter , and vowel") (targetValue (name "?{<num>,and(and(<!>,<let>),<vow>)}")))
(example (utterance "lines with words with vowel followed by string leftquoatation ! rightquoatation and number") (targetValue (name "?{concat(<vow>,<!>),<num>}")))
(example (utterance "items with  works containing leftquoatation ! rightquoatation , an upper case letter , and leftquoatation @ rightquoatation .") (targetValue (name "?{and(<!>,<@>),<cap>}")))
(example (utterance "lines that have a character with leftquoatation ! rightquoatation after .") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines without a character , number , or capital letter") (targetValue (name "?{and(and(<any>,<num>),<cap>)}")))
(example (utterance "lines ending with a character before a vowel") (targetValue (name "?{endwith(concat(<any>,<vow>))}")))
(example (utterance "lines starting with words with letter , 2 or more times") (targetValue (name "?{repeatatleast(<let>,2)}")))
(example (utterance "items with a small letter preceding a nonletter or character .") (targetValue (name "?{<low>,not(or(<let>,<any>))}")))
(example (utterance "lines with words that include a letter and a character") (targetValue (name "?{<any>,<let>}")))
(example (utterance "the lines should have both a number and a letter") (targetValue (name "?{and(<let>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a lower-case letter , or a character after leftquoatation @ rightquoatation") (targetValue (name "?{<@>,or(or(<!>,<low>),<any>)}")))
(example (utterance "lines with words ending in string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "items with a terminating letter .") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines ending in words with a vowel or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<@>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a vowel , or a character .") (targetValue (name "?{or(or(<any>,<!>),<vow>)}")))
(example (utterance "lines containing a lower-case letter , with any amount of times before leftquoatation ! rightquoatation") (targetValue (name "?{star(<low>),<!>}")))
(example (utterance "lines not having capital letter at least once or character") (targetValue (name "?{or(repeatatleast(<cap>,1),<any>)}")))
(example (utterance "lines with a character before capital letter or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{concat(<any>,or(or(<cap>,<!>),<@>))}")))
(example (utterance "lines with words starting in leftquoatation ! rightquoatation or letter") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines with words ending with character and number") (targetValue (name "?{endwith(<any>),<num>}")))
(example (utterance "lines with a letter before a vowel then number") (targetValue (name "?{concat(<let>,<vow>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a letter") (targetValue (name "?{startwith(<!>),concat(<@>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by 5 or more capital letters") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a letter , or a character preceding an upper case letter .") (targetValue (name "?{or(or(<let>,<!>),<any>),<cap>}")))
(example (utterance "lines with words with only 5 or more letters") (targetValue (name "?{<let>}")))
(example (utterance "lines with no capital letter or number before a lower-case letter") (targetValue (name "?{not(or(<cap>,<num>)),<low>}")))
(example (utterance "lines that have a vowel or capital letter") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "lines with a vowel number and a character") (targetValue (name "?{and(<num>,<any>),<vow>}")))
(example (utterance "lines with a capital before lower-case letter and string leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,and(<low>,<!>)}")))
(example (utterance "lines that have a letter , a vowel , all before a character") (targetValue (name "?{concat(<let>,<vow>),<any>}")))
(example (utterance "lines with 3 or more of leftquoatation ! rightquoatation before a character") (targetValue (name "?{<!>,repeatatleast(<any>,3)}")))
(example (utterance "lines that may or may not contain a number , letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<let>),<!>)}")))
(example (utterance "lines ending in a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{contain(endwith(<let>)),<!>}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation , a vowel , or a lower-case letter") (targetValue (name "?{<num>,or(or(<!>,<vow>),<low>)}")))
(example (utterance "lines containing only lower-case letter at least once") (targetValue (name "?{repeatatleast(<low>,1)}")))
(example (utterance "items with a character preceding a vowel preceding leftquoatation ! rightquoatation .") (targetValue (name "?{concat(concat(<any>,<vow>),<!>)}")))
(example (utterance "lines that have a number and the leftquoatation ! rightquoatation string and also an upper-case letter coming before a vowel") (targetValue (name "?{and(and(<num>,<!>),<cap>),<vow>}")))
(example (utterance "lines that contain a number , a lower-case letter , or leftquoatation ! rightquoatation followed by a character") (targetValue (name "?{or(or(<num>,<low>),<!>),<any>}")))
(example (utterance "lines with zero occurrences of a number , capital letter , or letter") (targetValue (name "?{or(or(<num>,<let>),<cap>)}")))
(example (utterance "lines with vowel followed by capital letter , 4 or more times") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,and(<!>,<any>)}")))
(example (utterance "lines that have a letter before a character") (targetValue (name "?{<let>,<any>}")))
(example (utterance "lines with words that begin with a character") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines ending with number , character , and string leftquoatation ! rightquoatation") (targetValue (name "?{contain(and(and(<num>,<any>),<!>))}")))
(example (utterance "lines without leftquoatation ! rightquoatation as a ending string") (targetValue (name "?{endwith(contain(<!>))}")))
(example (utterance "lines with a character after a lower-case letter at least 2 times") (targetValue (name "?{<low>,repeatatleast(<any>,2)}")))
(example (utterance "items without leftquoatation ! rightquoatation or any letter .") (targetValue (name "?{or(or(<cap>,<!>),<low>)}")))
(example (utterance "lines containing a character , followed by leftquoatation ! rightquoatation or 0 or more capital letters") (targetValue (name "?{or(concat(<any>,<!>),<cap>)}")))
(example (utterance "lines containing  leftquoatation ! rightquoatation , a capital letter , or a lower-case letter") (targetValue (name "?{or(<!>,<cap>),<low>}")))
(example (utterance "lines that must not contain the word leftquoatation ! rightquoatation , and a number") (targetValue (name "?{not(<!>),<num>}")))
(example (utterance "must have leftquoatation ! rightquoatation , a letter , or a character at least once") (targetValue (name "?{contain(or(or(<!>,<any>),<let>))}")))
(example (utterance "lines with a lower-case letter before a character , zero or more times in it") (targetValue (name "?{<low>,star(<any>)}")))
(example (utterance "lines with words which are ending with a letter , 5 or more times") (targetValue (name "?{repeatatleast(<let>,5)}")))
(example (utterance "lines with a vowel or a number , 5 or more times in them") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with words and a letter") (targetValue (name "?{or(or(<let>,<cap>),<low>)}")))
(example (utterance "lines with a letter , or a character") (targetValue (name "?{or(<low>,<any>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation after leftquoatation @ rightquoatation , a lower-case letter , and a character") (targetValue (name "?{and(and(<@>,<low>),<any>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , zero or more times before either leftquoatation @ rightquoatation , a capital letter , or a letter in them") (targetValue (name "?{star(<!>),or(or(<@>,<cap>),<let>)}")))
(example (utterance "lines containing a letter or string leftquoatation ! rightquoatation , 7 or more times") (targetValue (name "?{contain(or(<let>,<!>))}")))
(example (utterance "lines with a letter or a lower-case letter before a vowel in it") (targetValue (name "?{or(<let>,<low>),<vow>}")))
(example (utterance "lines not having lower-case letter followed by letter at least once") (targetValue (name "?{repeatatleast(concat(<low>,<let>),1)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or lower-case letter") (targetValue (name "?{or(concat(<!>,<@>),<low>)}")))
(example (utterance "lines without a vowel with string leftquoatation ! rightquoatation") (targetValue (name "?{concat(not(<vow>),<!>)}")))
(example (utterance "lines with a letter , leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<!>),<vow>),<@>}")))
(example (utterance "lines starting with a character and string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<any>),concat(<!>,<@>)}")))
(example (utterance "lines which are ending with a lower-case letter or containing a letter") (targetValue (name "?{endwith(<low>),contain(<let>)}")))
(example (utterance "lines with 6 or more leftquoatation ! rightquoatation or ending with 6 or more leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,endwith(<@>))}")))
(example (utterance "lines ending in a capital letter before leftquoatation ! rightquoatation then string leftquoatation @ rightquoatation") (targetValue (name "?{endwith(concat(<cap>,<!>)),<@>}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , and a number after leftquoatation @ rightquoatation") (targetValue (name "?{<@>,and(and(<vow>,<!>),<num>)}")))
(example (utterance "lines not ending in a character before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(endwith(<any>),<!>)}")))
(example (utterance "lines with words that contain only a character and leftquoatation ! rightquoatation in them") (targetValue (name "?{<any>,<!>}")))
(example (utterance "lines without a capital letter , character , or vowel") (targetValue (name "?{or(or(<cap>,<any>),<vow>)}")))
(example (utterance "lines with a capital letter preceded by another letter") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and a vowel") (targetValue (name "?{endwith(<!>),and(<@>,<vow>)}")))
(example (utterance "lines with character , 4 ore more times before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,4),<!>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , a letter , and character") (targetValue (name "?{and(and(<!>,<let>),<any>)}")))
(example (utterance "lines not having a number followed by leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<num>,<!>),<@>)}")))
(example (utterance "lines without a number , leftquoatation ! rightquoatation , or a lower-case letter") (targetValue (name "?{or(or(<num>,<!>),<low>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation , leftquoatation # rightquoatation , or a character .") (targetValue (name "?{<!>,or(or(<any>,<@>),<#>)}")))
(example (utterance "lines with zero or more of string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{or(and(<!>,<@>),<#>)}")))
(example (utterance "lines with a lower-case letter and a character at least 6 times") (targetValue (name "?{contain(<low>),repeatatleast(<any>,6)}")))
(example (utterance "lines with a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{<let>,repeatatleast(<!>,1)}")))
(example (utterance "lines with character before letter , 4 or more times") (targetValue (name "?{<any>,repeatatleast(<let>,4)}")))
(example (utterance "lines having both a lower-case and a capital letter") (targetValue (name "?{and(<low>,<cap>)}")))
(example (utterance "lines that do not have upper or lower case letter and do not contain leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<low>),<!>)}")))
(example (utterance "lines ending with a number 1 or more times") (targetValue (name "?{repeatatleast(endwith(<num>),1)}")))
(example (utterance "lines beginning with a capital letter at least 4 times") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines with either a number , a letter , or leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{concat(or(or(<num>,<let>),<!>),<@>)}")))
(example (utterance "lines not ending with lower-case letter , 2 or more times") (targetValue (name "?{repeatatleast(endwith(<low>),2)}")))
(example (utterance "items with leftquoatation ! rightquoatation or a numeral or a character preceding an upper case letter .") (targetValue (name "?{and(and(<any>,<num>),<!>),<cap>}")))
(example (utterance "lines having the following a vowel , leftquoatation ! rightquoatation , and a character before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<vow>,<!>),<any>),<@>}")))
(example (utterance "lines starting with lower-case letter and string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{startwith(<low>),star(<!>)}")))
(example (utterance "lines with a number , string leftquoatation ! rightquoatation , or letter") (targetValue (name "?{or(or(<num>,<!>),<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before ending with a lower case letter") (targetValue (name "?{and(<!>,<@>),endwith(<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before letter") (targetValue (name "?{<!>,<let>}")))
(example (utterance "lines with words and characters or leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "a line with a capital letter at the end") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines with zero or more of a character before vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<any>,<vow>),<!>)}")))
(example (utterance "lines that have a leftquoatation ! rightquoatation string appear before something that's not a leftquoatation @ rightquoatation string") (targetValue (name "?{contain(<!>),not(<@>)}")))
(example (utterance "lines in which a capital letter is seen prior to a character being contained") (targetValue (name "?{<cap>,contain(<any>)}")))
(example (utterance "lines not ending in a letter") (targetValue (name "?{repeatatleast(endwith(<let>),1)}")))
(example (utterance "lines with words an a letter or leftquoatation ! rightquoatation") (targetValue (name "?{<let>,repeatatleast(<!>,1)}")))
(example (utterance "lines containing letter 3 times and a number") (targetValue (name "?{and(repeatatleast(<cap>,3),<num>)}")))
(example (utterance "lines with a capital letter and string leftquoatation ! rightquoatation before character") (targetValue (name "?{and(<cap>,<!>),<any>}")))
(example (utterance "lines starting with a capital and containing number") (targetValue (name "?{startwith(<cap>),contain(<num>)}")))
(example (utterance "items terminating with leftquoatation ! rightquoatation at least 4 times .") (targetValue (name "?{repeatatleast(contain(<!>),4)}")))
(example (utterance "lines containing a letter before 7 or more capital letters") (targetValue (name "?{<let>,repeatatleast(<cap>,7)}")))
(example (utterance "lines with a lower-case letter and vowel ending in string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<vow>),endwith(<!>)}")))
(example (utterance "lines with words and a capital letter before a number") (targetValue (name "?{star(<cap>),<num>}")))
(example (utterance "lines with no or multiple vowels followed by a lower-case letter") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines don't contain leftquoatation ! rightquoatation , a capital letter , nor leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<cap>),<@>)}")))
(example (utterance "lines that have words ending with leftquoatation ! rightquoatation  , 2 or more times") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "words in lines that include a number or character") (targetValue (name "?{<num>,<any>}")))
(example (utterance "items with a numeral preceding a terminal small letter .") (targetValue (name "?{<num>,endwith(<low>)}")))
(example (utterance "lines with a capital letter , a vowel , or a lower-case letter , before leftquoatation ! rightquoatation .") (targetValue (name "?{<!>,or(or(<cap>,<vow>),<low>)}")))
(example (utterance "lines not having capital letter , 6 or more times and not leftquoatation ! rightquoatation") (targetValue (name "?{and(repeatatleast(<cap>,6),<!>)}")))
(example (utterance "lines with words with ending with a capital letter which is followed by a vowel") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines with capital letter or letter before character") (targetValue (name "?{or(<cap>,<let>),<any>}")))
(example (utterance "lines with a character or lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(or(<any>,<low>),<!>)}")))
(example (utterance "lines with 5 or more of a number or lower-case letter") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "lines with a capital letter followed by a character , at least 5 times") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines which are starting with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation , zero or more times") (targetValue (name "?{startwith(<!>),star(<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a number or a capital letter") (targetValue (name "?{<!>,or(<num>,<cap>)}")))
(example (utterance "lines with less than 5 of leftquoatation ! rightquoatation and no characters") (targetValue (name "?{and(repeatatleast(<!>,5),<any>)}")))
(example (utterance "lines having words and a character before leftquoatation ! rightquoatation 3 times") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "items with words containing a character preceding leftquoatation ! rightquoatation at least 7 times .") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines ending with a lower-case letter 7 times") (targetValue (name "?{repeatatleast(<low>,7)}")))
(example (utterance "all lines") (targetValue (name "?{contain(and(<vow>,<!>))}")))
(example (utterance "lines that have words containing a letter or a number") (targetValue (name "?{<let>,<num>}")))
(example (utterance "lines with a capital letter or a letter at least twice") (targetValue (name "?{or(<cap>,<let>)}")))
(example (utterance "items with any letter or character preceding a numeral .") (targetValue (name "?{and(and(<any>,<low>),<let>),<num>}")))
(example (utterance "lines with a lower-case letter and capital letter , 2 or more times") (targetValue (name "?{<low>,repeatatleast(<cap>,2)}")))
(example (utterance "lines which are containing leftquoatation ! rightquoatation , a lower-case letter , and a letter") (targetValue (name "?{and(and(<!>,<low>),<let>)}")))
(example (utterance "lines with words and 7 or more numbers") (targetValue (name "?{repeatatleast(<num>,7)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation 1 time or contain a number") (targetValue (name "?{repeatatleast(<!>,1),contain(<num>)}")))
(example (utterance "lines with a number , string leftquoatation ! rightquoatation , or vowel") (targetValue (name "?{or(or(<num>,<!>),<vow>)}")))
(example (utterance "lines with a vowel before leftquoatation ! rightquoatation or a capital letter at least once in them") (targetValue (name "?{or(concat(<vow>,<!>),<cap>)}")))
(example (utterance "lines with the word leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{<!>,<let>}")))
(example (utterance "lines containing words with a number , a lowercase letter and leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<!>),<low>}")))
(example (utterance "lines with a letter or string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{or(<let>,<!>),<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a vowel , and leftquoatation @ rightquoatation at least once") (targetValue (name "?{and(and(<!>,<vow>),<@>)}")))
(example (utterance "lines with a number or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a character") (targetValue (name "?{or(or(<num>,<!>),<@>),<any>}")))
(example (utterance "lines that have 1 character followed by the word leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(concat(<any>,<!>),<num>)}")))
(example (utterance "lines with words ending with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),<@>}")))
(example (utterance "lines with a lower-case letter before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<low>,<!>),<@>)}")))
(example (utterance "words with a number before a letter in the lines .") (targetValue (name "?{<num>,<let>}")))
(example (utterance "items with either a letter , a vowel , or leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<!>,<let>),<vow>),<@>}")))
(example (utterance "lines with zero or more of a lower-case letter , character , and string leftquoatation ! rightquoatation or a letter") (targetValue (name "?{or(and(and(<low>,<any>),<!>),<let>)}")))
(example (utterance "lines ending with words 7 or more times with a letter") (targetValue (name "?{repeatatleast(<let>,7)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower case letter then by a capital letter .") (targetValue (name "?{<!>,concat(<low>,<cap>)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation preceding a number") (targetValue (name "?{<!>,repeatatleast(<num>,1)}")))
(example (utterance "lines containing a letter , leftquoatation ! rightquoatation , a character .") (targetValue (name "?{and(and(<let>,<!>),<any>)}")))
(example (utterance "lines which are having a letter , a lower-case letter , and a number before a capital letter") (targetValue (name "?{and(and(<let>,<low>),<num>),<cap>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation after a capital letter") (targetValue (name "?{<cap>,contain(<!>)}")))
(example (utterance "lines that end with a character before leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{endwith(star(<any>)),<!>}")))
(example (utterance "items with words containing a small letter at least 1 time .") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "lines with either leftquoatation ! rightquoatation or a capital letter before a vowel") (targetValue (name "?{contain(or(<!>,<cap>)),<vow>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation with number following") (targetValue (name "?{<!>,concat(<@>,<num>)}")))
(example (utterance "lines containing a capital letter and followed by 6 or more numbers") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "the lines that contain  words with a letter repeated 7 or more times") (targetValue (name "?{<let>}")))
(example (utterance "lines with a number after a character at least zero times") (targetValue (name "?{<any>,star(<num>)}")))
(example (utterance "lines with words with a vowel , string leftquoatation ! rightquoatation , or capital letter") (targetValue (name "?{or(or(<vow>,<!>),<cap>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , a character , and leftquoatation @ rightquoatation in them") (targetValue (name "?{and(<!>,<any>),<@>}")))
(example (utterance "lines with at least 1 letter") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines having character ,  string leftquoatation ! rightquoatation , and vowel before capital letter") (targetValue (name "?{and(and(<any>,<!>),<vow>),<cap>}")))
(example (utterance "lines containing either a vowel , a lower-case letter , or leftquoatation ! rightquoatation in them") (targetValue (name "?{or(or(<vow>,<low>),<!>)}")))
(example (utterance "lines that contain a capital letter , a lower-case letter , and a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<low>),<let>),<!>}")))
(example (utterance "lines starting in zero or more of a character before a lower-case letter") (targetValue (name "?{star(concat(<any>,<low>))}")))
(example (utterance "lines with a capital letter before a letter") (targetValue (name "?{repeatatleast(<cap>,1),<let>}")))
(example (utterance "lines with words with a vowel , letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<let>),<!>}")))
(example (utterance "lines with a number at least 2 times") (targetValue (name "?{repeatatleast(<num>,2)}")))
(example (utterance "lines without a number , lower-case letter , or character") (targetValue (name "?{or(or(<num>,<low>),<any>)}")))
(example (utterance "the lines ending with either a number or character") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter , 6 or more times in them") (targetValue (name "?{<!>,repeatatleast(<low>,6)}")))
(example (utterance "lines wherein a character is preceded by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines ending in 6 or more of a lower-case letter or number") (targetValue (name "?{endwith(or(<low>,<num>))}")))
(example (utterance "you are going to be here and you are going to do well .") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines which are not having a vowel , not a character , and not a lower-case letter") (targetValue (name "?{and(and(<vow>,<any>),<low>)}")))
(example (utterance "lines with a letter preceded by the string 'dog , a character and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<!>,<any>),<!>),<let>}")))
(example (utterance "lines with letter followed by vowel , 3 or more times") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>),<num>}")))
(example (utterance "lines with a capital , 7 or more times before character") (targetValue (name "?{repeatatleast(<cap>,7),<any>}")))
(example (utterance "lines with numbers at least 6 times having a vowel") (targetValue (name "?{repeatatleast(<num>,6),contain(<vow>)}")))
(example (utterance "lines that contain a number followed by a character , at least 4 times") (targetValue (name "?{<num>,repeatatleast(<any>,4)}")))
(example (utterance "lines with a lower-case letter before letter then capital letter") (targetValue (name "?{concat(<low>,<let>),<cap>}")))
(example (utterance "lines with 6 or more of a letter before a vowel") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines not containing string leftquoatation ! rightquoatation at least once") (targetValue (name "?{not(repeatatleast(<!>,1))}")))
(example (utterance "lines where either leftquoatation ! rightquoatation or leftquoatation @ rightquoatation are preceded by leftquoatation # rightquoatation") (targetValue (name "?{or(concat(<#>,<!>),<@>)}")))
(example (utterance "a number appears before a character and have leftquoatation ! rightquoatation in the lines") (targetValue (name "?{concat(<num>,<any>),contain(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a letter before leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{concat(<!>,<let>),or(<@>,<vow>)}")))
(example (utterance "lines without a number , a vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<vow>),<!>)}")))
(example (utterance "lines with 5 or more of a vowel before capital letter") (targetValue (name "?{contain(concat(<vow>,<cap>))}")))
(example (utterance "lines having a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<!>)}")))
(example (utterance "lines with a capital and lower-case letter") (targetValue (name "?{and(and(<cap>,<low>),<let>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation preceding a capital letter or leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<!>,<cap>),<@>)}")))
(example (utterance "lines containing a character before a number") (targetValue (name "?{repeatatleast(<any>,1),<num>}")))
(example (utterance "lines containing lower-case letter followed by number") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "items with a numeral or small letter .") (targetValue (name "?{and(<low>,<num>)}")))
(example (utterance "lines that start with a capital letter that precedes a vowel") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines containing a capital letter , a character , or a vowel") (targetValue (name "?{or(<cap>,<any>),<vow>}")))
(example (utterance "lines without a lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(startwith(<low>),<!>)}")))
(example (utterance "lines containing words that end with a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines with a vowel or number before a lower-case letter") (targetValue (name "?{or(<vow>,<num>),<low>}")))
(example (utterance "lines with words of only a number") (targetValue (name "?{<num>}")))
(example (utterance "4 lines with a character , each followed by a vowel") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "items with a small letter at least5times .") (targetValue (name "?{<low>}")))
(example (utterance "lines with a lower-case letter before a vowel , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation in them") (targetValue (name "?{<low>,and(and(<vow>,<!>),<@>)}")))
(example (utterance "items with a character preceding a vowel , leftquoatation ! rightquoatation , or an uppercase letter .") (targetValue (name "?{<any>,and(and(<vow>,<cap>),<!>)}")))
(example (utterance "lines with words and 4 or more of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,4)}")))
(example (utterance "lines with a capital and lower-case letter before a number") (targetValue (name "?{and(<cap>,<low>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation , a character , and a lower-case letter") (targetValue (name "?{<!>,and(and(<@>,<any>),<low>)}")))
(example (utterance "lines that contain lower case letters and at least 1 character .") (targetValue (name "?{<low>,<any>}")))
(example (utterance "lines which are containing a character followed by a capital letter") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines with a number or 5 or more letters") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines with a lower-case letter before string leftquoatation ! rightquoatation or character") (targetValue (name "?{<low>,or(<!>,<any>)}")))
(example (utterance "lines ending in a 2 or more characters") (targetValue (name "?{repeatatleast(endwith(<any>),2)}")))
(example (utterance "lines with a capital letter , 7 or more times before a lower-case letter in them") (targetValue (name "?{repeatatleast(<cap>,7),<low>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation after leftquoatation @ rightquoatation or a character") (targetValue (name "?{or(<@>,concat(<any>,<!>))}")))
(example (utterance "lines without a number and leftquoatation ! rightquoatation") (targetValue (name "?{and(not(<num>),<!>)}")))
(example (utterance "lines with a vowel and starting in string leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,startwith(<!>))}")))
(example (utterance "lines that do not contain words that begin with a capital letter") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines with a capital letter before a lower-case letter") (targetValue (name "?{concat(repeatatleast(<cap>,1),<low>)}")))
(example (utterance "lines containing letter , character , and a capital letter") (targetValue (name "?{and(and(<let>,<any>),<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation with a letter") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with no capital letter , leftquoatation ! rightquoatation , or character") (targetValue (name "?{and(and(<cap>,<!>),<any>)}")))
(example (utterance "items with a vowel , a small letter , or a character .") (targetValue (name "?{or(or(<any>,<low>),<vow>)}")))
(example (utterance "the lines that have a capital letter , a character , and a vowel all coming before a number") (targetValue (name "?{and(and(<cap>,<any>),<vow>),<num>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by vowel , at least zero times .") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines with words with either a number , lower-case letter , or character") (targetValue (name "?{or(<num>,<low>),<any>}")))
(example (utterance "lines not containing only leftquoatation ! rightquoatation before a number in it") (targetValue (name "?{not(<!>),<num>}")))
(example (utterance "lines with words with number and lower-case letter before a capital") (targetValue (name "?{and(<num>,<low>),<cap>}")))
(example (utterance "lines with 3 or more of leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or lower-case letter") (targetValue (name "?{concat(<!>,<@>),repeatatleast(<low>,3)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation and followed by a vowel") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines that have the leftquoatation ! rightquoatation string present and , after that , a small letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with a vowel followed by a capital letter or a number") (targetValue (name "?{<vow>,or(<cap>,<num>)}")))
(example (utterance "lines with a number before lower-case letter or character") (targetValue (name "?{<num>,or(<low>,<any>)}")))
(example (utterance "lines starting with words a long with numbers") (targetValue (name "?{star(<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character") (targetValue (name "?{contain(concat(<!>,<any>))}")))
(example (utterance "lines were vowels come before a lower case letter") (targetValue (name "?{<vow>,<low>}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation before a vowel or number") (targetValue (name "?{<!>,or(<vow>,<num>)}")))
(example (utterance "lines not having any of leftquoatation ! rightquoatation , a character , or a letter") (targetValue (name "?{and(and(<!>,<any>),<let>)}")))
(example (utterance "lines with a letter before a number") (targetValue (name "?{repeatatleast(<let>,1),<num>}")))
(example (utterance "lines with less than 6 characters and letters") (targetValue (name "?{not(<any>),repeatatleast(<let>,6)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a letter , and a number iin them") (targetValue (name "?{<!>,and(and(<@>,<let>),<num>)}")))
(example (utterance "items with words containing a letter preceding a character .") (targetValue (name "?{<let>,<any>}")))
(example (utterance "lines with character before lower-case letter") (targetValue (name "?{<any>,<low>}")))
(example (utterance "lines with words without string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before a character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines that do not contain a character , vowel or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<vow>),<!>)}")))
(example (utterance "lines that contain words , leftquoatation ! rightquoatation and at least 1 number .") (targetValue (name "?{repeatatleast(<num>,1),<!>}")))
(example (utterance "items with a small letter preceding leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{<low>,concat(<!>,<any>)}")))
(example (utterance "lines containing 3 or more capital letters") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines that have a choice of 1 character , 1 capital letter , or the word leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<cap>),<!>)}")))
(example (utterance "lines without a number followed by leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{not(<num>),and(<!>,<@>)}")))
(example (utterance "lines that contain a letter followed by a lower-case letter , at least once") (targetValue (name "?{<let>,repeatatleast(<low>,1)}")))
(example (utterance "lines that have a vowel before a leftquoatation ! rightquoatation string at least 4 times") (targetValue (name "?{<vow>,repeatatleast(<!>,4)}")))
(example (utterance "lines with words with a number and a capital letter before leftquoatation ! rightquoatation in them") (targetValue (name "?{and(<num>,<cap>),<!>}")))
(example (utterance "lines with a letter followed by a capital letter") (targetValue (name "?{endwith(<let>),<cap>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation and having leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),contain(<@>)}")))
(example (utterance "items with a vowel or a character at least5times .") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before a lower-case letter and a character") (targetValue (name "?{endwith(<low>),concat(<any>,<!>)}")))
(example (utterance "lines with words containing a number , a vowel , and a letter") (targetValue (name "?{and(<num>,<vow>),<let>}")))
(example (utterance "lines with a lower-case letter before a character") (targetValue (name "?{<low>,contain(<any>)}")))
(example (utterance "lines with a capital letter or number") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines ending with number before string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{endwith(<num>),star(<!>)}")))
(example (utterance "lines with a capital  before vowel followed by number") (targetValue (name "?{<cap>,concat(<vow>,<num>)}")))
(example (utterance "lines without a capital , a vowel , or number") (targetValue (name "?{or(or(<cap>,<vow>),<num>)}")))
(example (utterance "lines with words that have a capital letter summary:") (targetValue (name "?{<cap>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation and not starting with string leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,startwith(<@>))}")))
(example (utterance "lines with a lowercase letter that does not precede leftquoatation ! rightquoatation") (targetValue (name "?{<low>,not(<!>)}")))
(example (utterance "lines containing a vowel and a character preceding leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<any>),<!>}")))
(example (utterance "lines with 2 or more of a letter before a capital letter") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines with a capital letter or leftquoatation ! rightquoatation before a number at least 1 time") (targetValue (name "?{or(<!>,concat(<cap>,<num>))}")))
(example (utterance "lines where the last word starts with a capital letter") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines that contain either a vowel or leftquoatation ! rightquoatation zero or more times") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation , a number , and a lower-case letter before a capital letter in them") (targetValue (name "?{and(and(<!>,<num>),<low>),<cap>}")))
(example (utterance "lines with or without a vowel beffore a number") (targetValue (name "?{star(<vow>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel then string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,concat(<vow>,<@>)}")))
(example (utterance "lines with a letter before a number and string leftquoatation ! rightquoatation") (targetValue (name "?{<let>,and(<num>,<!>)}")))
(example (utterance "lines that contain a letter followed by a vowel , zero or more times") (targetValue (name "?{star(<let>),<vow>}")))
(example (utterance "lines with a number followed by a lower-case letter then a character") (targetValue (name "?{concat(<num>,<low>),<any>}")))
(example (utterance "words with a vowel appears 6 or more times in the lines") (targetValue (name "?{<vow>}")))
(example (utterance "lines with words that don't have a capital before string leftquoatation ! rightquoatation") (targetValue (name "?{not(<cap>),<!>}")))
(example (utterance "lines ending with not containing string leftquoatation ! rightquoatation") (targetValue (name "?{contain(not(<!>))}")))
(example (utterance "lines which ends with a small letter") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a character , at least once") (targetValue (name "?{<!>,star(<any>)}")))
(example (utterance "items with any letter and numeral preceding a vowel .") (targetValue (name "?{and(and(<low>,<cap>),<num>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending in letter") (targetValue (name "?{contain(<!>),endwith(<let>)}")))
(example (utterance "lines containing the word leftquoatation ! rightquoatation with a number preceding it") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines ending with or without leftquoatation ! rightquoatation") (targetValue (name "?{endwith(contain(<!>))}")))
(example (utterance "lines that end with dog or zero") (targetValue (name "?{endwith(contain(<!>))}")))
(example (utterance "items without an upper case letter , a numeral , or leftquoatation ! rightquoatation .") (targetValue (name "?{and(and(<num>,<cap>),<!>)}")))
(example (utterance "lines with a character or lower-case letter ending in string leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<low>),endwith(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , vowel , and lower-case letter before number") (targetValue (name "?{and(and(<!>,<vow>),<low>),<num>}")))
(example (utterance "items with a numeral and character preceding a letter .") (targetValue (name "?{and(<any>,<num>),<let>}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , or number before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<num>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a number before a capital letter") (targetValue (name "?{or(or(<!>,<num>),<@>),<cap>}")))
(example (utterance "lines that end with a number and a letter") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines with words that contain a number , leftquoatation ! rightquoatation , and a character") (targetValue (name "?{and(<num>,<!>),<any>}")))
(example (utterance "lines with a vowel or lower-case letter") (targetValue (name "?{contain(<vow>),repeatatleast(<low>,1)}")))
(example (utterance "lines with 6 or more capital letters before a character") (targetValue (name "?{repeatatleast(<cap>,6),<any>}")))
(example (utterance "lines with a number , capital letter , or string leftquoatation ! rightquoatation before a letter") (targetValue (name "?{or(or(<num>,<cap>),<!>),<let>}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , a lower-case letter , or a vowel followed by a letter") (targetValue (name "?{or(or(<!>,<low>),<vow>),<let>}")))
(example (utterance "lines containing lower-case letter or not a capital letter") (targetValue (name "?{or(<low>,not(<cap>))}")))
(example (utterance "lines ending with capital letter , zero or more times or string leftquoatation ! rightquoatation") (targetValue (name "?{or(star(<cap>),<!>)}")))
(example (utterance "lines with a capital letter before ending with leftquoatation ! rightquoatation at least zero times") (targetValue (name "?{star(<cap>),endwith(<!>)}")))
(example (utterance "lines with a vowel then a capital followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<vow>,<cap>),<!>}")))
(example (utterance "lines with a capital letter followed by a letter occurrr zero or more times") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines with words that has at least 4 occurrences of leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation or a lower-case letter and character") (targetValue (name "?{concat(<vow>,<!>),and(<low>,<any>)}")))
(example (utterance "lines with words with ending with a letter , occurring  4 or more times") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with a lower-case letter at the end") (targetValue (name "?{<low>}")))
(example (utterance "lines with 4 or more lower-case letters before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,4),<!>}")))
(example (utterance "lines having lower-case letter , string leftquoatation ! rightquoatation , and string leftquoatation @ rightquoatation before character") (targetValue (name "?{and(and(<low>,<!>),<@>),<any>}")))
(example (utterance "items without a small letter or numeral .") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation before a letter") (targetValue (name "?{or(or(<!>,<@>),<#>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and lower-case letter before non capital letter") (targetValue (name "?{and(<!>,<low>),not(<cap>)}")))
(example (utterance "items with words containg a leter and a numeral .") (targetValue (name "?{<num>,<let>}")))
(example (utterance "items with an upper case letter , leftquoatation ! rightquoatation , or small letter preceding a numeral .") (targetValue (name "?{or(or(<cap>,<!>),<low>),<num>}")))
(example (utterance "the lines that do not have leftquoatation ! rightquoatation coming before a letter") (targetValue (name "?{not(concat(<!>,<let>))}")))
(example (utterance "lines with words that contain containing only vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines with a number 4 or more times") (targetValue (name "?{repeatatleast(<num>,4),<any>}")))
(example (utterance "lines with only a capital letter 1 or more times") (targetValue (name "?{repeatatleast(<cap>,1)}")))
(example (utterance "lines with a letter , a lower-case letter , as well as a vowel followed by leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<low>),<vow>),<!>}")))
(example (utterance "lines with zero or more characters or with a lower-case letter") (targetValue (name "?{star(<any>),contain(<low>)}")))
(example (utterance "lines with words starting with string leftquoatation ! rightquoatation before number") (targetValue (name "?{startwith(<!>),<num>}")))
(example (utterance "lines that do not contain a character or number") (targetValue (name "?{and(<any>,<num>)}")))
(example (utterance "lines starting in words without a lower-case letter") (targetValue (name "?{not(<low>)}")))
(example (utterance "lines with words that have no character") (targetValue (name "?{<any>}")))
(example (utterance "lines starting in zero or more of a number before a vowel") (targetValue (name "?{endwith(concat(<num>,<vow>))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or letter before character") (targetValue (name "?{or(<!>,<let>),<any>}")))
(example (utterance "lines with a character after a capital letter") (targetValue (name "?{and(<cap>,<let>),<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a character , and a lower-case letter") (targetValue (name "?{and(<!>,<any>),<low>}")))
(example (utterance "lines with a vowel followed by a number occurring zero or more times") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation , a lower-case letter , or leftquoatation @ rightquoatation after a vowel") (targetValue (name "?{endwith(<vow>),or(or(<!>,<low>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or letter before a number") (targetValue (name "?{or(or(<!>,<let>),<low>),<num>}")))
(example (utterance "lines having lower case letters before a letter , twice or more") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "the lines containing either a vowel or leftquoatation ! rightquoatation repeated 4 or more times") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation with string leftquoatation @ rightquoatation") (targetValue (name "?{startwith(concat(<!>,<@>)),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , letter , and character before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<let>),<any>),<@>}")))
(example (utterance "lines with vowel before string leftquoatation ! rightquoatation and letter") (targetValue (name "?{<vow>,and(<!>,<let>)}")))
(example (utterance "lines that do not contain a character 1 or more times .") (targetValue (name "?{repeatatleast(contain(<any>),1)}")))
(example (utterance "lines starting with words with number and lower-case letter") (targetValue (name "?{and(<num>,<low>)}")))
(example (utterance "lines with a number before lower-case letter") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "linesleftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{or(<let>,<!>),<cap>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before letter , 4 or more times") (targetValue (name "?{<!>,repeatatleast(<let>,4)}")))
(example (utterance "lines that have at least 3 combinations of a number followed by a character") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{or(<!>,<@>),<vow>}")))
(example (utterance "items with works containing a small letter at least twice .") (targetValue (name "?{<low>}")))
(example (utterance "lines with a letter or a lower-case letter , 6 or more times in them") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a vowel at least twice") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines without a vowel , or letter") (targetValue (name "?{and(and(<vow>,<let>),<low>)}")))
(example (utterance "lines containing a lower-case letter followed by a number and a vowel") (targetValue (name "?{contain(concat(<low>,<vow>)),<num>}")))
(example (utterance "items without leftquoatation ! rightquoatation .") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines containing 4 or more letters") (targetValue (name "?{contain(endwith(<let>))}")))
(example (utterance "lines with no vowel before leftquoatation ! rightquoatation and no leftquoatation @ rightquoatation") (targetValue (name "?{and(concat(<vow>,<!>),<@>)}")))
(example (utterance "items with a numeral , at least6times .") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "lines with a capital letter , but not a letter nor leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,not(or(<let>,<!>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation appearing before either leftquoatation @ rightquoatation , leftquoatation # rightquoatation , or a character") (targetValue (name "?{<!>,or(or(<@>,<#>),<any>)}")))
(example (utterance "lines that have words and a character followed by a vowel") (targetValue (name "?{<any>,<vow>}")))
(example (utterance "lines having no string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{star(<!>)}")))
(example (utterance "lines ending with a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{endwith(or(<cap>,<!>))}")))
(example (utterance "lines which are  containing a lower-case letter , a character , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<any>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by 4 or more lower-case letters") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with a vowel followed by a lower-case letter , 7 or more times in them") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or character before letter , 2 or more times") (targetValue (name "?{or(<!>,concat(<any>,<let>))}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation and lower-case letter and capital letter") (targetValue (name "?{<vow>,and(and(<!>,<low>),<cap>)}")))
(example (utterance "lines stating with a number before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a letter") (targetValue (name "?{startwith(<num>),and(and(<!>,<let>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter followed by a vowel at least once") (targetValue (name "?{concat(<!>,<let>),repeatatleast(<vow>,1)}")))
(example (utterance "lines with a lower-case letter before number and string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,and(<num>,<!>)}")))
(example (utterance "lines with a letter after leftquoatation ! rightquoatation 1 or more times") (targetValue (name "?{or(concat(<!>,<let>),<num>)}")))
(example (utterance "lines with less than 7 of leftquoatation ! rightquoatation and without a vowel") (targetValue (name "?{and(repeatatleast(<!>,7),<vow>)}")))
(example (utterance "lines with a vowel followed by a capital letter before a lower-case letter in them") (targetValue (name "?{concat(<vow>,<cap>),<low>}")))
(example (utterance "lines having a lower-case letter before a character or a letter") (targetValue (name "?{<low>,star(or(<any>,<let>))}")))
(example (utterance "lines that contain a number or a vowel") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending with a number") (targetValue (name "?{repeatatleast(<!>,1),endwith(<num>)}")))
(example (utterance "lines with no letter either zero times or greater than zero times") (targetValue (name "?{star(<let>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation at least once or capital letter") (targetValue (name "?{or(repeatatleast(<!>,1),<cap>)}")))
(example (utterance "lines with words and a number") (targetValue (name "?{repeatatleast(<num>,1)}")))
(example (utterance "lines with number before either leftquoatation ! rightquoatation , a character , or a capital") (targetValue (name "?{<num>,or(or(<!>,<any>),<cap>)}")))
(example (utterance "lines with a character followed by vowel , 3 or more times") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "lines containing letter , character , vowel before leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<any>),<vow>),<!>}")))
(example (utterance "lines with number before a capital or character") (targetValue (name "?{<num>,or(<cap>,<any>)}")))
(example (utterance "before leftquoatation ! rightquoatation there must be lines with either leftquoatation @ rightquoatation , a vowel , or a number") (targetValue (name "?{or(or(<@>,<vow>),<num>),<!>}")))
(example (utterance "lines with 5 or more of a lower-case letter before a letter") (targetValue (name "?{repeatatleast(<low>,5),<let>}")))
(example (utterance "lines containing a letter followed by 2 or more times a number") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "items with a character preceding a vowel or leftquoatation ! rightquoatation .") (targetValue (name "?{<any>,or(<vow>,<!>)}")))
(example (utterance "lines containing string like leftquoatation ! rightquoatation , a number , a small letter before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<num>),<low>),<@>}")))
(example (utterance "lines starting in 5 or more vowels before a number") (targetValue (name "?{startwith(repeatatleast(<vow>,5)),<num>}")))
(example (utterance "lines that contain a capital letter followed by a letter and a character") (targetValue (name "?{concat(<cap>,<let>),<any>}")))
(example (utterance "4 capital letters before every lower case letter .") (targetValue (name "?{repeatatleast(<cap>,4),<low>}")))
(example (utterance "lines with a number then leftquoatation ! rightquoatation that appears once or is repeated") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines with a lower-case letter after a number") (targetValue (name "?{<num>,<low>}")))
(example (utterance "lines with words with a character and string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,1),<!>}")))
(example (utterance "lines with number before vowel , zero or more times") (targetValue (name "?{<num>,star(<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before non character") (targetValue (name "?{concat(<!>,not(<any>))}")))
(example (utterance "lines with a capital 7 times") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines only containing 5 or more of leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines starting in zero or more of a number before string leftquoatation ! rightquoatation") (targetValue (name "?{star(concat(<num>,<!>))}")))
(example (utterance "lines with a letter or a number followed by a capital letter") (targetValue (name "?{or(<let>,concat(<num>,<cap>))}")))
(example (utterance "lines ending in words with 6 or more of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,6)}")))
(example (utterance "lines which are starting with a number or leftquoatation ! rightquoatation and a lower-case letter") (targetValue (name "?{startwith(<num>),and(<!>,<low>)}")))
(example (utterance "lines with 7 words with leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with a lower-case letter or a character before a capital letter in them") (targetValue (name "?{or(<low>,<any>),<cap>}")))
(example (utterance "lines starting in zero or more of leftquoatation ! rightquoatation before  a vowel") (targetValue (name "?{star(startwith(<!>)),<vow>}")))
(example (utterance "lines which have a letter occurring at least6times before a capital letter appears") (targetValue (name "?{repeatatleast(<let>,6),<cap>}")))
(example (utterance "lines containing a vowel zero times") (targetValue (name "?{contain(not(<vow>))}")))
(example (utterance "lines starting with a capital letter and leftquoatation ! rightquoatation at least once in them") (targetValue (name "?{startwith(<cap>),repeatatleast(<!>,1)}")))
(example (utterance "lines do not contain a character , a lower-case letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<low>),<!>)}")))
(example (utterance "lines which are not ending with a letter , zero or more times") (targetValue (name "?{star(endwith(<let>))}")))
(example (utterance "lines with any letter , lower-case letter , or a capital letter") (targetValue (name "?{or(or(<low>,<let>),<cap>)}")))
(example (utterance "items with small letter or leftquoatation ! rightquoatation or uppercase letter , or leftquoatation @ rightquoatation .") (targetValue (name "?{and(<low>,or(or(<!>,<cap>),<@>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a capital letter") (targetValue (name "?{and(<!>,<@>),<cap>}")))
(example (utterance "lines with a number and string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<num>,<!>),<@>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter or string leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<!>,<let>),<@>)}")))
(example (utterance "lines with a number , character , and vowel before a capital letter") (targetValue (name "?{and(and(<num>,<any>),<vow>),<cap>}")))
(example (utterance "lines with a letter followed by the word leftquoatation ! rightquoatation at least 7 times .") (targetValue (name "?{repeatatleast(concat(<let>,<!>),7)}")))
(example (utterance "lines with a character before a letter") (targetValue (name "?{repeatatleast(<any>,1),<let>}")))
(example (utterance "lines that contain a character , lower-case letter , or a number") (targetValue (name "?{or(or(<any>,<cap>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or 'truck or a lower-case letter before a number") (targetValue (name "?{or(or(<!>,<low>),<@>),<let>}")))
(example (utterance "items with a letter preceding a numeral at least 4 times .") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines with 'dogleftquoatation  or a character") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines ending in 7 or more letters or vowels") (targetValue (name "?{or(<let>,repeatatleast(<vow>,7))}")))
(example (utterance "lines that do not contain a capital letter or vowel , zero times or more") (targetValue (name "?{not(<cap>),star(<vow>)}")))
(example (utterance "lines containing a vowel , a character , or a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<any>),<let>),<!>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or letter") (targetValue (name "?{or(or(<!>,<cap>),<let>)}")))
(example (utterance "lines not having a letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "items with any letter preceding an uppe case letter .") (targetValue (name "?{and(and(<vow>,<low>),<let>),<cap>}")))
(example (utterance "lines without only a character") (targetValue (name "?{not(<any>)}")))
(example (utterance "lines starting with a capital letter following a number") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines ending in vowel , string leftquoatation ! rightquoatation , and number or a letter") (targetValue (name "?{or(and(and(<vow>,<!>),<num>),<let>)}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a character") (targetValue (name "?{<num>,or(or(<!>,<any>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation a letter , or leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{or(or(or(<!>,<let>),<@>),<#>)}")))
(example (utterance "lines with a lower-case letter before a capital letter") (targetValue (name "?{<low>,repeatatleast(<cap>,1)}")))
(example (utterance "lines with character , 3 or more times before letter") (targetValue (name "?{repeatatleast(<any>,3),<let>}")))
(example (utterance "lines with a lower-case letter or 5 strings of leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(and(<!>,<let>),<@>)}")))
(example (utterance "lines with a character or leftquoatation ! rightquoatation at least 7 times") (targetValue (name "?{repeatatleast(or(<any>,<!>),7)}")))
(example (utterance "lines starting with a vowel after a character") (targetValue (name "?{startwith(<any>),<vow>}")))
(example (utterance "lines that have words with a capital letter , a lower-case letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,<low>),<!>}")))
(example (utterance "lines with a vowel followed by a capital letter and leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,and(<cap>,<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or vowel , zero or more times") (targetValue (name "?{<!>,star(or(<@>,<vow>))}")))
(example (utterance "lines containing a capital letter and a lower-case letter") (targetValue (name "?{<cap>,<low>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string 'truck , character , or vowel") (targetValue (name "?{<!>,or(or(<@>,<any>),<vow>)}")))
(example (utterance "lines that contain a character or contain leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>),contain(<any>)}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{concat(<vow>,<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines which are containing either leftquoatation ! rightquoatation , a character , or a number") (targetValue (name "?{or(or(<!>,<any>),<num>)}")))
(example (utterance "items with a numeral preceding a letter .") (targetValue (name "?{star(<num>),<let>}")))
(example (utterance "lines with a charater , a capital letter") (targetValue (name "?{and(<any>,<cap>),<let>}")))
(example (utterance "lines which contain a vowel , number or character`") (targetValue (name "?{or(or(<vow>,<num>),<any>)}")))
(example (utterance "lines starting with a number before lower-case letter") (targetValue (name "?{startwith(<num>),<low>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation 4 times") (targetValue (name "?{repeatatleast(concat(<let>,<!>),4)}")))
(example (utterance "lines with lower-case letter before string leftquoatation ! rightquoatation with string leftquoatation @ rightquoatation or capital letter") (targetValue (name "?{concat(<low>,<!>),or(<@>,<cap>)}")))
(example (utterance "lines having a letter , leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a number") (targetValue (name "?{or(or(<let>,<!>),<@>),<num>}")))
(example (utterance "lines with a capital before words with ending with letter") (targetValue (name "?{<cap>,endwith(<let>)}")))
(example (utterance "lines ending in words with a letter and number") (targetValue (name "?{and(<let>,<num>)}")))
(example (utterance "line that have leftquoatation ! rightquoatation as well as a capital and another letter") (targetValue (name "?{and(and(<let>,<cap>),<!>)}")))
(example (utterance "lines where the last word is all lower-case") (targetValue (name "?{<low>}")))
(example (utterance "lines with a vowel or character before a capital letter") (targetValue (name "?{or(<vow>,<any>),<cap>}")))
(example (utterance "lines ending in a character without vowel") (targetValue (name "?{startwith(<any>),not(<vow>)}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{<cap>,or(or(<!>,<@>),<vow>)}")))
(example (utterance "a number before a capital letter at least once in the lines .") (targetValue (name "?{<num>,repeatatleast(<cap>,1)}")))
(example (utterance "a vowel ends a line containing a capital letter") (targetValue (name "?{endwith(<vow>),contain(<cap>)}")))
(example (utterance "lines containing a character or number") (targetValue (name "?{<any>,<num>}")))
(example (utterance "lines with a character or leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{or(<any>,<!>),<cap>}")))
(example (utterance "lines which are containing ending with leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{endwith(repeatatleast(<!>,6))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character , capital letter , or string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<any>,<cap>),<@>)}")))
(example (utterance "lines which are not having leftquoatation ! rightquoatation , 5 or more times and not a lower-case letter") (targetValue (name "?{and(repeatatleast(<!>,5),<low>)}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a capital letter") (targetValue (name "?{<!>,concat(<@>,<cap>)}")))
(example (utterance "items with a character preceding an upper case letter at least twice .") (targetValue (name "?{<any>,repeatatleast(<cap>,2)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a vowel , character , and string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),and(and(<vow>,<any>),<@>)}")))
(example (utterance "lines with a number before a capital letter") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines with words that don't string leftquoatation ! rightquoatation and lower-case letter") (targetValue (name "?{not(<!>),<low>}")))
(example (utterance "lines containing words which have a letter before a number .") (targetValue (name "?{<let>,<num>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a letter and numeral and leftquoatation @ rightquoatation .") (targetValue (name "?{<!>,and(and(<let>,<@>),<num>)}")))
(example (utterance "lines with a number that comes before a letter , and a vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<!>),<num>),<let>}")))
(example (utterance "lines with words and a letter or number") (targetValue (name "?{and(<let>,<num>),<low>}")))
(example (utterance "lines with words ending with a character , 3 or more times .") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "items with words containing a letter , a small letter , or a character .") (targetValue (name "?{or(<any>,<low>),<let>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before either string leftquoatation @ rightquoatation , capital letter , or letter") (targetValue (name "?{<!>,or(or(<@>,<cap>),<let>)}")))
(example (utterance "lines with a capital letter , vowel , or character") (targetValue (name "?{and(and(<cap>,<vow>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation with a capital letter") (targetValue (name "?{repeatatleast(<!>,1),startwith(<cap>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation , a vowel , or a letter") (targetValue (name "?{or(or(<!>,<let>),<vow>)}")))
(example (utterance "lines with a vowel before a character") (targetValue (name "?{<vow>,repeatatleast(<any>,1)}")))
(example (utterance "lines which are not having leftquoatation ! rightquoatation , not a capital letter , and not a number") (targetValue (name "?{and(and(<!>,<cap>),<num>)}")))
(example (utterance "lines with 6 characters , each followed by a number") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines containing a lower-case letter or leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation 1 or more times") (targetValue (name "?{or(<low>,<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines containing 3 lower-case letters or 3 vowels") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines containing a vowel and a character , zero or more times in them") (targetValue (name "?{contain(<vow>),star(<any>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines not having a lower-case letter") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "does not have leftquoatation ! rightquoatation , a character , or a lowercase letter") (targetValue (name "?{or(or(<!>,<any>),<low>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation and vowel before a character") (targetValue (name "?{and(<!>,<vow>),<any>}")))
(example (utterance "lines ending in vowel") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "lines ending with words with a character 6 times") (targetValue (name "?{repeatatleast(<any>,6)}")))
(example (utterance "lines that have a lower-case letter come after a capital letter , with a character following") (targetValue (name "?{<cap>,concat(<low>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or not a number") (targetValue (name "?{startwith(<!>),not(<num>)}")))
(example (utterance "lines with lower-case letters , 3 or more times before a capital letter") (targetValue (name "?{repeatatleast(<low>,3),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter and a vowel in them") (targetValue (name "?{<!>,and(<let>,<vow>)}")))
(example (utterance "lines that have a lower-case letter followed by 2 or more numbers") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "lines not ending with a character") (targetValue (name "?{contain(not(<any>))}")))
(example (utterance "a capital letter followed by a vowel or a number before a character appears in the lines") (targetValue (name "?{concat(<cap>,<vow>),concat(<num>,<any>)}")))
(example (utterance "lines with zero or more characters without a number") (targetValue (name "?{star(<any>),not(<num>)}")))
(example (utterance "lines with at least 1 letter and leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,1),<!>}")))
(example (utterance "lines that do not contain only a character , at least 4 or more times") (targetValue (name "?{repeatatleast(<any>,4)}")))
(example (utterance "lines with 3 or more of a number or character") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "lines with vowel before capital letter , 7 or so times") (targetValue (name "?{<vow>,repeatatleast(<cap>,7)}")))
(example (utterance "lines that contain a letter , a vowel , or a capital letter") (targetValue (name "?{or(or(<let>,<vow>),<cap>)}")))
(example (utterance "lines with a number or vowel , zero or more times") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines not starting in string leftquoatation ! rightquoatation with string leftquoatation @ rightquoatation") (targetValue (name "?{startwith(concat(<!>,<@>))}")))
(example (utterance "lines without  a character or a capital letter") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "lines with 3 or more words that start in a capital letter") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines with a capital followed by character , 3 or more times") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines containing words with a vowel and a lower-case letter") (targetValue (name "?{<vow>,<low>}")))
(example (utterance "lines in which an upper-case letter comes before a lower-case letter at least6times") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines which have a capital letter and nothing else") (targetValue (name "?{<cap>}")))
(example (utterance "items with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a vowel preceding a character .") (targetValue (name "?{or(or(<vow>,<!>),<@>),<any>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before not leftquoatation @ rightquoatation and not a character") (targetValue (name "?{<!>,not(and(<@>,<any>))}")))
(example (utterance "lines with a small letter present that also precedes a number") (targetValue (name "?{<low>,<num>}")))
(example (utterance "lines that have a number that follows leftquoatation ! rightquoatation") (targetValue (name "?{<!>,<num>}")))
(example (utterance "lines with words that have leftquoatation ! rightquoatation , a capital letter and a character") (targetValue (name "?{and(and(<!>,<cap>),<any>)}")))
(example (utterance "lines containing a character before a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<any>,<let>),<!>)}")))
(example (utterance "lines possessing the leftquoatation ! rightquoatation string , plus a vowel and also a number , and then an upper-case letter") (targetValue (name "?{and(and(<!>,<vow>),<num>),<cap>}")))
(example (utterance "lines with a character leftquoatation ! rightquoatation or") (targetValue (name "?{concat(<any>,<let>),or(<!>,<@>)}")))
(example (utterance "lines with vowel before lower-case letter or letter") (targetValue (name "?{<vow>,or(<low>,<let>)}")))
(example (utterance "lines with 5 or more of a character before number") (targetValue (name "?{<any>,repeatatleast(<num>,5)}")))
(example (utterance "lines that doesn't have zero or more of leftquoatation ! rightquoatation and anything not a number") (targetValue (name "?{and(star(<!>),<num>)}")))
(example (utterance "lines without a letter with 5 or more of leftquoatation ! rightquoatation") (targetValue (name "?{not(<let>),repeatatleast(<!>,5)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or vowel and a character") (targetValue (name "?{not(<!>),and(<vow>,<any>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or character followed by number") (targetValue (name "?{startwith(<!>),concat(<any>,<num>)}")))
(example (utterance "lines not containing a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{not(concat(<let>,<!>))}")))
(example (utterance "lines that have a letter prior to a capital letter , vowel , or character .") (targetValue (name "?{<let>,or(or(<cap>,<vow>),<any>)}")))
(example (utterance "lines with 4 ore more characters and a lower-case letter or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<any>,4),or(or(<low>,<!>),<@>)}")))
(example (utterance "items with words beginning with leftquoatation ! rightquoatation at least 4 times .") (targetValue (name "?{startwith(<!>)}")))
(example (utterance "lines that contain words that contain only leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines starting with capital letter not preceding lower-case letter") (targetValue (name "?{startwith(<cap>),not(<low>)}")))
(example (utterance "lines with words but without a number") (targetValue (name "?{not(<num>)}")))
(example (utterance "lines with a lower-case letter or a vowel before a character in them") (targetValue (name "?{or(<low>,<vow>),<any>}")))
(example (utterance "lines that don't only contain a letter , once or more") (targetValue (name "?{repeatatleast(<let>,1)}")))
(example (utterance "lines not ending with string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{endwith(repeatatleast(<!>,6))}")))
(example (utterance "lines with characters and numbers before the word leftquoatation ! rightquoatation .") (targetValue (name "?{and(<any>,<num>),<!>}")))
(example (utterance "lines without a capital letter or number") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or not a capital letter , zero or more times in them") (targetValue (name "?{or(<!>,not(<cap>))}")))
(example (utterance "items with an upper case letter or leftquoatation ! rightquoatation  .") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines with a character or string leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with character or string leftquoatation ! rightquoatation before vowel") (targetValue (name "?{or(<any>,<!>),<vow>}")))
(example (utterance "lines containing letter , string leftquoatation ! rightquoatation , and lower-case letter") (targetValue (name "?{and(and(<let>,<!>),<low>)}")))
(example (utterance "lines which are not having a character or a lower-case letter") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "lines with character followed by vowel and not a number") (targetValue (name "?{concat(<any>,<vow>),not(<num>)}")))
(example (utterance "lines with words with either a letter , a capital  , or lower-case letter") (targetValue (name "?{or(<let>,<cap>),<low>}")))
(example (utterance "lines with a letter before a capital letter") (targetValue (name "?{startwith(repeatatleast(<let>,1)),<cap>}")))
(example (utterance "items with a letter preceding a numeral .") (targetValue (name "?{or(<let>,<low>),<num>}")))
(example (utterance "lines that have 2 or more strings with leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(repeatatleast(<!>,2),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or containing a character after leftquoatation @ rightquoatation") (targetValue (name "?{concat(<@>,<!>),contain(<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a letter , and a vowel before a capital letter") (targetValue (name "?{and(and(<!>,<let>),<vow>),<cap>}")))
(example (utterance "lines ending with words which are containing only a vowel , 3 or more times") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines not containing a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<!>)}")))
(example (utterance "the lines that contain the sting leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , repeat 6 or more times") (targetValue (name "?{<!>,repeatatleast(<@>,6)}")))
(example (utterance "items with an upper case letter preceding a small letter or leftquoatation ! rightquoatation .") (targetValue (name "?{<cap>,or(<low>,<!>)}")))
(example (utterance "lines followed by a vowel words") (targetValue (name "?{<let>,<vow>}")))
(example (utterance "lines with a letter and 0 or more capital letters") (targetValue (name "?{<let>,star(<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceded by a vowel , character , and number") (targetValue (name "?{and(and(<vow>,<any>),<num>),<!>}")))
(example (utterance "lines that have a  number followed by a character and the word leftquoatation ! rightquoatation") (targetValue (name "?{<num>,and(<any>,<!>)}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation and leftquoatation @ rightquoatation , at least 7 times") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "lines with a character starting in a number") (targetValue (name "?{contain(<any>),startwith(<num>)}")))
(example (utterance "lines without a number followed by a vowel") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation or a letter preceding a numeral .") (targetValue (name "?{or(<let>,<!>),<num>}")))
(example (utterance "lines with a vowel or a capital before any character") (targetValue (name "?{or(<vow>,<cap>),<any>}")))
(example (utterance "lines with lower-case letter and string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation , 2 or more times") (targetValue (name "?{or(and(<low>,<!>),<@>)}")))
(example (utterance "ines with words with a capital letter , a letter , and a number in them") (targetValue (name "?{and(<cap>,<let>),<num>}")))
(example (utterance "lines containing a number minimum 1 time and not a capital letter") (targetValue (name "?{repeatatleast(<num>,1),not(<cap>)}")))
(example (utterance "lines with a letter before either leftquoatation ! rightquoatation , a character , or a number") (targetValue (name "?{<let>,or(or(<!>,<any>),<num>)}")))
(example (utterance "lines that contain either leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines ending with only a lower-case letter , 2 or more times") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines with 4 vowels or more followed by a lower-case letter") (targetValue (name "?{repeatatleast(<vow>,4),<low>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation or containing only string leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with vowel and letter before character or lower-case letter") (targetValue (name "?{and(<vow>,<let>),or(<any>,<low>)}")))
(example (utterance "lines with a capital letter , number or 'truck after leftquoatation ! rightquoatation") (targetValue (name "?{<!>,or(or(<cap>,<num>),<@>)}")))
(example (utterance "lines not containing 1 or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(repeatatleast(<!>,1),<@>)}")))
(example (utterance "lines with words with a number or capital letter") (targetValue (name "?{<num>,repeatatleast(<cap>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or leftquoatation # rightquoatation once") (targetValue (name "?{concat(<!>,<@>),repeatatleast(<#>,1)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before a lower-case letter followed by character") (targetValue (name "?{or(<!>,<@>),concat(<low>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least once or either capital letter , a character , or a number") (targetValue (name "?{repeatatleast(<!>,1),or(or(<cap>,<any>),<num>)}")))
(example (utterance "lines ending with a capital letter or by number") (targetValue (name "?{star(or(<cap>,<num>))}")))
(example (utterance "lines containing 1 capital letter prior to another alphanumeric character") (targetValue (name "?{<cap>,or(<any>,<let>)}")))
(example (utterance "lines with 4 or more of a number or lower-case letter") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or number") (targetValue (name "?{<!>,<num>}")))
(example (utterance "lines that do not contain a number or leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{not(and(<num>,<!>)),<vow>}")))
(example (utterance "lines ending in 4 or more of a vowel before number") (targetValue (name "?{repeatatleast(concat(<vow>,<num>),4)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{<!>,contain(<@>)}")))
(example (utterance "lines having words with no less than zero occurrences of a capital letter coming before a character") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines contains words at least once that contain leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with words ending in a vowel") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a number at least once in them") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines containing a lower-case letter preceding a number followed by a letter") (targetValue (name "?{<low>,concat(<num>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a character 6 times") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines which are not ending with a character before a vowel") (targetValue (name "?{concat(endwith(<any>),<vow>)}")))
(example (utterance "lines with words ending with containing a letter") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines that have a number followed by a capital letter , at least 5 times") (targetValue (name "?{<num>,repeatatleast(<cap>,5)}")))
(example (utterance "lines having a character and not leftquoatation ! rightquoatation") (targetValue (name "?{contain(<any>),not(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by capital letter , zero times") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines with 7 or more of leftquoatation ! rightquoatation or characters") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines not being words ending with a character") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and letter before a capital letter") (targetValue (name "?{contain(<!>),concat(<let>,<cap>)}")))
(example (utterance "lines with only leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,1)}")))
(example (utterance "a letter or a character begins the line at least once") (targetValue (name "?{or(<let>,repeatatleast(<any>,1))}")))
(example (utterance "lines cannot include a character or lower-case letter or number") (targetValue (name "?{and(and(<any>,<low>),<num>)}")))
(example (utterance "lines starting in 5 or more of a capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(concat(<cap>,<!>),5)}")))
(example (utterance "lines with a capital letter , 2 or more times before leftquoatation ! rightquoatation in them") (targetValue (name "?{repeatatleast(<cap>,2),<!>}")))
(example (utterance "lines with 7 or more lower-case letters before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,7),<!>}")))
(example (utterance "lines which are not ending with containing only leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines with a letter before vowel or capital letter") (targetValue (name "?{<let>,or(<vow>,<cap>)}")))
(example (utterance "lines with lower case followed by a capital letter after a letter") (targetValue (name "?{<let>,concat(<low>,<cap>)}")))
(example (utterance "items with either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or an upper case letter preceding a character .") (targetValue (name "?{or(or(<@>,<cap>),<!>),<any>}")))
(example (utterance "lines that have 4 or more combinations of the word leftquoatation ! rightquoatation and a character") (targetValue (name "?{repeatatleast(<!>,4),contain(<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter 3 times") (targetValue (name "?{contain(concat(<!>,<let>))}")))
(example (utterance "lines with lower-case letter before either string leftquoatation ! rightquoatation , letter , or string leftquoatation @ rightquoatation") (targetValue (name "?{<low>,or(or(<!>,<let>),<@>)}")))
(example (utterance "lines with a character or letter 6 times") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines with a capital letter , string leftquoatation ! rightquoatation , and number before a lower-case letter") (targetValue (name "?{and(and(<cap>,<!>),<num>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , vowel or character before a letter") (targetValue (name "?{or(or(<!>,<vow>),<any>),<let>}")))
(example (utterance "lines with words with either vowel , lower-case letter , or character") (targetValue (name "?{or(<vow>,<low>),<any>}")))
(example (utterance "lines with a number followed by a character followed by a capital letter") (targetValue (name "?{<num>,concat(<any>,<cap>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a capital letter") (targetValue (name "?{and(and(<!>,<@>),<cap>),<any>}")))
(example (utterance "the lines must have a character before a number at least once .") (targetValue (name "?{star(<any>),<num>}")))
(example (utterance "lines containing 7 or more capital letters") (targetValue (name "?{<cap>}")))
(example (utterance "lines having either a character , letter , or string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{or(or(<any>,<let>),<!>),<vow>}")))
(example (utterance "5 lines not having a dog character") (targetValue (name "?{not(repeatatleast(<any>,5)),<!>}")))
(example (utterance "lines with a vowel and string leftquoatation ! rightquoatation before a character") (targetValue (name "?{and(<vow>,<!>),<any>}")))
(example (utterance "lines ending in words starting with lower-case letter") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "lines with a character , a capital letter , and leftquoatation ! rightquoatation after a number") (targetValue (name "?{<num>,and(and(<any>,<cap>),<!>)}")))
(example (utterance "lines with words that contain only number , 4 or more times") (targetValue (name "?{<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a character , and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<any>),<@>}")))
(example (utterance "lines with words with ending with containing only a capital letter in them") (targetValue (name "?{<cap>}")))
(example (utterance "lines that contain a number or character , at least 7 times") (targetValue (name "?{or(<any>,<num>)}")))
(example (utterance "lines with 4 or more lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{contain(or(<low>,<!>))}")))
(example (utterance "lines starting with words and having leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines containing a vowel before leftquoatation ! rightquoatation , 5 or more times") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines without a vowel or a number") (targetValue (name "?{star(or(<vow>,<num>))}")))
(example (utterance "lines ending with  a lower-case letter at least zero times") (targetValue (name "?{endwith(contain(<low>))}")))
(example (utterance "lines containing a number , leftquoatation ! rightquoatation , and a vowel at least zero times") (targetValue (name "?{and(and(<num>,<!>),<vow>)}")))
(example (utterance "lines that have the leftquoatation ! rightquoatation string as an ending") (targetValue (name "?{<!>}")))
(example (utterance "items with an uppercase letter preceding leftquoatation ! rightquoatation preceding a letter .") (targetValue (name "?{concat(<cap>,<!>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel and a lower-case letter") (targetValue (name "?{<!>,or(<vow>,<low>)}")))
(example (utterance "lines which are having a vowel , a number , and leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<vow>,<num>),<!>),<@>}")))
(example (utterance "lines containing 2 or more lower-case letters") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines with words and a number before vowel") (targetValue (name "?{<num>,<vow>}")))
(example (utterance "lines containing either a character , letter , or capital letter") (targetValue (name "?{or(or(<any>,<let>),<cap>)}")))
(example (utterance "lines with 4 or more lower-case letters or vowels") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines with with either leftquoatation ! rightquoatation leftquoatation @ rightquoatation or a letter preceding a lower-case letter .") (targetValue (name "?{or(or(<!>,<let>),<@>),<low>}")))
(example (utterance "lines that contain a letter and leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation without a letter") (targetValue (name "?{or(<!>,not(<let>))}")))
(example (utterance "lines that have a vowel after a number more than 2 times") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation or without string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<num>,<!>),not(<@>)}")))
(example (utterance "lines which are containing a letter or starting with a capital letter") (targetValue (name "?{contain(<let>),startwith(<cap>)}")))
(example (utterance "lines with letter before number , 3 or more times") (targetValue (name "?{<let>,repeatatleast(<num>,3)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before only a vowel") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines not containing a vowel and not a capital") (targetValue (name "?{not(and(<vow>,<cap>))}")))
(example (utterance "lines with a vowel before a character starting in a number") (targetValue (name "?{concat(<vow>,<any>),startwith(<num>)}")))
(example (utterance "lines with a vowel before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines containing 2 or more lower-case letters and leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,2),<!>}")))
(example (utterance "lines that end only leftquoatation ! rightquoatation once") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation before a lower-case letter or string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<low>),<@>}")))
(example (utterance "lines without a character followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation and number or string leftquoatation @ rightquoatation") (targetValue (name "?{or(and(<!>,<num>),<@>)}")))
(example (utterance "lines containg words that begin with a character or letter") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines with a letter before character and string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<let>,<any>),or(<!>,<@>)}")))
(example (utterance "lines with a vowel then a number") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , number , and capital letter before a lower-case letter") (targetValue (name "?{and(and(<!>,<num>),<cap>),<low>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation before a letter or character") (targetValue (name "?{<!>,or(<let>,<any>)}")))
(example (utterance "lines that feature a small letter no less than 5 times and any number more than 5") (targetValue (name "?{repeatatleast(<low>,5)}")))
(example (utterance "lines with a number or a character after a letter") (targetValue (name "?{<let>,or(<num>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{<!>,endwith(<let>)}")))
(example (utterance "lines with a number or vowel") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "items with a small letter or character at least 1 time .") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,1),endwith(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation following truck , vowel and lower-case letter") (targetValue (name "?{<!>,and(and(<@>,<vow>),<low>)}")))
(example (utterance "lines with letter followed by vowel and containing string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<vow>),contain(<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding vowel , at least 7 times .") (targetValue (name "?{<!>,repeatatleast(<vow>,7)}")))
(example (utterance "lines ending with letter , 5 or more times") (targetValue (name "?{repeatatleast(<let>,5)}")))
(example (utterance "the lines that have a letter coming before either a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{<let>,or(<cap>,<!>)}")))
(example (utterance "lines that have a capital letter preceded by a character , and contain words") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines with 6 or more lower-case letter or numbers") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines not having a capital letter followed by a letter and not a character in them") (targetValue (name "?{and(concat(<cap>,<let>),<any>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation and a letter .") (targetValue (name "?{<!>,and(<@>,<let>)}")))
(example (utterance "lines with a character , a vowel , or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{<@>,or(or(<any>,<!>),<vow>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation 3 times or more before the number") (targetValue (name "?{repeatatleast(<!>,3),<num>}")))
(example (utterance "lines stating with leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines with words having a character") (targetValue (name "?{<any>}")))
(example (utterance "lines with zero or more of a capital letter or character") (targetValue (name "?{contain(or(<cap>,<any>))}")))
(example (utterance "lines having either string leftquoatation ! rightquoatation , number , or lower-case letter before character") (targetValue (name "?{or(or(<!>,<num>),<low>),<any>}")))
(example (utterance "lines without any letters .") (targetValue (name "?{and(and(<cap>,<let>),<vow>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "items with a numeral at least6times preceding a small letter .") (targetValue (name "?{repeatatleast(<num>,6),<low>}")))
(example (utterance "lines starting with noon string leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(not(<!>),<num>)}")))
(example (utterance "lines having either a letter , character , or vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<any>),<vow>),<!>}")))
(example (utterance "lines having either vowel , lower case letter , or a letter before a capital letter") (targetValue (name "?{or(or(<vow>,<low>),<let>),<cap>}")))
(example (utterance "lines which are not having leftquoatation ! rightquoatation at least once or a letter") (targetValue (name "?{or(repeatatleast(<!>,1),<let>)}")))
(example (utterance "lines containing either a vowel , capital or lower-case letter") (targetValue (name "?{or(or(<vow>,<cap>),<low>)}")))
(example (utterance "lines that start with a capital letter , a vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<vow>),<!>)}")))
(example (utterance "lines with a number before lower-case then capital letter") (targetValue (name "?{concat(<num>,<low>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a number occurring 3 or more times") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines with words staring with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),<@>}")))
(example (utterance "lines with a lower-case and capital letters before a vowel") (targetValue (name "?{and(<low>,<cap>),<vow>}")))
(example (utterance "lines with lower-case letter followed by capital letter before character") (targetValue (name "?{concat(<low>,<cap>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{repeatatleast(<!>,1),concat(<@>,<cap>)}")))
(example (utterance "lines beginning with 'leftquoatation dog rightquoatation or leftquoatation ! rightquoatation and a number") (targetValue (name "?{or(<!>,and(<!>,<num>))}")))
(example (utterance "items with a character preceding a letter .") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines with a character followed by leftquoatation ! rightquoatation") (targetValue (name "?{<any>,<!>}")))
(example (utterance "lines with a character before a lower-case letter and letter") (targetValue (name "?{and(concat(<any>,<low>),<let>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "items with a small letter , at least 7 times preceding a vowel .") (targetValue (name "?{repeatatleast(<low>,7),<vow>}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation , then a vowel followed by a capital letter") (targetValue (name "?{concat(<!>,<vow>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation then leftquoatation @ rightquoatation or another character") (targetValue (name "?{or(concat(<!>,<@>),<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation zero or more times") (targetValue (name "?{star(concat(<!>,<num>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a lower-case letter at least 3 times") (targetValue (name "?{repeatatleast(<low>,3),<!>}")))
(example (utterance "lines with words that don't have a capital , 6 or more times") (targetValue (name "?{not(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a vowel at least 7 times") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines with a character or 6 or rmore vowels") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines with a character before a lower case letter") (targetValue (name "?{<any>,concat(<low>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character then string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),concat(<any>,<@>)}")))
(example (utterance "lines containing either a letter , a capital letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<cap>),<!>}")))
(example (utterance "lines with a letter at least once before a small letter") (targetValue (name "?{repeatatleast(<let>,1),not(<cap>)}")))
(example (utterance "lines with words starting with string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{repeatatleast(<!>,6)}")))
(example (utterance "4 or more with words which contain a vowel followed by a number") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines with words containing a lower-case letter , a capital letter , and a number") (targetValue (name "?{and(<low>,<cap>),<num>}")))
(example (utterance "lines ending with 3 or more words with a character .") (targetValue (name "?{repeatatleast(<any>,3)}")))
(example (utterance "lines that have words containing a capital letter as well as a vowel") (targetValue (name "?{<cap>,<vow>}")))
(example (utterance "lines with words without leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "items with a letter or leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{or(<let>,<!>),<any>}")))
(example (utterance "lines that do not contain a lower-case letter before a capital letter , a character , or leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,or(or(<!>,<cap>),<any>))}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation followed by number at least once") (targetValue (name "?{repeatatleast(concat(<!>,<num>),1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before words , a capital letter , 6 or more times") (targetValue (name "?{<!>,repeatatleast(<cap>,6)}")))
(example (utterance "lines with vowel before letter followed by a character") (targetValue (name "?{<vow>,concat(<let>,<any>)}")))
(example (utterance "lines without word dog and no vowel") (targetValue (name "?{and(<!>,repeatatleast(<vow>,1))}")))
(example (utterance "lines with letters") (targetValue (name "?{or(or(<cap>,<vow>),<low>)}")))
(example (utterance "lines where a lower-case letter precedes a character") (targetValue (name "?{<any>,<low>}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{star(<!>),<vow>}")))
(example (utterance "lines with a letter before string leftquoatation ! rightquoatation then number") (targetValue (name "?{concat(<let>,<!>),<num>}")))
(example (utterance "lines with words with a lower-case letter followed by a vowel , 3 or more times in them") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines with a capital letter before non letter then lower-case letter") (targetValue (name "?{<cap>,not(concat(<let>,<low>))}")))
(example (utterance "lines with words starting in lower-case letter or number") (targetValue (name "?{startwith(<low>),<num>}")))
(example (utterance "lines containing a vowel , a letter , and leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<@>,<vow>),<let>)}")))
(example (utterance "lines starting with zero or more characters") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines ending in 3 or more of leftquoatation ! rightquoatation before a characters") (targetValue (name "?{endwith(concat(<!>,<any>))}")))
(example (utterance "lines that don't end with a letter") (targetValue (name "?{endwith(contain(<let>))}")))
(example (utterance "lines ending with letter , 7 or more times") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines having words with minimum 1 character") (targetValue (name "?{repeatatleast(<any>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation and a letter") (targetValue (name "?{contain(<!>),and(<@>,<let>)}")))
(example (utterance "lines with words and a letter or vowel") (targetValue (name "?{or(or(<let>,<low>),<vow>)}")))
(example (utterance "items without a numeral , an upper case letter , or leftquoatation ! rightquoatation .") (targetValue (name "?{or(or(<cap>,<num>),<!>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before character , 6 or more times") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines containing only 4 or more leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with a lower-case letter before a number , occurring 3 or more times") (targetValue (name "?{<low>,repeatatleast(<num>,3)}")))
(example (utterance "lines with words and a number before vowel") (targetValue (name "?{<num>,<vow>}")))
(example (utterance "items with words containing a letter and leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{and(<let>,<!>),<any>}")))
(example (utterance "lines with 6 or more of a letter before a capital letter") (targetValue (name "?{<let>,repeatatleast(<cap>,6)}")))
(example (utterance "lines ending in a zero or more vowels before a lower-case letter") (targetValue (name "?{endwith(star(<vow>)),<low>}")))
(example (utterance "lines without a character or a vowel") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines containing a letter or a character before leftquoatation ! rightquoatation") (targetValue (name "?{<let>,or(<!>,<any>)}")))
(example (utterance "lines not containing lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{not(concat(<low>,<!>))}")))
(example (utterance "items beginning with a character preceded by leftquoatation ! rightquoatation .") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines ending with number before containing a capital letter") (targetValue (name "?{endwith(<num>),contain(<cap>)}")))
(example (utterance "lines containing a vowel before capital and lower case letter") (targetValue (name "?{and(and(<cap>,<low>),<let>),<vow>}")))
(example (utterance "lines without string leftquoatation ! rightquoatation , number , character") (targetValue (name "?{or(or(<!>,<num>),<any>)}")))
(example (utterance "a capital letter followed by a vowel before a letter appears in the lines") (targetValue (name "?{concat(<cap>,<vow>),<let>}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , & a small letter") (targetValue (name "?{and(and(<vow>,<!>),<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation without a number") (targetValue (name "?{<!>,contain(not(<num>))}")))
(example (utterance "lines starting with a vowel and letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(and(<vow>,<let>),<!>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation followed by a capital") (targetValue (name "?{startwith(concat(<!>,<let>)),<cap>}")))
(example (utterance "lines starting a vowel  or 3") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines withleftquoatation ! rightquoatation and a number followed by leftquoatation @ rightquoatation") (targetValue (name "?{<!>,concat(<num>,<@>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation followed by a vowel at least 6 times") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines starting words with a capital letter , 3 or more times") (targetValue (name "?{repeatatleast(<cap>,3)}")))
(example (utterance "lines with capital letter , 6 or more times before vowel") (targetValue (name "?{repeatatleast(<cap>,6),<vow>}")))
(example (utterance "lines with a number , vowel , or character") (targetValue (name "?{or(or(<num>,<vow>),<any>)}")))
(example (utterance "lines not including the word leftquoatation ! rightquoatation before a letter") (targetValue (name "?{not(<!>),<let>}")))
(example (utterance "lines with capital letter have at least zero time .") (targetValue (name "?{<cap>}")))
(example (utterance "lines with a vowel or capital letter before lower-case letter") (targetValue (name "?{or(<vow>,<cap>),<low>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a character and containing leftquoatation @ rightquoatation at least 3 times") (targetValue (name "?{repeatatleast(<@>,3),or(<!>,<any>)}")))
(example (utterance "lines where leftquoatation ! rightquoatation appears 6 or  more times followed by either leftquoatation @ rightquoatation , a vowel , or a letter") (targetValue (name "?{repeatatleast(<!>,6),or(or(<@>,<vow>),<let>)}")))
(example (utterance "lines without a character and a vowel") (targetValue (name "?{and(repeatatleast(<any>,1),<vow>)}")))
(example (utterance "lines where a small-case alphabet character comes after a word string containing a , e , i , o or u") (targetValue (name "?{<vow>,<low>}")))
(example (utterance "lines ending with only a lower-case letter , 7 or more times") (targetValue (name "?{repeatatleast(<low>,7)}")))
(example (utterance "items without a number , not small letters or not a letter .") (targetValue (name "?{and(and(<num>,<low>),<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a vowel , and a small letter .") (targetValue (name "?{and(and(<vow>,<low>),<!>)}")))
(example (utterance "lines starting with lower-case letter or string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{startwith(or(<low>,<!>))}")))
(example (utterance "lines with zero or more numbers before non string leftquoatation ! rightquoatation") (targetValue (name "?{star(<num>),not(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation 6 times") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines not having a lower-case letter or character , zero or more times") (targetValue (name "?{or(not(<low>),<any>)}")))
(example (utterance "lines with a character followed by zero or more of leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before leftquoatation @ rightquoatation not being a vowel") (targetValue (name "?{concat(<!>,and(<@>,<vow>))}")))
(example (utterance "lines with a capital letter before a vowel or character") (targetValue (name "?{repeatatleast(<cap>,1),or(<vow>,<any>)}")))
(example (utterance "lines containing a letter , a character , or a vowel in words") (targetValue (name "?{or(<any>,<let>),<vow>}")))
(example (utterance "lines with words with a capital letter before vowel") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines without 5 or more capital letters or a vowels") (targetValue (name "?{not(<cap>),repeatatleast(<vow>,5)}")))
(example (utterance "lines having any vowel , the word leftquoatation ! rightquoatation , and any number") (targetValue (name "?{and(and(<vow>,<!>),<num>)}")))
(example (utterance "lines with a lower-case letter , 6 or more times or leftquoatation ! rightquoatation and a character in them") (targetValue (name "?{repeatatleast(<low>,6),and(<!>,<any>)}")))
(example (utterance "lines not containing lower-case letter , zero or more times") (targetValue (name "?{not(star(<low>))}")))
(example (utterance "lines ending in lower-case letter or string leftquoatation ! rightquoatation and vowel") (targetValue (name "?{endwith(<low>),and(<!>,<vow>)}")))
(example (utterance "lines with a number , a vowel , or leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{or(or(<num>,<vow>),<!>),<cap>}")))
(example (utterance "lines that contain a letter and do not contain leftquoatation ! rightquoatation") (targetValue (name "?{contain(<let>),not(<!>)}")))
(example (utterance "lines not having lower-case letter or number , string leftquoatation ! rightquoatation , and string leftquoatation @ rightquoatation") (targetValue (name "?{not(<low>),and(and(<num>,<!>),<@>)}")))
(example (utterance "lines with 5 or more of a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,5),<!>}")))
(example (utterance "lines with a character , a string leftquoatation ! rightquoatation , then a vowel") (targetValue (name "?{<any>,concat(<!>,<vow>)}")))
(example (utterance "line starting in a vowel before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<vow>,<!>),<@>)}")))
(example (utterance "lines containing words having leftquoatation ! rightquoatation or a letter followed by a number") (targetValue (name "?{or(<!>,<let>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation following a lower-case letter after leftquoatation @ rightquoatation") (targetValue (name "?{concat(<low>,<!>),<@>}")))
(example (utterance "lines that end with a number followed by a capital letter") (targetValue (name "?{endwith(<num>),<cap>}")))
(example (utterance "lines with a number , a vowel , and leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{<cap>,and(and(<num>,<vow>),<!>)}")))
(example (utterance "lines with a letter or vowel at least 6 times") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a ltter preceding an upper case letter .") (targetValue (name "?{and(and(<!>,<let>),<@>),<cap>}")))
(example (utterance "lines ending with words and a number , lower-case letter , or letter") (targetValue (name "?{or(or(<num>,<low>),<let>)}")))
(example (utterance "lines with lower-case letter at least once and vowel followed by number") (targetValue (name "?{repeatatleast(<low>,1),concat(<vow>,<num>)}")))
(example (utterance "lines which are not having a lower-case letter , not a number , and not a character") (targetValue (name "?{and(and(<low>,<num>),<any>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a character") (targetValue (name "?{or(or(<!>,<@>),<any>)}")))
(example (utterance "lines with words with number before not leftquoatation ! rightquoatation") (targetValue (name "?{<num>,not(<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or  string leftquoatation @ rightquoatation , 2 or more times") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines where 3 or more times an uppercase letter is contained") (targetValue (name "?{<cap>}")))
(example (utterance "lines with a vowel 4 times") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before a capital") (targetValue (name "?{contain(<!>),<cap>}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation") (targetValue (name "?{contain(<any>),<!>}")))
(example (utterance "lines containing a number followed by a letter before leftquoatation ! rightquoatation") (targetValue (name "?{<num>,concat(<let>,<!>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before not a capital") (targetValue (name "?{<!>,not(<cap>)}")))
(example (utterance "lines that have words that are only a capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines that contain a number , letter and a lower case letter") (targetValue (name "?{and(<low>,<num>),<let>}")))
(example (utterance "lines containing a character , a capital letter , and a number") (targetValue (name "?{and(<any>,<cap>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{contain(<!>),or(<@>,<#>)}")))
(example (utterance "items with a vowel preceding a small letter .") (targetValue (name "?{startwith(<vow>),<low>}")))
(example (utterance "lines that have a vowel preceded by leftquoatation ! rightquoatation at least 6 times") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines ending with words , not a letter") (targetValue (name "?{not(<let>)}")))
(example (utterance "lines containing words with starting with leftquoatation ! rightquoatation preceding a character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{<!>,contain(<low>)}")))
(example (utterance "lines with a character before non number") (targetValue (name "?{<any>,not(<num>)}")))
(example (utterance "lines containing words that have a vowel before a character") (targetValue (name "?{<vow>,<any>}")))
(example (utterance "lines contain only leftquoatation ! rightquoatation and a letter") (targetValue (name "?{<!>,<let>}")))
(example (utterance "lines with words starting in 5 or more vowels") (targetValue (name "?{repeatatleast(<vow>,5)}")))
(example (utterance "lines without a character before a number and leftquoatation ! rightquoatation") (targetValue (name "?{not(<any>),and(<num>,<!>)}")))
(example (utterance "lines with a vowel followed by 5 or more capital letters") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a number , a character , and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<num>,<@>),<any>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{not(<!>),concat(<@>,<vow>)}")))
(example (utterance "lines ending with a character followed by no numbers or 1 or more") (targetValue (name "?{star(concat(<any>,<num>))}")))
(example (utterance "items with either a small letter , a letter , or a vowel preceding leftquoatation ! rightquoatation .") (targetValue (name "?{or(or(<vow>,<let>),<low>),<!>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation and vowel") (targetValue (name "?{<!>,repeatatleast(<vow>,1)}")))
(example (utterance "lines having 5 or more times string leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with a letter or with zero or more of leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,contain(<!>))}")))
(example (utterance "lines starting in leftquoatation ! rightquoatation or with string leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),contain(<@>)}")))
(example (utterance "lines starting in words with string leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before only 2 or more letters") (targetValue (name "?{<!>,repeatatleast(<cap>,2)}")))
(example (utterance "lines which have leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a lower-case letter , and a vowel") (targetValue (name "?{<!>,and(and(<@>,<low>),<vow>)}")))
(example (utterance "lines with a letter") (targetValue (name "?{repeatatleast(<cap>,1),contain(<let>)}")))
(example (utterance "items with any letter and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<cap>),<!>)}")))
(example (utterance "lines with words without a character") (targetValue (name "?{not(<any>)}")))
(example (utterance "lines starting with a number , a capital letter or a lower-case letter") (targetValue (name "?{startwith(<cap>),concat(<low>,<num>)}")))
(example (utterance "items with a character , a letter , and leftquoatation ! rightquoatation preceding an upper case letter .") (targetValue (name "?{and(and(<!>,<let>),<any>),<cap>}")))
(example (utterance "lines with a letter before number then character") (targetValue (name "?{concat(<let>,<num>),<any>}")))
(example (utterance "lines wherein 6 or more times there is a letter preceded by a number") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines having no capital letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<vow>),<!>)}")))
(example (utterance "a lower-case letter appears 4 or more times , or line starts with leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,4),startwith(<!>)}")))
(example (utterance "lines with a vowel , a number , a letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<num>),<let>),<!>}")))
(example (utterance "lines with a capital letter and leftquoatation ! rightquoatation followed by a vowel") (targetValue (name "?{concat(and(<cap>,<!>),<vow>)}")))
(example (utterance "lines that have a capital letter before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{<cap>,and(<!>,<@>)}")))
(example (utterance "my sister's username is daphne12 .") (targetValue (name "?{contain(or(<vow>,<any>)),<num>}")))
(example (utterance "lines that contains a string with a vowel and a lower-case between the words leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<vow>),<low>),<@>}")))
(example (utterance "lines containing either a letter , number , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<num>),<!>)}")))
(example (utterance "lines having a capital letter as well as a number followed by word ending with a vowel") (targetValue (name "?{and(<cap>,<num>),endwith(<vow>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a capital letter at least once in them") (targetValue (name "?{and(and(<!>,<@>),<cap>)}")))
(example (utterance "lines with words with zero or more of a vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines with 2 or more lower-case letters before a vowel") (targetValue (name "?{repeatatleast(<low>,2),<vow>}")))
(example (utterance "lines with a vowel after a capital letter , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(or(<cap>,<!>),<any>),<vow>}")))
(example (utterance "lines not words with a vowel , not a lower-case letter , and not leftquoatation ! rightquoatation in it") (targetValue (name "?{and(and(<vow>,<low>),<!>)}")))
(example (utterance "lines with a capital letter occuring before a number followed by a lower case letter") (targetValue (name "?{<cap>,concat(<num>,<low>)}")))
(example (utterance "lines without vowels or starting with leftquoatation ! rightquoatation") (targetValue (name "?{not(<vow>),startwith(<!>)}")))
(example (utterance "lines followed by a vowel before a capital letter") (targetValue (name "?{concat(<low>,<vow>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and lower-case letter before a capital letter") (targetValue (name "?{and(and(<!>,<@>),<low>),<cap>}")))
(example (utterance "items with a numeral preceding leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{concat(<num>,<!>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before words  with string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,contain(<@>)}")))
(example (utterance "the string 'dog , a letter and a vowel appears before a character in the lines") (targetValue (name "?{and(and(<!>,<let>),<vow>),<any>}")))
(example (utterance "lines having a capital letter , number , and string leftquoatation ! rightquoatation before letter") (targetValue (name "?{and(and(<cap>,<num>),<!>),<let>}")))
(example (utterance "lines where words include a digit , upper-case letter , plus any letter") (targetValue (name "?{and(<num>,<cap>),<let>}")))
(example (utterance "lines with a letter before a lower-case letter or string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,1),or(<low>,<!>)}")))
(example (utterance "lines with a number before a character then string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<any>),<!>}")))
(example (utterance "lines with 4 or more lower-case letters or characters") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "items with a numeral or a character .") (targetValue (name "?{or(<any>,<num>)}")))
(example (utterance "lines ending with at least 1 character or leftquoatation ! rightquoatation") (targetValue (name "?{or(repeatatleast(<any>,1),<!>)}")))
(example (utterance "lines with vowel before either string leftquoatation ! rightquoatation , character , or a capital") (targetValue (name "?{<vow>,or(or(<!>,<any>),<cap>)}")))
(example (utterance "items without leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a small letter .") (targetValue (name "?{or(or(<!>,<@>),<low>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a vowel preceding an upper case letter .") (targetValue (name "?{and(and(<@>,<!>),<vow>),<cap>}")))
(example (utterance "lines containing a capital letter and a letter in them") (targetValue (name "?{and(<cap>,<let>)}")))
(example (utterance "lines with at least 6 numbers before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,6),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a number , as well as a letter followed by a character") (targetValue (name "?{and(and(<!>,<num>),<let>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character and letter") (targetValue (name "?{<!>,and(<any>,<let>)}")))
(example (utterance "lines with lower-case letter at least once and not a letter") (targetValue (name "?{repeatatleast(<low>,1),not(<let>)}")))
(example (utterance "lines not having letter , a character , or a number") (targetValue (name "?{or(or(<let>,<any>),<num>)}")))
(example (utterance "lines with vowel at least once before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<vow>,1),<!>}")))
(example (utterance "lines which are  not containing a capital letter , 6 or more times") (targetValue (name "?{contain(repeatatleast(<cap>,6))}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation then a capital letter") (targetValue (name "?{concat(<low>,<!>),<cap>}")))
(example (utterance "lines that contain a letter preceding leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<let>,<!>),<vow>)}")))
(example (utterance "lines not ending with string leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{or(endwith(<!>),<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before lower case , leftquoatation @ rightquoatation , or number") (targetValue (name "?{<!>,or(or(<low>,<@>),<num>)}")))
(example (utterance "lines containing 4 or more leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,4)}")))
(example (utterance "lines with words with a capital letter and a character in it") (targetValue (name "?{<cap>,<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceded by a lowercase letter or a number") (targetValue (name "?{or(<low>,<num>),<!>}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation or character") (targetValue (name "?{<vow>,or(<!>,<any>)}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(endwith(<!>),<@>)}")))
(example (utterance "lines with 6 or more letters and a capital letter") (targetValue (name "?{repeatatleast(<let>,6),contain(<cap>)}")))
(example (utterance "lines starting in a letter or capital letter and string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<let>),and(<cap>,<!>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation or a letter which is followed by leftquoatation @ rightquoatation") (targetValue (name "?{concat(<any>,<!>),concat(<let>,<@>)}")))
(example (utterance "lines with a number , character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<any>),<!>)}")))
(example (utterance "lines with a number before any character") (targetValue (name "?{<num>,and(and(<let>,<vow>),<any>)}")))
(example (utterance "lines with vowel before lower-case letter , zero times or more") (targetValue (name "?{<vow>,star(<low>)}")))
(example (utterance "lines concluding with the leftquoatation ! rightquoatation string or concluding with a letter") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a vowel at least 4 times .") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines with a number before vowel then capital letter") (targetValue (name "?{<num>,concat(<vow>,<cap>)}")))
(example (utterance "lines with a vowel , number , and character before a lower-case letter") (targetValue (name "?{and(and(<vow>,<num>),<any>),<low>}")))
(example (utterance "lines with words with ending with lower-case letter or letter") (targetValue (name "?{endwith(<low>),<let>}")))
(example (utterance "lines containing letter or string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{star(or(<let>,<!>))}")))
(example (utterance "no vowel , the string 'dog , or a number in the lines") (targetValue (name "?{and(and(<vow>,<!>),<num>)}")))
(example (utterance "twice or more , lines with a lower case letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines including a character , a capital letter , and a number") (targetValue (name "?{and(and(<any>,<cap>),<num>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation following a vowel") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines without starting in a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(concat(<let>,<!>))}")))
(example (utterance "items with a small letter , a character , and a numeral preceding leftquoatation ! rightquoatation .") (targetValue (name "?{and(and(<low>,<num>),<any>),<!>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation and a capital") (targetValue (name "?{<!>,<cap>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a letter before a character") (targetValue (name "?{concat(or(<!>,<let>),<any>)}")))
(example (utterance "lines containing words with a capital letter") (targetValue (name "?{repeatatleast(<cap>,1)}")))
(example (utterance "items beginning with a character at least 4 times .") (targetValue (name "?{repeatatleast(<any>,4)}")))
(example (utterance "lines that contain a capital letter followed by leftquoatation ! rightquoatation and a letter") (targetValue (name "?{<cap>,and(<!>,<let>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation before leftquoatation @ rightquoatation then leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),star(<#>)}")))
(example (utterance "lines containing a character followed by a number , leftquoatation ! rightquoatation , as well as a lower-case letter") (targetValue (name "?{<any>,and(and(<num>,<!>),<low>)}")))
(example (utterance "any minimum of times  , lines should have a character then a lower case letter") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines that have a character , a vowel , and a number") (targetValue (name "?{and(<any>,<vow>),<num>}")))
(example (utterance "lines ending with a vowel and a non-letter") (targetValue (name "?{endwith(<vow>),not(<let>)}")))
(example (utterance "lines with a letter at least once and which are starting with leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,1),startwith(<!>)}")))
(example (utterance "lines containing words without only a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines with a number followed by a vowel , at least 6 times") (targetValue (name "?{<num>,repeatatleast(<vow>,6)}")))
(example (utterance "lines with a lower-case letter , a character , or leftquoatation ! rightquoatation after leftquoatation @ rightquoatation") (targetValue (name "?{<@>,or(or(<low>,<any>),<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and starting with leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),startwith(<@>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation , a letter , and lower-case letter") (targetValue (name "?{and(<!>,<let>),<low>}")))
(example (utterance "lines with a character at least once or a small letter") (targetValue (name "?{repeatatleast(<any>,1),not(<cap>)}")))
(example (utterance "lines with the word leftquoatation ! rightquoatation listed at least 7 times followed by a lower-case letter") (targetValue (name "?{repeatatleast(<!>,7),<low>}")))
(example (utterance "lines with a character before a number and vowel") (targetValue (name "?{<any>,and(<num>,<vow>)}")))
(example (utterance "lines that , 7 or more times , end with leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines with a number preceded by the string 'dogleftquoatation  , the string  rightquoatation!leftquoatation  or a capital letter") (targetValue (name "?{or(or(<!>,<cap>),<!>),<num>}")))
(example (utterance "lines ending with a letter before a vowel") (targetValue (name "?{repeatatleast(concat(<let>,<vow>),1)}")))
(example (utterance "leftquoatation ! rightquoatation followed by lines with a letter then a number") (targetValue (name "?{concat(<let>,<num>),<!>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation , 7 or more times") (targetValue (name "?{contain(concat(<!>,<@>))}")))
(example (utterance "lines with words with only a capital letter or character") (targetValue (name "?{<cap>,<any>}")))
(example (utterance "lines not having vowel , letter , or a character") (targetValue (name "?{or(or(<vow>,<let>),<any>)}")))
(example (utterance "lines ending with zero or more of a capital letter or vowel") (targetValue (name "?{endwith(or(<cap>,<vow>))}")))
(example (utterance "lines with a vowel or a number , leftquoatation ! rightquoatation , or character") (targetValue (name "?{repeatatleast(<vow>,1),or(or(<num>,<!>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by 0 or more numbers") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines that begin with a capital letter and are followed by a lower-case letter") (targetValue (name "?{startwith(<cap>),<low>}")))
(example (utterance "lines containing a number or a capital letter at least 6 times") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines in which a character is preceded by either a letter or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<let>),<@>),<any>}")))
(example (utterance "lines ending in words with string leftquoatation ! rightquoatation , vowel , or lower-case letter") (targetValue (name "?{and(and(<!>,<vow>),<low>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation 1 or more times") (targetValue (name "?{repeatatleast(<!>,1),<@>}")))
(example (utterance "lines ending in lower-case letter before character or number") (targetValue (name "?{or(concat(<low>,<any>),<num>)}")))
(example (utterance "lines with a letter , a vowel , and a number") (targetValue (name "?{and(and(<let>,<vow>),<num>)}")))
(example (utterance "lines with an uppercase letter preceded by a vowel zero or more times") (targetValue (name "?{star(<vow>),<cap>}")))
(example (utterance "lines with lower-case letters before a number or character") (targetValue (name "?{repeatatleast(<low>,1),or(<num>,<any>)}")))
(example (utterance "lines that have a lower-case letter and a capital letter followed by a vowel") (targetValue (name "?{and(and(<low>,<cap>),<let>),<vow>}")))
(example (utterance "lines with words which contain a letter or a vowel") (targetValue (name "?{<let>,<vow>}")))
(example (utterance "lines containing words with a capital letter or ending with a vowel") (targetValue (name "?{<cap>,endwith(<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 3 or more times or a vowel followed by leftquoatation @ rightquoatation in them") (targetValue (name "?{repeatatleast(<!>,3),concat(<vow>,<@>)}")))
(example (utterance "line that have leftquoatation ! rightquoatation , at least 4 times before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,4),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or zero or more of leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,contain(<@>))}")))
(example (utterance "lines with a character and lower-case letter") (targetValue (name "?{contain(and(<any>,<low>))}")))
(example (utterance "lines containing 5 or more numbers or characters") (targetValue (name "?{contain(<num>),repeatatleast(<any>,5)}")))
(example (utterance "lines with 7 or more of leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,7),<@>}")))
(example (utterance "lines with words ending with a letter at least 6 times") (targetValue (name "?{repeatatleast(<let>,6)}")))
(example (utterance "lines with a number before a letter then a capital letter") (targetValue (name "?{<num>,concat(<let>,<cap>)}")))
(example (utterance "lines with a vowel starting with leftquoatation ! rightquoatation") (targetValue (name "?{star(<vow>),startwith(<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 5 or more times before character") (targetValue (name "?{repeatatleast(<!>,5),<any>}")))
(example (utterance "lines with words with zero or more capital letters before leftquoatation ! rightquoatation") (targetValue (name "?{star(<cap>),<!>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a character followed by a letter") (targetValue (name "?{or(<!>,<any>),<let>}")))
(example (utterance "lines which are having a capital letter , a vowel , and a character before a lower-case letter") (targetValue (name "?{and(and(<cap>,<vow>),<any>),<low>}")))
(example (utterance "lines which are containing words that contain only a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number and leftquoatation @ rightquoatation , character , or capital letter") (targetValue (name "?{concat(<!>,<num>),or(or(<@>,<any>),<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , leftquoatation # rightquoatation , or a vowel") (targetValue (name "?{<!>,or(or(<@>,<#>),<vow>)}")))
(example (utterance "lines with capital letter and character before letter") (targetValue (name "?{and(<cap>,<any>),<let>}")))
(example (utterance "lines with 5 or more of a number or vowel") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation or end with a vowel , at least 1 time") (targetValue (name "?{or(<!>,endwith(<vow>))}")))
(example (utterance "lines containing a letter , leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a character") (targetValue (name "?{and(and(<let>,<!>),<@>),<any>}")))
(example (utterance "lines with words starting with a capital , 4 or more times") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines without either a character or the leftquoatation ! rightquoatation string") (targetValue (name "?{contain(or(<any>,<!>))}")))
(example (utterance "words thst end with 7 or more lower-case letters") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<let>,<!>),<@>}")))
(example (utterance "lines with words that don't have a capital") (targetValue (name "?{<cap>}")))
(example (utterance "lines that contain the leftquoatation ! rightquoatation string and then a letter afterwards") (targetValue (name "?{<!>,<let>}")))
(example (utterance "lines without words and only a vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines that do not include capital letters or leftquoatation ! rightquoatation and that are not letters") (targetValue (name "?{and(and(<cap>,<!>),<let>)}")))
(example (utterance "lines having either lower-case letter , character , or string leftquoatation ! rightquoatation before a letter") (targetValue (name "?{or(or(<low>,<any>),<!>),<let>}")))
(example (utterance "lines with a vowel before a character ending in number") (targetValue (name "?{concat(<vow>,<any>),endwith(<num>)}")))
(example (utterance "lines which are ending with a capital letter or leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{endwith(or(<cap>,<!>))}")))
(example (utterance "lines that end with only leftquoatation ! rightquoatation 7 or more times") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines that have a vowel before a capital or lower-case letter") (targetValue (name "?{<vow>,or(<cap>,<low>)}")))
(example (utterance "lines with either a character , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation followed by a lower-case letter") (targetValue (name "?{or(or(<any>,<!>),<@>),<low>}")))
(example (utterance "lines with character followed by string leftquoatation ! rightquoatation before vowel") (targetValue (name "?{concat(<any>,<!>),<vow>}")))
(example (utterance "lines with a letter or starting in character") (targetValue (name "?{contain(<let>),startwith(<any>)}")))
(example (utterance "lines starting with 1 letter") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "items with an upper case letter , leftquoatation ! rightquoatation , or a small letter preceding a numeral .") (targetValue (name "?{or(or(<!>,<cap>),<low>),<num>}")))
(example (utterance "lines with words ending with a lower-case letter and character") (targetValue (name "?{endwith(<low>),<any>}")))
(example (utterance "lines having a lower-case letter , string leftquoatation ! rightquoatation , and string leftquoatation @ rightquoatation before string leftquoatation # rightquoatation") (targetValue (name "?{and(and(<low>,<!>),<@>),<#>}")))
(example (utterance "items with a numeral or an upper case letter at least5times .") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , or a lower-case letter") (targetValue (name "?{or(or(<vow>,<!>),<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital and lower-case letter") (targetValue (name "?{<!>,and(<cap>,<low>)}")))
(example (utterance "items with a character and a letter preceding a numeral .") (targetValue (name "?{and(<let>,<any>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a letter , or a number before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<let>),<num>),<@>}")))
(example (utterance "lines with 7 or more characters or leftquoatation ! rightquoatation followed by a lower-case letter") (targetValue (name "?{repeatatleast(<any>,7),concat(<!>,<low>)}")))
(example (utterance "items with a numeral preceding a letter at least5times .") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines containing a numeral succeeded with a type of character") (targetValue (name "?{<num>,<any>}")))
(example (utterance "lines without a capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{not(<cap>),<!>}")))
(example (utterance "lines with words having a number , capital letter , or lower case letter") (targetValue (name "?{or(<num>,<cap>),<low>}")))
(example (utterance "lines with a character before vowel and number") (targetValue (name "?{<any>,and(<vow>,<num>)}")))
(example (utterance "items with a letter preceding a small letter at least twice .") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines with capital letter followed by character before vowel") (targetValue (name "?{concat(<cap>,<any>),<vow>}")))
(example (utterance "lines ending in 3 or more characters") (targetValue (name "?{repeatatleast(<any>,3)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , a vowel , and a character in them") (targetValue (name "?{and(<!>,<vow>),<any>}")))
(example (utterance "lines with a capital letter followed by a lower-case letter , 3 or more times in them") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines with 2 ore more of a character before a letter") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines that do not have a character , a letter , or a lower-case letter") (targetValue (name "?{and(and(<any>,<let>),<low>)}")))
(example (utterance "lines with words , a character , number , and a vowel") (targetValue (name "?{and(<any>,<num>),<vow>}")))
(example (utterance "lines with words a character followed by a capital letter , 6 or more times") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines with words with a letter and capital letter") (targetValue (name "?{<let>,repeatatleast(<cap>,1)}")))
(example (utterance "lines with 6 or more capital letters before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<cap>,6),<!>}")))
(example (utterance "lines that have a character 6 or more times") (targetValue (name "?{repeatatleast(<any>,6)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation , capital letter") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines not starting with lower-case letter , 6 or more times") (targetValue (name "?{repeatatleast(startwith(<low>),6)}")))
(example (utterance "lines with a lower-case letter") (targetValue (name "?{<low>,<let>}")))
(example (utterance "no lower-case letters in the lines before vowels .") (targetValue (name "?{not(<low>),<vow>}")))
(example (utterance "lines with5or more upper-case letters") (targetValue (name "?{repeatatleast(<low>,5)}")))
(example (utterance "lines not having a vowel or lower-case letter") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines with a letter , or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<vow>),<!>),<@>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a letter , or a vowel .") (targetValue (name "?{or(or(<vow>,<let>),<!>)}")))
(example (utterance "lines with dog before a number , the string truck , and the string ring .") (targetValue (name "?{<!>,and(and(<num>,<@>),<#>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , character , or lower-case letter") (targetValue (name "?{or(or(<!>,<any>),<low>)}")))
(example (utterance "lines a vowel before a letter then capital letter") (targetValue (name "?{<vow>,concat(<let>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number and ending in string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<num>),endwith(<@>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation and a character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines containing either a vowel , leftquoatation ! rightquoatation , or a capital letter") (targetValue (name "?{or(<vow>,<!>),<cap>}")))
(example (utterance "lines that start with a word that has a character preceding a letter") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines without a lower-case letter just before a character") (targetValue (name "?{not(concat(<low>,<any>))}")))
(example (utterance "lines with words and a vowel or letter") (targetValue (name "?{or(<vow>,<low>),<let>}")))
(example (utterance "lines with a number preceding a lower-case letter at least 3 times") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines with a lower-case letter and number") (targetValue (name "?{<num>,repeatatleast(<low>,3)}")))
(example (utterance "lines with a vowel ending with a capital letter") (targetValue (name "?{<vow>,endwith(<cap>)}")))
(example (utterance "lines containing a letter after a lower-case letter , a vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<vow>),<!>),<let>}")))
(example (utterance "lines that have a vowel , a character , and a lower-case letter") (targetValue (name "?{and(and(<vow>,<any>),<low>)}")))
(example (utterance "lines containing either a letter , a number , or a character before a vowel") (targetValue (name "?{or(or(<let>,<num>),<any>),<vow>}")))
(example (utterance "lines with words that contain a number or leftquoatation ! rightquoatation in them") (targetValue (name "?{contain(<num>),<!>}")))
(example (utterance "lines ending with a letter , zero or more times") (targetValue (name "?{star(contain(<let>))}")))
(example (utterance "lines with words with a letter before leftquoatation ! rightquoatation , 5 or more times in them") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines containing a letter followed by a vowel as well as leftquoatation ! rightquoatation") (targetValue (name "?{<let>,and(<vow>,<!>)}")))
(example (utterance "items with a vowel , leftquoatation ! rightquoatation , and a character precedeing leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<!>,<any>),<vow>),<@>}")))
(example (utterance "lines with lower-case letters or leftquoatation ! rightquoatation") (targetValue (name "?{contain(<low>),<!>}")))
(example (utterance "lines with the string dog after a capital letter at 6 times") (targetValue (name "?{repeatatleast(<cap>,6),<!>}")))
(example (utterance "lines ending with 6 or more leftquoatation ! rightquoatation or vowels") (targetValue (name "?{or(<!>,repeatatleast(<vow>,6))}")))
(example (utterance "lines that begin with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation followed by a capital letter") (targetValue (name "?{startwith(<!>),concat(<@>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before lower-case letters") (targetValue (name "?{concat(<let>,<!>),<low>}")))
(example (utterance "lines containing a number and not with a vowel .") (targetValue (name "?{and(<num>,not(<vow>))}")))
(example (utterance "lines with words and a lower-case letter with a number") (targetValue (name "?{<low>,<num>}")))
(example (utterance "lines not a letter or leftquoatation ! rightquoatation at least twice") (targetValue (name "?{and(repeatatleast(<!>,2),<let>)}")))
(example (utterance "lines that either do not or contain at least 1 ending with words with leftquoatation ! rightquoatation") (targetValue (name "?{star(<!>)}")))
(example (utterance "lines with 7 or more letters before a vowel") (targetValue (name "?{repeatatleast(<let>,7),<vow>}")))
(example (utterance "lines that do not have a character after a letter at least 3 times") (targetValue (name "?{not(<let>),repeatatleast(<any>,3)}")))
(example (utterance "lines ending with a vowel before leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<vow>,<!>),<@>)}")))
(example (utterance "the lines that have words containing a lower-case letter and then a number") (targetValue (name "?{<low>,<num>}")))
(example (utterance "lines that contain either a character , a vowel , or a lower-case letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<vow>),<low>),<!>}")))
(example (utterance "lines with a character or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a letter") (targetValue (name "?{or(or(<any>,<!>),<@>),<let>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or ending with a letter") (targetValue (name "?{or(<!>,endwith(<let>))}")))
(example (utterance "lines with 7 or more capital letters or a vowels") (targetValue (name "?{or(<cap>,<vow>)}")))
(example (utterance "lines having the leftquoatation ! rightquoatation string and then the leftquoatation @ rightquoatation string appearing at least 7 times or greater") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines which are having leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a vowel before leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<vow>),<#>}")))
(example (utterance "lines with a letter , string leftquoatation ! rightquoatation , or number") (targetValue (name "?{or(or(<let>,<!>),<num>)}")))
(example (utterance "lines with a character following a letter at least 5 tines") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with a capital letter preceding a vowel") (targetValue (name "?{<cap>,<vow>}")))
(example (utterance "lines with words and a vowel , number , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<num>),<!>}")))
(example (utterance "items with a letter preceding leftquoatation ! rightquoatation at least thrice .") (targetValue (name "?{<let>,repeatatleast(<!>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and ending with leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<@>),contain(<!>)}")))
(example (utterance "lines with both a letter and a lower-case letter") (targetValue (name "?{and(<let>,<low>)}")))
(example (utterance "lines that start with a letter and character") (targetValue (name "?{startwith(<let>),repeatatleast(<any>,1)}")))
(example (utterance "leftquoatation ! rightquoatation at least once with leftquoatation @ rightquoatation before a number") (targetValue (name "?{repeatatleast(<!>,1),concat(<@>,<num>)}")))
(example (utterance "lines without a letter , a lower-case letter , or a number") (targetValue (name "?{or(or(<let>,<low>),<num>)}")))
(example (utterance "lines with a lower-case letter in them followed by a letter before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<let>),<!>}")))
(example (utterance "lines that have 4 or more numbers") (targetValue (name "?{repeatatleast(<num>,4)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation , a lower-case letter , or a capital letter") (targetValue (name "?{or(or(<!>,<low>),<cap>)}")))
(example (utterance "lines containing a vowel , a capital letter , or a letter") (targetValue (name "?{or(<cap>,<vow>),<let>}")))
(example (utterance "lines ending with words with either leftquoatation ! rightquoatation , a character , or leftquoatation @ rightquoatation in them") (targetValue (name "?{or(or(<!>,<any>),<@>)}")))
(example (utterance "lines that may or may not contain a vowel or a number") (targetValue (name "?{or(contain(<vow>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , letter , or number before a letter") (targetValue (name "?{or(or(<!>,<cap>),<num>),<let>}")))
(example (utterance "lines that end with a letter followed by a vowel , zero or more times") (targetValue (name "?{or(star(<let>),<vow>)}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation and not followed by a capital letter") (targetValue (name "?{contain(not(<!>)),<cap>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation or contain a vowel , zero times or more") (targetValue (name "?{star(<vow>),concat(<!>,<@>)}")))
(example (utterance "lines with a vowel before a number") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines with words with number or vowel") (targetValue (name "?{<num>,<vow>}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation at least 4 times or with a capital letter") (targetValue (name "?{or(repeatatleast(<!>,4),<cap>)}")))
(example (utterance "lines with words and a letter and vowel") (targetValue (name "?{<let>,<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceded by leftquoatation @ rightquoatation") (targetValue (name "?{<@>,<!>}")))
(example (utterance "lines that start with a capital letter and contain a vowel") (targetValue (name "?{contain(<vow>),startwith(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation at least 2 times") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with words with either character , vowel , or letter") (targetValue (name "?{or(<any>,<vow>),<let>}")))
(example (utterance "lines with a vowel before number") (targetValue (name "?{<vow>,repeatatleast(<num>,1)}")))
(example (utterance "lines containing a letter before a number") (targetValue (name "?{contain(<let>),<num>}")))
(example (utterance "lines which are ending with a letter before a vowel") (targetValue (name "?{endwith(<let>),<vow>}")))
(example (utterance "lines without a lower-case letter , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<low>,<!>),<@>)}")))
(example (utterance "lines which are not containing only leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation or letter") (targetValue (name "?{<any>,or(<!>,<let>)}")))
(example (utterance "lines which have a number and a vowel in them .") (targetValue (name "?{and(<num>,<vow>)}")))
(example (utterance "lines stating in words with string leftquoatation ! rightquoatation , a character , or lower-case letter") (targetValue (name "?{and(and(<!>,<any>),<low>)}")))
(example (utterance "items with a numeral or a vowel at least 1 time .") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with 4 or more instances of a number followed by a lower-case letter") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and at least 1 small letter") (targetValue (name "?{contain(<!>),and(and(<@>,<low>),<let>)}")))
(example (utterance "lines having words ending with a capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines containing a letter and number followed by a lower-case letter") (targetValue (name "?{and(<let>,<num>),<low>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , zero or more times or character") (targetValue (name "?{or(star(<!>),<any>)}")))
(example (utterance "lines containing a number , leftquoatation ! rightquoatation or a lower-case letter in front of a capital letter") (targetValue (name "?{<any>,and(and(<vow>,<!>),<low>)}")))
(example (utterance "lines with words with a number followed by a character at least once in them") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines with a number , zero or more times before leftquoatation ! rightquoatation and a lower-case letter in it") (targetValue (name "?{star(<num>),and(<!>,<low>)}")))
(example (utterance "lines ending with a number before a lower-case letter") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines without a letter after a character") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines which begin with a number that comes before the leftquoatation ! rightquoatation string") (targetValue (name "?{startwith(<num>),<!>}")))
(example (utterance "lines not starting with character followed by number") (targetValue (name "?{startwith(concat(<any>,<num>))}")))
(example (utterance "lines with words and a character or leftquoatation ! rightquoatation at least 6 times") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines ending in vowel or capital letter") (targetValue (name "?{or(repeatatleast(<vow>,1),<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a vowel , 2 times or more") (targetValue (name "?{<!>,repeatatleast(<vow>,2)}")))
(example (utterance "items beginning with a vowel .") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines with words and ending in a number or lower-case letter") (targetValue (name "?{endwith(<num>),<low>}")))
(example (utterance "lines with a vowel , capital letter , or number") (targetValue (name "?{or(or(<vow>,<cap>),<num>)}")))
(example (utterance "lines that begin with a character preceded by a vowel") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines with words with ending with character , 7 or more times") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines starting with capital letter before character") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines with less than 3 of strings leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(startwith(<!>),3)}")))
(example (utterance "lines that have a capital letter or a letter zero or more times") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines with a letter before a lower-case letter then capital letter") (targetValue (name "?{contain(<let>),concat(<low>,<cap>)}")))
(example (utterance "lines containing a character preceding leftquoatation ! rightquoatation or preceding a vowel") (targetValue (name "?{<any>,or(<!>,<vow>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation or a number") (targetValue (name "?{and(<!>,<@>),<num>}")))
(example (utterance "lines without a letter , string leftquoatation ! rightquoatation , or vowel") (targetValue (name "?{and(and(<let>,<!>),<vow>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation with a number before capital letter") (targetValue (name "?{star(<!>),concat(<num>,<cap>)}")))
(example (utterance "7 or more lines with a vowel followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation before containing a capital letter") (targetValue (name "?{endwith(<!>),contain(<cap>)}")))
(example (utterance "lines with a letter , character , and string leftquoatation ! rightquoatation before a number") (targetValue (name "?{and(and(<let>,<any>),<!>),<num>}")))
(example (utterance "at least once a capitol letter before leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,repeatatleast(<!>,1)}")))
(example (utterance "lines with a lower-case letter 3 times") (targetValue (name "?{repeatatleast(<low>,3),<let>}")))
(example (utterance "lines with 4 lower-case letters before a number") (targetValue (name "?{repeatatleast(<low>,4),<num>}")))
(example (utterance "lines with words and ending with leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before a character") (targetValue (name "?{endwith(<!>),<any>}")))
(example (utterance "lines having leftquoatation ! rightquoatation , a character , and number before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<any>),<num>),<@>}")))
(example (utterance "items with a numeral preceding a vowel at least once .") (targetValue (name "?{contain(concat(<num>,<vow>))}")))
(example (utterance "items with words that don't have leftquoatation ! rightquoatation .") (targetValue (name "?{repeatatleast(<!>,1)}")))
(example (utterance "lines ending with a character before leftquoatation ! rightquoatation") (targetValue (name "?{endwith(concat(<any>,<!>))}")))
(example (utterance "lines that have at least 1 word that start with a capital letter") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines with a character before a vowel then capital letter") (targetValue (name "?{concat(<any>,<vow>),<cap>}")))
(example (utterance "lines with a capital letter , string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{and(and(<cap>,<!>),<@>),<vow>}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation at least 7 times or leftquoatation @ rightquoatation") (targetValue (name "?{or(repeatatleast(<!>,7),<@>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation or a letter followed by a capital letter") (targetValue (name "?{or(<!>,<let>),<cap>}")))
(example (utterance "lines ending with a vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<vow>),<!>}")))
(example (utterance "lines with that do not have a letter before leftquoatation ! rightquoatation") (targetValue (name "?{<let>,not(<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation preceding a vowel .") (targetValue (name "?{concat(<!>,<@>),<vow>}")))
(example (utterance "lines with 2 or more of a vowel before a capital letter") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation then a letter") (targetValue (name "?{concat(<any>,<!>),<let>}")))
(example (utterance "lines containing a vowel , a number , and leftquoatation ! rightquoatation followed by a character") (targetValue (name "?{and(and(<vow>,<num>),<!>),<any>}")))
(example (utterance "lines with a number before lower-case letter and a capital letter") (targetValue (name "?{<num>,and(<low>,<cap>)}")))
(example (utterance "lines having a character following a number and leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<!>),<any>}")))
(example (utterance "lines with a small letter before a number , leftquoatation ! rightquoatation or character") (targetValue (name "?{<low>,or(or(<num>,<!>),<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before leftquoatation @ rightquoatation .") (targetValue (name "?{contain(concat(<!>,<@>))}")))
(example (utterance "lines without leftquoatation ! rightquoatation , vowel , or lower-case letter") (targetValue (name "?{and(and(<!>,<vow>),<low>)}")))
(example (utterance "items with  leftquoatation ! rightquoatation preceding a small letter .") (targetValue (name "?{<!>,endwith(<low>)}")))
(example (utterance "lines containing a letter before 4 or more lower-case letters") (targetValue (name "?{<let>,repeatatleast(<low>,4)}")))
(example (utterance "the lines that have a letter coming before either leftquoatation ! rightquoatation or a lower-case and a capital letter") (targetValue (name "?{concat(<let>,<!>),and(<low>,<cap>)}")))
(example (utterance "lines beginning with vowels 4 or more times") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or 5 or more of leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,startwith(<@>))}")))
(example (utterance "items with a numeral preceding a vowel at least thrice .") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines with words with a character , 4 or more times and a number in them") (targetValue (name "?{repeatatleast(<any>,4),<num>}")))
(example (utterance "lines with a vowel , lower-case letter , and string leftquoatation ! rightquoatation before a character") (targetValue (name "?{and(and(<vow>,<low>),<!>),<any>}")))
(example (utterance "a line follows a capital letter .") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after leftquoatation @ rightquoatation at least 3 times") (targetValue (name "?{<@>,repeatatleast(<!>,3)}")))
(example (utterance "lines with words and zero or more of string leftquoatation ! rightquoatation") (targetValue (name "?{star(<!>)}")))
(example (utterance "lines containing a lower-case at least once") (targetValue (name "?{or(contain(<low>),<cap>)}")))
(example (utterance "items with a vowel preceding a character preceding an upper case letter .") (targetValue (name "?{concat(<vow>,<any>),<cap>}")))
(example (utterance "lines without a number , or a character and not being a vowel") (targetValue (name "?{and(and(<num>,<any>),<vow>)}")))
(example (utterance "lines end with a letter followed by 0 or more of leftquoatation ! rightquoatation") (targetValue (name "?{star(concat(<let>,<!>))}")))
(example (utterance "lines with words , a character , a number 7 times") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines without a vowel , character , or capital letter") (targetValue (name "?{or(or(<vow>,<any>),<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a number , and a capital letter before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<num>),<cap>),<@>}")))
(example (utterance "lines with words with or without capital letters") (targetValue (name "?{<cap>,<let>}")))
(example (utterance "lines with a capital letter , vowel , or string leftquoatation ! rightquoatation before a letter") (targetValue (name "?{or(or(<cap>,<vow>),<!>),<let>}")))
(example (utterance "lines that have a letter followed by a capital letter") (targetValue (name "?{<let>,<cap>}")))
(example (utterance "lines that end with a lower-case letter followed by a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<low>,<!>),<vow>)}")))
(example (utterance "lines with words and starting in 7 or more of a lower-case letter") (targetValue (name "?{repeatatleast(<low>,7)}")))
(example (utterance "lines that contain a number followed by a vowel , at least 7 times") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines that contain a vowel preceding a number and a lower-case letter or the word leftquoatation ! rightquoatation") (targetValue (name "?{concat(<vow>,<num>),or(<low>,<!>)}")))
(example (utterance "lines that does not end with a vowel or a number") (targetValue (name "?{or(not(<vow>),<num>)}")))
(example (utterance "lines with only zero or more lower-case letters") (targetValue (name "?{star(<low>),<cap>}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation , a character , or a letter") (targetValue (name "?{or(or(<!>,<any>),<let>)}")))
(example (utterance "lines which are not starting with a character , zero or more times") (targetValue (name "?{startwith(star(<any>))}")))
(example (utterance "lines without leftquoatation ! rightquoatation or with string leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),contain(<@>)}")))
(example (utterance "lines ending with a lower-case letter and stating with letter") (targetValue (name "?{endwith(<low>),startwith(<let>)}")))
(example (utterance "lines that  consist of a number before a 1 character word") (targetValue (name "?{<num>,<any>}")))
(example (utterance "lines which contain words having 1 vowel accompanied by the string named leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,<!>}")))
(example (utterance "the lines containing a capital letter , leftquoatation ! rightquoatation , and a lower-case letter") (targetValue (name "?{and(and(<!>,<cap>),<low>)}")))
(example (utterance "lines having character , string leftquoatation ! rightquoatation , and letter before number") (targetValue (name "?{and(and(<any>,<!>),<let>),<num>}")))
(example (utterance "lines with 3 or more of a capital letter before number") (targetValue (name "?{<cap>,repeatatleast(<num>,3)}")))
(example (utterance "lines with 7 or more of a capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{contain(concat(<cap>,<!>))}")))
(example (utterance "lines which have at least 1 occurrence of a number followed by a letter") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines with a capital letter at least 5 times with a character or string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<cap>,5),or(<any>,<!>)}")))
(example (utterance "lines with a capital letter , character , and string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<cap>,<any>),<!>),<@>}")))
(example (utterance "lines with a number , letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<let>),<!>)}")))
(example (utterance "lines that do not contain a character or a capital letter") (targetValue (name "?{and(<any>,<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation  , zero or more times with a character .") (targetValue (name "?{star(<!>),contain(<any>)}")))
(example (utterance "lines with strings containing a number before a capital letter , before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<cap>),<!>}")))
(example (utterance "lines with words with either strings : dog or truck , or a letter") (targetValue (name "?{or(<!>,<@>),<let>}")))
(example (utterance "lines that have a 4 or more combinations of a vowel followed by a lower-case letter") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines with a vowel or number before string leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<num>),<!>}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),contain(<@>)}")))
(example (utterance "lines with a character , a capital letter , leftquoatation ! rightquoatation and a letter , in order") (targetValue (name "?{concat(<any>,<cap>),concat(<!>,<let>)}")))
(example (utterance "lines beginning with a lower-case letter or a vowel") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines ending in zero or more of a number before capital letter") (targetValue (name "?{endwith(concat(<num>,<cap>))}")))
(example (utterance "lines with a lower-case letter , a character , leftquoatation ! rightquoatation , and a vowel") (targetValue (name "?{and(and(<low>,<any>),<!>),<vow>}")))
(example (utterance "lines without leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation before leftquoatation $ rightquoatation") (targetValue (name "?{concat(or(or(<!>,<@>),<#>),<$>)}")))
(example (utterance "lines containing words with a letter , number , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<num>),<!>}")))
(example (utterance "lines with a number , string leftquoatation ! rightquoatation , or lower-case letter before a capital letter") (targetValue (name "?{or(or(<num>,<!>),<low>),<cap>}")))
(example (utterance "items with a upper case letter and leftquoatation ! rightquoatation preceding a vowel .") (targetValue (name "?{and(<cap>,<!>),<vow>}")))
(example (utterance "lines without a letter , leftquoatation ! rightquoatation , or a capital letter") (targetValue (name "?{or(or(<let>,<!>),<cap>)}")))
(example (utterance "lines with words with either character , letter , or a capital") (targetValue (name "?{or(<any>,<let>),<cap>}")))
(example (utterance "lines containing a vowel , followed by a capital letter and a number") (targetValue (name "?{<vow>,and(<cap>,<num>)}")))
(example (utterance "lines which are having a lower-case letter , leftquoatation ! rightquoatation , and a number before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<low>,<!>),<num>),<@>}")))
(example (utterance "lines with a capital letter before a letter then lower-case letter") (targetValue (name "?{concat(<cap>,<let>),<low>}")))
(example (utterance "lines with 4 or more of a word and number with a character") (targetValue (name "?{repeatatleast(<num>,4),<any>}")))
(example (utterance "items with words containing any letter and numeral .") (targetValue (name "?{and(<cap>,<low>),<num>}")))
(example (utterance "lines which contain a lower case letter occring 2 or more times before a number") (targetValue (name "?{repeatatleast(<low>,2),<num>}")))
(example (utterance "lines which end with a vowel following a number") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines containing words having leftquoatation ! rightquoatation followed by a number 1 or more times") (targetValue (name "?{<!>,repeatatleast(<num>,1)}")))
(example (utterance "lines containing a capital letter followed by a vowel , at least 7 times") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines that have in them a number and also the leftquoatation ! rightquoatation string") (targetValue (name "?{and(<!>,<num>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation followed by leftquoatation # rightquoatation .") (targetValue (name "?{startwith(<!>),concat(<@>,<#>)}")))
(example (utterance "lines with lower-case letter before vowel or capital letter") (targetValue (name "?{<low>,or(<vow>,<cap>)}")))
(example (utterance "lines with number or character , 6 or more times") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "lines that end with character before leftquoatation ! rightquoatation , 6 times or more .") (targetValue (name "?{repeatatleast(concat(<any>,<!>),6)}")))
(example (utterance "items with a small letter and vowel preceding a character .") (targetValue (name "?{and(<vow>,<low>),<any>}")))
(example (utterance "lines with a number , and letter before a character") (targetValue (name "?{<let>,and(<any>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least once") (targetValue (name "?{repeatatleast(<!>,1),not(<any>)}")))
(example (utterance "lines with 5 or more capital letters before a letter") (targetValue (name "?{repeatatleast(<cap>,5),<let>}")))
(example (utterance "lines without a vowel , character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<any>),<!>)}")))
(example (utterance "lines with 2 or more letters before a capital letter") (targetValue (name "?{repeatatleast(<let>,2),<cap>}")))
(example (utterance "items with any letter .") (targetValue (name "?{and(and(<low>,<let>),<vow>)}")))
(example (utterance "lines with a capital before string leftquoatation ! rightquoatation and lower-case letter") (targetValue (name "?{<cap>,and(<!>,<low>)}")))
(example (utterance "lines that have 3 or more numbers") (targetValue (name "?{<num>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or only containing a number at least 7 times") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines which are ending with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines containing words with letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number") (targetValue (name "?{repeatatleast(<!>,1),contain(<num>)}")))
(example (utterance "lines with a vowel before a number at least once") (targetValue (name "?{repeatatleast(<vow>,1),<num>}")))
(example (utterance "lines beginning with a capital & leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<cap>),or(<!>,<@>)}")))
(example (utterance "lines which have leftquoatation ! rightquoatation followed by a number before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<num>),<@>}")))
(example (utterance "lines not ending in zero or more of leftquoatation ! rightquoatation") (targetValue (name "?{endwith(star(<!>))}")))
(example (utterance "lines with 6 or more of a  lower-case letter before number") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "the lines that contain the sting leftquoatation ! rightquoatation and also a character") (targetValue (name "?{and(<!>,<any>)}")))
(example (utterance "lines containing a letter after a vowel at least twice") (targetValue (name "?{<vow>,repeatatleast(<let>,2)}")))
(example (utterance "lines with 7 or more of leftquoatation ! rightquoatation before a lower-case and capital letter") (targetValue (name "?{concat(<!>,<low>),repeatatleast(<cap>,7)}")))
(example (utterance "lines with capital letter or number before lower-case letter at least once") (targetValue (name "?{or(<cap>,concat(<num>,<low>))}")))
(example (utterance "lines without a vowel") (targetValue (name "?{repeatatleast(contain(<vow>),1)}")))
(example (utterance "lines containing a character or a capital letter at least 5 times") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "lines with a character before capital letter and string leftquoatation ! rightquoatation") (targetValue (name "?{<any>,and(and(<cap>,<let>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation   and a letter followed by a lower-case letter") (targetValue (name "?{and(<!>,<let>),concat(<@>,<low>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a number") (targetValue (name "?{not(<!>),concat(<@>,<num>)}")))
(example (utterance "lines containing character before capital letter") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines which are starting with a letter and not a character") (targetValue (name "?{startwith(<let>),not(<any>)}")))
(example (utterance "lines starting with a character or number , string leftquoatation ! rightquoatation , and a letter") (targetValue (name "?{startwith(<any>),and(and(<num>,<!>),<let>)}")))
(example (utterance "lines with a letter and a vowel , capital letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,1),and(and(<vow>,<cap>),<!>)}")))
(example (utterance "lines that must have the words leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a capital letter followed by a lower-case letter") (targetValue (name "?{and(and(<!>,<@>),<cap>),<low>}")))
(example (utterance "lines with a zero or more vowels or a lower-case letter") (targetValue (name "?{or(star(<vow>),<low>)}")))
(example (utterance "lines that does not have a vowel , a character , or a letter") (targetValue (name "?{and(and(<vow>,<any>),<let>)}")))
(example (utterance "lines with 3 or more of a vowel before capital letter") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines containing  only a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines that have a number , a character and then a letter") (targetValue (name "?{<num>,concat(<any>,<let>)}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , and a character") (targetValue (name "?{or(or(<!>,<any>),<vow>)}")))
(example (utterance "lines with words ending in a vowel or letter") (targetValue (name "?{endwith(<vow>),<let>}")))
(example (utterance "lines ending in and with only a vowel or number") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with no numbers , capital letters , or vowels") (targetValue (name "?{and(and(<num>,<cap>),<vow>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation with a number") (targetValue (name "?{concat(<low>,<!>),endwith(<num>)}")))
(example (utterance "lines having words with  only a vowel followed by a character") (targetValue (name "?{<vow>,<any>}")))
(example (utterance "items with an upper case letter , a vowel , or leftquoatation ! rightquoatation in front of a character .") (targetValue (name "?{and(and(<!>,<vow>),<cap>),<any>}")))
(example (utterance "ends with a letter or number") (targetValue (name "?{repeatatleast(or(<let>,<num>),1)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation , a letter , and vowel") (targetValue (name "?{and(<!>,<let>),<vow>}")))
(example (utterance "lines with less than 3 characters") (targetValue (name "?{repeatatleast(<any>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{<!>,star(<@>)}")))
(example (utterance "lines without a character before a letter") (targetValue (name "?{not(<any>),<let>}")))
(example (utterance "lines starting in a lower-case letter before a capital letter then string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<low>),concat(<cap>,<!>)}")))
(example (utterance "lines that end with a number , zero times or more") (targetValue (name "?{endwith(star(<num>))}")))
(example (utterance "lines with a number , then a character , then leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,concat(<any>,<!>))}")))
(example (utterance "lines with a lower-case letter starting in string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<low>),startwith(<!>)}")))
(example (utterance "lines ending with the words and a letter , 2 or more times") (targetValue (name "?{repeatatleast(<let>,2)}")))
(example (utterance "lines that have a vowel that is followed by the string 'dog 5 or more times") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or lower-case letter") (targetValue (name "?{<!>,or(<@>,<low>)}")))
(example (utterance "lines starting with 4 ore more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(repeatatleast(<!>,4),<@>)}")))
(example (utterance "lines with a vowel and letter before string leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<let>),not(<!>)}")))
(example (utterance "lines not having leftquoatation ! rightquoatation and not a capital letter , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation in them") (targetValue (name "?{and(<!>,or(or(<cap>,<@>),<#>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a letter , and a capital letter followed by a character") (targetValue (name "?{and(and(<!>,<let>),<cap>),<any>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before containing a capital") (targetValue (name "?{or(<!>,<@>),contain(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number then capital letter") (targetValue (name "?{concat(<!>,<num>),<cap>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , and string leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<#>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a numeral , or a letter preceding a character .") (targetValue (name "?{or(or(<!>,<let>),<num>),<any>}")))
(example (utterance "items with a vowel preceding a numeral or leftquoatation ! rightquoatation .") (targetValue (name "?{<vow>,or(<!>,<num>)}")))
(example (utterance "items with words a vowel preceding a small letter .") (targetValue (name "?{<vow>,<low>}")))
(example (utterance "lines that have at least once the word leftquoatation ! rightquoatation preceding a character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines with the string 'dog , a number , and vowel preceding leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<!>,<num>),<vow>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a vowel , or leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<vow>),<@>),<#>}")))
(example (utterance "lines stating with words and a vowel before a letter") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines ending with a character after the string dog 4 or more times") (targetValue (name "?{repeatatleast(concat(<!>,<any>),4)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a lower-case letter , a vowel or leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<low>,<vow>),<@>),<!>}")))
(example (utterance "the lines that end with leftquoatation ! rightquoatation occurring at least once") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines that have a vowel or at least 1 leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines with 2 or more letters or numbers") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines that have a number , then words") (targetValue (name "?{<num>}")))
(example (utterance "lines with a character after a letter at least 7 times") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines not containing string leftquoatation ! rightquoatation , 4 or more times") (targetValue (name "?{not(repeatatleast(<!>,4))}")))
(example (utterance "items with leftquoatation ! rightquoatation or a character at least 4 times .") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines not containing a number , vowel or character") (targetValue (name "?{or(or(<num>,<vow>),<any>)}")))
(example (utterance "lines with 6 or more numbers or leftquoatation ! rightquoatation and a letter") (targetValue (name "?{repeatatleast(<num>,6),and(<!>,<let>)}")))
(example (utterance "lines that finish with the leftquoatation ! rightquoatation string 4 times or more") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding an upper case letter and numeral .") (targetValue (name "?{<!>,and(<num>,<cap>)}")))
(example (utterance "lines that have a lower-case letter at the beginning 2 or more times") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "lines without zero or more of a lower-case letter or with string leftquoatation ! rightquoatation") (targetValue (name "?{or(star(<low>),<!>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a vowel and a number") (targetValue (name "?{<!>,and(<vow>,<num>)}")))
(example (utterance "lines with a character , 7 or more times before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,7),<!>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , lower-case letter , and character") (targetValue (name "?{and(and(<!>,<low>),<any>)}")))
(example (utterance "items with a numeral , a vowel , and an upper case letter preceding a letter .") (targetValue (name "?{and(and(<low>,<vow>),<num>),<let>}")))
(example (utterance "lines with a vowel , a character , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<any>),<!>}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation , a vowel , and a capital letter in them") (targetValue (name "?{<let>,and(and(<!>,<vow>),<cap>)}")))
(example (utterance "lines without string leftquoatation ! rightquoatation and that starts with string leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),startwith(<@>)}")))
(example (utterance "items with a letter , a numeral , or leftquoatation ! rightquoatation .") (targetValue (name "?{and(and(<!>,<num>),<let>)}")))
(example (utterance "lines starting with a character and a letter , zero or more times in them") (targetValue (name "?{startwith(<any>),star(<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a character , or a vowel .") (targetValue (name "?{or(or(<vow>,<any>),<!>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{<!>,<low>}")))
(example (utterance "lines without a letter or vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{not(<let>),concat(<vow>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation without number") (targetValue (name "?{repeatatleast(<!>,1),not(<num>)}")))
(example (utterance "lines that end with a character and start with leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<any>),startwith(<!>)}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation before number") (targetValue (name "?{endwith(contain(<!>)),<num>}")))
(example (utterance "lines with words and a letter or capital letter") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines which are containing a character or leftquoatation ! rightquoatation before a number") (targetValue (name "?{contain(<any>),concat(<!>,<num>)}")))
(example (utterance "lines with lower case letter or letter before capital letter") (targetValue (name "?{or(<low>,<let>),<cap>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation , a lower-case letter , or string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<low>),<@>)}")))
(example (utterance "items without leftquoatation ! rightquoatation , a character , or a letter .") (targetValue (name "?{or(or(<let>,<any>),<!>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , a lower-case letter , or a  letter") (targetValue (name "?{or(or(<!>,<let>),<low>)}")))
(example (utterance "lines with words that leftquoatation ! rightquoatation , not a character , and not leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<any>),<@>)}")))
(example (utterance "lines without a number , string leftquoatation ! rightquoatation , or character") (targetValue (name "?{and(and(<num>,<!>),<any>)}")))
(example (utterance "lines with  string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation before a number") (targetValue (name "?{and(<!>,<@>),<num>}")))
(example (utterance "lines which are containing a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines with a letter , a lower-case letter , and a vowel") (targetValue (name "?{and(and(<vow>,<let>),<low>)}")))
(example (utterance "lines with 2 or more of leftquoatation ! rightquoatation or with lower-case letter") (targetValue (name "?{repeatatleast(<!>,2),contain(<low>)}")))
(example (utterance "lines containing at least 1 instance fo leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation preceding leftquoatation # rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),concat(<@>,<#>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a letter , and leftquoatation @ rightquoatation followed by a lower-case letter") (targetValue (name "?{and(and(<!>,<let>),<@>),<low>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before words with a capital and a letter") (targetValue (name "?{<!>,and(<cap>,<let>)}")))
(example (utterance "lines with a vowel 5 times before a number") (targetValue (name "?{repeatatleast(<vow>,5),contain(<num>)}")))
(example (utterance "lines which are starting with not a capital letter or a lower-case letter") (targetValue (name "?{or(not(<cap>),<low>)}")))
(example (utterance "lines with 7 or mroe of a number before string leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{<num>,repeatatleast(or(<!>,<low>),7)}")))
(example (utterance "lines having a letter , character , and string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<let>,<any>),<!>),<@>}")))
(example (utterance "lines with lower case letter and number before leftquoatation ! rightquoatation .") (targetValue (name "?{and(<low>,<num>),<!>}")))
(example (utterance "lines containing a character followed by string leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{contain(concat(<any>,<!>))}")))
(example (utterance "lines ending with vowel or string leftquoatation ! rightquoatation at least once") (targetValue (name "?{or(endwith(<vow>),<!>)}")))
(example (utterance "lines with a lower case letter after leftquoatation ! rightquoatation , a number , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<num>),<@>),<low>}")))
(example (utterance "lines without a letter or leftquoatation ! rightquoatation but with leftquoatation @ rightquoatation") (targetValue (name "?{and(<let>,concat(<!>,<@>))}")))
(example (utterance "lines with 7 or more of a string leftquoatation ! rightquoatation ending in vowel") (targetValue (name "?{repeatatleast(<!>,7),endwith(<vow>)}")))
(example (utterance "lines containing only a letter") (targetValue (name "?{star(<let>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and 'truck 4 or more times") (targetValue (name "?{contain(and(<!>,<@>))}")))
(example (utterance "lines with words and a capital letter") (targetValue (name "?{<let>,<cap>}")))
(example (utterance "lines containing words that have capital letter , character or number") (targetValue (name "?{or(<any>,<cap>),<num>}")))
(example (utterance "lines with a letter before a vowel") (targetValue (name "?{or(<cap>,<let>),<vow>}")))
(example (utterance "lines not having lower-case letter , zero or more times and not a capital") (targetValue (name "?{and(star(<low>),<cap>)}")))
(example (utterance "lines having a character and a letter") (targetValue (name "?{contain(<any>),repeatatleast(<let>,1)}")))
(example (utterance "lines with a capital letter , vowel , and leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<cap>,<vow>),<!>),<@>}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation or a character , 6 or more times in them") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "zero or more occurrences of a character coming before a capital letter in a line") (targetValue (name "?{<any>,star(<cap>)}")))
(example (utterance "lines containing ending with string leftquoatation ! rightquoatation and lower-case letter") (targetValue (name "?{and(endwith(<!>),<low>)}")))
(example (utterance "lines with a character , number , a vowel") (targetValue (name "?{and(<any>,<num>),<vow>}")))
(example (utterance "lines with capitalized words , characters and vowels") (targetValue (name "?{and(<cap>,<any>),<vow>}")))
(example (utterance "items with a small letter , leftquoatation ! rightquoatation , and a numeral preceding an upper case letter .") (targetValue (name "?{and(and(<low>,<!>),<num>),<cap>}")))
(example (utterance "lines ending wih leftquoatation ! rightquoatation before a lower-case") (targetValue (name "?{repeatatleast(endwith(<!>),1),<low>}")))
(example (utterance "the lines that contain words words that also have a vowel at least 1 time") (targetValue (name "?{<vow>}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation at least 4 times") (targetValue (name "?{repeatatleast(<!>,4)}")))
(example (utterance "items with a letter , at least twice , preceding a small letter .") (targetValue (name "?{repeatatleast(<let>,2),<low>}")))
(example (utterance "lines without a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<low>),<!>)}")))
(example (utterance "lines that don't end with a number before a lower case letter") (targetValue (name "?{not(endwith(<num>)),<low>}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation nor leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{not(concat(<!>,<low>))}")))
(example (utterance "lines containing a character , a lower-case letter , and a capital letter") (targetValue (name "?{and(<cap>,<low>),<any>}")))
(example (utterance "lines with words with capital letter , 4 or more times") (targetValue (name "?{<cap>}")))
(example (utterance "lines ending with vowel") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines containing either a capital , vowel , or letter") (targetValue (name "?{or(or(<cap>,<vow>),<let>)}")))
(example (utterance "lines without a number and a character") (targetValue (name "?{or(<num>,contain(<any>))}")))
(example (utterance "lines without a number or vowel") (targetValue (name "?{or(<num>,repeatatleast(<vow>,1))}")))
(example (utterance "items terminating withe a vowel at least twice .") (targetValue (name "?{repeatatleast(<vow>,2)}")))
(example (utterance "lines without a lower-case letter , character , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<any>),<!>)}")))
(example (utterance "lines which are starting with a lower-case letter or containing a number") (targetValue (name "?{startwith(<low>),contain(<num>)}")))
(example (utterance "lines with words that contain a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{contain(<let>),<!>}")))
(example (utterance "lines with vowel , 5 or more times or ending with number") (targetValue (name "?{repeatatleast(<vow>,5),endwith(<num>)}")))
(example (utterance "lines that have a number or lower-case letter that occurs zero or more times") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "lines without 'dog or a letter") (targetValue (name "?{and(<!>,<let>)}")))
(example (utterance "ling with leftquoatation ! rightquoatation or letter") (targetValue (name "?{or(or(<!>,<cap>),<let>)}")))
(example (utterance "lines ending with not a vowel and lower-case letter") (targetValue (name "?{or(not(<vow>),<low>)}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation , 'truck or any number") (targetValue (name "?{or(and(<!>,<@>),<num>)}")))
(example (utterance "lines containing words with leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{<!>}")))
(example (utterance "lines with a vowel before a number") (targetValue (name "?{<vow>,and(<any>,<num>)}")))
(example (utterance "lines that must have the word leftquoatation ! rightquoatation followed by the word leftquoatation @ rightquoatation or a character") (targetValue (name "?{<!>,or(<@>,<any>)}")))
(example (utterance "lines with number or letter , 2 or more times") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines not having a number or string leftquoatation ! rightquoatation followed by letter") (targetValue (name "?{not(<num>),concat(<!>,<let>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before a capital , zero or more times") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines with only leftquoatation ! rightquoatation or leftquoatation @ rightquoatation at least once") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines starting with 5 or more characters") (targetValue (name "?{repeatatleast(<any>,5)}")))
(example (utterance "items with  words containing a vowel , at least thrice .") (targetValue (name "?{<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a number , or a capital letter .") (targetValue (name "?{or(or(<!>,<num>),<cap>)}")))
(example (utterance "lines not having a vowel , lower-case letter , or letter") (targetValue (name "?{or(or(<vow>,<low>),<let>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or 3 or more of leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with words with a capital before vowel , 3 or more times") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines with words that have leftquoatation ! rightquoatation at least 2 times") (targetValue (name "?{repeatatleast(<!>,2)}")))
(example (utterance "the lines that have a lower-case letter coming before a number that is followed by a character") (targetValue (name "?{<low>,concat(<num>,<any>)}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation and leftquoatation # rightquoatation and a vowel") (targetValue (name "?{endwith(<!>),and(and(<@>,<vow>),<#>)}")))
(example (utterance "lines that have a lower-case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation following a character") (targetValue (name "?{or(or(<low>,<!>),<any>),<@>}")))
(example (utterance "lines that end in a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines containing a capital letter or the word leftquoatation ! rightquoatation or leftquoatation @ rightquoatation , followed by a character") (targetValue (name "?{<any>,or(or(<cap>,<!>),<@>)}")))
(example (utterance "leftquoatation ! rightquoatation before either a lower case letter or leftquoatation @ rightquoatation") (targetValue (name "?{or(<low>,<@>),<!>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a capital") (targetValue (name "?{startwith(<!>),concat(<@>,<cap>)}")))
(example (utterance "lines with words starting in zero or more capital letters") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation at least 4 times preceding a small letter .") (targetValue (name "?{repeatatleast(<!>,4),<low>}")))
(example (utterance "lines with words that contain a vowel followed by a lower-case letter , at least 5 times") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "items with an upper case letter or character at least6times .") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines with words alongwith a letter , a vowel , and a number") (targetValue (name "?{and(<let>,<vow>),<num>}")))
(example (utterance "lines starting in 6 or more characters or a vowel") (targetValue (name "?{or(repeatatleast(<any>,6),<vow>)}")))
(example (utterance "lines ending with lower-case letter or number at least once") (targetValue (name "?{repeatatleast(or(<low>,<num>),1)}")))
(example (utterance "lines with a number , a lower-case letter , or a vowel after leftquoatation ! rightquoatation") (targetValue (name "?{<!>,or(or(<num>,<low>),<vow>)}")))
(example (utterance "lines having a character , string leftquoatation ! rightquoatation , and a capital before a number") (targetValue (name "?{and(and(<any>,<!>),<cap>),<num>}")))
(example (utterance "items with a numeral preceding leftquoatation ! rightquoatation and a letter .") (targetValue (name "?{<num>,and(<let>,<!>)}")))
(example (utterance "lines with number zero times") (targetValue (name "?{<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a capital letter at least 3 times") (targetValue (name "?{repeatatleast(<cap>,3),<!>}")))
(example (utterance "lines starting with letter followed by string leftquoatation ! rightquoatation , 5 or more times") (targetValue (name "?{startwith(concat(<let>,<!>))}")))
(example (utterance "lines with 6 or more of a lower-case letter before vowel") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines without a lower-case letter following a vowel") (targetValue (name "?{not(<vow>),<low>}")))
(example (utterance "lines with words and a letter 5 times") (targetValue (name "?{<let>}")))
(example (utterance "lines with words without a number") (targetValue (name "?{repeatatleast(<num>,1)}")))
(example (utterance "items terminating with a numeral or upper case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation .") (targetValue (name "?{endwith(<num>),or(or(<cap>,<!>),<@>)}")))
(example (utterance "lines ending with a lower-case letter") (targetValue (name "?{contain(and(<let>,<low>))}")))
(example (utterance "lines with a capital , lower-case , letter") (targetValue (name "?{<cap>,or(<low>,<let>)}")))
(example (utterance "lines with at least 1 letter or a character") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with words with a number , string leftquoatation ! rightquoatation , and letter before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<num>,<!>),<let>),<@>}")))
(example (utterance "lines that begin with a number followed by a character or leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<num>),or(<any>,<!>)}")))
(example (utterance "lines with zero or more capital letters or ending with a lower-case letter") (targetValue (name "?{star(<cap>),endwith(<low>)}")))
(example (utterance "lines starting with capital before string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<cap>),<!>}")))
(example (utterance "lines not containing number and not leftquoatation ! rightquoatation") (targetValue (name "?{not(and(<num>,<!>))}")))
(example (utterance "lines with a letter before character then non string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<any>),not(<!>)}")))
(example (utterance "lines with a vowel before a character then") (targetValue (name "?{concat(<vow>,<any>),<!>}")))
(example (utterance "zero or more lines with a letter") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation or a capital letter before a character") (targetValue (name "?{star(<!>),concat(<cap>,<any>)}")))
(example (utterance "lines with words with either string leftquoatation ! rightquoatation , a letter , or lower-case letter , zero or more times") (targetValue (name "?{or(or(<!>,<let>),<low>)}")))
(example (utterance "lines not being words starting with any character") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines with a capital letter before ending with leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,endwith(contain(<!>))}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation or a capital letter followed by a number , zero times or more") (targetValue (name "?{star(<!>),concat(<cap>,<num>)}")))
(example (utterance "lines with a character , a letter , and a number") (targetValue (name "?{and(and(<any>,<let>),<num>)}")))
(example (utterance "nes with a capital letter before leftquoatation ! rightquoatation or a letter") (targetValue (name "?{concat(<cap>,<!>),repeatatleast(<let>,1)}")))
(example (utterance "lines without a capital letter before leftquoatation ! rightquoatation or another letter") (targetValue (name "?{not(<cap>),or(<!>,<let>)}")))
(example (utterance "lines with a character , number , and letter before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<num>),<let>),<!>}")))
(example (utterance "lines starting with a capital letter or leftquoatation ! rightquoatation , 2 or more times in them") (targetValue (name "?{or(<cap>,repeatatleast(<!>,2))}")))
(example (utterance "lines with a lower-case letter starting with leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,1),startwith(<!>)}")))
(example (utterance "items with a vowel at least 3 times preceding a numeral .") (targetValue (name "?{repeatatleast(<vow>,3),<num>}")))
(example (utterance "lines that do not have leftquoatation ! rightquoatation nor leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "lines that start with a letter and have leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<let>),contain(<!>)}")))
(example (utterance "lines that either end in a capital letter or end in a vowel .") (targetValue (name "?{endwith(<cap>),contain(<vow>)}")))
(example (utterance "lines not having letter and ending with lower-case letter") (targetValue (name "?{not(<let>),endwith(<low>)}")))
(example (utterance "lines containing a capital letter followed by leftquoatation ! rightquoatation , a vowel , and leftquoatation @ rightquoatation") (targetValue (name "?{<cap>,and(and(<!>,<vow>),<@>)}")))
(example (utterance "lines with a character , a number , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<!>),<any>)}")))
(example (utterance "lines which start with leftquoatation ! rightquoatation followed by a lower-case letter") (targetValue (name "?{startwith(<!>),<low>}")))
(example (utterance "lines with 6 or more of a vowel or string leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{or(<vow>,concat(<!>,<cap>))}")))
(example (utterance "lines containing letter once") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation before a letter") (targetValue (name "?{startwith(<let>),<!>}")))
(example (utterance "lines containing 1 lower-case letter before a vowel") (targetValue (name "?{contain(concat(<low>,<vow>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a vowel before a lower-case letter") (targetValue (name "?{and(and(<!>,<@>),<vow>),<low>}")))
(example (utterance "lines which are containing a letter or either leftquoatation ! rightquoatation , a vowel , or leftquoatation @ rightquoatation") (targetValue (name "?{contain(<let>),or(or(<!>,<vow>),<@>)}")))
(example (utterance "lines with a lower-case letter before a character which is followed by a capital letter") (targetValue (name "?{<low>,concat(<any>,<cap>)}")))
(example (utterance "lines with less than 4  lower-case letters before a character") (targetValue (name "?{concat(repeatatleast(<low>,4),<any>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation 7 times") (targetValue (name "?{repeatatleast(or(<!>,<@>),7)}")))
(example (utterance "lines having a number , a lower-case letter and a letter") (targetValue (name "?{and(and(<num>,<low>),<let>)}")))
(example (utterance "lines with words with only a capital letter before a number") (targetValue (name "?{<cap>,<num>}")))
(example (utterance "items with a vowel at least 7 times before leftquoatation ! rightquoatation .") (targetValue (name "?{repeatatleast(<vow>,7),<!>}")))
(example (utterance "lines with a number or 1 or more characters") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "lines with a character before a number and capital letter") (targetValue (name "?{<any>,or(<num>,<cap>)}")))
(example (utterance "lines with words and a character , lower-case , or capital letter") (targetValue (name "?{or(<any>,<low>),<cap>}")))
(example (utterance "lines which do not have words with leftquoatation ! rightquoatation , 5 or more times") (targetValue (name "?{repeatatleast(<!>,5)}")))
(example (utterance "lines which don't start with a number but do begin with an upper-case letter") (targetValue (name "?{startwith(<cap>),not(<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before number , a letter , and string leftquoatation # rightquoatation") (targetValue (name "?{or(<!>,<@>),and(and(<num>,<let>),<#>)}")))
(example (utterance "lines with zero or more capital letters or vowels") (targetValue (name "?{or(<cap>,<vow>)}")))
(example (utterance "lends that end with a character and a string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<any>),<!>}")))
(example (utterance "lines with a capital letter or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a lower-case letter") (targetValue (name "?{or(or(<cap>,<!>),<@>),<low>}")))
(example (utterance "lines containing a vowel or character") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines with vowels after lower-case letter .") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines beginning with a capital letter followed by a number") (targetValue (name "?{startwith(<cap>),<num>}")))
(example (utterance "lines with words that containe iether a number , character , or letter") (targetValue (name "?{or(<any>,<num>),<let>}")))
(example (utterance "lines that include words with a vowel , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(<any>,<!>),<vow>}")))
(example (utterance "lines having 1 of the following: leftquoatation ! rightquoatation leftquoatation @ rightquoatation or a letter before leftquoatation # rightquoatation") (targetValue (name "?{or(or(<@>,<@>),<let>),<#>}")))
(example (utterance "lines with a capital letter , vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<vow>),<!>)}")))
(example (utterance "lines which are containing leftquoatation ! rightquoatation before ending with leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),endwith(<@>)}")))
(example (utterance "lines with 5 or more numbers or characters") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "lines containing a vowel or capital letter") (targetValue (name "?{<vow>,<cap>}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{<vow>,or(<!>,<low>)}")))
(example (utterance "lines containging leftquoatation ! rightquoatation or leftquoatation @ rightquoatation , twice or more times followed by a character") (targetValue (name "?{repeatatleast(or(<!>,<@>),2),<any>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or string leftquoatation # rightquoatation , 6 or more times") (targetValue (name "?{or(concat(<!>,<@>),<#>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 7 or more times before vowel and string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,7),and(<vow>,<@>)}")))
(example (utterance "lines with 4 or more of a lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,repeatatleast(<!>,4)}")))
(example (utterance "lines without leftquoatation ! rightquoatation either followed or not followed by a letter") (targetValue (name "?{star(concat(<!>,<let>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation , vowel , and capital letter before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<vow>),<cap>),<@>}")))
(example (utterance "lines with a capital before ending with vowel") (targetValue (name "?{<cap>,endwith(<vow>)}")))
(example (utterance "capital letter before a character before a number") (targetValue (name "?{concat(<cap>,<any>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation follwed by leftquoatation @ rightquoatation and a lower-case") (targetValue (name "?{concat(<!>,<@>),contain(<low>)}")))
(example (utterance "lines without a vowel before leftquoatation ! rightquoatation") (targetValue (name "?{concat(repeatatleast(<vow>,1),<!>)}")))
(example (utterance "items with words containing leftquoatation ! rightquoatation , a ltter , or a vowel .") (targetValue (name "?{or(<vow>,<let>),<!>}")))
(example (utterance "lines with words and starting with a lower-case letter") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a letter occurring  4 or more times") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines containing a lower-case letter followed by a vowel or leftquoatation ! rightquoatation minimum 1 time .") (targetValue (name "?{concat(<low>,<vow>),repeatatleast(<!>,1)}")))
(example (utterance "lines containing words ending with a capital letter before a letter") (targetValue (name "?{endwith(concat(<cap>,<let>))}")))
(example (utterance "lines which are with words with leftquoatation ! rightquoatation , a capital letter , and leftquoatation @ rightquoatation at least once") (targetValue (name "?{and(and(<!>,<cap>),<@>)}")))
(example (utterance "lines not starting in zero or more of leftquoatation ! rightquoatation") (targetValue (name "?{star(startwith(<!>))}")))
(example (utterance "lines not having vowel , not leftquoatation ! rightquoatation , and not a letter") (targetValue (name "?{and(and(<vow>,<!>),<let>)}")))
(example (utterance "lines not having letter and starting with lower-case letter") (targetValue (name "?{not(<let>),startwith(<low>)}")))
(example (utterance "items with a character , leftquoatation ! rightquoatation or small letter .") (targetValue (name "?{or(or(<any>,<!>),<low>)}")))
(example (utterance "lines with a vowel containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a number") (targetValue (name "?{star(<vow>),and(and(<!>,<@>),<num>)}")))
(example (utterance "lines containing a vowel or dog .") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines wherein leftquoatation ! rightquoatation precedes a vowel or leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(<vow>,<@>)}")))
(example (utterance "items with words containing an upper case letter , a vowel , and a character .") (targetValue (name "?{and(<any>,<vow>),<cap>}")))
(example (utterance "lines with the following - words with a letter and a character") (targetValue (name "?{<let>,<any>}")))
(example (utterance "lines with words with number , string leftquoatation ! rightquoatation , and lower-case letter at least once") (targetValue (name "?{and(and(<num>,<!>),<low>)}")))
(example (utterance "all lines") (targetValue (name "?{<vow>,star(<let>)}")))
(example (utterance "lines with a letter before string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{<let>,or(or(<!>,<@>),<#>)}")))
(example (utterance "lines with 2 words that do not contain a vowel") (targetValue (name "?{not(<vow>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation and beginning in lower-case ltter") (targetValue (name "?{concat(<num>,<!>),startwith(<low>)}")))
(example (utterance "lines with a character before either a lower-case letter , string leftquoatation ! rightquoatation , or string leftquoatation @ rightquoatation") (targetValue (name "?{<any>,contain(or(or(<low>,<!>),<@>))}")))
(example (utterance "lines with 3 or less of a number and character") (targetValue (name "?{not(<num>),repeatatleast(<any>,3)}")))
(example (utterance "lines with a character and string leftquoatation ! rightquoatation before number") (targetValue (name "?{and(<any>,<!>),<num>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a character before letter") (targetValue (name "?{not(<!>),concat(<any>,<let>)}")))
(example (utterance "lines with words and only zero or more numbers") (targetValue (name "?{<num>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before a character") (targetValue (name "?{or(<!>,concat(<@>,<any>))}")))
(example (utterance "lines with character before number followed by vowel") (targetValue (name "?{<any>,concat(<num>,<vow>)}")))
(example (utterance "lines with a vowel , letter , and number before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<let>),<num>),<!>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 7 or more times before containing vowel") (targetValue (name "?{repeatatleast(<!>,7),contain(<vow>)}")))
(example (utterance "lines having a letter , a minimum 7 times or greater appearing prior to a small letter") (targetValue (name "?{repeatatleast(<let>,7),<low>}")))
(example (utterance "items with a vowel , an upper case letter , or letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{or(or(<cap>,<vow>),<let>),<!>}")))
(example (utterance "lines with a letter not before leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<let>),not(<!>)}")))
(example (utterance "lines with words that don't have a vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines with a letter string leftquoatation ! rightquoatation , a lower-case letter , or a capital letter") (targetValue (name "?{<let>,or(or(<!>,<low>),<cap>)}")))
(example (utterance "lines with a vowel then leftquoatation ! rightquoatation before leftquoatation @ rightquoatation 4 times") (targetValue (name "?{concat(<vow>,<!>),repeatatleast(<@>,4)}")))
(example (utterance "lines either with a vowel or that ends with leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),endwith(<!>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation followed by 1 or more lower-case letters") (targetValue (name "?{startwith(concat(<!>,<low>))}")))
(example (utterance "lines with a lower-case letter before vowel and letter then string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<vow>),concat(<let>,<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a lowercase letter preceding a vowel") (targetValue (name "?{or(or(<!>,<@>),<low>),<vow>}")))
(example (utterance "lines with a letter , vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<vow>),<!>)}")))
(example (utterance "lines not starting with leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{startwith(concat(<!>,<let>))}")))
(example (utterance "lines with lower-case letter at least once before character or  string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,1),or(<any>,<!>)}")))
(example (utterance "lines not a having a number") (targetValue (name "?{repeatatleast(<num>,1)}")))
(example (utterance "lines with a number , a capital letter , leftquoatation ! rightquoatation , and a letter") (targetValue (name "?{<num>,and(and(<cap>,<!>),<let>)}")))
(example (utterance "lines with words with a capital or string leftquoatation ! rightquoatation at least once") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines with a capital letter at least 2 times prior to a vowel") (targetValue (name "?{repeatatleast(<cap>,2),<vow>}")))
(example (utterance "lines that do not contain an uppercase letter , number or vowel") (targetValue (name "?{or(or(<cap>,<num>),<vow>)}")))
(example (utterance "lines with a letter , character , and vowel") (targetValue (name "?{and(and(<let>,<any>),<vow>)}")))
(example (utterance "lines ending with 3 or more capital letters") (targetValue (name "?{repeatatleast(<cap>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before non lower-case letter") (targetValue (name "?{repeatatleast(<!>,1),not(<low>)}")))
(example (utterance "lines with a character before a lower-case letter followed by a number in them") (targetValue (name "?{<any>,concat(<low>,<num>)}")))
(example (utterance "lines with zero or more lower-case letters before a character") (targetValue (name "?{star(<low>),<any>}")))
(example (utterance "lines with words and 1 or more number or leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,1),<!>}")))
(example (utterance "lines with a number , zero or more times or starting with a character in them") (targetValue (name "?{star(<num>),startwith(<any>)}")))
(example (utterance "lines with a vowel or string leftquoatation ! rightquoatation before a character") (targetValue (name "?{concat(or(<vow>,<!>),<any>)}")))
(example (utterance "lines containing words with leftquoatation ! rightquoatation and vowel following it") (targetValue (name "?{<!>,<vow>}")))
(example (utterance "lines finishing with leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(contain(<!>),<any>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{contain(and(<!>,<@>))}")))
(example (utterance "lines with a character and vowel before capital letter") (targetValue (name "?{and(<any>,<vow>),<cap>}")))
(example (utterance "lines with a letter before a vowel and a lower-case letter in them") (targetValue (name "?{<let>,and(<vow>,<low>)}")))
(example (utterance "lines which do not contain a letter 4 or more times") (targetValue (name "?{repeatatleast(<let>,4)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation or vowel , 5 or more times") (targetValue (name "?{contain(<!>),repeatatleast(<vow>,5)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a letter followed by leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<let>),<@>}")))
(example (utterance "lines with a letter , number , or vowel") (targetValue (name "?{and(and(<let>,<num>),<vow>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation , at least 6 times") (targetValue (name "?{<!>,endwith(repeatatleast(<@>,6))}")))
(example (utterance "lines with either a letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{or(or(<let>,<!>),<@>),<#>}")))
(example (utterance "lines with a letter , a lower-case letter , or a number") (targetValue (name "?{or(or(<let>,<low>),<num>)}")))
(example (utterance "lines that do not have a character zero or more times") (targetValue (name "?{star(<any>)}")))
(example (utterance "lines containing a character , capital letter , and lower-case letter") (targetValue (name "?{and(and(<any>,<cap>),<low>)}")))
(example (utterance "lines starting in a character or non lower-case letter") (targetValue (name "?{startwith(<any>),not(<low>)}")))
(example (utterance "items terminating with a character , a vowel , or a small letter .") (targetValue (name "?{contain(and(and(<low>,<vow>),<any>))}")))
(example (utterance "lines with words without only a character") (targetValue (name "?{<any>}")))
(example (utterance "lines containing vowel , 2 or more times") (targetValue (name "?{repeatatleast(<vow>,2)}")))
(example (utterance "lines with a lower-case letter and a letter before a number in them") (targetValue (name "?{and(<low>,<let>),<num>}")))
(example (utterance "lines that have a character or capital letter before the word leftquoatation ! rightquoatation") (targetValue (name "?{<!>,or(<cap>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a number , or a vowel") (targetValue (name "?{repeatatleast(<!>,1),or(or(<@>,<num>),<vow>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation , number , or vowel") (targetValue (name "?{<any>,or(or(<!>,<num>),<vow>)}")))
(example (utterance "lines with words starting with a capital followed by string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines not ending with lower-case letter , zero or more times") (targetValue (name "?{star(endwith(<low>))}")))
(example (utterance "lines with a character before lower-case letter then string leftquoatation ! rightquoatation") (targetValue (name "?{<any>,concat(<low>,<!>)}")))
(example (utterance "leftquoatation ! rightquoatation appears before leftquoatation @ rightquoatation or leftquoatation # rightquoatation followed by a character in the lines") (targetValue (name "?{concat(<!>,<@>),concat(<#>,<any>)}")))
(example (utterance "lines containing a character followed by 5 or more of leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines which are containing a lower-case letter , leftquoatation ! rightquoatation , and a vowel") (targetValue (name "?{and(and(<low>,<!>),<vow>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with a number before 4 strings of leftquoatation ! rightquoatation") (targetValue (name "?{<num>,repeatatleast(<!>,4)}")))
(example (utterance "lines having either string leftquoatation ! rightquoatation , capital letter , or number before character") (targetValue (name "?{or(or(<!>,<cap>),<num>),<any>}")))
(example (utterance "lines with characters at least once starting with a lower-case letter") (targetValue (name "?{repeatatleast(<any>,1),startwith(<low>)}")))
(example (utterance "lines starting with a letter followed by a number , zero or more times in them") (targetValue (name "?{star(concat(<let>,<num>))}")))
(example (utterance "lines with vowel , 3 or more times and containing a capital") (targetValue (name "?{repeatatleast(<vow>,3),contain(<cap>)}")))
(example (utterance "lines with lower-case letter before capital letter and string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,and(<cap>,<!>)}")))
(example (utterance "lines with only a letter") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines starting with 6 or more characters") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines with letters after numbers") (targetValue (name "?{<num>,<let>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation and not leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,not(<@>))}")))
(example (utterance "lines with lower-case letter followed by character before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<any>),<!>}")))
(example (utterance "lines with 7 or more vowels followed by capital letters") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , before a number") (targetValue (name "?{concat(star(<!>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , zero or so times before a number") (targetValue (name "?{star(<!>),<num>}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with a character occuring before leftquoatation ! rightquoatation , 5 or more times") (targetValue (name "?{<any>,repeatatleast(<!>,5)}")))
(example (utterance "lines with a letter or not a lower-case letter") (targetValue (name "?{repeatatleast(<let>,1),not(<low>)}")))
(example (utterance "lines that do not have a number and the word  leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<!>)}")))
(example (utterance "lines that do not have a letter , vowel or the word leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<vow>),<!>)}")))
(example (utterance "lines starting with a number , zero times or more") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "lines not having a number or a letter") (targetValue (name "?{and(and(<num>,<vow>),<let>)}")))
(example (utterance "items without a numeral at least twice .") (targetValue (name "?{repeatatleast(<num>,2)}")))
(example (utterance "lines must have a capital letter followed by leftquoatation ! rightquoatation in it") (targetValue (name "?{<cap>,<!>}")))
(example (utterance "lines with zero or mroe of a number and string leftquoatation ! rightquoatation or letter") (targetValue (name "?{or(and(<num>,<!>),<let>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before capital letter") (targetValue (name "?{<!>,repeatatleast(<cap>,1)}")))
(example (utterance "items without a letter at least5times .") (targetValue (name "?{repeatatleast(<let>,5)}")))
(example (utterance "items with a numeral preceding leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{concat(<num>,<!>),<any>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation before a capital letter 3 or more times") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines which are ending with a letter or leftquoatation ! rightquoatation at least once") (targetValue (name "?{endwith(or(<let>,<!>))}")))
(example (utterance "lines containing a character or a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<any>),concat(<vow>,<!>)}")))
(example (utterance "lines with a vowel and starting with a number") (targetValue (name "?{repeatatleast(<vow>,1),startwith(<num>)}")))
(example (utterance "lines with 3 or more or leftquoatation ! rightquoatation before a number") (targetValue (name "?{repeatatleast(<!>,3),startwith(<num>)}")))
(example (utterance "items terminating with a vowel .") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines containing a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation or number") (targetValue (name "?{or(and(<!>,<@>),<num>)}")))
(example (utterance "lines starting in words with zero or more of a character") (targetValue (name "?{star(<any>)}")))
(example (utterance "lines ending in words with a number before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines starting in a character before a letter and capital letter") (targetValue (name "?{startwith(<any>),and(<let>,<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation with a capital letter") (targetValue (name "?{<cap>,and(<num>,<!>)}")))
(example (utterance "lines with a capital letter , 6 or more times or a lower-case letter before leftquoatation ! rightquoatation in them") (targetValue (name "?{repeatatleast(<cap>,6),concat(<low>,<!>)}")))
(example (utterance "lines stating with leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(<!>,repeatatleast(<num>,1))}")))
(example (utterance "lines contain leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "items with a numeral , leftquoatation ! rightquoatation , and letter preceding a small letter .") (targetValue (name "?{and(and(<num>,<let>),<!>),<low>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation before number or lower-case letter") (targetValue (name "?{or(concat(<!>,<num>),<low>)}")))
(example (utterance "lines having words containing a letter at least 7 times before the occurrence of a leftquoatation ! rightquoatation string") (targetValue (name "?{repeatatleast(<let>,7),<!>}")))
(example (utterance "lines with words with a letter before leftquoatation ! rightquoatation in them") (targetValue (name "?{<let>,<!>}")))
(example (utterance "lines with 6 or more vowels before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<vow>,6),<!>}")))
(example (utterance "lines that contain a character and end with a letter") (targetValue (name "?{endwith(<let>),contain(<any>)}")))
(example (utterance "lines with words that contain a letter followed by a lower-case letter , zero or more times") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines with a letter before a character") (targetValue (name "?{contain(<let>),<any>}")))
(example (utterance "lines with at least 1 character , or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{contain(and(and(<!>,<any>),<@>))}")))
(example (utterance "lines having a vowel , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation coming before a number") (targetValue (name "?{and(and(<vow>,<!>),<@>),<num>}")))
(example (utterance "lines with a character , vowel , or string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<any>,<vow>),<!>),<@>}")))
(example (utterance "lines that contain words ending with a letter followed by a lower-case letter") (targetValue (name "?{endwith(<let>),<low>}")))
(example (utterance "lines with a letter before a number") (targetValue (name "?{or(<cap>,<let>),<num>}")))
(example (utterance "lines with a letter before a vowel") (targetValue (name "?{repeatatleast(<let>,1),<vow>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , zero or more times and string leftquoatation @ rightquoatation followed by character") (targetValue (name "?{star(<!>),concat(<@>,<any>)}")))
(example (utterance "lines with only less than 5 vowels") (targetValue (name "?{repeatatleast(<vow>,5)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before ending with containing only string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,endwith(<@>)}")))
(example (utterance "lines with words and a letter") (targetValue (name "?{<cap>,<let>}")))
(example (utterance "items with a vowel preceding a letter at least thrice .") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines with words with either leftquoatation ! rightquoatation , a lower-case letter , or leftquoatation @ rightquoatation in them") (targetValue (name "?{or(<!>,<low>),<@>}")))
(example (utterance "lines containing a capital after a character") (targetValue (name "?{<any>,<cap>}")))
(example (utterance "lines with a character before a vowel or a letter in them") (targetValue (name "?{<any>,or(<vow>,<let>)}")))
(example (utterance "lines containing a vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "items with a small letter or leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{and(<low>,<!>),<any>}")))
(example (utterance "lines having leftquoatation ! rightquoatation , lower-case letter and string leftquoatation @ rightquoatation before a character") (targetValue (name "?{and(and(<!>,<low>),<@>),<any>}")))
(example (utterance "items with a numeral or leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{or(<num>,<!>),<any>}")))
(example (utterance "the lines that include either a letter or a capital letter , that is followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<cap>),<!>}")))
(example (utterance "lines without a vowel before lower-case letter") (targetValue (name "?{not(contain(<vow>)),<low>}")))
(example (utterance "lines with a letter and number") (targetValue (name "?{<low>,repeatatleast(<num>,1)}")))
(example (utterance "lines with a number or character before a capital at least once") (targetValue (name "?{or(<num>,concat(<any>,<cap>))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation at least once and containing a number") (targetValue (name "?{repeatatleast(<!>,1),contain(<num>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation as well as a capital letter") (targetValue (name "?{and(<!>,<cap>)}")))
(example (utterance "lines with a number") (targetValue (name "?{contain(not(<num>))}")))
(example (utterance "lines without a capital letter , string leftquoatation ! rightquoatation , or letter") (targetValue (name "?{and(and(<cap>,<!>),<let>)}")))
(example (utterance "lines with a capital letter followed by leftquoatation ! rightquoatation and a letter") (targetValue (name "?{concat(<cap>,<!>),star(<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or number") (targetValue (name "?{not(<!>),repeatatleast(<num>,1)}")))
(example (utterance "lines with words that have numbers , 3 times or more") (targetValue (name "?{repeatatleast(<num>,3)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , 1 or more times or beginning with a letter") (targetValue (name "?{repeatatleast(<!>,1),startwith(<let>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{repeatatleast(contain(<!>),2)}")))
(example (utterance "items with words containing just leftquoatation ! rightquoatation at least5times .") (targetValue (name "?{<!>}")))
(example (utterance "lines that do not start with a lower-case letter") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "lines ending with a character 4 times") (targetValue (name "?{repeatatleast(<any>,4)}")))
(example (utterance "lines ending in words and a letter before lower-case letter") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines starting with words and leftquoatation ! rightquoatation and a vowel") (targetValue (name "?{and(<!>,<vow>)}")))
(example (utterance "lines with a capital letter preceded by a vowel or by leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and vowel before a capital letter") (targetValue (name "?{and(<!>,<vow>),<cap>}")))
(example (utterance "items with a letter or numeral preceding a character .") (targetValue (name "?{or(<num>,<let>),<any>}")))
(example (utterance "lines containing 1 of the following: a vowel , a capital letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<cap>),<!>)}")))
(example (utterance "lines that have a lower-case letter preceding a capital letter") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "items with a character preceding a small letter , leftquoatation ! rightquoatation , or a numeral .") (targetValue (name "?{<any>,or(or(<low>,<!>),<num>)}")))
(example (utterance "lines with words with a letter or number before a vowel") (targetValue (name "?{or(or(<let>,<num>),<low>),<vow>}")))
(example (utterance "lines ending with a vowel leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<vow>),concat(<!>,<@>)}")))
(example (utterance "lines ending in a number and string leftquoatation ! rightquoatation before a character") (targetValue (name "?{endwith(<num>),concat(<!>,<any>)}")))
(example (utterance "lines containing a lower-case letter , a number , and a vowel") (targetValue (name "?{and(<low>,<num>),<vow>}")))
(example (utterance "lines that do not contain a capital letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<!>),<vow>)}")))
(example (utterance "lines having a capital  , leftquoatation ! rightquoatation , and a letter before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<cap>,<!>),<let>),<@>}")))
(example (utterance "lines without a letter , capital letter , or vowel") (targetValue (name "?{or(or(<let>,<cap>),<vow>)}")))
(example (utterance "lines with a lower-case letter before number or capital letter") (targetValue (name "?{<low>,and(<num>,<cap>)}")))
(example (utterance "items with words terminating with a numeral .") (targetValue (name "?{<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital then lower-case letter") (targetValue (name "?{concat(<!>,<cap>),<low>}")))
(example (utterance "lines with 6 ro more of a number before a vowel") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines where at least once a capital letter is preceded by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines ending with a lower-case letter or leftquoatation ! rightquoatation and leftquoatation @ rightquoatation in them") (targetValue (name "?{or(<low>,and(<!>,<@>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or 3 or more numbers") (targetValue (name "?{contain(<!>),repeatatleast(<num>,3)}")))
(example (utterance "lines which begin with a character or vowel") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines that do not contain a vowel , a capital letter , or a number") (targetValue (name "?{or(or(<vow>,<cap>),<num>)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation , a vowel , and the word leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<vow>),<@>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation , a letter , and character") (targetValue (name "?{and(<!>,<let>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a character preceding a vowel") (targetValue (name "?{concat(<any>,<vow>),<!>}")))
(example (utterance "lines containing  a number at least once followed by a lower-case letter") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines with words with lower-case letter before number") (targetValue (name "?{<low>,<num>}")))
(example (utterance "lines that do not contain any of the following 3 things:  a lower-case letter , leftquoatation ! rightquoatation , and a character .") (targetValue (name "?{and(and(<low>,<!>),<any>)}")))
(example (utterance "lines that contain a vowel before leftquoatation ! rightquoatation and then followed by a number") (targetValue (name "?{<vow>,concat(<!>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a number , and a capital letter") (targetValue (name "?{and(and(<!>,<num>),<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before character at least once") (targetValue (name "?{<!>,repeatatleast(<any>,1)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation or a capital letter at least 4 times") (targetValue (name "?{or(repeatatleast(<!>,4),<cap>)}")))
(example (utterance "lines containing a vowel preceded by leftquoatation ! rightquoatation") (targetValue (name "?{<!>,<vow>}")))
(example (utterance "lines with a number before a vowel or letter") (targetValue (name "?{<num>,or(<vow>,<let>)}")))
(example (utterance "lines with a lower-case letter , vowel , and string leftquoatation ! rightquoatation before a character") (targetValue (name "?{and(and(<low>,<vow>),<!>),<any>}")))
(example (utterance "lines ending with zero or more characters or numbers") (targetValue (name "?{or(endwith(<any>),<num>)}")))
(example (utterance "lines with words with a vowel then leftquoatation ! rightquoatation 6 times") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines with less than 2 characters before a letter") (targetValue (name "?{not(repeatatleast(<any>,2)),<let>}")))
(example (utterance "the lines that do not contain leftquoatation ! rightquoatation repeated 2 or more times") (targetValue (name "?{repeatatleast(<!>,2)}")))
(example (utterance "lines with 5 or more of a capital letter before number") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines not beginning with a number , 4 or more times") (targetValue (name "?{repeatatleast(startwith(<num>),4)}")))
(example (utterance "lines with words that contain a character , a capital letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<cap>),<!>}")))
(example (utterance "worded lines beginning with a vowel and capital letter") (targetValue (name "?{startwith(<vow>),<cap>}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation but that have a letter before that") (targetValue (name "?{<let>,endwith(<!>)}")))
(example (utterance "lines with either a letter , leftquoatation ! rightquoatation , or a vowel followed by a number") (targetValue (name "?{or(or(<let>,<!>),<vow>),<num>}")))
(example (utterance "lines with a letter , character , or a leftquoatation ! rightquoatation string") (targetValue (name "?{or(or(<let>,<any>),<!>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation and before capital letter") (targetValue (name "?{star(<!>),concat(<num>,<cap>)}")))
(example (utterance "items with words containing leftquoatation ! rightquoatation and a numeral .") (targetValue (name "?{<num>,<!>}")))
(example (utterance "lines with a lower-case letter , string leftquoatation ! rightquoatation , and letter before a capital letter") (targetValue (name "?{and(and(<low>,<!>),<let>),<cap>}")))
(example (utterance "lines which are not having leftquoatation ! rightquoatation before a vowel , 4 or more times") (targetValue (name "?{not(<!>),repeatatleast(<vow>,4)}")))
(example (utterance "lines where a lower case letter follows either a letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<!>),<@>),<low>}")))
(example (utterance "lines all having leftquoatation ! rightquoatation leftquoatation @ rightquoatation and a lower-case letter before leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<low>),<#>}")))
(example (utterance "lines having the following -a lower-case letter , leftquoatation ! rightquoatation , and a capital letter before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<low>,<!>),<cap>),<@>}")))
(example (utterance "lines followed by a character letter") (targetValue (name "?{<any>,<let>}")))
(example (utterance "lines with capital letter before either vowel , string leftquoatation ! rightquoatation , or string leftquoatation @ rightquoatation") (targetValue (name "?{<cap>,or(or(<vow>,<!>),<@>)}")))
(example (utterance "lines with 4 or more of a number before a character") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines with words not beginning with leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<!>)}")))
(example (utterance "lines containing words having a letter , a capital letter , as well as a number") (targetValue (name "?{and(<let>,<cap>),<num>}")))
(example (utterance "lines with 6 or more letters") (targetValue (name "?{<let>}")))
(example (utterance "lines that begin with a capital and end with a lowercase") (targetValue (name "?{startwith(<cap>),endwith(<low>)}")))
(example (utterance "lines with a letter , leftquoatation ! rightquoatation and number before a vowel") (targetValue (name "?{and(and(<let>,<!>),<num>),<vow>}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation and a vowel") (targetValue (name "?{endwith(concat(<!>,<vow>))}")))
(example (utterance "lines that begin with a letter followed by a capital letter and a lower-case letter") (targetValue (name "?{startwith(<let>),and(<cap>,<low>)}")))
(example (utterance "items with words containing a character .") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines with words that string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{not(<!>)}")))
(example (utterance "lines with words ending with a vowel") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines not containing letter or leftquoatation ! rightquoatation") (targetValue (name "?{not(and(<let>,<!>))}")))
(example (utterance "items with an upper case letter preceding a numeral , at least thrice .") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines that have a letter after a capital letter or vowel") (targetValue (name "?{or(<cap>,<vow>),<let>}")))
(example (utterance "lines ending in words with string leftquoatation ! rightquoatation and vowel") (targetValue (name "?{and(<!>,<vow>)}")))
(example (utterance "lines that have 4 or more letters before a number") (targetValue (name "?{repeatatleast(<let>,4),<num>}")))
(example (utterance "items terminating with a numeral preceding an upper case letter .") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation preceded by leftquoatation @ rightquoatation , a vowel , and a letter") (targetValue (name "?{and(and(<@>,<vow>),<let>),<!>}")))
(example (utterance "lines that have a capital letter or the word leftquoatation ! rightquoatation in front of a letter") (targetValue (name "?{or(<cap>,<!>),<let>}")))
(example (utterance "lines with a lower-case letter , leftquoatation ! rightquoatation , or vowel before a capital letter") (targetValue (name "?{or(or(<low>,<!>),<vow>),<cap>}")))
(example (utterance "lines with words starting a character , 6 or more times") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines with a letter or string leftquoatation ! rightquoatation before lower-case letter , zero or more times") (targetValue (name "?{or(<let>,<!>),star(<low>)}")))
(example (utterance "lines with words with a character , a vowel , and a lower-case letter in them") (targetValue (name "?{and(<any>,<vow>),<low>}")))
(example (utterance "lines that do not contain either a vowel or a character") (targetValue (name "?{and(<vow>,<any>)}")))
(example (utterance "at least 7 times lines with a vowel or any letter") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines that may have numbers , maybe at the end .") (targetValue (name "?{star(contain(<num>))}")))
(example (utterance "lines without zero or more of leftquoatation ! rightquoatation and vowel") (targetValue (name "?{not(<!>),star(<vow>)}")))
(example (utterance "lines that end with a number followed by a capital letter and a lower-case letter") (targetValue (name "?{endwith(<num>),and(<cap>,<low>)}")))
(example (utterance "lines that do not end with a character at least zero times") (targetValue (name "?{star(endwith(<any>))}")))
(example (utterance "lines with a letter before string leftquoatation ! rightquoatation or number") (targetValue (name "?{or(concat(<let>,<!>),<num>)}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , and a capital before vowel") (targetValue (name "?{and(and(<!>,<@>),<cap>),<vow>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a small letter and a character .") (targetValue (name "?{<!>,and(<low>,<any>)}")))
(example (utterance "lines with zero or more of a lower-case letter before vowel") (targetValue (name "?{concat(star(<low>),<vow>)}")))
(example (utterance "lines with at least 2 capital letters before another letter") (targetValue (name "?{repeatatleast(<cap>,2),<let>}")))
(example (utterance "lines with a character at least zero times") (targetValue (name "?{star(<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a capital letter") (targetValue (name "?{and(and(<!>,<@>),<cap>)}")))
(example (utterance "lines with words that contain a number , a character , and a lower-case letter") (targetValue (name "?{and(<num>,<any>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a lower-case letter being before a number") (targetValue (name "?{concat(<!>,<low>),<num>}")))
(example (utterance "lines starting with a letter string leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(and(<let>,<!>),<num>)}")))
(example (utterance "lines starting in less than 7 characters") (targetValue (name "?{startwith(repeatatleast(<any>,7))}")))
(example (utterance "lines without a letter") (targetValue (name "?{not(<let>)}")))
(example (utterance "lines containing a letter as well as a vowel followed by a character") (targetValue (name "?{and(<let>,<vow>),<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{<!>,contain(<let>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{<num>,or(or(<!>,<@>),<#>)}")))
(example (utterance "lines with a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines starting in string leftquoatation ! rightquoatation with a number or string leftquoatation @ rightquoatation") (targetValue (name "?{or(and(<!>,<num>),<@>)}")))
(example (utterance "lines with a letter or string leftquoatation ! rightquoatation before a number") (targetValue (name "?{contain(<let>),concat(<!>,<num>)}")))
(example (utterance "lines not starting in a vowel without a lower-case letter") (targetValue (name "?{and(startwith(<vow>),<low>)}")))
(example (utterance "lines where 4 or more times leftquoatation ! rightquoatation or a vowel is contained") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines without the string dog before a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<!>,<cap>),<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation , leftquoatation # rightquoatation , or a vowel .") (targetValue (name "?{<!>,or(or(<vow>,<@>),<#>)}")))
(example (utterance "lines with character before leftquoatation ! rightquoatation lower case letter , and a letter") (targetValue (name "?{<any>,and(and(<!>,<low>),<let>)}")))
(example (utterance "lines with a vowel and number before lower-case letter or string leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<num>),or(<low>,<!>)}")))
(example (utterance "lines with 5 or more of leftquoatation ! rightquoatation before having a letter and character") (targetValue (name "?{repeatatleast(<!>,5),and(<let>,<any>)}")))
(example (utterance "lines with a letter and number before vowel") (targetValue (name "?{contain(<let>),concat(<num>,<vow>)}")))
(example (utterance "items without an uppercase letter preceding a vowel .") (targetValue (name "?{not(<cap>),<vow>}")))
(example (utterance "lines starting with words with number followed by character") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines that have words with a character or a capital letter") (targetValue (name "?{<cap>,<any>}")))
(example (utterance "lines not having lower-case letter and not a letter") (targetValue (name "?{and(<low>,<let>)}")))
(example (utterance "lines having words with only 2 things: a lower-case letter and a vowel") (targetValue (name "?{<low>,<vow>}")))
(example (utterance "lines with 3 or more of a vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,repeatatleast(<!>,3)}")))
(example (utterance "items with a letter preceding leftquoatation ! rightquoatation , a numeral , and leftquoatation @ rightquoatation .") (targetValue (name "?{<let>,and(and(<num>,<@>),<!>)}")))
(example (utterance "lines with a lower-case letter or string leftquoatation ! rightquoatation and character") (targetValue (name "?{and(or(<low>,<!>),<any>)}")))
(example (utterance "lines that have words with a letter , character , and capital letter") (targetValue (name "?{and(<let>,<any>),<cap>}")))
(example (utterance "lines with words with a vowel , leftquoatation ! rightquoatation , and a letter in them") (targetValue (name "?{and(<vow>,<!>),<let>}")))
(example (utterance "lines with words with either a vowel , number , or lower-case letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<num>),<low>),<!>}")))
(example (utterance "lines that contain6or more combinations of a vowel before a number") (targetValue (name "?{repeatatleast(<vow>,6),<num>}")))
(example (utterance "lines with words that contain number , zero or more times") (targetValue (name "?{star(<num>)}")))
(example (utterance "lines ending with lower-case letter , zero or more times or a number") (targetValue (name "?{or(star(<low>),<num>)}")))
(example (utterance "lines having words with leftquoatation ! rightquoatation before letter or number") (targetValue (name "?{concat(<!>,<let>),<num>}")))
(example (utterance "lines with words with either a lower-case letter , string leftquoatation ! rightquoatation , or letter") (targetValue (name "?{or(<low>,<!>),<let>}")))
(example (utterance "lines containing words including a lower-case letter and leftquoatation ! rightquoatation , 7 or more times") (targetValue (name "?{and(<low>,<!>)}")))
(example (utterance "lines with a letter followed by a lower-case letter , zero or more times in them") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines with a vowel before a character , a letter , and leftquoatation ! rightquoatation in them") (targetValue (name "?{<vow>,and(and(<any>,<let>),<!>)}")))
(example (utterance "lines that have words with leftquoatation ! rightquoatation before 5 or more of leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with characters over 7 times before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,7),<!>}")))
(example (utterance "lines containing words where leftquoatation ! rightquoatation appears 6 or more times , or a lower-case letter") (targetValue (name "?{repeatatleast(<!>,6),<low>}")))
(example (utterance "lines without a vowel , lower-case letter , or character") (targetValue (name "?{or(or(<vow>,<low>),<any>)}")))
(example (utterance "lines with letter before number at least once") (targetValue (name "?{<let>,repeatatleast(<num>,1)}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation zero times") (targetValue (name "?{<cap>,star(<!>)}")))
(example (utterance "lines that contain a lower-case letter , leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{and(and(<#>,<low>),<!>),<@>}")))
(example (utterance "lines without a lower-case letter before a vowel") (targetValue (name "?{concat(<low>,star(<vow>))}")))
(example (utterance "lines with a lower-case letter , leftquoatation ! rightquoatation , and a capital letter after leftquoatation @ rightquoatation") (targetValue (name "?{<@>,and(and(<low>,<!>),<cap>)}")))
(example (utterance "lines having a character , a capital , and a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<cap>),<low>),<!>}")))
(example (utterance "lines containing containing only vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),<!>}")))
(example (utterance "lines with a number before 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(concat(<num>,<!>),3)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<@>),<#>)}")))
(example (utterance "lines without words containing a character preceding leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "letter ending with a vowel after a number") (targetValue (name "?{endwith(<num>),<vow>}")))
(example (utterance "lines beginning with a letter or containing a capital letter") (targetValue (name "?{repeatatleast(<cap>,1),startwith(<let>)}")))
(example (utterance "lines using the word string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , no times or more") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines containing either a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines with a character 7 times") (targetValue (name "?{repeatatleast(<any>,7)}")))
(example (utterance "lines with lower-case letter , 7 or more times before number") (targetValue (name "?{repeatatleast(<low>,7),<num>}")))
(example (utterance "lines without a lower-case letter , character , or string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<any>),<!>)}")))
(example (utterance "lines ending in words with a capital letter or character") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines with words not containing only a vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines with a character coming 4 or more times") (targetValue (name "?{<any>}")))
(example (utterance "lines that have a letter and the word leftquoatation ! rightquoatation followed by the word leftquoatation @ rightquoatation") (targetValue (name "?{and(<let>,<!>),<@>}")))
(example (utterance "lines starting with not leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation") (targetValue (name "?{or(not(<!>),<@>)}")))
(example (utterance "lines contaning leftquoatation ! rightquoatation , a lower-case , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<low>),<@>)}")))
(example (utterance "lines not having a letter before string leftquoatation ! rightquoatation or vowel") (targetValue (name "?{not(<let>),or(<!>,<vow>)}")))
(example (utterance "lines with words with either a vowel , a capital , or a lower-case letter") (targetValue (name "?{or(<vow>,<cap>),<low>}")))
(example (utterance "lines with a capital letter before containing a letter in them") (targetValue (name "?{<cap>,contain(<let>)}")))
(example (utterance "lines with letter , zero or more times and number before a capital") (targetValue (name "?{star(<let>),concat(<num>,<cap>)}")))
(example (utterance "lines with a letter before a lower-case letter then character") (targetValue (name "?{repeatatleast(<let>,1),concat(<low>,<any>)}")))
(example (utterance "items without a starting vowel .") (targetValue (name "?{startwith(star(<vow>))}")))
(example (utterance "lines that start with 5 or more numbers") (targetValue (name "?{repeatatleast(<num>,5)}")))
(example (utterance "lines that don't contain leftquoatation ! rightquoatation  or end with a capital letter .") (targetValue (name "?{not(<!>),endwith(<cap>)}")))
(example (utterance "lines with a character , leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , as well as leftquoatation # rightquoatation") (targetValue (name "?{contain(<any>),and(and(<!>,<@>),<#>)}")))
(example (utterance "lines must contain a character or a lower-case letter 2 or more times") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "lines with 5 or more of leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{and(<!>,<@>),repeatatleast(<#>,5)}")))
(example (utterance "lines with a character before a letter") (targetValue (name "?{<any>,repeatatleast(<let>,1)}")))
(example (utterance "lines without characters , not leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<any>,<!>),<@>)}")))
(example (utterance "lines with a letter , a character , or a number") (targetValue (name "?{or(<let>,<any>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a number before leftquoatation @ rightquoatation , zero or more times in them") (targetValue (name "?{and(<!>,<num>),star(<@>)}")))
(example (utterance "lines with words and without a capital letter , vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<low>),<!>)}")))
(example (utterance "lines containing a vowel followed by a number or a letter") (targetValue (name "?{<vow>,or(<num>,<let>)}")))
(example (utterance "lines containing a lower-case letter or a vowel at least 6 times") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a number , or leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<num>),<@>),<#>}")))
(example (utterance "lines with 6 or more of a capital letter before a number") (targetValue (name "?{<cap>,repeatatleast(<num>,6)}")))
(example (utterance "lines with a lower-case letter following any other letter") (targetValue (name "?{<let>,<low>}")))
(example (utterance "lines that have a vowel or number 4 or more times") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with a number before capital letter , character , or vowel") (targetValue (name "?{<num>,or(or(<cap>,<any>),<vow>)}")))
(example (utterance "lines with a number , string leftquoatation ! rightquoatation , and capital letter before a lower-case letter") (targetValue (name "?{and(and(<num>,<!>),<cap>),<low>}")))
(example (utterance "lines that have a vowel coming before a leftquoatation ! rightquoatation string , a letter , and a character") (targetValue (name "?{<vow>,and(and(<any>,<!>),<let>)}")))
(example (utterance "lines with a capital letter before a number and a letter") (targetValue (name "?{repeatatleast(<cap>,1),and(<num>,<let>)}")))
(example (utterance "lines with a capital letter before 2 or more of leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines not having leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a lower-case letter") (targetValue (name "?{concat(or(<!>,<@>),<low>)}")))
(example (utterance "lines with words ending with a lower-case letter , zero or more times") (targetValue (name "?{star(<low>)}")))
(example (utterance "lines that begin with words containing a vowel , letter , or the word leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<let>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , letter , or character") (targetValue (name "?{or(or(<!>,<let>),<any>)}")))
(example (utterance "lines that have a character coming after a vowel .") (targetValue (name "?{<vow>,contain(<any>)}")))
(example (utterance "items terminating with vowel preceding a numeral .") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines starting with words with capital letter followed by letter") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines which are containing a letter before either a character , leftquoatation ! rightquoatation , or a lower-case letter") (targetValue (name "?{contain(<let>),or(or(<any>,<!>),<low>)}")))
(example (utterance "lines with words with lower case letter , vowel , and leftquoatation ! rightquoatation at least once") (targetValue (name "?{and(and(<low>,<vow>),<!>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation and a number or letter") (targetValue (name "?{or(and(<!>,<num>),<let>)}")))
(example (utterance "lines without a lower case") (targetValue (name "?{not(<low>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines starting with 5 or more vowels") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines starting with words ending with a capital letter") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines having a number , leftquoatation ! rightquoatation , a lower-case letter , and a capital letter") (targetValue (name "?{and(and(<num>,<!>),<low>),<cap>}")))
(example (utterance "items with a numeral preceding a character at least thrice .") (targetValue (name "?{<num>,repeatatleast(<any>,3)}")))
(example (utterance "lines containing a lower-case letter or a vowel , 6 or more times") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "at least twice , leftquoatation ! rightquoatation followed by a character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines with a capital before lower-case letter then string leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,concat(<low>,<!>)}")))
(example (utterance "lines with 5 or more letters") (targetValue (name "?{<let>}")))
(example (utterance "lines with zero or more of a letter before a vowel") (targetValue (name "?{star(concat(<let>,<vow>))}")))
(example (utterance "lines that contain a capital letter repeated 7 times") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines that have a character and a letter preceding a vowel") (targetValue (name "?{and(<any>,<let>),<vow>}")))
(example (utterance "lines that contain a vowel before a letter") (targetValue (name "?{contain(<let>),<vow>}")))
(example (utterance "lines with a capital letter after a number at least 5 times") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines with a character before a letter") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines with a capital letter or a character") (targetValue (name "?{or(star(<cap>),<any>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a small letter , and a numeral .") (targetValue (name "?{and(and(<num>,<!>),<low>)}")))
(example (utterance "lines with zero or more numbers starting in string leftquoatation ! rightquoatation") (targetValue (name "?{star(<num>),startwith(<!>)}")))
(example (utterance "lines with a vowel before a number") (targetValue (name "?{<vow>,contain(<num>)}")))
(example (utterance "a character and a lower cased letter is required of lines") (targetValue (name "?{and(<any>,<low>)}")))
(example (utterance "lines ending with a vowel preceding a capital letter") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines with a letter before a capital") (targetValue (name "?{<let>,<cap>}")))
(example (utterance "lines that have a combination of a lower-case letter followed by the word leftquoatation ! rightquoatation , then the word leftquoatation @ rightquoatation , 1 or more times") (targetValue (name "?{concat(<low>,<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a character before a vowel") (targetValue (name "?{and(<!>,<any>),<vow>}")))
(example (utterance "all lines") (targetValue (name "?{not(<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before either leftquoatation @ rightquoatation , a letter , or a vowel in them") (targetValue (name "?{<!>,or(or(<@>,<let>),<vow>)}")))
(example (utterance "lines with a number followed by leftquoatation ! rightquoatation") (targetValue (name "?{contain(<num>),<!>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation before 3 or more vowels") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines with a number or lower-case letter before character") (targetValue (name "?{or(<num>,<low>),<any>}")))
(example (utterance "lines ending in a character or lower-case letter") (targetValue (name "?{endwith(or(<any>,<low>))}")))
(example (utterance "lines ending in a capital letter with string leftquoatation ! rightquoatation") (targetValue (name "?{concat(endwith(<cap>),<!>)}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation , a number , or a character") (targetValue (name "?{or(<!>,<num>),<any>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation not leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),not(<@>)}")))
(example (utterance "lines with at least 1 letter or capital letter") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation and no character") (targetValue (name "?{<!>,not(<any>)}")))
(example (utterance "lines without a vowel , lower-case letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<low>),<!>)}")))
(example (utterance "lines starting with words a long with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "all lines") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "ends with lower-case letter") (targetValue (name "?{repeatatleast(<low>,1)}")))
(example (utterance "lines in which there is a small letter that is seen a minimum of 3 times or higher") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before string leftquoatation # rightquoatation") (targetValue (name "?{or(<!>,<@>),<#>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with words with a letter") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation") (targetValue (name "?{and(star(<!>),<let>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation or number at least once") (targetValue (name "?{contain(or(<!>,<num>))}")))
(example (utterance "lines without a letter") (targetValue (name "?{not(or(<let>,<low>))}")))
(example (utterance "lines with words that contain a capital letter , a lower-case letter , and a letter") (targetValue (name "?{and(<cap>,<let>),<low>}")))
(example (utterance "lines with 3 or more of leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<!>,<let>),repeatatleast(<low>,3)}")))
(example (utterance "lines without a lower-case letter and not a vowel") (targetValue (name "?{and(<low>,<vow>)}")))
(example (utterance "lines that contain a character followed by a capital letter , at least 3 times") (targetValue (name "?{repeatatleast(<any>,3),<cap>}")))
(example (utterance "lines with words ending in only a number") (targetValue (name "?{<num>}")))
(example (utterance "lines not containing a capital , zero or more times") (targetValue (name "?{not(star(<cap>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceding either a number , character , ro leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<num>,<any>),<@>)}")))
(example (utterance "lines not ending with lower-case letter at least once") (targetValue (name "?{repeatatleast(endwith(<low>),1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation not being a letter") (targetValue (name "?{star(<!>),not(<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation , an upper case letter and letter .") (targetValue (name "?{<!>,and(and(<@>,<cap>),<let>)}")))
(example (utterance "items with a vowel preceding an upper case letter at least 1 time .") (targetValue (name "?{<vow>,repeatatleast(<cap>,1)}")))
(example (utterance "lines not having vowel before number") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines containing a number before a capital letter , 4 or more times in them") (targetValue (name "?{concat(<num>,repeatatleast(<cap>,4))}")))
(example (utterance "lines starting in 7 or more of string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(startwith(<!>),7),<@>}")))
(example (utterance "lines with a lower-case letter after leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a character") (targetValue (name "?{or(or(<!>,<@>),<any>),<low>}")))
(example (utterance "lines containing words with or without a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{star(<let>),<!>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before ending with vowel , 3 or more times") (targetValue (name "?{<!>,repeatatleast(endwith(<vow>),3)}")))
(example (utterance "lines 7 or more times with a character before a lower case letter") (targetValue (name "?{repeatatleast(<any>,7),<low>}")))
(example (utterance "lines containing the words dog and truck in capitals .") (targetValue (name "?{<!>,and(and(<num>,<@>),<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<@>),<#>)}")))
(example (utterance "lines with words containing a number , capital letter , or vowel .") (targetValue (name "?{or(<num>,<cap>),<vow>}")))
(example (utterance "lines with a lower-case letter preceded by either a vowel , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<@>),<low>}")))
(example (utterance "items without a vowel , a character , and a letter .") (targetValue (name "?{and(and(<let>,<any>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending in number") (targetValue (name "?{and(<!>,endwith(<num>))}")))
(example (utterance "lines with words and a letter and vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<vow>),<!>}")))
(example (utterance "lines with a capital letter before a character at least once in them") (targetValue (name "?{<cap>,repeatatleast(<any>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter or a number in them") (targetValue (name "?{<!>,or(<low>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a lower-case letter followed by a number") (targetValue (name "?{<low>,concat(<!>,<num>)}")))
(example (utterance "lines with a vowel once or more before a non letter") (targetValue (name "?{repeatatleast(<vow>,1),not(<let>)}")))
(example (utterance "lines that have a character occurring at least 3 times before a leftquoatation ! rightquoatation string") (targetValue (name "?{repeatatleast(<any>,3),<!>}")))
(example (utterance "items terminating with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{endwith(concat(<!>,<@>))}")))
(example (utterance "lines ending in zero or more of a letter or vowel") (targetValue (name "?{star(or(<let>,<vow>))}")))
(example (utterance "lines with a capital letter followed by a number") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines that end with only 2 or letters") (targetValue (name "?{repeatatleast(<let>,2)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before either a character , string leftquoatation @ rightquoatation , or a lower-case letter") (targetValue (name "?{<!>,or(or(<any>,<@>),<low>)}")))
(example (utterance "lines that end with a vowel followed by a letter") (targetValue (name "?{endwith(<vow>),<let>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before character or vowel") (targetValue (name "?{<!>,or(<any>,<vow>)}")))
(example (utterance "lines ending in words with string leftquoatation ! rightquoatation and lower-case letter") (targetValue (name "?{and(and(<!>,<let>),<low>)}")))
(example (utterance "lines containing a letter preceding a number before leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{concat(<let>,<num>),or(<!>,<@>)}")))
(example (utterance "lines ending with a lower-case letter at least once") (targetValue (name "?{repeatatleast(contain(<low>),1)}")))
(example (utterance "lines with a number before a character") (targetValue (name "?{contain(<num>),<any>}")))
(example (utterance "lines with character before letter followed by lower-case letter") (targetValue (name "?{<any>,concat(<let>,<low>)}")))
(example (utterance "lines containing a number before a number and with or without a vowel .") (targetValue (name "?{concat(<cap>,<num>),star(<vow>)}")))
(example (utterance "lines with a capital letter and string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a character") (targetValue (name "?{and(and(<cap>,<!>),<@>),<any>}")))
(example (utterance "lines with a character or capital letter before vowel") (targetValue (name "?{or(<any>,<cap>),<vow>}")))
(example (utterance "lines with a letter at least 7 times") (targetValue (name "?{repeatatleast(<let>,7)}")))
(example (utterance "lines finishing with leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines that contain the word leftquoatation ! rightquoatation and ends with the word leftquoatation @ rightquoatation 1 or more  times") (targetValue (name "?{<!>,repeatatleast(endwith(<@>),1)}")))
(example (utterance "lines with words with only leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{<!>,<low>}")))
(example (utterance "lines which have  a number or capital letter") (targetValue (name "?{<cap>,<num>}")))
(example (utterance "lines with words and 4 or more characters") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines with lower case letter before vowel at least once") (targetValue (name "?{<low>,repeatatleast(<vow>,1)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation followed by a letter or leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(<let>,<@>)}")))
(example (utterance "lines with a capital letter before character then number") (targetValue (name "?{<cap>,concat(<any>,<num>)}")))
(example (utterance "lines containing words starting with a capital letter before leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<cap>),<!>}")))
(example (utterance "lines that have words and a number or leftquoatation ! rightquoatation") (targetValue (name "?{<!>,repeatatleast(<num>,1)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation , string leftquoatation # rightquoatation , and lower-case letter") (targetValue (name "?{<!>,and(and(<@>,<#>),<low>)}")))
(example (utterance "lines containing 2 or more capital letters") (targetValue (name "?{repeatatleast(<cap>,2)}")))
(example (utterance "lines which end with a vowel and with leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<vow>),contain(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines with 7 or more of a capital letter before number") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines with 6 or more of a capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with a letter or character") (targetValue (name "?{or(<let>,<cap>),<any>}")))
(example (utterance "items without a letter or leftquoatation ! rightquoatation .") (targetValue (name "?{or(or(<let>,<!>),<vow>)}")))
(example (utterance "lines that have at least 1 number before they end with a letter") (targetValue (name "?{<num>,endwith(<let>)}")))
(example (utterance "lines with letter before a number at least once") (targetValue (name "?{<let>,repeatatleast(contain(<num>),1)}")))
(example (utterance "items without any letter .") (targetValue (name "?{and(<let>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , vowel , or capital letter or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(or(<!>,<vow>),<cap>),<@>)}")))
(example (utterance "lines with at least 1 number or leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines without a vowel and no capital letter") (targetValue (name "?{and(<vow>,<cap>)}")))
(example (utterance "lines with a vowel and capital or lower-case letter before a number") (targetValue (name "?{or(or(<vow>,<cap>),<low>),<num>}")))
(example (utterance "lines containing a character or leftquoatation ! rightquoatation before lower case letters .") (targetValue (name "?{or(<any>,<!>),<low>}")))
(example (utterance "items with any letter before leftquoatation ! rightquoatation .") (targetValue (name "?{or(<low>,<cap>),<!>}")))
(example (utterance "lines with a capital letter , number , or character") (targetValue (name "?{or(or(<cap>,<num>),<any>),<!>}")))
(example (utterance "items with an upper case letter preceding leftquoatation ! rightquoatation or leftquoatation @ rightquoatation at least 1 time .") (targetValue (name "?{concat(<cap>,<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines that have 3 or more words that end in a letter") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines that have a lower-case letter or number followed by a character") (targetValue (name "?{or(<low>,<num>),<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceded by a letter followed by a lowercase lettere") (targetValue (name "?{<let>,concat(<low>,<!>)}")))
(example (utterance "lines not starting with a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{and(startwith(<let>),<!>)}")))
(example (utterance "lines ending with containing a letter at least once in it") (targetValue (name "?{endwith(contain(<let>))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation at least once before number") (targetValue (name "?{repeatatleast(<!>,1),<num>}")))
(example (utterance "7 or more lines ending with a number") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines with letters and a character before leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<any>),<!>}")))
(example (utterance "lines without a capital letter , a character , or a number") (targetValue (name "?{or(or(<cap>,<any>),<num>)}")))
(example (utterance "lines with a vowel , capital letter , or string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<cap>),<!>),<@>}")))
(example (utterance "lines ending in a character before a letter") (targetValue (name "?{or(concat(<any>,<cap>),<let>)}")))
(example (utterance "lines stating with a letter") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines with a lower-case letter before letter then character") (targetValue (name "?{concat(<low>,<let>),<any>}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{star(<!>),contain(<vow>)}")))
(example (utterance "lines that contain a character followed by a number before a lower-case letter") (targetValue (name "?{concat(<any>,<num>),<low>}")))
(example (utterance "lines with a lower case letter or character") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines that end with a number at least once") (targetValue (name "?{repeatatleast(<num>,1)}")))
(example (utterance "lines starting in 4 or more of a letter or character") (targetValue (name "?{or(<let>,repeatatleast(<any>,4))}")))
(example (utterance "lines with the string 'dog or 7 or more of a vowel , lower-case letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<!>,or(or(<vow>,<low>),<!>))}")))
(example (utterance "items with words containing a character preceding a numeral .") (targetValue (name "?{<any>,<num>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation and number") (targetValue (name "?{contain(<!>),and(<@>,<num>)}")))
(example (utterance "lines not having character , a capital letter , and not a vowel") (targetValue (name "?{and(and(<any>,<cap>),<vow>)}")))
(example (utterance "lines not ending with lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(endwith(<low>),<!>)}")))
(example (utterance "line without a vowel") (targetValue (name "?{star(<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character that ends with a capital letter") (targetValue (name "?{concat(<!>,<any>),endwith(<cap>)}")))
(example (utterance "lines which are having either a vowel , a lower-case letter , or a number before leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<low>),<num>),<!>}")))
(example (utterance "lines with lower case letters and a vowel before a letter") (targetValue (name "?{and(<low>,<vow>),<let>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),<vow>}")))
(example (utterance "lines with words with zero or more of leftquoatation ! rightquoatation and character") (targetValue (name "?{<!>,star(<any>)}")))
(example (utterance "word lines which contain any 1 of 3 elements , which are leftquoatation ! rightquoatation , a number or an upper-case letter") (targetValue (name "?{or(<!>,<cap>),<num>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation and vowel or character") (targetValue (name "?{or(and(<!>,<vow>),<any>)}")))
(example (utterance "lines with letter at least once before string leftquoatation ! rightquoatation followed by character") (targetValue (name "?{repeatatleast(<let>,1),concat(<!>,<any>)}")))
(example (utterance "lines with a capital letter followed by at least 1 vowel") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines with a character , string leftquoatation ! rightquoatation , or capital letter before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<any>,<!>),<cap>),<@>}")))
(example (utterance "lines with at least capital letter followed by letter") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines with 'dog ; 7 times") (targetValue (name "?{repeatatleast(<!>,7)}")))
(example (utterance "lines with lower-case letter before capital letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,concat(<cap>,<!>)}")))
(example (utterance "lines with words with zero or more lower-case letters") (targetValue (name "?{star(<low>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation") (targetValue (name "?{<num>,and(<cap>,<!>)}")))
(example (utterance "lines with a capital vowel leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,and(and(<vow>,<!>),<let>)}")))
(example (utterance "lines with a lower-case letter followed by a capital letter at least zero times") (targetValue (name "?{contain(concat(<low>,<cap>))}")))
(example (utterance "items with words containing a letter and vowel .") (targetValue (name "?{and(<vow>,<let>),<cap>}")))
(example (utterance "lines with a lower-case letter before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or letter") (targetValue (name "?{<low>,or(or(<!>,<@>),<let>)}")))
(example (utterance "lines with vowel , 5 or more times before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<vow>,5),<!>}")))
(example (utterance "before 'dog , the lines must have a number , vowel , or letter first .") (targetValue (name "?{or(or(<num>,<vow>),<let>),<!>}")))
(example (utterance "items with any letter , at least6times .") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines that have a minimum of 1 lower-case letter followed by a character") (targetValue (name "?{<low>,repeatatleast(<any>,1)}")))
(example (utterance "lines with zero or more numbers or leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines with a capital letter either leftquoatation ! rightquoatation , a number , or leftquoatation @ rightquoatation") (targetValue (name "?{<cap>,or(or(<!>,<num>),<@>)}")))
(example (utterance "lines with words starting with a character and a letter") (targetValue (name "?{startwith(<any>),<let>}")))
(example (utterance "lines containing number , string leftquoatation ! rightquoatation , and character") (targetValue (name "?{and(and(<num>,<!>),<any>)}")))
(example (utterance "items with a letter at least 4 times .") (targetValue (name "?{repeatatleast(<let>,4)}")))
(example (utterance "lines with 7 lower-case letters before a letter .") (targetValue (name "?{repeatatleast(<low>,7),<let>}")))
(example (utterance "lines with a vowel , string leftquoatation ! rightquoatation , and letter before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<vow>,<!>),<let>),<@>}")))
(example (utterance "lines with only 5 or more capital letters") (targetValue (name "?{repeatatleast(<cap>,5)}")))
(example (utterance "lines with words and 2 or more numbers") (targetValue (name "?{contain(<num>)}")))
(example (utterance "lines containing only a lower-case letter before a character") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "2 or more lines starting with a letter") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "items without a character , a vowel , or leftquoatation ! rightquoatation .") (targetValue (name "?{or(or(<!>,<vow>),<any>)}")))
(example (utterance "lines stating in a vowel before a capital letter") (targetValue (name "?{startwith(<vow>),<cap>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation after a vowel") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "items beginnig with an upper case letter preceding a character .") (targetValue (name "?{startwith(<cap>),<any>}")))
(example (utterance "lines not having vowel , not a lower-case letter , and not a number") (targetValue (name "?{and(and(<vow>,<low>),<num>)}")))
(example (utterance "lines that conclude with a letter contained in them") (targetValue (name "?{contain(<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation or an uppercase letter preceding a numeral .") (targetValue (name "?{and(<!>,<cap>),<num>}")))
(example (utterance "items with a vowel , a small letter , a letter , and a vowel preceding leftquoatation ! rightquoatation .") (targetValue (name "?{and(and(<let>,<vow>),<low>),<!>}")))
(example (utterance "items with an upper case letter , a letter , and a numeral .") (targetValue (name "?{and(and(<let>,<cap>),<num>)}")))
(example (utterance "lines without a vowel , a character , or a number") (targetValue (name "?{and(and(<vow>,<any>),<num>)}")))
(example (utterance "lines with words that don't have vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{not(<vow>),<!>}")))
(example (utterance "lines containing a capital letter or a number at least 7 times") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines which are ending with words with starting with a characte") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines with words and a number , vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<vow>),<!>}")))
(example (utterance "lines with a lower-case letter , a character , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<!>,<low>),<any>}")))
(example (utterance "lines without a character followed by a lower-case letter") (targetValue (name "?{not(concat(<any>,<low>))}")))
(example (utterance "lines having either lower-case letter , capital letter , or string leftquoatation ! rightquoatation before vowel") (targetValue (name "?{or(or(<low>,<cap>),<!>),<vow>}")))
(example (utterance "items with an upper case letter , a character , a small letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{and(and(<cap>,<any>),<low>),<!>}")))
(example (utterance "lines with at least 7 occurrences of a character preceded by a capital letter") (targetValue (name "?{<cap>,repeatatleast(<any>,7)}")))
(example (utterance "lines containing a character and leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<!>)}")))
(example (utterance "lines with words with zero or more of a string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a number") (targetValue (name "?{and(and(<!>,<num>),<@>)}")))
(example (utterance "lines containing words with , number , character and letter") (targetValue (name "?{and(<num>,<any>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation before a letter") (targetValue (name "?{concat(<!>,<@>),<let>}")))
(example (utterance "lines with a capital letter then a number and no leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<num>),not(<!>)}")))
(example (utterance "lines ending with a letter at least 3 times") (targetValue (name "?{or(<let>,repeatatleast(<cap>,3))}")))
(example (utterance "lines without leftquoatation ! rightquoatation before a number .") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "items with a small letter or character preceding leftquoatation ! rightquoatation .") (targetValue (name "?{or(<any>,<low>),<!>}")))
(example (utterance "lined ending with numbers and leftquoatation ! rightquoatation with leftquoatation @ rightquoatation after") (targetValue (name "?{endwith(<num>),concat(<!>,<@>)}")))
(example (utterance "lines with words , a number , a letter , or character .") (targetValue (name "?{or(<num>,<let>),<any>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),endwith(<@>)}")))
(example (utterance "lines containing no letters or more than 1 letter") (targetValue (name "?{<let>}")))
(example (utterance "lines starting with a lower-case letter or string leftquoatation ! rightquoatation , 4 or more times") (targetValue (name "?{or(startwith(<low>),<!>)}")))
(example (utterance "lines with words and a vowel or letter before a lower-case letter") (targetValue (name "?{or(<vow>,<let>),<low>}")))
(example (utterance "lines with lower-case letter , 7 or more times before character") (targetValue (name "?{repeatatleast(<low>,7),<any>}")))
(example (utterance "lines ending in a lower-case letter or starting in capital letter") (targetValue (name "?{or(<low>,startwith(<cap>))}")))
(example (utterance "lines that contain a lower-case letter and leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{and(<low>,<!>),<let>}")))
(example (utterance "lines containing a capital or a vowel , 4 or more times") (targetValue (name "?{or(contain(<cap>),<vow>)}")))
(example (utterance "lines with words without a vowel") (targetValue (name "?{not(<vow>)}")))
(example (utterance "lines with a letter before 7 or more of leftquoatation ! rightquoatation") (targetValue (name "?{<let>,repeatatleast(<!>,7)}")))
(example (utterance "lines that have words with a letter , a lower-case letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<low>),<!>}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation or vowel") (targetValue (name "?{<cap>,or(<!>,<vow>)}")))
(example (utterance "lines with lower-case letter , 5 or more times and leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,5),contain(<!>)}")))
(example (utterance "if the line starts with a vowel before leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<vow>),<!>}")))
(example (utterance "lines with words which contain leftquoatation ! rightquoatation , a capital letter , or a character") (targetValue (name "?{or(<!>,<cap>),<any>}")))
(example (utterance "lines with letter followed by vowel") (targetValue (name "?{concat(<cap>,<let>),<vow>}")))
(example (utterance "lines containing either a character , a letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<let>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a lower-case letter before a capital letter in them") (targetValue (name "?{concat(<!>,<low>),<cap>}")))
(example (utterance "lines not having a number , not a letter , and not leftquoatation ! rightquoatation in it") (targetValue (name "?{and(and(<num>,<let>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a vowel or a letter , then by a capital letter") (targetValue (name "?{concat(<!>,<vow>),concat(<let>,<cap>)}")))
(example (utterance "items with a character following a numeral .") (targetValue (name "?{<num>,<any>}")))
(example (utterance "lines starting with a lower-case letter and letter") (targetValue (name "?{startwith(<low>),contain(<let>)}")))
(example (utterance "leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{startwith(<!>),<low>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation followed by a capital letter , 5 or more times") (targetValue (name "?{repeatatleast(concat(<!>,<cap>),5)}")))
(example (utterance "lines that do no end with a capital letter") (targetValue (name "?{contain(star(<cap>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter , a capital , or a vowel") (targetValue (name "?{<!>,or(or(<let>,<cap>),<vow>)}")))
(example (utterance "lines without a number , leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{and(and(<num>,<!>),<vow>)}")))
(example (utterance "items with a numeral preceding a vowel preceding a character .") (targetValue (name "?{concat(<num>,<vow>),<any>}")))
(example (utterance "lines with letter before string leftquoatation ! rightquoatation and ending with string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<let>,<!>),endwith(<@>)}")))
(example (utterance "lines that have words with a lower-case letter and the word leftquoatation ! rightquoatation followed by the word leftquoatation @ rightquoatation") (targetValue (name "?{<low>,concat(<!>,<@>)}")))
(example (utterance "lines with words with lower-case letter , zero or more times") (targetValue (name "?{<low>}")))
(example (utterance "lines with less than 3 of a letter before a lower-case letter") (targetValue (name "?{not(<let>),repeatatleast(<low>,3)}")))
(example (utterance "lines not having a number or a lower-case letter") (targetValue (name "?{and(<num>,<low>)}")))
(example (utterance "lines with a letter before capital letter and vowel") (targetValue (name "?{<let>,and(<cap>,<vow>)}")))
(example (utterance "lines with 5 or more of a letter or number") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines beginning with either an uppercase letter or character") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines containing a vowel before a character , 2 or more times") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines which are having either a capital letter , a letter , or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<cap>,<let>),<!>),<@>}")))
(example (utterance "items with a character or a vowel preceding a letter .") (targetValue (name "?{or(<vow>,<any>),<let>}")))
(example (utterance "lines finishing with words with a lower-case letter") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines with words and 2 or more vowels") (targetValue (name "?{repeatatleast(<vow>,2)}")))
(example (utterance "lines with words with letter followed by capital letter") (targetValue (name "?{<let>,<cap>}")))
(example (utterance "lines with a number or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<num>,<!>),<@>),<let>}")))
(example (utterance "lines with words starting with leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{startwith(<!>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a character , zero or more times in them") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines not having vowel followed by character") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines containing no letter or vowel .") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceded by a capital letter which is preceded by a lower-case letter") (targetValue (name "?{concat(<low>,<cap>),<!>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation or a letter , or contain leftquoatation @ rightquoatation zero or more times") (targetValue (name "?{star(<@>),or(<!>,<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation at least5times preceding a small letter .") (targetValue (name "?{repeatatleast(<!>,5),<low>}")))
(example (utterance "lines with words with zero or more of a vowel and lower-case letter") (targetValue (name "?{and(<vow>,<low>)}")))
(example (utterance "lines that have a capital letter before leftquoatation ! rightquoatation and end with a lower-case letter") (targetValue (name "?{concat(<cap>,<!>),endwith(<low>)}")))
(example (utterance "items with an upper case letter and a numeral preceding a small letter .") (targetValue (name "?{and(<num>,<cap>),<low>}")))
(example (utterance "lines containing a number after a vowel 5 or more times") (targetValue (name "?{<vow>,repeatatleast(<num>,5)}")))
(example (utterance "lines having a number before a character and leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<num>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a capital letter , and a character before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<cap>),<any>),<@>}")))
(example (utterance "lines with a number and letter before a capital letter") (targetValue (name "?{and(<num>,<let>),<cap>}")))
(example (utterance "lines that start with words that have a letter , a lower-case letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<low>),<!>)}")))
(example (utterance "lines ending in a lower case letter .") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines with a lower-case letter after a capital letter") (targetValue (name "?{<cap>,<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or lower-case letter before a vowel") (targetValue (name "?{or(or(<!>,<low>),<@>),<vow>}")))
(example (utterance "lines with words that contain a letter , lower-case letter , and a number") (targetValue (name "?{and(<let>,<low>),<num>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation and not a vowel or capital letter") (targetValue (name "?{or(and(<!>,<vow>),<cap>)}")))
(example (utterance "lines containing vowel at least once before lower-case letter") (targetValue (name "?{repeatatleast(contain(<vow>),1),<low>}")))
(example (utterance "items with leftquoatation ! rightquoatation and a character preceding a small letter .") (targetValue (name "?{and(<!>,<any>),<low>}")))
(example (utterance "lines with the ending leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{endwith(or(<!>,<vow>))}")))
(example (utterance "lines having either string leftquoatation ! rightquoatation , number , or character before a capital") (targetValue (name "?{or(or(<!>,<num>),<any>),<cap>}")))
(example (utterance "had leftquoatation ! rightquoatation 7 or or more times") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines without only 4 or more capital letters") (targetValue (name "?{repeatatleast(<cap>,4)}")))
(example (utterance "lines with a small letters 4 or more times") (targetValue (name "?{<low>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines that have a number or 0 or more letters") (targetValue (name "?{star(or(<num>,<let>))}")))
(example (utterance "items with a numeral preceding a letter at least 7 times .") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation then a letter") (targetValue (name "?{<vow>,concat(<!>,<let>)}")))
(example (utterance "lines with words with zero or more of string leftquoatation ! rightquoatation with leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),<@>}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation then string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<num>,<!>),contain(<@>)}")))
(example (utterance "lines without any letters") (targetValue (name "?{<let>}")))
(example (utterance "lines starting with a character preceding a letter") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a number") (targetValue (name "?{and(and(<!>,<@>),<num>)}")))
(example (utterance "lines containing a character preceded by a lowercase letter") (targetValue (name "?{<low>,<any>}")))
(example (utterance "lines without a number before a vowel") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines without vowels preceding letters") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a letter with a vowel after") (targetValue (name "?{or(<!>,<vow>),<let>}")))
(example (utterance "lines with a vowel or leftquoatation ! rightquoatation before a number") (targetValue (name "?{or(<vow>,<!>),<num>}")))
(example (utterance "lines with a number followed by a letter , 4 or more times in them") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines that end with a letter or number") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines with 4 or more vowels before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<vow>,4),<!>}")))
(example (utterance "lines without a capital letter with lower-case or string leftquoatation ! rightquoatation") (targetValue (name "?{not(<cap>),or(<low>,<!>)}")))
(example (utterance "lines not having a vowel ending with leftquoatation ! rightquoatation") (targetValue (name "?{not(<vow>),endwith(<!>)}")))
(example (utterance "lines with a letter after leftquoatation ! rightquoatation at least once") (targetValue (name "?{repeatatleast(<!>,1),<let>}")))
(example (utterance "lines that have a letter come before a leftquoatation ! rightquoatation string at least 3 times") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before not only a number") (targetValue (name "?{<!>,not(<num>)}")))
(example (utterance "lines with zero or more of a letter and string leftquoatation ! rightquoatation before number") (targetValue (name "?{and(<let>,<!>),star(<num>)}")))
(example (utterance "lines having character , letter , and number before vowel") (targetValue (name "?{and(and(<any>,<let>),<num>),<vow>}")))
(example (utterance "lines with a vowel or without a capital letter") (targetValue (name "?{or(<vow>,not(<cap>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a capital letter , 4 or more times in it") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines with a capital before string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , and string leftquoatation # rightquoatation") (targetValue (name "?{<cap>,and(and(<!>,<@>),<#>)}")))
(example (utterance "lines starting with words with string leftquoatation ! rightquoatation before a character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines not words with string leftquoatation ! rightquoatation at least once") (targetValue (name "?{repeatatleast(<!>,1)}")))
(example (utterance "lines with a character , 4 or more times or string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , and vowel") (targetValue (name "?{repeatatleast(<any>,4),and(and(<!>,<@>),<vow>)}")))
(example (utterance "lines that do not have words ending in a capital letter") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines with 2 words that don't have a letter") (targetValue (name "?{repeatatleast(<let>,2)}")))
(example (utterance "lines which are not having a vowel , leftquoatation ! rightquoatation , or a letter") (targetValue (name "?{or(or(<vow>,<!>),<let>)}")))
(example (utterance "lines containing containing only a letter occurring 2 or more times") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{<cap>,or(<!>,<low>)}")))
(example (utterance "lines with 2 or more of leftquoatation ! rightquoatation") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines with a lower-case letter before a vowel , string leftquoatation ! rightquoatation , or capital letter") (targetValue (name "?{<low>,or(or(<vow>,<!>),<cap>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation before character") (targetValue (name "?{<!>,endwith(<any>)}")))
(example (utterance "lines with zero or more of a capital letter and a number or string leftquoatation ! rightquoatation") (targetValue (name "?{star(<cap>),or(<num>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a number and string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),and(and(<num>,<@>),<@>)}")))
(example (utterance "lines that do not have a capital letter or a lower-case letter") (targetValue (name "?{and(<cap>,<low>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation at least once ending with any letter") (targetValue (name "?{repeatatleast(<!>,1),endwith(<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{not(and(and(<!>,<@>),<#>))}")))
(example (utterance "lines with 6 or more of a capital letter or vowel") (targetValue (name "?{or(contain(<cap>),<vow>)}")))
(example (utterance "lines that have words with a capital letter , lower case letter and a character .") (targetValue (name "?{and(<low>,<cap>),<any>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before containing number") (targetValue (name "?{<!>,contain(<num>)}")))
(example (utterance "lines containing words including a character , leftquoatation ! rightquoatation , and a capital letter") (targetValue (name "?{and(and(<any>,<!>),<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a numeral at least 7 times .") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or letter") (targetValue (name "?{not(and(<!>,<let>))}")))
(example (utterance "lines that don't have any numbers , letters or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<!>,<num>),<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a letter") (targetValue (name "?{or(or(<!>,<let>),<cap>)}")))
(example (utterance "lines containing either a character , a letter , or a capital letter") (targetValue (name "?{or(<let>,<any>),<cap>}")))
(example (utterance "lines without a letter before a capital letter") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or a character") (targetValue (name "?{or(concat(<!>,<@>),<any>)}")))
(example (utterance "lines without a letter") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter , number , or vowel") (targetValue (name "?{contain(<!>),or(or(<let>,<num>),<vow>)}")))
(example (utterance "lines with a capital or lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{contain(<cap>),concat(<low>,<!>)}")))
(example (utterance "lines ending with a number before a character at least of 1 time") (targetValue (name "?{endwith(<num>),repeatatleast(<any>,1)}")))
(example (utterance "lines that have words with either a lower-case or capital letter or a vowel") (targetValue (name "?{or(<vow>,<low>),<cap>}")))
(example (utterance "items without a small letter , a vowel , or leftquoatation ! rightquoatation .") (targetValue (name "?{and(and(<vow>,<low>),<!>)}")))
(example (utterance "lines that have a character followed by a number then a letter") (targetValue (name "?{concat(<any>,<let>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , vowel , or lower-case letter") (targetValue (name "?{or(or(<!>,<vow>),<low>)}")))
(example (utterance "lines containing a number or with at least 1 capital letter") (targetValue (name "?{repeatatleast(<cap>,1),contain(<num>)}")))
(example (utterance "lines with number , zero or more times or containing string leftquoatation ! rightquoatation") (targetValue (name "?{star(<num>),contain(<!>)}")))
(example (utterance "lines with words that contain only leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{<!>,<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 3 or more times before a letter in them") (targetValue (name "?{repeatatleast(<!>,3),<let>}")))
(example (utterance "lines with a number after leftquoatation ! rightquoatation") (targetValue (name "?{<!>,<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a number or leftquoatation @ rightquoatation before a letter") (targetValue (name "?{or(or(<!>,<num>),<@>),<let>}")))
(example (utterance "lines that contain a letter followed by anything other than a lower-case letter") (targetValue (name "?{<let>,not(<low>)}")))
(example (utterance "lines with a vowel before capital letter") (targetValue (name "?{contain(concat(<vow>,<cap>))}")))
(example (utterance "lines containing a character , a vowel , and a letter") (targetValue (name "?{and(<any>,<let>),<vow>}")))
(example (utterance "items with either leftquoatation ! rightquoatation , a numeral , or leftquoatation @ rightquoatation in front of a character .") (targetValue (name "?{or(or(<@>,<num>),<!>),<any>}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation or a letter , 7 times or more") (targetValue (name "?{repeatatleast(or(<!>,<let>),7)}")))
(example (utterance "lines appearing 2 or more times without a vowel or a capital letter") (targetValue (name "?{or(<vow>,repeatatleast(<cap>,2))}")))
(example (utterance "lines with a number") (targetValue (name "?{concat(<num>,<cap>),or(or(<low>,<!>),<@>)}")))
(example (utterance "lines that begin with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation at least6times") (targetValue (name "?{startwith(<@>),repeatatleast(<!>,6)}")))
(example (utterance "start lines with a letter than the string' dog '") (targetValue (name "?{<let>,<!>}")))
(example (utterance "lines with a character followed by a vowel") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "lines with a number before 5 of leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a lower-case letter , 6 or more times in them") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines with words without a character") (targetValue (name "?{not(<any>)}")))
(example (utterance "lines with words with a vowel or a number , 2 or more times iin it") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with a number after a capital letter at least 5 times") (targetValue (name "?{repeatatleast(<cap>,5),<num>}")))
(example (utterance "lines containing words with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation or a number") (targetValue (name "?{<!>,or(<@>,<num>)}")))
(example (utterance "lines with words with starting with a number before a letter in them") (targetValue (name "?{startwith(<num>),<let>}")))
(example (utterance "lines starting with a capital or lower-case letter") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines not starting with a letter at least once in them") (targetValue (name "?{repeatatleast(startwith(<let>),1)}")))
(example (utterance "lines with a letter before a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<cap>),repeatatleast(<!>,1)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a letter 4 or more times") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines that finish with leftquoatation ! rightquoatation and contain leftquoatation @ rightquoatation preceding a capital letter") (targetValue (name "?{concat(<@>,<cap>),endwith(<!>)}")))
(example (utterance "lines containing 4 or more words leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{contain(<!>),repeatatleast(<low>,4)}")))
(example (utterance "lines starting in a number before vowel") (targetValue (name "?{startwith(concat(<num>,<vow>))}")))
(example (utterance "lines without a capital letter or vowel") (targetValue (name "?{contain(and(<cap>,<vow>))}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a terminating upper case letter .") (targetValue (name "?{<!>,endwith(<cap>)}")))
(example (utterance "lines ending in zero or more of a letter and string leftquoatation ! rightquoatation") (targetValue (name "?{or(star(<let>),<!>)}")))
(example (utterance "lines starting with words and leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with a capital letter before a vowel .") (targetValue (name "?{<cap>,<vow>}")))
(example (utterance "lines with words that contain a vowel , zero or more times in them") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "items with a letter preceding leftquoatation ! rightquoatation , an upper case letter , and a numeral .") (targetValue (name "?{<let>,and(and(<!>,<num>),<cap>)}")))
(example (utterance "lines with 2 or more words containing character") (targetValue (name "?{<any>}")))
(example (utterance "lines ending with a lower-case letter followed by leftquoatation ! rightquoatation at least once in them") (targetValue (name "?{repeatatleast(concat(<low>,<!>),1)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or number before vowel") (targetValue (name "?{or(<!>,<num>),<vow>}")))
(example (utterance "lines that don't have letters") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines that have 3 things in them:  a letter , leftquoatation ! rightquoatation , and a vowel .") (targetValue (name "?{and(and(<let>,<!>),<vow>)}")))
(example (utterance "lines with words with a number before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines that must not contain a character , letter , or the word leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<let>),<!>)}")))
(example (utterance "lines wherein 4 or more times there are words with a character") (targetValue (name "?{<any>}")))
(example (utterance "lines containing words only leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,<@>}")))
(example (utterance "lines with a lower-case letter before either string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , or number") (targetValue (name "?{<low>,or(or(<!>,<@>),<num>)}")))
(example (utterance "lines where a lower-case letter appears before a vowel") (targetValue (name "?{<low>,star(<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a small letter preceding a number .") (targetValue (name "?{<!>,concat(<low>,<num>)}")))
(example (utterance "lines with zero or more of a lower-case letter or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{contain(or(or(<low>,<!>),<@>))}")))
(example (utterance "items with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation preceding a letter .") (targetValue (name "?{and(<@>,<!>),<let>}")))
(example (utterance "lines that do not contain a character and a number followed by a lower-case letter") (targetValue (name "?{not(<any>),concat(<num>,<low>)}")))
(example (utterance "lower-case letter before a letter followed by a vowel") (targetValue (name "?{concat(<low>,<let>),<vow>}")))
(example (utterance "items with an upper case letter , a vowel , and 'dog") (targetValue (name "?{and(and(<vow>,<cap>),<!>),<num>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a number and a vowel") (targetValue (name "?{<!>,and(<num>,<vow>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or starting in string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,startwith(<@>)}")))
(example (utterance "lines containing either string leftquoatation ! rightquoatation , number , or string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<num>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or number") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines with 5 or more of a character before a letter") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines with a lower case letter followed by leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{contain(<low>),or(<!>,<@>)}")))
(example (utterance "lines ending in and with only leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines without zero or more of a character before a letter") (targetValue (name "?{star(concat(<any>,<let>))}")))
(example (utterance "lines containing a letter followed by a character 1 or more times") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with a letter , string leftquoatation ! rightquoatation , and capital letter") (targetValue (name "?{and(and(<let>,<!>),<cap>)}")))
(example (utterance "lines containing a character after a capital letter at least 5 times") (targetValue (name "?{repeatatleast(<cap>,5),<any>}")))
(example (utterance "lines having a number , a vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<vow>),<!>)}")))
(example (utterance "lines not starting with string leftquoatation ! rightquoatation or a character") (targetValue (name "?{startwith(or(<!>,<any>))}")))
(example (utterance "lines with words with a lower-case and capital letter") (targetValue (name "?{and(<low>,<cap>)}")))
(example (utterance "lines beginning with a character or lower-case letter") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "lines with 5 letters before a character") (targetValue (name "?{repeatatleast(<let>,5),<any>}")))
(example (utterance "lines with words with a number or letter") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with a letter , vowel , and character before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<vow>),<any>),<!>}")))
(example (utterance "items with a letter preceding an upper case letter preceding a numeral .") (targetValue (name "?{<let>,concat(<cap>,<num>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a numeral , a vowel , or leftquoatation @ rightquoatation .") (targetValue (name "?{<!>,or(or(<vow>,<num>),<@>)}")))
(example (utterance "lines 7 or more of a number before vowel") (targetValue (name "?{contain(concat(<num>,<vow>))}")))
(example (utterance "items terminating with leftquoatation ! rightquoatation preceding a vowel .") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines having a capital letter after a character , a letter , or a number") (targetValue (name "?{or(or(<any>,<let>),<num>),<cap>}")))
(example (utterance "items with a letter or leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{or(<let>,<!>),<@>}")))
(example (utterance "lines containing a letter and leftquoatation ! rightquoatation before leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<let>,<cap>),<!>),<@>}")))
(example (utterance "lines with 7 or more of strings leftquoatation ! rightquoatation before a number") (targetValue (name "?{repeatatleast(<!>,7),<num>}")))
(example (utterance "lines with capital before a character and string leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,and(<any>,<!>)}")))
(example (utterance "lines that contain a lower-case letter followed by a character and a vowel") (targetValue (name "?{<low>,concat(<any>,<vow>)}")))
(example (utterance "lines having a letter before either a number , a lower-case letter , or a capital letter") (targetValue (name "?{<let>,or(or(<num>,<low>),<cap>)}")))
(example (utterance "lines containing words that end with a character 1 or more times") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines end with leftquoatation ! rightquoatation but not leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),not(<@>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation followed by number , 3 or more times") (targetValue (name "?{or(<!>,concat(<@>,<num>))}")))
(example (utterance "lines with words with a letter , string leftquoatation ! rightquoatation , or number") (targetValue (name "?{or(<let>,<!>),<num>}")))
(example (utterance "lines that must not have a character or a letter") (targetValue (name "?{and(<any>,<let>)}")))
(example (utterance "lines with words which are ending with leftquoatation ! rightquoatation and a character") (targetValue (name "?{endwith(<!>),<any>}")))
(example (utterance "lines with words with a letter or leftquoatation ! rightquoatation , 6 or more times in them") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines ending with containing a number , 3 or more times in them") (targetValue (name "?{repeatatleast(contain(<num>),3)}")))
(example (utterance "the lines that have either a capital letter or lower-case letter at least once") (targetValue (name "?{star(or(<let>,<low>))}")))
(example (utterance "lines with a capital letter , leftquoatation ! rightquoatation , and character before a number") (targetValue (name "?{and(and(<cap>,<!>),<any>),<num>}")))
(example (utterance "lines that must not have the word leftquoatation ! rightquoatation or a letter or vowel") (targetValue (name "?{or(and(<!>,<let>),<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a letter .") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines that contain a capital letter followed by a vowel or a lower-case letter") (targetValue (name "?{<cap>,or(<vow>,<low>)}")))
(example (utterance "lines not having vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines that ends with a vowel followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(endwith(<vow>),<!>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation or vowel at least once") (targetValue (name "?{not(<!>),repeatatleast(<vow>,1)}")))
(example (utterance "the lines that do not have a number repeated 6 or more times") (targetValue (name "?{repeatatleast(<num>,6)}")))
(example (utterance "lines having a character , lower-case letter , and string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<any>,<low>),<!>),<@>}")))
(example (utterance "lines with the word 'dogleftquoatation  , plus a number and a letter") (targetValue (name "?{and(<!>,<let>),<num>}")))
(example (utterance "lines without zero or more of a number and letter") (targetValue (name "?{not(<num>),star(<let>)}")))
(example (utterance "lines with a lower-case letter and leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<low>,<!>),<@>)}")))
(example (utterance "lines which are not having a lower-case letter , leftquoatation ! rightquoatation , or a vowel") (targetValue (name "?{or(or(<low>,<!>),<vow>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation before vowel at least once") (targetValue (name "?{startwith(<!>),repeatatleast(<vow>,1)}")))
(example (utterance "lines with a lower-case letter before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{<low>,or(or(<!>,<@>),<vow>)}")))
(example (utterance "lines starting with words contain only a vowel") (targetValue (name "?{<vow>}")))
(example (utterance "items with words contating leftquoatation ! rightquoatation , a vowel , or leftquoatation @ rightquoatation at least 1 time .") (targetValue (name "?{or(or(<vow>,<!>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter , number , and character") (targetValue (name "?{<!>,and(and(<cap>,<num>),<any>)}")))
(example (utterance "lines with 7 letters before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,7),<!>}")))
(example (utterance "items with an upper case letter , a character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<!>),<cap>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , 7 or more times or a vowel in them") (targetValue (name "?{repeatatleast(<!>,7),<vow>}")))
(example (utterance "lines with character and string leftquoatation ! rightquoatation before vowel") (targetValue (name "?{and(<any>,<!>),<vow>}")))
(example (utterance "lines with lower-case letter , string leftquoatation ! rightquoatation , and capital letter before a number") (targetValue (name "?{and(and(<low>,<!>),<cap>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and capital letter") (targetValue (name "?{and(and(<!>,<let>),<cap>)}")))
(example (utterance "lines with words without a number before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines with vowel before a capital and a lower-case letter") (targetValue (name "?{<vow>,and(<cap>,<low>)}")))
(example (utterance "lines containing words having leftquoatation ! rightquoatation , leftquoatation @ rightquoatation and a character") (targetValue (name "?{and(<!>,<@>),<any>}")))
(example (utterance "items with a small letter or vowel preceding an upper case letter .") (targetValue (name "?{or(<low>,<vow>),<cap>}")))
(example (utterance "lines with 4 or more of string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(endwith(<!>),4),<@>}")))
(example (utterance "lines with less than 5 of leftquoatation ! rightquoatation or with string leftquoatation @ rightquoatation") (targetValue (name "?{or(repeatatleast(<!>,5),<@>)}")))
(example (utterance "lines not starting in a number") (targetValue (name "?{startwith(repeatatleast(<num>,1))}")))
(example (utterance "line with ha number or character preceding a letter") (targetValue (name "?{or(<num>,<any>),<let>}")))
(example (utterance "lines with words with only a number before string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,<!>}")))
(example (utterance "lines that don't leftquoatation ! rightquoatation followed by a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with a capital before leftquoatation ! rightquoatation zero times") (targetValue (name "?{<cap>,star(contain(<!>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a character , 3 or more times in them") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "items with words containing an upper case letter , a character , or a vowel .") (targetValue (name "?{or(<any>,<vow>),<cap>}")))
(example (utterance "lines with a character before not leftquoatation ! rightquoatation , 7 or more times in them") (targetValue (name "?{<any>,not(repeatatleast(<!>,7))}")))
(example (utterance "lines starting in words with a character , string leftquoatation ! rightquoatation , or vowel") (targetValue (name "?{or(or(<any>,<!>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter or without string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<let>),not(<@>)}")))
(example (utterance "lines ending with words or without a vowel .") (targetValue (name "?{star(<vow>)}")))
(example (utterance "lines without a number , a letter , and a lower-case letter") (targetValue (name "?{and(and(<num>,<let>),<low>)}")))
(example (utterance "lines with 5 or more of a number before vowel") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "items with a character preceding a numeral or a small letter .") (targetValue (name "?{concat(<any>,<low>),not(<num>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before either a number or a leftquoatation @ rightquoatation string at least twice") (targetValue (name "?{or(concat(<!>,<num>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , vowel , and number before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<!>,<vow>),<num>),<@>}")))
(example (utterance "lines with vowel followed by letter before character .") (targetValue (name "?{concat(<vow>,<let>),<any>}")))
(example (utterance "lines having lettered words , aq numerical and the link leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<num>),<!>}")))
(example (utterance "lines containing letter and not a lower-case letter") (targetValue (name "?{contain(<let>),not(<low>)}")))
(example (utterance "lines with lower-case letter followed by number before letter") (targetValue (name "?{concat(<low>,<num>),<let>}")))
(example (utterance "items with a vowel preceding leftquoatation ! rightquoatation preceding a numeral .") (targetValue (name "?{concat(<vow>,<!>),<num>}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation at least 6 times") (targetValue (name "?{<num>,repeatatleast(<!>,6)}")))
(example (utterance "items with a numeral preceding a letter preceding an upper case letter .") (targetValue (name "?{concat(<num>,<let>),<cap>}")))
(example (utterance "lines which are containing a lower-case letter or either leftquoatation ! rightquoatation , a number , or leftquoatation @ rightquoatation") (targetValue (name "?{contain(<low>),or(or(<!>,<num>),<@>)}")))
(example (utterance "lines with a character , number , and string leftquoatation ! rightquoatation before a letter") (targetValue (name "?{and(and(<any>,<num>),<!>),<let>}")))
(example (utterance "blines beginning with a number preceded by a vowel") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "items with leftquoatation ! rightquoatation at least5times preceding a numeral .") (targetValue (name "?{repeatatleast(<!>,5),<num>}")))
(example (utterance "lines with a number before 6 or more letters") (targetValue (name "?{<num>,repeatatleast(<let>,6)}")))
(example (utterance "items with a small letter preceding either leftquoatation ! rightquoatation , a character , or an upper case letter .") (targetValue (name "?{<low>,or(or(<cap>,<!>),<any>)}")))
(example (utterance "lines with a vowel or letter before a character") (targetValue (name "?{or(or(<vow>,<low>),<let>),<any>}")))
(example (utterance "lines which have an upper-case letter , the leftquoatation ! rightquoatation string and also a vowel") (targetValue (name "?{and(and(<cap>,<!>),<vow>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines which are starting with words with a capital letter and leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,<!>)}")))
(example (utterance "lines with a capital letter and string leftquoatation ! rightquoatation before a number") (targetValue (name "?{repeatatleast(<cap>,1),concat(<!>,<num>)}")))
(example (utterance "small letters followed by a number 3+ times before a capital") (targetValue (name "?{repeatatleast(concat(<low>,<num>),3),<cap>}")))
(example (utterance "lines not words with vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a capital letter , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<cap>),<@>)}")))
(example (utterance "lines that contain words that begin with a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<!>)}")))
(example (utterance "lines not containing letter , 5 or more times") (targetValue (name "?{contain(repeatatleast(<let>,5))}")))
(example (utterance "lines without a vowel ending in number") (targetValue (name "?{not(<vow>),endwith(<num>)}")))
(example (utterance "lines with vowel then a character and letter") (targetValue (name "?{<vow>,concat(<any>,<let>)}")))
(example (utterance "lines that have a number followed by a vowel , which is then followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<vow>),<!>}")))
(example (utterance "lines ending in 6 or more of a character or lower-case letter") (targetValue (name "?{endwith(or(<any>,<low>))}")))
(example (utterance "lines with characters or numbers before a capital letter") (targetValue (name "?{<cap>,or(<any>,<num>)}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation or non string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<any>,<!>),not(<@>)}")))
(example (utterance "lines ending in 5 or more of leftquoatation ! rightquoatation") (targetValue (name "?{endwith(repeatatleast(<!>,5))}")))
(example (utterance "lines with a capital followed by vowel before lower-case letter") (targetValue (name "?{concat(<cap>,<vow>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a lower-case letter , a character , or a vowel") (targetValue (name "?{or(or(<low>,<any>),<vow>),<!>}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation preceding a character and a capital letter") (targetValue (name "?{<!>,and(<any>,<cap>)}")))
(example (utterance "items with words containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a small letter .") (targetValue (name "?{or(or(<@>,<low>),<!>)}")))
(example (utterance "lines ending with a vowel 2 times") (targetValue (name "?{endwith(contain(<vow>))}")))
(example (utterance "lines do not contain a capital letter , leftquoatation ! rightquoatation , nor a letter") (targetValue (name "?{or(or(<cap>,<!>),<let>)}")))
(example (utterance "lines with a character before capital or lower-case letter") (targetValue (name "?{<any>,or(<cap>,<low>)}")))
(example (utterance "items with a small letter ending in a letter .") (targetValue (name "?{<low>,endwith(<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least 1 time before a character and vowel") (targetValue (name "?{repeatatleast(<!>,1),and(<any>,<vow>)}")))
(example (utterance "lines with a character and zero or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(and(<any>,<!>),<@>)}")))
(example (utterance "lines containing either string leftquoatation ! rightquoatation , capital letter , or lower-case letter") (targetValue (name "?{or(or(<!>,<cap>),<low>)}")))
(example (utterance "lines containing a lower-case letter , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(<low>,<!>),<any>}")))
(example (utterance "the lines that start with leftquoatation ! rightquoatation that comes before a number") (targetValue (name "?{startwith(<!>),contain(<num>)}")))
(example (utterance "lines containing the word dog , followed by a number and a capital letter") (targetValue (name "?{concat(<!>,<num>),<cap>}")))
(example (utterance "lines having leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a number prior to a vowel") (targetValue (name "?{and(and(<!>,<@>),<num>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a vowel before leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<@>),<vow>),<#>}")))
(example (utterance "lines with words with a letter and string leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{and(<let>,<!>),<cap>}")))
(example (utterance "lines with words with 3 or more of a character and lower-case letter") (targetValue (name "?{and(<any>,<low>)}")))
(example (utterance "lines ending in zero or more of and number") (targetValue (name "?{endwith(<!>),star(<num>)}")))
(example (utterance "lines with a character , the word leftquoatation ! rightquoatation , and a capital letter") (targetValue (name "?{and(and(<any>,<!>),<cap>)}")))
(example (utterance "line with a lower-case letter , 4 times before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,4),<!>}")))
(example (utterance "lines not containing a lower-case letter at least 4 times") (targetValue (name "?{repeatatleast(<low>,4)}")))
(example (utterance "lines without a letter before a number then string leftquoatation ! rightquoatation") (targetValue (name "?{not(<let>),concat(<num>,<!>)}")))
(example (utterance "lines with a lower-case letter followed by leftquoatation ! rightquoatation at least once before leftquoatation @ rightquoatation in them") (targetValue (name "?{repeatatleast(concat(<low>,<!>),1),<@>}")))
(example (utterance "lines containing words with a vowel or letter") (targetValue (name "?{<vow>,<let>}")))
(example (utterance "lines with a numberand  lower-case and capital letters") (targetValue (name "?{and(and(<num>,<low>),<cap>)}")))
(example (utterance "lines with a letter at least once and string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , and a lower-case letter") (targetValue (name "?{repeatatleast(<let>,1),and(and(<!>,<@>),<low>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation before lower-case letter , zero or more times") (targetValue (name "?{startwith(<!>),star(<low>)}")))
(example (utterance "lines containing words with leftquoatation ! rightquoatation , a number , and a lower-case letter") (targetValue (name "?{and(<!>,<num>),<low>}")))
(example (utterance "items with a vowel , leftquoatation ! rightquoatation , or an upper case letter preceding a character .") (targetValue (name "?{or(or(<!>,<vow>),<cap>),<any>}")))
(example (utterance "lines containing a capital letter and at least 1 lower-case letter after that") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines with words and a number , lower-case letter , and character") (targetValue (name "?{and(<num>,<low>),<any>}")))
(example (utterance "lines without leftquoatation ! rightquoatation before leftquoatation @ rightquoatation without string leftquoatation # rightquoatation") (targetValue (name "?{and(concat(<!>,<@>),<#>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least 2 times") (targetValue (name "?{repeatatleast(<!>,2)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter then string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<cap>),contain(<@>)}")))
(example (utterance "lines that end with a number after a character") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines in which a number comes prior to a lower-case letter , and followed by a letter") (targetValue (name "?{<num>,concat(<low>,<let>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation zero or more times followed by a character") (targetValue (name "?{star(concat(<!>,<any>))}")))
(example (utterance "lines starting with a capital letter with string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<cap>),or(<!>,<@>)}")))
(example (utterance "lines with words that start with the word leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),<@>}")))
(example (utterance "lines containing a vowel preceded by a lowercase letter and followed by a letter") (targetValue (name "?{concat(<vow>,<let>),<low>}")))
(example (utterance "lines ending in a vowel before a capital letter") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines with a letter after a capital letter .") (targetValue (name "?{<cap>,<let>}")))
(example (utterance "lines ending in a number 5 times or greater") (targetValue (name "?{repeatatleast(<num>,5)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character then leftquoatation @ rightquoatation") (targetValue (name "?{<!>,concat(<any>,<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter followed by a lower-case letter") (targetValue (name "?{<!>,concat(<cap>,<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation at least once or either a capital , number , or vowel") (targetValue (name "?{repeatatleast(<!>,1),or(or(<cap>,<num>),<vow>)}")))
(example (utterance "lines with a number followed by a letter 3 times") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines with 2 or more vowels before a letter") (targetValue (name "?{repeatatleast(<vow>,2),<let>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or capital letter before lower-case letter") (targetValue (name "?{or(<!>,<cap>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before either leftquoatation @ rightquoatation , leftquoatation # rightquoatation , or leftquoatation $ rightquoatation") (targetValue (name "?{<!>,or(or(<@>,<#>),<$>)}")))
(example (utterance "lines with words containing leftquoatation ! rightquoatation , 7 times") (targetValue (name "?{repeatatleast(<!>,7)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation and more than 2 times preceding lower-case letters .") (targetValue (name "?{repeatatleast(<!>,2),<low>}")))
(example (utterance "the leftquoatation ! rightquoatation is the best friend of a man") (targetValue (name "?{<any>,repeatatleast(<!>,1)}")))
(example (utterance "lines with words with a lower-case letter before letter") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "items beginning with a character preceding a letter .") (targetValue (name "?{startwith(<any>),<let>}")))
(example (utterance "lines that have a character prior to a letter with leftquoatation ! rightquoatation after") (targetValue (name "?{<any>,concat(<let>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number , a character and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<num>,<any>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a lower-case letter , or a capital letter before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<low>),<cap>),<@>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation at least 7 times before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(contain(<!>),7),<@>}")))
(example (utterance "lines containing a number or leftquoatation ! rightquoatation") (targetValue (name "?{<num>,contain(<!>)}")))
(example (utterance "lines not having number followed by letter and not a lower-case letter") (targetValue (name "?{and(concat(<num>,<let>),<low>)}")))
(example (utterance "lines with words with a vowel before a character or a number in them") (targetValue (name "?{concat(<vow>,<any>),<num>}")))
(example (utterance "lines without a vowel and a number") (targetValue (name "?{and(<vow>,<num>)}")))
(example (utterance "lines which are not having a character followed by a number , zero or more times") (targetValue (name "?{star(concat(<any>,<num>))}")))
(example (utterance "lines which have a capital letter before either leftquoatation ! rightquoatation , a letter , or leftquoatation @ rightquoatation") (targetValue (name "?{<cap>,or(or(<!>,<let>),<@>)}")))
(example (utterance "lines without a capital lower-case letter") (targetValue (name "?{or(not(<cap>),<low>)}")))
(example (utterance "a number does not appears at least once before a vowel in the lines") (targetValue (name "?{concat(<num>,repeatatleast(<vow>,1))}")))
(example (utterance "lines with 6 or more of only a vowel") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "lines do not start with a number nor leftquoatation ! rightquoatation") (targetValue (name "?{or(startwith(<num>),<!>)}")))
(example (utterance "lines with a letter after leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>),<let>}")))
(example (utterance "lines having either a capital , string leftquoatation ! rightquoatation , or number before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<cap>,<!>),<num>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),endwith(<@>)}")))
(example (utterance "lines that have a character followed by a letter , the word leftquoatation ! rightquoatation , and the word leftquoatation @ rightquoatation") (targetValue (name "?{<any>,and(and(<let>,<!>),<@>)}")))
(example (utterance "lines with a vowel without a capital letter") (targetValue (name "?{repeatatleast(<vow>,1),not(<cap>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and leftquoatation # rightquoatation") (targetValue (name "?{not(<!>),and(<@>,<#>)}")))
(example (utterance "lines with only leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines which are ending with a lower-case letter followed by leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(concat(<low>,<!>),<num>)}")))
(example (utterance "lines having a lower-case letter , vowel , and number before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<vow>),<num>),<!>}")))
(example (utterance "lines ending with a number or lower-case letter") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "items with a character at least 7 times preceding a number .") (targetValue (name "?{repeatatleast(<any>,7),<num>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation a lower-case letter , 5 or more times") (targetValue (name "?{concat(repeatatleast(<low>,5),<!>)}")))
(example (utterance "lines with lower-case letter before character , 7 or more times") (targetValue (name "?{<low>,repeatatleast(<any>,7)}")))
(example (utterance "lines with a vowel followed by leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a letter") (targetValue (name "?{<vow>,and(and(<!>,<@>),<let>)}")))
(example (utterance "lines where 7 or more times there is a vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines which contain 3 or more times a lowercase lettere") (targetValue (name "?{<low>}")))
(example (utterance "lines without a character before ending in vowel") (targetValue (name "?{concat(<any>,endwith(<vow>))}")))
(example (utterance "lines ending with a character followed by a letter") (targetValue (name "?{endwith(contain(<any>)),<let>}")))
(example (utterance "lines with words with a capital followed by character at least once") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{or(<!>,<@>),<vow>}")))
(example (utterance "items with words containing an upper case letter , a character , and leftquoatation ! rightquoatation .") (targetValue (name "?{and(<cap>,<!>),<any>}")))
(example (utterance "lines with words which are  either leftquoatation ! rightquoatation , a lower-case letter , or leftquoatation @ rightquoatation , zero or more times") (targetValue (name "?{or(or(<!>,<low>),<@>)}")))
(example (utterance "lines with 7 or more characters before a letter") (targetValue (name "?{contain(repeatatleast(<any>,7)),<let>}")))
(example (utterance "lines with 6 or more capital letters") (targetValue (name "?{repeatatleast(<cap>,6)}")))
(example (utterance "lines with words and a lower-case or capital letter or a character") (targetValue (name "?{or(<low>,<cap>),<any>}")))
(example (utterance "lines with a vowel before a number or string leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,repeatatleast(or(<num>,<!>),1)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation starting in a vowel") (targetValue (name "?{repeatatleast(<!>,1),startwith(<vow>)}")))
(example (utterance "lines with words with letter , 6 or more times") (targetValue (name "?{<let>}")))
(example (utterance "does not have a letter , a number , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<num>),<!>)}")))
(example (utterance "all lines") (targetValue (name "?{not(<low>),star(<num>)}")))
(example (utterance "lines not having leftquoatation ! rightquoatation or with leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),startwith(<@>)}")))
(example (utterance "lines with a lower-case letter or string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(or(<low>,<!>),<@>)}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a character") (targetValue (name "?{<let>,and(and(<!>,<any>),<@>)}")))
(example (utterance "lines that begin with a number , followed by a letter and leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<num>),concat(<let>,<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation at least 7 times preceding a vowel .") (targetValue (name "?{repeatatleast(<!>,7),<vow>}")))
(example (utterance "lines where an alphabet character succeeds a , e , i , o or u") (targetValue (name "?{<vow>,<let>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{endwith(concat(<!>,<vow>))}")))
(example (utterance "lines with a lower case letter or vowel") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "2 or more lines containing a letter or a vowel") (targetValue (name "?{contain(or(<let>,<vow>))}")))
(example (utterance "lines in which a character appears no less than zero times before a leftquoatation ! rightquoatation string occurs") (targetValue (name "?{star(<any>),<!>}")))
(example (utterance "lines having 1 capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines without ending with a number") (targetValue (name "?{contain(not(<num>))}")))
(example (utterance "lines with a character and leftquoatation ! rightquoatation") (targetValue (name "?{and(repeatatleast(<any>,1),<!>)}")))
(example (utterance "lines with a letter , a number , or a vowel .") (targetValue (name "?{or(<let>,<num>),<vow>}")))
(example (utterance "lines ending zero or more times with a lower-case letter") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines with words and a capital letter , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(<cap>,<!>),<any>}")))
(example (utterance "lines with words and a character before string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<any>,<!>),<@>}")))
(example (utterance "lines ending with character or containing string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<any>),contain(<!>)}")))
(example (utterance "lines containing a vowel , a number , and a character") (targetValue (name "?{and(<vow>,<num>),<any>}")))
(example (utterance "lines with a letter and character before capital letter") (targetValue (name "?{and(<let>,<any>),<cap>}")))
(example (utterance "items with words that have only an upper case letter .") (targetValue (name "?{<cap>}")))
(example (utterance "lines with words with a lower-case letter , letter , or character") (targetValue (name "?{or(or(<low>,<let>),<any>)}")))
(example (utterance "lines ending with at least 1 letter after a lower case letter") (targetValue (name "?{<low>,repeatatleast(endwith(<let>),1)}")))
(example (utterance "lines with a lower-case letter before vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,or(<vow>,<!>)}")))
(example (utterance "lines which are starting with a number at least once") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "lines with words that don't have letter and string leftquoatation ! rightquoatation") (targetValue (name "?{not(<let>),<!>}")))
(example (utterance "lines containing a lower-case letter , a capital letter , and a letter") (targetValue (name "?{and(<low>,<cap>),<let>}")))
(example (utterance "lines with words have a number before a vowel") (targetValue (name "?{not(<num>),<vow>}")))
(example (utterance "lines with a capital , a number , before a vowel") (targetValue (name "?{or(<cap>,<num>),<vow>}")))
(example (utterance "lines with a character before vowel 3 times") (targetValue (name "?{<any>,repeatatleast(<vow>,3)}")))
(example (utterance "lines with at least 2 words containing a capital letter") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines containing either a character or leftquoatation ! rightquoatation 7 or more times") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines which are ending with a lower-case letter and containing a number") (targetValue (name "?{endwith(<low>),contain(<num>)}")))
(example (utterance "the lines that contain a character or a number before before a letter") (targetValue (name "?{or(<any>,<num>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character or string leftquoatation @ rightquoatation before number") (targetValue (name "?{concat(<!>,<any>),and(<@>,<num>)}")))
(example (utterance "lines with 6 or more of a capital letter before a vowel") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines without a capital letter , character , or a leftquoatation ! rightquoatation string") (targetValue (name "?{or(or(<!>,<cap>),<any>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation and a number") (targetValue (name "?{contain(and(<!>,<num>))}")))
(example (utterance "lines with words starting with a letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines containing number , vowel and a letter") (targetValue (name "?{and(and(<num>,<vow>),<let>)}")))
(example (utterance "lines that contain a capital letter followed by a vowel , zero times or more") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines which have the leftquoatation ! rightquoatation string 7 times or greater and also have a vowel which comes prior to the leftquoatation @ rightquoatation string") (targetValue (name "?{repeatatleast(<!>,7),concat(<vow>,<@>)}")))
(example (utterance "lines ending with or without leftquoatation ! rightquoatation") (targetValue (name "?{contain(endwith(<!>))}")))
(example (utterance "lines containing a letter before the strings  leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{concat(<let>,or(<!>,<@>))}")))
(example (utterance "lines with lower-case letter , 6 or more times before capital letter") (targetValue (name "?{repeatatleast(<low>,6),<cap>}")))
(example (utterance "lines with out lower-case letters , vowels , or characters") (targetValue (name "?{or(or(<any>,<low>),<vow>)}")))
(example (utterance "lines with a lower-case letter and leftquoatation ! rightquoatation after a letter") (targetValue (name "?{and(<low>,<let>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , letter , or string leftquoatation @ rightquoatation before a number") (targetValue (name "?{or(or(<!>,<let>),<@>),<num>}")))
(example (utterance "lines containing words which include either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a character") (targetValue (name "?{or(or(<!>,<@>),<any>)}")))
(example (utterance "lines containing 2 or more characters or numbers .") (targetValue (name "?{contain(or(<any>,<num>))}")))
(example (utterance "lines which are containing either leftquoatation ! rightquoatation , a capital letter , or a character") (targetValue (name "?{or(or(<!>,<cap>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter before string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<cap>),<@>}")))
(example (utterance "lines with lower-case letter and a character") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines with a letter or a capital letter , 3 times or more") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines without a capital letter") (targetValue (name "?{contain(repeatatleast(<cap>,1))}")))
(example (utterance "items with a small letter preceding an upper case letter or character .") (targetValue (name "?{<low>,or(<any>,<cap>)}")))
(example (utterance "lines containing 4 or more vowels or lower case letters .") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines which are having leftquoatation ! rightquoatation , a character , and a letter before a number") (targetValue (name "?{and(and(<!>,<any>),<let>),<num>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a letter , leftquoatation @ rightquoatation and a character") (targetValue (name "?{contain(<!>),and(and(<let>,<@>),<any>)}")))
(example (utterance "lines that have words containing a small letter preceding the leftquoatation ! rightquoatation string") (targetValue (name "?{<low>,<!>}")))
(example (utterance "multiple lines that do not contain a letter followed by a number") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines containing words hving a number , leftquoatation ! rightquoatation , as well as a vowel followed by a character") (targetValue (name "?{and(and(<num>,<!>),<vow>),<any>}")))
(example (utterance "lines with words with letter or capital letter") (targetValue (name "?{<let>,<cap>}")))
(example (utterance "lines containing a vowel preceding leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<vow>,<!>),<@>}")))
(example (utterance "lines with 4 or more times where a letter is followed by a vowel") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines ending in only zero or more of a lower-case letter") (targetValue (name "?{star(<low>)}")))
(example (utterance "lines not having number before character") (targetValue (name "?{not(<num>),<any>}")))
(example (utterance "lines containing only a letter at least zero times") (targetValue (name "?{<let>}")))
(example (utterance "lines having either string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or vowel before number .") (targetValue (name "?{or(or(<!>,<@>),<vow>),<num>}")))
(example (utterance "lines with a number followed by lower-case letter , 7 or more times") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines with words and only a character or string leftquoatation ! rightquoatation") (targetValue (name "?{<any>,<!>}")))
(example (utterance "lines with a number after a vowel , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<@>),<num>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(or(<!>,<@>),3)}")))
(example (utterance "items with a vowel at least 7 time preceding a letter .") (targetValue (name "?{repeatatleast(<vow>,7),<let>}")))
(example (utterance "lines with a character before a non letter") (targetValue (name "?{<any>,not(<let>)}")))
(example (utterance "lines ending in 5 ore more of leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{repeatatleast(concat(<!>,<low>),5)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a letter and a capital letter") (targetValue (name "?{and(and(<cap>,<@>),<!>),<let>}")))
(example (utterance "lines with 3 or more of string leftquoatation ! rightquoatation before a capital letter or number") (targetValue (name "?{repeatatleast(<!>,3),or(<num>,<cap>)}")))
(example (utterance "lines containing lower-case letter or capital letter with string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{or(or(or(<low>,<cap>),<!>),<@>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation coming before a character that is not a number") (targetValue (name "?{<!>,not(<num>)}")))
(example (utterance "lines containing a vowel before a capital letter") (targetValue (name "?{<vow>,contain(<cap>)}")))
(example (utterance "lines with 7 times a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines that start with word that does or doesn't have at least 1 lower-case letter") (targetValue (name "?{star(<low>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation before a capital or lower-case letter") (targetValue (name "?{or(concat(<!>,<cap>),<low>)}")))
(example (utterance "lines with 6 or more of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,6)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter occurring 7 times or more") (targetValue (name "?{<!>,repeatatleast(<cap>,7)}")))
(example (utterance "lines not ending with vowel followed by string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(concat(<vow>,<!>))}")))
(example (utterance "lines with a capital letter and leftquoatation ! rightquoatation then a letter") (targetValue (name "?{and(<cap>,<!>),<let>}")))
(example (utterance "lines with a character or with only zero or more capital letters") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "lines with a vowel , number , or letter") (targetValue (name "?{or(or(<vow>,<num>),<let>)}")))
(example (utterance "lines which are ending with a number and containing leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<num>),contain(<!>)}")))
(example (utterance "lines with a letter or leftquoatation ! rightquoatation once") (targetValue (name "?{or(<let>,<!>),repeatatleast(<num>,1)}")))
(example (utterance "lines with a character and letter before a capital letter") (targetValue (name "?{and(<any>,<let>),<cap>}")))
(example (utterance "lines that have either a letter before a capitial letter or the string 'dog ;") (targetValue (name "?{or(or(<!>,<num>),<let>),<cap>}")))
(example (utterance "ines with either leftquoatation ! rightquoatation , a lower-case letter , or leftquoatation @ rightquoatation followed by a capital letter") (targetValue (name "?{or(or(<!>,<low>),<@>),<cap>}")))
(example (utterance "lines that have a capital latter occuring before a vowel and a character") (targetValue (name "?{<cap>,and(<vow>,<any>)}")))
(example (utterance "items with a small letter at least 4 times preceding a letter .") (targetValue (name "?{repeatatleast(<low>,4),<let>}")))
(example (utterance "lines finishing with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation , a number , as well as leftquoatation # rightquoatation") (targetValue (name "?{endwith(<!>),and(and(<@>,<num>),<#>)}")))
(example (utterance "lines with a number followed by a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation with string leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{repeatatleast(<!>,1),concat(<@>,<vow>)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation or a lower-case letter followed by a character") (targetValue (name "?{or(<low>,<!>),<any>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines which begin with a lower-case letter followed by a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<low>,<vow>),<!>)}")))
(example (utterance "lines not containing lower-case letter , 2 or more times") (targetValue (name "?{contain(not(<low>))}")))
(example (utterance "lines which are  having a letter , a vowel , and leftquoatation ! rightquoatation before a number") (targetValue (name "?{and(and(<let>,<vow>),<!>),<num>}")))
(example (utterance "lines with character followed by string leftquoatation ! rightquoatation before lower-case letter or vowel") (targetValue (name "?{concat(<any>,<!>),or(<low>,<vow>)}")))
(example (utterance "lines with only a letter or a vowel at least 1 time") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "items with a numeral preceding an upper case letter preceding a character .") (targetValue (name "?{concat(<num>,<cap>),<any>}")))
(example (utterance "items with leftquoatation ! rightquoatation at least twice preceding a numeral .") (targetValue (name "?{repeatatleast(<!>,2),<num>}")))
(example (utterance "lines without a capital letter , lower-case letter , or number") (targetValue (name "?{and(and(<cap>,<low>),<num>)}")))
(example (utterance "lines with a lower-case letter followed by a capital letter or any letter") (targetValue (name "?{concat(<low>,<cap>),startwith(<let>)}")))
(example (utterance "lines with lower-case letter , zero or more times before letter") (targetValue (name "?{star(<low>),<let>}")))
(example (utterance "lines which are containing words with a vowel before a number") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines with words and zero or more capital letters with string leftquoatation ! rightquoatation") (targetValue (name "?{star(<cap>),<!>}")))
(example (utterance "lines containing 3 or more lower-case letters") (targetValue (name "?{<low>}")))
(example (utterance "lines with a capital letter before lower-case letter") (targetValue (name "?{contain(<cap>),star(<low>)}")))
(example (utterance "lines with a capital followed by character or ending with string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<any>),endwith(<!>)}")))
(example (utterance "lines end containing only a character or leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation and capital letter or string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<let>,<!>),or(<cap>,<@>)}")))
(example (utterance "the lines that contain a vowel before leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,contain(<!>)}")))
(example (utterance "zero or more lines with words ending with a number") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines with a vowel , then a letter before a capital letter") (targetValue (name "?{star(<vow>),concat(<let>,<cap>)}")))
(example (utterance "items with a vowel preceding a numeral or a character .") (targetValue (name "?{<vow>,or(<any>,<num>)}")))
(example (utterance "lines with 3 or more of leftquoatation ! rightquoatation with string leftquoatation @ rightquoatation before a character") (targetValue (name "?{repeatatleast(<!>,3),concat(<@>,<any>)}")))
(example (utterance "lines without a character before a vowel then string leftquoatation ! rightquoatation") (targetValue (name "?{concat(concat(<any>,<vow>),<!>)}")))
(example (utterance "lines ending with a character , with at least 1 number before the last character") (targetValue (name "?{repeatatleast(<num>,1),endwith(<any>)}")))
(example (utterance "lines with words and 3 or more of a capital letter before number") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines not starting in a capital letter") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines with a vowel") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "likes with a lower case letter followed by leftquoatation ! rightquoatation and beginning with leftquoatation @ rightquoatation") (targetValue (name "?{concat(<low>,<!>),startwith(<@>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation at least once before lower-case letter , string leftquoatation @ rightquoatation , and capital letter") (targetValue (name "?{repeatatleast(<!>,1),and(and(<low>,<@>),<cap>)}")))
(example (utterance "lines with a character before a capital letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation in them") (targetValue (name "?{<any>,and(and(<cap>,<!>),<@>)}")))
(example (utterance "lines with words and starting with a lower-case letter before vowel") (targetValue (name "?{startwith(<low>),<vow>}")))
(example (utterance "lines starting in a lower-case letter without string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<low>),not(<!>)}")))
(example (utterance "lines with words and a letter") (targetValue (name "?{repeatatleast(<let>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , number , and vowel before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<num>),<vow>),<@>}")))
(example (utterance "lines that do not start with a capital letter , at least 7 or more times") (targetValue (name "?{repeatatleast(startwith(<cap>),7)}")))
(example (utterance "lines with words with ending with a letter before leftquoatation ! rightquoatation in it") (targetValue (name "?{endwith(<let>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation followed by string leftquoatation # rightquoatation") (targetValue (name "?{<!>,concat(<@>,<#>)}")))
(example (utterance "lines with a lower case letter before any characters or leftquoatation ! rightquoatation") (targetValue (name "?{<low>,or(<any>,<!>)}")))
(example (utterance "lines taht have words shouldn't have leftquoatation ! rightquoatation or 'truck with numbers") (targetValue (name "?{and(<!>,<@>),<num>}")))
(example (utterance "lines with a character and leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{contain(<any>),or(or(<!>,<@>),<#>)}")))
(example (utterance "lines with a letter before ending with vowel , 4 or more times") (targetValue (name "?{<let>,endwith(repeatatleast(<vow>,4))}")))
(example (utterance "lines with lower-case letter and letter before string leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{and(<low>,<let>),repeatatleast(<!>,3)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines containing at least 2 characters") (targetValue (name "?{repeatatleast(<any>,2)}")))
(example (utterance "lines which bigis with a vowel , a number , a character or a capital letter") (targetValue (name "?{or(or(or(<any>,<vow>),<cap>),<num>)}")))
(example (utterance "lines ending in zero or more of a character") (targetValue (name "?{endwith(contain(<any>))}")))
(example (utterance "lines not having leftquoatation ! rightquoatation or a lower-case letter , 5 or more times in them") (targetValue (name "?{or(not(<!>),<low>)}")))
(example (utterance "lines not ending with a character followed by string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(concat(<any>,<!>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a number before a vowel") (targetValue (name "?{or(or(<!>,<num>),<@>),<vow>}")))
(example (utterance "lines containing a capital letter followed by a number or a character") (targetValue (name "?{<cap>,or(<num>,<any>)}")))
(example (utterance "lines with zero or more of a lower-case letter or number") (targetValue (name "?{star(or(<low>,<num>))}")))
(example (utterance "lines with words and letter or string leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,<!>),<let>}")))
(example (utterance "letters ending with a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<let>),<!>}")))
(example (utterance "lines with a letter and number before vowel") (targetValue (name "?{and(<let>,<num>),<vow>}")))
(example (utterance "lines with either a character or vowel or a letter") (targetValue (name "?{or(or(<any>,<let>),<vow>)}")))
(example (utterance "lines with a letter 3 times before a character") (targetValue (name "?{repeatatleast(<let>,3),<any>}")))
(example (utterance "lines with 2 or more of a letter before a letter") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation and ending with a vowel") (targetValue (name "?{not(<!>),endwith(<vow>)}")))
(example (utterance "lines with a letter before a number or leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<num>),contain(<!>)}")))
(example (utterance "lines containing a character , zero or more times or a letter in them") (targetValue (name "?{or(star(<any>),<let>)}")))
(example (utterance "lines with at least 7 of  leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines containing words that have a number and it appears at least 5 times ot any number of times over 5") (targetValue (name "?{<num>}")))
(example (utterance "lines that contain a character , leftquoatation ! rightquoatation , and a vowel") (targetValue (name "?{and(and(<any>,<!>),<vow>)}")))
(example (utterance "items with an upper case letter preceding leftquoatation ! rightquoatation or a numeral , or a small letter .") (targetValue (name "?{<cap>,or(or(<!>,<low>),<num>)}")))
(example (utterance "lines with a capital letter , a lower-case letter , and leftquoatation ! rightquoatation after a capital letter") (targetValue (name "?{<any>,and(and(<cap>,<low>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , vowel , or character before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<vow>),<any>),<@>}")))
(example (utterance "lines with words that contain only a vowel or leftquoatation ! rightquoatation in them") (targetValue (name "?{<vow>,<!>}")))
(example (utterance "lines having a vowel , a number , and a lower-case letter") (targetValue (name "?{and(and(<vow>,<num>),<let>),<low>}")))
(example (utterance "no letters , characters or leftquoatation ! rightquoatation in the lines") (targetValue (name "?{or(or(<!>,<let>),<any>)}")))
(example (utterance "lines containing only letter , 3 or more times") (targetValue (name "?{contain(<let>)}")))
(example (utterance "items with a vowel preceding the ending leftquoatation ! rightquoatation .") (targetValue (name "?{<vow>,endwith(<!>)}")))
(example (utterance "lines with words that include a letter , leftquoatation ! rightquoatation , or a lower-case letter") (targetValue (name "?{or(<!>,<low>),<let>}")))
(example (utterance "lines in which a number is preceded by a capital letter and followed by a letter") (targetValue (name "?{concat(<cap>,<num>),<let>}")))
(example (utterance "items with a letter preceding a vowel , a character , or a numeral .") (targetValue (name "?{<let>,or(or(<num>,<any>),<vow>)}")))
(example (utterance "lines staring with characters or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or even a lower case letter") (targetValue (name "?{or(<any>,or(or(<!>,<@>),<low>))}")))
(example (utterance "lines starting with a vowel after a lower case letter") (targetValue (name "?{startwith(<low>),<vow>}")))
(example (utterance "lines where a vowel is followed by a character at least 7 times") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines that have 5 or more combinations of a lower-case letter followed by a character") (targetValue (name "?{repeatatleast(<low>,5),<any>}")))
(example (utterance "lines that do not have a character or leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with words and 6 or more characters or a capital letter") (targetValue (name "?{repeatatleast(<any>,6),<cap>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation or character") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a character , or a numeral .") (targetValue (name "?{or(or(<!>,<num>),<any>)}")))
(example (utterance "lines with the words either leftquoatation ! rightquoatation , a character , or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<any>),<@>}")))
(example (utterance "lines with a vowel after a capital letter , a lower-case letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<low>),<!>),<vow>}")))
(example (utterance "lines that must have a capital letter , a number and a vowel") (targetValue (name "?{and(and(<cap>,<num>),<vow>)}")))
(example (utterance "lines ending with a number") (targetValue (name "?{<num>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation , a lower-case letter , and a number") (targetValue (name "?{and(and(<!>,<low>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number or character") (targetValue (name "?{<!>,or(<num>,<any>)}")))
(example (utterance "lines that either have leftquoatation ! rightquoatation and a capital letter or leftquoatation @ rightquoatation , no less than zero times") (targetValue (name "?{or(<@>,and(<!>,<cap>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation , and character before a letter") (targetValue (name "?{and(and(<!>,<any>),<@>),<let>}")))
(example (utterance "lines with a letter before a character at least once") (targetValue (name "?{repeatatleast(<let>,1),<any>}")))
(example (utterance "lines with a number , lower-case letter , or string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{or(or(<num>,<low>),<!>),<vow>}")))
(example (utterance "lines without a letter , string leftquoatation ! rightquoatation , or lower-case letter") (targetValue (name "?{and(and(<let>,<!>),<low>)}")))
(example (utterance "lines having either a letter or a number , followed by a vowel") (targetValue (name "?{or(or(<low>,<cap>),<num>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 7 or more times and leftquoatation @ rightquoatation or a vowel in them") (targetValue (name "?{repeatatleast(<!>,7),or(<@>,<vow>)}")))
(example (utterance "line with vowel or characters before number") (targetValue (name "?{or(<vow>,<any>),<num>}")))
(example (utterance "lines with a vowel after a capital letter and the string 'dog") (targetValue (name "?{concat(and(<cap>,<!>),<vow>)}")))
(example (utterance "lines that have a letter , leftquoatation ! rightquoatation , or a character after a number") (targetValue (name "?{or(or(<let>,<!>),<num>),<any>}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with a letter and ending with leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,1),endwith(<!>)}")))
(example (utterance "lines containinbg words having a lower-case letter , a character , as well as a number") (targetValue (name "?{and(<low>,<any>),<num>}")))
(example (utterance "lines not containing number , zero or more times") (targetValue (name "?{contain(star(<num>))}")))
(example (utterance "items with an upper case letter , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation at least 3 times .") (targetValue (name "?{or(and(<cap>,<!>),<@>)}")))
(example (utterance "lines with number , 3 or more times and letter before lower-case letter") (targetValue (name "?{repeatatleast(<num>,3),concat(<let>,<low>)}")))
(example (utterance "lines with a vowel or 6 or more capital letters") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "lines starting with a lower-case letter , 3 times or more") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation or ending in string leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),endwith(<@>)}")))
(example (utterance "lines which have a character at least 7 times before a letter") (targetValue (name "?{repeatatleast(<any>,7),<let>}")))
(example (utterance "lines end with leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines ending in 6 or more of leftquoatation ! rightquoatation or vowel") (targetValue (name "?{endwith(<!>),repeatatleast(<let>,6)}")))
(example (utterance "lines with characters that follow leftquoatation ! rightquoatation") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines which are containing a lower-case letter followed by leftquoatation ! rightquoatation at least once") (targetValue (name "?{repeatatleast(concat(<low>,<!>),1)}")))
(example (utterance "lines with words and a character before string leftquoatation ! rightquoatation then leftquoatation @ rightquoatation") (targetValue (name "?{concat(<any>,<!>),<@>}")))
(example (utterance "lines with a capital letter , a lower-case letter , and leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<cap>,<low>),<!>),<@>}")))
(example (utterance "lines with lower-case letter after a letter at least 5 times") (targetValue (name "?{<let>,repeatatleast(<low>,5)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , followed by a character , and then containing a lower-case letter") (targetValue (name "?{concat(<!>,<any>),contain(<low>)}")))
(example (utterance "lines having at least 3 letters and leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,3),contain(<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation at least thrice .") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with words that contain a letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<vow>),<!>}")))
(example (utterance "lines with a character or leftquoatation ! rightquoatation before a letter in them") (targetValue (name "?{or(<any>,<!>),<let>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before words that contain only string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,<@>}")))
(example (utterance "lines with 2 or more of leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{repeatatleast(<!>,2),concat(<@>,<#>)}")))
(example (utterance "lines with a number followed by a vowel") (targetValue (name "?{concat(<num>,<vow>),<let>}")))
(example (utterance "lines containing a character and 7 or more of leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<!>)}")))
(example (utterance "lines which are starting with a number or ending with leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<num>),endwith(<!>)}")))
(example (utterance "lines ending in a number") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines not having leftquoatation ! rightquoatation or leftquoatation @ rightquoatation and not a vowel") (targetValue (name "?{and(and(<!>,<@>),<vow>)}")))
(example (utterance "lines with a capital letter before non number") (targetValue (name "?{<cap>,not(<num>)}")))
(example (utterance "lines that have either a leftquoatation ! rightquoatation string or a letter in lower-case coming before a vowel") (targetValue (name "?{or(<low>,<!>),<vow>}")))
(example (utterance "lines with words , string leftquoatation ! rightquoatation , and capital letter before string leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<cap>),<@>}")))
(example (utterance "lines with words and a letter") (targetValue (name "?{repeatatleast(<let>,1)}")))
(example (utterance "lines ending with words not a vowel") (targetValue (name "?{not(<vow>)}")))
(example (utterance "lines with words ending with string leftquoatation ! rightquoatation or character") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines containing at least 5 characters before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,5),<!>}")))
(example (utterance "lines ending with a vowel string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{star(<!>),endwith(<vow>)}")))
(example (utterance "lines with words and a vowel before a letter") (targetValue (name "?{<vow>,<let>}")))
(example (utterance "lines with a capital before either a letter or leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,or(or(<low>,<!>),<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or leftquoatation # rightquoatation or a number") (targetValue (name "?{<!>,or(or(<@>,<#>),<num>)}")))
(example (utterance "lines beginning with 2 or more leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),repeatatleast(<@>,2)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation at least once before vowel or string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),or(<vow>,<@>)}")))
(example (utterance "lines with a capital letter followed by a lower-case letter , greater than or equal to zero times") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines that contains words containing a capital letter , character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<any>),<!>}")))
(example (utterance "lines with an upper-case letter preceding a character twice or more") (targetValue (name "?{<cap>,repeatatleast(<any>,2)}")))
(example (utterance "lines with a capital letter and string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{and(and(<let>,<cap>),<!>),<low>}")))
(example (utterance "leftquoatation ! rightquoatation or a capital letter appears in the beginning of the lines") (targetValue (name "?{or(star(<!>),<cap>)}")))
(example (utterance "lines with words with a capital letter before only a character") (targetValue (name "?{<cap>,<any>}")))
(example (utterance "items with 'dog") (targetValue (name "?{<!>,not(<num>)}")))
(example (utterance "lines with a letter before a character , and leftquoatation ! rightquoatation or a capital") (targetValue (name "?{<let>,and(and(<any>,<!>),<cap>)}")))
(example (utterance "lines with a vowel and leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{and(<vow>,<!>),<num>}")))
(example (utterance "lines with a character or a lower-case letter at least once in them") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "items with a small letter preceding a character .") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and leftquoatation # rightquoatation and a capital letter") (targetValue (name "?{<!>,and(and(<@>,<#>),<cap>)}")))
(example (utterance "items with an upper case letter , a character , and a numeral preceding a vowel .") (targetValue (name "?{and(and(<cap>,<num>),<any>),<vow>}")))
(example (utterance "identify lines that contain leftquoatation ! rightquoatation then any letter repeated 7 times") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with words with a lower-case letter , number , or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<low>,or(or(<num>,<!>),<@>)}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , or a number after a letter") (targetValue (name "?{<let>,or(or(<vow>,<!>),<num>)}")))
(example (utterance "lines with only a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or letter") (targetValue (name "?{contain(<!>),<let>}")))
(example (utterance "lines containing words with string leftquoatation ! rightquoatation followed by a capital") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines which are containing a character , a number , and a letter") (targetValue (name "?{and(and(<any>,<num>),<let>)}")))
(example (utterance "lines without a number , character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<any>),<!>)}")))
(example (utterance "lines should have a letterfollowed by a lower-case letter") (targetValue (name "?{concat(<let>,<low>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , a character , and a letter in them") (targetValue (name "?{and(<!>,<any>),<let>}")))
(example (utterance "lines that end with a letter preceding a character") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before capital or lower case letters or a vowel") (targetValue (name "?{<!>,or(or(<cap>,<low>),<vow>)}")))
(example (utterance "lines containing 7 times capital letter") (targetValue (name "?{repeatatleast(<cap>,7)}")))
(example (utterance "lines with a character before a lower-case letter or string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,1),or(<low>,<!>)}")))
(example (utterance "lines that have words with a letter , a capital letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<!>,<let>),<cap>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation before a number 6 or more times") (targetValue (name "?{<!>,repeatatleast(<num>,6)}")))
(example (utterance "line with letters starting with leftquoatation ! rightquoatation") (targetValue (name "?{contain(<let>),startwith(<!>)}")))
(example (utterance "number-containing lines seen at least 7 times or more which are present prior to the leftquoatation ! rightquoatation string") (targetValue (name "?{repeatatleast(<num>,7),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a number after leftquoatation # rightquoatation") (targetValue (name "?{<#>,and(and(<!>,<@>),<num>)}")))
(example (utterance "lines with with a letter followed by 7 or more of leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with vowel followed by string leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines with words with a letter and lower-case letter") (targetValue (name "?{<let>,<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a letter before any lower case letter") (targetValue (name "?{concat(<!>,<let>),<low>}")))
(example (utterance "lines do not have a lower - case letter , a vowel , or a character") (targetValue (name "?{or(or(<low>,<vow>),<any>)}")))
(example (utterance "lines with a vowel before a letter") (targetValue (name "?{repeatatleast(concat(<vow>,<let>),1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation following a number") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines with words using a number and capital letter") (targetValue (name "?{repeatatleast(<num>,1),<cap>}")))
(example (utterance "lines with words with lower-case letter , string leftquoatation ! rightquoatation , and string leftquoatation @ rightquoatation") (targetValue (name "?{and(<low>,<!>),<@>}")))
(example (utterance "lines not having number , zero or more times and not a lower-case letter") (targetValue (name "?{and(star(<num>),<low>)}")))
(example (utterance "lines that don't start with a capital letter with leftquoatation ! rightquoatation after") (targetValue (name "?{startwith(concat(<cap>,<!>))}")))
(example (utterance "lines that must not have a vowel , a capital letter , or the word leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<cap>),<!>)}")))
(example (utterance "lines ending in lower-case letter or leftquoatation ! rightquoatation and a character") (targetValue (name "?{or(<low>,and(<!>,<any>))}")))
(example (utterance "lines having words containing leftquoatation ! rightquoatation followed by a vowel or a letter") (targetValue (name "?{concat(<!>,<vow>),<let>}")))
(example (utterance "a lower-case letter and number after lines with the string'dog' .") (targetValue (name "?{<!>,and(<low>,<num>)}")))
(example (utterance "lines with words and zero or more of leftquoatation ! rightquoatation") (targetValue (name "?{star(<!>),<any>}")))
(example (utterance "items witha character at last thrice preceding a vowel .") (targetValue (name "?{repeatatleast(<any>,3),<vow>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or a letter") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines which have either leftquoatation ! rightquoatation a vowel or a character .") (targetValue (name "?{or(or(<!>,<vow>),<any>)}")))
(example (utterance "lines with a 5 or more of a capital letter and vowel or a letter") (targetValue (name "?{or(and(<cap>,<vow>),<low>)}")))
(example (utterance "lines containing number and lower-case letter at least once") (targetValue (name "?{contain(and(<num>,<low>))}")))
(example (utterance "lines ending in words with string leftquoatation ! rightquoatation before vowel") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "no lines containing leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{and(not(<!>),<cap>)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation or letter followed by a capital") (targetValue (name "?{endwith(<!>),concat(<let>,<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before lower-case letter and character") (targetValue (name "?{or(<!>,<@>),and(<low>,<any>)}")))
(example (utterance "lines with lower-case letter before string leftquoatation ! rightquoatation , 5 or more times") (targetValue (name "?{<low>,repeatatleast(<!>,5)}")))
(example (utterance "lines with words or a character either a vowel , a capital letter and string leftquoatation ! rightquoatation") (targetValue (name "?{<any>,or(or(<vow>,<cap>),<!>)}")))
(example (utterance "lines with 0 or more characters , a letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation") (targetValue (name "?{star(<any>),and(and(<let>,<!>),<@>)}")))
(example (utterance "lines with a letter followed by 7 or more numbers") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines with only a lower-case letter and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<!>)}")))
(example (utterance "lines with a character string leftquoatation ! rightquoatation and before leftquoatation @ rightquoatation") (targetValue (name "?{and(<any>,<!>),<@>}")))
(example (utterance "lines with either leftquoatation ! rightquoatation leftquoatation @ rightquoatation or a lower-case letter") (targetValue (name "?{or(<low>,<!>),<@>}")))
(example (utterance "lines with a vowel followed by a number , a lower-case letter , and a letter") (targetValue (name "?{<vow>,and(and(<num>,<low>),<let>)}")))
(example (utterance "lines beginning with a capital letter , zero or more times") (targetValue (name "?{star(<cap>)}")))
(example (utterance "lines containing lower-case letter and not string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<low>),not(<!>)}")))
(example (utterance "lines and a capital letter , and after , a lower-case letter") (targetValue (name "?{<cap>,<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a vowel , a lower-case letter , and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<vow>,<low>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before words with a number , character , and string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<num>,<any>),<@>)}")))
(example (utterance "lines with only 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation before a letter") (targetValue (name "?{<cap>,concat(<!>,<let>)}")))
(example (utterance "items with words containing a character or leftquoatation ! rightquoatation .") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation and 'truck and a number") (targetValue (name "?{and(and(<!>,<@>),<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation zero before leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),<@>}")))
(example (utterance "lines with a number and letter") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "items with a numeral preceding a vowel preceding a small letter .") (targetValue (name "?{concat(<num>,<vow>),<low>}")))
(example (utterance "lines containing a character preceded by a lowercase letter , vowel or capital letter") (targetValue (name "?{or(or(<low>,<vow>),<cap>),<any>}")))
(example (utterance "lines containing words and ending with a capital letter or a lower-case letter") (targetValue (name "?{endwith(<cap>),<low>}")))
(example (utterance "lines which are not containing only a letter and not a vowel") (targetValue (name "?{and(<let>,<vow>)}")))
(example (utterance "lines with no vowel before a capital letter and no lower-case letter") (targetValue (name "?{and(concat(<vow>,<cap>),<low>)}")))
(example (utterance "lines with a capital letter and containing leftquoatation ! rightquoatation 1 or more times") (targetValue (name "?{repeatatleast(<cap>,1),contain(<!>)}")))
(example (utterance "lines where a numeral comes after a symbol character and alphabet character") (targetValue (name "?{and(<any>,<let>),<num>}")))
(example (utterance "items with words containg a vowel at least 1 time .") (targetValue (name "?{<vow>}")))
(example (utterance "lines that have a lower-case letter , a capital letter , and a leftquoatation ! rightquoatation string appearing before a number") (targetValue (name "?{and(and(<!>,<low>),<cap>),<num>}")))
(example (utterance "lines that do not contain a lower-case letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<!>),<vow>)}")))
(example (utterance "lines with words without zero or more of a lower-case letter") (targetValue (name "?{star(<low>)}")))
(example (utterance "lines without a capital letter , a vowel , as well as a lower-case letter") (targetValue (name "?{or(or(<cap>,<vow>),<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , number , and character") (targetValue (name "?{and(and(<!>,<num>),<any>)}")))
(example (utterance "lines ending with a character 1 or more times") (targetValue (name "?{endwith(repeatatleast(<any>,1))}")))
(example (utterance "lines ending in a letter or starting in number") (targetValue (name "?{or(<let>,startwith(<num>))}")))
(example (utterance "lines with at least zero vowels or letters") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{<any>,star(<!>)}")))
(example (utterance "lines with words with a letter and no number") (targetValue (name "?{<let>,not(<num>)}")))
(example (utterance "lines with a capital before vowel at least once") (targetValue (name "?{<cap>,repeatatleast(<vow>,1)}")))
(example (utterance "lines with a lower-case letter before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a number") (targetValue (name "?{<low>,or(or(<!>,<num>),<@>)}")))
(example (utterance "lines starting with a lower-case letter then a number") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "items with a letter preceding a terminating vowel .") (targetValue (name "?{<let>,endwith(<vow>)}")))
(example (utterance "lines that have a lower-case letter and the word leftquoatation ! rightquoatation , then ends with the word leftquoatation @ rightquoatation") (targetValue (name "?{and(<low>,<!>),endwith(<@>)}")))
(example (utterance "lines in which there's no leftquoatation ! rightquoatation string , and also no vowels nor characters") (targetValue (name "?{and(and(<!>,<vow>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character then a letter") (targetValue (name "?{<!>,concat(<any>,<let>)}")))
(example (utterance "lines with words ending with string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines containing a capital letter , followed by 5 or more of leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,repeatatleast(<!>,5)}")))
(example (utterance "lines ending with containing only a letter or a character in them") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with words with a lower-case letter before a capital letter in them") (targetValue (name "?{<low>,<cap>}")))
(example (utterance "lines with words and a vowel and string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{and(and(<vow>,<!>),<@>),<#>}")))
(example (utterance "lines not containing a capital letter , a letter , or a character") (targetValue (name "?{and(and(<cap>,<let>),<any>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation with a character after") (targetValue (name "?{<num>,concat(<!>,<any>)}")))
(example (utterance "lines that have a vowel or letter") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or a character") (targetValue (name "?{or(concat(<!>,<@>),<any>)}")))
(example (utterance "lines not having a vowel , a letter , and not a number") (targetValue (name "?{and(and(<vow>,<let>),<num>)}")))
(example (utterance "lines ending with a vowel followed by a character") (targetValue (name "?{contain(concat(<vow>,<any>))}")))
(example (utterance "lines with a character before a number then a capital letter") (targetValue (name "?{concat(<any>,<num>),<cap>}")))
(example (utterance "lines not containing a letter") (targetValue (name "?{contain(not(<let>))}")))
(example (utterance "items with a numeral and a character preceding leftquoatation ! rightquoatation .") (targetValue (name "?{and(<num>,<any>),<!>}")))
(example (utterance "lines that contain a lower-case letter or a character") (targetValue (name "?{<any>,<low>}")))
(example (utterance "lines not starting with leftquoatation ! rightquoatation or a letter") (targetValue (name "?{startwith(or(<!>,<let>))}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation or a capital") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , number , and capital letter or a vowel") (targetValue (name "?{or(and(and(<!>,<num>),<cap>),<vow>)}")))
(example (utterance "lines with a letter before a number or character") (targetValue (name "?{concat(<let>,or(<num>,<any>))}")))
(example (utterance "lines that have either leftquoatation ! rightquoatation or a number") (targetValue (name "?{<num>,<!>}")))
(example (utterance "lines with words with a number then a letter") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "items with an upper case letter preceding a letter at least5times .") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a vowel preceding a letter") (targetValue (name "?{<!>,concat(<vow>,<let>)}")))
(example (utterance "lines containing either a lower-case letter , a vowel , or a number after leftquoatation ! rightquoatation") (targetValue (name "?{<!>,or(or(<low>,<vow>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a number ending with a capital letter") (targetValue (name "?{and(<!>,<num>),endwith(<cap>)}")))
(example (utterance "lines with character , zero or more times before a number and leftquoatation ! rightquoatation") (targetValue (name "?{star(<any>),and(<num>,<!>)}")))
(example (utterance "lines containing a number before leftquoatation ! rightquoatation preceding a letter") (targetValue (name "?{<num>,concat(<!>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a vowel , a capital letter , or a lower-case letter") (targetValue (name "?{<!>,or(or(<vow>,<cap>),<low>)}")))
(example (utterance "lines with words and a character , capital letter , and number") (targetValue (name "?{and(<any>,<cap>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a character or a letter") (targetValue (name "?{or(<any>,<let>),<!>}")))
(example (utterance "lines not starting with a letter") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "lines not having vowel or lower-case letter , 7 or more times") (targetValue (name "?{repeatatleast(or(<vow>,<low>),7)}")))
(example (utterance "lines with an uppercase letter or leftquoatation ! rightquoatation preceding a lowercase letter") (targetValue (name "?{or(<cap>,<!>),<low>}")))
(example (utterance "lines with a 5 or more vowels before a capital letter") (targetValue (name "?{repeatatleast(<vow>,5),<cap>}")))
(example (utterance "lines without a vowel ending in string leftquoatation ! rightquoatation") (targetValue (name "?{not(<vow>),endwith(<!>)}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation or a vowel and a lower-case letter") (targetValue (name "?{and(<vow>,<low>),<!>}")))
(example (utterance "lines that do not contain a lower-case letter or begin with a letter") (targetValue (name "?{not(<low>),startwith(<let>)}")))
(example (utterance "lines containing at least 1 lower-case letter") (targetValue (name "?{<low>}")))
(example (utterance "lines containing number before lower-case letter") (targetValue (name "?{contain(<num>),<low>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation before a capital letter and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<cap>),<@>}")))
(example (utterance "lines with 7 or more of a letter or number") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines with words with a character , lower-case letter , or number") (targetValue (name "?{or(<any>,<low>),<num>}")))
(example (utterance "lines starting in words ending in a lower-case letter") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , a number , or a capital letter before a lower-case letter") (targetValue (name "?{or(or(<!>,<num>),<cap>),<low>}")))
(example (utterance "items with an uppercase letter preceding 'dog") (targetValue (name "?{<cap>,concat(<!>,<vow>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines with words leftquoatation ! rightquoatation 6 times") (targetValue (name "?{not(<!>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation followed by a letter zero times") (targetValue (name "?{startwith(concat(<!>,<let>))}")))
(example (utterance "words with a letter appears before leftquoatation ! rightquoatation zero or more times in the lines") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation following leftquoatation @ rightquoatation , a character , and a capital letter") (targetValue (name "?{<!>,and(and(<@>,<any>),<cap>)}")))
(example (utterance "lines stating with words , leftquoatation ! rightquoatation , and a letter") (targetValue (name "?{and(<!>,<let>)}")))
(example (utterance "lines with words , a character , zero") (targetValue (name "?{<any>}")))
(example (utterance "lines containing words with a capital letter or a number at least 4 times") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{endwith(concat(<!>,<@>))}")))
(example (utterance "lines with a vowel followed by character or letter") (targetValue (name "?{<vow>,or(<any>,<let>)}")))
(example (utterance "lines with 4 or more of leftquoatation ! rightquoatation ending in vowel") (targetValue (name "?{repeatatleast(<!>,4),endwith(<vow>)}")))
(example (utterance "lines having either string leftquoatation ! rightquoatation , capital letter , or letter before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<cap>),<let>),<@>}")))
(example (utterance "lines with lower-case letter or a capital , 2 or more times") (targetValue (name "?{or(<low>,<cap>)}")))
(example (utterance "lines with characters or a capital letter , before leftquoatation ! rightquoatation") (targetValue (name "?{star(or(<any>,<cap>)),<!>}")))
(example (utterance "lines that contain a lower-case letter followed by a character , zero times or more") (targetValue (name "?{contain(concat(<low>,<any>))}")))
(example (utterance "lines not ending with only a letter") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with words with a letter and character followed by vowel") (targetValue (name "?{<let>,concat(<any>,<vow>)}")))
(example (utterance "lines that contain a vowel followed by a lower-case letter") (targetValue (name "?{<vow>,contain(<low>)}")))
(example (utterance "items with a numeral preceding a letter at least twice .") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation at least once") (targetValue (name "?{endwith(<!>),repeatatleast(<@>,1)}")))
(example (utterance "items with a letter or character at least 4 times .") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines which have a vowel , and 1 of the following before the vowel: leftquoatation ! rightquoatation , a number , or a letter") (targetValue (name "?{or(or(<num>,<let>),<!>),<vow>}")))
(example (utterance "lines which are not having a letter before a character , 5 or more times") (targetValue (name "?{not(<let>),repeatatleast(<any>,5)}")))
(example (utterance "lines containing the leftquoatation ! rightquoatation string on 5 occasions or greater which then conclude with a vowel") (targetValue (name "?{repeatatleast(<!>,5),endwith(<vow>)}")))
(example (utterance "lines starting with vowel and capital letter , 6 or more times") (targetValue (name "?{startwith(<vow>),repeatatleast(<cap>,6)}")))
(example (utterance "lines with number words") (targetValue (name "?{<num>}")))
(example (utterance "lines that have words , leftquoatation ! rightquoatation , leftquoatation @ rightquoatation or number") (targetValue (name "?{or(<!>,<@>),<num>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation , a lower-case letter , or a vowel") (targetValue (name "?{or(or(<!>,<low>),<vow>)}")))
(example (utterance "lines with 4 or more letters") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines with words that contain a lower-case and a capital letter") (targetValue (name "?{contain(<low>),<cap>}")))
(example (utterance "lines with words that contain string leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{contain(<!>),<low>}")))
(example (utterance "lines ending in a character or having a capital letter") (targetValue (name "?{endwith(<any>),contain(<cap>)}")))
(example (utterance "lines that don't end with a lower-case letter 7 times") (targetValue (name "?{repeatatleast(endwith(<low>),7)}")))
(example (utterance "lines which are not ending with a letter and not a lower-case letter") (targetValue (name "?{and(endwith(<let>),<low>)}")))
(example (utterance "lines having either leftquoatation ! rightquoatation or leftquoatation @ rightquoatation , before a letter") (targetValue (name "?{or(or(<low>,<!>),<@>),<let>}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , a character , or leftquoatation @ rightquoatation followed by a capital letter") (targetValue (name "?{or(or(<!>,<any>),<@>),<cap>}")))
(example (utterance "lines having a character before a vowel at least 1 time") (targetValue (name "?{<any>,repeatatleast(<vow>,1)}")))
(example (utterance "lines containing a number or a capital letter , 2 or more times") (targetValue (name "?{or(<num>,<cap>)}")))
(example (utterance "lines containing either a letter , a capital letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<cap>),<!>)}")))
(example (utterance "items with vowel preceding a numeral at least thrice .") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines that contain words that have leftquoatation ! rightquoatation or 5 or more vowels") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines ending with a character at least twice") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines which are not having a capital letter , not leftquoatation ! rightquoatation , and not a lower-case letter") (targetValue (name "?{and(and(<cap>,<!>),<low>)}")))
(example (utterance "lines which are  ending with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{endwith(<!>),concat(<@>,<#>)}")))
(example (utterance "items with words containing a vowel or character .") (targetValue (name "?{<any>,<vow>}")))
(example (utterance "lines with words and a capital letter before string leftquoatation ! rightquoatation then leftquoatation @ rightquoatation") (targetValue (name "?{concat(<cap>,<!>),<@>}")))
(example (utterance "lines ending with a number after a capital letter") (targetValue (name "?{endwith(<cap>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or no letter") (targetValue (name "?{repeatatleast(<!>,1),not(<let>)}")))
(example (utterance "lines with words that end with a lower-case letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<low>),<!>}")))
(example (utterance "lines having either a character , string leftquoatation ! rightquoatation , or number before letter") (targetValue (name "?{or(or(<any>,<!>),<num>),<let>}")))
(example (utterance "lines with lower-case letter at least once and not a letter") (targetValue (name "?{repeatatleast(<!>,4)}")))
(example (utterance "lines without a vowel before leftquoatation ! rightquoatation") (targetValue (name "?{not(<vow>),<!>}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation nor a character") (targetValue (name "?{and(<!>,<any>)}")))
(example (utterance "lines with words starting in 2 or more characters") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines with a number after a vowel or a lower-case letter") (targetValue (name "?{or(<vow>,<low>),<num>}")))
(example (utterance "lines ending with a capital letter coming before a lower-case letter") (targetValue (name "?{endwith(<cap>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a lower-case , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{or(<!>,<low>),and(<@>,<#>)}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , and a letter") (targetValue (name "?{and(and(<vow>,<!>),<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before lower-case letter then string leftquoatation # rightquoatation") (targetValue (name "?{and(<!>,<@>),concat(<low>,<#>)}")))
(example (utterance "lines that contain a lower-case letter followed by a letter , at least 6 times") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines with words and 3 or more of leftquoatation ! rightquoatation with string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,3),<@>}")))
(example (utterance "lines with either leftquoatation ! rightquoatation before a character , or leftquoatation @ rightquoatation before a vowel or lower-case letter") (targetValue (name "?{concat(<!>,<any>),or(or(<@>,<vow>),<low>)}")))
(example (utterance "lines ending with words with 7 or more of string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,7)}")))
(example (utterance "lines with leftquoatation ! rightquoatation coming before a vowel , leftquoatation @ rightquoatation , or a character") (targetValue (name "?{<!>,or(or(<vow>,<@>),<any>)}")))
(example (utterance "lines with words of either leftquoatation ! rightquoatation , a vowel , or number") (targetValue (name "?{or(<!>,<vow>),<num>}")))
(example (utterance "lines which are not starting with leftquoatation ! rightquoatation or a number") (targetValue (name "?{startwith(or(<!>,<num>))}")))
(example (utterance "lines containing either string leftquoatation ! rightquoatation , lower-case letter , or a character") (targetValue (name "?{or(or(<!>,<low>),<any>)}")))
(example (utterance "lines without a lower case letter  , at least 4 times before a capital letter") (targetValue (name "?{not(repeatatleast(<low>,4)),<cap>}")))
(example (utterance "items terminating with a small letter preceding an upper case letter .") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines with letter followed by character before lower-case letter") (targetValue (name "?{concat(<let>,<any>),<low>}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{<num>,repeatatleast(<!>,6)}")))
(example (utterance "lines with a letter or lower-case letter , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<let>),or(or(<low>,<vow>),<!>)}")))
(example (utterance "items with an upper case letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{<cap>,endwith(<!>)}")))
(example (utterance "lines with a lower-case letter before character") (targetValue (name "?{and(<let>,<low>),<any>}")))
(example (utterance "lines with words with a capital , a number and characters") (targetValue (name "?{and(<cap>,<num>),<any>}")))
(example (utterance "lines ending in 3 or more of a number or string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<num>),repeatatleast(<!>,3)}")))
(example (utterance "lines that end with a vowel , a number , and a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(and(and(<vow>,<num>),<cap>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and 0 or more lower-case letters") (targetValue (name "?{<!>,star(<low>)}")))
(example (utterance "lines with words having leftquoatation ! rightquoatation or leftquoatation @ rightquoatation at least twice") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with a letter before a lower-case letter and character") (targetValue (name "?{<let>,and(<low>,<any>)}")))
(example (utterance "lines with a lower-case letter followed by a number before anything not being leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<num>),not(<!>)}")))
(example (utterance "lines not ending with number , 4 or more times") (targetValue (name "?{endwith(repeatatleast(<num>,4))}")))
(example (utterance "items with a character preceding a number preceding leftquoatation ! rightquoatation .") (targetValue (name "?{<any>,concat(<num>,<!>)}")))
(example (utterance "lines not having lower case letter , not leftquoatation ! rightquoatation , and not a number") (targetValue (name "?{and(and(<low>,<!>),<num>)}")))
(example (utterance "lines starting with either a number or a capital letter followed by leftquoatation ! rightquoatation 4 times") (targetValue (name "?{or(<num>,concat(<cap>,<!>))}")))
(example (utterance "lines contain a lower-case letter and 4 or more characters") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines without a number before string leftquoatation ! rightquoatation then vowel") (targetValue (name "?{concat(<num>,concat(<!>,<vow>))}")))
(example (utterance "lines beginning with words , leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a letter") (targetValue (name "?{and(and(<!>,<let>),<@>)}")))
(example (utterance "lines with either a letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation followed by a capital letter") (targetValue (name "?{or(or(<let>,<!>),<@>),<cap>}")))
(example (utterance "items with a letter preceding an upper case letter at least6times .") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation or a vowel before leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<vow>),star(<@>)}")))
(example (utterance "lines with 3 or more of a character and string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<any>),repeatatleast(<!>,3)}")))
(example (utterance "lines starting with a lower-case letter before a character or vowel") (targetValue (name "?{or(concat(<low>,<any>),<vow>)}")))
(example (utterance "lines with words that have a capital letter coming after a lower-case letter") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines that have a vowel followed by leftquoatation ! rightquoatation or no or more characters") (targetValue (name "?{<vow>,star(or(<!>,<any>))}")))
(example (utterance "lines with words containing a vowel , a character and a number") (targetValue (name "?{and(<vow>,<any>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter and a capital letter in them") (targetValue (name "?{<!>,and(<low>,<cap>)}")))
(example (utterance "lines that have a lower-case letter at the start") (targetValue (name "?{<low>}")))
(example (utterance "lines with a vowel before leftquoatation ! rightquoatation at least zero times") (targetValue (name "?{<!>,star(<vow>)}")))
(example (utterance "lines with lower-case letter before character , letter , and number") (targetValue (name "?{<low>,and(and(<any>,<let>),<num>)}")))
(example (utterance "lines containing a capital letter , leftquoatation ! rightquoatation , and a lower-case letter preceding a number") (targetValue (name "?{and(and(<cap>,<!>),<low>),<num>}")))
(example (utterance "lines leftquoatation ! rightquoatation , 4 times before vowel") (targetValue (name "?{repeatatleast(<!>,4),<vow>}")))
(example (utterance "lines with a capital letter followed by a lower-case letter") (targetValue (name "?{<cap>,<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a vowel , or a letter") (targetValue (name "?{or(or(<!>,<vow>),<let>)}")))
(example (utterance "lines not having a character before leftquoatation ! rightquoatation followed by a vowel in them") (targetValue (name "?{not(<any>),concat(<!>,<vow>)}")))
(example (utterance "lines not having leftquoatation ! rightquoatation , and not a letter") (targetValue (name "?{and(and(<num>,<!>),<let>)}")))
(example (utterance "the only string leftquoatation ! rightquoatation appears zero or more times before a capital letter in the lines") (targetValue (name "?{star(<!>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by number preceding vowel") (targetValue (name "?{concat(<!>,<num>),<vow>}")))
(example (utterance "lines with a capital or lower-case letter or string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{or(or(<cap>,<!>),<low>),<vow>}")))
(example (utterance "lines with a lower-case or capital letter") (targetValue (name "?{or(<low>,<cap>)}")))
(example (utterance "lines with less than 7 characters") (targetValue (name "?{repeatatleast(<any>,7)}")))
(example (utterance "lines having either leftquoatation ! rightquoatation , a letter , or leftquoatation @ rightquoatation leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<let>),<@>),<#>}")))
(example (utterance "lines with words without a lower-case letter") (targetValue (name "?{not(<low>)}")))
(example (utterance "lines having a vowel followed by either leftquoatation ! rightquoatation , a lower-case letter , or leftquoatation @ rightquoatation") (targetValue (name "?{<vow>,or(or(<!>,<low>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation") (targetValue (name "?{star(<vow>),endwith(<!>)}")))
(example (utterance "lines containing a lower-case letter , a character , and a letter") (targetValue (name "?{and(<let>,<any>),<low>}")))
(example (utterance "lines starting with zero or more of a capital letter before a lower-case letter") (targetValue (name "?{startwith(concat(<cap>,<low>))}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and capital letter") (targetValue (name "?{<vow>,and(and(<!>,<cap>),<@>)}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation ending in lower-case letter") (targetValue (name "?{concat(<any>,<!>),endwith(<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a capital and lower-case letter before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<cap>),<low>),<@>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation or a number 3 times") (targetValue (name "?{repeatatleast(or(<!>,<num>),3)}")))
(example (utterance "lines with a lower-case letter") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines ending in words with a character and letter") (targetValue (name "?{and(<any>,<let>)}")))
(example (utterance "lines containing either lower-case letter , a letter , or character") (targetValue (name "?{or(or(<low>,<let>),<any>)}")))
(example (utterance "lines starting in 3 or more of a character or lower-case letter") (targetValue (name "?{repeatatleast(or(<any>,<low>),3)}")))
(example (utterance "lines containing words with a vowel or a captial letter") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a letter preceding an upper case letter .") (targetValue (name "?{or(or(<@>,<!>),<let>),<cap>}")))
(example (utterance "lines with lower-case letter followed by a capital , 5 or more times") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines with words ending in a character before string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<any>),<!>}")))
(example (utterance "items with words containing a character , an upper case letter , and a vowel .") (targetValue (name "?{and(<vow>,<cap>),<any>}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation at least 3 times") (targetValue (name "?{repeatatleast(<!>,3)}")))
(example (utterance "lines ending with 5 or more of acharacter before a letter") (targetValue (name "?{endwith(<any>),repeatatleast(<let>,5)}")))
(example (utterance "lines with a capital or string leftquoatation ! rightquoatation followed by number at least once") (targetValue (name "?{or(<cap>,concat(<!>,<num>))}")))
(example (utterance "lines with a number before a lower-case 6 times") (targetValue (name "?{<num>,repeatatleast(<low>,6)}")))
(example (utterance "lines with a vowel before the letter with a capital letter .") (targetValue (name "?{and(<cap>,<vow>),<let>}")))
(example (utterance "lines with a capital letter before a number at least 4 times") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation , vowel , or character") (targetValue (name "?{<cap>,or(or(<!>,<vow>),<any>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation and number") (targetValue (name "?{<!>,contain(<num>)}")))
(example (utterance "items with an upper case letter preceding a numeral , a vowel , and leftquoatation ! rightquoatation .") (targetValue (name "?{<cap>,and(and(<num>,<!>),<vow>)}")))
(example (utterance "lines that 4 or more times contain either an uppercase or lowercase letter") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines having a lower-case letter , leftquoatation ! rightquoatation , and a character") (targetValue (name "?{and(and(<low>,<!>),<any>)}")))
(example (utterance "lines which are starting with a capital letter or leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{or(<cap>,star(<!>))}")))
(example (utterance "lines with words , a number and leftquoatation ! rightquoatation 5 times") (targetValue (name "?{<num>,repeatatleast(<!>,5)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<num>,or(<!>,<@>)}")))
(example (utterance "items with words contating a vowel preceding an upper case letter .") (targetValue (name "?{<vow>,<cap>}")))
(example (utterance "lines starting with a letter with leftquoatation ! rightquoatation") (targetValue (name "?{<let>,startwith(<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding an upper case letter at least 1 time .") (targetValue (name "?{concat(<!>,<vow>),repeatatleast(<cap>,1)}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation before a number then capital letter") (targetValue (name "?{endwith(concat(<!>,<num>)),<cap>}")))
(example (utterance "lines with words that don't have letter , string leftquoatation ! rightquoatation , or string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<!>),<@>)}")))
(example (utterance "lines with words with a letter , string leftquoatation ! rightquoatation , and lower-case letter") (targetValue (name "?{and(<let>,<!>),<low>}")))
(example (utterance "lines having the leftquoatation ! rightquoatation , lover-case letter , or a vowel before a character") (targetValue (name "?{or(or(<!>,<low>),<vow>),<any>}")))
(example (utterance "items with an upper case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<@>,<cap>),<!>)}")))
(example (utterance "lines with words that contain a lower-case letter , leftquoatation ! rightquoatation , or a vowel") (targetValue (name "?{or(<low>,<!>),<vow>}")))
(example (utterance "lines with a letter or number , 6 or more times") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines with words starting with a number before string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<num>),<!>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{endwith(<!>),<low>}")))
(example (utterance "lines with a number before 2 or more leftquoatation ! rightquoatation") (targetValue (name "?{<num>,repeatatleast(<!>,2)}")))
(example (utterance "lines containing either a character , leftquoatation ! rightquoatation , or a number in them") (targetValue (name "?{or(or(<any>,<!>),<num>)}")))
(example (utterance "lines which are not ending with a character at least once") (targetValue (name "?{endwith(repeatatleast(<any>,1))}")))
(example (utterance "lines without only a lower-case letter without string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<!>)}")))
(example (utterance "lines with a number before a character ending with a lower-case letter") (targetValue (name "?{concat(<num>,<any>),endwith(<low>)}")))
(example (utterance "lines with words and numbers with a vowels") (targetValue (name "?{and(<num>,<vow>)}")))
(example (utterance "lines with 2 or more capital letters before a number") (targetValue (name "?{repeatatleast(<cap>,2),<num>}")))
(example (utterance "lines that start with leftquoatation ! rightquoatation and a capital letter") (targetValue (name "?{startwith(<!>),repeatatleast(<cap>,1)}")))
(example (utterance "lines with words with only a capital letter") (targetValue (name "?{<cap>,<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or either a vowel , a lower-case letter , or leftquoatation @ rightquoatation , 7 or more times in them") (targetValue (name "?{or(<!>,or(or(<vow>,<low>),<@>))}")))
(example (utterance "lines containing a letter 4 or more times") (targetValue (name "?{concat(repeatatleast(<let>,4),<num>)}")))
(example (utterance "lines containing a vowel or a number at least 2 times") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with 6 or more of a vowel before number") (targetValue (name "?{<vow>,repeatatleast(<num>,6)}")))
(example (utterance "lines containing a number or leftquoatation ! rightquoatation 3 or more times") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation or 6 or more letters") (targetValue (name "?{or(contain(<!>),<let>)}")))
(example (utterance "items with a letter , a smal letter , or leftquoatation ! rightquoatation in front of a vowel .") (targetValue (name "?{or(or(<!>,<let>),<low>),<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation not preceded by a character") (targetValue (name "?{not(<any>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a capital letter , 2 or more times in them") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines not containing a lower-case letter , a number , or a letter") (targetValue (name "?{or(or(<low>,<num>),<let>)}")))
(example (utterance "lines that end with a capital letter , at least 1 time") (targetValue (name "?{repeatatleast(<cap>,1)}")))
(example (utterance "lines with words that don't have a vowel zero or more times") (targetValue (name "?{not(<vow>)}")))
(example (utterance "lines without a vowel , leftquoatation ! rightquoatation , or a number") (targetValue (name "?{or(or(<vow>,<!>),<num>)}")))
(example (utterance "lines with words with a number , string leftquoatation ! rightquoatation , and lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<!>),<low>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before letter and capital letter") (targetValue (name "?{<!>,and(<let>,<cap>)}")))
(example (utterance "lines with a vowel , character , or lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<any>),<low>),<!>}")))
(example (utterance "lines with words that end with a character or letter") (targetValue (name "?{endwith(<any>),<let>}")))
(example (utterance "lines containing a lower-case letter or a character preceding leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<any>),<!>}")))
(example (utterance "lines with words zero or more of a letter or vowel") (targetValue (name "?{or(or(<let>,<low>),<vow>)}")))
(example (utterance "lines with 4 or more of leftquoatation ! rightquoatation or with string leftquoatation @ rightquoatation before number") (targetValue (name "?{repeatatleast(<!>,4),concat(<@>,<num>)}")))
(example (utterance "lines not containing lower case letters or having a character  .") (targetValue (name "?{and(<any>,<low>)}")))
(example (utterance "lines that do not contain words with a vowel followed by a letter") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines with 6 or more of a vowel before a lower-case letter") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines with a letter containing a vowel") (targetValue (name "?{repeatatleast(<let>,1),contain(<vow>)}")))
(example (utterance "lines ending with a number after a vowel") (targetValue (name "?{<vow>,endwith(<num>)}")))
(example (utterance "lines without zero or more of only a lower-case letter") (targetValue (name "?{star(<low>)}")))
(example (utterance "lines that have a character , a lower-case letter , and leftquoatation ! rightquoatation , all before a capital letter") (targetValue (name "?{and(and(<any>,<!>),<low>),<cap>}")))
(example (utterance "items with a character preceding a letter at least6times .") (targetValue (name "?{<any>,repeatatleast(<let>,6)}")))
(example (utterance "lines that contain the word leftquoatation ! rightquoatation followed by the word leftquoatation @ rightquoatation , a lower-case letter , and the word leftquoatation # rightquoatation") (targetValue (name "?{<!>,and(and(<@>,<low>),<#>)}")))
(example (utterance "lines with a vowel before character and a letter") (targetValue (name "?{concat(<vow>,and(<any>,<let>))}")))
(example (utterance "lines with only a lower-case letter or vowel") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines having a character , lower-case letter , and letter before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<low>),<let>),<!>}")))
(example (utterance "lines without a lower-case letter without string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{not(and(<low>,<!>)),<vow>}")))
(example (utterance "lines not having a letter , a lower-case letter , and not a vowel") (targetValue (name "?{and(and(<let>,<low>),<vow>)}")))
(example (utterance "lines having a lower-case letter , string leftquoatation ! rightquoatation , and a letter before a vowel") (targetValue (name "?{and(and(<low>,<!>),<let>),<vow>}")))
(example (utterance "lines with number or capital letter before character") (targetValue (name "?{or(<num>,<cap>),<any>}")))
(example (utterance "lines which are ending with containing a number and leftquoatation ! rightquoatation") (targetValue (name "?{contain(and(<num>,<!>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter , number , or string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<cap>,<num>),<@>)}")))
(example (utterance "items without leftquoatation ! rightquoatation and no character .") (targetValue (name "?{and(repeatatleast(<!>,1),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before  a vowel then capital letter , string leftquoatation ! rightquoatation , and lower-case letter") (targetValue (name "?{concat(<!>,<vow>),and(and(<cap>,<@>),<low>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation and number before string leftquoatation @ rightquoatation") (targetValue (name "?{contain(and(<!>,<num>)),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation before a character") (targetValue (name "?{concat(<!>,<@>),<any>}")))
(example (utterance "lines without words , string leftquoatation ! rightquoatation , or vowel with a letter") (targetValue (name "?{and(and(<let>,<!>),<vow>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation preceded by an uppercase letter") (targetValue (name "?{endwith(<cap>),<!>}")))
(example (utterance "lines not having letter , 7 or more times before character") (targetValue (name "?{concat(repeatatleast(<let>,7),<any>)}")))
(example (utterance "lines not having leftquoatation ! rightquoatation , leftquoatation @ rightquoatation and letter") (targetValue (name "?{or(<!>,and(<@>,<let>))}")))
(example (utterance "lines containing words with a letter before a number") (targetValue (name "?{star(<let>),<num>}")))
(example (utterance "lines with lower-case letter followed by string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before string leftquoatation # rightquoatation") (targetValue (name "?{concat(<low>,<!>),concat(<@>,<#>)}")))
(example (utterance "lines with any letter followed by a lower-case letter before a vowel .") (targetValue (name "?{concat(<let>,<low>),<vow>}")))
(example (utterance "lines having a lower-case letter , string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a character") (targetValue (name "?{and(and(<low>,<!>),<any>),<@>}")))
(example (utterance "lines ending with a lower-case letter followed by a number in them") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "lines whose words must have leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation or with a capital letter") (targetValue (name "?{or(<!>,contain(<cap>))}")))
(example (utterance "lines with either leftquoatation ! rightquoatation or a number after a capital letter") (targetValue (name "?{<cap>,or(<num>,<!>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation being followed by any letter") (targetValue (name "?{endwith(<!>),concat(<@>,<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , an upper case letter , or a letter preceding a numeral .") (targetValue (name "?{or(or(<cap>,<!>),<let>),<num>}")))
(example (utterance "lines that include leftquoatation ! rightquoatation plus a , e , i , o or u") (targetValue (name "?{and(<!>,<vow>)}")))
(example (utterance "lines  having no lower-case letter and character") (targetValue (name "?{and(<low>,<any>)}")))
(example (utterance "lines not having any of the following - leftquoatation ! rightquoatation , a vowel , or a capital letter") (targetValue (name "?{or(or(<!>,<vow>),<cap>)}")))
(example (utterance "lines without a capital letter and leftquoatation ! rightquoatation") (targetValue (name "?{not(and(<cap>,<!>))}")))
(example (utterance "lines with words , string leftquoatation ! rightquoatation , capital letter , character") (targetValue (name "?{and(<!>,<cap>),<any>}")))
(example (utterance "lines which are containing a vowel and a capital letter") (targetValue (name "?{and(<vow>,<cap>)}")))
(example (utterance "lines without zero or more of a vowel with a capital letter") (targetValue (name "?{not(<vow>),star(<cap>)}")))
(example (utterance "the lines that begin with a letter coming before a capital letter") (targetValue (name "?{startwith(<let>),<cap>}")))
(example (utterance "lines with words containing a number before a character") (targetValue (name "?{<num>,<any>}")))
(example (utterance "lines containing a character before a number and a letter") (targetValue (name "?{<any>,and(<num>,<let>)}")))
(example (utterance "lines contains only a letter or a vowel") (targetValue (name "?{<let>,<vow>}")))
(example (utterance "items with a small letter , a vowel , or leftquoatation ! rightquoatation in front of leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<!>,<vow>),<low>),<@>}")))
(example (utterance "the lines beginning with a vowel") (targetValue (name "?{startwith(<vow>)}")))
(example (utterance "lines with a vowel and letter before character") (targetValue (name "?{and(<vow>,<let>),<any>}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation , a capital letter , or a number preceding the word leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<num>),<cap>),<@>}")))
(example (utterance "lines with a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines not being words with a vowel") (targetValue (name "?{repeatatleast(<vow>,1)}")))
(example (utterance "lines not having a character followed by a capital") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or lower-case letter , 5 or more times") (targetValue (name "?{repeatatleast(or(<!>,<low>),5)}")))
(example (utterance "items without a terminating number .") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines ending with containing string leftquoatation ! rightquoatation and character") (targetValue (name "?{contain(and(<!>,<any>))}")))
(example (utterance "first a vowel then a character in lines") (targetValue (name "?{startwith(<vow>),<any>}")))
(example (utterance "lines with a letter before string leftquoatation ! rightquoatation and a capital , 3 or more times") (targetValue (name "?{concat(<let>,<!>),repeatatleast(<cap>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a number followed by a lower-case letter") (targetValue (name "?{<low>,or(<!>,<num>)}")))
(example (utterance "lines containing number , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<num>,<!>),<@>)}")))
(example (utterance "lines with a 6 or more letters") (targetValue (name "?{or(<cap>,<let>)}")))
(example (utterance "lines ending in lower-case letter after a letter and leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<let>),and(<low>,<!>)}")))
(example (utterance "lines with 1 or more leftquoatation ! rightquoatation or a letter") (targetValue (name "?{repeatatleast(<!>,1),<let>}")))
(example (utterance "lines with words with a vowel , leftquoatation ! rightquoatation , and a lower-case letter in it") (targetValue (name "?{and(<vow>,<!>),<low>}")))
(example (utterance "lines where vowel , letters , and lower-case letters come after a number") (targetValue (name "?{<num>,or(or(<vow>,<low>),<let>)}")))
(example (utterance "items with words containing a numeral , leftquoatation ! rightquoatation , and a character .") (targetValue (name "?{and(<any>,<num>),<!>}")))
(example (utterance "lines not having leftquoatation ! rightquoatation or a vowel and a letter") (targetValue (name "?{and(or(<!>,<vow>),<let>)}")))
(example (utterance "lines do not contain a capital letter , a letter , nor leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<let>),<!>)}")))
(example (utterance "lines with 1 or more character , or ending with leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,1),endwith(<!>)}")))
(example (utterance "lines containing a lower-case letter or number") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines with words that contain character and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<!>)}")))
(example (utterance "lines with a lower-case letter , number , or character before string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<num>),<any>),<!>}")))
(example (utterance "lines with a capital letter or a number preceding a character") (targetValue (name "?{concat(or(<cap>,<num>),<any>)}")))
(example (utterance "lines having words containing a number followed by leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{concat(<num>,<!>),<@>}")))
(example (utterance "lines where at least once there is a character or uppercase letter") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "lines ending in a number and string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<num>),concat(<!>,<@>)}")))
(example (utterance "lines with number before lower-case letter or a capital letter") (targetValue (name "?{<num>,or(<low>,<cap>)}")))
(example (utterance "lines having a letter before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{<let>,and(<!>,<@>)}")))
(example (utterance "lines not having character , zero or more times before lower-case letter") (targetValue (name "?{concat(star(<any>),<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation ending in leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<@>),contain(<!>)}")))
(example (utterance "ines with words with leftquoatation ! rightquoatation followed by a vowel and leftquoatation @ rightquoatation in them") (targetValue (name "?{concat(<!>,<vow>),<@>}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{not(concat(<!>,<@>))}")))
(example (utterance "items with either leftquoatation ! rightquoatation , a numeral , or a character in front of a vowel .") (targetValue (name "?{or(or(<any>,<!>),<num>),<vow>}")))
(example (utterance "lines without a capital letter , leftquoatation ! rightquoatation , and a number") (targetValue (name "?{and(and(<cap>,<!>),<num>)}")))
(example (utterance "no minimum of times , lines with a capital letter before a character") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{<!>,contain(endwith(<low>))}")))
(example (utterance "lines starting in 3 or more of a number or string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(or(<num>,<!>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a letter") (targetValue (name "?{<!>,<let>}")))
(example (utterance "lines with a character before a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<any>,<let>),<!>)}")))
(example (utterance "lines which are ending with words with a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<!>)}")))
(example (utterance "lines containing either a letter , character , or a capital") (targetValue (name "?{or(or(<let>,<any>),<cap>)}")))
(example (utterance "lines with words without a vowel or character") (targetValue (name "?{not(<vow>),<any>}")))
(example (utterance "lines with no capital letters") (targetValue (name "?{or(or(<cap>,<vow>),<any>)}")))
(example (utterance "lines containing a letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation after leftquoatation # rightquoatation") (targetValue (name "?{<#>,or(or(<let>,<!>),<@>)}")))
(example (utterance "lines having a letter or a character") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation and a letter in them") (targetValue (name "?{<!>,<let>}")))
(example (utterance "lines containing a numeral preceding an alphabet character") (targetValue (name "?{<num>,<let>}")))
(example (utterance "lines with a vowel before lower-case letter and character") (targetValue (name "?{<vow>,and(<low>,<any>)}")))
(example (utterance "items with a numeral and vowel preceding a letter .") (targetValue (name "?{and(<num>,<vow>),<let>}")))
(example (utterance "lines with words containing a letter , a character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<!>),<let>}")))
(example (utterance "lines beginning with a lower case letter with  leftquoatation ! rightquoatation following") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines without a letter and only a character") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with a number and capital letter before a lower-case letter") (targetValue (name "?{and(and(<num>,<cap>),<let>),<low>}")))
(example (utterance "lines containing a letter followed by a capital letter or a vowel") (targetValue (name "?{<let>,or(<cap>,<vow>)}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation and a lower-case letter") (targetValue (name "?{and(<!>,<low>)}")))
(example (utterance "lines with a character , leftquoatation ! rightquoatation , and a number") (targetValue (name "?{and(and(<any>,<!>),<num>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation before capital letter") (targetValue (name "?{<!>,star(<cap>)}")))
(example (utterance "items with a small letter or vowel .") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation a letter , or leftquoatation @ rightquoatation") (targetValue (name "?{<any>,or(or(<!>,<let>),<@>)}")))
(example (utterance "lines with a number not before a letter") (targetValue (name "?{<num>,not(contain(<let>))}")))
(example (utterance "items with a numeral or vowel .") (targetValue (name "?{and(<vow>,<num>)}")))
(example (utterance "lines with words that don't containing leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation at least 7 times and leftquoatation @ rightquoatation followed by a character") (targetValue (name "?{repeatatleast(<!>,7),concat(<@>,<any>)}")))
(example (utterance "lines that open with words containing only a character") (targetValue (name "?{<any>}")))
(example (utterance "lines with 2 or more of string leftquoatation ! rightquoatation before non string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,2),not(<@>)}")))
(example (utterance "lines with a vowel or lower-case letter") (targetValue (name "?{or(<vow>,<let>),<low>}")))
(example (utterance "lines with  number before a capital letter , 7 or more times") (targetValue (name "?{<num>,repeatatleast(<cap>,7)}")))
(example (utterance "lines that start with 5 or more capital letters or leftquoatation ! rightquoatation") (targetValue (name "?{or(repeatatleast(<cap>,5),<!>)}")))
(example (utterance "lines without words with a vowel or capital letter") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "lines with words , leftquoatation ! rightquoatation , a lower-case letter , and 'truck") (targetValue (name "?{and(and(<!>,<low>),<@>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation and a lower-case letter in them") (targetValue (name "?{<num>,and(<!>,<low>)}")))
(example (utterance "lines containg leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a character , and a capital letter at least 7 times") (targetValue (name "?{repeatatleast(<!>,7),and(and(<@>,<any>),<cap>)}")))
(example (utterance "lines with words , a character , capital letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<cap>),<!>}")))
(example (utterance "items with words containing an upper case letter at least 1 time .") (targetValue (name "?{<cap>}")))
(example (utterance "lines with words having a lower-case letter , a character with a letter") (targetValue (name "?{and(<low>,<any>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a character  , letter , and a number") (targetValue (name "?{and(and(<any>,<let>),<num>),<!>}")))
(example (utterance "lines with a character or 7 or more of a number or letter") (targetValue (name "?{or(<any>,or(or(<num>,<low>),<let>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before words which are starting with a capital letter") (targetValue (name "?{<!>,startwith(<cap>)}")))
(example (utterance "lines with words containing either a letter , leftquoatation ! rightquoatation , or a vowel and a lower-case letter") (targetValue (name "?{or(or(<let>,<!>),<vow>),<low>}")))
(example (utterance "lines with 3 or more capital letters or letters") (targetValue (name "?{or(<cap>,<let>)}")))
(example (utterance "lines with 3 or more of a  lower-case letter or character") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines with a vowel preceded by a number or a character") (targetValue (name "?{or(<num>,<any>),<vow>}")))
(example (utterance "lines ending in 7 or more vowels") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines containing 'dog , a letter , or truck before a number") (targetValue (name "?{or(or(<!>,<let>),<@>),<num>}")))
(example (utterance "lines containing a character preceded by leftquoatation ! rightquoatation") (targetValue (name "?{<!>,<any>}")))
(example (utterance "lines without leftquoatation ! rightquoatation , a number , or a capital") (targetValue (name "?{or(or(<!>,<num>),<cap>)}")))
(example (utterance "lines with words that do not contain a number , a character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<!>,<num>),<any>)}")))
(example (utterance "lines not containing a lower-case letter , capital letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<cap>),<!>)}")))
(example (utterance "lines with number , 2 or more times before character") (targetValue (name "?{repeatatleast(<num>,2),<any>}")))
(example (utterance "lines that do not contain a character , a vowel , or a letter") (targetValue (name "?{or(or(<vow>,<any>),<let>)}")))
(example (utterance "items with an upper case letter preceding a vowel or a number .") (targetValue (name "?{<cap>,or(<num>,<vow>)}")))
(example (utterance "pet specialty stores sell alot of items your dog will need such as dog beds , dog sweaters , dog food , dog treats , and dog toys .") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with a character and leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{and(<any>,<!>),<low>}")))
(example (utterance "lines without a capital letter , not being a number , and not being leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<num>),<!>)}")))
(example (utterance "lines with number , 2 or more times and ending with string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,2),endwith(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),contain(<@>)}")))
(example (utterance "lines with a capital letter , or a letter , 4 times .") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines starting with character before lower-case letter or letter") (targetValue (name "?{or(concat(<any>,<low>),<let>)}")))
(example (utterance "a line with a letter after a number") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number then a character") (targetValue (name "?{concat(<!>,<num>),<any>}")))
(example (utterance "lines with 3 or more of a lower-case letter before character") (targetValue (name "?{<low>,repeatatleast(<any>,3)}")))
(example (utterance "lines that don't have a capital letter before a number") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or stating in a vowel") (targetValue (name "?{not(<!>),startwith(<vow>)}")))
(example (utterance "lines with zero or less string leftquoatation ! rightquoatation or character") (targetValue (name "?{or(not(<!>),<any>)}")))
(example (utterance "lines starting with number at least once before vowel") (targetValue (name "?{repeatatleast(startwith(<num>),1),<vow>}")))
(example (utterance "lines including a letter , a number , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<num>),<!>)}")))
(example (utterance "lines containing a letter and a vowel preceding leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<vow>),<!>}")))
(example (utterance "lines with the following strings in the following order:  leftquoatation ! rightquoatation leftquoatation @ rightquoatation and ring , zero or more times") (targetValue (name "?{concat(<!>,<@>),star(<#>)}")))
(example (utterance "lines that have words containing a number at least6times") (targetValue (name "?{<num>}")))
(example (utterance "lines that do not contain the straing leftquoatation ! rightquoatation or a letter") (targetValue (name "?{and(<let>,<!>)}")))
(example (utterance "lines containing ending with a character at least 1 time") (targetValue (name "?{repeatatleast(endwith(<let>),1)}")))
(example (utterance "lines with words with either a capital letter , a lower-case letter , or leftquoatation ! rightquoatation in it") (targetValue (name "?{or(<cap>,<low>),<!>}")))
(example (utterance "lines containing a character , leftquoatation ! rightquoatation , and a letter in them") (targetValue (name "?{and(and(<any>,<!>),<let>)}")))
(example (utterance "lines which are having a letter , leftquoatation ! rightquoatation , and a character before a lower-case letter") (targetValue (name "?{and(and(<let>,<!>),<any>),<low>}")))
(example (utterance "ends with a capital letter after a character") (targetValue (name "?{endwith(<any>),<cap>}")))
(example (utterance "lines ending in character before string leftquoatation ! rightquoatation") (targetValue (name "?{contain(concat(<any>,<!>))}")))
(example (utterance "lines that start with a vowel or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<vow>),concat(<!>,<@>)}")))
(example (utterance "lines with a capital or lower-case letter") (targetValue (name "?{or(contain(<cap>),<low>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation , leftquoatation # rightquoatation , and a vowel .") (targetValue (name "?{<!>,and(and(<@>,<#>),<vow>)}")))
(example (utterance "lines containing a vowel preceded by leftquoatation ! rightquoatation") (targetValue (name "?{<!>,contain(<vow>)}")))
(example (utterance "lines with number or vowel , 4 or more times") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a vowel preceding a small letter .") (targetValue (name "?{concat(<!>,<vow>),<low>}")))
(example (utterance "lines that don't have a lower case letter before leftquoatation ! rightquoatation") (targetValue (name "?{not(<low>),<!>}")))
(example (utterance "lines with a capital letter , number , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<num>),<!>)}")))
(example (utterance "lines with a lower-case letter preceded at least once by a letter") (targetValue (name "?{repeatatleast(<let>,1),<low>}")))
(example (utterance "lines with the words that contain ending with a letter") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , a capital letter , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<cap>),<@>)}")))
(example (utterance "lines that do not contain a letter but contain leftquoatation ! rightquoatation") (targetValue (name "?{not(<let>),contain(<!>)}")))
(example (utterance "lines with a character , a lower-case letter or a letter") (targetValue (name "?{<any>,or(<low>,<let>)}")))
(example (utterance "lines that must not have the word leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines with a number and lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<low>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a vowel with a letter") (targetValue (name "?{concat(<!>,<let>),<vow>}")))
(example (utterance "lines with a letter , number , or vowel") (targetValue (name "?{or(or(<let>,<num>),<vow>)}")))
(example (utterance "lines without  words with a vowel , not leftquoatation ! rightquoatation , and not a number") (targetValue (name "?{and(and(<vow>,<!>),<num>)}")))
(example (utterance "lines not contain a number nor a letter") (targetValue (name "?{not(<num>),contain(<let>)}")))
(example (utterance "lines not ending with string leftquoatation ! rightquoatation followed by letter") (targetValue (name "?{endwith(concat(<!>,<let>))}")))
(example (utterance "items with words containing a numeral , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation .") (targetValue (name "?{and(<num>,<!>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines that start with 6 or more instances of leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation succeeded by an alphabet character , at least6occurrences") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation following leftquoatation @ rightquoatation at least 5 times") (targetValue (name "?{concat(<@>,<!>)}")))
(example (utterance "lines which are ending with a vowel , 2 or more times before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(endwith(<vow>),2),<!>}")))
(example (utterance "lines containing a vowel preceding leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a capital letter") (targetValue (name "?{<vow>,and(and(<!>,<@>),<cap>)}")))
(example (utterance "items with a letter preceding a character .") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or number before a letter") (targetValue (name "?{or(<!>,<num>),<let>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation before a lower-case lettere") (targetValue (name "?{startwith(<!>),repeatatleast(<low>,1)}")))
(example (utterance "lines that have a vowel or lower-case letter before the word leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<low>),<!>}")))
(example (utterance "lines with a lower-case letter after a letter and a capital letter") (targetValue (name "?{and(<let>,<cap>),<low>}")))
(example (utterance "lines with character before vowel followed by number") (targetValue (name "?{<any>,concat(<vow>,<num>)}")))
(example (utterance "lines with words with only zero or more characters") (targetValue (name "?{<any>}")))
(example (utterance "lines with a character , 6 or more times and not a capital") (targetValue (name "?{repeatatleast(<any>,6),not(<cap>)}")))
(example (utterance "lines that have only the word leftquoatation ! rightquoatation and any number") (targetValue (name "?{and(<!>,<num>)}")))
(example (utterance "lines with words and either a letter , number , or character") (targetValue (name "?{or(<let>,<num>),<any>}")))
(example (utterance "2 or more lines with a character followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines with words with no lower-case letter or vowel") (targetValue (name "?{and(<low>,<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(<!>,repeatatleast(<any>,1))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation following by leftquoatation @ rightquoatation before a character once or more times") (targetValue (name "?{<!>,repeatatleast(concat(<@>,<any>),1)}")))
(example (utterance "lines with a capital letter and a character after a vowel") (targetValue (name "?{<vow>,and(<cap>,<any>)}")))
(example (utterance "lines with a number or leftquoatation ! rightquoatation and a character , 4 or more times in them") (targetValue (name "?{or(<num>,and(<!>,<any>))}")))
(example (utterance "items with a vowel , a letter , or a character .") (targetValue (name "?{or(<let>,<any>),<vow>}")))
(example (utterance "lines that have 4 or more uppercase letters") (targetValue (name "?{repeatatleast(<cap>,4)}")))
(example (utterance "lines without words and only a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines starting with either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a number") (targetValue (name "?{or(or(<!>,<@>),<num>)}")))
(example (utterance "lines that have a vowel or a character have a number before them") (targetValue (name "?{<num>,and(<any>,<vow>)}")))
(example (utterance "lines that have a character in front of the leftquoatation ! rightquoatation string and the number") (targetValue (name "?{<any>,and(<!>,<num>)}")))
(example (utterance "lines starting with not a character or a capital") (targetValue (name "?{or(not(<any>),<cap>)}")))
(example (utterance "lines with at least 1 letter before a leftquoatation ! rightquoatation string and a letter that's upper case") (targetValue (name "?{<let>,and(<!>,<cap>)}")))
(example (utterance "no number , capital letter or leftquoatation ! rightquoatation can be contained in the line .") (targetValue (name "?{or(or(<num>,<cap>),<!>)}")))
(example (utterance "lines that contain only leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with words and a number with a character") (targetValue (name "?{and(<num>,<any>)}")))
(example (utterance "lines with lower-case letter or character , 5 or more times") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines with a number or a lower-case letter at least once in it") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "lines where a capital levtter followed by a vowel is not present") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , and which don't have a capital letter5times or more") (targetValue (name "?{and(repeatatleast(<cap>,5),<!>)}")))
(example (utterance "lines that do not have leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<#>)}")))
(example (utterance "lines not starting with lower-case letter at least once") (targetValue (name "?{startwith(repeatatleast(<low>,1))}")))
(example (utterance "lines without only leftquoatation ! rightquoatation without string leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "lines with 2 or more letter before a character") (targetValue (name "?{repeatatleast(<let>,2),<any>}")))
(example (utterance "lines with a character/vowel before small letter") (targetValue (name "?{or(<any>,<vow>),<low>}")))
(example (utterance "lines in which a capital letter is present followed by a vowel .") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "items with words containing small letter preceding a character .") (targetValue (name "?{<low>,<any>}")))
(example (utterance "lines end with a capital letter and a character , then followed by leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<cap>),concat(<any>,<!>)}")))
(example (utterance "lines not ending with string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines containing a letter followed by 3 or more characters") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with a vowel , lower-case letter and then a capital letter") (targetValue (name "?{concat(<vow>,<low>),<cap>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or without a lower-case letter or without a letter") (targetValue (name "?{or(or(<!>,<low>),<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a vowel , and a character") (targetValue (name "?{and(and(<!>,<vow>),<any>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation before capital letter at least once") (targetValue (name "?{contain(<!>),repeatatleast(<cap>,1)}")))
(example (utterance "lines with a letter or number before lower-case letter") (targetValue (name "?{or(<let>,<num>),<low>}")))
(example (utterance "lines having either a letter , number , or string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<num>),<!>),<@>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation , number , or letter") (targetValue (name "?{or(<!>,<num>),<let>}")))
(example (utterance "lines with 6 or more capital letters before a letter") (targetValue (name "?{repeatatleast(<cap>,6),<let>}")))
(example (utterance "lines with a vowel and character , string leftquoatation ! rightquoatation , or lower-case letter") (targetValue (name "?{<vow>,or(or(<any>,<!>),<low>)}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation and 'ring and a number") (targetValue (name "?{endwith(<!>),and(and(<@>,<num>),<#>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before a lower-case letter , number , or capital letter") (targetValue (name "?{<!>,or(or(<low>,<num>),<cap>)}")))
(example (utterance "lines that have a number followed by a vowel or capital letter") (targetValue (name "?{<num>,or(<vow>,<cap>)}")))
(example (utterance "lines with vowels and letters more than 6 times .") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines where a letter in lower-case comes before either a vowel or a character") (targetValue (name "?{<low>,or(<any>,<vow>)}")))
(example (utterance "items with a numeral , a letter , and leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<!>,<let>),<num>),<@>}")))
(example (utterance "items without leftquoatation ! rightquoatation , upper case letter , or character .") (targetValue (name "?{and(and(<!>,<any>),<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation or number at least once") (targetValue (name "?{concat(<!>,<@>),repeatatleast(<num>,1)}")))
(example (utterance "lines ending with capital letter at least twice") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or letter , 6 or more times") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "items with an upper case letter following a character but preceding a vowel .") (targetValue (name "?{<any>,concat(<cap>,<vow>)}")))
(example (utterance "items with any letter or numeral .") (targetValue (name "?{or(or(<num>,<low>),<cap>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation that occurs at least once before a character") (targetValue (name "?{repeatatleast(<!>,1),<any>}")))
(example (utterance "lines with words that contain a lower-case letter or a character , zero times or more") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines that a number after no or more capital letters") (targetValue (name "?{star(<cap>),<num>}")))
(example (utterance "lines with a number before letter") (targetValue (name "?{startwith(<num>),repeatatleast(<let>,1)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by at least 1 vowel") (targetValue (name "?{contain(concat(<!>,<vow>))}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a small letter , at least twice .") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , a number , and a capital letter in them") (targetValue (name "?{and(<!>,<num>),<cap>}")))
(example (utterance "items with a character preceding a letter at least6times .") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "items with a small letter , at least5times preceding an upper case letter .") (targetValue (name "?{repeatatleast(<low>,5),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a vowel , and a character after leftquoatation @ rightquoatation") (targetValue (name "?{<@>,and(and(<!>,<vow>),<any>)}")))
(example (utterance "lines starting in a character and a vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<any>),or(<vow>,<!>)}")))
(example (utterance "lines that do not contain only a lower-case letter") (targetValue (name "?{<low>}")))
(example (utterance "lines with 3 or more words and lower-case letter") (targetValue (name "?{repeatatleast(<low>,3)}")))
(example (utterance "lines with character or letter before lower-case letter") (targetValue (name "?{or(<any>,<let>),<low>}")))
(example (utterance "lines with either a vowel , a letter , or a number followed by a capital letter") (targetValue (name "?{or(or(<vow>,<let>),<num>),<cap>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation or contain a number at least 6 times") (targetValue (name "?{repeatatleast(<num>,6),<!>}")))
(example (utterance "lines with vowel before capital letter or letter at least once") (targetValue (name "?{or(concat(<vow>,<cap>),<let>)}")))
(example (utterance "lines having a character , a letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<let>),<!>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a capital letter , and a letter") (targetValue (name "?{and(and(<!>,<cap>),<let>)}")))
(example (utterance "lines with a character and only leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , character , and letter") (targetValue (name "?{and(and(<!>,<any>),<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation containing only a lower-case letter") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines containing 5 or more of leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines with a character or lower-case letter") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "lines that have a vowel and either 1 of these words:  leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{repeatatleast(<vow>,1),or(or(<!>,<@>),<#>)}")))
(example (utterance "lines ending with a letter or vowel") (targetValue (name "?{or(repeatatleast(<cap>,1),<vow>)}")))
(example (utterance "lines with a capital letter or leftquoatation ! rightquoatation as a string at least twice") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines with 4 or more of a number or letter") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines with a lower-case letter before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<low>,or(<!>,<@>)}")))
(example (utterance "lines with a character followed by a capital letter and then a number") (targetValue (name "?{concat(<any>,<cap>),<num>}")))
(example (utterance "lines with words starting with a letter") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "lines containing at least 1 of the following: a lower-case letter , a capital letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<cap>),<!>)}")))
(example (utterance "lines with words with either a character , string leftquoatation ! rightquoatation , or a vowel , 6 or more times") (targetValue (name "?{or(or(<any>,<!>),<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation along with a character and a vowel") (targetValue (name "?{and(and(<vow>,<any>),<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , 6 or more times followed by a number") (targetValue (name "?{repeatatleast(<!>,6),<num>}")))
(example (utterance "lines that begin with leftquoatation ! rightquoatation or include leftquoatation @ rightquoatation") (targetValue (name "?{contain(<@>),startwith(<!>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation and letter") (targetValue (name "?{<num>,and(<!>,<let>)}")))
(example (utterance "lines starting in vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(or(<vow>,<!>),1)}")))
(example (utterance "lines 5 or more times with a lower-case letter after a number") (targetValue (name "?{<num>,repeatatleast(<low>,5)}")))
(example (utterance "items with 'dog") (targetValue (name "?{star(<!>),contain(<@>)}")))
(example (utterance "lines with a vowel before a number that contain a dog .") (targetValue (name "?{<vow>,and(and(<num>,<!>),<cap>)}")))
(example (utterance "lines that have at least 3 combinations of a character followed by a lower-case letter") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines with a character after a vowel at least once") (targetValue (name "?{repeatatleast(<vow>,1),<any>}")))
(example (utterance "lines with a number or 6 or more leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines containing a capital letter 4 or more times before a character") (targetValue (name "?{<cap>,repeatatleast(<any>,4)}")))
(example (utterance "lines that have a number or lower case letter before a vowel") (targetValue (name "?{or(<low>,<num>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a character") (targetValue (name "?{<!>,<any>}")))
(example (utterance "lines starting with words that contain only a number in them") (targetValue (name "?{<num>}")))
(example (utterance "lines with words and zero or more of a number or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines with a vowel , a number , and a character all before a capital letter") (targetValue (name "?{and(and(<vow>,<num>),<any>),<cap>}")))
(example (utterance "lines with words and a number , capital letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<cap>),<!>}")))
(example (utterance "items with leftquoatation ! rightquoatation or an uppercase letter or letter preceding a character .") (targetValue (name "?{or(or(<cap>,<!>),<let>),<any>}")))
(example (utterance "lines with letter , 2 or more times or string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<let>,2),concat(<!>,<@>)}")))
(example (utterance "lines without words and starting in number") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "lines with 6 or more characters or lower-case letters") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "lines with letter , zero or more times before ending with number") (targetValue (name "?{star(<let>),endwith(<num>)}")))
(example (utterance "lines with a letter , number , then a vowel .") (targetValue (name "?{<let>,endwith(concat(<num>,<vow>))}")))
(example (utterance "i like to walk my leftquoatation ! rightquoatation on a daily basis") (targetValue (name "?{<low>,<!>}")))
(example (utterance "lines with words and a number , capital letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<cap>),<!>}")))
(example (utterance "lines with a character 6 times before a letter") (targetValue (name "?{repeatatleast(<any>,6),<let>}")))
(example (utterance "items with a vowel , a small letter , and a number .") (targetValue (name "?{and(and(<num>,<low>),<vow>)}")))
(example (utterance "lines containing either a vowel , string leftquoatation ! rightquoatation , or capital") (targetValue (name "?{or(or(<vow>,<!>),<cap>)}")))
(example (utterance "lines which are not ending with leftquoatation ! rightquoatation , 4 or more times") (targetValue (name "?{repeatatleast(endwith(<!>),4)}")))
(example (utterance "lines containing a character , lower-case letter , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<low>),<!>)}")))
(example (utterance "lines with lower-case letter , zero or more times and not a vowel") (targetValue (name "?{star(<low>),not(<vow>)}")))
(example (utterance "lines starting with  lower-case letter before leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{startwith(<low>),and(and(<!>,<@>),<#>)}")))
(example (utterance "lines without a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<low>),<!>)}")))
(example (utterance "items with an upper case letter preceding a number and a small letter .") (targetValue (name "?{<cap>,and(<num>,<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation following a vowel at least 6 times") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines with letter or containing number , 4 or more times") (targetValue (name "?{or(<let>,contain(<num>))}")))
(example (utterance "lines having leftquoatation ! rightquoatation and a letter followed by with or without leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<let>),star(<@>)}")))
(example (utterance "items with a vowel or an upper case letter or leftquoatation ! rightquoatation .") (targetValue (name "?{or(or(<!>,<cap>),<vow>)}")))
(example (utterance "items with a character preceding a vowel or an upper case letter .") (targetValue (name "?{<any>,or(<vow>,<cap>)}")))
(example (utterance "lines that end with a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines having either a character , string leftquoatation ! rightquoatation , or string leftquoatation @ rightquoatation before vowel") (targetValue (name "?{or(or(<any>,<!>),<@>),<vow>}")))
(example (utterance "lines with lower-case letter or character , 7 or more times") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines that have words that contain a small letter 1 time or greater") (targetValue (name "?{<low>}")))
(example (utterance "lines with a number before lower-case letter then string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<low>),not(<!>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation or with a number and vowel") (targetValue (name "?{star(<!>),and(<num>,<vow>)}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and leftquoatation # rightquoatation and capital letter") (targetValue (name "?{endwith(<!>),and(and(<@>,<#>),<cap>)}")))
(example (utterance "lines containing words with 7 or more characters") (targetValue (name "?{<any>}")))
(example (utterance "lines not having leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a letter") (targetValue (name "?{and(and(<!>,<@>),<let>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation with a lower-case letter before string leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),concat(<low>,<@>)}")))
(example (utterance "lines that have at least 7 combinations of a letter preceding the word leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<let>,<!>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation occurring before either leftquoatation @ rightquoatation , a vowel , or a letter") (targetValue (name "?{<!>,or(or(<@>,<vow>),<let>)}")))
(example (utterance "lines with a number or ending in letter") (targetValue (name "?{or(<num>,endwith(<let>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation or character at lease once") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines with a capital letter or without string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<cap>,1),not(<!>)}")))
(example (utterance "items with an upper case letter , leftquoatation ! rightquoatation , or a vowel preceding a numeral .") (targetValue (name "?{or(or(<!>,<cap>),<vow>),<num>}")))
(example (utterance "lines with a vowel and a capital letter followed by a character") (targetValue (name "?{and(<vow>,<cap>),<any>}")))
(example (utterance "items with any letter preceding a character .") (targetValue (name "?{and(<cap>,<low>),<any>}")))
(example (utterance "lines where a vowel and leftquoatation ! rightquoatation and leftquoatation @ rightquoatation appear before a number") (targetValue (name "?{and(and(<!>,<vow>),<@>),<num>}")))
(example (utterance "lines not starting with character , 2 or more times") (targetValue (name "?{repeatatleast(startwith(<any>),2)}")))
(example (utterance "items with a character preceding a letter at least 4 times .") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines not having a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with words and zero or more of leftquoatation ! rightquoatation and letter") (targetValue (name "?{and(<!>,<let>)}")))
(example (utterance "lines containing a number , the string dog or a lower-case letter in front of a capital letter") (targetValue (name "?{or(or(<num>,<!>),<low>),<cap>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation and a lower-case letter or string leftquoatation @ rightquoatation at least once") (targetValue (name "?{or(and(<!>,<low>),<@>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(endwith(<!>),1)}")))
(example (utterance "items terminating with a small letter , at least thrice .") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines starting with a letter with a lower-case letter") (targetValue (name "?{and(<low>,startwith(<let>))}")))
(example (utterance "a vowel before leftquoatation ! rightquoatation is preceded by the lines with a capital letter .") (targetValue (name "?{concat(<cap>,<vow>),<!>}")))
(example (utterance "lines containing 4 letters succeeded by a capital letter") (targetValue (name "?{<let>,repeatatleast(<cap>,4)}")))
(example (utterance "lines starting in number with a capital letter before lower-case letter") (targetValue (name "?{startwith(<num>),concat(<cap>,<low>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , a capital letter , and leftquoatation @ rightquoatation in them") (targetValue (name "?{and(<!>,<cap>),<@>}")))
(example (utterance "items with a numeral preceding a character , at least6times .") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines containing a number followed by a lower case letter at least 6 times .") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "items with a character preceding a numeral or leftquoatation ! rightquoatation .") (targetValue (name "?{<any>,or(<!>,<num>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{<!>,contain(or(<@>,<#>))}")))
(example (utterance "lines without a vowel before a number or leftquoatation ! rightquoatation") (targetValue (name "?{not(<vow>),or(<num>,<!>)}")))
(example (utterance "lines containing both a lower-case and capital letter at least 3 times .") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation and not leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,not(<@>))}")))
(example (utterance "lines which containing either a letter , a number , or a capital letter") (targetValue (name "?{or(or(<let>,<num>),<cap>)}")))
(example (utterance "lines that have a letter , no times or all times , all of which appear prior to the leftquoatation ! rightquoatation string") (targetValue (name "?{star(<let>),<!>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or number") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a number") (targetValue (name "?{endwith(<!>),concat(<@>,<num>)}")))
(example (utterance "lines with a number , zero or more times and letter or capital letter") (targetValue (name "?{star(<num>),or(<let>,<cap>)}")))
(example (utterance "lines without a capital letter , string leftquoatation ! rightquoatation , or vowel") (targetValue (name "?{and(and(<cap>,<!>),<vow>)}")))
(example (utterance "lines with 5 or more characters before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,5),<!>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or 5 numbers") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation and leftquoatation @ rightquoatation or a lower-case letter") (targetValue (name "?{<!>,or(<@>,<low>)}")))
(example (utterance "lines with a number and vowel before leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<vow>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before ending with leftquoatation @ rightquoatation , zero or more times in it") (targetValue (name "?{<!>,star(endwith(<@>))}")))
(example (utterance "lines with 7 or more of leftquoatation ! rightquoatation before a letter") (targetValue (name "?{<!>,repeatatleast(<let>,7)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{or(<!>,contain(<cap>))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before a number and a letter") (targetValue (name "?{<!>,and(<num>,<let>)}")))
(example (utterance "lines with words and 5 or more of leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "items with a numeral preceding a vowel at least once .") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines starting with a number before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(startwith(<num>),1),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter") (targetValue (name "?{<!>,contain(not(<let>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a character followed by a letter") (targetValue (name "?{and(<!>,<any>),<let>}")))
(example (utterance "lines with a number followed by a capital letter and leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<cap>),<!>}")))
(example (utterance "lines not having leftquoatation ! rightquoatation or starting with a character in ttthem") (targetValue (name "?{repeatatleast(<!>,7),<let>}")))
(example (utterance "lines containing a character") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation or a lower case letter") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines containing a vowel preceded by a letter") (targetValue (name "?{<let>,<vow>}")))
(example (utterance "lines without leftquoatation ! rightquoatation , without a vowel , and without a capital letter") (targetValue (name "?{and(and(<!>,<vow>),<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before containing only leftquoatation @ rightquoatation , 6 or more times in them") (targetValue (name "?{<!>,repeatatleast(<@>,6)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{and(<!>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending in lower-case letter") (targetValue (name "?{contain(<!>),endwith(<low>)}")))
(example (utterance "lines without a capital letter ending with string leftquoatation ! rightquoatation") (targetValue (name "?{not(<cap>),endwith(<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding any letter .") (targetValue (name "?{or(concat(<!>,<cap>),<low>)}")))
(example (utterance "lines with a lower-case letter before string leftquoatation ! rightquoatation ending in string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<low>,<!>),endwith(<@>)}")))
(example (utterance "lines with words and a letter before a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<low>),<!>}")))
(example (utterance "lines that have a character followed by leftquoatation ! rightquoatation 3 or more times") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines with words ending with a number before letter") (targetValue (name "?{endwith(<num>),<let>}")))
(example (utterance "lines stating with a number or character") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "lines containing 1 leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines ending in a vowel and capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<vow>),concat(<cap>,<!>)}")))
(example (utterance "lines that do not contain a number or leftquoatation ! rightquoatation") (targetValue (name "?{not(<num>),repeatatleast(<!>,1)}")))
(example (utterance "lines containing words with vowels or leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,<!>}")))
(example (utterance "lines starting with a vowel 2 times") (targetValue (name "?{repeatatleast(or(<vow>,<let>),2)}")))
(example (utterance "lines with a letter before zero or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{concat(<let>,<!>),star(<@>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation which is followed by leftquoatation @ rightquoatation at least once") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with words wihich are ending with leftquoatation ! rightquoatation and a capital letter") (targetValue (name "?{endwith(<!>),<cap>}")))
(example (utterance "lines with 2 or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{repeatatleast(<!>,2),concat(<@>,<#>)}")))
(example (utterance "lines with number before ending with character") (targetValue (name "?{<num>,endwith(<any>)}")))
(example (utterance "items with words containing an upper case letter and a character .") (targetValue (name "?{<any>,<cap>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , 4 or more times before string leftquoatation @ rightquoatation") (targetValue (name "?{concat(repeatatleast(<!>,4),<@>)}")))
(example (utterance "lines where 6 or more times there are words with a character") (targetValue (name "?{<any>}")))
(example (utterance "lines starting in zero or more of a character before string leftquoatation ! rightquoatation") (targetValue (name "?{star(startwith(<any>)),<!>}")))
(example (utterance "lines with characters more than5times before capital letters") (targetValue (name "?{repeatatleast(<any>,5),<cap>}")))
(example (utterance "lines starting in zero or more of a number or string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(or(<num>,<!>))}")))
(example (utterance "lines that have a lower-case letter preceding the word leftquoatation ! rightquoatation and a capital letter") (targetValue (name "?{<low>,and(<!>,<cap>)}")))
(example (utterance "lines with words with either string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , or character before string leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<@>),<any>),<#>}")))
(example (utterance "items with a numeral at least5times preceding a capital letter .") (targetValue (name "?{repeatatleast(<num>,5),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a lower-case letter , and a number in them") (targetValue (name "?{<!>,and(and(<@>,<low>),<num>)}")))
(example (utterance "lines with a letter , vowel , or number") (targetValue (name "?{or(or(<let>,<vow>),<num>)}")))
(example (utterance "lines with a character and string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{and(<any>,<!>),<@>}")))
(example (utterance "lines ending in 7 or more vowels") (targetValue (name "?{contain(repeatatleast(<vow>,7))}")))
(example (utterance "lines with words including leftquoatation ! rightquoatation , a number and a vowel") (targetValue (name "?{and(<!>,<vow>),<num>}")))
(example (utterance "lines that end with words that only contain a letter , at least 3 times") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with a lower-case letter before a number then a capital letter") (targetValue (name "?{<low>,concat(<num>,<cap>)}")))
(example (utterance "lines with words with starting with leftquoatation ! rightquoatation , 5 or more times in them") (targetValue (name "?{repeatatleast(<!>,5)}")))
(example (utterance "lines with a lower-case letter before capital letter then number") (targetValue (name "?{concat(<low>,<cap>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a letter coming before a vowel") (targetValue (name "?{or(<!>,<let>),<vow>}")))
(example (utterance "lines with vowel followed by letter , 2 or more times") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines that contain a vowel followed by leftquoatation ! rightquoatation and a lower-case letter") (targetValue (name "?{<vow>,and(<!>,<low>)}")))
(example (utterance "items with words containing a numeral and a character .") (targetValue (name "?{<any>,<num>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a letter or a vowel .") (targetValue (name "?{<!>,or(<vow>,<let>)}")))
(example (utterance "lines ending either with a letter or a character") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines containing a vowel or capital letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<cap>),<!>}")))
(example (utterance "lines with a number followed by leftquoatation ! rightquoatation , 4 or more times in it") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines which are ending with words with either a character , a number , or a lower-case letter") (targetValue (name "?{or(or(<any>,<num>),<low>)}")))
(example (utterance "lines with character at least once before vowel") (targetValue (name "?{repeatatleast(<any>,1),<vow>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation , a capital , or string leftquoatation @ rightquoatation and not a vowel") (targetValue (name "?{and(or(or(<!>,<cap>),<@>),<vow>)}")))
(example (utterance "lines having characters at least 6 times before a vowel") (targetValue (name "?{repeatatleast(<any>,6),<vow>}")))
(example (utterance "lines with a capital letter followed by a character before a letter in them") (targetValue (name "?{concat(<cap>,<any>),<let>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before letter , character , and a capital") (targetValue (name "?{<!>,and(and(<let>,<any>),<cap>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation and a letter or leftquoatation @ rightquoatation in them") (targetValue (name "?{and(<!>,<let>),<@>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 4 or more times before character") (targetValue (name "?{repeatatleast(<!>,4),<any>}")))
(example (utterance "lines with at least 1 lower-case letter") (targetValue (name "?{repeatatleast(<low>,1)}")))
(example (utterance "lines with words that don't have a capital letter , 5 or more times in them") (targetValue (name "?{repeatatleast(<cap>,5)}")))
(example (utterance "lines that start with a number and a vowel") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines which do not contain a vowel or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<@>,<vow>),<!>)}")))
(example (utterance "lines with a vowel followed by a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<vow>,<low>),<!>}")))
(example (utterance "lines with a letter , zero or more times , preceded by a vowel") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation and a character , 5 or more times in them") (targetValue (name "?{concat(<cap>,<!>),repeatatleast(<any>,5)}")))
(example (utterance "lines with 2 or more of a lower-case letter before letter") (targetValue (name "?{repeatatleast(<low>,2),<let>}")))
(example (utterance "lines ending with character at least once or vowel") (targetValue (name "?{or(repeatatleast(<any>,1),<vow>)}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation and a letter") (targetValue (name "?{contain(and(<!>,<let>))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before a capital letter") (targetValue (name "?{or(<!>,<@>),<cap>}")))
(example (utterance "lines that have 7 or more instances of leftquoatation ! rightquoatation before a character") (targetValue (name "?{<!>,repeatatleast(<any>,7)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation and at least 1 leftquoatation # rightquoatation") (targetValue (name "?{or(<!>,and(<@>,<#>))}")))
(example (utterance "lines with a character before vowel or lower-case letter") (targetValue (name "?{<any>,or(<vow>,<low>)}")))
(example (utterance "lines containing either character , a capital , or a letter") (targetValue (name "?{or(or(<any>,<cap>),<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation coming before a vowel and a letter") (targetValue (name "?{<!>,and(<vow>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or letter") (targetValue (name "?{<!>,or(or(<@>,<low>),<let>)}")))
(example (utterance "sentence: lines containing a character or vowel") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines with a number preceded 2 or more times by a number") (targetValue (name "?{repeatatleast(<num>,2),<let>}")))
(example (utterance "lines with zero or more capital letters before a character") (targetValue (name "?{star(<cap>),<any>}")))
(example (utterance "items with words containing a vowel or an upper case letter .") (targetValue (name "?{<cap>,<vow>}")))
(example (utterance "lines containing a capital letter before lower-case letter , 5 or more times") (targetValue (name "?{contain(<cap>),repeatatleast(<low>,5)}")))
(example (utterance "lines in which leftquoatation ! rightquoatation , a number , and a character , are preceded by a vowel") (targetValue (name "?{<vow>,and(and(<!>,<num>),<any>)}")))
(example (utterance "lines having a capital letter and leftquoatation ! rightquoatation") (targetValue (name "?{contain(and(<cap>,<!>))}")))
(example (utterance "items with a vowel preceding a nonletter .") (targetValue (name "?{<vow>,not(<let>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation before character at least once") (targetValue (name "?{contain(<!>),repeatatleast(<any>,1)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation or a capital letter preceding a character") (targetValue (name "?{concat(<!>,<@>),concat(<cap>,<any>)}")))
(example (utterance "lines ending with capital letter , 7 times or more") (targetValue (name "?{repeatatleast(contain(<cap>),7)}")))
(example (utterance "lines ending with a lower-case letter before a capital followed by letter") (targetValue (name "?{endwith(<low>),concat(<cap>,<let>)}")))
(example (utterance "leftquoatation ! rightquoatation a number or a vowel appears in the lines") (targetValue (name "?{or(or(<!>,<num>),<vow>)}")))
(example (utterance "items with an upper case letter preceding a numeral at least 7 times .") (targetValue (name "?{concat(<cap>,repeatatleast(<num>,7))}")))
(example (utterance "lines with a lower-case and capital letter and vowel before a character") (targetValue (name "?{and(and(<low>,<vow>),<cap>),<any>}")))
(example (utterance "lines with words that contain only a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines with a vowel or letter before a character") (targetValue (name "?{or(<vow>,<let>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation or a character") (targetValue (name "?{contain(<!>),or(<@>,<any>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or character before a capital") (targetValue (name "?{or(<!>,<any>),<cap>}")))
(example (utterance "items with words containing a vowel or a character .") (targetValue (name "?{contain(<any>),<vow>}")))
(example (utterance "lines without a vowel , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a number") (targetValue (name "?{concat(or(or(<vow>,<!>),<@>),<num>)}")))
(example (utterance "lines with a number before capital letter or string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,or(<cap>,<!>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation , zero or more times") (targetValue (name "?{contain(or(<!>,<@>))}")))
(example (utterance "lines containing at least 1 letter or number") (targetValue (name "?{or(contain(<let>),<num>)}")))
(example (utterance "lines with a vowel preceded by a lower-case letter , a capital letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<cap>),<!>),<vow>}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation followed by a capital letter , at least 1 time") (targetValue (name "?{endwith(concat(<!>,<cap>))}")))
(example (utterance "lines with a number after a capital letter") (targetValue (name "?{<cap>,<num>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation and not containing only string leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a lower-case letter before leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<low>),repeatatleast(<@>,1)}")))
(example (utterance "lines that start with a lower-case letter before 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<low>),repeatatleast(<!>,3)}")))
(example (utterance "items with a numeral preceding an upper case letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{<num>,concat(<cap>,<!>)}")))
(example (utterance "lines starting with a number before capital letter") (targetValue (name "?{startwith(<num>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation  or containing a vowel") (targetValue (name "?{star(<!>),contain(<vow>)}")))
(example (utterance "items with a character or a letter preceding a numeral .") (targetValue (name "?{or(<let>,<any>),<num>}")))
(example (utterance "lines that don't contain a character at least 6 times") (targetValue (name "?{repeatatleast(contain(<any>),6)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a small letter at least twice .") (targetValue (name "?{concat(<!>,<any>),repeatatleast(<low>,2)}")))
(example (utterance "lines with 4 or more of a number before a letter") (targetValue (name "?{<num>,repeatatleast(<let>,4)}")))
(example (utterance "lines containing a letter or a vowel , 3 times or more") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a character , a number or capital") (targetValue (name "?{and(<!>,<@>),or(or(<any>,<num>),<cap>)}")))
(example (utterance "lines having a capital letter followed by leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{<cap>,concat(<!>,<vow>)}")))
(example (utterance "lines with words that contain only leftquoatation ! rightquoatation before a number in them") (targetValue (name "?{<!>,<num>}")))
(example (utterance "lines not having a lower-case letter or capital letter , string leftquoatation ! rightquoatation , and number") (targetValue (name "?{not(<low>),and(and(<cap>,<!>),<num>)}")))
(example (utterance "lines with words a long with characters and leftquoatation ! rightquoatation and a letter") (targetValue (name "?{and(<any>,<!>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation 5 times starting with a letter") (targetValue (name "?{repeatatleast(<!>,5),startwith(<let>)}")))
(example (utterance "lines containging a capital letter and a letter") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a capital letter , and a character") (targetValue (name "?{concat(<!>,<cap>),<any>}")))
(example (utterance "items with words with leftquoatation ! rightquoatation , a vowel , and a character .") (targetValue (name "?{and(<any>,<!>),<vow>}")))
(example (utterance "items without an upper case letter preceding a character .") (targetValue (name "?{not(<cap>),<any>}")))
(example (utterance "lines without any numbers") (targetValue (name "?{contain(<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a consonant") (targetValue (name "?{<!>,not(<vow>)}")))
(example (utterance "lines with less than 5 lower-case letters or leftquoatation ! rightquoatation") (targetValue (name "?{not(<low>),repeatatleast(<!>,5)}")))
(example (utterance "lines without only a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and lower-case letter") (targetValue (name "?{and(repeatatleast(<!>,1),<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 6 or more times before a character in them") (targetValue (name "?{repeatatleast(<!>,6),<any>}")))
(example (utterance "lines not having capital letter followed by number before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(concat(<cap>,<num>),<!>)}")))
(example (utterance "lines containing a number , character , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<any>),<!>)}")))
(example (utterance "lines featuring words either with the leftquoatation ! rightquoatation string or without the leftquoatation @ rightquoatation string") (targetValue (name "?{<!>,not(<@>)}")))
(example (utterance "lines which are containing a number followed by a capital letter") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines not having letter and not a number") (targetValue (name "?{and(<let>,<num>)}")))
(example (utterance "lines with lower-case letters with a number before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<num>),<!>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation or lower-case") (targetValue (name "?{or(not(<!>),<low>)}")))
(example (utterance "lines that have a letter  and end with leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,endwith(<!>))}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation or a character followed by a capital letter") (targetValue (name "?{endwith(or(<!>,<any>)),<cap>}")))
(example (utterance "lines with a number or zero or more of leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,contain(<!>))}")))
(example (utterance "lines must have a number , an upper case and a lower case letter") (targetValue (name "?{and(and(<cap>,<low>),<num>)}")))
(example (utterance "lines with words with a vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),<!>}")))
(example (utterance "lines with a number followed by a capital letter , zero or more times in it") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines with words with a capital letter , a number , and leftquoatation ! rightquoatation in it") (targetValue (name "?{and(<cap>,<num>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation in them") (targetValue (name "?{<!>,and(and(<vow>,<@>),<#>)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation before the word leftquoatation @ rightquoatation , which is then followed by the word leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),<#>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or lower-case letter , string leftquoatation @ rightquoatation , and a capital at least once") (targetValue (name "?{or(<!>,and(and(<low>,<@>),<cap>))}")))
(example (utterance "lines with a number , 5 or more times and not a vowel in them") (targetValue (name "?{repeatatleast(<num>,5),not(<vow>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines containing words with 6 or more times a letter or a capital letter") (targetValue (name "?{repeatatleast(<let>,6),<cap>}")))
(example (utterance "lines with lower-case letter before vowel , 6 or more times") (targetValue (name "?{<low>,repeatatleast(<vow>,6)}")))
(example (utterance "items with words without an upper case letter .") (targetValue (name "?{not(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after leftquoatation @ rightquoatation") (targetValue (name "?{<@>,<!>}")))
(example (utterance "items with words containing a small letter or numeral .") (targetValue (name "?{<num>,<low>}")))
(example (utterance "lines with a character before number and string leftquoatation ! rightquoatation") (targetValue (name "?{<any>,and(<num>,<!>)}")))
(example (utterance "lines starting with a number or a vowel") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines with a zero or more of a vowel before string leftquoatation ! rightquoatation , character , or lower-case letter") (targetValue (name "?{star(<vow>),or(or(<!>,<any>),<low>)}")))
(example (utterance "lines with a character at least once") (targetValue (name "?{<any>}")))
(example (utterance "lines with words with either character , number , or lower-case letter") (targetValue (name "?{or(<any>,<num>),<low>}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation , lower-case letter , and character") (targetValue (name "?{<cap>,and(and(<!>,<low>),<any>)}")))
(example (utterance "lines with words that contain a character or a letter , at least twice") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines which are containing leftquoatation ! rightquoatation and ending with leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),endwith(<@>)}")))
(example (utterance "lines containing ending with leftquoatation ! rightquoatation or a character in them") (targetValue (name "?{endwith(or(<!>,<any>))}")))
(example (utterance "items terminating with a numeral preceding leftquoatation ! rightquoatation .") (targetValue (name "?{endwith(<num>),<!>}")))
(example (utterance "lines without 4 or more times a number") (targetValue (name "?{contain(repeatatleast(<num>,4))}")))
(example (utterance "lines containing only string leftquoatation ! rightquoatation , 4 or more times before number") (targetValue (name "?{repeatatleast(<!>,4),<num>}")))
(example (utterance "lines with zero or more of a letter before a vowel") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines that contain a capital letter followed by leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{concat(concat(<cap>,<!>),<@>)}")))
(example (utterance "lines starting without numbers") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation with a vowel") (targetValue (name "?{endwith(<!>),contain(<vow>)}")))
(example (utterance "lines ending in zero or more of leftquoatation ! rightquoatation or character") (targetValue (name "?{endwith(or(<!>,<any>))}")))
(example (utterance "lines with words with vowel followed by lower-case letter at least once") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines which are not having a vowel , not leftquoatation ! rightquoatation , and not a capital letter") (targetValue (name "?{and(and(<vow>,<!>),<cap>)}")))
(example (utterance "lines with a lower-case letter or vowel") (targetValue (name "?{<low>,<vow>}")))
(example (utterance "lines with words with either a vowel , string leftquoatation ! rightquoatation , or string leftquoatation @ rightquoatation") (targetValue (name "?{or(<vow>,<!>),<@>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation and a capital letter , 7 or so times") (targetValue (name "?{contain(<!>),repeatatleast(<cap>,7)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , lower-case letter , and a letter before the string 'truck") (targetValue (name "?{and(and(<!>,<low>),<let>),<@>}")))
(example (utterance "lines that end with a vowel followed by a character") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines that do not contain a letter , vowel , or lowercase letter") (targetValue (name "?{or(or(<low>,<let>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after leftquoatation @ rightquoatation") (targetValue (name "?{contain(<@>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , lower-case letter , and vowel") (targetValue (name "?{and(and(<!>,<low>),<vow>)}")))
(example (utterance "lines with a character or vowel") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines with a capital letter and vowel before character") (targetValue (name "?{and(<cap>,<vow>),<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and leftquoatation @ rightquoatation , a capital letter and leftquoatation # rightquoatation") (targetValue (name "?{contain(<!>),and(and(<@>,<cap>),<#>)}")))
(example (utterance "lines not having a capital and lower-case letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{not(<cap>),concat(<low>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least 2 times with leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,2),contain(<@>)}")))
(example (utterance "lines ending with character and a capital , zero or more times") (targetValue (name "?{endwith(<any>),star(<cap>)}")))
(example (utterance "lines with words that contain a vowel , a number , and a lower-case letter") (targetValue (name "?{and(<vow>,<num>),<low>}")))
(example (utterance "lines with words with a letter followed by string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{concat(<let>,<!>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or ending in zero or more of a character") (targetValue (name "?{or(<!>,endwith(<any>))}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation before the string 'truck or a lower-case letter") (targetValue (name "?{star(<!>),or(<@>,<low>)}")))
(example (utterance "lines containing a capital letter , a number , and a lower-case letter") (targetValue (name "?{and(<cap>,<num>),<low>}")))
(example (utterance "lines having a character , leftquoatation ! rightquoatation , and a number coming before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<any>,<!>),<num>),<@>}")))
(example (utterance "lines with a letter not being leftquoatation ! rightquoatation") (targetValue (name "?{star(<let>),not(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before zero or more of only a capital letter") (targetValue (name "?{<!>,star(<cap>)}")))
(example (utterance "lines with a lower-case letter before character") (targetValue (name "?{contain(<low>),<any>}")))
(example (utterance "lines with a vowel , 7 or more times before a capital letter in them") (targetValue (name "?{repeatatleast(<vow>,7),<cap>}")))
(example (utterance "lines in which a lower-case letter is present prior to the appearance of capital letters") (targetValue (name "?{contain(<low>),<cap>}")))
(example (utterance "lines with 5 or more of a vowel before a character") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines starting in words with a capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with a lower-case letter and capital letter") (targetValue (name "?{and(and(<low>,<let>),<cap>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a character , lower-case letter , or leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<any>,<low>),<@>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation 7 times") (targetValue (name "?{<!>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation at least once before string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(endwith(<!>),1),<@>}")))
(example (utterance "lines with a number before words with string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,repeatatleast(<!>,1)}")))
(example (utterance "lines can either have a lower case letter or a letter that follows a character") (targetValue (name "?{or(<low>,<any>),<let>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation , number , and vowel") (targetValue (name "?{<!>,and(and(<@>,<num>),<vow>)}")))
(example (utterance "lines without a number , character , or lower-case letter") (targetValue (name "?{and(and(<num>,<any>),<low>)}")))
(example (utterance "liens with a letter , or a character , number , and leftquoatation ! rightquoatation") (targetValue (name "?{contain(<let>),and(and(<any>,<num>),<!>)}")))
(example (utterance "lines that begin with a character or leftquoatation ! rightquoatation a minimum of 1 time") (targetValue (name "?{or(startwith(<any>),<!>)}")))
(example (utterance "lines with lower-case letter and character before number") (targetValue (name "?{and(<low>,<any>),<num>}")))
(example (utterance "lines which are containing a letter , leftquoatation ! rightquoatation , and a number") (targetValue (name "?{and(and(<let>,<!>),<num>)}")))
(example (utterance "lines with a number or letter") (targetValue (name "?{or(or(<num>,<cap>),<let>)}")))
(example (utterance "ines with a capital letter or a lower-case letter before a vowel in them") (targetValue (name "?{or(<cap>,<low>),<vow>}")))
(example (utterance "lines with a lower-case letter before capital letter and number") (targetValue (name "?{<low>,and(<cap>,<num>)}")))
(example (utterance "lines containing a number followed by a capital letter") (targetValue (name "?{<num>,<cap>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a capital letter or a vowel") (targetValue (name "?{<!>,or(<cap>,<vow>)}")))
(example (utterance "lines with a capital letter , leftquoatation ! rightquoatation , or a lower-case letter preceding leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<cap>,<!>),<low>),<@>}")))
(example (utterance "lines with 7 or more characters or a lower-case letter") (targetValue (name "?{repeatatleast(<any>,7),<low>}")))
(example (utterance "lines with words ending with a character") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines containing 6 lower-case letters before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,6),<!>}")))
(example (utterance "multiple lines with capital letters or characters") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines with a letter") (targetValue (name "?{or(or(<let>,<low>),<cap>)}")))
(example (utterance "the lines with a lower - case letter at least once before a vowel .") (targetValue (name "?{repeatatleast(<low>,1),<vow>}")))
(example (utterance "lines with a letter before its end with a capital letter") (targetValue (name "?{<let>,endwith(<cap>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation before a number or leftquoatation @ rightquoatation in them") (targetValue (name "?{concat(<!>,<num>),<@>}")))
(example (utterance "lines that have words that do not have a capital letter followed by a character") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines with words and a vowel , capital letter , or character") (targetValue (name "?{or(<vow>,<cap>),<any>}")))
(example (utterance "lines with lower-case letter followed by string leftquoatation ! rightquoatation before character") (targetValue (name "?{concat(<low>,<!>),<any>}")))
(example (utterance "lines containing words including a letter and with or without leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<!>)}")))
(example (utterance "lines with a number , string leftquoatation ! rightquoatation , or character before a capital letter") (targetValue (name "?{or(or(<num>,<!>),<any>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , or a number before leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,concat(<num>,<@>))}")))
(example (utterance "a lower case letter appears before containing a number at least once in the lines") (targetValue (name "?{<low>,contain(repeatatleast(<num>,1))}")))
(example (utterance "lines with a number or capital letter before leftquoatation ! rightquoatation") (targetValue (name "?{<num>,or(<!>,<cap>)}")))
(example (utterance "lines that contain the word leftquoatation ! rightquoatation at least once , plus the word leftquoatation @ rightquoatation or a character") (targetValue (name "?{repeatatleast(<!>,1),or(<any>,<@>)}")))
(example (utterance "lines which don't have a number preceded by a lower-case letter") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "lines starting with words with string leftquoatation ! rightquoatation or vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "before leftquoatation ! rightquoatation , lines have either a number , character , or capital letter .") (targetValue (name "?{or(or(<num>,<any>),<cap>),<!>}")))
(example (utterance "lines containing a lower-case letter preceding a letter , zero or more times") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines starting with a character , or a letter zero or more times") (targetValue (name "?{or(star(<any>),<let>)}")))
(example (utterance "lines with the string dog , truck or ring before a letter") (targetValue (name "?{<!>,and(and(<let>,<@>),<#>)}")))
(example (utterance "lines with a character followed by with or without a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<vow>),star(<!>)}")))
(example (utterance "lines with 5 or more numbers before a lower-case letter") (targetValue (name "?{repeatatleast(<num>,5),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before the string 'truck or letter") (targetValue (name "?{<!>,or(or(<@>,<let>),<cap>)}")))
(example (utterance "lines without words and a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with either a vowel or leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<vow>,1),and(<!>,<@>)}")))
(example (utterance "lines with words and 7 or more of leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "items with a character at least 3 times .") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines with 3 or more of leftquoatation ! rightquoatation with string leftquoatation @ rightquoatation or leftquoatation # rightquoatation or a character") (targetValue (name "?{repeatatleast(<!>,3),or(or(<@>,<any>),<#>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation and lower-case letter or string leftquoatation @ rightquoatation , zero or more times") (targetValue (name "?{or(and(<!>,<low>),<@>)}")))
(example (utterance "lines that have a minimum of 1 vowel") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "lines with words with either string leftquoatation ! rightquoatation , a letter , or a capital") (targetValue (name "?{or(<!>,<let>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation before a capital letter") (targetValue (name "?{concat(<!>,<@>),<cap>}")))
(example (utterance "lines not having leftquoatation ! rightquoatation followed by a vowel before a letter in them") (targetValue (name "?{not(concat(<!>,<vow>)),<let>}")))
(example (utterance "lines not ending with a capital letter") (targetValue (name "?{repeatatleast(endwith(<cap>),1)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a letter or leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{<!>,or(or(<@>,<#>),<let>)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation , a number , or a vowel") (targetValue (name "?{and(and(<!>,<num>),<vow>)}")))
(example (utterance "lines with 7 or more of a lower-case letter before character or string leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<low>,<any>),<!>)}")))
(example (utterance "lines with words that don't have a letter or leftquoatation ! rightquoatation in them") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with a lower-case letter before containing a vowel in them") (targetValue (name "?{<low>,contain(<vow>)}")))
(example (utterance "lines without a letter , a number , or lower-case letter") (targetValue (name "?{and(and(<low>,<let>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and not ending in a character") (targetValue (name "?{<!>,not(endwith(<any>))}")))
(example (utterance "lines without leftquoatation ! rightquoatation , a number , or leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<num>),<@>)}")))
(example (utterance "lines that end with a lowercase letter at least 3 times .") (targetValue (name "?{repeatatleast(endwith(<low>),3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{contain(<!>),concat(<@>,<#>)}")))
(example (utterance "lines that have a capital letter , a number , and a vowel before leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<cap>),<num>),<!>}")))
(example (utterance "lines that end with a letter before the presence of a character") (targetValue (name "?{endwith(<let>),<any>}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation or character") (targetValue (name "?{<cap>,or(<!>,<any>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation , character , and a letter") (targetValue (name "?{<!>,and(and(<@>,<any>),<let>)}")))
(example (utterance "lines containing words with leftquoatation ! rightquoatation followed by a vowel in it") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines containing words with a letter , a capital letter , and a vowel") (targetValue (name "?{and(<let>,<cap>),<vow>}")))
(example (utterance "lines with words with a character , capital letter , and vowel") (targetValue (name "?{and(<any>,<cap>),<vow>}")))
(example (utterance "lines ending with 5 or more vowels before a character") (targetValue (name "?{repeatatleast(endwith(<vow>),5),<any>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation before and that does not start with leftquoatation @ rightquoatation") (targetValue (name "?{<!>,not(startwith(<@>))}")))
(example (utterance "lines containing a number that precedes a vowel") (targetValue (name "?{<num>,<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before non string leftquoatation @ rightquoatation or number") (targetValue (name "?{<!>,not(and(<@>,<num>))}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , vowel , and letter before a character") (targetValue (name "?{and(and(<!>,<vow>),<let>),<any>}")))
(example (utterance "lines with words having leftquoatation ! rightquoatation before a vowel or a character") (targetValue (name "?{concat(<!>,<vow>),<any>}")))
(example (utterance "items terminating with an upper case letter preceding a character .") (targetValue (name "?{endwith(<cap>),<any>}")))
(example (utterance "lines with at least 1 of leftquoatation ! rightquoatation , but not a number") (targetValue (name "?{repeatatleast(<!>,1),not(<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation preceding an uppercase letter or leftquoatation @ rightquoatation or a character") (targetValue (name "?{<!>,or(or(<cap>,<@>),<any>)}")))
(example (utterance "lines containing a lower-case letter once") (targetValue (name "?{contain(endwith(<low>))}")))
(example (utterance "lines that do not have a capital letter") (targetValue (name "?{star(<cap>)}")))
(example (utterance "lines not containing a letter or a lower-case letter") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines containing a number before leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a lower-case letter") (targetValue (name "?{<num>,and(and(<!>,<@>),<low>)}")))
(example (utterance "lines containing upper or lower case alphabet characters in at least5instances") (targetValue (name "?{or(<cap>,<let>)}")))
(example (utterance "lines not beginning with leftquoatation ! rightquoatation as well as a vowel") (targetValue (name "?{and(startwith(<!>),<vow>)}")))
(example (utterance "lines with words , a vowel , number , and capital letter") (targetValue (name "?{and(<vow>,<num>),<cap>}")))
(example (utterance "lines that contain words that have a character , leftquoatation ! rightquoatation or at least 1 leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<any>,<!>),<@>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , and a capital , 7 or more times") (targetValue (name "?{contain(and(and(<!>,<@>),<cap>))}")))
(example (utterance "lines with 7 or more of a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines ending with a vowel before a letter") (targetValue (name "?{concat(endwith(<let>),<vow>)}")))
(example (utterance "lines with a letter or that starts with 'dog") (targetValue (name "?{or(<let>,startwith(<!>))}")))
(example (utterance "items with a numeral after a letter or leftquoatation ! rightquoatation .") (targetValue (name "?{or(<!>,<let>),<num>}")))
(example (utterance "lines with 3 or more of a vowel or character") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before either a capital letter , a character , or leftquoatation @ rightquoatation in them") (targetValue (name "?{<!>,or(or(<cap>,<any>),<@>)}")))
(example (utterance "lines containing a vowel , and a number") (targetValue (name "?{and(and(<any>,<vow>),<num>)}")))
(example (utterance "lines having a vowel not followed by a number or leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,not(or(<num>,<!>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation and not a lower-case letter") (targetValue (name "?{and(contain(<!>),<low>)}")))
(example (utterance "all lines with words") (targetValue (name "?{and(<low>,<!>)}")))
(example (utterance "lines containing words with leftquoatation ! rightquoatation and a character") (targetValue (name "?{<!>,<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a lower-cased letter and leftquoatation @ rightquoatation preceding a vowel") (targetValue (name "?{and(and(<!>,<low>),<@>),<vow>}")))
(example (utterance "items with a letter preceding leftquoatation ! rightquoatation at least5times .") (targetValue (name "?{<let>,repeatatleast(<!>,5)}")))
(example (utterance "lines with words with capital letter before string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{<cap>,star(<!>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation and a vowel") (targetValue (name "?{endwith(<!>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a character before string leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<any>),<@>),<#>}")))
(example (utterance "lines with a lower-case letter before not leftquoatation ! rightquoatation , not a letter , and not leftquoatation @ rightquoatation in them") (targetValue (name "?{<low>,not(and(and(<!>,<let>),<@>))}")))
(example (utterance "lines with a vowel after either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a letter") (targetValue (name "?{or(or(<!>,<@>),<let>),<vow>}")))
(example (utterance "lines without string leftquoatation ! rightquoatation , capital letter , or lower-case letter") (targetValue (name "?{and(and(<!>,<cap>),<low>)}")))
(example (utterance "lines that contain a number followed by a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<num>,<low>),<!>)}")))
(example (utterance "lines containing 4 or more letters and leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(contain(<let>),4),<!>}")))
(example (utterance "lines with words that don't have a lower-case letter before a vowel in them") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines containing a lowercase letter preceded by a vowel at least twice") (targetValue (name "?{<vow>,repeatatleast(<low>,2)}")))
(example (utterance "lines with 2 or more characters or a vowels") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines that include a number , leftquoatation ! rightquoatation and a lower-case letter") (targetValue (name "?{and(and(<low>,<num>),<!>)}")))
(example (utterance "lines with words with 5 or more of only characters") (targetValue (name "?{<any>}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<cap>),<!>}")))
(example (utterance "lines with words and a letter followed by a capital letter with a vowel") (targetValue (name "?{concat(<let>,<cap>),<vow>}")))
(example (utterance "lines with a letter followed by a number and a capital letter") (targetValue (name "?{<let>,and(<num>,<cap>)}")))
(example (utterance "lines with words with either string leftquoatation ! rightquoatation , number , or a lower-case letter") (targetValue (name "?{or(<!>,<num>),<low>}")))
(example (utterance "items with leftquoatation ! rightquoatation , letter , and an upper case letter preceding leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<!>,<let>),<cap>),<@>}")))
(example (utterance "lines having a character before a capital letter , a number , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<num>),<!>),<any>}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation or a letter") (targetValue (name "?{star(or(<!>,<let>))}")))
(example (utterance "lines which are not having a character , a number , or a vowel") (targetValue (name "?{or(or(<any>,<num>),<vow>)}")))
(example (utterance "lines with 4 or more letters or lower-case letters") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines that begin with the word leftquoatation ! rightquoatation or a lower-case letter , at least 4 times") (targetValue (name "?{or(startwith(<!>),<low>)}")))
(example (utterance "lines with either a letter or a vowel preceding leftquoatation ! rightquoatation") (targetValue (name "?{contain(<let>),concat(<vow>,<!>)}")))
(example (utterance "lines having character , number . and leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<any>,<num>),<!>),<@>}")))
(example (utterance "lines including leftquoatation ! rightquoatation and that start with a capital letter and then a lower-case letter") (targetValue (name "?{concat(<cap>,<low>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least 6 times") (targetValue (name "?{repeatatleast(<!>,6),<vow>}")))
(example (utterance "lines ending in a letter") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines starting with words , not a vowel") (targetValue (name "?{not(<vow>)}")))
(example (utterance "lines that contain the word leftquoatation ! rightquoatation and a vowel before a character") (targetValue (name "?{and(<!>,<vow>),<any>}")))
(example (utterance "lines without a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{not(<low>),repeatatleast(<!>,1)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation 6 times or a capital letter 6 times") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines not having a letter or a capital and not a lower-case letter") (targetValue (name "?{and(or(<let>,<cap>),<low>)}")))
(example (utterance "items with words containing a character , a small letter , and leftquoatation ! rightquoatation .") (targetValue (name "?{and(<any>,<!>),<low>}")))
(example (utterance "lines containing a lower-case or a capital letter") (targetValue (name "?{or(contain(<low>),<cap>)}")))
(example (utterance "lines that do not have a character , a lower-case letter , or a capital letter") (targetValue (name "?{and(and(<any>,<low>),<cap>)}")))
(example (utterance "lines with words that don't leftquoatation ! rightquoatation before a capital letter in them") (targetValue (name "?{not(<!>),<cap>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceding either a vowel , a capital letter , or leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<vow>,<cap>),<@>)}")))
(example (utterance "a lower case letter after a vowel in the lines") (targetValue (name "?{contain(<vow>),<low>}")))
(example (utterance "lines which are having a letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation before a lower-case letter") (targetValue (name "?{and(and(<let>,<!>),<@>),<low>}")))
(example (utterance "items without a letter , upper case letter , or vowel .") (targetValue (name "?{or(or(<let>,<vow>),<cap>)}")))
(example (utterance "lines starting with words with string leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{repeatatleast(<!>,2)}")))
(example (utterance "lines with a lower case letter followed by a letter , or not") (targetValue (name "?{contain(concat(<low>,<let>))}")))
(example (utterance "lines ending with words having leftquoatation ! rightquoatation at least 1 time") (targetValue (name "?{repeatatleast(<!>,1)}")))
(example (utterance "lines with or without a capital letter or leftquoatation ! rightquoatation followed by a character") (targetValue (name "?{star(<cap>),concat(<!>,<any>)}")))
(example (utterance "lines with a letter before vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{<let>,or(<vow>,<!>)}")))
(example (utterance "items with a character at least5times preceding a letter .") (targetValue (name "?{repeatatleast(<any>,5),<let>}")))
(example (utterance "lines wherein the second character is a vowel") (targetValue (name "?{star(startwith(<low>)),<vow>}")))
(example (utterance "lines starting in zero or more of a lower-case letter or vowel") (targetValue (name "?{star(or(<low>,<vow>))}")))
(example (utterance "lines with 3 or more of a number before capital letter") (targetValue (name "?{startwith(concat(<num>,<cap>))}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , a letter , or a lower case letter") (targetValue (name "?{contain(<let>),or(or(<!>,<low>),<@>)}")))
(example (utterance "lines containing a lower-case letter or leftquoatation ! rightquoatation 1 or more times") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines with a lower-case letter before a number , a vowel , and a character in them") (targetValue (name "?{<low>,and(and(<num>,<vow>),<any>)}")))
(example (utterance "lines with words with a number and a capital , zero or more times") (targetValue (name "?{and(<num>,<cap>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation and leftquoatation @ rightquoatation , at least twice") (targetValue (name "?{contain(<!>),repeatatleast(<@>,2)}")))
(example (utterance "lines with words and lower-case letter or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{<low>,concat(<!>,<@>)}")))
(example (utterance "lines ending with 4 or more of a character before string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(concat(<any>,<!>))}")))
(example (utterance "lines not having a letter , a vowel , and not a lower-case") (targetValue (name "?{and(and(<let>,<vow>),<low>)}")))
(example (utterance "items with a character at least twice preceding a vowel .") (targetValue (name "?{repeatatleast(<any>,2),<vow>}")))
(example (utterance "items with a letter and a character preceding leftquoatation ! rightquoatation .") (targetValue (name "?{and(<any>,<let>),<!>}")))
(example (utterance "lines with words starting with a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines that contain a lower-case letter followed by 3 or more capital letters") (targetValue (name "?{<low>,repeatatleast(<cap>,3)}")))
(example (utterance "lines with a number before 6 or more of a character or vowel") (targetValue (name "?{concat(<num>,<any>),repeatatleast(<vow>,6)}")))
(example (utterance "lines with the 7 or more of leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),repeatatleast(<#>,7)}")))
(example (utterance "lines with no letter , character , or lower-case letter") (targetValue (name "?{or(or(<let>,<any>),<low>)}")))
(example (utterance "lines containing a vowel , string leftquoatation ! rightquoatation , and character") (targetValue (name "?{and(and(<vow>,<!>),<any>)}")))
(example (utterance "lines with zero or more vowels before a letter or leftquoatation ! rightquoatation") (targetValue (name "?{star(<vow>),or(<let>,<!>)}")))
(example (utterance "lines ending with a character , 5 or more times") (targetValue (name "?{repeatatleast(endwith(<any>),5)}")))
(example (utterance "lines with words with zero or more of a lower-case letter and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<!>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a lower-case letter or a capital letter , zero times or more") (targetValue (name "?{or(concat(<!>,<low>),<cap>)}")))
(example (utterance "lines containing a letter followed by a number 6 times") (targetValue (name "?{repeatatleast(concat(<cap>,<num>),6)}")))
(example (utterance "lines that have a character or capital letter followed by a number") (targetValue (name "?{or(<any>,<cap>),<num>}")))
(example (utterance "lines with a lower-case letter followed by a capital letter , at least twice") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines which are containing a letter , 6 or more times and leftquoatation ! rightquoatation") (targetValue (name "?{and(repeatatleast(<let>,6),<!>)}")))
(example (utterance "lines with words ending in zero or more letters") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with less than 2 capital letters without string leftquoatation ! rightquoatation") (targetValue (name "?{and(repeatatleast(<cap>,2),<!>)}")))
(example (utterance "the lines that begin with either a capital letter or lower-case letter") (targetValue (name "?{or(<cap>,<let>)}")))
(example (utterance "lines that contain a lower-case letter or leftquoatation ! rightquoatation , at least 3 times") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines that have words containing leftquoatation ! rightquoatation followed by zero or more lower-case letters") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with a letter before number or lower-case letter") (targetValue (name "?{<let>,or(<num>,<low>)}")))
(example (utterance "lines that do not contain a capital letter or leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{not(<cap>),star(<!>)}")))
(example (utterance "lines with more than zero string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{star(<!>),or(<@>,<#>)}")))
(example (utterance "leftquoatation ! rightquoatation first then lines with words containing a lower case letter") (targetValue (name "?{<!>,<low>}")))
(example (utterance "items with a vowel preceding leftquoatation ! rightquoatation or leftquoatation @ rightquoatation .") (targetValue (name "?{<vow>,or(<@>,<!>)}")))
(example (utterance "lines with a number after a letter at least 3 times") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation and a letter preceding a lower-case letter") (targetValue (name "?{and(<!>,<let>),<low>}")))
(example (utterance "lines with words that don't have character before number") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines beginning with a number or a vowel preceding leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,concat(<vow>,<!>))}")))
(example (utterance "lines with only zero or more of a number or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines containing either a lower-case letter , capital or a vowel") (targetValue (name "?{or(or(<low>,<cap>),<vow>)}")))
(example (utterance "lines not containing a number or a vowel") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines without a number , lower-case letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<low>),<!>)}")))
(example (utterance "lines which begin with vowels") (targetValue (name "?{<vow>}")))
(example (utterance "lines that doesn't have leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{not(concat(<!>,<num>))}")))
(example (utterance "lines that contain a characer , leftquoatation ! rightquoatation and a lower-case letter , but do not contain leftquoatation @ rightquoatation") (targetValue (name "?{star(<@>),and(and(<any>,<!>),<low>)}")))
(example (utterance "lines 4 or more times with leftquoatation ! rightquoatation or a lower-case letter .") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , lower-case letter , or letter") (targetValue (name "?{and(and(<!>,<low>),<let>)}")))
(example (utterance "lines that contain a vowel before a number") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines with a vowel or containing only leftquoatation ! rightquoatation at least once in them") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines that have words containing either leftquoatation ! rightquoatation , a capital letter , or a vowel") (targetValue (name "?{or(<vow>,<cap>),<!>}")))
(example (utterance "lines with vowel followed by number , 2 or more times") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "items with words containing leftquoatation ! rightquoatation and leftquoatation @ rightquoatation .") (targetValue (name "?{<!>,<@>}")))
(example (utterance "lines with words that contain the word leftquoatation ! rightquoatation , a vowel , or a lower-case letter") (targetValue (name "?{or(<vow>,<low>),<!>}")))
(example (utterance "lines containing a number , a character , and a letter") (targetValue (name "?{and(and(<num>,<any>),<cap>)}")))
(example (utterance "lines not having a number or leftquoatation ! rightquoatation") (targetValue (name "?{or(contain(<num>),<!>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation and number before capital letter") (targetValue (name "?{<!>,concat(<num>,<cap>)}")))
(example (utterance "lines with character or letter , 7 or more times") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines containing a capital letter followed by a number") (targetValue (name "?{<cap>,<num>}")))
(example (utterance "lines containing at least 2 letters") (targetValue (name "?{<let>}")))
(example (utterance "items without a terminating letter or character .") (targetValue (name "?{or(endwith(<any>),<let>)}")))
(example (utterance "a letter or a numbre appears at the end of the lines") (targetValue (name "?{endwith(or(<let>,<num>))}")))
(example (utterance "lines with a vowel before a number , letter , and character") (targetValue (name "?{<vow>,and(and(<num>,<let>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after leftquoatation @ rightquoatation 1 or more times") (targetValue (name "?{repeatatleast(<@>,1),<!>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before a vowel or letter") (targetValue (name "?{endwith(<!>),or(<vow>,<let>)}")))
(example (utterance "lines with words containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{or(<!>,<@>),<#>}")))
(example (utterance "lines with words and a number , string leftquoatation ! rightquoatation or 'truck , or vowel") (targetValue (name "?{or(or(<num>,<!>),<vow>),<@>}")))
(example (utterance "lines which are having leftquoatation ! rightquoatation , a lower-case letter , and a vowel before a character") (targetValue (name "?{and(and(<!>,<low>),<vow>),<any>}")))
(example (utterance "lines with a capital letter or character .") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines with words with either a number , a character , or a capital letter in them") (targetValue (name "?{or(<num>,<any>),<cap>}")))
(example (utterance "lines not ending in leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{endwith(or(<!>,<@>))}")))
(example (utterance "lines with words and 4 or more of leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines with 4 or more of a capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines starting with a number") (targetValue (name "?{contain(not(<num>))}")))
(example (utterance "lines beginning with either a number or a capital letter") (targetValue (name "?{or(<num>,<cap>)}")))
(example (utterance "items with a small letter preceding an uppercase letter .") (targetValue (name "?{<low>,star(<cap>)}")))
(example (utterance "lines with a characters or a lower-case letter before the string 'dog ;") (targetValue (name "?{or(<any>,<low>),endwith(<!>)}")))
(example (utterance "lines not with a lower-case letter , vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<vow>),<!>)}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a vowel") (targetValue (name "?{or(or(<!>,<@>),<vow>)}")))
(example (utterance "lines with a character followed by a capital") (targetValue (name "?{concat(<any>,<cap>),endwith(<let>)}")))
(example (utterance "leftquoatation ! rightquoatation walking is fun to do") (targetValue (name "?{or(<num>,<!>),<let>}")))
(example (utterance "lines without a capital before lower-case letter") (targetValue (name "?{concat(not(<cap>),<low>)}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(or(<vow>,<!>),<any>)}")))
(example (utterance "lines without a capital letter , lower-case letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<cap>,<low>),<!>)}")))
(example (utterance "lines which are containing a capital letter and leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{contain(and(<cap>,<!>))}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation , character , or capital letter") (targetValue (name "?{or(or(<!>,<any>),<cap>)}")))
(example (utterance "lines that do not have these items - a number , lower-case letter , or the word leftquoatation ! rightquoatation") (targetValue (name "?{or(and(and(<num>,<low>),<any>),<!>)}")))
(example (utterance "lines ending in words with a lower-case letter") (targetValue (name "?{repeatatleast(<low>,1)}")))
(example (utterance "lines that end with a vowel or character") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines without a lower-case letter and leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(and(<low>,<!>),<@>)}")))
(example (utterance "lines that have words in them which include beginning with a numeral") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , character , and vowel before letter") (targetValue (name "?{and(and(<!>,<any>),<vow>),<let>}")))
(example (utterance "lines with a vowel , a number , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<num>),<!>)}")))
(example (utterance "lines containing a lower-case letter after a number at least 1 time") (targetValue (name "?{<num>,repeatatleast(<low>,1)}")))
(example (utterance "items with a small letter at least 1 time before leftquoatation ! rightquoatation .") (targetValue (name "?{repeatatleast(<low>,1),<!>}")))
(example (utterance "lines with words with lower-case letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,<!>}")))
(example (utterance "items without leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation and a lower-case letter followed by a vowel") (targetValue (name "?{and(<!>,<low>),<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation prior to a vowel and lowercase letter") (targetValue (name "?{<!>,and(<vow>,<low>)}")))
(example (utterance "lines with a capital letter followed by dog") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with zero or more vowel before zero or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{concat(<vow>,<!>),star(<@>)}")))
(example (utterance "lines containing a character , lower-case letter , and a number") (targetValue (name "?{and(and(<any>,<low>),<num>)}")))
(example (utterance "lines that have a character preceding a letter , or containing a number") (targetValue (name "?{concat(<any>,<let>),contain(<num>)}")))
(example (utterance "lines with a vowel and the leftquoatation ! rightquoatation string beforehand") (targetValue (name "?{<!>,<vow>}")))
(example (utterance "lines with words and only a capital letter before leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,<!>}")))
(example (utterance "lines ending with a capital letter after leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>),<cap>}")))
(example (utterance "lines with words with zero or more of string leftquoatation ! rightquoatation or with string leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),<@>}")))
(example (utterance "lines with 3 or more letters before characters") (targetValue (name "?{<let>,repeatatleast(<any>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or lower-case letter before string leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<low>),<@>}")))
(example (utterance "items without a letter nor character .") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "at least twice lines with a character after a vowel") (targetValue (name "?{<vow>,repeatatleast(<any>,2)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , zero or more times before either a capital letter , character , or string leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),or(or(<cap>,<any>),<@>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation at least once or end with leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),endwith(<@>)}")))
(example (utterance "no minimum , a capital letter before any vowel in lines") (targetValue (name "?{<cap>,star(<vow>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation then capital letter") (targetValue (name "?{concat(<any>,<!>),<cap>}")))
(example (utterance "items with any letter .") (targetValue (name "?{or(or(<low>,<cap>),<let>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation at least once") (targetValue (name "?{repeatatleast(concat(<!>,<@>),1)}")))
(example (utterance "lines with character followed by string leftquoatation ! rightquoatation , 5 or more times") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines with a vowel after a number 1 or more times") (targetValue (name "?{<num>,repeatatleast(<vow>,1)}")))
(example (utterance "lines that have a vowel followed by a letter") (targetValue (name "?{<vow>,<let>}")))
(example (utterance "lines with words that contain character , 5 or more times") (targetValue (name "?{contain(<any>)}")))
(example (utterance "items with a small letter preceding a vowel .") (targetValue (name "?{<low>,<vow>}")))
(example (utterance "lines with either a capital letter , or string leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation before a character") (targetValue (name "?{or(or(<cap>,<!>),<@>),<any>}")))
(example (utterance "lines with a letter or character") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines with a letter before words that contain the string 'dog in them") (targetValue (name "?{<let>,contain(<!>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation , 4 or more times") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines without the word 'dog ; or an upper-case letter or any alphabet character") (targetValue (name "?{or(or(<let>,<!>),<cap>)}")))
(example (utterance "lines that doesn't have leftquoatation ! rightquoatation or lower-case letters") (targetValue (name "?{or(contain(<!>),<low>)}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation , capital letter , or number") (targetValue (name "?{startwith(<let>),or(or(<!>,<cap>),<num>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation , 'truck , or leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{or(or(<!>,<@>),<@>),<vow>}")))
(example (utterance "lines with a number or a vowel") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with lower-case letter , 4 or more times before capital letter") (targetValue (name "?{repeatatleast(<low>,4),<cap>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or a capital letter before string leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<cap>),<@>}")))
(example (utterance "lines with words that have leftquoatation ! rightquoatation at least 3 times") (targetValue (name "?{contain(<!>)}")))
(example (utterance "items with words with either a letter or leftquoatation ! rightquoatation .") (targetValue (name "?{or(<!>,<let>),<low>}")))
(example (utterance "items with a character , leftquoatation ! rightquoatation , and small letter before leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<any>,<!>),<low>),<@>}")))
(example (utterance "lines should have the following : string leftquoatation ! rightquoatation a letter and string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<let>,<!>),<@>)}")))
(example (utterance "lines with a letter followed by leftquoatation ! rightquoatation then a character , at least once") (targetValue (name "?{concat(<let>,<!>),repeatatleast(<any>,1)}")))
(example (utterance "lines ending with 7 or more of leftquoatation ! rightquoatation") (targetValue (name "?{endwith(contain(<!>))}")))
(example (utterance "lines containing a letter and leftquoatation ! rightquoatation") (targetValue (name "?{and(<!>,<let>)}")))
(example (utterance "lines that contain a capital letter followed by a letter , or leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<cap>,<let>),and(<!>,<@>)}")))
(example (utterance "lines containing words but not leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{not(<!>),<low>}")))
(example (utterance "lines with words with either string leftquoatation ! rightquoatation , a capital , or  vowel") (targetValue (name "?{or(<!>,<cap>),<vow>}")))
(example (utterance "lines with lower-case letter , zero or more times before number") (targetValue (name "?{star(<low>),<num>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation once") (targetValue (name "?{endwith(<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines with letter before a character at least once") (targetValue (name "?{<let>,repeatatleast(<any>,1)}")))
(example (utterance "lines with a capital letter , 2 or more times before a character in them") (targetValue (name "?{repeatatleast(<cap>,2),<any>}")))
(example (utterance "lines with a letter and a vowel after a character") (targetValue (name "?{<any>,and(<let>,<vow>)}")))
(example (utterance "items beginning with a small letter at least 1 time .") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "all lines") (targetValue (name "?{endwith(<vow>),star(<let>)}")))
(example (utterance "items with leftquoatation ! rightquoatation at least5times .") (targetValue (name "?{repeatatleast(<!>,5)}")))
(example (utterance "items with either a numeral , a letter , or leftquoatation ! rightquoatation preceding a vowel .") (targetValue (name "?{or(or(<num>,<!>),<let>),<vow>}")))
(example (utterance "lines starting in words with 6 or more of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,6)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , zero or more times or containing a letter in them") (targetValue (name "?{star(<!>),contain(<let>)}")))
(example (utterance "lines without a capital letter or character") (targetValue (name "?{and(<cap>,<any>)}")))
(example (utterance "lines with a vowel , string leftquoatation ! rightquoatation , and letter before number") (targetValue (name "?{and(and(<vow>,<!>),<let>),<num>}")))
(example (utterance "lines with 5 or more of a lower-case letter before a vowel") (targetValue (name "?{<low>,repeatatleast(<vow>,5)}")))
(example (utterance "lines with either a number , a lower-case letter , or leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<num>,<low>),<!>),<@>}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a capital letter followed by leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<@>),<cap>),<#>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before number or string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(<num>,<@>)}")))
(example (utterance "words ending with a number appears 5 or more times in the lines") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 3 or more times and lower-case letter or letter") (targetValue (name "?{repeatatleast(<!>,3),or(<low>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number and character") (targetValue (name "?{<!>,and(<num>,<any>)}")))
(example (utterance "lines with a capital followed by string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation in them") (targetValue (name "?{<num>,concat(<!>,<@>)}")))
(example (utterance "lines without a character , lower-case letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<low>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation preceding a vowel followed by a letter") (targetValue (name "?{concat(<!>,<vow>),<let>}")))
(example (utterance "lines with words ending with leftquoatation ! rightquoatation before a letter") (targetValue (name "?{endwith(<!>),<let>}")))
(example (utterance "ines with leftquoatation ! rightquoatation after a letter and a lower-case letter") (targetValue (name "?{and(<let>,<low>),<!>}")))
(example (utterance "lines with a lower-case letter before a number , occurring 4 or more times") (targetValue (name "?{<low>,repeatatleast(<num>,4)}")))
(example (utterance "before a character your lines should have a vowel , a letter , or leftquoatation ! rightquoatation string") (targetValue (name "?{or(or(<vow>,<let>),<!>),<any>}")))
(example (utterance "lines ending that have leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation") (targetValue (name "?{or(contain(<@>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and 'truck") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "lines ending with a number or letter") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines that only have a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines with a vowel before words with string leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,repeatatleast(<!>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , lower-case letter , or character before a capital letter") (targetValue (name "?{or(or(<!>,<low>),<any>),<cap>}")))
(example (utterance "lines containing a letter , a vowel , and a number") (targetValue (name "?{and(<let>,<vow>),<num>}")))
(example (utterance "lines that have a vowel or lower-case letter .") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation before number , string leftquoatation @ rightquoatation , and letter") (targetValue (name "?{contain(<!>),and(and(<num>,<@>),<let>)}")))
(example (utterance "a letter before a number and leftquoatation ! rightquoatation comprise the lines") (targetValue (name "?{and(<!>,<let>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and letter after a lower-case") (targetValue (name "?{<low>,and(<!>,<let>)}")))
(example (utterance "lines before ending a letter with string leftquoatation ! rightquoatation") (targetValue (name "?{<!>,endwith(<let>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{contain(and(<!>,<@>))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation at least once and ending with string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),endwith(<@>)}")))
(example (utterance "lines with words beginning with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<num>),<@>}")))
(example (utterance "lines with a vowel after leftquoatation ! rightquoatation at least 3 times") (targetValue (name "?{<!>,repeatatleast(<vow>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and lower-case letter before number") (targetValue (name "?{and(<!>,<low>),repeatatleast(<num>,1)}")))
(example (utterance "lines containing capital letter , lower-case letter , and vowel") (targetValue (name "?{and(and(<cap>,<low>),<vow>)}")))
(example (utterance "lines with character followed by vowel , 6 or more times") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel with a capital letter") (targetValue (name "?{concat(<!>,<vow>),contain(<cap>)}")))
(example (utterance "items with a numeral , a letter , and a vowel preceding a small letter .") (targetValue (name "?{and(and(<let>,<num>),<vow>),<low>}")))
(example (utterance "lines with words and a vowel , leftquoatation ! rightquoatation , or a lower-case letter") (targetValue (name "?{or(<vow>,<!>),<low>}")))
(example (utterance "items with leftquoatation ! rightquoatation at least twice .") (targetValue (name "?{<!>}")))
(example (utterance "lines containing capital letter or letter , zero or more times") (targetValue (name "?{or(<cap>,star(<let>))}")))
(example (utterance "lines with words that don't string leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with a capital letter before anything but a lower-case letter") (targetValue (name "?{<cap>,not(<low>)}")))
(example (utterance "lines that include leftquoatation ! rightquoatation plus a numeral plus any character") (targetValue (name "?{and(and(<any>,<num>),<!>)}")))
(example (utterance "lines with 3 or more vowels") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "lines ending with a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with character , 7 or more times and not  lower-case letter") (targetValue (name "?{repeatatleast(<any>,7),not(<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or character before vowel") (targetValue (name "?{or(<!>,<any>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or character ending in lower-case letter") (targetValue (name "?{or(<!>,<any>),endwith(<low>)}")))
(example (utterance "lines without a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines without a number or starting in a lower-case letter") (targetValue (name "?{not(<num>),startwith(<low>)}")))
(example (utterance "lines with words with capital letter and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,<!>)}")))
(example (utterance "lines with a capital letter or vowel") (targetValue (name "?{and(<cap>,<vow>)}")))
(example (utterance "lines with no leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{concat(star(<!>),<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a character , then leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),concat(<any>,<@>)}")))
(example (utterance "lines with words with a number before character") (targetValue (name "?{<num>,<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character or with vowel") (targetValue (name "?{concat(<!>,<any>),contain(<vow>)}")))
(example (utterance "lines containing either leftquoatation ! rightquoatation , a vowel , a letter , or 3 or more of leftquoatation @ rightquoatation") (targetValue (name "?{or(or(or(<!>,<vow>),<let>),<@>)}")))
(example (utterance "lines with 7 digits") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines with a letter before a lower-case letter then a number") (targetValue (name "?{<let>,concat(<low>,<num>)}")))
(example (utterance "lines leading off with the leftquoatation ! rightquoatation string and then a small letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "only the lines not containing leftquoatation ! rightquoatation or leftquoatation @ rightquoatation .") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with a lower-case letter or letter , 7 or more times") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines with words with either lower-case letter , capital letter , or number") (targetValue (name "?{or(<low>,<cap>),<num>}")))
(example (utterance "lines with words and containing either a number , a character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<any>),<!>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or non vowel") (targetValue (name "?{<!>,not(<vow>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation , leftquoatation @ rightquoatation and a letter") (targetValue (name "?{and(and(<!>,<@>),<let>)}")))
(example (utterance "lines with 3 or more numbers before a character") (targetValue (name "?{repeatatleast(<num>,3),<any>}")))
(example (utterance "a line with a letter followed by a number that is repeated 7 or more times") (targetValue (name "?{<let>,repeatatleast(<num>,7)}")))
(example (utterance "lines having a vowel , a number , and a lower-case letter") (targetValue (name "?{and(and(<vow>,<num>),<cap>),<low>}")))
(example (utterance "lines containing a letter at least zero times") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a number with string leftquoatation # rightquoatation") (targetValue (name "?{and(or(or(<!>,<@>),<num>),<#>)}")))
(example (utterance "lines with number or character before string leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<any>),<!>}")))
(example (utterance "lines without a capital letter , character , or letter") (targetValue (name "?{or(or(<cap>,<any>),<let>)}")))
(example (utterance "2 or more times lines should have a capital letter then a lower cased letter") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines with a number , zero or more times or leftquoatation ! rightquoatation before a character") (targetValue (name "?{star(<num>),concat(<!>,<any>)}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , letter , and character before vowel") (targetValue (name "?{and(and(<!>,<let>),<any>),<vow>}")))
(example (utterance "lines with a lower-case letter before a number or capital letter") (targetValue (name "?{<low>,or(<num>,<cap>)}")))
(example (utterance "items with 'dog preceding a vowel at least twice .") (targetValue (name "?{<!>,repeatatleast(<vow>,2)}")))
(example (utterance "lines with a capital before a lower-case letter before leftquoatation ! rightquoatation or a letter") (targetValue (name "?{concat(<cap>,<low>),or(<!>,<let>)}")))
(example (utterance "lines which are containing ending with a number , 7 or more times") (targetValue (name "?{endwith(repeatatleast(<num>,7))}")))
(example (utterance "lines containing capital letter or vowel and number") (targetValue (name "?{and(or(<cap>,<vow>),<num>)}")))
(example (utterance "lines ending with words with a character") (targetValue (name "?{star(<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter and number") (targetValue (name "?{<!>,and(<cap>,<num>)}")))
(example (utterance "lines that begin with a letter or a number") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before words ending in number") (targetValue (name "?{<!>,endwith(<num>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation , a character , and a lower-case letter followed by leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<any>),<low>),<@>}")))
(example (utterance "lines with words that contain a lower-case letter followed by leftquoatation ! rightquoatation , at least twice") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines which are starting with a capital letter or a character at least once") (targetValue (name "?{or(startwith(<cap>),<any>)}")))
(example (utterance "lines starting with a capital letter at least twice") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines containing any number of characters") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines without a letter , capital letter , or a number") (targetValue (name "?{and(and(<let>,<cap>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before string 'truck , lower-case letter , and character") (targetValue (name "?{<!>,and(and(<@>,<low>),<any>)}")))
(example (utterance "lines with a lower-case letter between leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,concat(<low>,<@>)}")))
(example (utterance "lines containing 3 capital letters before a character") (targetValue (name "?{repeatatleast(<cap>,3),<any>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation or a character at least6times .") (targetValue (name "?{or(concat(<!>,<any>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or letter before string leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<@>),<let>),<#>}")))
(example (utterance "lines that contain a character and ends with a letter") (targetValue (name "?{<any>,endwith(<let>)}")))
(example (utterance "lines containing a letter , a character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<any>),<!>}")))
(example (utterance "lines with number followed by character , zero or more times") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines with a lower-case letter before a vowel") (targetValue (name "?{or(<low>,<cap>),<vow>}")))
(example (utterance "items with a character , a vowel , or a letter .") (targetValue (name "?{and(and(<let>,<vow>),<any>)}")))
(example (utterance "lines that have at least 1 combination of a number before a character") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lines not ending in zero or more of a capital letter") (targetValue (name "?{endwith(star(<cap>))}")))
(example (utterance "items with words containing a letter , a small letter , and a vowel .") (targetValue (name "?{and(<vow>,<low>),<let>}")))
(example (utterance "lines that don't start with a capital letter") (targetValue (name "?{startwith(star(<cap>))}")))
(example (utterance "lines wherein at least once a letter is preceded by a vowel") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines do not contain 7 or more capital letters , nor a number") (targetValue (name "?{and(repeatatleast(<cap>,7),<num>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before vowel and string leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),and(<vow>,<@>)}")))
(example (utterance "items terminating with an upper case letter or a character .") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "lines with a character , number , or vowel") (targetValue (name "?{or(or(<any>,<num>),<vow>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or vowel at least once") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines containing a number which is followed by a capital letter") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "leftquoatation ! rightquoatation does not appears zero or more times in the lines") (targetValue (name "?{contain(star(<!>))}")))
(example (utterance "items without letters or a numeral .") (targetValue (name "?{or(or(<let>,<num>),<cap>)}")))
(example (utterance "lines containing a letter , lower-case letter , and vowel") (targetValue (name "?{and(<let>,<low>),<vow>}")))
(example (utterance "lines with a number and ending with a capital letter") (targetValue (name "?{<num>,endwith(<cap>)}")))
(example (utterance "lines containing a vowel followed by either a lower-case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{<vow>,or(or(<low>,<!>),<@>)}")))
(example (utterance "lines without a vowel or letter") (targetValue (name "?{or(or(<vow>,<cap>),<let>)}")))
(example (utterance "lines with a lower-case letter and leftquoatation ! rightquoatation") (targetValue (name "?{<let>,and(<low>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a character , with 6 or more times") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation before capital letter and vowel") (targetValue (name "?{contain(<!>),and(<cap>,<vow>)}")))
(example (utterance "lines with a capital letter before character") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or 'truck or a number before a letter") (targetValue (name "?{or(or(<!>,<@>),<num>),<let>}")))
(example (utterance "lines that have a character followed by either the word leftquoatation ! rightquoatation , a number , or a lower-case letter") (targetValue (name "?{or(or(<low>,<num>),<!>),<any>}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation before a vowel or string leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<!>,<vow>),<@>)}")))
(example (utterance "no lines having a number or leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines with less than 5 of leftquoatation ! rightquoatation") (targetValue (name "?{contain(repeatatleast(<!>,5))}")))
(example (utterance "lines ending with 3 or more letters") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with a number before a character at least 7 times") (targetValue (name "?{repeatatleast(<num>,7),<any>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation after leftquoatation @ rightquoatation , a capital letter and a number") (targetValue (name "?{and(and(<@>,<cap>),<num>),<!>}")))
(example (utterance "lines without a character , leftquoatation ! rightquoatation , or a number") (targetValue (name "?{and(and(<any>,<!>),<num>)}")))
(example (utterance "lines with a letter , number , and leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{and(and(<let>,<num>),<!>),<cap>}")))
(example (utterance "lines with 3 or more strings of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,3)}")))
(example (utterance "lines with words and a vowel and character before leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,concat(<any>,<!>)}")))
(example (utterance "lines not having a letter") (targetValue (name "?{or(or(<cap>,<low>),<let>)}")))
(example (utterance "lines with words with a capital and lower-case letter") (targetValue (name "?{and(<cap>,<low>)}")))
(example (utterance "lines with a letter occuring zero or more times before a number") (targetValue (name "?{star(<let>),<num>}")))
(example (utterance "lines not containing only a character or a leftquoatation ! rightquoatation .") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with  4 or more of a vowel before lower-case letter") (targetValue (name "?{<vow>,repeatatleast(<low>,4)}")))
(example (utterance "lines which are not containing only a letter , 5 or more times") (targetValue (name "?{repeatatleast(<let>,5)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation and a lower-case letter must come before a capital letter") (targetValue (name "?{and(<!>,<low>),<cap>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a vowel zero times") (targetValue (name "?{contain(or(<!>,<vow>))}")))
(example (utterance "lines that end with a number after a character") (targetValue (name "?{endwith(<any>),<num>}")))
(example (utterance "lines with no words with leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation or a lower-case letter , 1 time or more") (targetValue (name "?{or(startwith(<!>),<low>)}")))
(example (utterance "lines containing a vowel followed by a number") (targetValue (name "?{<vow>,<num>}")))
(example (utterance "lines with just 1 lower-case letter and 1 vowel") (targetValue (name "?{and(<low>,<vow>)}")))
(example (utterance "lines that have the leftquoatation ! rightquoatation dog followed by a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or ending with a character") (targetValue (name "?{<!>,endwith(<any>)}")))
(example (utterance "items with an uppe case letter preceding a small letter , at least5times .") (targetValue (name "?{<cap>,repeatatleast(<low>,5)}")))
(example (utterance "lines with zero or more of a character or string leftquoatation ! rightquoatation") (targetValue (name "?{star(or(<any>,<!>))}")))
(example (utterance "lines that have words with 1 of the following: a number , capital letter , or the word leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<num>),<!>}")))
(example (utterance "lines that have a lowercase letter , a character and a letter") (targetValue (name "?{and(and(<low>,<any>),<let>)}")))
(example (utterance "lines with a vowel at least once before a capital letter in them") (targetValue (name "?{repeatatleast(<vow>,1),<cap>}")))
(example (utterance "lines containing words that have a character preceding a vowel") (targetValue (name "?{<any>,<vow>}")))
(example (utterance "lines containing 6 or more lower-case letters") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines with a vowel took after by a lower-case letter at any rate once") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines with words that contain a character , a capital letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<!>),<cap>}")))
(example (utterance "lines contain a letter , leftquoatation ! rightquoatation , a vowel , followed by a capital letter") (targetValue (name "?{and(and(<let>,<!>),<vow>),<cap>}")))
(example (utterance "lines using the word with ending with a letter at least once .") (targetValue (name "?{repeatatleast(<let>,1)}")))
(example (utterance "lines with a letter before lower-case letter or string leftquoatation ! rightquoatation") (targetValue (name "?{<let>,or(<low>,<!>)}")))
(example (utterance "lines with a character before the leftquoatation ! rightquoatation then leftquoatation @ rightquoatation") (targetValue (name "?{concat(<any>,<!>),<@>}")))
(example (utterance "lines finishing with a lower-case letter preceding a number and leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<low>),and(<num>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , character , and number") (targetValue (name "?{and(and(<!>,<any>),<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by lower-case letter before a character") (targetValue (name "?{concat(<!>,<low>),<any>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , leftquoatation # rightquoatation , and leftquoatation $ rightquoatation") (targetValue (name "?{<!>,and(and(<@>,<#>),<$>)}")))
(example (utterance "lines not ending in string leftquoatation ! rightquoatation before character") (targetValue (name "?{concat(endwith(<!>),<any>)}")))
(example (utterance "lines that have 5 or more of the same number .") (targetValue (name "?{contain(<num>)}")))
(example (utterance "lines with words and a number and capital letter") (targetValue (name "?{and(<num>,<cap>),<let>}")))
(example (utterance "lines with 6 or more words that contain only a capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines which are not having a character and either a lower-case letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{not(<any>),or(or(<low>,<vow>),<!>)}")))
(example (utterance "lines not having a vowel followed by a capital letter in them") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines with a vowel before either a letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{<vow>,or(or(<let>,<!>),<@>)}")))
(example (utterance "lines with a lower-case letter , capital letter , and character before a number") (targetValue (name "?{and(and(<low>,<cap>),<any>),<num>}")))
(example (utterance "lines that either end with a number or start with leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<!>),endwith(<num>)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation before the word leftquoatation @ rightquoatation , which is in turn followed by the word leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),and(<any>,<#>)}")))
(example (utterance "lines with 5 or more of string leftquoatation ! rightquoatation with string leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{repeatatleast(<!>,5),concat(<@>,<#>)}")))
(example (utterance "multiple lines that do not a have number but contain a lower-case letter") (targetValue (name "?{and(<low>,<num>)}")))
(example (utterance "lines containing number , character , and a letter") (targetValue (name "?{and(and(<num>,<any>),<let>)}")))
(example (utterance "lines with words , a vowel , a number , string leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<num>),<!>}")))
(example (utterance "lines ending with character or string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{repeatatleast(or(<any>,<!>),6)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter .") (targetValue (name "?{<!>,<cap>}")))
(example (utterance "lines beginning with a letter followed by a lower case letter") (targetValue (name "?{startwith(<let>),<low>}")))
(example (utterance "lines with words that contain a letter or a character , zero times or more") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a character at least twice") (targetValue (name "?{<any>,repeatatleast(<!>,2)}")))
(example (utterance "lines that start with a letter or contain a lower-case letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<!>),startwith(<let>)}")))
(example (utterance "lines containing over 4 characters and capitals") (targetValue (name "?{repeatatleast(<any>,4),contain(<cap>)}")))
(example (utterance "lines starting in a vowel or character") (targetValue (name "?{repeatatleast(or(<vow>,<any>),1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character with a capital letter") (targetValue (name "?{concat(<!>,<any>),startwith(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and number or 6 or more lower-case letters") (targetValue (name "?{or(and(and(<!>,<num>),<@>),<low>)}")))
(example (utterance "lines with either a character , number , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<num>),<!>)}")))
(example (utterance "lines that have words and end with leftquoatation ! rightquoatation 1 or more times") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a character then a letter") (targetValue (name "?{concat(<!>,<any>),<let>}")))
(example (utterance "lines with a letter before a number or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<let>,or(or(<num>,<!>),<@>)}")))
(example (utterance "lines with a vowel before ending with a letter in tthem") (targetValue (name "?{<vow>,endwith(<let>)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines that do not have leftquoatation ! rightquoatation or a number") (targetValue (name "?{and(<!>,<num>)}")))
(example (utterance "lines which are ending with words with not leftquoatation ! rightquoatation") (targetValue (name "?{not(<!>)}")))
(example (utterance "lines with words that contain a character followed by a letter") (targetValue (name "?{<any>,<let>}")))
(example (utterance "lines having a vowel or a character 1 time .") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines with no numbers , capital letter , or character") (targetValue (name "?{or(or(<num>,<cap>),<any>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation and vowel , lower-case letter , and capital letter") (targetValue (name "?{not(<!>),and(and(<vow>,<low>),<cap>)}")))
(example (utterance "lines letters followed by a character") (targetValue (name "?{<let>,<any>}")))
(example (utterance "lines not containing a letter") (targetValue (name "?{not(repeatatleast(<let>,1))}")))
(example (utterance "lines with 2 or more of leftquoatation ! rightquoatation ending in a character") (targetValue (name "?{repeatatleast(<!>,2),endwith(<any>)}")))
(example (utterance "lines not starting with a character leftquoatation ! rightquoatation") (targetValue (name "?{not(startwith(<any>)),<!>}")))
(example (utterance "lines that must have a character , number or lower-case letter before the word leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<num>),<low>),<!>}")))
(example (utterance "lines that do not have a lower-case letter , leftquoatation ! rightquoatation , or a capital letter") (targetValue (name "?{or(or(<low>,<!>),<cap>)}")))
(example (utterance "lines containing zero or more times a character or a lower case letter and  leftquoatation ! rightquoatation") (targetValue (name "?{star(<any>),or(<!>,<low>)}")))
(example (utterance "lines with a capital letter , appearing at least twice , before leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,repeatatleast(<!>,2)}")))
(example (utterance "lines having lettered words or a character .") (targetValue (name "?{<let>,<any>}")))
(example (utterance "lines with a capital letter does or doesn't follow a character") (targetValue (name "?{star(<any>),<cap>}")))
(example (utterance "lines ending with a number before a letter") (targetValue (name "?{endwith(<num>),<any>}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(startwith(<!>),<any>)}")))
(example (utterance "lines containing a vowel before with or without a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<vow>,<low>),<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a lower-case letter once") (targetValue (name "?{contain(concat(<!>,<low>))}")))
(example (utterance "lines which contain a capital letter , a character , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<!>),<any>)}")))
(example (utterance "lines containing words with a number , a letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<let>),<!>}")))
(example (utterance "lines with a character , lower-case letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<low>),<!>)}")))
(example (utterance "lines with words with a character or string leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with a capital letter followed by a vowel or a number") (targetValue (name "?{or(<vow>,<num>),<cap>}")))
(example (utterance "lines without a number , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<vow>),<!>)}")))
(example (utterance "lines with a lower case letter followed by capital letter , zero or more time") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines ending in 6 or more of a vowel or letter") (targetValue (name "?{endwith(<vow>),repeatatleast(<let>,6)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation followed by a number , 3 times or more") (targetValue (name "?{repeatatleast(concat(<!>,<num>),3)}")))
(example (utterance "lines having vowel by leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,<!>}")))
(example (utterance "lines not containing a character , a capital letter , or a lower-case letter") (targetValue (name "?{and(and(<any>,<cap>),<low>)}")))
(example (utterance "items with a small letter preceding a vowel preceding leftquoatation ! rightquoatation .") (targetValue (name "?{<low>,concat(<vow>,<!>)}")))
(example (utterance "lines with a character or a vowel , 3 or more times in them") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines starting with number followed by string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<num>,<!>),<@>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,1),<!>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or character , 7 or more times") (targetValue (name "?{startwith(<!>),repeatatleast(<any>,7)}")))
(example (utterance "lines ending with words with a capital before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation and letter before a lower-case letter") (targetValue (name "?{not(<!>),concat(<let>,<low>)}")))
(example (utterance "lines that end with a letter and a number before a capital letter") (targetValue (name "?{endwith(<let>),concat(<num>,<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before letter followed by number") (targetValue (name "?{<!>,concat(<let>,<num>)}")))
(example (utterance "lines with letter followed by vowel , 7 or more times") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines with a vowel present which precedes either the leftquoatation ! rightquoatation string or a character") (targetValue (name "?{<vow>,or(<any>,<!>)}")))
(example (utterance "lines in which leftquoatation ! rightquoatation appears before a capital letter at least5times") (targetValue (name "?{<!>,repeatatleast(<cap>,5)}")))
(example (utterance "lines without a lower-case letter or string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<!>)}")))
(example (utterance "lines with words and a letter before a non number") (targetValue (name "?{<let>,not(<num>)}")))
(example (utterance "items with an upper caee letter or a character preceding a vowel .") (targetValue (name "?{or(<cap>,<any>),<vow>}")))
(example (utterance "lines with a character before either leftquoatation ! rightquoatation , a number , or leftquoatation @ rightquoatation in them") (targetValue (name "?{<any>,or(or(<!>,<num>),<@>)}")))
(example (utterance "lines without a number , capital letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<cap>),<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or containing a vowel , 5 or more times") (targetValue (name "?{or(<!>,contain(<vow>))}")))
(example (utterance "lines that all have a lower-case letter , a number , or a letter") (targetValue (name "?{or(<low>,<num>),<let>}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation followed by a number at least once") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines that contain a capital letter or leftquoatation ! rightquoatation and a number") (targetValue (name "?{or(<cap>,and(<!>,<num>))}")))
(example (utterance "lines containing a character or leftquoatation ! rightquoatation at least zero times") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "items with any letter .") (targetValue (name "?{or(or(<let>,<cap>),<low>)}")))
(example (utterance "lines not starting with 5 or more capital letters") (targetValue (name "?{startwith(repeatatleast(<cap>,5))}")))
(example (utterance "lines with character followed by string leftquoatation ! rightquoatation , 4 or more times") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or character") (targetValue (name "?{repeatatleast(or(<!>,<any>),1)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation once or more") (targetValue (name "?{<!>}")))
(example (utterance "lines with 7 or more vowels before a character") (targetValue (name "?{repeatatleast(<vow>,7),<any>}")))
(example (utterance "lines with a letter before vowel or number") (targetValue (name "?{or(concat(<let>,<vow>),<num>)}")))
(example (utterance "lines that contain at least 1 character") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines with or without a capital letter and a vowel before a character") (targetValue (name "?{star(<cap>),concat(<vow>,<any>)}")))
(example (utterance "lines with words zero or more times don't have a letter") (targetValue (name "?{star(<let>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , a lower-case letter , and leftquoatation @ rightquoatation in them") (targetValue (name "?{and(<!>,<low>),<@>}")))
(example (utterance "lines with words and zero or more of a capital letter and string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<cap>,<!>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or words that have a capital letter") (targetValue (name "?{<cap>,<!>}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , a lower-case letter , or leftquoatation @ rightquoatation followed by a letter") (targetValue (name "?{or(or(<!>,<low>),<@>),<let>}")))
(example (utterance "lines with words that contain lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<low>),<!>}")))
(example (utterance "lines with only a character and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<!>)}")))
(example (utterance "lines not having a letter and not leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation") (targetValue (name "?{and(<let>,or(<!>,<@>))}")))
(example (utterance "lines that begin with a capital letter or a character , at least 3 times") (targetValue (name "?{startwith(or(<cap>,<any>))}")))
(example (utterance "items with leftquoatation ! rightquoatation and any letter preceding a vowel .") (targetValue (name "?{and(and(<!>,<cap>),<low>),<vow>}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation or either leftquoatation @ rightquoatation , leftquoatation # rightquoatation , or leftquoatation $ rightquoatation in them") (targetValue (name "?{<!>,or(or(<@>,<#>),<$>)}")))
(example (utterance "lines which are containing a character before a lower-case letter") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines that have words with either a capital letter  , leftquoatation ! rightquoatation or a letter") (targetValue (name "?{or(<cap>,<!>),<let>}")))
(example (utterance "lines do not contain a character , a lower-case letter , or a number") (targetValue (name "?{or(or(<any>,<low>),<num>)}")))
(example (utterance "lines with a number , capital letter , or vowel") (targetValue (name "?{and(and(<num>,<cap>),<vow>)}")))
(example (utterance "lines with a letter followed by vowel and string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{concat(<let>,<vow>),repeatatleast(<!>,6)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a letter before a number") (targetValue (name "?{or(or(<!>,<@>),<let>),<num>}")))
(example (utterance "lines which are containing a vowel and a letter at least once") (targetValue (name "?{contain(and(<vow>,<let>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before 5 or more vowels") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines which end with a single letter .") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number with a character after") (targetValue (name "?{<!>,concat(<num>,<any>)}")))
(example (utterance "lines with a capital letter before words with string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{<cap>,concat(<!>,<low>)}")))
(example (utterance "lines without a vowel , a number , or leftquoatation ! rightquoatation as well as a lower-case letter") (targetValue (name "?{and(or(or(<vow>,<num>),<!>),<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 3 or more times preceded by a number") (targetValue (name "?{<num>,repeatatleast(<!>,3)}")))
(example (utterance "lines that end with either a number or a lower-case letter") (targetValue (name "?{endwith(or(<num>,<low>))}")))
(example (utterance "lines with words and a number or starting in a capital letter") (targetValue (name "?{<num>,startwith(<cap>)}")))
(example (utterance "lines with a character not before a capital letter") (targetValue (name "?{<cap>,not(<any>)}")))
(example (utterance "lines with words ending with a lower-case letter") (targetValue (name "?{<low>}")))
(example (utterance "lines end with a capital letter and a character") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines which are containing leftquoatation ! rightquoatation or ending with a character") (targetValue (name "?{or(<!>,endwith(<any>))}")))
(example (utterance "lines with 3 or more of a vowel or letter") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines beginning with a number or a letter") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines beging with a number preceded by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "a letter appears before leftquoatation ! rightquoatation and preceded by a vowel in the lines") (targetValue (name "?{<let>,concat(<!>,<vow>)}")))
(example (utterance "lines with a numbers and characters follow lower-case letters") (targetValue (name "?{<low>,and(<any>,<num>)}")))
(example (utterance "lines with a number or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , 5 or more times in it") (targetValue (name "?{or(<num>,concat(<!>,<@>))}")))
(example (utterance "lines with a capital letter before a lower-case letter") (targetValue (name "?{<cap>,contain(<low>)}")))
(example (utterance "lines with a vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{contain(concat(<vow>,<!>))}")))
(example (utterance "lines with the letter string leftquoatation ! rightquoatation followed by a vowel") (targetValue (name "?{<!>,concat(<let>,<vow>)}")))
(example (utterance "lines containing either leftquoatation ! rightquoatation , a number , or a lower-case letter preceding leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<num>),<low>),<@>}")))
(example (utterance "lines containing numbers and characters") (targetValue (name "?{<num>,<any>}")))
(example (utterance "lines with characters at least once , ending with a number") (targetValue (name "?{repeatatleast(<any>,1),endwith(<num>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by either a character , a letter , or a number") (targetValue (name "?{<!>,or(or(<any>,<let>),<num>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a vowel , and a lower-case letter") (targetValue (name "?{and(and(<!>,<vow>),<low>)}")))
(example (utterance "lines with a character before vowel 3 times") (targetValue (name "?{contain(concat(<any>,<vow>))}")))
(example (utterance "these lines can have a number and a capital letter , but they don't have to .") (targetValue (name "?{or(<num>,<cap>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation or leftquoatation @ rightquoatation 7 or more times .") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "items with words containing any letter .") (targetValue (name "?{and(<let>,<vow>),<low>}")))
(example (utterance "lines with less than 5 of a string leftquoatation ! rightquoatation or 'truck") (targetValue (name "?{not(<!>),repeatatleast(<@>,5)}")))
(example (utterance "lines with a character before ending in a number") (targetValue (name "?{<any>,endwith(contain(<num>))}")))
(example (utterance "lines containing only string leftquoatation ! rightquoatation , 4 or more times") (targetValue (name "?{repeatatleast(<!>,4)}")))
(example (utterance "lines which are having either a vowel , a capital letter , or a letter before a number") (targetValue (name "?{or(or(<vow>,<cap>),<let>),<num>}")))
(example (utterance "lines with number , 3 or more times before capital letter") (targetValue (name "?{repeatatleast(<num>,3),<cap>}")))
(example (utterance "lines with either a number , a vowel , or character") (targetValue (name "?{or(<num>,<vow>),<any>}")))
(example (utterance "lines which are not having a capital letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with a number before letter and string leftquoatation ! rightquoatation") (targetValue (name "?{and(concat(<num>,<let>),<!>)}")))
(example (utterance "lines with words with ending which are containing only leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines not containing a vowel or a letter") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a letter before a character") (targetValue (name "?{or(or(<!>,<@>),<let>),<any>}")))
(example (utterance "lines ending with letter or string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<let>),and(<!>,<@>)}")))
(example (utterance "lines with words with a letter preceding a capital letter") (targetValue (name "?{<let>,<cap>}")))
(example (utterance "lines which are ending with a character or a number at least once") (targetValue (name "?{or(<any>,repeatatleast(<num>,1))}")))
(example (utterance "lines with leftquoatation ! rightquoatation with leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,contain(<@>))}")))
(example (utterance "lines not having a lower-case letter or leftquoatation ! rightquoatation , 6 or more times in them") (targetValue (name "?{or(<low>,repeatatleast(<!>,6))}")))
(example (utterance "lines that have a capital letter plus a character") (targetValue (name "?{and(<cap>,<any>)}")))
(example (utterance "the lines contained both a letter and a  number .") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines with words ending in vowel or number") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines having either a lower-case letter , leftquoatation ! rightquoatation , or a number before a letter in them") (targetValue (name "?{or(or(<low>,<!>),<num>),<let>}")))
(example (utterance "lines without the leftquoatation ! rightquoatation string present , as well as no letter and no vowel within") (targetValue (name "?{and(and(<!>,<let>),<vow>)}")))
(example (utterance "lines with a vowel 2 times before a number") (targetValue (name "?{repeatatleast(<vow>,2),<num>}")))
(example (utterance "lines having either a character , a number , or leftquoatation ! rightquoatation before a letter in them") (targetValue (name "?{or(or(<any>,<num>),<!>),<let>}")))
(example (utterance "items with an uppercase letter and a small letter before leftquoatation ! rightquoatation .") (targetValue (name "?{and(<cap>,<low>),<!>}")))
(example (utterance "lines with vowel ending words") (targetValue (name "?{<vow>}")))
(example (utterance "lines with a letter before a lower-case letter or a number in them") (targetValue (name "?{<let>,or(<low>,<num>)}")))
(example (utterance "lines containing a character , a vowel , and a letter") (targetValue (name "?{concat(<any>,<vow>),<let>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , capital letter , and vowel before leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<cap>),<vow>),<@>}")))
(example (utterance "lines with a number before a vowel at least 2 times") (targetValue (name "?{<num>,repeatatleast(<vow>,2)}")))
(example (utterance "lines containing 7 or more incidents of leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(concat(<!>,<@>),7)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and vowel") (targetValue (name "?{<!>,and(<@>,<vow>)}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation , lower-case letter , or character") (targetValue (name "?{<num>,or(or(<!>,<low>),<any>)}")))
(example (utterance "lines with zero or more numbers or characters") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "items with a vowel or leftquoatation ! rightquoatation preceding a small letter .") (targetValue (name "?{or(<!>,<vow>),<low>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a lower-case letter or a capital letter") (targetValue (name "?{<!>,or(<low>,<cap>)}")))
(example (utterance "lines that do not have a lower-case letter , a number , or a letter") (targetValue (name "?{and(and(<low>,<num>),<let>)}")))
(example (utterance "lines with a letter , string leftquoatation ! rightquoatation , and capital letter before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<let>,<!>),<cap>),<@>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before a letter or character") (targetValue (name "?{concat(<!>,<let>),<any>}")))
(example (utterance "lines with no letter or capital letter") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines with a letter before vowel") (targetValue (name "?{<let>,repeatatleast(<vow>,1)}")))
(example (utterance "lines ending with a vowel at least 3 times") (targetValue (name "?{repeatatleast(<vow>,3)}")))
(example (utterance "items with a vowel or leftquoatation ! rightquoatation preceding a character .") (targetValue (name "?{or(<vow>,<!>),<any>}")))
