(example (utterance "lines that end with either a vowel or a capital letter") (targetValue (name "?{or(<cap>,<vow>)}")))
(example (utterance "lines with words and a vowel , character , and lower-case letter") (targetValue (name "?{and(<vow>,<any>),<low>}")))
(example (utterance "items with a numeral preceding leftquoatation ! rightquoatation and an upper case letter .") (targetValue (name "?{<num>,and(<!>,<cap>)}")))
(example (utterance "lines with vowel followed by number before lower-case letter") (targetValue (name "?{concat(<vow>,<num>),<low>}")))
(example (utterance "lines which contain a capital letter before a lower case letter zero or more times .") (targetValue (name "?{<cap>,star(<low>)}")))
(example (utterance "lines in which a capital letter is followed by a leftquoatation ! rightquoatation string , coming before a letter that's lower-case") (targetValue (name "?{concat(<cap>,<!>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number or a letter in them") (targetValue (name "?{<!>,or(<num>,<let>)}")))
(example (utterance "lines with a lower-case letter at least 4 times or a vowel before a capital letter") (targetValue (name "?{repeatatleast(<low>,4),concat(<vow>,<cap>)}")))
(example (utterance "lines with 5 or more letters before a number") (targetValue (name "?{repeatatleast(<let>,5),<num>}")))
(example (utterance "lines containing a letter or 5 or more characters") (targetValue (name "?{startwith(<let>),repeatatleast(<any>,5)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation after words with a character") (targetValue (name "?{<any>,<!>}")))
(example (utterance "items with a vowel , or leftquoatation ! rightquoatation , or a upper case letter preceding leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<cap>,<!>),<vow>),<@>}")))
(example (utterance "lines with a number followed by a vowel , zero times before leftquoatation ! rightquoatation") (targetValue (name "?{star(concat(<num>,<vow>)),<!>}")))
(example (utterance "lines containing a vowel that is not preceded by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines without words with a number and not leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<num>,<!>),<@>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a capital letter , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<cap>),<@>),<#>}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a small letter .") (targetValue (name "?{and(and(<!>,<low>),<@>)}")))
(example (utterance "lines containing a letter or leftquoatation ! rightquoatation but not leftquoatation @ rightquoatation") (targetValue (name "?{star(<@>),or(<let>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{<!>,or(<@>,<vow>)}")))
(example (utterance "lines not having 4 times character") (targetValue (name "?{repeatatleast(<any>,4)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation then a number or string leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),or(or(<num>,<#>),<#>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a number , and leftquoatation @ rightquoatation after leftquoatation # rightquoatation") (targetValue (name "?{<#>,and(and(<!>,<num>),<@>)}")))
(example (utterance "lines with words with a lower-case letter or ending in string leftquoatation ! rightquoatation") (targetValue (name "?{<low>,endwith(<!>)}")))
(example (utterance "lines having upper case letters without leftquoatation ! rightquoatation or a number .") (targetValue (name "?{or(or(<low>,<!>),<num>)}")))
(example (utterance "lines containing a letter , 5 or more times followed by a lower-case letter") (targetValue (name "?{repeatatleast(<let>,5),<low>}")))
(example (utterance "lines with more than zero string leftquoatation ! rightquoatation and a letter before leftquoatation ! rightquoatation") (targetValue (name "?{and(<!>,<let>),star(<@>)}")))
(example (utterance "lines which are starting with leftquoatation ! rightquoatation or a letter , zero or more times") (targetValue (name "?{star(or(<!>,<let>))}")))
(example (utterance "lines without a number a letter and a character") (targetValue (name "?{and(and(<num>,<let>),<any>)}")))
(example (utterance "lines starting in leftquoatation ! rightquoatation or letter with a capital letter") (targetValue (name "?{startwith(<!>),concat(<let>,<cap>)}")))
(example (utterance "lines with a lower-case letter bfore string leftquoatation ! rightquoatation and vowel") (targetValue (name "?{<low>,and(<!>,<vow>)}")))
(example (utterance "lines with a letter or a capital letter before either leftquoatation ! rightquoatation , a vowel , or leftquoatation @ rightquoatation in them") (targetValue (name "?{or(<let>,<cap>),or(or(<!>,<vow>),<@>)}")))
(example (utterance "lines that do not contain a number , a capital letter , or a letter") (targetValue (name "?{and(and(<num>,<cap>),<let>)}")))
(example (utterance "lines with a number following a letter") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines with 3 or more letters or leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<cap>,3),contain(<!>)}")))
(example (utterance "lines containing words ending with a capital letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<cap>),<!>}")))
(example (utterance "lines containing  letter , 5 or more times") (targetValue (name "?{repeatatleast(<let>,5)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation more than 5 times and a letter after a number") (targetValue (name "?{repeatatleast(<!>,5),concat(<num>,<let>)}")))
(example (utterance "lines with words and stating with leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a lower-case letter , a vowel , and a character") (targetValue (name "?{and(and(<low>,<vow>),<any>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a capital letter , as well as leftquoatation @ rightquoatation followed by a number") (targetValue (name "?{and(and(<!>,<cap>),<@>),<num>}")))
(example (utterance "lines with no capital letters before leftquoatation ! rightquoatation") (targetValue (name "?{concat(not(<cap>),<!>)}")))
(example (utterance "lines that have a lower-case letter preceded by a number") (targetValue (name "?{<num>,<low>}")))
(example (utterance "lines with words that contain a number followed by a capital letter , 3 times or more") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines that have a character and a number followed by leftquoatation ! rightquoatation") (targetValue (name "?{<!>,and(<any>,<num>)}")))
(example (utterance "the lines that start with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation repeated 6 times or more") (targetValue (name "?{repeatatleast(concat(<!>,<@>),6)}")))
(example (utterance "lines about a person and a dog .") (targetValue (name "?{<any>,<!>}")))
(example (utterance "6or more times in lines use a character before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines that contain a vowel , a character , or a number followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<any>),<num>),<!>}")))
(example (utterance "items with words contating leftquoatation ! rightquoatation , an upper case letter , and a number .") (targetValue (name "?{and(<num>,<!>),<cap>}")))
(example (utterance "lines ending with 2 or more lower-case letters or characters") (targetValue (name "?{endwith(or(<low>,<any>))}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{<any>,or(<!>,<low>)}")))
(example (utterance "lines that contain a leftquoatation ! rightquoatation string at least 3 times before a capital letter occurs") (targetValue (name "?{repeatatleast(<!>,3),<cap>}")))
(example (utterance "lines with a capital letter 7 times before a vowel") (targetValue (name "?{repeatatleast(<cap>,7),<vow>}")))
(example (utterance "lines which contain a letter before leftquoatation ! rightquoatation and a character") (targetValue (name "?{<let>,and(<!>,<any>)}")))
(example (utterance "lines that conclude with the leftquoatation ! rightquoatation string , after which an upper-case letter is seen") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines with words with 7 or more numbers") (targetValue (name "?{<num>}")))
(example (utterance "lines with 6 or more of a character before a number") (targetValue (name "?{repeatatleast(<any>,6),<num>}")))
(example (utterance "lines which are containing a lower-case letter before a character and leftquoatation ! rightquoatation") (targetValue (name "?{and(concat(<low>,<any>),<!>)}")))
(example (utterance "lines with a character or a letter") (targetValue (name "?{contain(or(<any>,<let>))}")))
(example (utterance "lines with words with a capital letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation before a number in them") (targetValue (name "?{and(and(<cap>,<!>),<@>),<num>}")))
(example (utterance "lines not ending in a vowel or letter") (targetValue (name "?{endwith(or(<vow>,<let>))}")))
(example (utterance "lines that start with a number followed by a letter") (targetValue (name "?{startwith(<num>),<let>}")))
(example (utterance "the lines that do not have a capital letter , lower case letter , or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<let>),<!>)}")))
(example (utterance "items with an upper case letter preceding a character at least twice .") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "none of the lines should have a vowel , a capital letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<cap>)}")))
(example (utterance "lines with words that contain a vowel , a letter , and a character") (targetValue (name "?{and(<vow>,<let>),<any>}")))
(example (utterance "lines beginning with a character preceded by a letter in lower-case") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines that dont have lower-case letters , the string dog , or a number") (targetValue (name "?{or(or(<low>,<num>),<!>)}")))
(example (utterance "leftquoatation ! rightquoatation or a vowel appears 2 or more times in the end of the lines") (targetValue (name "?{endwith(or(<!>,<vow>))}")))
(example (utterance "lines that do not contain a letter , capital letter , or a character") (targetValue (name "?{and(and(<let>,<cap>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or 5 or more of leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines containing a lower-case letter after either leftquoatation ! rightquoatation , a number , or a letter") (targetValue (name "?{or(or(<!>,<num>),<let>),<low>}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation and with a lower-case letter") (targetValue (name "?{endwith(<!>),<low>}")))
(example (utterance "lines with a vowel after a capital letter at least 3 times") (targetValue (name "?{<cap>,repeatatleast(<vow>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or without leftquoatation @ rightquoatation , at least 7 times .") (targetValue (name "?{or(<!>,not(<@>))}")))
(example (utterance "items with a vowel preceding a letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{concat(<vow>,<let>),<!>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation that occurs before a vowel") (targetValue (name "?{startwith(<!>),<vow>}")))
(example (utterance "lower-case letter before 'dog or a vowel") (targetValue (name "?{<low>,or(<!>,<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a small letter or letter .") (targetValue (name "?{<!>,or(<low>,<let>)}")))
(example (utterance "items with an upper case letter or vowel preceding a numeral .") (targetValue (name "?{or(<vow>,<cap>),<num>}")))
(example (utterance "lines with words with a capital letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(<cap>,<!>),<@>}")))
(example (utterance "lines with 3 more capital letters") (targetValue (name "?{repeatatleast(<cap>,3)}")))
(example (utterance "items with either leftquoatation ! rightquoatation or a letter .") (targetValue (name "?{or(or(<let>,<!>),<cap>)}")))
(example (utterance "lines with either a letter , a character , or leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<any>),<!>),<@>}")))
(example (utterance "lines containing a vowel followed by a capital letter , a character , and the word leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,and(and(<cap>,<any>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation and leftquoatation # rightquoatation") (targetValue (name "?{and(concat(<!>,<@>),<#>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , with any amount before a capital letter") (targetValue (name "?{not(star(<!>)),<cap>}")))
(example (utterance "lines with a letter and a vowel before any lower-case letter") (targetValue (name "?{and(<let>,<vow>),<low>}")))
(example (utterance "lines with a number and a vowel , letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,1),or(or(<vow>,<let>),<!>)}")))
(example (utterance "lines with words with a capital or lower-case letter") (targetValue (name "?{<cap>,<low>}")))
(example (utterance "lines starting in words with a number or letter") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "ines containing a number followed by leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , as well as a character") (targetValue (name "?{<num>,and(and(<!>,<@>),<any>)}")))
(example (utterance "lines  with 7 or more vowels") (targetValue (name "?{repeatatleast(<vow>,7)}")))
(example (utterance "lines with words with either a number , a lower-case letter , or a capital letter in them") (targetValue (name "?{or(<num>,<low>),<cap>}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation before a letter or string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<let>,<cap>),<@>)}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation , and a vowel") (targetValue (name "?{<cap>,and(<!>,<vow>)}")))
(example (utterance "lines with a number and string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a character") (targetValue (name "?{and(and(<num>,<!>),<@>),<any>}")))
(example (utterance "lines with a capital letter , number , or leftquoatation ! rightquoatation and a vowel") (targetValue (name "?{and(or(or(<cap>,<num>),<!>),<vow>)}")))
(example (utterance "lines with lower-case letter or vowel , 5 or more times") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or number , 2 or more times") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines which don't contain just a number") (targetValue (name "?{<num>}")))
(example (utterance "lines with a number before vowel then string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,concat(<vow>,<!>)}")))
(example (utterance "lines starting in 6 or more of a vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(or(<vow>,<!>),6)}")))
(example (utterance "lines with lower-case letter before a capital letter") (targetValue (name "?{<low>,not(<cap>)}")))
(example (utterance "lines having number , string leftquoatation ! rightquoatation , and string leftquoatation @ rightquoatation before a lower-case letter") (targetValue (name "?{and(and(<num>,<!>),<@>),<low>}")))
(example (utterance "lines with 3 or more of a letter before capital letter") (targetValue (name "?{<let>,repeatatleast(<cap>,3)}")))
(example (utterance "lines are empty") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with a letter and number , or leftquoatation ! rightquoatation") (targetValue (name "?{or(and(<let>,<num>),<!>)}")))
(example (utterance "lines with a capital letter before a vowel then string leftquoatation ! rightquoatation") (targetValue (name "?{concat(concat(<cap>,<vow>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a lower-case letter , or string 'truck before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<low>),<@>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a character after a number") (targetValue (name "?{repeatatleast(<num>,1),or(<!>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by 6 or more capital letters") (targetValue (name "?{contain(<!>),repeatatleast(<cap>,6)}")))
(example (utterance "lines containing words that have a number followed by a vowel and a lower-case letter") (targetValue (name "?{concat(<num>,<vow>),<low>}")))
(example (utterance "lines starting with words having a number or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines start with 1 letter followed by vowel .") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines that have a number followed by a letter") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines that do not contain a number 5 or more times") (targetValue (name "?{repeatatleast(<num>,5)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines with words that contain 5 or more of leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines with words that don't have a lower-case letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines where leftquoatation ! rightquoatation has a letter before it , no less than zero times") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with at least 1 capital letter with a character before it") (targetValue (name "?{<any>,repeatatleast(<cap>,1)}")))
(example (utterance "lines with a lower-case letter , vowel , or character") (targetValue (name "?{or(or(<low>,<vow>),<any>)}")))
(example (utterance "lines ending with 2 or more characters") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines with a vowel , a capital letter , as well as leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{and(and(<vow>,<cap>),<!>),<num>}")))
(example (utterance "lines with some kind of letter or vowel or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<let>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before ending in letter") (targetValue (name "?{<!>,endwith(<let>)}")))
(example (utterance "lines with words with either a letter , vowel , or capital letter") (targetValue (name "?{or(<let>,<vow>),<cap>}")))
(example (utterance "lines that contain words with a capital letter followed by leftquoatation ! rightquoatation , zero times or more") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with character before not a capital and not a lower-case letter") (targetValue (name "?{<any>,not(and(<cap>,<low>))}")))
(example (utterance "your choice , lines with either leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines without a vowel or with capital letter") (targetValue (name "?{or(repeatatleast(<vow>,1),<cap>)}")))
(example (utterance "lines contains leftquoatation ! rightquoatation , a character , or a capital letter at least 6 times") (targetValue (name "?{or(or(<!>,<any>),<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least once or which are ending with a character") (targetValue (name "?{repeatatleast(<!>,1),endwith(<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation then leftquoatation @ rightquoatation and a character or number") (targetValue (name "?{concat(<!>,<@>),or(<any>,<num>)}")))
(example (utterance "lines ending with a vowel or letter") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation or number") (targetValue (name "?{concat(<cap>,<!>),repeatatleast(<num>,1)}")))
(example (utterance "lines with number containing only a character , 7 or more times") (targetValue (name "?{<num>,repeatatleast(<any>,7)}")))
(example (utterance "items without a numeral , a letter , or leftquoatation ! rightquoatation .") (targetValue (name "?{or(or(<!>,<num>),<let>)}")))
(example (utterance "lines containing words having either a letter , a capital letter , or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<cap>),<!>),<@>}")))
(example (utterance "lines with a7 or more of a capital letter or a letter before character") (targetValue (name "?{repeatatleast(<cap>,7),concat(<let>,<any>)}")))
(example (utterance "lines with a lower-case letter followed by leftquoatation ! rightquoatation once or more") (targetValue (name "?{contain(concat(<low>,<!>))}")))
(example (utterance "lines with a lower-case letter and a vowel or a capital letter , 4 or more times in them") (targetValue (name "?{or(and(<low>,<vow>),<cap>)}")))
(example (utterance "lines that have the string 'ring , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a letter") (targetValue (name "?{or(or(<@>,<!>),<#>),<let>}")))
(example (utterance "lines with a number 2 times") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines containing a lower-case letter or ending with a number 1 or more times") (targetValue (name "?{repeatatleast(<low>,1),endwith(<num>)}")))
(example (utterance "lines containing letter , lower-case letter , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<low>),<!>)}")))
(example (utterance "lines with a capital letter then a lower-case letter and leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<low>),star(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , zero or more times or starting with a vowel in them") (targetValue (name "?{star(<!>),startwith(<vow>)}")))
(example (utterance "lines without a number not being a letter") (targetValue (name "?{not(and(<num>,<let>))}")))
(example (utterance "lines without a character , vowel , and number") (targetValue (name "?{and(and(<any>,<vow>),<num>)}")))
(example (utterance "lines that contain a lower-case letter at least once") (targetValue (name "?{<low>}")))
(example (utterance "lines which are starting with words that contain only leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines with words and a letter before a capital letter and then leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<cap>),<!>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation , at least 7 times") (targetValue (name "?{startwith(<!>),repeatatleast(<@>,7)}")))
(example (utterance "lines with words with vowel , lower-case letter , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<low>),<!>}")))
(example (utterance "lines starting in 2 or more characters") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines containing a capital letter followed by the word leftquoatation ! rightquoatation") (targetValue (name "?{<!>,<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least once not being leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),not(<@>)}")))
(example (utterance "lines with 6 or more letters before a lower-case letter") (targetValue (name "?{repeatatleast(<let>,6),<low>}")))
(example (utterance "lines not starting with character") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines which are not having leftquoatation ! rightquoatation or a lower-case letter followed by leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),concat(<low>,<@>)}")))
(example (utterance "lines that begin with a lower-case letter or leftquoatation ! rightquoatation , zero times or more") (targetValue (name "?{or(startwith(<!>),<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or at least 1 capital letter") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines which are starting with words with not a letter") (targetValue (name "?{not(<let>)}")))
(example (utterance "either a letter or a lower case letter first , then a capital letter") (targetValue (name "?{or(<let>,<low>),<cap>}")))
(example (utterance "lines ending with a number after 2 or more times a lower case letter") (targetValue (name "?{repeatatleast(<low>,2),endwith(<num>)}")))
(example (utterance "at least 1 line ending with a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{endwith(concat(<let>,<!>))}")))
(example (utterance "items with a character preceding a numeral .") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines starting in 7 or more of a number before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(concat(<num>,<!>),7)}")))
(example (utterance "lines having words with a vowel before a character") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with a letter or a lower-case letter before leftquoatation ! rightquoatation in them") (targetValue (name "?{or(<let>,<low>),<!>}")))
(example (utterance "lines containing a lower-case letter , a capital letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<low>,<cap>),<!>),<@>}")))
(example (utterance "lines with 4 of leftquoatation ! rightquoatation before capital letters") (targetValue (name "?{<!>,repeatatleast(<cap>,4)}")))
(example (utterance "lines with no vowel and leftquoatation ! rightquoatation") (targetValue (name "?{and(not(<vow>),<!>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation ending with a letter") (targetValue (name "?{star(<!>),endwith(<let>)}")))
(example (utterance "items with a small letter preceding a letter at least 4 times .") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines ending in lower-case letter or vowel") (targetValue (name "?{endwith(or(<low>,<vow>))}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , a character , and a vowel") (targetValue (name "?{and(and(<!>,<any>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a number at least 4 times") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation after a lower-case letter , a capital letter , and a character") (targetValue (name "?{and(and(<low>,<cap>),<any>),<!>}")))
(example (utterance "lines that have either leftquoatation ! rightquoatation , a capital letter , or leftquoatation @ rightquoatation followed by a letter") (targetValue (name "?{or(or(<!>,<cap>),<@>),<let>}")))
(example (utterance "lines with a lower-case letter preceding a character at least 6 times") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines with a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before not a vowel , 2 or more times in them") (targetValue (name "?{<!>,not(repeatatleast(<vow>,2))}")))
(example (utterance "lines with number before letter , 3 or more times") (targetValue (name "?{<num>,repeatatleast(<let>,3)}")))
(example (utterance "lines with a vowel after a lower-case letter followed by a character") (targetValue (name "?{concat(<low>,<any>),<vow>}")))
(example (utterance "lines with lower-case letter before a number") (targetValue (name "?{<low>,contain(<num>)}")))
(example (utterance "lines with words starting in a character before vowel") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "lines with less than 7 of the strings'dog' or 'truck") (targetValue (name "?{or(contain(<!>),<@>)}")))
(example (utterance "items with a small letter preceding leftquoatation ! rightquoatation at least5times .") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines with a character preceded by a letter zero or more times") (targetValue (name "?{star(<let>),<any>}")))
(example (utterance "lines with a number before lower-case or capital letter") (targetValue (name "?{concat(<num>,<low>),repeatatleast(<cap>,1)}")))
(example (utterance "lines without a character or leftquoatation ! rightquoatation before a capital") (targetValue (name "?{or(<any>,concat(<!>,<cap>))}")))
(example (utterance "lines containing a number and a lower-case letter followed by a vowel") (targetValue (name "?{and(<num>,<low>),<vow>}")))
(example (utterance "lines with a capital followed by lower-case letter , 5 or more times") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "only leftquoatation ! rightquoatation in the lines") (targetValue (name "?{<!>}")))
(example (utterance "lines containing characters") (targetValue (name "?{and(<any>,<cap>)}")))
(example (utterance "lines with a vowel , number , or string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<num>),<!>),<@>}")))
(example (utterance "lines with a capital letter or a lower-case letter 3 or more times") (targetValue (name "?{repeatatleast(<cap>,3),<low>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a vowel , leftquoatation @ rightquoatation and a small letter .") (targetValue (name "?{<!>,and(and(<vow>,<@>),<low>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding an upper case letter , a letter , and leftquoatation @ rightquoatation .") (targetValue (name "?{<!>,and(and(<cap>,<let>),<@>)}")))
(example (utterance "lines with a lower-case letter coming before leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{<low>,concat(<!>,<let>)}")))
(example (utterance "lines with 1 number and letter") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines with 6 or more characters or string leftquoatation ! rightquoatation to start") (targetValue (name "?{or(<any>,startwith(<!>))}")))
(example (utterance "lines ending by a character or number after a vowel .") (targetValue (name "?{or(concat(<vow>,<any>),<num>)}")))
(example (utterance "items with a upper case letter preceding a small letter at least 1 time .") (targetValue (name "?{<cap>,repeatatleast(<low>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , lower-case letter , and vowel before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<low>),<vow>),<@>}")))
(example (utterance "lines with a capital letter or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(<cap>,<!>),<@>}")))
(example (utterance "lines not having a letter or string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{or(not(<let>),<!>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation before a character") (targetValue (name "?{<!>,<any>}")))
(example (utterance "lines which are not having a lower-case letter , not leftquoatation ! rightquoatation , and not a capital letter") (targetValue (name "?{and(and(<low>,<!>),<cap>)}")))
(example (utterance "lines that include words with a character after a vowel") (targetValue (name "?{<vow>,<any>}")))
(example (utterance "lines that contain a character or a number followed by a capital letter") (targetValue (name "?{or(<any>,<num>),<cap>}")))
(example (utterance "lower case letter or numbers appears at least once in the lines") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines with the 4 or more of leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{<!>,repeatatleast(<low>,4)}")))
(example (utterance "lines with a capital letter following a lower-case letter at least5times") (targetValue (name "?{<low>,repeatatleast(<cap>,5)}")))
(example (utterance "lines with words 2 of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<!>,2)}")))
(example (utterance "lines which are containing leftquoatation ! rightquoatation before leftquoatation @ rightquoatation at least once") (targetValue (name "?{contain(<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel , lower-case letter , or character") (targetValue (name "?{<!>,or(or(<vow>,<low>),<any>)}")))
(example (utterance "lines with either 2 vowels or letters") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "items with a vowel and a character .") (targetValue (name "?{and(<any>,<vow>)}")))
(example (utterance "lines that start with at least 1 character before a vowel") (targetValue (name "?{startwith(repeatatleast(<any>,1)),<vow>}")))
(example (utterance "lines that avoid using leftquoatation ! rightquoatation more than once .") (targetValue (name "?{not(<!>)}")))
(example (utterance "lines with words with a lower-case letter , number , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<num>),<!>}")))
(example (utterance "lines that have a lower-case letter , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(or(<low>,<!>),<any>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation and a character , or leftquoatation @ rightquoatation at least once") (targetValue (name "?{or(and(<!>,<any>),<@>)}")))
(example (utterance "lines containing character or leftquoatation ! rightquoatation at least 5 times") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines with a vowel then leftquoatation ! rightquoatation 2 times") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "lines ending with a letter followed by a lower-case letter at least 4 times") (targetValue (name "?{endwith(concat(<let>,<low>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter or a capital letter in them") (targetValue (name "?{<!>,or(<let>,<cap>)}")))
(example (utterance "lines finishing with a capital letter , twice or more  or a vowel") (targetValue (name "?{or(repeatatleast(<cap>,2),<vow>)}")))
(example (utterance "lines ending in zero or more of a character or vowel") (targetValue (name "?{or(endwith(<any>),<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation and leftquoatation # rightquoatation 5 or more times") (targetValue (name "?{contain(and(and(<!>,<@>),<#>))}")))
(example (utterance "lines ending with zero or more lower-case letters") (targetValue (name "?{endwith(star(<low>))}")))
(example (utterance "lines that end with a vowel and do not have a lower-case letter") (targetValue (name "?{not(<low>),endwith(<vow>)}")))
(example (utterance "multiple lines containing a vowel repeated 6 times") (targetValue (name "?{<vow>}")))
(example (utterance "lines containing a lower case letter followed by any letter , 3 or more times .") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines not having character at least once and not a lower-case letter") (targetValue (name "?{and(repeatatleast(<any>,1),<low>)}")))
(example (utterance "lines do not contain a lower-case letter , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(or(<low>,<!>),<any>)}")))
(example (utterance "lines with a capital letter followed by a lower-case letter 7 times or more") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines containing words with a capital letter at least 7 times") (targetValue (name "?{<cap>}")))
(example (utterance "lines with a number after leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a vowel") (targetValue (name "?{and(and(<!>,<@>),<vow>),<num>}")))
(example (utterance "lines with less than 6 of a vowel before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(concat(<vow>,<!>),6)}")))
(example (utterance "items without a numeral , an upper case letter , or a vowel .") (targetValue (name "?{or(or(<num>,<vow>),<cap>)}")))
(example (utterance "lines with a vowel and a letter before lower-case letter") (targetValue (name "?{and(<vow>,<let>),<low>}")))
(example (utterance "multiple lines containing words that are then followed by a number and finally by a capital letter") (targetValue (name "?{<num>,<cap>}")))
(example (utterance "lines start with words and end with a number") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation after a capital letter") (targetValue (name "?{<cap>,and(and(<vow>,<!>),<@>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation zero or more times and also containing a vowel") (targetValue (name "?{star(<!>),<vow>}")))
(example (utterance "lines with a capital letter before a character followed by a lower-case letter in them") (targetValue (name "?{<cap>,concat(<any>,<low>)}")))
(example (utterance "lines not having a lower-case letter or leftquoatation ! rightquoatation and a vowel in them") (targetValue (name "?{not(<low>),and(<!>,<vow>)}")))
(example (utterance "lines with zero or more of string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{star(<!>),concat(<@>,<#>)}")))
(example (utterance "lines that end with a capital letter or a vowel") (targetValue (name "?{or(<cap>,<vow>)}")))
(example (utterance "lines with a lower-case letter or letter") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines with words with starting with a character or a vowel in them") (targetValue (name "?{startwith(<any>),<vow>}")))
(example (utterance "lines with words and a lower-case letter , string leftquoatation ! rightquoatation , and capital letter") (targetValue (name "?{and(<low>,<!>),<cap>}")))
(example (utterance "lines which are having either a character , leftquoatation ! rightquoatation , or a number before a capital letter") (targetValue (name "?{or(or(<any>,<!>),<num>),<cap>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a lettter , a character and leftquoatation truck' .") (targetValue (name "?{<!>,and(and(<any>,<let>),<@>)}")))
(example (utterance "lines not having a vowel followed by a letter at least once in them") (targetValue (name "?{repeatatleast(concat(<vow>,<let>),1)}")))
(example (utterance "lines with words ending in capital letter") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines without string leftquoatation ! rightquoatation 3 times") (targetValue (name "?{repeatatleast(<!>,3)}")))
(example (utterance "lines with words with a character") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines containing a number first , followed by a character") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "any line with a character followed by a vowel at least once") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "items with a small letter preceding a numeral or a vowel .") (targetValue (name "?{<low>,and(<vow>,<num>)}")))
(example (utterance "lines containing words with a capital letter , as well as a letter before a number .") (targetValue (name "?{and(<cap>,<let>),<num>}")))
(example (utterance "identify lines that contain a lower-case letter and end with leftquoatation ! rightquoatation") (targetValue (name "?{<low>,endwith(<!>)}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation or a vowel before leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),concat(<vow>,<@>)}")))
(example (utterance "all lines") (targetValue (name "?{star(concat(<num>,<vow>))}")))
(example (utterance "lines that have only 1 letter preceding the word leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "liens with 2 or more of a vowel before capital letter") (targetValue (name "?{<vow>,repeatatleast(<cap>,2)}")))
(example (utterance "lines with a character , string leftquoatation ! rightquoatation , and letter before a capital letter") (targetValue (name "?{and(and(<any>,<!>),<let>),<cap>}")))
(example (utterance "lines that have a lower-case letter before leftquoatation ! rightquoatation and then at least once capital letters") (targetValue (name "?{concat(<low>,<!>),repeatatleast(<cap>,1)}")))
(example (utterance "lines with a character and letter before a lower-case letter") (targetValue (name "?{and(<any>,<let>),<low>}")))
(example (utterance "lines with the 4 or more of a string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{or(concat(<!>,<@>),<vow>)}")))
(example (utterance "lines containing a number , a letter , and a lower-case letter") (targetValue (name "?{and(<num>,<let>),<low>}")))
(example (utterance "lines which contain a lower-case letter or vowel before leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<vow>),<!>}")))
(example (utterance "lines starting in vowel or leftquoatation ! rightquoatation and character") (targetValue (name "?{or(<vow>,and(<!>,<any>))}")))
(example (utterance "lines with a capital letter after a vowel") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by character or a capital at least once") (targetValue (name "?{concat(<!>,<any>),repeatatleast(<cap>,1)}")))
(example (utterance "lines with number before either string leftquoatation ! rightquoatation , a vowel , or string leftquoatation @ rightquoatation") (targetValue (name "?{<num>,or(or(<!>,<vow>),<@>)}")))
(example (utterance "lines with a lower-case letter before a vowel") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "items with a numeral or letter before a small letter .") (targetValue (name "?{or(<num>,<let>),<low>}")))
(example (utterance "lines with words , a lower-case letter , and vowel") (targetValue (name "?{<low>,<vow>}")))
(example (utterance "lines containing a lower-case letter and the word dog , followed by a number , then a capital letter") (targetValue (name "?{or(or(<low>,<!>),<num>),<cap>}")))
(example (utterance "lines with a capital letter in them before ending with a character") (targetValue (name "?{<cap>,endwith(<any>)}")))
(example (utterance "lines containing either a capital , string leftquoatation ! rightquoatation , or a vowel") (targetValue (name "?{or(or(<cap>,<!>),<vow>)}")))
(example (utterance "lines with lower-case letter or a character") (targetValue (name "?{or(<low>,contain(<any>))}")))
(example (utterance "lines with words with either lower-case letter , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<vow>),<!>}")))
(example (utterance "lines with a vowel after a lower-case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<low>,<!>),<@>),<vow>}")))
(example (utterance "items with a character preceding an upper case letter at least thrice .") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation after a vowel or a letter") (targetValue (name "?{or(<vow>,<let>),<!>}")))
(example (utterance "lines which are starting with leftquoatation ! rightquoatation before a capital letter , zero or more times") (targetValue (name "?{startwith(<!>),star(<cap>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation followed by a lower-case letter , at least 1 time") (targetValue (name "?{endwith(concat(<!>,<low>))}")))
(example (utterance "lines containing zero times vowel") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "lines not having leftquoatation ! rightquoatation ending with a character") (targetValue (name "?{not(<!>),endwith(<any>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation and characters") (targetValue (name "?{endwith(<!>),star(<any>)}")))
(example (utterance "lines ending with 2 or more numbers") (targetValue (name "?{repeatatleast(<num>,2)}")))
(example (utterance "lines with zero or more characters before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(star(<any>),<!>)}")))
(example (utterance "lines not containing a character , followed by at least 1 capital letter") (targetValue (name "?{repeatatleast(concat(<any>,<cap>),1)}")))
(example (utterance "lines with 2 or more characters before a number") (targetValue (name "?{repeatatleast(<any>,2),<num>}")))
(example (utterance "lines with words and a number , leftquoatation ! rightquoatation , or a vowel") (targetValue (name "?{or(<num>,<!>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation ending with leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),endwith(<#>)}")))
(example (utterance "items with a letter , small letter , and a vowel .") (targetValue (name "?{and(and(<vow>,<low>),<let>)}")))
(example (utterance "lines containing a letter , 3 or more times in them .") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines with 2 or more characters") (targetValue (name "?{<any>}")))
(example (utterance "lines containing at least 1 lower-case letter") (targetValue (name "?{repeatatleast(<low>,1)}")))
(example (utterance "lines beginning with a lower-case letter or a number 1 or more times") (targetValue (name "?{repeatatleast(or(<low>,<num>),1)}")))
(example (utterance "lines with a character before a letter , capital letter , and number") (targetValue (name "?{<any>,and(and(<let>,<cap>),<num>)}")))
(example (utterance "items beginning with a vowel preceding a small letter .") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines with a number before a letter or vowel") (targetValue (name "?{<num>,or(or(<let>,<vow>),<cap>)}")))
(example (utterance "lines that begin with 2 capital letters followed by leftquoatation ! rightquoatation or a character") (targetValue (name "?{concat(<cap>,<!>),repeatatleast(<any>,2)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation followed by number , zero or more times") (targetValue (name "?{star(concat(<!>,<num>))}")))
(example (utterance "items with a numeral preceding an upper case letter .") (targetValue (name "?{<num>,not(<cap>)}")))
(example (utterance "lines with words and either leftquoatation ! rightquoatation , a character , or number") (targetValue (name "?{or(<!>,<any>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or leftquoatation # rightquoatation or a capital letter") (targetValue (name "?{<!>,or(or(<@>,<#>),<cap>)}")))
(example (utterance "lines with words ending in a number") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines containing either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a lower-case letter , and followed by leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<@>),<low>),<#>}")))
(example (utterance "lines that have a letter in lower-case followed by a number") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "lines ending with a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<vow>),repeatatleast(<!>,1)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or lower-case letter") (targetValue (name "?{or(concat(<!>,<@>),<low>)}")))
(example (utterance "lines which words contain either of these - letter , number , or vowel") (targetValue (name "?{or(<any>,<let>),<num>}")))
(example (utterance "lines with letter before character and lower-case letter") (targetValue (name "?{<let>,and(<any>,<low>)}")))
(example (utterance "lines which are ending with a vowel followed by a letter at least once") (targetValue (name "?{repeatatleast(concat(<vow>,<let>),1)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation then capital letter") (targetValue (name "?{startwith(<!>),<cap>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation at least once , and followed by leftquoatation @ rightquoatation and a vowel") (targetValue (name "?{repeatatleast(<!>,1),and(<@>,<vow>)}")))
(example (utterance "lines containing words having a letter , a character , as well as leftquoatation ! rightquoatation , 4 times or more") (targetValue (name "?{and(and(<let>,<any>),<!>)}")))
(example (utterance "lines with letter before vowel and number") (targetValue (name "?{<let>,and(<vow>,<num>)}")))
(example (utterance "lines with zero or more characters before a number") (targetValue (name "?{contain(star(<any>)),<num>}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation at least 5 times a long with a letter") (targetValue (name "?{repeatatleast(<!>,5),<let>}")))
(example (utterance "items without a numeral , vowel , or an upper case letter .") (targetValue (name "?{and(and(<vow>,<cap>),<num>)}")))
(example (utterance "lines with 4 or more vowels or starting in string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<vow>,4),startwith(<!>)}")))
(example (utterance "lines without a capital") (targetValue (name "?{not(<cap>),repeatatleast(<let>,1)}")))
(example (utterance "lines that has leftquoatation ! rightquoatation or a vowel before leftquoatation @ rightquoatation") (targetValue (name "?{or(<vow>,<!>),<@>}")))
(example (utterance "lines with a number , character , and letter before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<any>),<let>),<!>}")))
(example (utterance "lines with capital letter after vowels") (targetValue (name "?{<vow>,not(<low>)}")))
(example (utterance "lines with at least 5 vowels before a number") (targetValue (name "?{repeatatleast(<vow>,5),<num>}")))
(example (utterance "items terminating with a character at least5times .") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation followed by number") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines with a vowel and letter before leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<let>),<!>}")))
(example (utterance "lines that contain a capital letter followed by a letter and a character , or leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>),and(<let>,<any>)}")))
(example (utterance "items without an upper case letter or vowel .") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a vowel , and leftquoatation @ rightquoatation preceding an upper case letter .") (targetValue (name "?{and(and(<vow>,<!>),<@>),<cap>}")))
(example (utterance "lines with a lower-case letter and leftquoatation ! rightquoatation before a number") (targetValue (name "?{and(<low>,<!>),<num>}")))
(example (utterance "ines with a character or a capital letter occurring before leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<cap>),<!>}")))
(example (utterance "lines with vowel , 5 or more times before character") (targetValue (name "?{repeatatleast(<vow>,5),<any>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before number") (targetValue (name "?{endwith(<!>),<num>}")))
(example (utterance "lines containing words with a lower-case letter or character") (targetValue (name "?{<low>,<any>}")))
(example (utterance "items with with any letter or character .") (targetValue (name "?{or(<let>,<low>),<any>}")))
(example (utterance "lines containing words that contain only a vowel preceding leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,<!>}")))
(example (utterance "a number or leftquoatation ! rightquoatation , a capital letter , or leftquoatation @ rightquoatation appears at least once in the lines") (targetValue (name "?{repeatatleast(<num>,1),or(or(<!>,<cap>),<@>)}")))
(example (utterance "lines with a capital letter or leftquoatation ! rightquoatation , 4 or more times in them") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines with lower-case letter or number , 4 or more times") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines that have a number followed by a letter") (targetValue (name "?{<num>,contain(<let>)}")))
(example (utterance "lines wherein either an uppercase letter , a letter , or leftquoatation ! rightquoatation are preceded by leftquoatation @ rightquoatation") (targetValue (name "?{<@>,or(or(<cap>,<let>),<!>)}")))
(example (utterance "lines with a 2 or more of a number followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines that don't have a character or a vowel") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation , number , and vowel") (targetValue (name "?{and(<!>,<num>),<vow>}")))
(example (utterance "lines containing a number or leftquoatation ! rightquoatation , then followed by leftquoatation @ rightquoatation") (targetValue (name "?{contain(or(<num>,<!>)),<@>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a letter after a character") (targetValue (name "?{<any>,or(<let>,<!>)}")))
(example (utterance "lines that have a letter in lower-case coming before something that isn't a vowel") (targetValue (name "?{<low>,not(<vow>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation and vowel before containing a letter") (targetValue (name "?{and(<!>,<vow>),contain(<let>)}")))
(example (utterance "lines with a lower-case letter , a capital letter , and a letter") (targetValue (name "?{and(and(<low>,<cap>),<let>)}")))
(example (utterance "lines with words stating in a character or number") (targetValue (name "?{startwith(<any>),<num>}")))
(example (utterance "lines without a number or capital letter") (targetValue (name "?{not(<num>),star(<cap>)}")))
(example (utterance "lines with zero or more of the string 'dog'before a vowel then number") (targetValue (name "?{<!>,star(concat(<vow>,<num>))}")))
(example (utterance "lines with 7 or more of leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{<!>,repeatatleast(<@>,7)}")))
(example (utterance "lines with 3 or more characters or lower-case letters") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "lines without a number before string leftquoatation ! rightquoatation") (targetValue (name "?{not(concat(<num>,<!>))}")))
(example (utterance "lines not ending with vowel at least once") (targetValue (name "?{endwith(repeatatleast(<vow>,1))}")))
(example (utterance "lines with a vowel preceding a capital letter at least zero times") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before capital letter , string leftquoatation @ rightquoatation , and vowel") (targetValue (name "?{<!>,and(and(<cap>,<@>),<vow>)}")))
(example (utterance "lines with 2 or more vowels or leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before ending with a capital letter at least once in them") (targetValue (name "?{<!>,endwith(repeatatleast(<cap>,1))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a letter at least 6 times") (targetValue (name "?{<!>,repeatatleast(<let>,6)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case or upper case letter") (targetValue (name "?{<!>,or(or(<low>,<cap>),<let>)}")))
(example (utterance "lines with no letter , capital letter , or character") (targetValue (name "?{or(or(<let>,<cap>),<any>)}")))
(example (utterance "items beginning with a character preceding a vowel .") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "items without a character at least6times .") (targetValue (name "?{repeatatleast(<any>,6)}")))
(example (utterance "lines with a character or vowel before capital letter") (targetValue (name "?{or(<any>,<vow>),<cap>}")))
(example (utterance "lines without a letter and ending in a character") (targetValue (name "?{contain(<let>),endwith(<any>)}")))
(example (utterance "lines containing words before a singular small letter") (targetValue (name "?{<let>,<low>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation at least once before string leftquoatation @ rightquoatation") (targetValue (name "?{startwith(repeatatleast(<!>,1)),<@>}")))
(example (utterance "lines with character before vowel or starting with a capital") (targetValue (name "?{concat(<any>,<vow>),startwith(<cap>)}")))
(example (utterance "items begining with an upper case letter or a character .") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before ending with vowel at least once") (targetValue (name "?{<!>,endwith(repeatatleast(<vow>,1))}")))
(example (utterance "lines not having vowel , not a letter , and not a capital letter") (targetValue (name "?{and(and(<vow>,<let>),<cap>)}")))
(example (utterance "items with any letter at least5times .") (targetValue (name "?{or(<low>,<cap>)}")))
(example (utterance "the lines that have leftquoatation ! rightquoatation and a capital letter , but not leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a letter and leftquoatation @ rightquoatation .") (targetValue (name "?{<!>,and(<let>,<@>)}")))
(example (utterance "lines with a vowel as well as leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{contain(<vow>),or(<!>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number or letter") (targetValue (name "?{<!>,and(and(<num>,<cap>),<low>)}")))
(example (utterance "lines not starting with at least 1 of leftquoatation ! rightquoatation") (targetValue (name "?{startwith(repeatatleast(<!>,1))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation at least once and containing leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,1),contain(<@>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a lower-case letter in them") (targetValue (name "?{and(<!>,<@>),<low>}")))
(example (utterance "lines with with a vowel preceded by a character") (targetValue (name "?{endwith(<any>),<vow>}")))
(example (utterance "items with a small letter preceding leftquoatation ! rightquoatation at least thrice .") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines containing a capital letter followed by a vowel") (targetValue (name "?{<cap>,<vow>}")))
(example (utterance "no letter , number , or vowel is in the lines") (targetValue (name "?{or(or(<let>,<num>),<vow>)}")))
(example (utterance "lines having a capital letter preceding by a lower-case letter , vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<vow>),<!>),<cap>}")))
(example (utterance "lines that end with only a character") (targetValue (name "?{<any>}")))
(example (utterance "lines that have either a character , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation before a number") (targetValue (name "?{or(or(<!>,<any>),<@>),<num>}")))
(example (utterance "lines that don't contain leftquoatation ! rightquoatation more than once") (targetValue (name "?{contain(repeatatleast(<!>,2))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation but not a vowel") (targetValue (name "?{or(<!>,not(<vow>))}")))
(example (utterance "lines with letter before lower-case letter or vowel") (targetValue (name "?{<let>,or(<low>,<vow>)}")))
(example (utterance "lines with a capital letter before a number") (targetValue (name "?{<cap>,contain(<num>)}")))
(example (utterance "lines without a number or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<num>,<!>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter followed by a letter , zero or more times in them") (targetValue (name "?{<!>,star(concat(<low>,<let>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending in string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,endwith(<@>)}")))
(example (utterance "lines with a character , a number , and leftquoatation ! rightquoatation after a letter") (targetValue (name "?{<let>,and(and(<any>,<num>),<!>)}")))
(example (utterance "lines with words starting in string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<!>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation before 3 or more vowels") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines containing a vowel as well as a lower-case letter followed by a number") (targetValue (name "?{and(<vow>,<low>),<num>}")))
(example (utterance "lines with words ending with a capital  or character") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines that conclude with a letter numbering at least 6 times or any number of times  more than 6") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with a number followed by a character and a lower-case letter") (targetValue (name "?{concat(<num>,<any>),<low>}")))
(example (utterance "lines with 3 or more number before a vowel") (targetValue (name "?{repeatatleast(<num>,3),<vow>}")))
(example (utterance "lines with vowel , zero or more times before lower-case letter and a capital") (targetValue (name "?{star(<vow>),and(<low>,<cap>)}")))
(example (utterance "lines that have6or more letters or the word ''dog'") (targetValue (name "?{repeatatleast(<let>,6),startwith(<!>)}")))
(example (utterance "lines containing 5 or more times a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines not having lower-case letter , not a vowel , and not a character") (targetValue (name "?{and(and(<low>,<vow>),<any>)}")))
(example (utterance "lines without a capital letter before a number and string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{not(<cap>),and(and(<num>,<!>),<@>)}")))
(example (utterance "lines with a character before a vowel at least 5 times") (targetValue (name "?{repeatatleast(<any>,5),<vow>}")))
(example (utterance "lines contain a lower-case letter preceding a vowel before a character") (targetValue (name "?{concat(<low>,<vow>),<any>}")))
(example (utterance "items with words containing a numeral , a letter , or a vowel .") (targetValue (name "?{or(<vow>,<let>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number followed by a vowel in them") (targetValue (name "?{<!>,concat(<num>,<vow>)}")))
(example (utterance "lines with a capital letter , leftquoatation ! rightquoatation , or a letter") (targetValue (name "?{or(or(<cap>,<!>),<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or character before a capital letter") (targetValue (name "?{or(or(<!>,<@>),<any>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least once before a character") (targetValue (name "?{repeatatleast(<!>,1),contain(<any>)}")))
(example (utterance "lines that contain 7 or more times leftquoatation ! rightquoatation followed by a charactere") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines with words starting with characters followed by any number") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "items with a vowel or letter and a character preceding an upper case letter .") (targetValue (name "?{and(and(<let>,<vow>),<any>),<cap>}")))
(example (utterance "lines with a character , lower-case letter , and leftquoatation ! rightquoatation before a number") (targetValue (name "?{and(and(<any>,<low>),<!>),<num>}")))
(example (utterance "lines with vowel then a character") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "liens with a letter before a number or string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<let>),or(<num>,<!>)}")))
(example (utterance "lines with 4 ore more numbers before a lower-case letter") (targetValue (name "?{repeatatleast(<num>,4),<low>}")))
(example (utterance "lines with a vowel before number then lower-case letter") (targetValue (name "?{<vow>,concat(<num>,<low>)}")))
(example (utterance "lines with words  ending with only a character") (targetValue (name "?{<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a vowel , or a character after a letter") (targetValue (name "?{<let>,or(or(<!>,<vow>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter , character , and string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<low>,<any>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation at least zero times") (targetValue (name "?{contain(concat(<@>,<!>))}")))
(example (utterance "lines with 2 or more of a number or lower-case letter") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "lines containing a lower-case letter or a capital letter , followed by a number") (targetValue (name "?{or(<low>,<cap>),<num>}")))
(example (utterance "items with either a letter or a character .") (targetValue (name "?{or(or(<cap>,<any>),<low>)}")))
(example (utterance "lines with words with a letter and containing only a lower-case letter in them") (targetValue (name "?{<let>,<low>}")))
(example (utterance "lines with words , a number , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<!>)}")))
(example (utterance "lines with a letter before words with string leftquoatation ! rightquoatation") (targetValue (name "?{<let>,contain(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , letter , and number") (targetValue (name "?{and(and(<!>,<let>),<num>)}")))
(example (utterance "lines that end with words that contain a lower-case letter , a number , or the word leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<!>),<num>)}")))
(example (utterance "lines with words with zero or more number before string leftquoatation ! rightquoatation") (targetValue (name "?{star(<num>),<!>}")))
(example (utterance "lines that don't start with a vowel before a capital letter") (targetValue (name "?{concat(startwith(<vow>),<cap>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation or contain a vowel followed by a capital letter") (targetValue (name "?{or(<!>,<vow>),<cap>}")))
(example (utterance "lines with words with a letter and string leftquoatation ! rightquoatation , 5 or more times") (targetValue (name "?{<let>,repeatatleast(<!>,5)}")))
(example (utterance "lines with words with letter or string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation") (targetValue (name "?{<let>,and(<!>,<@>)}")))
(example (utterance "lines containing words without a lower-case letter") (targetValue (name "?{<low>}")))
(example (utterance "lines with words with either leftquoatation ! rightquoatation , a number , or leftquoatation @ rightquoatation or a capital letter in them") (targetValue (name "?{or(or(<!>,<num>),<@>),<cap>}")))
(example (utterance "lines with any type of letter") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines starting in string leftquoatation ! rightquoatation without string leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),not(<@>)}")))
(example (utterance "items with a small letter or vowel at least 1 time .") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines with words with a capital followed by lower-case letter , zero or more times") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines containing at least 1 number or letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<let>),<!>}")))
(example (utterance "lines beginning with a vowel or a number") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines starting with capital letter or number , zero or more times") (targetValue (name "?{or(startwith(<cap>),<num>)}")))
(example (utterance "lines containing words and either a capital letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>),<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or leftquoatation @ rightquoatation at least 5 times") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with the following before leftquoatation ! rightquoatation : a number , a vowel , string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<num>,<vow>),<@>),<!>}")))
(example (utterance "lines with 5 or more characters before a lower-case letter") (targetValue (name "?{repeatatleast(<any>,5),<low>}")))
(example (utterance "items with an upper case letter and leftquoatation ! rightquoatation preceding a small letter .") (targetValue (name "?{and(<cap>,<!>),<low>}")))
(example (utterance "lines with a lower-case letter , character , or vowel") (targetValue (name "?{or(or(<low>,<any>),<vow>)}")))
(example (utterance "lines with a letter before string leftquoatation ! rightquoatation then a lower-case letter") (targetValue (name "?{<let>,concat(<!>,<low>)}")))
(example (utterance "lines with a vowel and a number preceding a character") (targetValue (name "?{and(<vow>,<num>),<any>}")))
(example (utterance "lines with words that contain string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),<@>}")))
(example (utterance "items without leftquoatation ! rightquoatation preceding a vowel .") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least once and leftquoatation @ rightquoatation , a lower-case letter , and leftquoatation # rightquoatation in them") (targetValue (name "?{repeatatleast(<!>,1),and(and(<@>,<low>),<#>)}")))
(example (utterance "lines with a vowel but not leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),not(<!>)}")))
(example (utterance "lines not having a vowel , a number , and a lower-case letter") (targetValue (name "?{and(and(<vow>,<num>),<low>)}")))
(example (utterance "lines that have a vowel before the term leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),<!>}")))
(example (utterance "lines containing a character , leftquoatation ! rightquoatation , or a number") (targetValue (name "?{or(<any>,<!>),<num>}")))
(example (utterance "lines with a capital letter followed by leftquoatation ! rightquoatation before containing a letter in them") (targetValue (name "?{concat(<cap>,<!>),contain(<let>)}")))
(example (utterance "lines with words with 3 or more of leftquoatation ! rightquoatation or character") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines containing 0 numbers") (targetValue (name "?{<num>}")))
(example (utterance "lines with 4 or more of a number before capital letter") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines containing words with leftquoatation ! rightquoatation before a lower-case letter in them") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with less than 2 vowels") (targetValue (name "?{repeatatleast(<vow>,2)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or letter") (targetValue (name "?{or(or(<!>,<let>),<cap>)}")))
(example (utterance "lines ending with lower-case letter or not leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,not(<!>))}")))
(example (utterance "lines with words that don't have a lower-case letter or a vowel in them") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines containing a lower-case letter after leftquoatation ! rightquoatation , a number , or a vowel") (targetValue (name "?{or(or(<!>,<num>),<vow>),<low>}")))
(example (utterance "lines with a letter before number then lower-case letter") (targetValue (name "?{<let>,concat(<num>,<low>)}")))
(example (utterance "lines with capital letter followed by a character , zero or more times") (targetValue (name "?{concat(<cap>,<any>)}")))
(example (utterance "lines with a capital before number followed by vowel") (targetValue (name "?{<cap>,concat(<num>,<vow>)}")))
(example (utterance "lines with 5 or more lower-case letters or numbers") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "items with a small letter , leftquoatation ! rightquoatation , or a character preceding a vowel .") (targetValue (name "?{or(or(<low>,<!>),<any>),<vow>}")))
(example (utterance "lines containing lower-case letter , string leftquoatation ! rightquoatation , and a letter") (targetValue (name "?{and(and(<low>,<!>),<let>)}")))
(example (utterance "lines with words with a vowel , number , or lower-case letter") (targetValue (name "?{or(<vow>,<num>),<low>}")))
(example (utterance "lines with words and a lower-case letter and character") (targetValue (name "?{and(<let>,<low>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character and lower-case letter") (targetValue (name "?{<!>,and(<any>,<low>)}")))
(example (utterance "lines with letter before character , 7 or more times") (targetValue (name "?{<let>,repeatatleast(<any>,7)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , zero or more times and not a number") (targetValue (name "?{star(<!>),not(<num>)}")))
(example (utterance "lines with number followed by string leftquoatation ! rightquoatation before lower-case letter and vowel") (targetValue (name "?{concat(<num>,<!>),and(<low>,<vow>)}")))
(example (utterance "lines without a lower-case letter or starting in string leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,startwith(<!>))}")))
(example (utterance "lines with words and a lower-case letter") (targetValue (name "?{contain(<low>)}")))
(example (utterance "items with an upper case letter preceding a vowel .") (targetValue (name "?{<cap>,contain(<vow>)}")))
(example (utterance "lines having no number followed by capital letter") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 7 or more times") (targetValue (name "?{repeatatleast(<!>,7),not(<low>)}")))
(example (utterance "lines not starting in string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<!>)}")))
(example (utterance "lines starting with either leftquoatation ! rightquoatation , a vowel or a capital letter or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(or(<!>,<vow>),<cap>),<@>)}")))
(example (utterance "lines without vowels or characters") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation , number , and letter") (targetValue (name "?{<cap>,and(and(<!>,<num>),<let>)}")))
(example (utterance "words with a character appears before leftquoatation ! rightquoatation zero or more times in the lines") (targetValue (name "?{star(<any>),<!>}")))
(example (utterance "lines that contain a vowel and not followed by the leftquoatation ! rightquoatation , zero times or more") (targetValue (name "?{star(<vow>),not(<!>)}")))
(example (utterance "lines that do not contain a capital letter , a lower-case letter , and a letter") (targetValue (name "?{and(and(<cap>,<low>),<let>)}")))
(example (utterance "lines with a capital letter or a lower-case 1 at the end") (targetValue (name "?{or(<low>,contain(<cap>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation which does not precede leftquoatation @ rightquoatation") (targetValue (name "?{<!>,not(<@>)}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation , zero or more times in them") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines with words and a vowel , letter , or character") (targetValue (name "?{or(<vow>,<let>),<any>}")))
(example (utterance "lines with words and a number with leftquoatation ! rightquoatation and a vowel") (targetValue (name "?{and(<num>,<!>),<vow>}")))
(example (utterance "lines without leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , lower-case letters , or characters") (targetValue (name "?{or(<!>,and(and(<@>,<low>),<any>))}")))
(example (utterance "lines not containing a letter , not leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{contain(and(and(<let>,<!>),<@>))}")))
(example (utterance "items with words beginning with a small letter .") (targetValue (name "?{star(<low>)}")))
(example (utterance "lines with only leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "lines with a character before number and a capital letter") (targetValue (name "?{<any>,and(<num>,<cap>)}")))
(example (utterance "lines ending with a letter or containing only leftquoatation ! rightquoatation in them") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with character before number , 2 or more times") (targetValue (name "?{<any>,repeatatleast(<num>,2)}")))
(example (utterance "lines having vowel before a number , character and leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,and(and(<num>,<any>),<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a lower-case letter at least 3 times") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines with a lower-case letter ending with a vowel 1 or more times") (targetValue (name "?{repeatatleast(<low>,1),endwith(<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a character , and leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<any>,<!>),<@>)}")))
(example (utterance "lines containing either string leftquoatation ! rightquoatation , vowel , or capital letter") (targetValue (name "?{or(or(<!>,<vow>),<cap>)}")))
(example (utterance "lines ending with a character and a lower-case letter .") (targetValue (name "?{concat(<any>,<low>)}")))
(example (utterance "lines containing either a vowel , leftquoatation ! rightquoatation , or a letter , then followed by a capital letter") (targetValue (name "?{or(or(<vow>,<!>),<let>),<cap>}")))
(example (utterance "lines that begin with only a capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines with less than 3 of leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(repeatatleast(<!>,3),<@>)}")))
(example (utterance "lines starting in a letter and lower-case letter") (targetValue (name "?{startwith(<let>),repeatatleast(<low>,1)}")))
(example (utterance "lines without a character and a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{not(<any>),or(<let>,<!>)}")))
(example (utterance "lines containing the word dog 7 times") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines containing at least 1 capital letter or a character") (targetValue (name "?{or(repeatatleast(<cap>,1),<any>)}")))
(example (utterance "lines with a capital letter and leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{repeatatleast(<cap>,1),or(or(<!>,<@>),<vow>)}")))
(example (utterance "lines that do not contain a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{and(<!>,<low>)}")))
(example (utterance "lines that must have a vowel and the words leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<vow>,<!>),<@>)}")))
(example (utterance "lines containing words that end with a number , at least 3 times") (targetValue (name "?{repeatatleast(<num>,3)}")))
(example (utterance "lines which contain words with vowels in them which also appear prior to the leftquoatation ! rightquoatation string") (targetValue (name "?{<vow>,<!>}")))
(example (utterance "lines with a character or lower-case letter before vowel") (targetValue (name "?{or(<any>,<low>),<vow>}")))
(example (utterance "lines that must have 1 lower-case letter followed by the word leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,1),contain(<!>)}")))
(example (utterance "lines with a number before a capital letter") (targetValue (name "?{<num>,contain(<cap>)}")))
(example (utterance "lines containing more than 1 number before a vowel") (targetValue (name "?{repeatatleast(<num>,2),<vow>}")))
(example (utterance "lines having words with a number and a lower-case letter") (targetValue (name "?{<num>,<low>}")))
(example (utterance "lines that have a capital letter or number followed by a lower-case letter") (targetValue (name "?{or(<cap>,<num>),<low>}")))
(example (utterance "lines not having a capital letter or a vowel") (targetValue (name "?{and(<cap>,<vow>)}")))
(example (utterance "lines with at least 1 occurrence of a capital letter before a number") (targetValue (name "?{<cap>,repeatatleast(<num>,1)}")))
(example (utterance "lines which are ending with containing a capital letter and a lower-case letter") (targetValue (name "?{contain(and(<cap>,<low>))}")))
(example (utterance "lines with a capital letter or any letter and a lower case letter before leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<let>),<low>),<!>}")))
(example (utterance "lines with a number , a character , leftquoatation ! rightquoatation and a capital letter") (targetValue (name "?{<num>,or(or(<any>,<!>),<cap>)}")))
(example (utterance "lines containing a capital letter , a vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,<vow>),<!>}")))
(example (utterance "lines with words with a letter followed by a number in them") (targetValue (name "?{<let>,<num>}")))
(example (utterance "lines starting with a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines without a letter before leftquoatation ! rightquoatation") (targetValue (name "?{not(<let>),<!>}")))
(example (utterance "items with leftquoatation ! rightquoatation , vowel , or letter .") (targetValue (name "?{or(<!>,<vow>),<let>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a character before leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<any>),<@>}")))
(example (utterance "items with leftquoatation ! rightquoatation or a character or leftquoatation @ rightquoatation preceding leftquoatation # rightquoatation .") (targetValue (name "?{or(or(<!>,<@>),<any>),<#>}")))
(example (utterance "line with 3 or more of a capital letter before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(endwith(<!>),<@>)}")))
(example (utterance "lines ending with the leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "items with a character at lease6times before leftquoatation ! rightquoatation .") (targetValue (name "?{repeatatleast(<any>,6),<!>}")))
(example (utterance "lines with number before vowel , 3 or more times") (targetValue (name "?{<num>,repeatatleast(<vow>,3)}")))
(example (utterance "lines ending with a vowel followed by a number or leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<vow>,<num>),<!>)}")))
(example (utterance "lines ending with a character before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines with words and zero or more numbers") (targetValue (name "?{<num>}")))
(example (utterance "lines without a vowel , letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<vow>,<let>),<!>)}")))
(example (utterance "lines with 3 or less capital letters in them") (targetValue (name "?{repeatatleast(<cap>,3)}")))
(example (utterance "lines ending with number , 2 or more times") (targetValue (name "?{contain(repeatatleast(<num>,2))}")))
(example (utterance "items with leftquoatation ! rightquoatation at least thrice preceding a vowel .") (targetValue (name "?{repeatatleast(<!>,3),<vow>}")))
(example (utterance "lines with a vowel or a capital letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{<!>,or(<vow>,<cap>)}")))
(example (utterance "lines not containing a number , either zero times or greater") (targetValue (name "?{star(<num>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation occur 6 or more times before leftquoatation @ rightquoatation") (targetValue (name "?{concat(repeatatleast(<!>,6),<@>)}")))
(example (utterance "lines which are starting with words with a capital letter , zero or more times") (targetValue (name "?{star(<cap>)}")))
(example (utterance "lines having mimnimum 1 number and a capital letter") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before either a capital letter , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation in them") (targetValue (name "?{<!>,or(or(<cap>,<@>),<#>)}")))
(example (utterance "lines that don't have a letter come before a character") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with a capital letter before a lower-case letter") (targetValue (name "?{contain(<cap>),<low>}")))
(example (utterance "lines with a vowel before non character") (targetValue (name "?{repeatatleast(<vow>,1),not(<any>)}")))
(example (utterance "lines that have5or more combinations of a lower case letter followed by a number") (targetValue (name "?{repeatatleast(<low>,5),<num>}")))
(example (utterance "lines ending with 5 or more of leftquoatation ! rightquoatation or characters") (targetValue (name "?{or(endwith(<!>),<any>)}")))
(example (utterance "lines which have a character , leftquoatation ! rightquoatation , and vowel") (targetValue (name "?{and(and(<any>,<vow>),<!>)}")))
(example (utterance "6 or more lines with words with a lower-case letter") (targetValue (name "?{<low>}")))
(example (utterance "lines having words with either a lower-case letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<!>),<vow>)}")))
(example (utterance "lines starting with words with letter , 3 or more times") (targetValue (name "?{repeatatleast(<let>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a character , before leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<any>),<@>}")))
(example (utterance "lines with a number before vowel") (targetValue (name "?{<num>,contain(<vow>)}")))
(example (utterance "lines with a lower-case letter , string leftquoatation ! rightquoatation , or capital letter") (targetValue (name "?{or(or(<low>,<!>),<cap>)}")))
(example (utterance "lines with a letter before a vowel , number , or character") (targetValue (name "?{<let>,or(or(<vow>,<num>),<any>)}")))
(example (utterance "lines with 3 or more of a character before vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{or(concat(<any>,<vow>),<!>)}")))
(example (utterance "lines ending in words with leftquoatation ! rightquoatation and lower-case letter") (targetValue (name "?{and(<!>,<low>)}")))
(example (utterance "lines with words with a lower-case letter followed by a number , zero or more times in them") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "lines with words and a lower-case and capital letter") (targetValue (name "?{and(<low>,<let>),<cap>}")))
(example (utterance "lines without a letter before a number and no vowels") (targetValue (name "?{and(concat(<let>,<num>),<vow>)}")))
(example (utterance "lines without a vowel before a letter and string leftquoatation ! rightquoatation") (targetValue (name "?{not(<vow>),and(<let>,<!>)}")))
(example (utterance "lines without a letter , a vowel , or lower-case letter") (targetValue (name "?{or(or(<let>,<vow>),<low>)}")))
(example (utterance "lines with a capital , leftquoatation ! rightquoatation , & leftquoatation @ rightquoatation before a number") (targetValue (name "?{and(and(<cap>,<!>),<@>),<num>}")))
(example (utterance "lines with either a vowel and a letter or leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),or(<let>,<!>)}")))
(example (utterance "lines which end with leftquoatation ! rightquoatation before a character , 4 times or more times") (targetValue (name "?{repeatatleast(concat(<!>,<any>),4)}")))
(example (utterance "lines without a lower-case letter and number before string leftquoatation ! rightquoatation") (targetValue (name "?{not(<low>),concat(<num>,<!>)}")))
(example (utterance "lines with a number or letter after leftquoatation ! rightquoatation") (targetValue (name "?{concat(<!>,<let>),startwith(<num>)}")))
(example (utterance "lines with either string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation or a capital") (targetValue (name "?{or(or(<!>,<@>),<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter or leftquoatation @ rightquoatation 1 or more times") (targetValue (name "?{repeatatleast(<!>,1),or(<let>,<@>)}")))
(example (utterance "lines with words with a number then leftquoatation ! rightquoatation") (targetValue (name "?{<num>,<!>}")))
(example (utterance "lines with words and a letter or vowel") (targetValue (name "?{or(<let>,<vow>),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a capital letter , and a number") (targetValue (name "?{and(and(<!>,<cap>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 7 or more times before a character in them") (targetValue (name "?{repeatatleast(<!>,7),<any>}")))
(example (utterance "lines with words that don't string leftquoatation ! rightquoatation before a character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a character , or leftquoatation @ rightquoatation before a letter") (targetValue (name "?{or(or(<!>,<any>),<@>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter .") (targetValue (name "?{<!>,repeatatleast(<low>,1)}")))
(example (utterance "lines containing a vowel , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<@>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation or vowel") (targetValue (name "?{<low>,or(<!>,<vow>)}")))
(example (utterance "lines with a number , 5 or more times before leftquoatation ! rightquoatation in it") (targetValue (name "?{repeatatleast(<num>,5),<!>}")))
(example (utterance "lines with 2 or more of a lower-case letter before number") (targetValue (name "?{<low>,repeatatleast(<num>,2)}")))
(example (utterance "lines with a vowel before a character then lower-case letter") (targetValue (name "?{concat(<vow>,<any>),<low>}")))
(example (utterance "lines without a vowel before a lower case letter") (targetValue (name "?{startwith(concat(<vow>,<low>))}")))
(example (utterance "lines which have leftquoatation ! rightquoatation , 6 or more times before a number or a lower-case letter") (targetValue (name "?{repeatatleast(<!>,6),or(<num>,<low>)}")))
(example (utterance "lines containing an uppercase number or leftquoatation ! rightquoatation 6 or more times") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation , a vowel leftquoatation @ rightquoatation") (targetValue (name "?{<num>,and(and(<!>,<vow>),<@>)}")))
(example (utterance "lines with a capital letter before a character") (targetValue (name "?{concat(repeatatleast(<cap>,1),<any>)}")))
(example (utterance "lines that contain a character followed by a letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{<any>,or(or(<let>,<vow>),<!>)}")))
(example (utterance "lines not starting in a character") (targetValue (name "?{repeatatleast(startwith(<any>),1)}")))
(example (utterance "lines that do not include a lower-case letter") (targetValue (name "?{star(<low>)}")))
(example (utterance "lines not containing followed by leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{not(concat(<!>,<@>))}")))
(example (utterance "lines that have 3 or more combinations that do not contain the word leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{not(<!>),repeatatleast(<vow>,3)}")))
(example (utterance "lines with 7 or more or a character before number") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation once") (targetValue (name "?{startwith(<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines with words that have a letter , a capital letter , and a lower-case letter") (targetValue (name "?{and(and(<let>,<cap>),<low>)}")))
(example (utterance "lines with a character and a number") (targetValue (name "?{and(<any>,<num>)}")))
(example (utterance "lines containing a lower case letter or capital letter") (targetValue (name "?{or(<low>,<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a lower-case letter , a letter , as well as a vowel") (targetValue (name "?{<!>,and(and(<low>,<let>),<vow>)}")))
(example (utterance "no lines with leftquoatation ! rightquoatation , a capital letter or a vowel") (targetValue (name "?{and(and(<!>,<cap>),<vow>)}")))
(example (utterance "lines containing capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or capital letter before a letter") (targetValue (name "?{repeatatleast(or(<!>,<cap>),1),<let>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,not(<@>))}")))
(example (utterance "lines with 7 or more capital letters or leftquoatation ! rightquoatation") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a letter , a lower-case letter , or a number") (targetValue (name "?{<!>,or(or(<let>,<low>),<num>)}")))
(example (utterance "lines with words and a number without string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,not(<!>)}")))
(example (utterance "lines with a letter preceding leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "a character appears before a capital letter followed by a lower-case letter in the lines") (targetValue (name "?{<any>,concat(<cap>,<low>)}")))
(example (utterance "lines that doesn't start with a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{or(startwith(<vow>),<!>)}")))
(example (utterance "lines having words with number , string leftquoatation ! rightquoatation and a letter") (targetValue (name "?{or(<num>,<!>),<let>}")))
(example (utterance "lines containing a vowel followed by ending with a lower-case letter") (targetValue (name "?{<vow>,endwith(<low>)}")))
(example (utterance "lines with a number or leftquoatation ! rightquoatation preceded by a letter") (targetValue (name "?{<let>,or(<num>,<!>)}")))
(example (utterance "lines ending in words with string leftquoatation ! rightquoatation or a letter") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines with a lower-case letter followed by either a letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{<low>,or(or(<let>,<!>),<@>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation or vowel at least once") (targetValue (name "?{contain(or(<!>,<vow>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending in a zero or more of a capital letter") (targetValue (name "?{<!>,star(endwith(<cap>))}")))
(example (utterance "lines containing either a number , a lower-case letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<low>),<!>}")))
(example (utterance "lines having words with leftquoatation ! rightquoatation , a vowel , and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<vow>),<@>)}")))
(example (utterance "lines not containing only a lower-case letter or leftquoatation ! rightquoatation in them") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "items with a small letter or character at least 4 times .") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "lines having leftquoatation ! rightquoatation followed by a letter") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with words that do not contain a lower-case letter") (targetValue (name "?{not(<low>)}")))
(example (utterance "lines which are starting with leftquoatation ! rightquoatation or ending with leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<vow>),<num>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , character , or number") (targetValue (name "?{or(or(<!>,<any>),<num>)}")))
(example (utterance "lines which have a character at least 3 times or greater") (targetValue (name "?{<any>}")))
(example (utterance "lines containing a capital letter and 2 or more vowels") (targetValue (name "?{startwith(or(<cap>,<vow>))}")))
(example (utterance "lines that have 3 or more vowels") (targetValue (name "?{<vow>}")))
(example (utterance "lines with words and a vowel before leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,<!>}")))
(example (utterance "lines containing a number before string leftquoatation ! rightquoatation and character") (targetValue (name "?{contain(<num>),and(<!>,<any>)}")))
(example (utterance "items with words containing a vowel , characcter , and number .") (targetValue (name "?{and(<num>,<any>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , character , and capital letter") (targetValue (name "?{and(and(<!>,<any>),<cap>)}")))
(example (utterance "lines with a lower-case letter followed by a vowel , 7 or more times in them") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines containing at least 1 letter and vowel") (targetValue (name "?{contain(and(<let>,<vow>))}")))
(example (utterance "the lines that contain a letter coming before leftquoatation ! rightquoatation at least 2 times") (targetValue (name "?{<let>,repeatatleast(<!>,2)}")))
(example (utterance "lines with words with a letter followed by string leftquoatation ! rightquoatation at least once") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with no capital letter before leftquoatation ! rightquoatation and no vowel") (targetValue (name "?{and(concat(<cap>,<!>),<vow>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before words with not a character") (targetValue (name "?{<!>,not(<any>)}")))
(example (utterance "lines where a lower-case letter is preceded by a character no less than zero times") (targetValue (name "?{<any>,star(<low>)}")))
(example (utterance "lines with words leftquoatation ! rightquoatation , leftquoatation @ rightquoatation and a letter") (targetValue (name "?{and(<!>,<@>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number , at least 2 times") (targetValue (name "?{<!>,repeatatleast(<num>,2)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation before a capital letter followed by a vowel") (targetValue (name "?{<!>,concat(<cap>,<vow>)}")))
(example (utterance "lines with a vowel , at least 3 times before a character") (targetValue (name "?{repeatatleast(<vow>,3),<any>}")))
(example (utterance "lines with a capital letter , character , and vowel") (targetValue (name "?{and(and(<cap>,<any>),<vow>)}")))
(example (utterance "items with words contating leftquoatation ! rightquoatation , truck' , and a vowel .") (targetValue (name "?{and(<!>,<@>),<vow>}")))
(example (utterance "lines not containing a vowel 4 times") (targetValue (name "?{contain(not(<vow>))}")))
(example (utterance "lines ending with a vowel before a capital letter") (targetValue (name "?{endwith(<cap>),<vow>}")))
(example (utterance "lines without a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{not(or(<low>,<!>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter then string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<low>),repeatatleast(<@>,1)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation before a number") (targetValue (name "?{endwith(<!>),star(<num>)}")))
(example (utterance "lower case letter without a character , zero or additional times following") (targetValue (name "?{star(<any>),<low>}")))
(example (utterance "lines with or without a letter , and leftquoatation ! rightquoatation or a lower-case letter") (targetValue (name "?{star(<let>),or(<!>,<low>)}")))
(example (utterance "lines with neither a character nor a number") (targetValue (name "?{or(<num>,<any>)}")))
(example (utterance "lines ending with a capital letter and a number") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines possessing a vowel which precedes a letter no less than 3 times and all above") (targetValue (name "?{repeatatleast(<vow>,3),<let>}")))
(example (utterance "lines with a character followed by a lower-case letter") (targetValue (name "?{concat(<any>,<low>),<cap>}")))
(example (utterance "lines with a letter , a character , as well as leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<any>),<!>)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a character") (targetValue (name "?{and(and(<!>,<@>),<any>)}")))
(example (utterance "lines containing either letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<!>),<@>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,1),<!>}")))
(example (utterance "lines with 5 or more capital letters before a lower-case letter") (targetValue (name "?{repeatatleast(<cap>,5),<low>}")))
(example (utterance "lines without a lower-case letter , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<vow>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation ending in a number") (targetValue (name "?{or(<!>,<@>),endwith(<num>)}")))
(example (utterance "lines starting with letter followed by lower-case letter , zero or more times") (targetValue (name "?{startwith(concat(<let>,<low>))}")))
(example (utterance "lines with words ending with a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation") (targetValue (name "?{contain(<any>),star(<!>)}")))
(example (utterance "lines containing 6 or more vowels or leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines with words with zero or more of leftquoatation ! rightquoatation or with a lower-case letter") (targetValue (name "?{star(<!>),<low>}")))
(example (utterance "lines with a character , letter , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<let>),<!>)}")))
(example (utterance "lines not having a capital letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation") (targetValue (name "?{not(<cap>),concat(<!>,<@>)}")))
(example (utterance "lines where a character occurs 7 or more times before a capital letter") (targetValue (name "?{<any>,repeatatleast(<cap>,7)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , zero or more times or letter and a lower-case letter") (targetValue (name "?{star(<!>),and(<let>,<low>)}")))
(example (utterance "lines where 3 or more times a number is preceded by a character") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines with words with a lower-case letter before a character or leftquoatation ! rightquoatation in them") (targetValue (name "?{<low>,or(<any>,<!>)}")))
(example (utterance "items without a letter at least 1 time preceding leftquoatation ! rightquoatation .") (targetValue (name "?{concat(repeatatleast(<let>,1),<!>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a vowel and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,contain(and(<vow>,<@>))}")))
(example (utterance "lines where a character or vowel occur at least 1 time") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "lines without a character , a vowel , or a capital letter") (targetValue (name "?{or(or(<any>,<vow>),<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation at least twice before a character") (targetValue (name "?{repeatatleast(<!>,2),<any>}")))
(example (utterance "lines with a number , zero or more times and not a vowel in it") (targetValue (name "?{star(<num>),not(<vow>)}")))
(example (utterance "lines with a lower-case letter , zero or more times") (targetValue (name "?{<num>,star(<low>)}")))
(example (utterance "lines with the leftquoatation ! rightquoatation before a character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines containing words having leftquoatation ! rightquoatation or leftquoatation @ rightquoatation and leftquoatation # rightquoatation") (targetValue (name "?{or(<!>,<@>),<#>}")))
(example (utterance "lines that end with a capital letter and a vowel followed by an additional letter") (targetValue (name "?{endwith(<cap>),concat(<vow>,<let>)}")))
(example (utterance "items with a numeral preceding a character at least 1 time .") (targetValue (name "?{<num>,repeatatleast(<any>,1)}")))
(example (utterance "lines that begin with leftquoatation ! rightquoatation or a letter , at least 5 times") (targetValue (name "?{startwith(<!>),repeatatleast(<let>,5)}")))
(example (utterance "lines having a number , leftquoatation ! rightquoatation , and a capital letter before leftquoatation @ rightquoatation in them") (targetValue (name "?{and(and(<num>,<!>),<cap>),<@>}")))
(example (utterance "items with words containing a small letter and an uppercase letter .") (targetValue (name "?{<low>,<cap>}")))
(example (utterance "items beginning with a character which precedes an upper case letter .") (targetValue (name "?{startwith(<any>),<cap>}")))
(example (utterance "lines without a capital followed by a lower case letter at least 1 time") (targetValue (name "?{repeatatleast(concat(<cap>,<low>),1)}")))
(example (utterance "lines with a lower-case letter and vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<vow>),<!>}")))
(example (utterance "lines with words and a character before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "items beginning with a numeral at least 4 times .") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "lines containing a character , 6 times or more or ending with a letter") (targetValue (name "?{repeatatleast(<any>,6),endwith(<let>)}")))
(example (utterance "lines not having a letter , leftquoatation ! rightquoatation , or a number") (targetValue (name "?{and(and(<let>,<!>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation 4 times before a number") (targetValue (name "?{repeatatleast(<!>,4),<num>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<!>,<low>),<let>}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{contain(concat(<!>,<@>))}")))
(example (utterance "lines having capital letters followed by a character") (targetValue (name "?{<cap>,<any>}")))
(example (utterance "lines without a character , string leftquoatation ! rightquoatation , or capital letter") (targetValue (name "?{or(or(<any>,<!>),<cap>)}")))
(example (utterance "lines with words and a lower-case letter , character , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<any>),<!>)}")))
(example (utterance "lines with words with a capital followed by vowel and a lower-case letter") (targetValue (name "?{concat(<cap>,<vow>),<low>}")))
(example (utterance "lines stating with 5 or more letters") (targetValue (name "?{startwith(or(<cap>,<let>))}")))
(example (utterance "lines with number before string leftquoatation ! rightquoatation or containing a capital") (targetValue (name "?{concat(<num>,<!>),contain(<cap>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation , a capital letter , or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<cap>),<@>}")))
(example (utterance "lines that do not contain a capital letter , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<cap>,<!>),<@>)}")))
(example (utterance "lines with a lower-case letter , string leftquoatation ! rightquoatation , or character before a letter") (targetValue (name "?{or(or(<low>,<!>),<any>),<let>}")))
(example (utterance "lines with dog and a number") (targetValue (name "?{<!>,<num>}")))
(example (utterance "lines ending with words that contain string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "a capital letter before lines that are without leftquoatation ! rightquoatation") (targetValue (name "?{not(concat(<!>,<cap>))}")))
(example (utterance "lines ending with numeral or lower-case letter") (targetValue (name "?{contain(or(<num>,<low>))}")))
(example (utterance "lines starting with lower-case letter or letter , 3 or more times") (targetValue (name "?{or(<low>,repeatatleast(<let>,3))}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a letter , or number") (targetValue (name "?{or(or(<!>,<let>),<num>)}")))
(example (utterance "items with a small letter .") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a letter , and a vowel") (targetValue (name "?{and(and(<!>,<let>),<vow>)}")))
(example (utterance "lines that end with words only with leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines containing 6 vowels") (targetValue (name "?{repeatatleast(<vow>,6)}")))
(example (utterance "lines which are containing a lower-case letter before leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{contain(<low>),concat(<!>,<@>)}")))
(example (utterance "lines containing capital letter or character , at least twice") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines that have 2 or more combinations consisting of the word leftquoatation ! rightquoatation followed by a capital letter") (targetValue (name "?{repeatatleast(<!>,2),<cap>}")))
(example (utterance "lines starting with a vowel") (targetValue (name "?{not(<let>),<vow>}")))
(example (utterance "lines with a letter preceding a number at least 6 times") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines with a zero or more characters or leftquoatation ! rightquoatation before letter") (targetValue (name "?{star(<any>),concat(<!>,<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with zero or more of a character before a number or capital letter") (targetValue (name "?{concat(<any>,<num>),star(<cap>)}")))
(example (utterance "lines that begin with a letter and a vowel which precede leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<let>),concat(<vow>,<!>)}")))
(example (utterance "lines containing words having a character or leftquoatation ! rightquoatation as well as a capital letter") (targetValue (name "?{or(<any>,<!>),<cap>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before a character then a capital letter") (targetValue (name "?{concat(<!>,<any>),<cap>}")))
(example (utterance "items without a numeral preceding leftquoatation ! rightquoatation .") (targetValue (name "?{not(<num>),<!>}")))
(example (utterance "lines which finish with either the leftquoatation ! rightquoatation string or a letter") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines that contain a lower-case letter followed by leftquoatation ! rightquoatation or leftquoatation @ rightquoatation at least once") (targetValue (name "?{<low>,repeatatleast(or(<!>,<@>),1)}")))
(example (utterance "lines with letter or character , 5 or more times") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with 5 or more of a lower-case letter before vowel") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "items with words containing a small letter , a vowel , and a numeral .") (targetValue (name "?{and(<vow>,<low>),<num>}")))
(example (utterance "lines with zero or more of string leftquoatation ! rightquoatation starting in lower-case letter") (targetValue (name "?{star(<!>),startwith(<low>)}")))
(example (utterance "lines stating with a character or having leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<any>),contain(<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before containing a lower-case letter") (targetValue (name "?{<!>,contain(<low>)}")))
(example (utterance "lines with 2 or more lower-case letters before a character") (targetValue (name "?{repeatatleast(<low>,2),<any>}")))
(example (utterance "items without a character preceding a vowel .") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "the lines that contain words with a character before a lower-case letter") (targetValue (name "?{<any>,<low>}")))
(example (utterance "lines with a lower-case before capital letter then character") (targetValue (name "?{<low>,concat(<cap>,<any>)}")))
(example (utterance "lines with words and zero or more of leftquoatation ! rightquoatation before a lower case letter") (targetValue (name "?{star(<!>),<low>}")))
(example (utterance "lines having a character and a vowel") (targetValue (name "?{<any>,contain(<vow>)}")))
(example (utterance "lines with number before lower-case letter , character , and string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,and(and(<low>,<any>),<!>)}")))
(example (utterance "lines with 4 or more vowels") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines with words ending with a vowel") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a numeral or a vowel .") (targetValue (name "?{<!>,or(<vow>,<num>)}")))
(example (utterance "lines that include at least 1 letter") (targetValue (name "?{<let>}")))
(example (utterance "items with a character , a letter , and a vowel preceding a numeral .") (targetValue (name "?{and(and(<vow>,<let>),<any>),<num>}")))
(example (utterance "lines with words with vowel before ending with letter") (targetValue (name "?{<vow>,endwith(<let>)}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , vowel , and string leftquoatation @ rightquoatation before string leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<vow>),<@>),<#>}")))
(example (utterance "lines with a vowel after string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a letter") (targetValue (name "?{and(and(<!>,<@>),<let>),<vow>}")))
(example (utterance "either start with string leftquoatation ! rightquoatation or end with any number") (targetValue (name "?{endwith(<num>),startwith(<!>)}")))
(example (utterance "lines without a number of leftquoatation ! rightquoatation") (targetValue (name "?{not(<num>),<!>}")))
(example (utterance "lines containing a character or a number , twice or more times") (targetValue (name "?{or(<any>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and capital letter or a lower-case letter") (targetValue (name "?{or(and(and(<!>,<let>),<cap>),<low>)}")))
(example (utterance "lines with words and only 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation and capital letter or number") (targetValue (name "?{endwith(<!>),or(<cap>,<num>)}")))
(example (utterance "lines without a lower-case letter or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{and(<low>,or(<!>,<@>))}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation before either a lower-case letter , he word leftquoatation @ rightquoatation , or the word leftquoatation # rightquoatation") (targetValue (name "?{<!>,or(or(<#>,<@>),<low>)}")))
(example (utterance "lines starting with vowel and letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<vow>),concat(<let>,<!>)}")))
(example (utterance "items with a small leter and character preceding leftquoatation ! rightquoatation .") (targetValue (name "?{and(<low>,<any>),<!>}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{endwith(<!>),contain(<let>)}")))
(example (utterance "lines ending in lower-case letter or with lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<let>),concat(<low>,<!>)}")))
(example (utterance "lines with a lower-case letter and a character") (targetValue (name "?{and(and(<let>,<low>),<any>)}")))
(example (utterance "items beginning with a character .") (targetValue (name "?{<any>}")))
(example (utterance "lines wit words and a character , number , and letter") (targetValue (name "?{and(<any>,<num>),<let>}")))
(example (utterance "items with a letter , leftquoatation ! rightquoatation , and numeral preceding an upper case letter .") (targetValue (name "?{and(and(<num>,<let>),<!>),<cap>}")))
(example (utterance "lines contain leftquoatation ! rightquoatation , at least 6 times prior to a capital letter") (targetValue (name "?{repeatatleast(<!>,6),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a letter before a lower-case letter") (targetValue (name "?{or(or(<!>,<let>),<@>),<low>}")))
(example (utterance "leftquoatation ! rightquoatation , a letter , and a vowel or leftquoatation @ rightquoatation appears 4 or more times in the lines") (targetValue (name "?{or(and(and(<!>,<let>),<vow>),<@>)}")))
(example (utterance "words with either a lower-case letter , a letter , or a character appears in the lines") (targetValue (name "?{or(<low>,<let>),<any>}")))
(example (utterance "lines without a character , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<any>,<!>),<@>)}")))
(example (utterance "lines with 3 or more of a capital letter or vowel") (targetValue (name "?{or(<cap>,<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation with a letter before it") (targetValue (name "?{<let>,contain(<!>)}")))
(example (utterance "lines with words with a capital letter before a character in them") (targetValue (name "?{<cap>,<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character and string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(<any>,<@>)}")))
(example (utterance "lines with words with a capital letter before string leftquoatation ! rightquoatation then letter") (targetValue (name "?{concat(<cap>,<!>),<let>}")))
(example (utterance "lines with words with letter and capital letter , zero or more times") (targetValue (name "?{and(<let>,<cap>)}")))
(example (utterance "lines with a lower-case letter followed by leftquoatation ! rightquoatation and which are ending with a letter") (targetValue (name "?{concat(<low>,<!>),endwith(<let>)}")))
(example (utterance "lines containing a vowel before 0 or more of leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,star(<!>)}")))
(example (utterance "lines that either do not contain a capital letter or contain at least 1") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or a number") (targetValue (name "?{<!>,or(<@>,<num>)}")))
(example (utterance "lines containing a letter preceded by a number zero or more times") (targetValue (name "?{<num>,star(<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by 0 or more lower-case letters") (targetValue (name "?{<!>,star(<low>)}")))
(example (utterance "items with a vowel preceding an upper case letter preceding a letter .") (targetValue (name "?{<vow>,concat(<cap>,<let>)}")))
(example (utterance "lines ending with a capital letter but not leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<cap>),not(<!>)}")))
(example (utterance "lines containing zero or more times , a number") (targetValue (name "?{star(<num>)}")))
(example (utterance "lines with words that have a capital letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation") (targetValue (name "?{and(<cap>,<@>),<!>}")))
(example (utterance "lines with no letters or numbers") (targetValue (name "?{and(and(<let>,<num>),<low>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , a character , and a capital letter") (targetValue (name "?{and(<!>,<any>),<cap>}")))
(example (utterance "4 or more lines with a character before a vowel .") (targetValue (name "?{<any>,repeatatleast(<vow>,4)}")))
(example (utterance "lines containing a number followed by the word leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(<num>,<@>)}")))
(example (utterance "lines containing a capital letter which is followed by a number") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines ending with words with a capital letter followed by leftquoatation ! rightquoatation in them") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines that contain a capital letter or a vowel followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<vow>),<!>}")))
(example (utterance "lines which are not having a lower-case letter or either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a letter") (targetValue (name "?{not(<low>),or(or(<!>,<@>),<let>)}")))
(example (utterance "lines which are not having a character , not a capital letter , and not leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<cap>),<!>)}")))
(example (utterance "lines with a number after a vowel or a letter") (targetValue (name "?{and(<vow>,<any>),<num>}")))
(example (utterance "lines starting in 4 or more of leftquoatation ! rightquoatation or letter") (targetValue (name "?{startwith(or(<!>,<let>))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or number") (targetValue (name "?{or(or(<!>,<@>),<num>)}")))
(example (utterance "lines with a lower-case letter before either leftquoatation ! rightquoatation , a vowel , or leftquoatation @ rightquoatation in them") (targetValue (name "?{<low>,or(or(<!>,<vow>),<@>)}")))
(example (utterance "items with a smal letter preceding leftquoatation ! rightquoatation and a character .") (targetValue (name "?{<low>,and(<!>,<any>)}")))
(example (utterance "lines not having vowel or letter , 7 or more times") (targetValue (name "?{or(not(<vow>),<let>)}")))
(example (utterance "lines containing words followed by a number , leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{or(<num>,<!>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter") (targetValue (name "?{<!>,star(<let>)}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation and a character") (targetValue (name "?{<num>,and(<!>,<any>)}")))
(example (utterance "lines with a character at least once or not leftquoatation ! rightquoatation in it") (targetValue (name "?{repeatatleast(<any>,1),not(<!>)}")))
(example (utterance "lines end with a vowel and a character") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines starting with a letter at least 3 times") (targetValue (name "?{repeatatleast(<let>,3)}")))
(example (utterance "lines with a vowel followed by a character before leftquoatation ! rightquoatation in them") (targetValue (name "?{concat(<vow>,<any>),<!>}")))
(example (utterance "lines with words with a capital , a character , and a letter") (targetValue (name "?{and(<cap>,<any>),<let>}")))
(example (utterance "lines containing a letter before a vowel , 5 times or more") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines zer or more of leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation zero times") (targetValue (name "?{endwith(<!>),star(<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter a long with a character") (targetValue (name "?{<!>,and(<cap>,<any>)}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{or(repeatatleast(<!>,1),<cap>)}")))
(example (utterance "lines with a character at least once and starting with string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,1),startwith(<!>)}")))
(example (utterance "items with words contating a letter or a character .") (targetValue (name "?{and(<low>,<any>),<cap>}")))
(example (utterance "the lines starting with a vowel repeated 7 or more times") (targetValue (name "?{repeatatleast(<vow>,7)}")))
(example (utterance "lines with words with either string leftquoatation ! rightquoatation , a character , or capital letter") (targetValue (name "?{or(<!>,<any>),<cap>}")))
(example (utterance "lines that begin with a letter or anything other than a character") (targetValue (name "?{or(<let>,not(<any>))}")))
(example (utterance "lines with letter before not containing string leftquoatation ! rightquoatation") (targetValue (name "?{<let>,not(contain(<!>))}")))
(example (utterance "lines with 4 or more vowels before a number") (targetValue (name "?{repeatatleast(<vow>,4),<num>}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation or a number or vowel") (targetValue (name "?{or(<!>,<num>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or ending with zero or more of a  letter") (targetValue (name "?{or(<!>,endwith(<let>))}")))
(example (utterance "lines not having letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<!>),<@>)}")))
(example (utterance "lines starting in a lower-case letter before a letter") (targetValue (name "?{repeatatleast(concat(<low>,<let>),1)}")))
(example (utterance "lines with words with a character or leftquoatation ! rightquoatation") (targetValue (name "?{<any>,<!>}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation before a character") (targetValue (name "?{or(or(<!>,<@>),<#>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , as well as leftquoatation # rightquoatation followed by a lower-case letter") (targetValue (name "?{and(and(<!>,<@>),<#>),<low>}")))
(example (utterance "lines with zero or more of a character before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<any>,<!>),<@>)}")))
(example (utterance "lines with a number , string leftquoatation ! rightquoatation , and vowel") (targetValue (name "?{and(and(<num>,<!>),<vow>)}")))
(example (utterance "lines with words with a character , vowel , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,<vow>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel , 4 or more times in them") (targetValue (name "?{<!>,repeatatleast(<vow>,4)}")))
(example (utterance "lines that have a number before string leftquoatation ! rightquoatation") (targetValue (name "?{<num>,<!>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation or character , 6 or more times") (targetValue (name "?{or(not(<!>),<any>)}")))
(example (utterance "lines containing words having leftquoatation ! rightquoatation , 2 or more times as well as leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,2),<@>}")))
(example (utterance "lines with 3 or more of a capital letter or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{contain(or(or(<cap>,<!>),<@>))}")))
(example (utterance "lines containing words beginning with a vowel as well as leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<vow>),<!>}")))
(example (utterance "lines with a lower-case letter or number") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines without capital letters or numbers") (targetValue (name "?{not(or(<cap>,<num>))}")))
(example (utterance "lines with zero or more characters or a number") (targetValue (name "?{or(star(<any>),<num>)}")))
(example (utterance "lines that have either a letter or a character") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with a number after a vowel and a capital letter") (targetValue (name "?{and(<vow>,<cap>),<num>}")))
(example (utterance "lines with capital letter followed by lower-case letter before vowel") (targetValue (name "?{concat(<cap>,<low>),<vow>}")))
(example (utterance "lines with a letter , string leftquoatation ! rightquoatation , or vowel") (targetValue (name "?{or(or(<let>,<!>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a number or which are ending with a vowel") (targetValue (name "?{concat(<!>,<num>),endwith(<vow>)}")))
(example (utterance "lines that do not contain a lower-case letter followed by leftquoatation ! rightquoatation , 2 times or more") (targetValue (name "?{repeatatleast(concat(<low>,<!>),2)}")))
(example (utterance "lines having minimum either 1 vowel or leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines containing a capital letter , a number , and a letter") (targetValue (name "?{and(<cap>,<num>),<let>}")))
(example (utterance "lines with words either a letter , string leftquoatation ! rightquoatation  or a lower-case letter") (targetValue (name "?{or(<let>,<!>),<low>}")))
(example (utterance "items with small letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{<low>,<!>}")))
(example (utterance "items with a letter preceding a terminating numeral .") (targetValue (name "?{<let>,endwith(<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{<!>,and(<low>,<vow>)}")))
(example (utterance "lines containing words and leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "liens with a vowel , string leftquoatation ! rightquoatation , or capital letter before a number") (targetValue (name "?{or(or(<vow>,<!>),<cap>),<num>}")))
(example (utterance "lines with 7 or more of a capital letter before letter") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines with a letter or leftquoatation ! rightquoatation , capital letter , and string leftquoatation @ rightquoatation") (targetValue (name "?{or(<let>,and(and(<!>,<cap>),<@>))}")))
(example (utterance "lines with a letter followed by a character , 4 or more times in them") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with words and a capital letter") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation followed by lower-case letter") (targetValue (name "?{<!>,concat(<@>,<low>)}")))
(example (utterance "lines that have both leftquoatation ! rightquoatation and a capital letter occur before a number") (targetValue (name "?{and(<cap>,<!>),<num>}")))
(example (utterance "lines containing vowel or letter") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before not a number") (targetValue (name "?{or(<!>,<@>),not(<num>)}")))
(example (utterance "ines with a capital letter at least once and which are ending with a number") (targetValue (name "?{repeatatleast(<cap>,1),endwith(<num>)}")))
(example (utterance "lines that have words containing a capital letter , a leftquoatation ! rightquoatation string , and a letter") (targetValue (name "?{and(<cap>,<let>),<!>}")))
(example (utterance "lines containing a lower-case letter , a vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<!>),<vow>}")))
(example (utterance "lines end with leftquoatation ! rightquoatation and either a number or a character") (targetValue (name "?{or(and(<!>,<num>),<any>)}")))
(example (utterance "lines with a character , a number and leftquoatation ! rightquoatation") (targetValue (name "?{and(<!>,<any>),<num>}")))
(example (utterance "lines with a number , lower-case letter , and") (targetValue (name "?{and(and(<num>,<low>),<!>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a capital letter") (targetValue (name "?{or(<!>,concat(<@>,<cap>))}")))
(example (utterance "lines starting in leftquoatation ! rightquoatation before a capital letter and string leftquoatation @ rightquoatation") (targetValue (name "?{startwith(<!>),and(<cap>,<@>)}")))
(example (utterance "lines with a letter before a lower-case letter") (targetValue (name "?{<let>,star(<low>)}")))
(example (utterance "lines with at least 1 character or number") (targetValue (name "?{or(<any>,<num>)}")))
(example (utterance "lines with words starting with string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines containing either a number , leftquoatation ! rightquoatation , or a capital letter in it") (targetValue (name "?{or(or(<num>,<!>),<cap>)}")))
(example (utterance "lines having either a capital letter , a number , or a vowel or a lower-case letter at least once in them") (targetValue (name "?{or(or(or(<cap>,<num>),<vow>),<low>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation follwed by a vowel 3 times") (targetValue (name "?{repeatatleast(concat(<!>,<vow>),3)}")))
(example (utterance "lines with a lower-case letter followed by capital letter , 7 or more times") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines not having leftquoatation ! rightquoatation followed by a number before a letter in them") (targetValue (name "?{concat(concat(<!>,<num>),<let>)}")))
(example (utterance "lines without lower-case letters 6 times or more") (targetValue (name "?{repeatatleast(<low>,6)}")))
(example (utterance "lines with 2 or more characters or with string leftquoatation ! rightquoatation and lower-case letter") (targetValue (name "?{repeatatleast(<any>,2),and(<!>,<low>)}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation , lower-case letter , or vowel") (targetValue (name "?{or(<!>,<low>),<vow>}")))
(example (utterance "lines with no lower-case letters in front of a letter") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines containing words that contain a vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines that have at least 1 combination of the word leftquoatation ! rightquoatation and a capital letter") (targetValue (name "?{and(<!>,<cap>)}")))
(example (utterance "lines that start with a number and then a letter") (targetValue (name "?{concat(<num>,<let>)}")))
(example (utterance "lines with a lower-case letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation after leftquoatation # rightquoatation") (targetValue (name "?{<#>,and(and(<low>,<!>),<@>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation , not a number , and not a character") (targetValue (name "?{and(and(<!>,<num>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter , zero or more times") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with words that contain a capital letter , a number , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>),<num>}")))
(example (utterance "lines that have 6 or more combinations of only a capital letter or number") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation then a vowel") (targetValue (name "?{concat(<cap>,<!>),<vow>}")))
(example (utterance "lines without a number , lower-case letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<low>),<!>)}")))
(example (utterance "lines with words and a letter") (targetValue (name "?{<let>,<low>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a small letter , leftquoatation @ rightquoatation and a letter .") (targetValue (name "?{<!>,and(and(<low>,<@>),<let>)}")))
(example (utterance "lines without a letter or string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<cap>),<!>)}")))
(example (utterance "lines with a lower case letter or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<low>,<!>),<@>)}")))
(example (utterance "lines with no leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{or(contain(<!>),<low>)}")))
(example (utterance "lines containing words in which a number is followed by a letter") (targetValue (name "?{<num>,<let>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , with or without only leftquoatation @ rightquoatation before it") (targetValue (name "?{star(<@>),<!>}")))
(example (utterance "lines with words with number before not a vowel") (targetValue (name "?{<num>,not(<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation following a number , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{<!>,and(and(<num>,<@>),<#>)}")))
(example (utterance "items with a small letter preceding a vowel .") (targetValue (name "?{star(<low>),<vow>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or with a vowel") (targetValue (name "?{or(<!>,contain(<vow>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and capital letter before a letter") (targetValue (name "?{and(and(<!>,<cap>),<@>),<let>}")))
(example (utterance "the lines that have a vowel coming before a character") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines with character before a vowel") (targetValue (name "?{<any>,not(<vow>)}")))
(example (utterance "lines ending with a capital letter preceding a vowel") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines with words and starting with a character and lower-case letter") (targetValue (name "?{startwith(<any>),<low>}")))
(example (utterance "lines with less than 3 letters") (targetValue (name "?{repeatatleast(endwith(<let>),3)}")))
(example (utterance "lines that have a vowel and a character") (targetValue (name "?{and(<vow>,<any>)}")))
(example (utterance "lines ending with a character , 6 or more times") (targetValue (name "?{endwith(contain(<any>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation and number preceded by vowel") (targetValue (name "?{and(<!>,<vow>),<num>}")))
(example (utterance "lines that start with leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{startwith(<!>),repeatatleast(<cap>,1)}")))
(example (utterance "items with a vowel at least 4 times preceding a character .") (targetValue (name "?{repeatatleast(<vow>,4),<any>}")))
(example (utterance "lines with words and a vowel with only a letter") (targetValue (name "?{<vow>,<let>}")))
(example (utterance "lines with words and zero or more of a vowel and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<!>)}")))
(example (utterance "lines that have a lower-case letter after leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a number") (targetValue (name "?{or(or(<num>,<@>),<!>),<low>}")))
(example (utterance "lines with a vowel or a lower-case letter 7 times") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation at least once and end with a capital letter") (targetValue (name "?{repeatatleast(<!>,1),endwith(<cap>)}")))
(example (utterance "lines that have 2 or more combinations consisting of the word leftquoatation ! rightquoatation followed by a capital letter") (targetValue (name "?{<!>,repeatatleast(<cap>,2)}")))
(example (utterance "lines ending in words with lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines having a character , capital letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<cap>),<!>),<let>}")))
(example (utterance "lines which have , in succession: a capital letter , a number , and a character") (targetValue (name "?{concat(<cap>,<num>),<any>}")))
(example (utterance "lines with words and 2 or more letter or with a capital letter") (targetValue (name "?{repeatatleast(<let>,2),<cap>}")))
(example (utterance "lines with lower-case letter before not containing character") (targetValue (name "?{<low>,not(contain(<any>))}")))
(example (utterance "lines with vowel before number and ending with string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<vow>,<num>),endwith(<!>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "lines with words ending with vowels , or a zero") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines containing either letter , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<vow>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation not a capital letter") (targetValue (name "?{startwith(<!>),not(<cap>)}")))
(example (utterance "items with a vowel at least twice prior to an upper case letter .") (targetValue (name "?{repeatatleast(<vow>,2),<cap>}")))
(example (utterance "start with a capital letter then a number , a vowel  all followed by leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<!>),<cap>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or number before string leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),concat(<num>,<@>)}")))
(example (utterance "the lines with a string leftquoatation ! rightquoatation before the lower-case letter , the number , and leftquoatation @ rightquoatation .") (targetValue (name "?{<!>,and(and(<low>,<@>),<num>)}")))
(example (utterance "lines ending in a vowel before a character") (targetValue (name "?{endwith(<vow>),<any>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines without a vowel not being leftquoatation ! rightquoatation") (targetValue (name "?{and(repeatatleast(<vow>,1),<!>)}")))
(example (utterance "lines with a lower-case letter followed by a capital letter with at least6occurrences") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines with words and 4 or more vowels") (targetValue (name "?{<vow>}")))
(example (utterance "lines without leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),repeatatleast(<@>,1)}")))
(example (utterance "lines containing lower-case letter or number") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines with vowel before number or string leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,or(<num>,<!>)}")))
(example (utterance "the lines with a capital letter coming before a character , followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<any>),<!>}")))
(example (utterance "line ending with a number or vowel") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation at least once") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with zero or more of a letter before character") (targetValue (name "?{<let>,star(<any>)}")))
(example (utterance "lines ending with zero or more strings leftquoatation ! rightquoatation or numbers") (targetValue (name "?{or(<!>,star(<num>))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or capital letter") (targetValue (name "?{<!>,or(<@>,<cap>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before character or capital letter") (targetValue (name "?{<!>,or(<any>,<cap>)}")))
(example (utterance "lines with capital letter vowel followed at least once") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines with vowel followed by letter , 5 or more times") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines with a character and string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before a number") (targetValue (name "?{and(and(<any>,<!>),<@>),<num>}")))
(example (utterance "lines with a character , lower-case letter , or the string dog before a letter") (targetValue (name "?{or(or(<any>,<low>),<!>),<let>}")))
(example (utterance "lines with words that don't have a capital letter") (targetValue (name "?{repeatatleast(<cap>,1)}")))
(example (utterance "lines containing words in lower-case letters , a letter , and leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<let>),<!>}")))
(example (utterance "lines where a vowel is not preceded , 2 or more times , by a character") (targetValue (name "?{concat(repeatatleast(<any>,2),<vow>)}")))
(example (utterance "lines containing 5 or more times a vowel") (targetValue (name "?{repeatatleast(<vow>,5)}")))
(example (utterance "lines which have leftquoatation ! rightquoatation at least once before a vowel") (targetValue (name "?{repeatatleast(<!>,1),<vow>}")))
(example (utterance "lines ending in a capital letter") (targetValue (name "?{contain(repeatatleast(<cap>,1))}")))
(example (utterance "lines that have a character before the word string leftquoatation ! rightquoatation") (targetValue (name "?{<any>,contain(<!>)}")))
(example (utterance "lines without a vowel after the string 'dog") (targetValue (name "?{contain(concat(<vow>,<!>))}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and at least 1 vowel") (targetValue (name "?{startwith(<!>),repeatatleast(<vow>,1)}")))
(example (utterance "lines starting with a character before lower-case letter") (targetValue (name "?{startwith(<any>),<low>}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation or have just have a number") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines with a lower-case letter , character , and string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<low>,<any>),<!>),<@>}")))
(example (utterance "lines with or without a number followed by a lower-case letter , leftquoatation ! rightquoatation , as well as leftquoatation @ rightquoatation") (targetValue (name "?{star(<num>),and(and(<low>,<!>),<@>)}")))
(example (utterance "lines with 7 or more of a letter before a lower-case letter") (targetValue (name "?{<let>,repeatatleast(<low>,7)}")))
(example (utterance "lines ending with only string leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines with a letter before character") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines with no vowels or leftquoatation dog at the end") (targetValue (name "?{or(<vow>,endwith(<!>))}")))
(example (utterance "lines with words with a capital letter or leftquoatation ! rightquoatation , 6 or more times in them") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines not containing a lower-case letter and 0 or more letters") (targetValue (name "?{not(<low>),star(<let>)}")))
(example (utterance "lines with a number or ending in lower-case letter") (targetValue (name "?{or(<num>,endwith(<low>))}")))
(example (utterance "lines containing a character followed by 3 or more lower-case letters") (targetValue (name "?{repeatatleast(<low>,3),<any>}")))
(example (utterance "lines with character before not leftquoatation ! rightquoatation") (targetValue (name "?{<any>,not(<!>)}")))
(example (utterance "lines starting in a lower-case letter and string leftquoatation ! rightquoatation or a character") (targetValue (name "?{startwith(<low>),or(<!>,<any>)}")))
(example (utterance "lines ending with a capital , 3 or more times") (targetValue (name "?{contain(endwith(<cap>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation zero times before a character") (targetValue (name "?{star(<!>),<any>}")))
(example (utterance "lines that start with leftquoatation ! rightquoatation or 2 or more numbers") (targetValue (name "?{startwith(<!>),repeatatleast(<num>,2)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , capital letter , or character") (targetValue (name "?{<!>,or(or(<@>,<cap>),<any>)}")))
(example (utterance "lines with a capital letter and a vowel before a lower-case letter in them") (targetValue (name "?{and(<cap>,<vow>),<low>}")))
(example (utterance "lines starting with vowel or a capital , 5 or more times") (targetValue (name "?{repeatatleast(or(<vow>,<cap>),5)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines which are  starting with a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines with words and a lower-case letter , number , or a character") (targetValue (name "?{or(<low>,<num>),<any>}")))
(example (utterance "lines with a number preceding a capital letter 2 or more times") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a letter at least 1 time .") (targetValue (name "?{<!>,repeatatleast(<let>,1)}")))
(example (utterance "lines without a character , number , or capital letter") (targetValue (name "?{or(or(<any>,<num>),<cap>)}")))
(example (utterance "lines which are containing a vowel followed by leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{repeatatleast(concat(<vow>,<!>),3)}")))
(example (utterance "lines containing lower-case letter and string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{and(<low>,repeatatleast(<!>,6))}")))
(example (utterance "lines containing words that have a lower-case letter or a number with a vowel") (targetValue (name "?{or(<low>,<num>),<vow>}")))
(example (utterance "lines containing 4 or more leftquoatation ! rightquoatation after leftquoatation @ rightquoatation") (targetValue (name "?{<@>,repeatatleast(<!>,4)}")))
(example (utterance "lines that end with words with a character or a letter") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{and(<!>,repeatatleast(<vow>,1))}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{<!>,endwith(<vow>)}")))
(example (utterance "lines with only 2 or more of leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines with 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{<!>,repeatatleast(<num>,3)}")))
(example (utterance "lines which are having either leftquoatation ! rightquoatation , a capital letter , or a character before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<cap>),<any>),<@>}")))
(example (utterance "lines with lower-case letter before string leftquoatation ! rightquoatation at least once") (targetValue (name "?{<low>,repeatatleast(<!>,1)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , starting with a number") (targetValue (name "?{contain(<!>),startwith(<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a character , zero times or more") (targetValue (name "?{concat(<!>,star(<any>))}")))
(example (utterance "lines with a character before either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation in them") (targetValue (name "?{<any>,or(or(<!>,<@>),<#>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before character , string leftquoatation @ rightquoatation , and string leftquoatation # rightquoatation") (targetValue (name "?{<!>,and(and(<any>,<@>),<#>)}")))
(example (utterance "lines end with 2 or more letters") (targetValue (name "?{endwith(repeatatleast(<let>,2))}")))
(example (utterance "lines in which a lower-case letter is preceded by a letter and followed by a character") (targetValue (name "?{<let>,concat(<low>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or character and vowel") (targetValue (name "?{contain(<!>),and(<any>,<vow>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation , a capital , or string leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,or(or(<@>,<cap>),<#>))}")))
(example (utterance "lines without a number before a letter") (targetValue (name "?{concat(<num>,repeatatleast(<let>,1))}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation at least once before number") (targetValue (name "?{repeatatleast(endwith(<!>),1),<num>}")))
(example (utterance "lines with words contain only a vowel , 7 or more times") (targetValue (name "?{<vow>}")))
(example (utterance "lines with a capital letter before at least 1 character") (targetValue (name "?{<cap>,repeatatleast(<any>,1)}")))
(example (utterance "lines having either a vowel , character , or number") (targetValue (name "?{or(or(<vow>,<any>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter followed by leftquoatation @ rightquoatation in them") (targetValue (name "?{<!>,concat(<let>,<@>)}")))
(example (utterance "lines which have both a character and also a letter") (targetValue (name "?{and(<any>,<let>)}")))
(example (utterance "lines with 7 or more of a vowel before a letter") (targetValue (name "?{<vow>,repeatatleast(<let>,7)}")))
(example (utterance "lines with a letter followed by leftquoatation ! rightquoatation or a character") (targetValue (name "?{<let>,contain(or(<!>,<any>))}")))
(example (utterance "lines with less than 3 of a vowel or capital letter") (targetValue (name "?{or(<vow>,repeatatleast(<cap>,3))}")))
(example (utterance "lines with a capital or a character , 5 or more times") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "items without a vowel or any letter .") (targetValue (name "?{and(and(<vow>,<cap>),<low>)}")))
(example (utterance "lines containing a character preceded by a letter and an uppercase letter") (targetValue (name "?{and(<let>,<cap>),<any>}")))
(example (utterance "items with a vowel preceding a numeral at least 7 times .") (targetValue (name "?{<vow>,repeatatleast(<num>,7)}")))
(example (utterance "your choice , no minimum , lines starting with a letter") (targetValue (name "?{star(<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation followed by either a character , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{not(<!>),or(or(<any>,<@>),<#>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{star(<!>),<cap>}")))
(example (utterance "lines without a capital letter , string leftquoatation ! rightquoatation , or character") (targetValue (name "?{contain(or(or(<cap>,<!>),<any>))}")))
(example (utterance "lines with zero or more of string leftquoatation ! rightquoatation and a lower-case letter or string leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),or(<low>,<@>)}")))
(example (utterance "lines with zero or more strings leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<!>,<@>),<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before either a letter , a capital , or string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<let>,<cap>),<@>)}")))
(example (utterance "lines having a capital letter , a vowel and a number") (targetValue (name "?{and(and(<cap>,<vow>),<num>),<let>}")))
(example (utterance "lines with at least 7 or more vowels and either a lower-case letter or letter .") (targetValue (name "?{or(and(<vow>,<low>),<let>)}")))
(example (utterance "lines with a letter before string leftquoatation ! rightquoatation and a capital letter before character") (targetValue (name "?{concat(<let>,<!>),concat(<cap>,<any>)}")))
(example (utterance "lines that contain a number followed by leftquoatation ! rightquoatation , at least 3 times") (targetValue (name "?{contain(concat(<num>,<!>))}")))
(example (utterance "items with a letter at least thrice preceding a vowel .") (targetValue (name "?{repeatatleast(<let>,3),<vow>}")))
(example (utterance "lines with a lower-case letter before a number and vowel , 2 times or more") (targetValue (name "?{concat(<low>,<num>),repeatatleast(<vow>,2)}")))
(example (utterance "lines with words and 3 or more capital letters") (targetValue (name "?{repeatatleast(<cap>,3)}")))
(example (utterance "lines containing a capital letter , a lower-case letter , a character , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<low>),<any>),<!>}")))
(example (utterance "lines with words with a lower-case letter or a number iin them") (targetValue (name "?{<low>,<num>}")))
(example (utterance "line that contains only numbers , can occur zero or more times .") (targetValue (name "?{contain(<num>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation 4 times") (targetValue (name "?{<any>,repeatatleast(<!>,4)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a number , 7 or more times in them") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation before a lower-case letter") (targetValue (name "?{concat(concat(<!>,<@>),<low>)}")))
(example (utterance "lines without a number , capital letter , or a lower-case letter") (targetValue (name "?{and(and(<cap>,<num>),<low>)}")))
(example (utterance "lines ending with lower-case letter before vowel at least once") (targetValue (name "?{endwith(<low>),repeatatleast(<vow>,1)}")))
(example (utterance "lines with character or a capital , zero or more times") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "lines not ending in string leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{concat(endwith(<!>),<cap>)}")))
(example (utterance "lines that have a character and the word leftquoatation ! rightquoatation plus a lower-case letter") (targetValue (name "?{and(and(<any>,<!>),<low>)}")))
(example (utterance "lines starting with a lower-case letter followed by a letter in them") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines with words an a capital letter or vowel") (targetValue (name "?{contain(<cap>),<vow>}")))
(example (utterance "items without leftquoatation ! rightquoatation , a letter , or a character .") (targetValue (name "?{or(or(<any>,<let>),<!>)}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation and a character followed by a number") (targetValue (name "?{and(<!>,<any>),<num>}")))
(example (utterance "lines with words and only 1 ore more lower-case letter") (targetValue (name "?{<low>}")))
(example (utterance "lines with a number , capital letter , or string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{or(or(<num>,<cap>),<!>),<vow>}")))
(example (utterance "lines ending with containing only a lower-case letter , at least 5 times") (targetValue (name "?{repeatatleast(<low>,5)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before character or capital letter") (targetValue (name "?{<!>,or(<any>,<cap>)}")))
(example (utterance "lines having a capital letter , leftquoatation ! rightquoatation , and a letter") (targetValue (name "?{and(and(<cap>,<!>),<let>)}")))
(example (utterance "lines where vowels come before letters") (targetValue (name "?{<vow>,<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a character before a number") (targetValue (name "?{or(<!>,<any>),<num>}")))
(example (utterance "lines with at least 7 letters , and lower-case letters") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines with words with a character followed by a capital letter , 5 or more times in them") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines with the string named leftquoatation ! rightquoatation preceding a number and after that , a small letter") (targetValue (name "?{<!>,concat(<num>,<low>)}")))
(example (utterance "lines not containing a capital letter , a number , and a letter") (targetValue (name "?{and(and(<cap>,<num>),<let>)}")))
(example (utterance "lines not having number before either a letter , string leftquoatation ! rightquoatation , or character") (targetValue (name "?{not(<num>),or(or(<let>,<!>),<any>)}")))
(example (utterance "lines that have a character which is numbering at 4 or greater appearances and that precedes a small letter") (targetValue (name "?{repeatatleast(<any>,4),<low>}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation and zero or more lower case-letters") (targetValue (name "?{endwith(<!>),star(<low>)}")))
(example (utterance "lines with 7 or more of leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines with a letter before ending in lower-case letter") (targetValue (name "?{<let>,endwith(<low>)}")))
(example (utterance "lines with words with a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines stating with 4 or more letters or leftquoatation ! rightquoatation") (targetValue (name "?{or(repeatatleast(<let>,4),<!>)}")))
(example (utterance "lines not having lower-case letter followed by a capital") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines with 5 or more vowels or letters") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 2 or more times before containing string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,2),contain(<@>)}")))
(example (utterance "lines with a character , capital letter , and vowel") (targetValue (name "?{and(and(<any>,<cap>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a letter , or leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{or(or(<!>,<let>),<@>),<vow>}")))
(example (utterance "lines with words string leftquoatation ! rightquoatation and a character , 2 or more times") (targetValue (name "?{and(<!>,<any>)}")))
(example (utterance "lines which have a lower case letter and a number preceding a capital letter") (targetValue (name "?{and(<low>,<num>),<cap>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation and a capital letter with a letter in front of it") (targetValue (name "?{and(<!>,<let>),<cap>}")))
(example (utterance "lines containing a character after a vowel") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines with 5 or more of a character before vowel") (targetValue (name "?{<any>,repeatatleast(<vow>,5)}")))
(example (utterance "lines with words with a character 5 times") (targetValue (name "?{<any>}")))
(example (utterance "lines with words , leftquoatation ! rightquoatation , and a letter") (targetValue (name "?{<!>,repeatatleast(<let>,1)}")))
(example (utterance "lines that have words that contain a vowel , a capital letter , and a lower-case letter") (targetValue (name "?{and(<vow>,<low>),<cap>}")))
(example (utterance "lines beginning with a character .") (targetValue (name "?{<any>}")))
(example (utterance "items with a character or a leftquoatation ! rightquoatation at least5times .") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines containing a vowel") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines with words , leftquoatation ! rightquoatation , a capital letter , and number") (targetValue (name "?{and(<!>,<cap>),<num>}")))
(example (utterance "lines that end with a combination of the word leftquoatation ! rightquoatation , a character and a vowel") (targetValue (name "?{contain(and(and(<!>,<vow>),<any>))}")))
(example (utterance "lines with vowel , zero , or more times before a letter") (targetValue (name "?{star(<vow>),<let>}")))
(example (utterance "lines with a number or starting in zero or more of leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,startwith(<!>))}")))
(example (utterance "lines with a letter before character then string leftquoatation ! rightquoatation") (targetValue (name "?{<let>,concat(<any>,<!>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation followed by a lower-case letter") (targetValue (name "?{or(<!>,<@>),<low>}")))
(example (utterance "lines without a capital letter , character , or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<any>),<!>)}")))
(example (utterance "items with a letter preceding a character and a numeral .") (targetValue (name "?{<let>,and(<num>,<any>)}")))
(example (utterance "items with a letter at least6times prior to leftquoatation ! rightquoatation .") (targetValue (name "?{repeatatleast(<let>,6),<!>}")))
(example (utterance "lines with words with character followed by letter") (targetValue (name "?{<any>,<let>}")))
(example (utterance "lines with words with ending with a letter or leftquoatation ! rightquoatation in them") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation , a number , and lower-case letter") (targetValue (name "?{and(and(<!>,<num>),<low>)}")))
(example (utterance "lines containing a lower-case letter followed by a vowel more than once") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "the lines which contain a character followed by a vowel , the string 'dog , and lastly leftquoatation ! rightquoatation") (targetValue (name "?{<any>,and(and(<vow>,<!>),<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or character followed by vowel , 4 or more times") (targetValue (name "?{or(<!>,concat(<any>,<vow>))}")))
(example (utterance "lines with a lower-case letter , 2 or more times occurring before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,2),<!>}")))
(example (utterance "lines having either number , letter , or character before a vowel") (targetValue (name "?{or(or(<num>,<let>),<any>),<vow>}")))
(example (utterance "items with leftquoatation ! rightquoatation or a character or leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<!>,<@>),<any>)}")))
(example (utterance "lines containing dog followed by 7 or more numbers") (targetValue (name "?{<!>,repeatatleast(<num>,7)}")))
(example (utterance "lines containing only zero or more leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines with 2 or more characters or letters") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines with 7 or more words without a vowel") (targetValue (name "?{not(<vow>)}")))
(example (utterance "lines having either string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , or character before vowel") (targetValue (name "?{or(or(<!>,<@>),<any>),<vow>}")))
(example (utterance "lines without a number before leftquoatation ! rightquoatation") (targetValue (name "?{not(<num>),star(<!>)}")))
(example (utterance "lines that have words with the word leftquoatation ! rightquoatation following a capital letter") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines without a lower-case letter or with capital letter") (targetValue (name "?{or(not(<low>),<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , zero or more times before leftquoatation @ rightquoatation in them") (targetValue (name "?{concat(star(<!>),<@>)}")))
(example (utterance "vowel before") (targetValue (name "?{<vow>,repeatatleast(<!>,3)}")))
(example (utterance "lines not having number , letter , or lower-case letter") (targetValue (name "?{or(or(<num>,<let>),<low>)}")))
(example (utterance "lines with a vowel before lower-case letter then letter") (targetValue (name "?{<vow>,concat(<low>,<let>)}")))
(example (utterance "lines not having vowel , at least 7 times .") (targetValue (name "?{repeatatleast(<vow>,7)}")))
(example (utterance "lines with a letter , zero or more times before containing leftquoatation ! rightquoatation in them") (targetValue (name "?{star(<let>),contain(<!>)}")))
(example (utterance "lines that have zero or more of leftquoatation ! rightquoatation and a character") (targetValue (name "?{star(<!>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and capital letter before a letter") (targetValue (name "?{and(<!>,<cap>),contain(<let>)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation and number before string leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),concat(<num>,<@>)}")))
(example (utterance "items with a vowel or numeral preceding a letter .") (targetValue (name "?{or(<vow>,<num>),<let>}")))
(example (utterance "before leftquoatation ! rightquoatation lines must have either a character , a capital letter or a lower case letter") (targetValue (name "?{or(or(<cap>,<any>),<low>),<!>}")))
(example (utterance "lines with no letter , no string leftquoatation ! rightquoatation , and not a character") (targetValue (name "?{and(and(<let>,<!>),<any>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation or ending with string leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),endwith(<#>)}")))
(example (utterance "lines which are containing a letter followed by a vowel") (targetValue (name "?{concat(<let>,<vow>)}")))
(example (utterance "lines that contain a letter or begin with a vowel") (targetValue (name "?{contain(<let>),startwith(<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{contain(<!>),concat(<@>,<#>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation and leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{star(<!>),or(<@>,<vow>)}")))
(example (utterance "items with a small letter at least 5 times .") (targetValue (name "?{repeatatleast(<low>,5)}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation starting in string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<cap>,<!>),startwith(<@>)}")))
(example (utterance "lines with a character or letter at least once") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines with a number , string leftquoatation ! rightquoatation , and capital letter") (targetValue (name "?{and(and(<num>,<!>),<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a lower-case letter at least 2 times") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "the line start with a number following a capital letter , zero or more times .") (targetValue (name "?{star(concat(<num>,<cap>))}")))
(example (utterance "lines containing a capital letter before at least 1 letter") (targetValue (name "?{<cap>,repeatatleast(contain(<let>),1)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation followed by a capital letter , then a number") (targetValue (name "?{<!>,concat(<cap>,<num>)}")))
(example (utterance "lines which aare not having a character or leftquoatation ! rightquoatation and a vowel") (targetValue (name "?{not(<any>),and(<!>,<vow>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation before a lower-case letter leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),concat(<low>,<@>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before a character , string leftquoatation @ rightquoatation , and lower-case letter") (targetValue (name "?{<!>,and(and(<any>,<@>),<low>)}")))
(example (utterance "lines containing 4 or more of leftquoatation ! rightquoatation before a number") (targetValue (name "?{repeatatleast(contain(<!>),4),<num>}")))
(example (utterance "lines having words and containing leftquoatation ! rightquoatation following leftquoatation @ rightquoatation or a capital letter") (targetValue (name "?{concat(<!>,<@>),<cap>}")))
(example (utterance "lines that must not have a number , character , or vowel") (targetValue (name "?{or(or(<num>,<any>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation ending by a character") (targetValue (name "?{endwith(concat(<!>,<any>))}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 6 or more times and string leftquoatation @ rightquoatation followed by number") (targetValue (name "?{repeatatleast(<!>,6),concat(<@>,<num>)}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a capital letter before lower-case letter") (targetValue (name "?{or(or(<!>,<@>),<cap>),<low>}")))
(example (utterance "lines without a character , a number , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<num>),<!>)}")))
(example (utterance "on at least 1 occurance , lines should have a letter following a lower case letter") (targetValue (name "?{repeatatleast(<low>,1),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and number before a letter") (targetValue (name "?{and(<!>,<num>),<let>}")))
(example (utterance "lines with5or more times vowels followed by a numbers 5") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines with vowel followed by string leftquoatation ! rightquoatation , 4 or more times") (targetValue (name "?{concat(<vow>,<!>)}")))
(example (utterance "items with words containing either a upper case letter , a character , or a vowel .") (targetValue (name "?{or(<vow>,<any>),<cap>}")))
(example (utterance "lines with words with 3 or more of a character or number") (targetValue (name "?{or(<any>,<num>)}")))
(example (utterance "3 or more lines with letters") (targetValue (name "?{<let>}")))
(example (utterance "lines starting with a number and leftquoatation ! rightquoatation , 2 or more times in them") (targetValue (name "?{startwith(<num>),repeatatleast(<!>,2)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation , a lower-case letter , or character") (targetValue (name "?{or(<!>,<low>),<any>}")))
(example (utterance "lines which are ending with words containing only a lower-case letter at least once") (targetValue (name "?{repeatatleast(<low>,1)}")))
(example (utterance "lines that do not contain a vowel or a number") (targetValue (name "?{and(<num>,<vow>)}")))
(example (utterance "lines with a character after a letter at least 5 times") (targetValue (name "?{<let>,repeatatleast(<any>,5)}")))
(example (utterance "items with a numeral or a small letter preceding an upper case letter .") (targetValue (name "?{or(<low>,<num>),<cap>}")))
(example (utterance "items with an upper class and vowel preceding leftquoatation ! rightquoatation .") (targetValue (name "?{and(<vow>,<cap>),<!>}")))
(example (utterance "lines with 3 or more or a letter before capital letter") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines ending with a letter before another letter") (targetValue (name "?{endwith(<any>),<low>}")))
(example (utterance "lines that have phrases starting with the word leftquoatation ! rightquoatation followed by a capital letter") (targetValue (name "?{startwith(<!>),<cap>}")))
(example (utterance "vowel followed by a capital letter .") (targetValue (name "?{<vow>,<cap>}")))
(example (utterance "lines with a character or starting in string leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,startwith(<!>))}")))
(example (utterance "lines that have a lower-case letter , the word leftquoatation dog' , and any vowel") (targetValue (name "?{and(and(<low>,<!>),<vow>)}")))
(example (utterance "lines with either a vowel or a letter that come before a capitalized letter") (targetValue (name "?{or(<vow>,<let>),<cap>}")))
(example (utterance "items with words containing a character , a vowel , and a numeral .") (targetValue (name "?{and(<num>,<vow>),<any>}")))
(example (utterance "lines without leftquoatation ! rightquoatation before a character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines which do not have words with ending with a letter") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with zero or more numbers and leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{star(<num>),or(or(<!>,<@>),<#>)}")))
(example (utterance "items with a letter preceding leftquoatation ! rightquoatation or leftquoatation @ rightquoatation .") (targetValue (name "?{<let>,or(<!>,<@>)}")))
(example (utterance "lines with a vowel or letter 7 times") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "letter before a vowel") (targetValue (name "?{or(<low>,<let>),<vow>}")))
(example (utterance "lines with a capital letter , string leftquoatation ! rightquoatation , or character") (targetValue (name "?{or(or(<cap>,<!>),<any>)}")))
(example (utterance "lines having a character , lower-case letter , and letter before a vowel") (targetValue (name "?{and(and(<any>,<low>),<let>),<vow>}")))
(example (utterance "lines with with capital letter plus character before low case") (targetValue (name "?{and(<cap>,<any>),<low>}")))
(example (utterance "lines starting in 2 or more of leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{repeatatleast(or(<!>,<low>),2)}")))
(example (utterance "lines with a lower-case letter , string leftquoatation ! rightquoatation , or character before a capital letter") (targetValue (name "?{or(or(<low>,<!>),<any>),<cap>}")))
(example (utterance "lines containing a number , a vowel , or a lower-case letter") (targetValue (name "?{or(<num>,<vow>),<low>}")))
(example (utterance "lines not ending with leftquoatation ! rightquoatation , 2 or so times") (targetValue (name "?{repeatatleast(endwith(<!>),2)}")))
(example (utterance "lines with  lower-case letter before either leftquoatation ! rightquoatation , a letter , or number") (targetValue (name "?{<low>,or(or(<!>,<let>),<num>)}")))
(example (utterance "lines with words with a capital letter followed by a lower-case letter at least 6 times") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "items with words containing a small letter , a character , or leftquoatation ! rightquoatation .") (targetValue (name "?{or(<any>,<low>),<!>}")))
(example (utterance "lines with words and a vowel , string leftquoatation ! rightquoatation , or number") (targetValue (name "?{or(<vow>,<!>),<num>}")))
(example (utterance "lines with a letter preceding a vowel") (targetValue (name "?{<let>,<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation  before leftquoatation @ rightquoatation and both lower case and capital letters") (targetValue (name "?{<!>,and(and(<@>,<low>),<cap>)}")))
(example (utterance "lines containing a capital letter , leftquoatation ! rightquoatation , and a lower-case letter in them") (targetValue (name "?{and(and(<cap>,<!>),<low>)}")))
(example (utterance "lines with a number before a letter 5 or more times") (targetValue (name "?{repeatatleast(<num>,5),<let>}")))
(example (utterance "lines that contain a letter , a vowel , and a number followed by leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<vow>),<num>),<!>}")))
(example (utterance "lines with words that contain a character , letter , and a capital") (targetValue (name "?{and(and(<any>,<let>),<cap>)}")))
(example (utterance "lines with 4 or more of a capital letter or number") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines with a lower-case or capital letter") (targetValue (name "?{or(<let>,<low>),<cap>}")))
(example (utterance "lines that do not end with a letter") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , then leftquoatation @ rightquoatation , then leftquoatation # rightquoatation") (targetValue (name "?{startwith(<!>),concat(<@>,<#>)}")))
(example (utterance "lines with a character followed by leftquoatation ! rightquoatation before a lower-case letter in them") (targetValue (name "?{concat(<any>,<!>),<low>}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation before character") (targetValue (name "?{startwith(<!>),<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation either a vowel or a lower-case letter , zero or more times") (targetValue (name "?{contain(or(or(<vow>,<low>),<!>))}")))
(example (utterance "lines having an upper-case letter numbering at least 6 times or greater") (targetValue (name "?{<cap>}")))
(example (utterance "items with a small letter , leftquoatation ! rightquoatation , and a letter preceding an upper case letter .") (targetValue (name "?{and(and(<low>,<let>),<!>),<cap>}")))
(example (utterance "lines that don't have a character , a vowel , or a number") (targetValue (name "?{or(or(<any>,<vow>),<num>)}")))
(example (utterance "lines having either a lower-case letter , a capital letter , or a number before leftquoatation ! rightquoatation in them") (targetValue (name "?{or(or(<low>,<cap>),<num>),<!>}")))
(example (utterance "lines containing a lower case letter preceded by a vowel") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines where either leftquoatation ! rightquoatation or a vowel appear 4 or more times") (targetValue (name "?{contain(or(<!>,<vow>))}")))
(example (utterance "lines with dog , lower-case letters , or vowels before a letter") (targetValue (name "?{or(or(<!>,<low>),<vow>),<let>}")))
(example (utterance "lines with a vowel followed by a letter , at least 5 times") (targetValue (name "?{<vow>,repeatatleast(<let>,5)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation , leftquoatation @ rightquoatation or a number") (targetValue (name "?{or(or(<!>,<num>),<@>)}")))
(example (utterance "lines containing words which have a character , a letter , and a capital letter") (targetValue (name "?{and(<any>,<let>),<cap>}")))
(example (utterance "lines without a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(concat(<let>,<!>),1)}")))
(example (utterance "lines starting in a character with a vowel") (targetValue (name "?{startwith(<any>),contain(<vow>)}")))
(example (utterance "lines not ending with string leftquoatation ! rightquoatation") (targetValue (name "?{contain(endwith(<!>))}")))
(example (utterance "lines with a lower-case letter before non string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<low>,1),not(<!>)}")))
(example (utterance "lines containing a number and a character") (targetValue (name "?{<num>,contain(<any>)}")))
(example (utterance "lines with a capital and a number before string leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,<num>),<!>}")))
(example (utterance "lines without a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{and(contain(<low>),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a vowel and with leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<vow>),<@>}")))
(example (utterance "lines with a number before a character") (targetValue (name "?{star(<num>),<any>}")))
(example (utterance "lines with a letter followed by string leftquoatation ! rightquoatation or not leftquoatation @ rightquoatation") (targetValue (name "?{concat(<let>,<!>),not(<@>)}")))
(example (utterance "lines having either lower-case letter , string leftquoatation ! rightquoatation , or string leftquoatation @ rightquoatation before number") (targetValue (name "?{or(or(<low>,<!>),<@>),<num>}")))
(example (utterance "lines that terminate with a vowel coming after a character") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "lines with a character , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation with a number beforehand .") (targetValue (name "?{or(or(<any>,<!>),<num>),<@>}")))
(example (utterance "lines with words start with a capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "lines have words with a number , a lower-case letter , or a vowel") (targetValue (name "?{or(<num>,<low>),<vow>}")))
(example (utterance "lines ending in a letter or with capital") (targetValue (name "?{repeatatleast(or(<let>,<cap>),1)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<!>,<let>),<cap>}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , and string leftquoatation # rightquoatation before a capital") (targetValue (name "?{and(and(<!>,<@>),<#>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a letter then capital letter") (targetValue (name "?{concat(<!>,<let>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation then leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{contain(concat(<!>,<@>)),<vow>}")))
(example (utterance "lines in which a vowel is followed a capital letter which is followed by a letter that's lower-case") (targetValue (name "?{<vow>,concat(<cap>,<low>)}")))
(example (utterance "lines that contain the word leftquoatation ! rightquoatation preceding the word leftquoatation @ rightquoatation followed by the word leftquoatation # rightquoatation") (targetValue (name "?{concat(<!>,<@>),<#>}")))
(example (utterance "lines without only leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{concat(<!>,<cap>)}")))
(example (utterance "lines with words that aren't  ending with a number") (targetValue (name "?{endwith(<num>)}")))
(example (utterance "lines with 6 or more letters") (targetValue (name "?{contain(or(<cap>,<low>))}")))
(example (utterance "lines with a lowercase letter preceding anything but a letter") (targetValue (name "?{<low>,not(<let>)}")))
(example (utterance "lines starting without containing leftquoatation ! rightquoatation") (targetValue (name "?{contain(not(<!>))}")))
(example (utterance "lines with a vowel before a letter or a lower-case letter and leftquoatation ! rightquoatation in them") (targetValue (name "?{concat(<vow>,<let>),and(<low>,<!>)}")))
(example (utterance "lines starting with zero or more lower-case letters") (targetValue (name "?{startwith(<low>)}")))
(example (utterance "lines that end a lower-case letter") (targetValue (name "?{<low>}")))
(example (utterance "lines with words and letters before a lower-case letter or a character") (targetValue (name "?{concat(<let>,<low>),<any>}")))
(example (utterance "lines containing either letter , number , or character") (targetValue (name "?{or(or(<let>,<num>),<any>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation or starting with a capital") (targetValue (name "?{or(<!>,startwith(<cap>))}")))
(example (utterance "lines which contain a number preceded by a character") (targetValue (name "?{<any>,<num>}")))
(example (utterance "items with leftquoatation ! rightquoatation , a character , and a numeral .") (targetValue (name "?{and(<num>,<any>),<!>}")))
(example (utterance "lines with a letter followed by 5 capital letters") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines that have 4 or more characters") (targetValue (name "?{repeatatleast(<any>,4)}")))
(example (utterance "a character appears before containing a lower-case letter in the lines") (targetValue (name "?{<any>,contain(<low>)}")))
(example (utterance "lines without a capital letter and or leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,star(<!>))}")))
(example (utterance "lines with words starting with string leftquoatation ! rightquoatation , 7 or more times") (targetValue (name "?{startwith(<!>)}")))
(example (utterance "lines without a capital letter followed by a letter") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines with a character followed by leftquoatation ! rightquoatation or 0 or more letters") (targetValue (name "?{or(concat(<any>,<!>),<let>)}")))
(example (utterance "lines with a capital letter after a vowel at least zero times") (targetValue (name "?{<vow>,star(<cap>)}")))
(example (utterance "lines have to contain any of the following: a vowel , a lower case letter or a capital letter") (targetValue (name "?{or(or(<vow>,<low>),<cap>)}")))
(example (utterance "lines with a vowel before leftquoatation ! rightquoatation , 6 or more times in them") (targetValue (name "?{<vow>,repeatatleast(<!>,6)}")))
(example (utterance "lines not starting with a character 4 times") (targetValue (name "?{repeatatleast(startwith(<any>),4)}")))
(example (utterance "lines that might contain leftquoatation ! rightquoatation before a number") (targetValue (name "?{star(<!>),contain(<num>)}")))
(example (utterance "lines with words that contain a number followed by a capital letter , at least twice") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines starting with letter or string leftquoatation ! rightquoatation , 7 or more times") (targetValue (name "?{or(startwith(<let>),<!>)}")))
(example (utterance "lines with a capital letter before not a number , 3 or more times in them") (targetValue (name "?{<cap>,not(repeatatleast(<num>,3))}")))
(example (utterance "at least6times before leftquoatation ! rightquoatation use leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<@>,6),<!>}")))
(example (utterance "lines with letter or string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines containing either a capital letter , string leftquoatation ! rightquoatation , or lower-case letter") (targetValue (name "?{or(or(<cap>,<!>),<low>)}")))
(example (utterance "lines with lower-case letter or containing only string leftquoatation ! rightquoatation at least once") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "items with a small letter , leftquoatation ! rightquoatation , and numeral preceding a character .") (targetValue (name "?{and(and(<low>,<!>),<num>),<any>}")))
(example (utterance "lines containing a letter or leftquoatation ! rightquoatation") (targetValue (name "?{<let>,<!>}")))
(example (utterance "lines starting with character or number before letter") (targetValue (name "?{or(<any>,concat(<num>,<let>))}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a character , or a number after a vowel") (targetValue (name "?{<vow>,or(or(<!>,<any>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation or a letter") (targetValue (name "?{concat(<!>,or(<@>,<let>))}")))
(example (utterance "lines with 5 or more of a vowel or lower-case letter") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "items with words that contain a numeral preceding leftquoatation ! rightquoatation at least 1 times .") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , 2 or more times before a vowel in them") (targetValue (name "?{repeatatleast(<!>,2),<vow>}")))
(example (utterance "lines with words and only at least 1 character") (targetValue (name "?{<any>}")))
(example (utterance "lines with a letter before words with zero or more of leftquoatation ! rightquoatation") (targetValue (name "?{<let>,star(<!>)}")))
(example (utterance "lines without a number and letter before string leftquoatation ! rightquoatation") (targetValue (name "?{not(<num>),concat(<let>,<!>)}")))
(example (utterance "lines with words ending in leftquoatation ! rightquoatation before a character") (targetValue (name "?{endwith(<!>),<any>}")))
(example (utterance "lines having leftquoatation ! rightquoatation before a letter followed by a character") (targetValue (name "?{<!>,concat(<let>,<any>)}")))
(example (utterance "lines with a number and a letter .") (targetValue (name "?{and(<num>,<low>)}")))
(example (utterance "lines with words that contain vowel , string leftquoatation ! rightquoatation , and a capital") (targetValue (name "?{and(and(<vow>,<!>),<cap>)}")))
(example (utterance "lines containing a vowel before number , zero or more times") (targetValue (name "?{contain(concat(<vow>,<num>))}")))
(example (utterance "items terminating with a small letter or a character .") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines with words with a character followed by a letter") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines with a lower case letter after 5 vowels") (targetValue (name "?{repeatatleast(<vow>,5),<low>}")))
(example (utterance "lines with vowel before string leftquoatation ! rightquoatation , lower-case letter , and letter") (targetValue (name "?{<vow>,and(and(<!>,<low>),<let>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation or a lower-case letter or a number") (targetValue (name "?{or(or(<low>,<num>),<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a small letter , and a numeral following leftquoatation @ rightquoatation .") (targetValue (name "?{<@>,and(and(<num>,<low>),<!>)}")))
(example (utterance "lines with 3 or more of a number before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation , lower-case letter , and vowel") (targetValue (name "?{<any>,or(or(<!>,<low>),<vow>)}")))
(example (utterance "lines where a letter is preceded by a lowercase letter") (targetValue (name "?{<low>,<let>}")))
(example (utterance "lines with a character after a lower-case letter , 4 or more times") (targetValue (name "?{<low>,repeatatleast(<any>,4)}")))
(example (utterance "lines with a letter preceding  a number zero or more times .") (targetValue (name "?{<let>,star(<num>)}")))
(example (utterance "lines with a lower-case letter , a letter , and a character") (targetValue (name "?{and(<low>,<let>),<any>}")))
(example (utterance "items with a vowel preceding leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{<vow>,concat(<!>,<@>)}")))
(example (utterance "items with a any letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{or(<cap>,<low>),<!>}")))
(example (utterance "lines stating with leftquoatation ! rightquoatation before a lower-case letter , or leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{startwith(<!>),or(or(<low>,<@>),<#>)}")))
(example (utterance "lines with a lower-case letter followed by a letter") (targetValue (name "?{concat(<low>,<let>),<num>}")))
(example (utterance "line with a capital letter before a letter") (targetValue (name "?{concat(<cap>,<let>)}")))
(example (utterance "lines not starting in leftquoatation ! rightquoatation before number") (targetValue (name "?{concat(startwith(<!>),<num>)}")))
(example (utterance "lines with 5 or more of leftquoatation ! rightquoatation ending in a letter") (targetValue (name "?{repeatatleast(<!>,5),endwith(<let>)}")))
(example (utterance "items terminating with leftquoatation ! rightquoatation preceding a character at least 4 times .") (targetValue (name "?{endwith(<!>),repeatatleast(<any>,4)}")))
(example (utterance "lines that do not contain a number followed by a character and do not contain leftquoatation ! rightquoatation") (targetValue (name "?{and(concat(<num>,<any>),<!>)}")))
(example (utterance "lines with 5 or more capital letters or vowels") (targetValue (name "?{or(<cap>,<vow>)}")))
(example (utterance "lines which have a vowel coming before a letter") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines with a letter , a lower-case letter , or leftquoatation ! rightquoatation before a number") (targetValue (name "?{or(or(<let>,<low>),<!>),<num>}")))
(example (utterance "lines with a lower-case letter and strings leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before letter") (targetValue (name "?{and(and(<low>,<!>),<@>),<let>}")))
(example (utterance "lines with a vowel before a lower-case letter without capital letter") (targetValue (name "?{concat(<vow>,<low>),not(<cap>)}")))
(example (utterance "lines ending with 7 or more leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , 4 or more times before letter") (targetValue (name "?{repeatatleast(<!>,4),<let>}")))
(example (utterance "lines with 3 or more numbers or lower-case letters") (targetValue (name "?{or(<num>,<low>)}")))
(example (utterance "lines not end with leftquoatation ! rightquoatation") (targetValue (name "?{endwith(or(<let>,<!>))}")))
(example (utterance "lines with a number at least 4 times with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<num>,4),concat(<!>,<@>)}")))
(example (utterance "lines with a letter before a lower-case and vowel") (targetValue (name "?{<let>,and(<low>,<vow>)}")))
(example (utterance "lines without a number , character , or capital letter") (targetValue (name "?{or(or(<num>,<any>),<cap>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation or a character at least 7 times") (targetValue (name "?{repeatatleast(or(<!>,<any>),7)}")))
(example (utterance "lines with a vowel before a number then string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<vow>,<num>),<!>}")))
(example (utterance "lines containing a letter or leftquoatation ! rightquoatation at least 4 times") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation , 7 or more times") (targetValue (name "?{<num>,repeatatleast(<!>,7)}")))
(example (utterance "lines with words that contain an vowel") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines without characters , lower-case letters , or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<!>),<low>)}")))
(example (utterance "lines with words ending with a character at least 5 times") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines with at least 2 number or vowels") (targetValue (name "?{or(<num>,<vow>)}")))
(example (utterance "lines starting with words and a character at least once") (targetValue (name "?{repeatatleast(<any>,1)}")))
(example (utterance "lines starting with words with a character or letter") (targetValue (name "?{or(or(<any>,<cap>),<let>)}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation , leftquoatation # rightquoatation , and leftquoatation $ rightquoatation") (targetValue (name "?{<!>,and(and(<@>,<#>),<$>)}")))
(example (utterance "lines that end with a lower-case letter or a vowel") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines with words with a character and string leftquoatation ! rightquoatation") (targetValue (name "?{<any>,<!>}")))
(example (utterance "lines with a vowel , string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{and(<vow>,<!>),or(or(<@>,<any>),<let>)}")))
(example (utterance "lines with a character followed by leftquoatation ! rightquoatation , at least 7 times") (targetValue (name "?{<any>,repeatatleast(<!>,7)}")))
(example (utterance "lines containing 2 or more capital letters") (targetValue (name "?{repeatatleast(<cap>,2)}")))
(example (utterance "lines ending leftquoatation ! rightquoatation 1 or more times") (targetValue (name "?{endwith(contain(<!>))}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation followed by vowel or character") (targetValue (name "?{or(concat(<!>,<vow>),<any>)}")))
(example (utterance "lines with no vowel or number 5 times") (targetValue (name "?{not(<vow>),repeatatleast(<num>,5)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by vowel , 2 or more times") (targetValue (name "?{concat(<!>,<vow>)}")))
(example (utterance "lines without a number before ending with leftquoatation ! rightquoatation") (targetValue (name "?{not(<num>),endwith(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , number , or letter") (targetValue (name "?{or(or(<!>,<num>),<let>)}")))
(example (utterance "lines starting in a letter before string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(concat(<let>,<!>))}")))
(example (utterance "lines beginning with a number or finishing with a lower-case letter") (targetValue (name "?{startwith(<num>),endwith(<low>)}")))
(example (utterance "lines that must not have any lower-case letter or vowel") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "items with an upper case letter preceding a character .") (targetValue (name "?{<cap>,star(<any>)}")))
(example (utterance "lines that do not contain a lower-case letter , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<vow>),<!>)}")))
(example (utterance "lines ending with a letter before number") (targetValue (name "?{contain(concat(<let>,<num>))}")))
(example (utterance "lines with 5 or more of a letter") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines having vowel , character , and string leftquoatation ! rightquoatation before number") (targetValue (name "?{and(and(<vow>,<any>),<!>),<num>}")))
(example (utterance "items with a letter or numeral preceding a vowel .") (targetValue (name "?{or(<num>,<let>),<vow>}")))
(example (utterance "lines finishing with either a number , a lower-case letter , or a vowel") (targetValue (name "?{contain(or(or(<num>,<low>),<vow>))}")))
(example (utterance "lines with a letter and leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{and(<let>,<!>),<vow>}")))
(example (utterance "lines ending in a lower-case letter before zero or more numbers") (targetValue (name "?{endwith(<low>),star(<num>)}")))
(example (utterance "lines ending in words and with string leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines starting in words with a lower-case letter , string leftquoatation ! rightquoatation , or number") (targetValue (name "?{or(or(<low>,<!>),<num>)}")))
(example (utterance "lines with 3 or more vowels before characters") (targetValue (name "?{concat(<vow>,<any>)}")))
(example (utterance "lines with a character and vowel before string leftquoatation ! rightquoatation then lower-case letter") (targetValue (name "?{and(<any>,<vow>),concat(<!>,<low>)}")))
(example (utterance "lines having a lower-case letter , string leftquoatation ! rightquoatation , and string leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{and(and(<low>,<!>),<@>),<vow>}")))
(example (utterance "lines containing a vowel , leftquoatation ! rightquoatation , a capital letter , and a lower-case letter") (targetValue (name "?{and(and(<vow>,<!>),<cap>),<low>}")))
(example (utterance "lines without letter or capital letter") (targetValue (name "?{and(<let>,<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a character , or a vowel before leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<vow>,<!>),<any>),<@>}")))
(example (utterance "lines with a2 or more of a lower-case before capital letter") (targetValue (name "?{contain(<low>),repeatatleast(<cap>,2)}")))
(example (utterance "lines containing a letter or character preceding a vowel") (targetValue (name "?{or(<any>,<let>),<vow>}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation followed by lower-case letter before string leftquoatation @ rightquoatation") (targetValue (name "?{concat(concat(<!>,<low>),<@>)}")))
(example (utterance "items with a small letter , twice or more before an upper case letter .") (targetValue (name "?{repeatatleast(<low>,2),<cap>}")))
(example (utterance "lines not having vowel at least once") (targetValue (name "?{repeatatleast(<vow>,1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation coming zero or more times before a letter") (targetValue (name "?{star(<!>),<let>}")))
(example (utterance "lines with a character coming before a capital letter followed by a number") (targetValue (name "?{<any>,concat(<cap>,<num>)}")))
(example (utterance "lines with words with starting with a number before a capital letter in them") (targetValue (name "?{startwith(<num>),<cap>}")))
(example (utterance "lines with a capital letter or a number , zero or more times in them") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines having either leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , or string leftquoatation # rightquoatation before character") (targetValue (name "?{or(or(<!>,<@>),<#>),<any>}")))
(example (utterance "lines with a letter 7 times") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a vowel , and a capital letter") (targetValue (name "?{and(<!>,<vow>),<cap>}")))
(example (utterance "ines not having leftquoatation ! rightquoatation or a lower-case letter , 6 or more times") (targetValue (name "?{not(<!>),repeatatleast(<low>,6)}")))
(example (utterance "lines with leftquoatation ! rightquoatation of leftquoatation @ rightquoatation or a capital letter") (targetValue (name "?{or(or(<!>,<@>),<cap>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation , character , or vowel before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<any>),<vow>),<@>}")))
(example (utterance "lines not containing leftquoatation ! rightquoatation , a capital letter , or a vowel") (targetValue (name "?{contain(and(and(<!>,<cap>),<vow>))}")))
(example (utterance "lines with an occurrence of a number no less than zero times before a capital letter") (targetValue (name "?{star(<num>),<cap>}")))
(example (utterance "lines finishing with a character at least once") (targetValue (name "?{repeatatleast(contain(<any>),1)}")))
(example (utterance "lines with with character 6 times") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines containing words and a letter and a small letter subsequent to that") (targetValue (name "?{<let>,<low>}")))
(example (utterance "items with an uppercase letter preceding leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{<cap>,concat(<!>,<@>)}")))
(example (utterance "lines ending with vowel before lower case letter") (targetValue (name "?{endwith(<vow>),<low>}")))
(example (utterance "lines that end with a character or a vowel") (targetValue (name "?{or(<any>,<vow>)}")))
(example (utterance "the lines that contain either a lower case letter or leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{or(<low>,<!>),<num>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation and a number") (targetValue (name "?{contain(<!>),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation and a character before a lower-case letter") (targetValue (name "?{<!>,or(or(<low>,<@>),<any>)}")))
(example (utterance "lines that have a character , a number , or a letter") (targetValue (name "?{or(or(<any>,<num>),<let>)}")))
(example (utterance "lines not words with string leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{repeatatleast(<!>,3)}")))
(example (utterance "lines that have words with a capital letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,<!>}")))
(example (utterance "lines with words without a letter") (targetValue (name "?{not(<let>)}")))
(example (utterance "lines with number before lower-case letter , 7 or more times") (targetValue (name "?{<num>,repeatatleast(<low>,7)}")))
(example (utterance "lines that do not have leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{and(<!>,<cap>)}")))
(example (utterance "a number berfe the words , dog truck and another character") (targetValue (name "?{<num>,and(and(<!>,<@>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or with only zero or more of string leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines with 7 or more of a character before a letter") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines containing words ending with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with a number , a capital letter , or a vowel after leftquoatation ! rightquoatation") (targetValue (name "?{<!>,or(or(<num>,<cap>),<vow>)}")))
(example (utterance "lines starting with a capital letter at least 1 time") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines with a capital letter after the string dog at least zero times") (targetValue (name "?{<!>,star(<cap>)}")))
(example (utterance "lines with a number or vowel before lower-case letter") (targetValue (name "?{or(<num>,<vow>),<low>}")))
(example (utterance "lines with a number or ending in capital letter") (targetValue (name "?{or(<num>,endwith(<cap>))}")))
(example (utterance "lines with a character followed by a letter , zero or more times in it") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines without a capital letter or a lower-case letter") (targetValue (name "?{contain(or(<cap>,<low>))}")))
(example (utterance "lines beginning with either a vowel or a letter") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines with with leftquoatation ! rightquoatation following leftquoatation @ rightquoatation at least 5 times") (targetValue (name "?{endwith(concat(<@>,<!>))}")))
(example (utterance "lines with a number and starting in string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,1),startwith(<!>)}")))
(example (utterance "lines with a character before a letter , 3 or more times in them") (targetValue (name "?{<any>,repeatatleast(<let>,3)}")))
(example (utterance "lines with a capital letter following a character") (targetValue (name "?{<any>,<cap>}")))
(example (utterance "lines containing a character and either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a number") (targetValue (name "?{startwith(<any>),or(or(<!>,<@>),<num>)}")))
(example (utterance "items with a small letter , a character , or leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation .") (targetValue (name "?{or(or(<low>,<any>),<!>),<@>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{endwith(<!>),repeatatleast(<cap>,1)}")))
(example (utterance "lines with dog , a number and truck .") (targetValue (name "?{and(<!>,<num>),<@>}")))
(example (utterance "lines containing words with the leftquoatation ! rightquoatation or leftquoatation @ rightquoatation strings , or the leftquoatation # rightquoatation or leftquoatation $ rightquoatation strings") (targetValue (name "?{or(or(<!>,<@>),<#>),<$>}")))
(example (utterance "lines containing a number before either a vowel , a lower-case letter , or leftquoatation ! rightquoatation") (targetValue (name "?{<num>,or(or(<vow>,<low>),<!>)}")))
(example (utterance "items with words containing any letter or leftquoatation ! rightquoatation .") (targetValue (name "?{or(<low>,<!>),<cap>}")))
(example (utterance "lines containing only a capital letter at least once before leftquoatation ! rightquoatation in them") (targetValue (name "?{repeatatleast(<cap>,1),<!>}")))
(example (utterance "lines containing a capital or string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation") (targetValue (name "?{and(or(<cap>,<!>),<@>)}")))
(example (utterance "lines with words and starting in 7 or more characters") (targetValue (name "?{repeatatleast(<any>,7)}")))
(example (utterance "lines with words with a number followed by a lower-case letter in them") (targetValue (name "?{<num>,<low>}")))
(example (utterance "lines with a number before a vowel") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation before number") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines with a lower-case letter and character before letter at least once") (targetValue (name "?{and(<low>,<any>),repeatatleast(<let>,1)}")))
(example (utterance "lines with no lower-case letter before a character") (targetValue (name "?{concat(endwith(<low>),<any>)}")))
(example (utterance "lines with a vowel with a character") (targetValue (name "?{or(<vow>,contain(<any>))}")))
(example (utterance "lines with words with a letter before string leftquoatation ! rightquoatation of leftquoatation @ rightquoatation") (targetValue (name "?{<let>,or(<!>,<@>)}")))
(example (utterance "lower-case letters in lines followed by leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and leftquoatation # rightquoatation") (targetValue (name "?{concat(<low>,<!>),and(<@>,<#>)}")))
(example (utterance "items terminating with a small letter preceding a vowel .") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "the line words should start with a vowel") (targetValue (name "?{<vow>}")))
(example (utterance "lines having a letter before a character and leftquoatation ! rightquoatation") (targetValue (name "?{concat(<let>,<any>),<!>}")))
(example (utterance "lines not containing a vowel , a lower case letter and a capital letter") (targetValue (name "?{and(and(<vow>,<low>),<cap>)}")))
(example (utterance "lines with words containing a number , or a capital , or a character") (targetValue (name "?{or(<num>,<cap>),<any>}")))
(example (utterance "lines do not contain 5 or more capital letters") (targetValue (name "?{repeatatleast(<cap>,5)}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation or begin with a number") (targetValue (name "?{<!>,startwith(<num>)}")))
(example (utterance "lines which are ending with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation , 6 or more times") (targetValue (name "?{repeatatleast(or(<!>,<@>),6)}")))
(example (utterance "lines with a vowel before a lower case or capital letter") (targetValue (name "?{<vow>,or(<low>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{<!>,not(repeatatleast(<vow>,1))}")))
(example (utterance "lines ending in 4 or more of string leftquoatation ! rightquoatation before capital letter") (targetValue (name "?{endwith(<!>),repeatatleast(<cap>,4)}")))
(example (utterance "items with a numeral preceding a letter , at least twice .") (targetValue (name "?{<num>,repeatatleast(<let>,2)}")))
(example (utterance "items with leftquoatation ! rightquoatation and an upper case letter preceding leftquoatation @ rightquoatation .") (targetValue (name "?{and(<!>,<cap>),<@>}")))
(example (utterance "lines with a character , leftquoatation ! rightquoatation , and a vowel with a number after") (targetValue (name "?{and(and(<any>,<!>),<vow>),<num>}")))
(example (utterance "lines containing 7 lower-case letters") (targetValue (name "?{<low>}")))
(example (utterance "lines with a capital letter before a character , letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,or(or(<any>,<let>),<!>)}")))
(example (utterance "lines with a character or string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation , zero or more times") (targetValue (name "?{or(<any>,concat(<!>,<@>))}")))
(example (utterance "lines starting with a capital followed by number , zero or more times") (targetValue (name "?{startwith(concat(<cap>,<num>))}")))
(example (utterance "lines that have a vowel and lowercase letter preceding leftquoatation ! rightquoatation") (targetValue (name "?{and(<vow>,<low>),<!>}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation before string leftquoatation # rightquoatation") (targetValue (name "?{endwith(or(<!>,<@>)),<#>}")))
(example (utterance "lines without a character , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<any>,<!>),<num>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation followed by zero or more times leftquoatation @ rightquoatation") (targetValue (name "?{star(concat(<!>,<@>))}")))
(example (utterance "lines with words , a number once") (targetValue (name "?{<num>}")))
(example (utterance "lines with words that either have a lower-case letter , a number or the word leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<num>),<!>}")))
(example (utterance "lines with a vowel before a character , number , or string leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,or(or(<any>,<num>),<!>)}")))
(example (utterance "lines without a capital letter or any letter") (targetValue (name "?{and(<cap>,<let>)}")))
(example (utterance "lines containing both leftquoatation ! rightquoatation and leftquoatation @ rightquoatation with a character before a number") (targetValue (name "?{<any>,and(and(<num>,<!>),<@>)}")))
(example (utterance "lines not containing a vowel , a lower-case letter , or a letter") (targetValue (name "?{and(and(<vow>,<low>),<let>)}")))
(example (utterance "lines with words with a character or lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<low>),<!>}")))
(example (utterance "lines with words and zero or more of a number before capital letter") (targetValue (name "?{concat(<num>,<cap>)}")))
(example (utterance "lines with a letter , 2 or more times before leftquoatation ! rightquoatation in it") (targetValue (name "?{repeatatleast(<let>,2),<!>}")))
(example (utterance "lines that have words containing a at least 1 letter or character") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with words with a character , string leftquoatation ! rightquoatation , or lower-case letter") (targetValue (name "?{or(<any>,<!>),<low>}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines with words containing a vowel and a number") (targetValue (name "?{<vow>,<num>}")))
(example (utterance "lines with words ending with number followed by string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "line where 1 of 3 things comes after the string 'dog':  leftquoatation ! rightquoatation , a capital letter , or leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<!>,<cap>),<@>)}")))
(example (utterance "lines do not start with 4 or more of leftquoatation ! rightquoatation") (targetValue (name "?{startwith(repeatatleast(<!>,4))}")))
(example (utterance "lines that have words containing either a vowel , lower-case letter , or a letter in upper case") (targetValue (name "?{or(<low>,<vow>),<cap>}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation or containing only a character in them") (targetValue (name "?{or(<!>,<any>)}")))
(example (utterance "lines with words with either a letter , lower-case letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<low>),<!>}")))
(example (utterance "lines that do not contain a number , a character , or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<any>),<!>)}")))
(example (utterance "lines with a lower-case letter before a vowel") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines ending with vowel or starting with character") (targetValue (name "?{endwith(<vow>),startwith(<any>)}")))
(example (utterance "lines with a number before a capitalor string leftquoatation ! rightquoatation , 3 or more times") (targetValue (name "?{<num>,repeatatleast(or(<cap>,<!>),3)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation before a letter") (targetValue (name "?{startwith(<!>),not(<let>)}")))
(example (utterance "lines that contain more than a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines with 4 or more of a character and number") (targetValue (name "?{contain(and(<any>,<num>))}")))
(example (utterance "items with a numeral preceding a character .") (targetValue (name "?{concat(<num>,<any>)}")))
(example (utterance "lower case letters before ending with vowels in the lines") (targetValue (name "?{<low>,endwith(<vow>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a letter followed by the sting leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<let>),<#>}")))
(example (utterance "lines containing at least 1 of leftquoatation ! rightquoatation or a letter") (targetValue (name "?{repeatatleast(<!>,1),contain(<let>)}")))
(example (utterance "lines starting with a vowel before number") (targetValue (name "?{startwith(<vow>),<num>}")))
(example (utterance "lines that have words containing either a character , a vowel , or leftquoatation ! rightquoatation") (targetValue (name "?{or(<!>,<any>),<vow>}")))
(example (utterance "lines ending with a lower-case letter at least 4 times") (targetValue (name "?{repeatatleast(<low>,4)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a number") (targetValue (name "?{or(<!>,<@>),<num>}")))
(example (utterance "lines with words and zero or more of leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , lower-case letter , and letter before a number") (targetValue (name "?{and(and(<!>,<low>),<let>),<num>}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation before words with string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,star(<@>)}")))
(example (utterance "lines containing at least 1 occurrence of a capital or lower-case letter") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines that have only  leftquoatation ! rightquoatation then a vowel") (targetValue (name "?{contain(<!>),<vow>}")))
(example (utterance "lines with words starting with letter , 7 or more times") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "lines containing 6 or more vowels before including a capital letter") (targetValue (name "?{repeatatleast(<vow>,6),<cap>}")))
(example (utterance "items without leftquoatation ! rightquoatation , numeral , or capital letter .") (targetValue (name "?{and(and(<!>,<num>),<cap>)}")))
(example (utterance "lines that begin with words that beging with a lowercase letter") (targetValue (name "?{<low>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or number") (targetValue (name "?{and(contain(<!>),<num>)}")))
(example (utterance "character-less lines") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines having capital letter , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{and(and(<cap>,<!>),<@>),<#>}")))
(example (utterance "lines containing a vowel preceded by a number") (targetValue (name "?{<num>,<vow>}")))
(example (utterance "lines containing 4 or more vowels") (targetValue (name "?{<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation as well as leftquoatation @ rightquoatation , 4 times or more") (targetValue (name "?{contain(and(<!>,<@>))}")))
(example (utterance "lines with words with vowel before letter , zero or more times") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines containing a lower-case letter after a letter") (targetValue (name "?{<let>,<low>}")))
(example (utterance "items with leftquoatation ! rightquoatation , twice or more preceding leftquoatation @ rightquoatation .") (targetValue (name "?{repeatatleast(<!>,2),<@>}")))
(example (utterance "items with a vowel preceding a small letter preceding leftquoatation ! rightquoatation .") (targetValue (name "?{<vow>,concat(<low>,<!>)}")))
(example (utterance "items with a letter preceding a small letter preceding an upper case letter .") (targetValue (name "?{concat(<let>,<low>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and beginning with a letter") (targetValue (name "?{contain(<!>),startwith(<let>)}")))
(example (utterance "lines with a lower-case letter before words starting with a capital letter") (targetValue (name "?{<low>,startwith(<cap>)}")))
(example (utterance "lines with a lower case letter , leftquoatation ! rightquoatation and no capital letters") (targetValue (name "?{not(<cap>),and(<!>,<low>)}")))
(example (utterance "lines with words with either a letter , a capital , or vowel") (targetValue (name "?{or(<let>,<cap>),<vow>}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a lower-case letter in them") (targetValue (name "?{<cap>,and(and(<!>,<@>),<low>)}")))
(example (utterance "lines with a vowel and lower-case letter") (targetValue (name "?{and(and(<let>,<vow>),<low>)}")))
(example (utterance "lines that have a capital letter at the end") (targetValue (name "?{<cap>}")))
(example (utterance "lines with words not having a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{not(<low>),<!>}")))
(example (utterance "lines without leftquoatation ! rightquoatation and no characters before the string 'dog") (targetValue (name "?{and(concat(<any>,<!>),<!>)}")))
(example (utterance "at least6times have lines with a vowel followed by a number") (targetValue (name "?{concat(<vow>,<num>)}")))
(example (utterance "lines with a vowel before a lower-case and upper-case letter") (targetValue (name "?{concat(<vow>,<low>),and(and(<any>,<cap>),<let>)}")))
(example (utterance "multiple lines that contain a lower-case letter followed by a character which is not a number") (targetValue (name "?{<low>,not(<num>)}")))
(example (utterance "lines with a vowel , lower-case letter , or string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<low>),<!>),<@>}")))
(example (utterance "lines ending with lower-case letter at least once before letter") (targetValue (name "?{repeatatleast(endwith(<low>),1),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a lower-case letter") (targetValue (name "?{and(and(<!>,<@>),<low>)}")))
(example (utterance "lines with words starting with a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<vow>),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation then 2 or more letters") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines starting with a character and ending with a number") (targetValue (name "?{startwith(<any>),endwith(<num>)}")))
(example (utterance "lines with vowel and string leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{and(<vow>,<!>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or letter before capital letter") (targetValue (name "?{or(<!>,<let>),<cap>}")))
(example (utterance "lines ending ending with a capital or lower-case letter") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines that do not have a number followed by a capital letter at least zero times") (targetValue (name "?{star(concat(<num>,<cap>))}")))
(example (utterance "lines not having a lower-case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<low>,<!>),<@>)}")))
(example (utterance "lines with a vowel or string leftquoatation ! rightquoatation before a number and string leftquoatation @ rightquoatation and leftquoatation # rightquoatation") (targetValue (name "?{or(<vow>,<!>),and(and(<num>,<@>),<#>)}")))
(example (utterance "lines with a lower-case letter , number , or string leftquoatation ! rightquoatation before a character") (targetValue (name "?{and(and(<low>,<num>),<!>),<any>}")))
(example (utterance "lines with a capital letter before a character , string leftquoatation ! rightquoatation , or letter") (targetValue (name "?{<cap>,or(or(<any>,<!>),<let>)}")))
(example (utterance "lines with the string truck after leftquoatation ! rightquoatation or a letter") (targetValue (name "?{or(<!>,<let>),<@>}")))
(example (utterance "items with leftquoatation ! rightquoatation and letter preceding a vowel .") (targetValue (name "?{and(<!>,<let>),<vow>}")))
(example (utterance "lines with words leftquoatation ! rightquoatation and a capital letter , zero or more times") (targetValue (name "?{and(<!>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a letter following leftquoatation @ rightquoatation at least 3 times") (targetValue (name "?{concat(<@>,<!>),repeatatleast(<let>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter , then leftquoatation @ rightquoatation and a number") (targetValue (name "?{<!>,and(and(<cap>,<@>),<num>)}")))
(example (utterance "lines that must have a vowel , letter , and the word leftquoatation ! rightquoatation followed by a character") (targetValue (name "?{and(and(<vow>,<let>),<!>),<any>}")))
(example (utterance "lines with vowel before string leftquoatation ! rightquoatation or character , 4 or more times") (targetValue (name "?{or(concat(<vow>,<!>),<any>)}")))
(example (utterance "items with a numeral preceding a character at least6times .") (targetValue (name "?{<num>,repeatatleast(<any>,6)}")))
(example (utterance "lines with a letter or character or with string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(or(<cap>,<let>),<any>),<!>)}")))
(example (utterance "lines that contain 1 of the following: a character , a letter , or a lower-case letter") (targetValue (name "?{or(or(<any>,<let>),<low>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation followed by a capital letter and a letter") (targetValue (name "?{<!>,and(<cap>,<let>)}")))
(example (utterance "a vowel must come before a lower case letter and must have string leftquoatation ! rightquoatation") (targetValue (name "?{or(<!>,<low>),<vow>}")))
(example (utterance "lines ending with words and a lower-case letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines containing 7 or more vowels or numbers") (targetValue (name "?{repeatatleast(or(<vow>,<num>),7)}")))
(example (utterance "lines with a lower-case letter , a vowel , and a character , at least 3 times") (targetValue (name "?{repeatatleast(<low>,3),and(and(<num>,<vow>),<any>)}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation occuring before leftquoatation # rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<@>),<#>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and lower-case letter or vowel") (targetValue (name "?{or(and(<!>,<low>),<vow>)}")))
(example (utterance "lines with a capital letter before containing a number , 4 or more times in them") (targetValue (name "?{<cap>,contain(repeatatleast(<num>,4))}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a small letter or letter .") (targetValue (name "?{<!>,or(<let>,<low>)}")))
(example (utterance "lines that do not have a lower-case letter or a vowel at least zero times") (targetValue (name "?{or(<low>,star(<vow>))}")))
(example (utterance "lines with a letter , string leftquoatation ! rightquoatation , or character before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<!>),<any>),<@>}")))
(example (utterance "lines that have a number and a capital letter and a letter") (targetValue (name "?{and(and(<num>,<cap>),<let>)}")))
(example (utterance "lines with a lower-case letter , vowel , and string leftquoatation ! rightquoatation before a number") (targetValue (name "?{and(and(<low>,<vow>),<!>),<num>}")))
(example (utterance "lines that begin with a character which precedes a number") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines with a capital letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines containing a character followed by a number , 6 or more times") (targetValue (name "?{<any>,repeatatleast(<num>,6)}")))
(example (utterance "items without a numeral , or letter , or vowel .") (targetValue (name "?{and(and(<num>,<let>),<vow>)}")))
(example (utterance "lines with a character , lower-case letter , and vowel before a capital letter") (targetValue (name "?{and(and(<any>,<low>),<vow>),<cap>}")))
(example (utterance "lines without leftquoatation ! rightquoatation , but with leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),contain(<@>)}")))
(example (utterance "items with either leftquoatation ! rightquoatation or any letter preceding a character .") (targetValue (name "?{or(or(<low>,<!>),<let>),<any>}")))
(example (utterance "lines starting in leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{startwith(<!>),contain(<vow>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation followed by letter or lower-case letter") (targetValue (name "?{or(concat(<!>,<let>),<low>)}")))
(example (utterance "lines containing a number followed by leftquoatation ! rightquoatation at least 1 time .") (targetValue (name "?{<num>,repeatatleast(contain(<!>),1)}")))
(example (utterance "lines not having string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation , 6 or more times") (targetValue (name "?{or(not(<!>),<@>)}")))
(example (utterance "lines having number , letter , and character before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<let>),<any>),<!>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a vowel preceding a small letter .") (targetValue (name "?{<!>,concat(<vow>,<low>)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a vowel preceding a numeral .") (targetValue (name "?{contain(<!>),concat(<vow>,<num>)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation followed by letter") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines with a vowel before a lower-case letter or leftquoatation ! rightquoatation , 6 or more times in them") (targetValue (name "?{concat(<vow>,<low>),repeatatleast(<!>,6)}")))
(example (utterance "lines that do not begin with either a character or a capital letter") (targetValue (name "?{and(startwith(<any>),<cap>)}")))
(example (utterance "lines ending with not a capital or vowel") (targetValue (name "?{or(not(<cap>),<vow>)}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{contain(<low>),and(<!>,<@>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceded by a number or a vowel") (targetValue (name "?{or(<num>,<vow>),<!>}")))
(example (utterance "items with words containg any letter .") (targetValue (name "?{or(<vow>,<let>),<cap>}")))
(example (utterance "lines which have a capital letter before ending with a lower-case letter") (targetValue (name "?{<cap>,endwith(<low>)}")))
(example (utterance "lines that have the word leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a lower case letter followed by leftquoatation # rightquoatation") (targetValue (name "?{or(or(<!>,<@>),<#>),<low>}")))
(example (utterance "lines with words containing leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),<@>}")))
(example (utterance "lines containing either letter , lower-case letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<let>,<low>),<!>)}")))
(example (utterance "lines whiich are containing a lower-case letter and a vowel") (targetValue (name "?{and(<low>,<vow>)}")))
(example (utterance "lines with  the string 'dog'and a word that has a lower-case letter at least 3 times") (targetValue (name "?{repeatatleast(<low>,3),<!>}")))
(example (utterance "items with a letter preceding leftquoatation ! rightquoatation at least 4 times .") (targetValue (name "?{concat(<let>,<!>)}")))
(example (utterance "lines containing only leftquoatation ! rightquoatation") (targetValue (name "?{and(<let>,<!>)}")))
(example (utterance "lines with 4 or more capital letters followed by leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<cap>,4),<!>}")))
(example (utterance "lines with a lower-case letter , number , and vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<num>),<vow>),<!>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation before leftquoatation @ rightquoatation 3 times or more") (targetValue (name "?{<!>,repeatatleast(<@>,3)}")))
(example (utterance "lines having either a lower-case letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation or a number , occurring 3 or more times") (targetValue (name "?{or(or(or(<low>,<!>),<@>),<num>)}")))
(example (utterance "lines containing capital letter") (targetValue (name "?{or(<cap>,<let>)}")))
(example (utterance "items with a numeral , a vowel , or an upper case letter .") (targetValue (name "?{and(and(<vow>,<num>),<cap>)}")))
(example (utterance "lines with less than 2 of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(or(<!>,<@>),2)}")))
(example (utterance "lines without a vowel before a capital letter") (targetValue (name "?{concat(<vow>,star(<cap>))}")))
(example (utterance "lines with words with a capital , zero or more times or string leftquoatation ! rightquoatation") (targetValue (name "?{star(<cap>),<!>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a character") (targetValue (name "?{or(<!>,<@>),<any>}")))
(example (utterance "lines which do not contain the leftquoatation ! rightquoatation string and the leftquoatation @ rightquoatation string either zero times or greater") (targetValue (name "?{not(<!>),star(<@>)}")))
(example (utterance "lines ending in 2 or more of leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{repeatatleast(concat(<!>,<low>),2)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation or an uppercase letter") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines having words with leftquoatation ! rightquoatation , a number and a letter") (targetValue (name "?{and(<!>,<let>),<num>}")))
(example (utterance "lines with a number before string leftquoatation ! rightquoatation then letter") (targetValue (name "?{concat(<num>,<!>),<let>}")))
(example (utterance "number appears 7 or more times before a vowel in the lines") (targetValue (name "?{repeatatleast(<num>,7),<vow>}")))
(example (utterance "lines having leftquoatation ! rightquoatation or characters at least 4 times") (targetValue (name "?{repeatatleast(or(<!>,<any>),4)}")))
(example (utterance "a letter appears before a number in the lines") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation following a character") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines containing the word leftquoatation ! rightquoatation with 1 letter capitalized") (targetValue (name "?{and(and(<vow>,<!>),<cap>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a lower-case letter and string leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),and(<low>,<@>)}")))
(example (utterance "lines ending with a number at least 3 times") (targetValue (name "?{repeatatleast(<num>,3)}")))
(example (utterance "lines with dog or a number") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , not leftquoatation @ rightquoatation , and not leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<#>)}")))
(example (utterance "lines with 6 or more letters or characters") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "line of code that includes leftquoatation ! rightquoatation then leftquoatation @ rightquoatation repeated 4 times after") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{or(<!>,<@>),<vow>}")))
(example (utterance "lines with either dog , a number , or a letter .") (targetValue (name "?{or(or(<!>,<num>),<let>)}")))
(example (utterance "lines starting with a letter or a vowel") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines with a vowel number least once before") (targetValue (name "?{repeatatleast(<num>,1),<vow>}")))
(example (utterance "lines without leftquoatation ! rightquoatation , a letter , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<let>),<@>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a number") (targetValue (name "?{or(and(<!>,<@>),<num>)}")))
(example (utterance "lines containing a vowel , a capital letter , and a lower-case letter") (targetValue (name "?{and(<vow>,<cap>),<low>}")))
(example (utterance "lines with a capital or leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{or(<cap>,<!>),not(<vow>)}")))
(example (utterance "lines ending with a capital letter and string leftquoatation ! rightquoatation") (targetValue (name "?{contain(and(<cap>,<!>))}")))
(example (utterance "lines with a vowel before leftquoatation ! rightquoatation and ending in a lower-case letter") (targetValue (name "?{concat(<vow>,<!>),endwith(<low>)}")))
(example (utterance "items with a numeral at least 1 time preceding a small letter .") (targetValue (name "?{repeatatleast(<num>,1),<low>}")))
(example (utterance "items with a letter or leftquoatation ! rightquoatation at least 1 time .") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines without a letter before a number") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines where 5 or more times a character is preceded by leftquoatation ! rightquoatation") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation at least once before a letter") (targetValue (name "?{not(repeatatleast(<!>,1)),<let>}")))
(example (utterance "lines containing a number or a capital letter , 7 times or more") (targetValue (name "?{or(<num>,<cap>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and vowel before a character") (targetValue (name "?{and(and(<!>,<@>),<vow>),<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or 3 or more characters") (targetValue (name "?{or(contain(<!>),<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation before a character .") (targetValue (name "?{concat(or(or(<!>,<@>),<#>),<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{endwith(repeatatleast(<!>,2))}")))
(example (utterance "lines with a letter and starting in a vowel") (targetValue (name "?{repeatatleast(<let>,1),startwith(<vow>)}")))
(example (utterance "lines with a lower-case letter , capital letter , or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<low>,<cap>),<!>),<@>}")))
(example (utterance "lined ending with words with a capital letter with a number after") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines that do not contain a capital letter or a character followed by a letter") (targetValue (name "?{concat(and(<cap>,<any>),<let>)}")))
(example (utterance "items without leftquoatation ! rightquoatation , a character , or a small letter .") (targetValue (name "?{or(or(<!>,<low>),<any>)}")))
(example (utterance "lines that do not start with a character or leftquoatation ! rightquoatation") (targetValue (name "?{and(startwith(<!>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a letter at least 3 times") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , vowel , or an upper case letter preceding a small letter .") (targetValue (name "?{or(or(<vow>,<!>),<cap>),<low>}")))
(example (utterance "lines containing words with a capital before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with a lower-case letter or a vowel and leftquoatation ! rightquoatation") (targetValue (name "?{contain(<low>),and(<vow>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(concat(<!>,<@>),1)}")))
(example (utterance "items with an upper case letter or numeral preceding a small letter .") (targetValue (name "?{or(<num>,<cap>),<low>}")))
(example (utterance "lines with words and capital letter or character") (targetValue (name "?{or(<cap>,<let>),<any>}")))
(example (utterance "items terminating with a small letter precedig leftquoatation ! rightquoatation .") (targetValue (name "?{endwith(<low>),<!>}")))
(example (utterance "lines with a letter , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(or(<let>,<!>),<any>)}")))
(example (utterance "lines which have words with either a vowel , leftquoatation ! rightquoatation , or a letter") (targetValue (name "?{or(<vow>,<!>),<let>}")))
(example (utterance "lines that must have a vowel preceding a letter , character , or the word leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,or(or(<let>,<!>),<any>)}")))
(example (utterance "lines with a letter preceded by leftquoatation ! rightquoatation 6 or more times") (targetValue (name "?{repeatatleast(<!>,6),<let>}")))
(example (utterance "lines with a lower-case letter after leftquoatation ! rightquoatation , a letter , and a capital letter") (targetValue (name "?{and(and(<!>,<let>),<cap>),<low>}")))
(example (utterance "lines with zero or more of a number before a lower-case letter and string leftquoatation ! rightquoatation and 'truck") (targetValue (name "?{star(<num>),and(and(<low>,<!>),<@>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a number followed by a vowel") (targetValue (name "?{or(<!>,<vow>),<num>}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(startwith(<!>),<vow>)}")))
(example (utterance "lines with 4 or more capital letters or characters") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , vowel , and capital letter") (targetValue (name "?{and(and(<!>,<vow>),<cap>)}")))
(example (utterance "lines with a letter followed by a character before not a capital letter in them") (targetValue (name "?{concat(<let>,<any>),not(<cap>)}")))
(example (utterance "lines containing a capital letter before 5 or more vowels") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines with numbers before a lower-case letter , a vowel , or a character") (targetValue (name "?{<num>,and(and(<low>,<vow>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and a vowel at least 3 times") (targetValue (name "?{concat(<!>,<@>),repeatatleast(<vow>,3)}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation or string leftquoatation @ rightquoatation , 5 or more times") (targetValue (name "?{repeatatleast(or(<!>,<@>),5)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation and then a vowel") (targetValue (name "?{endwith(<!>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and vowel or string leftquoatation @ rightquoatation") (targetValue (name "?{contain(<!>),or(<vow>,<@>)}")))
(example (utterance "lines with character before vowel and containing a lower-case letter") (targetValue (name "?{concat(<any>,<vow>),contain(<low>)}")))
(example (utterance "lines with 6 or more of a capital letter before a vowel") (targetValue (name "?{<cap>,repeatatleast(<vow>,6)}")))
(example (utterance "lines with words with a number or letter , zero or more times") (targetValue (name "?{or(<num>,<let>)}")))
(example (utterance "lines that end with a lower-case letter followed by a capital letter") (targetValue (name "?{endwith(<low>),<cap>}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation or lower-case letter") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines with 6 or more times a letter  and leftquoatation ! rightquoatation") (targetValue (name "?{contain(and(<let>,<!>))}")))
(example (utterance "lines that begin with a lower-case letter or leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation .") (targetValue (name "?{startwith(<low>),and(and(<!>,<@>),<#>)}")))
(example (utterance "lines with a character before number and string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{concat(<any>,<num>),concat(<!>,<@>)}")))
(example (utterance "lines with words with a vowel 5 times") (targetValue (name "?{repeatatleast(<vow>,5)}")))
(example (utterance "lines with leftquoatation ! rightquoatation 6 times or more then a character") (targetValue (name "?{and(repeatatleast(<!>,6),<any>)}")))
(example (utterance "at least once lines without a vowel or a letter") (targetValue (name "?{repeatatleast(or(<vow>,<let>),1)}")))
(example (utterance "lines with words and zero or more of leftquoatation ! rightquoatation or letter") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines without a character , a letter , or a lower-case letter") (targetValue (name "?{or(or(<any>,<let>),<low>)}")))
(example (utterance "lines without a number followed by 5 or more times a vowel") (targetValue (name "?{repeatatleast(concat(<num>,<vow>),5)}")))
(example (utterance "lines which contain a vowel and a letter") (targetValue (name "?{and(<vow>,<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a capital letter") (targetValue (name "?{and(<!>,<cap>)}")))
(example (utterance "lines with a letter then a number") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a character") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines containing 6 or more lower-case letters , followed by a letter") (targetValue (name "?{repeatatleast(<low>,6),<let>}")))
(example (utterance "lines with a vowel before a letter , 4 or more times in it") (targetValue (name "?{<vow>,repeatatleast(<let>,4)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{contain(concat(<!>,<vow>))}")))
(example (utterance "lines starting in words with a vowel or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines with lower-case letters before 7 of leftquoatation ! rightquoatation") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "items with words containing leftquoatation ! rightquoatation and a vowel preceding a numeral .") (targetValue (name "?{<!>,concat(<vow>,<num>)}")))
(example (utterance "items without a numeral , leftquoatation ! rightquoatation , or a letter .") (targetValue (name "?{or(or(<let>,<!>),<num>)}")))
(example (utterance "lines containing a letter , leftquoatation ! rightquoatation , and 0 or more characters") (targetValue (name "?{concat(<let>,<!>),star(<any>)}")))
(example (utterance "lines with a number before a vowel or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<num>,or(or(<vow>,<!>),<@>)}")))
(example (utterance "lines with a capital letter before leftquoatation ! rightquoatation , a letter , and a vowel") (targetValue (name "?{<cap>,and(and(<!>,<let>),<vow>)}")))
(example (utterance "lines containing a lower case letter after a vowel and a character before a number") (targetValue (name "?{concat(<vow>,<low>),concat(<num>,<any>)}")))
(example (utterance "lines with 2 or more of a letter before capital letter") (targetValue (name "?{<let>,repeatatleast(<cap>,2)}")))
(example (utterance "lines that contain a letter , a character , or leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{or(or(<let>,<any>),<!>),<num>}")))
(example (utterance "lines that do not contain a character or a letter") (targetValue (name "?{or(or(<any>,<vow>),<let>)}")))
(example (utterance "lines with a capital letter before number or vowel") (targetValue (name "?{or(concat(<cap>,<num>),<vow>)}")))
(example (utterance "a vowel and leftquoatation ! rightquoatation appears before a letter in the lines") (targetValue (name "?{and(<vow>,<!>),<let>}")))
(example (utterance "lines with words , a letter , and leftquoatation ! rightquoatation or a number") (targetValue (name "?{and(<let>,<!>),<num>}")))
(example (utterance "lines with a number followed by leftquoatation ! rightquoatation or a character , 3 or more times in them") (targetValue (name "?{concat(<num>,<!>),repeatatleast(<any>,3)}")))
(example (utterance "lines with words with capital letter or character before string leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<any>),<!>}")))
(example (utterance "lines with words ending in number or capital letter") (targetValue (name "?{endwith(<num>),<cap>}")))
(example (utterance "lines ending with a character or a lower-case letter directly preceded by a number") (targetValue (name "?{or(concat(<num>,<any>),<low>)}")))
(example (utterance "lines containing a capital letter and ending with a lower-case letter") (targetValue (name "?{<cap>,endwith(<low>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation , letter , and string leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<let>),<@>}")))
(example (utterance "lines without a lower-case letter before a number") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "lines with 6 or more lower-case letters followed by a number") (targetValue (name "?{repeatatleast(<low>,6),<num>}")))
(example (utterance "lines with a character after a letter , a vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<vow>),<!>),<any>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding a vowel preceding a character .") (targetValue (name "?{concat(<!>,<vow>),<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation after a capital letter at least zero times") (targetValue (name "?{star(<cap>),<!>}")))
(example (utterance "lines without a capital letter or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<cap>,<!>),<@>)}")))
(example (utterance "lines culminating without a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(not(<low>),<!>)}")))
(example (utterance "lines that have at least 4 words and 4 letters") (targetValue (name "?{<let>}")))
(example (utterance "lines with a letter and character before vowel") (targetValue (name "?{and(<let>,<any>),<vow>}")))
(example (utterance "lines with words with a capital letter , string leftquoatation ! rightquoatation , and number") (targetValue (name "?{and(<cap>,<!>),<num>}")))
(example (utterance "lines that do not contain a letter , a character , or a lower-case letter") (targetValue (name "?{or(or(<low>,<let>),<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character then a number or vowel") (targetValue (name "?{concat(<!>,<any>),or(<num>,<vow>)}")))
(example (utterance "lines having either lower-case letter , string leftquoatation ! rightquoatation , or number before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<low>,<!>),<num>),<@>}")))
(example (utterance "lines containing either string leftquoatation ! rightquoatation , a lower-case letter , or a letter or a capital") (targetValue (name "?{or(or(or(<!>,<low>),<let>),<cap>)}")))
(example (utterance "lines staring in words with string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a capital letter") (targetValue (name "?{or(or(<!>,<@>),<cap>)}")))
(example (utterance "lines without a letter or leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<!>),<cap>)}")))
(example (utterance "lines with words") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines with a character or that ends with a number") (targetValue (name "?{or(<any>,endwith(<num>))}")))
(example (utterance "lines with letter followed by capital letter , zero or more times") (targetValue (name "?{concat(<let>,<cap>)}")))
(example (utterance "lines with a number , capital letter , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<cap>),<!>)}")))
(example (utterance "lines in which leftquoatation ! rightquoatation is followed by a letter that's lower-case , at least6times") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines containing words with a lower-case letter , a number , and a character") (targetValue (name "?{and(<low>,<num>),<any>}")))
(example (utterance "lines ending in 7 or more lower-case letters or with a capital letter") (targetValue (name "?{or(repeatatleast(<low>,7),<cap>)}")))
(example (utterance "lines containing words and without a number") (targetValue (name "?{<num>}")))
(example (utterance "lines with words starting with string leftquoatation ! rightquoatation or a number") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines that start with6or more letters") (targetValue (name "?{repeatatleast(<let>,6)}")))
(example (utterance "lines with a number , letter , and string leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{and(and(<num>,<let>),<!>),<vow>}")))
(example (utterance "lines with 3 or more of a lower-case letter or number") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines starting with words containing only letters") (targetValue (name "?{<let>}")))
(example (utterance "lines ending with containing leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines without words with string leftquoatation ! rightquoatation without a letter") (targetValue (name "?{and(<!>,<let>)}")))
(example (utterance ": lines ending with words with vowel or letter") (targetValue (name "?{or(<vow>,<let>)}")))
(example (utterance "lines with 4 or more of a lower-case or capital letter") (targetValue (name "?{or(<low>,<cap>)}")))
(example (utterance "lines with a number , zero or more times or not a character in them") (targetValue (name "?{star(<num>),not(<any>)}")))
(example (utterance "lines with a capital letter before string leftquoatation ! rightquoatation or lower-case letter and character") (targetValue (name "?{concat(<cap>,<!>),and(<low>,<any>)}")))
(example (utterance "lines with a character or a vowel before leftquoatation ! rightquoatation in them") (targetValue (name "?{or(<any>,<vow>),<!>}")))
(example (utterance "4 or more lines with a capital letter or a number in them") (targetValue (name "?{or(<num>,<cap>)}")))
(example (utterance "lines with 4 or more of a character or number") (targetValue (name "?{or(<any>,<num>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation with lower-case letter before string leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),concat(<low>,<@>)}")))
(example (utterance "lines with characters before a lower-case letter then by leftquoatation ! rightquoatation at minimum") (targetValue (name "?{<any>,repeatatleast(concat(<low>,<!>),1)}")))
(example (utterance "lines that end with a vowel or a lower-case letter , at least 6 times") (targetValue (name "?{repeatatleast(or(<vow>,<low>),6)}")))
(example (utterance "lines without words with a number before vowel") (targetValue (name "?{concat(<num>,<vow>)}")))
(example (utterance "lines not having lower-case letter , not a letter , and not leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<low>,<let>),<!>)}")))
(example (utterance "lines that don't have a number that is found before a letter") (targetValue (name "?{not(<num>),<let>}")))
(example (utterance "lines with a vowel before leftquoatation ! rightquoatation and containing a lower-case letter in them") (targetValue (name "?{concat(<vow>,<!>),contain(<low>)}")))
(example (utterance "lines with only zero or more letters") (targetValue (name "?{or(<cap>,<let>)}")))
(example (utterance "lines with a character , a letter , and a lowe-case letter , in order") (targetValue (name "?{concat(<any>,<let>),<low>}")))
(example (utterance "lines with a capital letter or 7 strings of leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , a number , and vowel") (targetValue (name "?{and(and(<!>,<num>),<vow>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation  at least 3 times and use leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<@>,3),<!>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before words and a lower-case letter") (targetValue (name "?{<!>,star(<low>)}")))
(example (utterance "lines having a capital letter and a number") (targetValue (name "?{and(<cap>,<num>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a lower-case letter .") (targetValue (name "?{star(<!>),<low>}")))
(example (utterance "lines with 6 or more of a character and letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(and(<any>,<let>),<!>)}")))
(example (utterance "lines containing a lower-case letter after a number 7 or more times") (targetValue (name "?{repeatatleast(<num>,7),<low>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation and string leftquoatation # rightquoatation before a character") (targetValue (name "?{concat(<!>,<@>),concat(<#>,<any>)}")))
(example (utterance "lines with lower-case letter , 2 or more times or vowel followed by number") (targetValue (name "?{repeatatleast(<low>,2),concat(<vow>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation at least 5 times before a character followed by a letter") (targetValue (name "?{repeatatleast(<!>,5),concat(<any>,<let>)}")))
(example (utterance "lines without a character , a number and a vowel") (targetValue (name "?{and(and(<any>,<num>),<vow>)}")))
(example (utterance "lines without words with zero or more vowels") (targetValue (name "?{star(<vow>)}")))
(example (utterance "lines ending with a vowel followed by a letter in them") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines with a vowel , 2 times") (targetValue (name "?{<vow>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation , a capital letter , and a vowel") (targetValue (name "?{and(and(<cap>,<vow>),<!>)}")))
(example (utterance "lines containing the word leftquoatation ! rightquoatation , a letter , and the word leftquoatation @ rightquoatation with a vowel before") (targetValue (name "?{and(and(<!>,<let>),<vow>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter then vowel") (targetValue (name "?{concat(<!>,<cap>),<vow>}")))
(example (utterance "lines containing a character before a letter") (targetValue (name "?{contain(<any>),not(<let>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a letter , and a vowel after a character") (targetValue (name "?{<any>,and(and(<!>,<let>),<vow>)}")))
(example (utterance "lines with words with a capital letter or character") (targetValue (name "?{or(<cap>,<any>)}")))
(example (utterance "lines containing a number , a lower-case letter , and a letter") (targetValue (name "?{and(<num>,<low>),<let>}")))
(example (utterance "lines with a lower-case or capital letter or a character before a number") (targetValue (name "?{or(or(<low>,<cap>),<any>),<num>}")))
(example (utterance "lines with words , leftquoatation ! rightquoatation , and a letter") (targetValue (name "?{and(<!>,<low>),<let>}")))
(example (utterance "lines with a capital letter and a lower-case letter following it") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "lines containing words that have a capital letter preceding a letter") (targetValue (name "?{<cap>,<let>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation before a capital letter and not leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<cap>),not(<@>)}")))
(example (utterance "lines containing capital letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<!>)}")))
(example (utterance "lines with 6 or more lower-case letters or leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines not having character , a letter , or number") (targetValue (name "?{or(or(<any>,<let>),<num>)}")))
(example (utterance "lines not ending with string leftquoatation ! rightquoatation and not a number") (targetValue (name "?{and(endwith(<!>),<num>)}")))
(example (utterance "lines containing ending with a capital letter at least once in them") (targetValue (name "?{repeatatleast(endwith(<cap>),1)}")))
(example (utterance "items with words containing leftquoatation ! rightquoatation .") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with words without zero or more characters") (targetValue (name "?{not(<any>)}")))
(example (utterance "lines that don't contain leftquoatation ! rightquoatation , a character , or a number") (targetValue (name "?{and(and(<!>,<any>),<num>)}")))
(example (utterance "lines with a vowel leftquoatation ! rightquoatation at least once .") (targetValue (name "?{<vow>,repeatatleast(<!>,1)}")))
(example (utterance "lines containing a capital letter before a lower-case letter") (targetValue (name "?{concat(<cap>,<low>)}")))
(example (utterance "items with an upper case letter and a vowel at least5times .") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "lines with words with  lower-case letter or either string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , or capital letter") (targetValue (name "?{<low>,or(or(<!>,<@>),<cap>)}")))
(example (utterance "lines with a 2 or more lower case letters") (targetValue (name "?{<low>}")))
(example (utterance "lines with words with the consecutive letters leftquoatation ! rightquoatation before a letter or the consecutive letters leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<let>),<@>}")))
(example (utterance "lines with 5 or more number with leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,5),contain(<!>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a letter , and a character preceding leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<let>,<!>),<any>),<@>}")))
(example (utterance "lines containing character , 7 or more times") (targetValue (name "?{contain(<any>)}")))
(example (utterance "lines containing capital letter or lower-case letter , zero or more times") (targetValue (name "?{star(or(<cap>,<low>))}")))
(example (utterance "lines containing a number at least once") (targetValue (name "?{repeatatleast(contain(<num>),1)}")))
(example (utterance "lines with a character ending with leftquoatation ! rightquoatation") (targetValue (name "?{<any>,endwith(<!>)}")))
(example (utterance "lines with a vowel before a number followed by a lower-case letter") (targetValue (name "?{concat(<num>,<low>),<vow>}")))
(example (utterance "lines with a lower case , then a number and vowel") (targetValue (name "?{<low>,and(<num>,<vow>)}")))
(example (utterance "items with a letter preceding a vowel , an upper case letter , and leftquoatation ! rightquoatation .") (targetValue (name "?{<let>,and(and(<vow>,<!>),<cap>)}")))
(example (utterance "lines with words and only leftquoatation ! rightquoatation") (targetValue (name "?{<!>}")))
(example (utterance "items with leftquoatation ! rightquoatation or a small letter preceding an upper case letter .") (targetValue (name "?{or(<!>,<low>),<cap>}")))
(example (utterance "lines with capital letter or string leftquoatation ! rightquoatation before vowel") (targetValue (name "?{or(<cap>,<!>),<vow>}")))
(example (utterance "lines containing a lower case-letter or character") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines with a vowel followed by string leftquoatation ! rightquoatation before a letter") (targetValue (name "?{concat(<vow>,<!>),<let>}")))
(example (utterance "lines with number followed by lower-case letter , 5 or more times") (targetValue (name "?{concat(<num>,<low>)}")))
(example (utterance "lines with a capital letter followed by a character and a letter") (targetValue (name "?{<cap>,and(<any>,<let>)}")))
(example (utterance "lines with a lower-case letter , letter , or number before string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<let>),<num>),<!>}")))
(example (utterance "lines containing a lower-case letter followed by a capital letter") (targetValue (name "?{<low>,<cap>}")))
(example (utterance "lines with a vowel before a capital letter then string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(concat(<vow>,<cap>)),<!>}")))
(example (utterance "lines that must not have a character or a capital letter") (targetValue (name "?{or(repeatatleast(<any>,1),<cap>)}")))
(example (utterance "lines that contain lower-case letter , a character , or leftquoatation ! rightquoatation , zero times or more") (targetValue (name "?{contain(or(or(<low>,<any>),<!>))}")))
(example (utterance "lines ending with a capital , 4 or more times") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines starting with a letter or at least 1 number") (targetValue (name "?{repeatatleast(or(<let>,<num>),1)}")))
(example (utterance "lines without a lower-case letter before string leftquoatation ! rightquoatation without string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<low>,and(<!>,<@>))}")))
(example (utterance "items with a vowel preceding a character and leftquoatation ! rightquoatation .") (targetValue (name "?{<vow>,and(<!>,<any>)}")))
(example (utterance "lines with a lower-case letter before vowel and character") (targetValue (name "?{<low>,and(<vow>,<any>)}")))
(example (utterance "lines with words ending in 4 or more vowels") (targetValue (name "?{endwith(<vow>)}")))
(example (utterance "lines followed zero or more characters") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "items with an upper class letter preceding leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a letter .") (targetValue (name "?{<cap>,or(or(<@>,<!>),<let>)}")))
(example (utterance "lines without a vowel followed by a letter that is lower-case") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines that do not contain leftquoatation ! rightquoatation , a number or a lowercase letter") (targetValue (name "?{and(and(<!>,<num>),<low>)}")))
(example (utterance "lines with 6 or more words and capital letters") (targetValue (name "?{<cap>}")))
(example (utterance "lines with words with a number , a vowel , and leftquoatation ! rightquoatation in them") (targetValue (name "?{and(<num>,<vow>),<!>}")))
(example (utterance "lines in which there's a word containing a small letter plus a letter") (targetValue (name "?{<low>,<let>}")))
(example (utterance "lines that do not contain a lower-case letter followed by leftquoatation ! rightquoatation or a letter") (targetValue (name "?{not(<low>),or(<!>,<let>)}")))
(example (utterance "lines with words and a vowel or character") (targetValue (name "?{star(<vow>),<any>}")))
(example (utterance "lines with a vowel before leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a character") (targetValue (name "?{<vow>,or(or(<!>,<any>),<@>)}")))
(example (utterance "lines having string leftquoatation ! rightquoatation , a letter , and capital letter before number") (targetValue (name "?{and(and(<!>,<let>),<cap>),<num>}")))
(example (utterance "lines containing words  ending a with vowel followed by a lower case letter") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines with capital letter followed by character before containing string leftquoatation ! rightquoatation") (targetValue (name "?{concat(<cap>,<any>),contain(<!>)}")))
(example (utterance "lines starting with leftquoatation ! rightquoatation after a number and a capital letter") (targetValue (name "?{startwith(<num>),concat(<cap>,<!>)}")))
(example (utterance "lines with a number before vowel then letter") (targetValue (name "?{<num>,concat(<vow>,<let>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , letter , or a number") (targetValue (name "?{and(and(<!>,<let>),<num>)}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation or a letter and string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(<let>,<@>)}")))
(example (utterance "lines with words and starting in 3 or more characters") (targetValue (name "?{startwith(<any>)}")))
(example (utterance "lines with words , capital letter , and a character") (targetValue (name "?{<cap>,repeatatleast(<any>,1)}")))
(example (utterance "lines with a letter before a vowel , string leftquoatation ! rightquoatation , or lower-case letter") (targetValue (name "?{<let>,and(and(<vow>,<!>),<low>)}")))
(example (utterance "lines with a letter following a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>),<let>}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation , a character , and leftquoatation @ rightquoatation followed by leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<any>),<@>),<#>}")))
(example (utterance "lines that have a capital letter after leftquoatation ! rightquoatation at least 3 times") (targetValue (name "?{<!>,repeatatleast(<cap>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by either a letter , leftquoatation @ rightquoatation , leftquoatation # rightquoatation , and a character") (targetValue (name "?{concat(<!>,<let>),and(and(<@>,<#>),<any>)}")))
(example (utterance "lines with vowel followed by a capital and number , 2 or more times") (targetValue (name "?{concat(<vow>,<cap>),repeatatleast(<num>,2)}")))
(example (utterance "lines with a letter before a lower-case letter") (targetValue (name "?{or(<let>,<vow>),<low>}")))
(example (utterance "lines with a number , leftquoatation ! rightquoatation , and a capital letter") (targetValue (name "?{and(<num>,<!>),<cap>}")))
(example (utterance "lines containing a number followed by a letter or leftquoatation ! rightquoatation") (targetValue (name "?{<num>,or(<let>,<!>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation , a letter , or a number") (targetValue (name "?{or(or(<!>,<let>),<num>)}")))
(example (utterance "items without a captial letter .") (targetValue (name "?{not(<cap>)}")))
(example (utterance "lines containing 3 or more capital letters before a letter") (targetValue (name "?{repeatatleast(<cap>,3),<let>}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding any letter .") (targetValue (name "?{<!>,or(or(<cap>,<vow>),<let>)}")))
(example (utterance "lines with words starting with a letter") (targetValue (name "?{startwith(<let>)}")))
(example (utterance "lines that contain a number , a character , or a lower-case letter") (targetValue (name "?{or(or(<num>,<any>),<low>)}")))
(example (utterance "lines with 4 or more capital letters before a letter") (targetValue (name "?{repeatatleast(<cap>,4),<let>}")))
(example (utterance "lines not having leftquoatation ! rightquoatation with a lower-case letter") (targetValue (name "?{not(<!>),endwith(<low>)}")))
(example (utterance "lines not starting in leftquoatation ! rightquoatation or character") (targetValue (name "?{or(not(<!>),<any>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , a number , and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<!>,<num>),<@>)}")))
(example (utterance "lines with a character before lower-case letter then number") (targetValue (name "?{concat(<any>,<low>),<num>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation followed by a character , 4 or more times") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a non letter") (targetValue (name "?{<!>,not(<let>)}")))
(example (utterance "lines not having letter , not a vowel , and not a capital") (targetValue (name "?{and(and(<let>,<vow>),<cap>)}")))
(example (utterance "2 or more lines with a lower-case letter and character") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines containing words starting with number") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "lines with lower-case letter , before capital letter or character") (targetValue (name "?{<low>,or(<cap>,<any>)}")))
(example (utterance "lines with words with only a lower-case letter") (targetValue (name "?{<low>}")))
(example (utterance "lines without only a lower-case letter") (targetValue (name "?{endwith(<low>)}")))
(example (utterance "lines with words that contain a character or leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<any>,or(<!>,<@>)}")))
(example (utterance "the lines have words with at least 1 letter , character , and number .") (targetValue (name "?{and(<let>,<any>),<num>}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{<num>,not(or(<!>,<@>))}")))
(example (utterance "lines not having a letter before ending with vowel") (targetValue (name "?{not(<let>),endwith(<vow>)}")))
(example (utterance "lines with a capital letter , vowel , and a letter") (targetValue (name "?{contain(and(and(<cap>,<vow>),<let>))}")))
(example (utterance "lines with a vowel before lower-case letter before number") (targetValue (name "?{concat(<vow>,<low>),<num>}")))
(example (utterance "lines with words and the leftquoatation ! rightquoatation and lower-case letter or leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<low>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or 0 or more numbers") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines without a vowel or a number") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with words with only zero or more capital letters") (targetValue (name "?{<cap>}")))
(example (utterance "lines not having a capital letter leftquoatation ! rightquoatation") (targetValue (name "?{and(repeatatleast(<cap>,1),<!>)}")))
(example (utterance "lines with 3 or more numbers") (targetValue (name "?{repeatatleast(<num>,3)}")))
(example (utterance "lines with a character before vowel then capital letter") (targetValue (name "?{<any>,concat(<vow>,<cap>)}")))
(example (utterance "lines without words with a capital or lower-case letter") (targetValue (name "?{or(<cap>,<low>)}")))
(example (utterance "lines without a letter before a capital letter , zero or more times") (targetValue (name "?{concat(<let>,star(<cap>))}")))
(example (utterance "lines having leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation as well as leftquoatation # rightquoatation") (targetValue (name "?{<!>,contain(and(<@>,<#>))}")))
(example (utterance "lines which begin with a vowel that shows up 6 times or any number of times greater than 6") (targetValue (name "?{repeatatleast(<vow>,6)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation before having capital letter") (targetValue (name "?{contain(star(<!>)),<cap>}")))
(example (utterance "lines with words with a lower-case letter , letter , and string leftquoatation ! rightquoatation") (targetValue (name "?{and(<low>,<!>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation have a vowel , and a capital letter before it") (targetValue (name "?{and(<cap>,<vow>),<!>}")))
(example (utterance "lines with a dog before introducing a person .") (targetValue (name "?{and(<cap>,<!>),<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a number , leftquoatation @ rightquoatation , and a letter") (targetValue (name "?{<!>,and(and(<num>,<@>),<let>)}")))
(example (utterance "lines with a number , 7 or more times or string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<num>,7),concat(<!>,<@>)}")))
(example (utterance "lines that must have a capital letter , a letter , and the word leftquoatation ! rightquoatation preceding a character") (targetValue (name "?{and(and(<cap>,<let>),<!>),<any>}")))
(example (utterance "string starting with leftquoatation ! rightquoatation  in lines") (targetValue (name "?{<!>}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or a number followed by a letter") (targetValue (name "?{or(or(<!>,<@>),<num>),<let>}")))
(example (utterance "lines which are not having a number and a letter at least once") (targetValue (name "?{not(<num>),repeatatleast(<let>,1)}")))
(example (utterance "lines not having vowel , not a letter , and not leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<vow>,<let>),<!>)}")))
(example (utterance "lines with words and a letter , vowel , or character") (targetValue (name "?{or(<let>,<vow>),<any>}")))
(example (utterance "lines that doesn't have 5 or more numbers") (targetValue (name "?{repeatatleast(contain(<num>),5)}")))
(example (utterance "lines with a capital letter or a character occuring before a number") (targetValue (name "?{or(<cap>,<any>),<num>}")))
(example (utterance "lines starting in number before non vowel") (targetValue (name "?{startwith(<num>),not(<vow>)}")))
(example (utterance "lines with either no occurence or at least 1 occurence a character before a vowel") (targetValue (name "?{<any>,star(<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel then leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<vow>),<@>}")))
(example (utterance "lines starting in a vowel with string leftquoatation ! rightquoatation") (targetValue (name "?{or(repeatatleast(<vow>,1),<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , letter , and number before a character") (targetValue (name "?{and(and(<!>,<let>),<num>),<any>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation before lower-case letter , 3 or more times") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines ending in leftquoatation ! rightquoatation or non number") (targetValue (name "?{endwith(<!>),not(<num>)}")))
(example (utterance "lines that end with 2 or more characters or the word leftquoatation ! rightquoatation 2 or more times") (targetValue (name "?{endwith(or(<any>,<!>))}")))
(example (utterance "lines with number before character and letter") (targetValue (name "?{<num>,and(<any>,<let>)}")))
(example (utterance "lines that begin with a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{startwith(or(<low>,<!>))}")))
(example (utterance "lines containing the following -string leftquoatation ! rightquoatation before a lower-case letter followed by a letter") (targetValue (name "?{concat(<!>,concat(<low>,<let>))}")))
(example (utterance "lines not containing string leftquoatation ! rightquoatation before vowel") (targetValue (name "?{contain(not(<!>)),<vow>}")))
(example (utterance "lines that have letters preceding any numbers") (targetValue (name "?{<let>,contain(<num>)}")))
(example (utterance "lines containing a capital letter , a letter , and a vowel in words") (targetValue (name "?{and(<cap>,<let>),<vow>}")))
(example (utterance "lines that have a vowel after a capital letter") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines with a vowel , and 1 or more of leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(and(<vow>,<!>),<@>)}")))
(example (utterance "lines ending with a vowel") (targetValue (name "?{contain(<vow>)}")))
(example (utterance "lines with 3 or more of leftquoatation ! rightquoatation with string leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(<!>,3),concat(<@>,<@>)}")))
(example (utterance "lines with no leftquoatation ! rightquoatation , capital letter , or character") (targetValue (name "?{and(and(<!>,<cap>),<any>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation before a lower-case letter") (targetValue (name "?{concat(<!>,contain(<low>))}")))
(example (utterance "lines that don't contain a number and leftquoatation ! rightquoatation") (targetValue (name "?{not(<num>),<!>}")))
(example (utterance "lines having vowel , string leftquoatation ! rightquoatation , and a capital before character") (targetValue (name "?{and(and(<vow>,<!>),<cap>),<any>}")))
(example (utterance "lines starting in a number or vowel") (targetValue (name "?{or(repeatatleast(<num>,1),<vow>)}")))
(example (utterance "lines containing a capital letter or a letter at least zero times") (targetValue (name "?{or(<cap>,<let>)}")))
(example (utterance "lines starting in a lower-case letter or with a letter") (targetValue (name "?{or(<low>,contain(<let>))}")))
(example (utterance "items without a letter , small letter , or character .") (targetValue (name "?{or(or(<any>,<low>),<let>)}")))
(example (utterance "lines not having lower-case letter or containing a capital") (targetValue (name "?{not(<low>),contain(<cap>)}")))
(example (utterance "lines ending with lower-case letter or string leftquoatation ! rightquoatation , zero or more times") (targetValue (name "?{star(or(<low>,<!>))}")))
(example (utterance "lines that contain a vowel or 7 or more numbers") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before a character") (targetValue (name "?{or(<!>,<@>),star(<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation not being before leftquoatation @ rightquoatation or leftquoatation # rightquoatation or a character") (targetValue (name "?{<!>,not(or(or(<@>,<#>),<any>))}")))
(example (utterance "lines with words starting with a vowel or a capital letter") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "lines with a vowel at least once or leftquoatation ! rightquoatation before a character in them") (targetValue (name "?{repeatatleast(<vow>,1),concat(<!>,<any>)}")))
(example (utterance "lines with a character before a number or capital letter") (targetValue (name "?{or(concat(<any>,<num>),<cap>)}")))
(example (utterance "string 'dog ; appears before a character followed by a number in the lines") (targetValue (name "?{<!>,concat(<any>,<num>)}")))
(example (utterance "lines that have leftquoatation ! rightquoatation before 6 or more numbers") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines with words with letter and containing only a character") (targetValue (name "?{<let>,<any>}")))
(example (utterance "lines which are starting with words with a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "a line with a leftquoatation ! rightquoatation  , a capital  , or a lower case before a character") (targetValue (name "?{or(or(<!>,<cap>),<low>),<any>}")))
(example (utterance "lines ending with words that contain letter") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines which contain a capital letter , a number , or have leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<cap>,<num>),<!>),<@>}")))
(example (utterance "lines having a capital , a character , and letter before lower-case letter") (targetValue (name "?{and(and(<cap>,<any>),<let>),<low>}")))
(example (utterance "lines without a number , vowel , or a lower-case letter") (targetValue (name "?{and(and(<num>,<vow>),<low>)}")))
(example (utterance "lines with a lower-case letter or character") (targetValue (name "?{or(<low>,<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a number at least once in them") (targetValue (name "?{contain(<!>),repeatatleast(<num>,1)}")))
(example (utterance "lines with words and the string dog at least 6 times") (targetValue (name "?{<!>}")))
(example (utterance "lines ending in 6 or more of leftquoatation ! rightquoatation or with string leftquoatation @ rightquoatation") (targetValue (name "?{or(repeatatleast(<!>,6),<@>)}")))
(example (utterance "lines containing 4 or more times a character or uppercase letter") (targetValue (name "?{or(<any>,<cap>)}")))
(example (utterance "items terminating with leftquoatation ! rightquoatation preceding a letter .") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines without a character before capital letter") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines with 1 or more vowel before a lower-case letter") (targetValue (name "?{repeatatleast(<vow>,1),<low>}")))
(example (utterance "lines having either letter , leftquoatation ! rightquoatation , or a number before capital letter") (targetValue (name "?{or(or(<let>,<!>),<num>),<cap>}")))
(example (utterance "lines that have leftquoatation ! rightquoatation and then a character , zero or more times") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "lines with a vowel before a capital letter at least zero times") (targetValue (name "?{<vow>,contain(star(<cap>))}")))
(example (utterance "lines without a number or strings leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{not(<num>),concat(<!>,<@>)}")))
(example (utterance "lines containing a number , the word leftquoatation ! rightquoatation , and the word leftquoatation @ rightquoatation before a vowel") (targetValue (name "?{and(and(<num>,<!>),<vow>),<@>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a capital letter or string leftquoatation @ rightquoatation or leftquoatation # rightquoatation") (targetValue (name "?{not(<!>),or(or(<cap>,<@>),<#>)}")))
(example (utterance "lines without a character , vowel , or letter") (targetValue (name "?{and(and(<any>,<vow>),<let>)}")))
(example (utterance "lines containing 0 or more of leftquoatation ! rightquoatation , but not leftquoatation @ rightquoatation") (targetValue (name "?{star(<!>),not(<@>)}")))
(example (utterance "lines containing a character , the word leftquoatation ! rightquoatation and a number") (targetValue (name "?{<any>,concat(<!>,<num>)}")))
(example (utterance "lines containing a capital letter or a vowel zero or more times") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "lines ending in a letter before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<let>),or(<!>,<@>)}")))
(example (utterance "the lines that contain leftquoatation ! rightquoatation or 'truck repeated 4 times") (targetValue (name "?{or(<!>,<@>)}")))
(example (utterance "lines starting with 7 or more of leftquoatation ! rightquoatation or vowel") (targetValue (name "?{repeatatleast(or(<!>,<vow>),7)}")))
(example (utterance "lines without only zero or more of a number") (targetValue (name "?{star(<num>)}")))
(example (utterance "lines with 7 or more of a character before a vowel") (targetValue (name "?{concat(<any>,<vow>)}")))
(example (utterance "lines containing a capital , 5 or more times") (targetValue (name "?{contain(<cap>)}")))
(example (utterance "lines with words either leftquoatation ! rightquoatation , a number , and a vowel") (targetValue (name "?{or(<!>,<num>),<vow>}")))
(example (utterance "lines containing a letter or a character once") (targetValue (name "?{contain(or(<let>,<any>))}")))
(example (utterance "lines with vowel before character , 7 or more times") (targetValue (name "?{<vow>,repeatatleast(<any>,7)}")))
(example (utterance "lines that end with a lower case over 7 times") (targetValue (name "?{contain(endwith(<low>))}")))
(example (utterance "lines with words ending in vowel before letter") (targetValue (name "?{concat(<vow>,<let>)}")))
(example (utterance "lines not ending with leftquoatation ! rightquoatation before a letter") (targetValue (name "?{not(endwith(<!>)),<let>}")))
(example (utterance "lines containing an upper-case leter preceding a character prior to the leftquoatation ! rightquoatation string") (targetValue (name "?{<cap>,concat(<any>,<!>)}")))
(example (utterance "lines with a lower-case letter , vowel , or string leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<low>,<vow>),<!>)}")))
(example (utterance "lines with a vowel or a number , 7 or more times in them") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lines with a vowel , zero or more times before leftquoatation ! rightquoatation") (targetValue (name "?{star(<vow>),<!>}")))
(example (utterance "items without leftquoatation ! rightquoatation or a character or leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<!>,<any>),<@>)}")))
(example (utterance "lines starting with 2 times vowel") (targetValue (name "?{repeatatleast(<vow>,2)}")))
(example (utterance "lines with a capital letter followed by leftquoatation ! rightquoatation and which are ending with a letter") (targetValue (name "?{concat(<cap>,<!>),endwith(<let>)}")))
(example (utterance "lines with a vowel or a capital , 2 or more times") (targetValue (name "?{or(<vow>,<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a character and a vowel") (targetValue (name "?{<!>,and(<any>,<vow>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation at least once before containing letter") (targetValue (name "?{repeatatleast(<!>,1),contain(<let>)}")))
(example (utterance "items with a letter , a small letter , and a vowel before leftquoatation ! rightquoatation .") (targetValue (name "?{and(and(<vow>,<low>),<let>),<!>}")))
(example (utterance "lines without a lower-case letter followed by a character") (targetValue (name "?{concat(<low>,<any>)}")))
(example (utterance "lines starting with words contain a number") (targetValue (name "?{contain(<num>)}")))
(example (utterance "items with a numeral preceding a letter preceding a vowel .") (targetValue (name "?{concat(<num>,<let>),<vow>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , number , and character before string 'truck'[") (targetValue (name "?{and(and(<!>,<num>),<any>),<@>}")))
(example (utterance "lines that do not have a capital letter or a number") (targetValue (name "?{and(<cap>,<num>)}")))
(example (utterance "items with a small letter preceding leftquoatation ! rightquoatation and leftquoatation @ rightquoatation .") (targetValue (name "?{<low>,and(<@>,<!>)}")))
(example (utterance "lines with 3 or more of a vowel or lower-case letter") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines starting with a vowel after a number") (targetValue (name "?{startwith(<num>),<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation 6 or more times before leftquoatation @ rightquoatation and leftquoatation # rightquoatation") (targetValue (name "?{repeatatleast(<!>,6),and(<@>,<#>)}")))
(example (utterance "the lines containing words , plus leftquoatation ! rightquoatation followed by either leftquoatation @ rightquoatation or a vowel") (targetValue (name "?{concat(<!>,<@>),<vow>}")))
(example (utterance "lines with a lower-case letter followed by a vowel , 3 or more times in it") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines that end with character and have string leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<any>),contain(<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation or leftquoatation # rightquoatation or a letter") (targetValue (name "?{<!>,or(or(<@>,<let>),<#>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or a character or a capital letter") (targetValue (name "?{or(or(<!>,<any>),<cap>)}")))
(example (utterance "items with a letter at least 4 times preceding a small letter .") (targetValue (name "?{repeatatleast(<let>,4),<low>}")))
(example (utterance "lines containing vowel , number , and a letter") (targetValue (name "?{and(and(<vow>,<num>),<let>)}")))
(example (utterance "lines that have a letter , the word leftquoatation ! rightquoatation ,  , and a vowel followed by a character") (targetValue (name "?{and(and(<let>,<!>),<vow>),<any>}")))
(example (utterance "a number in the lines") (targetValue (name "?{<num>}")))
(example (utterance "lines that contain a vowel , character , or capital letter") (targetValue (name "?{or(or(<vow>,<any>),<cap>)}")))
(example (utterance "lines containing a lower-case letter or a number followed by a letter") (targetValue (name "?{or(<low>,<num>),<let>}")))
(example (utterance "lines with words and without a letter") (targetValue (name "?{or(<let>,<cap>)}")))
(example (utterance "lines that start with leftquoatation ! rightquoatation then leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(concat(<!>,<@>),1)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a number , 6 or more times in them") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines with a number and a character before that number") (targetValue (name "?{<any>,<num>}")))
(example (utterance "lines containing 5 or more of a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),repeatatleast(<!>,5)}")))
(example (utterance "lines that contain any character that are then followed by leftquoatation ! rightquoatation") (targetValue (name "?{<any>,<!>}")))
(example (utterance "lines with a letter") (targetValue (name "?{<let>}")))
(example (utterance "lines with a small letter at the end and then the leftquoatation ! rightquoatation string") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "items terminating with a numeral at least6times .") (targetValue (name "?{repeatatleast(<num>,6)}")))
(example (utterance "lines containing  leftquoatation ! rightquoatation followed by a letter , 5 or more times") (targetValue (name "?{concat(<!>,<let>)}")))
(example (utterance "lines ending in 4 or more of leftquoatation ! rightquoatation or vowel") (targetValue (name "?{or(<!>,repeatatleast(<vow>,4))}")))
(example (utterance "lines with a character before a capital letter") (targetValue (name "?{<any>,contain(<cap>)}")))
(example (utterance "lines with words with either a lower-case letter , vowel , or letter") (targetValue (name "?{or(<low>,<vow>),<let>}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation preceding a number , zero or more times") (targetValue (name "?{<!>,star(<num>)}")))
(example (utterance "lines with lower-case letter before number or zero or more of acapital letter") (targetValue (name "?{or(concat(<low>,<num>),<cap>)}")))
(example (utterance "items with a numeral preceding a small letter , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation .") (targetValue (name "?{<num>,or(or(<low>,<!>),<@>)}")))
(example (utterance "items with a letter preceding an upper case letter preceding a character .") (targetValue (name "?{<let>,concat(<cap>,<any>)}")))
(example (utterance "lines with a number after leftquoatation ! rightquoatation , a character , or a vowel") (targetValue (name "?{or(or(<!>,<any>),<vow>),<num>}")))
(example (utterance "lines with a letter occuring6or more times before a character") (targetValue (name "?{repeatatleast(<let>,6),<any>}")))
(example (utterance "lines with a character preceding a letter which precedes a vowel") (targetValue (name "?{concat(<any>,<let>),<vow>}")))
(example (utterance "lines containing both a lower-case letter and a letter preceding a character") (targetValue (name "?{and(<low>,<let>),<any>}")))
(example (utterance "lines with character") (targetValue (name "?{<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a letter , and then leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<let>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter ending in string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<low>),endwith(<@>)}")))
(example (utterance "lines that have a number before a lower-case letter and leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<low>),<!>}")))
(example (utterance "lines with a capital followed by vowel before number") (targetValue (name "?{concat(<cap>,<vow>),<num>}")))
(example (utterance "lines without a character , 5 or more times followed by leftquoatation ! rightquoatation") (targetValue (name "?{not(repeatatleast(<any>,5)),<!>}")))
(example (utterance "lines with a character before string leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , and a capital") (targetValue (name "?{<any>,and(and(<!>,<@>),<cap>)}")))
(example (utterance "lines with a lower-case letter before containing leftquoatation ! rightquoatation and a letter in them") (targetValue (name "?{<low>,contain(and(<!>,<let>))}")))
(example (utterance "line containing leftquoatation ! rightquoatation or a capital letter") (targetValue (name "?{or(<!>,<cap>)}")))
(example (utterance "lines containing either capital letter , regular letter , or a lower letter .") (targetValue (name "?{or(or(<cap>,<let>),<low>)}")))
(example (utterance "lines ending with a lower-case letter before a vowel in them") (targetValue (name "?{endwith(<low>),<vow>}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation before a character in them") (targetValue (name "?{concat(<!>,<@>),<any>}")))
(example (utterance "lines ending with a lower-case letter") (targetValue (name "?{contain(not(<low>))}")))
(example (utterance "lines with words , a vowel , and a capital letter") (targetValue (name "?{<vow>,<cap>}")))
(example (utterance "lines starting in 6 or more of leftquoatation ! rightquoatation and capital letters") (targetValue (name "?{startwith(<!>),repeatatleast(<cap>,6)}")))
(example (utterance "lines ending with character or not a number") (targetValue (name "?{endwith(<any>),not(<num>)}")))
(example (utterance "lines ending in a capital letter before leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<cap>),not(<!>)}")))
(example (utterance "lines with a vowel placed before a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,concat(<let>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a vowel or a lower-case letter") (targetValue (name "?{contain(<!>),or(<vow>,<low>)}")))
(example (utterance "lines with a number followed by a capital or a vowel") (targetValue (name "?{<num>,or(<cap>,<vow>)}")))
(example (utterance "lines that contain words with a number or a vowel") (targetValue (name "?{<vow>,<num>}")))
(example (utterance "lines with a letter , at least twice") (targetValue (name "?{contain(<let>)}")))
(example (utterance "lines with words and a lower-case letter , string leftquoatation ! rightquoatation , or number") (targetValue (name "?{or(<low>,<!>),<num>}")))
(example (utterance "lines that do not include leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines ending in words with string leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a lower-case letter") (targetValue (name "?{and(and(<!>,<low>),<@>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{repeatatleast(or(<!>,<cap>),1)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation , a vowel , and a capital letter") (targetValue (name "?{contain(and(and(<!>,<vow>),<cap>))}")))
(example (utterance "lines not ending in a letter before number") (targetValue (name "?{not(endwith(<let>)),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a capital letter in them before ending with a lower-case letter") (targetValue (name "?{or(<!>,<cap>),endwith(<low>)}")))
(example (utterance "items without a numeral , upper case letter , or character .") (targetValue (name "?{and(and(<any>,<cap>),<num>)}")))
(example (utterance "lines without a letter , number , or character") (targetValue (name "?{and(and(<let>,<num>),<any>)}")))
(example (utterance "lines ending in vowel or letter") (targetValue (name "?{or(repeatatleast(<vow>,1),<let>)}")))
(example (utterance "lines containing words with a capital letter followed by a vowel in them") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines opening with an alphabet character succeeded with a digit") (targetValue (name "?{concat(<let>,<num>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation before 'vowel , 4 or more times") (targetValue (name "?{contain(<!>),repeatatleast(<vow>,4)}")))
(example (utterance "lines that contain only a lower-case letter and leftquoatation ! rightquoatation") (targetValue (name "?{<low>,<!>}")))
(example (utterance "lines which are containing a letter or leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{contain(<let>),concat(<!>,<@>)}")))
(example (utterance "lines where there is 1 instance of a letter preceding a capital letter") (targetValue (name "?{contain(concat(<let>,<cap>))}")))
(example (utterance "lines that contain 1 of these - the word leftquoatation ! rightquoatation , a lower-case letter , or a character - followed by the word leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<low>),<any>),<@>}")))
(example (utterance "lines ending with words starting with a number") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "lines having either a lower-case letter , leftquoatation ! rightquoatation , or a number before a capital letter in them") (targetValue (name "?{or(or(<low>,<!>),<num>),<cap>}")))
(example (utterance "lines with vowel followed by string leftquoatation ! rightquoatation before character") (targetValue (name "?{concat(<vow>,<!>),<any>}")))
(example (utterance "lines with words with a number and vowel") (targetValue (name "?{<num>,<vow>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before either string leftquoatation @ rightquoatation , a vowel , or string leftquoatation # rightquoatation") (targetValue (name "?{<!>,or(or(<@>,<vow>),<#>)}")))
(example (utterance "lines not containing 3 or more letters") (targetValue (name "?{repeatatleast(<let>,3)}")))
(example (utterance "lines containing a character , followed by leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<any>,<!>),<@>)}")))
(example (utterance "lines containing a letter preceded by a character") (targetValue (name "?{contain(<any>),<let>}")))
(example (utterance "lines where 6 or more times there is a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with string of words that start with leftquoatation ! rightquoatation and follow with leftquoatation @ rightquoatation") (targetValue (name "?{<!>,<@>}")))
(example (utterance "lines with vowel before string leftquoatation ! rightquoatation or starting with string leftquoatation @ rightquoatation") (targetValue (name "?{concat(<vow>,<!>),startwith(<@>)}")))
(example (utterance "lines starting in leftquoatation ! rightquoatation or ending in capital letter") (targetValue (name "?{or(<!>,endwith(<cap>))}")))
(example (utterance "lines with 6 or more of a lower-case letter before number") (targetValue (name "?{<low>,repeatatleast(<num>,6)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a numeral , and an upper case letter preceding leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<cap>,<!>),<num>),<@>}")))
(example (utterance "lines starting with a lower-case letter preceding a letter after a character") (targetValue (name "?{startwith(<any>),concat(<low>,<let>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation and a number") (targetValue (name "?{<!>,and(<@>,<num>)}")))
(example (utterance "lines containing a lower-case letter zero times") (targetValue (name "?{contain(and(<low>,<cap>))}")))
(example (utterance "lines starting in capital letter with a character") (targetValue (name "?{startwith(concat(<cap>,<any>))}")))
(example (utterance "lines that have 4 lower-case and vowel combinations , with the letter first") (targetValue (name "?{concat(<low>,<vow>)}")))
(example (utterance "lines with 6 or more vowels or letteres") (targetValue (name "?{repeatatleast(or(<vow>,<let>),6)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation or a vowel") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines with vowel , 7 or more times before number") (targetValue (name "?{repeatatleast(<vow>,7),<num>}")))
(example (utterance "lines that start with a character 7 times") (targetValue (name "?{repeatatleast(<any>,7)}")))
(example (utterance "lines with a character or lower-case letter before capital letter") (targetValue (name "?{or(<any>,<low>),<cap>}")))
(example (utterance "lines without a letter followed by a number and without string leftquoatation ! rightquoatation") (targetValue (name "?{and(concat(<let>,<num>),<!>)}")))
(example (utterance "lines that end with a number before a lower case letter .") (targetValue (name "?{endwith(<num>),<low>}")))
(example (utterance "lines containing a letter preceding leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation") (targetValue (name "?{<let>,concat(<!>,<@>)}")))
(example (utterance "lines with zero or more of leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{and(star(<!>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation or a small letter") (targetValue (name "?{or(<!>,<@>),<low>}")))
(example (utterance "lines which end with a lower case letter or a string leftquoatation ! rightquoatation .") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "leftquoatation ! rightquoatation in the line 7 times or more") (targetValue (name "?{repeatatleast(<!>,7)}")))
(example (utterance "lines with no number with leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "items with a vowel or letter .") (targetValue (name "?{or(<let>,<vow>)}")))
(example (utterance "lines with words and a number or capital letter") (targetValue (name "?{<num>,<cap>}")))
(example (utterance "lines with capitalized words followed by a vowel") (targetValue (name "?{<cap>,<vow>}")))
(example (utterance "lines containing number or not a vowel") (targetValue (name "?{or(<num>,not(<vow>))}")))
(example (utterance "lines without a letter") (targetValue (name "?{star(<let>)}")))
(example (utterance "lines with a vowel or leftquoatation ! rightquoatation , 5 or more times in them") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines end with a number , a lower-case letter and a capital letter") (targetValue (name "?{endwith(<num>),and(<low>,<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a capital letter , vowel , or string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,or(or(<cap>,<vow>),<@>)}")))
(example (utterance "leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , a capital letter , or a lower-case letter") (targetValue (name "?{<!>,or(or(<@>,<cap>),<low>)}")))
(example (utterance "lines with a number at least once before a letter in them") (targetValue (name "?{repeatatleast(<num>,1),<let>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or lower-case or capital letter") (targetValue (name "?{or(and(<!>,<low>),<cap>)}")))
(example (utterance "lines with a number or a capital letter occurring at least once") (targetValue (name "?{or(<num>,<cap>)}")))
(example (utterance "lines that end with a letter or character") (targetValue (name "?{or(<any>,<let>)}")))
(example (utterance "lines do not contain a number , leftquoatation ! rightquoatation , or a character") (targetValue (name "?{or(or(<num>,<!>),<any>)}")))
(example (utterance "lines with either a number , a vowel , or a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<vow>),<let>),<!>}")))
(example (utterance "items with either leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or a vowel preceding an upper case letter .") (targetValue (name "?{or(or(<@>,<!>),<vow>),<cap>}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a capital letter") (targetValue (name "?{<let>,and(and(<!>,<@>),<cap>)}")))
(example (utterance "lines containing a capital letter , leftquoatation ! rightquoatation , a letter , and a character") (targetValue (name "?{and(and(<cap>,<!>),<let>),<any>}")))
(example (utterance "lines having leftquoatation ! rightquoatation after a character , leftquoatation @ rightquoatation , or a lower-case letter") (targetValue (name "?{or(or(<any>,<@>),<low>),<!>}")))
(example (utterance "lines with a vowel , leftquoatation ! rightquoatation or a capital letter before leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<vow>,<!>),<cap>),<@>}")))
(example (utterance "lines with words and only a lower-case and capital letter") (targetValue (name "?{<low>,<cap>}")))
(example (utterance "any words at ending as long as the lines have leftquoatation ! rightquoatation followed by any character") (targetValue (name "?{concat(<!>,<any>)}")))
(example (utterance "items with a character , leftquoatation ! rightquoatation , and an upper case letter preceding leftquoatation @ rightquoatation .") (targetValue (name "?{and(and(<cap>,<!>),<any>),<@>}")))
(example (utterance "lines where a capital letter does not precede leftquoatation ! rightquoatation") (targetValue (name "?{<cap>,not(star(<!>))}")))
(example (utterance "lines that don't contain a vowel or leftquoatation ! rightquoatation") (targetValue (name "?{not(and(<vow>,<!>))}")))
(example (utterance "lines with words with a lower-case letter or a letter , occurring 3 or more times") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines containing only a letter , 6 times before leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<let>,6),<!>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before letter , lower-case letter , and character") (targetValue (name "?{<!>,and(and(<let>,<low>),<any>)}")))
(example (utterance "items with a letter preceding a character and uppercase letter .") (targetValue (name "?{<let>,and(<any>,<cap>)}")))
(example (utterance "lines with a letter or ending in 4 or more of a lower-case letter") (targetValue (name "?{or(<let>,endwith(<low>))}")))
(example (utterance "lines with 4 or more of leftquoatation ! rightquoatation and capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(and(<!>,<cap>),<@>)}")))
(example (utterance "lines that contain a capital letter followed by a vowel , at least twice") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines with a number or leftquoatation ! rightquoatation") (targetValue (name "?{or(<num>,<!>)}")))
(example (utterance "lines that contain a number followed by a vowel or leftquoatation ! rightquoatation or a letter") (targetValue (name "?{<num>,or(or(<vow>,<!>),<let>)}")))
(example (utterance "lines that contain a lower-case letter , a number , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<!>,<low>),<num>)}")))
(example (utterance "lines containing vowel or lowercase letter") (targetValue (name "?{or(<low>,<vow>)}")))
(example (utterance "lines which are starting with a vowel followed by a capital letter") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines containing either number , letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<let>),<!>)}")))
(example (utterance "lines that end with leftquoatation ! rightquoatation and a vowel") (targetValue (name "?{contain(and(<!>,<vow>))}")))
(example (utterance "lines containing a capital letter , a vowel , and a letter preceding leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<cap>,<vow>),<let>),<!>}")))
(example (utterance "lines having leftquoatation ! rightquoatation , a character , a number and a capital letter") (targetValue (name "?{or(or(<any>,<num>),<cap>),<!>}")))
(example (utterance "lines with letter before string leftquoatation ! rightquoatation , 6 or more times") (targetValue (name "?{<let>,repeatatleast(<!>,6)}")))
(example (utterance "lines ending with letter or lower-case letter , 2 or more times") (targetValue (name "?{repeatatleast(or(<let>,<low>),2)}")))
(example (utterance "lines with words ending in zero or more characters") (targetValue (name "?{endwith(<any>)}")))
(example (utterance "lines that don't contain a lower-case letter or leftquoatation ! rightquoatation 4 times or more") (targetValue (name "?{or(not(<!>),<low>)}")))
(example (utterance "lines that don't start with a lower-case letter , at least once") (targetValue (name "?{startwith(star(<low>))}")))
(example (utterance "lines with a character or capital letter before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(or(<any>,<cap>),<!>)}")))
(example (utterance "lines with less than 7 capital letters") (targetValue (name "?{repeatatleast(<cap>,7)}")))
(example (utterance "lines without a vowel before a lower-case letter") (targetValue (name "?{not(star(<vow>)),<low>}")))
(example (utterance "lines starting in a lower-case letter before string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(concat(<low>,<!>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with a lower-case letter and string leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{and(<low>,<!>),<cap>}")))
(example (utterance "lines without a capital letter , leftquoatation ! rightquoatation , or a number") (targetValue (name "?{or(or(<cap>,<!>),<num>)}")))
(example (utterance "lines containing a capital , letter , and character") (targetValue (name "?{and(and(<cap>,<let>),<any>)}")))
(example (utterance "lines with words that contain leftquoatation ! rightquoatation before a capital letter in them") (targetValue (name "?{contain(<!>),<cap>}")))
(example (utterance "lines with either the word leftquoatation ! rightquoatation a capital letter or a lower-case letter preceding a vowel .") (targetValue (name "?{or(or(<!>,<cap>),<low>),<vow>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or 7 lower-case letters") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceding a character before a number") (targetValue (name "?{concat(<!>,<any>),<num>}")))
(example (utterance "lines without leftquoatation ! rightquoatation followed by a lower-case letter at least 2 times") (targetValue (name "?{repeatatleast(concat(<!>,<low>),2)}")))
(example (utterance "lines with words and leftquoatation ! rightquoatation and leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{and(<!>,<@>),<#>}")))
(example (utterance "lines with a number before vowel and lower-case letter") (targetValue (name "?{<num>,and(<vow>,<low>)}")))
(example (utterance "lines not having a capital  , not a vowel , and not a number") (targetValue (name "?{and(and(<cap>,<vow>),<num>)}")))
(example (utterance "capital letter words with zero or more lines .") (targetValue (name "?{<cap>}")))
(example (utterance "lines with capital letters after a lower case letter .") (targetValue (name "?{startwith(<low>),<cap>}")))
(example (utterance "lines with leftquoatation ! rightquoatation and a character preceded by leftquoatation @ rightquoatation") (targetValue (name "?{<@>,and(<!>,<any>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or letters") (targetValue (name "?{and(<!>,<let>)}")))
(example (utterance "lines with a character followed by a number occurring 5 or more times") (targetValue (name "?{concat(<any>,<num>)}")))
(example (utterance "lines starting with a character preceding a letter") (targetValue (name "?{concat(<any>,<let>)}")))
(example (utterance "lines that do not have leftquoatation ! rightquoatation and a vowel before a capital letter") (targetValue (name "?{concat(<vow>,<cap>),star(<!>)}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation or a character") (targetValue (name "?{contain(<!>),<any>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before a vowel , leftquoatation @ rightquoatation , and a capital letter") (targetValue (name "?{<!>,and(and(<vow>,<@>),<cap>)}")))
(example (utterance "lines with words and strings leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<#>)}")))
(example (utterance "lines that don't end with a character or leftquoatation ! rightquoatation") (targetValue (name "?{or(endwith(<any>),<!>)}")))
(example (utterance "lines with words and only 2 or more lower-case letters") (targetValue (name "?{<low>}")))
(example (utterance "lines with strings leftquoatation ! rightquoatation and leftquoatation @ rightquoatation then a number") (targetValue (name "?{contain(<!>),concat(<@>,<num>)}")))
(example (utterance "lines containing a vowel , a capital , and number") (targetValue (name "?{and(and(<vow>,<cap>),<num>)}")))
(example (utterance "lines stating with 7 or more numbers") (targetValue (name "?{startwith(<num>)}")))
(example (utterance "a character with lines 7 or more times before a vowel .") (targetValue (name "?{repeatatleast(<any>,7),<vow>}")))
(example (utterance "lines having leftquoatation ! rightquoatation after a letter , leftquoatation @ rightquoatation , or a capital letter") (targetValue (name "?{or(or(<let>,<@>),<cap>),<!>}")))
(example (utterance "lines not having character before containing letter") (targetValue (name "?{not(<any>),contain(<let>)}")))
(example (utterance "lines with a character before a vowel 7 times") (targetValue (name "?{<any>,repeatatleast(<vow>,7)}")))
(example (utterance "lines ending in zero or more of a vowel or letter") (targetValue (name "?{endwith(or(<vow>,<let>))}")))
(example (utterance "lines that start with leftquoatation ! rightquoatation") (targetValue (name "?{star(<!>)}")))
(example (utterance "lines with 1 character before the word leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "items without leftquoatation ! rightquoatation , a small letter , or a numeral .") (targetValue (name "?{or(or(<!>,<num>),<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or which are ending with a capital letter") (targetValue (name "?{contain(<!>),endwith(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation followed by a lower-case letter 7 or more times") (targetValue (name "?{concat(<!>,<low>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a lower-case letter , or a number") (targetValue (name "?{or(<!>,<low>),<num>}")))
(example (utterance "lines that contain a number and leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation , zero times or more") (targetValue (name "?{and(<num>,<!>),star(<@>)}")))
(example (utterance "lines with words with leftquoatation ! rightquoatation , a character , and a vowel in them") (targetValue (name "?{and(<!>,<any>),<vow>}")))
(example (utterance "lines with a number , a lower-case letter , or leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<low>),<!>)}")))
(example (utterance "lines with  a number preceded by a capital letter") (targetValue (name "?{<cap>,<num>}")))
(example (utterance "lines that end with a lower-case letter and not followed by  leftquoatation ! rightquoatation") (targetValue (name "?{star(concat(<low>,<!>))}")))
(example (utterance "lines having words with or without a letter or a vowel") (targetValue (name "?{<let>,star(<vow>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or numeral preceding a small letter .") (targetValue (name "?{and(and(<!>,<num>),<@>),<low>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before lower-case letter or character") (targetValue (name "?{<!>,or(<low>,<any>)}")))
(example (utterance "lines that contain a capital letter , the word leftquoatation ! rightquoatation , and a vowel followed by the word leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<cap>,<!>),<vow>),<@>}")))
(example (utterance "lines with a character before a letter and lower-case letter") (targetValue (name "?{<any>,and(<let>,<low>)}")))
(example (utterance "items with an upper case letter , at least 4 times preceding a character .") (targetValue (name "?{repeatatleast(<cap>,4),<any>}")))
(example (utterance "lines containing a capital , zero or more times before string leftquoatation ! rightquoatation") (targetValue (name "?{concat(star(<cap>),<!>)}")))
(example (utterance "lines with words with starting with a number , 4 or more times in it") (targetValue (name "?{repeatatleast(<num>,4)}")))
(example (utterance "lines with a number , a vowel , as well as a letter followed by leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<num>,<vow>),<let>),<!>}")))
(example (utterance "lines containing a number and 0 or more of leftquoatation ! rightquoatation") (targetValue (name "?{<num>,star(<!>)}")))
(example (utterance "lines containing a letter or a number not  followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<num>),not(<!>)}")))
(example (utterance "lines starting in vowel ending in string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(<vow>),endwith(<!>)}")))
(example (utterance "lines ending with a capital letter at least zero times") (targetValue (name "?{star(<cap>)}")))
(example (utterance "lines that contain a character at least twice and does not contain leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<any>,2),not(<!>)}")))
(example (utterance "lines containing a number followed by a vowel , zero or more times in them") (targetValue (name "?{contain(concat(<num>,<vow>))}")))
(example (utterance "lines with a capital letter and ending with a lower-case letter") (targetValue (name "?{endwith(<low>),startwith(<cap>)}")))
(example (utterance "lines with a letter before leftquoatation ! rightquoatation , a vowel , and a capital letter") (targetValue (name "?{and(and(<vow>,<cap>),<let>),<!>}")))
(example (utterance "minimum of 1 time , lines showing a lower case letter or a capital letter") (targetValue (name "?{or(<low>,<cap>)}")))
(example (utterance "lines that contain 1 string leftquoatation ! rightquoatation or  a capital letter  , or a character before a number .") (targetValue (name "?{or(or(<!>,<cap>),<any>),<num>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a letter") (targetValue (name "?{repeatatleast(or(<!>,<let>),1)}")))
(example (utterance "lines with a capital letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines not containing string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation") (targetValue (name "?{and(not(<!>),<@>)}")))
(example (utterance "lines with a vowel and a character preceding leftquoatation ! rightquoatation") (targetValue (name "?{contain(<vow>),concat(<any>,<!>)}")))
(example (utterance "items with a letter , vowel , and leftquoatation ! rightquoatation .") (targetValue (name "?{and(and(<vow>,<let>),<!>)}")))
(example (utterance "lines having either leftquoatation ! rightquoatation , a capital letter , or leftquoatation @ rightquoatation before a vowel in them") (targetValue (name "?{or(or(<!>,<cap>),<@>),<vow>}")))
(example (utterance "lines containing lower case") (targetValue (name "?{<low>}")))
(example (utterance "items with an upper case letter preceding leftquoatation ! rightquoatation at least 7 times .") (targetValue (name "?{<cap>,repeatatleast(<!>,7)}")))
(example (utterance "lines containing words with a capital letter before leftquoatation ! rightquoatation followed by a character") (targetValue (name "?{concat(<cap>,<!>),<any>}")))
(example (utterance "lines with character or starting with number , 6 or more times") (targetValue (name "?{or(<any>,startwith(<num>))}")))
(example (utterance "lines with 2 or more of a vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{<vow>,repeatatleast(<!>,2)}")))
(example (utterance "lines without a letter 2 or more times") (targetValue (name "?{repeatatleast(<let>,2)}")))
(example (utterance "lines with no capital letter or number then leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,concat(<num>,<!>))}")))
(example (utterance "lines not having a capital at least once before lower-case letter") (targetValue (name "?{concat(repeatatleast(<cap>,1),<low>)}")))
(example (utterance "items with a character or a letter , at least 7 times .") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation before leftquoatation @ rightquoatation , 6 or more times in them") (targetValue (name "?{contain(concat(<!>,<@>))}")))
(example (utterance "lines which are starting with a letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<let>,<!>)}")))
(example (utterance "lines with 7 or more numbers") (targetValue (name "?{<num>}")))
(example (utterance "items beginning with a vowel or leftquoatation ! rightquoatation .") (targetValue (name "?{startwith(or(<!>,<vow>))}")))
(example (utterance "lines that contain leftquoatation ! rightquoatation or leftquoatation @ rightquoatation followed by leftquoatation # rightquoatation , at least 5 times") (targetValue (name "?{or(<!>,concat(<@>,<#>))}")))
(example (utterance "lines with a lower-case letter followed by a number , a character , and a letter") (targetValue (name "?{<low>,and(and(<num>,<any>),<let>)}")))
(example (utterance "lines starting in zero or more of leftquoatation ! rightquoatation or capital letter") (targetValue (name "?{startwith(<!>),star(<cap>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation and a letter before string leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<let>),<@>),<#>}")))
(example (utterance "lines starting 7 times with capital letter") (targetValue (name "?{startwith(<cap>)}")))
(example (utterance "lines with a letter , then a character , at least 6 times") (targetValue (name "?{concat(<let>,<any>)}")))
(example (utterance "lines without a lower case letter at least 1 time") (targetValue (name "?{repeatatleast(<low>,1)}")))
(example (utterance "lines that contain a character followed by a letter , at least twice") (targetValue (name "?{repeatatleast(<any>,2),<let>}")))
(example (utterance "lines with only 4 or more characters or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines containing lower-case characters") (targetValue (name "?{and(<low>,<any>)}")))
(example (utterance "lines with an uppercase alphabet character after an upper or lower case a , e , i , o or u") (targetValue (name "?{concat(<vow>,<cap>)}")))
(example (utterance "lines ending with leftquoatation ! rightquoatation at least 2 times") (targetValue (name "?{endwith(<!>)}")))
(example (utterance "lines with a vowel before lower-case letter , number , and character") (targetValue (name "?{<vow>,and(and(<low>,<num>),<any>)}")))
(example (utterance "lines with 2 or more vowels before a lower-case letter") (targetValue (name "?{repeatatleast(<vow>,2),<low>}")))
(example (utterance "a vowel preceded by only capitals") (targetValue (name "?{star(<cap>),<vow>}")))
(example (utterance "lines that start with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation , 4 or more times") (targetValue (name "?{startwith(<@>),repeatatleast(<!>,4)}")))
(example (utterance "lines with words and only 1 or more of a capital letter") (targetValue (name "?{<cap>}")))
(example (utterance "items with a small letter preceding a numeral at least5times .") (targetValue (name "?{concat(<low>,<num>)}")))
(example (utterance "lines with a number before leftquoatation ! rightquoatation at least 4 times") (targetValue (name "?{repeatatleast(<num>,4),<!>}")))
(example (utterance "lines with a letter , leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{or(<let>,<!>),<@>}")))
(example (utterance "lines with words without a capital letter or string leftquoatation ! rightquoatation") (targetValue (name "?{and(<cap>,<!>)}")))
(example (utterance "lines have to have at least 1 string leftquoatation ! rightquoatation with a lower case letter") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines in which a letter is at the end on no less than 1 occasion") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines wherein a letter is contained 6 or more times") (targetValue (name "?{contain(<let>)}")))
(example (utterance "the lines that contain leftquoatation ! rightquoatation repeated 6 or more times followed by a lower-case letter") (targetValue (name "?{repeatatleast(<!>,6),<low>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before letter , 2 or more times") (targetValue (name "?{<!>,repeatatleast(<let>,2)}")))
(example (utterance "items with leftquoatation ! rightquoatation preceding leftquoatation @ rightquoatation preceding an upper case letter .") (targetValue (name "?{<!>,concat(<@>,<cap>)}")))
(example (utterance "lines with a character , capital letter , or string leftquoatation ! rightquoatation before a letter") (targetValue (name "?{or(or(<any>,<cap>),<!>),<let>}")))
(example (utterance "lines with a number following a capital letter at least 2 times") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines without a number or vowel , any amount of times") (targetValue (name "?{and(star(<num>),<vow>)}")))
(example (utterance "lines without leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{concat(or(<!>,<@>),<#>)}")))
(example (utterance "items with a small letter or letter at least twice .") (targetValue (name "?{or(<low>,<let>)}")))
(example (utterance "lines with either leftquoatation ! rightquoatation , a capital letter , or leftquoatation @ rightquoatation followed by a number") (targetValue (name "?{or(or(<!>,<cap>),<@>),<num>}")))
(example (utterance "lines with zero or more lower-case letters before a number") (targetValue (name "?{star(<low>),contain(<num>)}")))
(example (utterance "lines with a vowel and a lower-case letter before a capital letter iin them") (targetValue (name "?{and(<vow>,<low>),<cap>}")))
(example (utterance "lines having no letter or a number") (targetValue (name "?{or(<let>,<num>)}")))
(example (utterance "lines that contain a certain character zero or more times") (targetValue (name "?{<any>}")))
(example (utterance "lines with 4 or more of a capital letter before vowel") (targetValue (name "?{concat(<cap>,<vow>)}")))
(example (utterance "lines need to have either a lower-case letter , a number , or a letter before leftquoatation ! rightquoatation") (targetValue (name "?{or(or(<num>,<low>),<let>),<!>}")))
(example (utterance "lines not having a character , capital letter , or number") (targetValue (name "?{or(or(<any>,<cap>),<num>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , zero or more times or leftquoatation @ rightquoatation and a character in them") (targetValue (name "?{star(<!>),and(<@>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation not followed in leftquoatation @ rightquoatation , at least 1 time") (targetValue (name "?{<!>,not(repeatatleast(<@>,1))}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a number , 4 or more times in them") (targetValue (name "?{contain(and(<!>,<num>))}")))
(example (utterance "lines with a character or starting in capital letter") (targetValue (name "?{contain(<any>),startwith(<cap>)}")))
(example (utterance "lines with a lower-case letter containing a number 7 times") (targetValue (name "?{<low>,contain(repeatatleast(<num>,7))}")))
(example (utterance "lines with a lower-case letter after a character and a capital letter") (targetValue (name "?{and(<any>,<cap>),<low>}")))
(example (utterance "lines containing a letter , leftquoatation ! rightquoatation , and a character") (targetValue (name "?{and(<let>,<!>),<any>}")))
(example (utterance "lines that don't begin with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{startwith(or(<!>,<@>))}")))
(example (utterance "lines where a character can only come after a letter is used") (targetValue (name "?{<let>,contain(<any>)}")))
(example (utterance "lines ending with a lower-case letter") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines having either leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , or number before a capital") (targetValue (name "?{or(or(<!>,<@>),<num>),<cap>}")))
(example (utterance "lines do not have a character , leftquoatation ! rightquoatation , and not a letter'") (targetValue (name "?{and(and(<any>,<!>),<let>)}")))
(example (utterance "lines having either a letter , string leftquoatation ! rightquoatation , or a lower-case letter before a capital letter") (targetValue (name "?{or(or(<let>,<!>),<low>),<cap>}")))
(example (utterance "lines which are not having a character before a vowel") (targetValue (name "?{not(<any>),<vow>}")))
(example (utterance "lines starting with number followed by vowel , 3 or more times") (targetValue (name "?{startwith(concat(<num>,<vow>))}")))
(example (utterance "lines with vowel , 5 or more times before letter") (targetValue (name "?{repeatatleast(<vow>,5),<let>}")))
(example (utterance "lines without a number , leftquoatation ! rightquoatation , or a letter") (targetValue (name "?{or(or(<num>,<!>),<let>)}")))
(example (utterance "lines with vowel before either string leftquoatation ! rightquoatation , number , or string leftquoatation @ rightquoatation") (targetValue (name "?{<vow>,or(or(<!>,<num>),<@>)}")))
(example (utterance "lines wherein 5 or more times a letter precedes a vowel") (targetValue (name "?{<let>,repeatatleast(<vow>,5)}")))
(example (utterance "lines with a number after a vowel") (targetValue (name "?{<vow>,<num>}")))
(example (utterance "lines with words that contain a character or a vowel followed by leftquoatation ! rightquoatation") (targetValue (name "?{or(<any>,<vow>),<!>}")))
(example (utterance "lines starting in a capital letter or string leftquoatation ! rightquoatation") (targetValue (name "?{startwith(or(<cap>,<!>))}")))
(example (utterance "lines with a number and string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<num>),repeatatleast(<!>,1)}")))
(example (utterance "items with leftquoatation ! rightquoatation or numeral at least6times .") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "lines with words with a letter , capital letter , and character") (targetValue (name "?{and(<let>,<cap>),<any>}")))
(example (utterance "lines with 2 or more of a vowel or lower-case letter") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines having a  letter , or leftquoatation ! rightquoatation before a vowel") (targetValue (name "?{or(or(<cap>,<low>),<!>),<vow>}")))
(example (utterance "lines with a letter before a character or string leftquoatation ! rightquoatation") (targetValue (name "?{<let>,or(<any>,<!>)}")))
(example (utterance "lines containing he word leftquoatation ! rightquoatation or a number followed by the word leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<num>),<@>}")))
(example (utterance "lines with 2 or more letters before a number") (targetValue (name "?{repeatatleast(<let>,2),<num>}")))
(example (utterance "lines without a character not ending in string leftquoatation ! rightquoatation") (targetValue (name "?{and(<any>,endwith(<!>))}")))
(example (utterance "lines with words starting in capital letter or string leftquoatation ! rightquoatation") (targetValue (name "?{or(<cap>,<!>)}")))
(example (utterance "lines where leftquoatation ! rightquoatation has a character or a capital letter in front of it .") (targetValue (name "?{or(<cap>,<any>),<!>}")))
(example (utterance "lines starting with string leftquoatation ! rightquoatation , 6 or more times before vowel") (targetValue (name "?{startwith(repeatatleast(<!>,6)),<vow>}")))
(example (utterance "lines with 6 or more numbers before a letter") (targetValue (name "?{repeatatleast(<num>,6),<let>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation , a character , a letter , and a lower-case letter") (targetValue (name "?{<!>,and(and(<any>,<let>),<low>)}")))
(example (utterance "lines with words and ending in string leftquoatation ! rightquoatation") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines with a capital t least once before a letter followed by string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<cap>,1),concat(<let>,<!>)}")))
(example (utterance "lines with at least 1 leftquoatation ! rightquoatation or leftquoatation @ rightquoatation and a number") (targetValue (name "?{repeatatleast(<!>,1),and(<@>,<num>)}")))
(example (utterance "lines with vowel or string leftquoatation ! rightquoatation before lower-case letter") (targetValue (name "?{or(<vow>,<!>),<low>}")))
(example (utterance "lines with no number 3 times") (targetValue (name "?{repeatatleast(<num>,3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , string leftquoatation @ rightquoatation , or a character before a letter") (targetValue (name "?{or(or(<!>,<@>),<any>),<let>}")))
(example (utterance "leftquoatation ! rightquoatation string  or a letter appears 5 or more times in the lines") (targetValue (name "?{or(<!>,<let>)}")))
(example (utterance "lines with a character coming after a number or leftquoatation ! rightquoatation") (targetValue (name "?{or(<!>,<num>),<any>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or leftquoatation @ rightquoatation or lower-case letter before a number") (targetValue (name "?{or(or(<!>,<@>),<low>),<num>}")))
(example (utterance "lines with words with at least 1 character") (targetValue (name "?{repeatatleast(<any>,1),<let>}")))
(example (utterance "lines with 4 or more of a letter or character") (targetValue (name "?{or(<let>,<any>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , a number , and a letter") (targetValue (name "?{and(<!>,<num>),<let>}")))
(example (utterance "lines that have at least6occurrences of a lower-case letter following a capital letter") (targetValue (name "?{<cap>,repeatatleast(<low>,6)}")))
(example (utterance "lines not having a letter and containing a capital") (targetValue (name "?{not(<let>),contain(<cap>)}")))
(example (utterance "items with leftquoatation ! rightquoatation , a letter , and leftquoatation @ rightquoatation preceding a numeral .") (targetValue (name "?{and(and(<@>,<let>),<!>),<num>}")))
(example (utterance "lines where leftquoatation ! rightquoatation does not follow a character") (targetValue (name "?{not(<any>),contain(<!>)}")))
(example (utterance "lines with words with zero or more of a character with string leftquoatation ! rightquoatation") (targetValue (name "?{star(<any>),<!>}")))
(example (utterance "lines having character , string leftquoatation ! rightquoatation , and capital letter before string leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<any>,<!>),<cap>),<@>}")))
(example (utterance "line with leftquoatation ! rightquoatation , lower-case letter , or vowel before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<!>,<low>),<vow>),<@>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a character , letter , and string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(and(<any>,<let>),<@>)}")))
(example (utterance "lines that do not start with a number .") (targetValue (name "?{star(startwith(<num>))}")))
(example (utterance "lines starting in string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before leftquoatation # rightquoatation") (targetValue (name "?{or(<!>,concat(<@>,<#>))}")))
(example (utterance "lines that contain a character and a vowel followed by a letter") (targetValue (name "?{and(<any>,<vow>),<let>}")))
(example (utterance "lines with 2 or more of a character before letter") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "items with") (targetValue (name "?{<!>,not(and(<@>,<num>))}")))
(example (utterance "lines with a number and letter before string leftquoatation ! rightquoatation") (targetValue (name "?{and(<num>,<let>),<!>}")))
(example (utterance "lines that don't have leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{not(<!>),and(and(<@>,<cap>),<#>)}")))
(example (utterance "lines with words and zero or more of a lower-case letter") (targetValue (name "?{contain(<low>)}")))
(example (utterance "lines that have either leftquoatation ! rightquoatation or a lower-case or capital letter") (targetValue (name "?{or(<!>,<low>),<cap>}")))
(example (utterance "lines ending in and with only a letter") (targetValue (name "?{endwith(<let>)}")))
(example (utterance "lines with 7 or more capital letters or vowels before a letter") (targetValue (name "?{repeatatleast(<cap>,7),concat(<vow>,<let>)}")))
(example (utterance "items with a small letter preceding leftquoatation ! rightquoatation or a numeral .") (targetValue (name "?{<low>,or(<num>,<!>)}")))
(example (utterance "lines with a vowel before letter") (targetValue (name "?{repeatatleast(contain(<vow>),1),<let>}")))
(example (utterance "lines with a character , letter , and vowel before string leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<let>),<vow>),<!>}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before not containing only string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,not(<@>)}")))
(example (utterance "lines not containing a lower-case letter before a capital") (targetValue (name "?{contain(concat(<low>,<cap>))}")))
(example (utterance "lines starting with capital letter or vowel") (targetValue (name "?{or(<cap>,<vow>)}")))
(example (utterance "lines ending with character at least once before capital letter") (targetValue (name "?{repeatatleast(endwith(<any>),1),<cap>}")))
(example (utterance "lines containing only string leftquoatation ! rightquoatation , 7 or more times") (targetValue (name "?{contain(<!>)}")))
(example (utterance "lines not having number , not a lower-case letter , and not a vowel") (targetValue (name "?{and(and(<num>,<low>),<vow>)}")))
(example (utterance "lines with words with a letter and string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation") (targetValue (name "?{<let>,concat(<!>,<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before a lower-case letter and string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,and(<low>,<@>)}")))
(example (utterance "lines with a lower-case letter after leftquoatation ! rightquoatation , a capital letter , and a vowel") (targetValue (name "?{and(and(<!>,<cap>),<vow>),<low>}")))
(example (utterance "lines with a character before number then capital letter") (targetValue (name "?{<any>,concat(<num>,<cap>)}")))
(example (utterance "lines ending with a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{endwith(repeatatleast(<low>,1)),<!>}")))
(example (utterance "lines with a letter , zero or more times .") (targetValue (name "?{or(<let>,<low>)}")))
(example (utterance "lines with words leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{<!>}")))
(example (utterance "lines starting in leftquoatation ! rightquoatation or a letter") (targetValue (name "?{startwith(or(<!>,<let>))}")))
(example (utterance "lines that have words ending with a lower-case letter or leftquoatation ! rightquoatation") (targetValue (name "?{or(<low>,<!>)}")))
(example (utterance "lines containing 6 characters preceding a vowel") (targetValue (name "?{<any>,repeatatleast(<vow>,6)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation , capital letter , a number and with or without a letter .") (targetValue (name "?{star(<let>),and(and(<num>,<!>),<cap>)}")))
(example (utterance "lines which are starting with a lower-case letter before leftquoatation ! rightquoatation or a letter") (targetValue (name "?{or(concat(<low>,<!>),<let>)}")))
(example (utterance "lines which are containing either a capital letter , a letter , or a character") (targetValue (name "?{or(or(<cap>,<let>),<any>)}")))
(example (utterance "lines with a number after a character , a vowel , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<any>,<vow>),<!>),<num>}")))
(example (utterance "lines that end with 3 or more vowels or the word leftquoatation ! rightquoatation") (targetValue (name "?{endwith(or(<vow>,<!>))}")))
(example (utterance "lines with 2 or more vowels or characters") (targetValue (name "?{or(<vow>,<any>)}")))
(example (utterance "lines whiich are ending with a vowel followed by a lower-case letter") (targetValue (name "?{concat(<vow>,<low>)}")))
(example (utterance "lines that ends with words containing a capital letter or a letter") (targetValue (name "?{or(contain(<cap>),<let>)}")))
(example (utterance "lines containing 2 or more numbers followed by a lower-case letter") (targetValue (name "?{repeatatleast(<num>,2),<low>}")))
(example (utterance "lines having leftquoatation ! rightquoatation , a lower-case letter , and a number") (targetValue (name "?{and(and(<!>,<low>),<num>),<any>}")))
(example (utterance "lines with 3 or more of a vowel or number") (targetValue (name "?{or(<vow>,<num>)}")))
(example (utterance "lower cased letter is first then leftquoatation ! rightquoatation on lines") (targetValue (name "?{concat(<low>,<!>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation or lower-case letter before number") (targetValue (name "?{or(<!>,<low>),<num>}")))
(example (utterance "lines containing a number before the ending which terminates with a leftquoatation ! rightquoatation string") (targetValue (name "?{<num>,endwith(<!>)}")))
(example (utterance "lines with a character , number , or a lower case-letter") (targetValue (name "?{or(or(<any>,<num>),<low>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation and string leftquoatation @ rightquoatation or vowel , 5 or more times") (targetValue (name "?{or(and(<!>,<@>),<vow>)}")))
(example (utterance "lines beginning at least 1 number or leftquoatation ! rightquoatation") (targetValue (name "?{or(repeatatleast(<num>,1),<!>)}")))
(example (utterance "lines that end with a combination that has a vowel , the word leftquoatation ! rightquoatation , or the word leftquoatation @ rightquoatation") (targetValue (name "?{contain(or(or(<vow>,<!>),<@>))}")))
(example (utterance "lines containing dog or any numbers") (targetValue (name "?{or(<!>,<num>)}")))
(example (utterance "line with words and a capital letter before a character") (targetValue (name "?{<cap>,<any>}")))
(example (utterance "lines which are ending with a capital letter and a character or leftquoatation ! rightquoatation") (targetValue (name "?{endwith(<cap>),or(<any>,<!>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation and 'truck and a character before a capital letter") (targetValue (name "?{and(and(<!>,<@>),<any>),<cap>}")))
(example (utterance "lines with a small letter before a word containing a capital , as well as a number") (targetValue (name "?{<low>,and(and(<cap>,<num>),<vow>)}")))
(example (utterance "lines that doesn't have leftquoatation ! rightquoatation before5or more of leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(concat(<!>,<@>),5)}")))
(example (utterance "lines not containing a letter vowel") (targetValue (name "?{or(contain(<let>),<vow>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation not before a character") (targetValue (name "?{star(<!>),not(<any>)}")))
(example (utterance "lines where a vowel is preceded by a letter or capital letter") (targetValue (name "?{or(<let>,<cap>),<vow>}")))
(example (utterance "lines containing words with lower-case letter , 2 or more times") (targetValue (name "?{repeatatleast(<low>,2)}")))
(example (utterance "lines that have a lower-case letter followed by either a vowel , capital letter , or a letter") (targetValue (name "?{or(or(<cap>,<vow>),<let>),<low>}")))
(example (utterance "lines which have a lower-case letter followed by a character before a capital letter") (targetValue (name "?{concat(<low>,<any>),<cap>}")))
(example (utterance "lines with a lower-case letter before leftquoatation ! rightquoatation") (targetValue (name "?{<low>,contain(<!>)}")))
(example (utterance "lines with a character after a letter , leftquoatation ! rightquoatation , or leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<let>,<!>),<@>),<any>}")))
(example (utterance "lines with a vowel , capital letter , or string leftquoatation ! rightquoatation before a character") (targetValue (name "?{or(or(<vow>,<cap>),<!>),<any>}")))
(example (utterance "lines ending in a number or character") (targetValue (name "?{or(endwith(<num>),<any>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation before string leftquoatation @ rightquoatation , string leftquoatation # rightquoatation , and a letter") (targetValue (name "?{<!>,and(and(<@>,<#>),<let>)}")))
(example (utterance "lines with words with a number and string leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(<num>,1),<!>}")))
(example (utterance "lines with characters followed by a capital letter at least 1 time") (targetValue (name "?{concat(<any>,<cap>)}")))
(example (utterance "lines that have 2 or more combinations consisting of the word leftquoatation ! rightquoatation followed by a number") (targetValue (name "?{concat(<!>,<num>)}")))
(example (utterance "lines containing a letter after a character , leftquoatation ! rightquoatation , and leftquoatation @ rightquoatation") (targetValue (name "?{and(and(<any>,<!>),<@>),<let>}")))
(example (utterance "lines with words with string leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , leftquoatation # rightquoatation , and leftquoatation $ rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<#>),<$>}")))
(example (utterance "lines with words , a number , and leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{and(<num>,<!>),<@>}")))
(example (utterance "lines in which leftquoatation ! rightquoatation is followed by a letter , a character , and a leftquoatation @ rightquoatation string") (targetValue (name "?{<!>,and(and(<let>,<any>),<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before capital letter with a vowel") (targetValue (name "?{concat(<!>,<cap>),contain(<vow>)}")))
(example (utterance "lines which end 6 or more times with a capital letter") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation preceding a character which precedes a vowel") (targetValue (name "?{<!>,concat(<any>,<vow>)}")))
(example (utterance "lines with letter before either string leftquoatation ! rightquoatation , capital letter , or character") (targetValue (name "?{<let>,or(or(<!>,<cap>),<any>)}")))
(example (utterance "before a character lines having a capital letter , the string 'dog , and leftquoatation ! rightquoatation .") (targetValue (name "?{and(and(<cap>,<!>),<!>),<any>}")))
(example (utterance "lines that have a lower case letter , vowel and letter") (targetValue (name "?{and(<low>,<vow>),<let>}")))
(example (utterance "lines with leftquoatation ! rightquoatation before leftquoatation @ rightquoatation before leftquoatation # rightquoatation , leftquoatation $ rightquoatation , or a character") (targetValue (name "?{concat(<!>,<@>),or(or(<#>,<$>),<any>)}")))
(example (utterance "lines with a capital letter or vowel before lower-case letter") (targetValue (name "?{or(<cap>,<vow>),<low>}")))
(example (utterance "lines beginning with a letter that's lower-case and followed by a capital letter") (targetValue (name "?{concat(<low>,<cap>)}")))
(example (utterance "lines with words with zero or more of a lower-case letter before letter") (targetValue (name "?{concat(<low>,<let>)}")))
(example (utterance "lines in which a character comes 5 or more times .") (targetValue (name "?{<any>}")))
(example (utterance "lines which are not having leftquoatation ! rightquoatation , a character , or a vowel") (targetValue (name "?{or(or(<!>,<any>),<vow>)}")))
(example (utterance "lines beginning with a vowel or a lower-case letter") (targetValue (name "?{or(<vow>,<low>)}")))
(example (utterance "lines with 3 or more of a capital letter or number") (targetValue (name "?{or(<cap>,<num>)}")))
(example (utterance "lines containing at least 1 capital letter before a number") (targetValue (name "?{repeatatleast(<cap>,1),<num>}")))
(example (utterance "lines with leftquoatation ! rightquoatation or a character then string leftquoatation @ rightquoatation") (targetValue (name "?{or(<!>,<any>),<@>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation and a vowel") (targetValue (name "?{<!>,endwith(<vow>)}")))
(example (utterance "lines ending with string leftquoatation ! rightquoatation before not leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),not(<@>)}")))
(example (utterance "lines with capital letter and leftquoatation ! rightquoatation before leftquoatation @ rightquoatation") (targetValue (name "?{and(<cap>,<!>),<@>}")))
(example (utterance "lines ending with number or string leftquoatation ! rightquoatation , 2 or more times") (targetValue (name "?{endwith(or(<num>,<!>))}")))
(example (utterance "lines with 7 or more lower-case letter or a number") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines that start with the word leftquoatation ! rightquoatation , another character and the word leftquoatation @ rightquoatation followed by a capital letter") (targetValue (name "?{and(and(<!>,<any>),<@>),<cap>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{and(<!>,<@>)}")))
(example (utterance "lines with words containing a character and a lowercase letter") (targetValue (name "?{<any>,<low>}")))
(example (utterance "lines with words ending with a capital letter , leftquoatation ! rightquoatation , as well as a vowel") (targetValue (name "?{and(and(<cap>,<!>),<vow>)}")))
(example (utterance "lines with a capital letter followed by zero or more numbers") (targetValue (name "?{concat(<cap>,<num>)}")))
(example (utterance "lines that have words that have a letter and a character , and also the leftquoatation ! rightquoatation string") (targetValue (name "?{and(<any>,<let>),<!>}")))
(example (utterance "lines having either a lower-case letter , string leftquoatation ! rightquoatation , or vowel before string leftquoatation @ rightquoatation") (targetValue (name "?{or(or(<low>,<!>),<vow>),<@>}")))
(example (utterance "lines with 5 or more small letter words") (targetValue (name "?{<low>}")))
(example (utterance "lines without leftquoatation ! rightquoatation or a vowel or number") (targetValue (name "?{and(and(<!>,<vow>),<num>)}")))
(example (utterance "lines with words and only 6 or more letters") (targetValue (name "?{<let>}")))
(example (utterance "lines with a letter and leftquoatation ! rightquoatation before a lower-case letter in them") (targetValue (name "?{and(<let>,<!>),<low>}")))
(example (utterance "lines with a vowel after a number or a capital letter") (targetValue (name "?{and(<num>,<cap>),<vow>}")))
(example (utterance "lines that end with a capital letter") (targetValue (name "?{endwith(<cap>)}")))
(example (utterance "lines not ending in a number") (targetValue (name "?{endwith(repeatatleast(<num>,1))}")))
(example (utterance "lines starting with a vowel or a character , 6 or more times in them") (targetValue (name "?{startwith(<vow>),repeatatleast(<any>,6)}")))
(example (utterance "leftquoatation ! rightquoatation in lines with words 3 times") (targetValue (name "?{<!>}")))
(example (utterance "lines which do not contain leftquoatation ! rightquoatation or leftquoatation @ rightquoatation") (targetValue (name "?{repeatatleast(or(<!>,<@>),1)}")))
(example (utterance "lines with a vowel , a capital letter , string leftquoatation ! rightquoatation , then a lower-case letter .") (targetValue (name "?{and(and(<vow>,<cap>),<!>),<low>}")))
(example (utterance "lines with words that have leftquoatation ! rightquoatation after leftquoatation @ rightquoatation") (targetValue (name "?{<@>,<!>}")))
(example (utterance "lines without a letter , a number , and leftquoatation ! rightquoatation") (targetValue (name "?{and(and(<let>,<num>),<!>)}")))
(example (utterance "lines with a letter and number") (targetValue (name "?{or(<low>,<num>)}")))
(example (utterance "lines with string leftquoatation ! rightquoatation and number before lower-case letter") (targetValue (name "?{and(<!>,<num>),<low>}")))
(example (utterance "lines with 5 or more of a number or capital letter") (targetValue (name "?{or(<num>,<cap>)}")))
(example (utterance "lines with words with either a vowel , string leftquoatation ! rightquoatation , or character") (targetValue (name "?{or(<vow>,<!>),<any>}")))
(example (utterance "lines with words and a character or string leftquoatation ! rightquoatation before a capital letter") (targetValue (name "?{or(<any>,<!>),<cap>}")))
(example (utterance "lines starting with a character after a number") (targetValue (name "?{startwith(<num>),<any>}")))
(example (utterance "a lower-case letter after lines with a character or a number .") (targetValue (name "?{or(<any>,<num>),<low>}")))
(example (utterance "lines containing a character , lower-case , and capital letter") (targetValue (name "?{and(and(<low>,<any>),<cap>)}")))
(example (utterance "lines with a number or string leftquoatation ! rightquoatation or leftquoatation @ rightquoatation before string leftquoatation # rightquoatation") (targetValue (name "?{or(or(<num>,<!>),<@>),<#>}")))
(example (utterance "lines that end with a letter or a vowel and a lower-case letter") (targetValue (name "?{or(and(<vow>,<low>),<let>)}")))
(example (utterance "items with a letter or a character .") (targetValue (name "?{<any>,<let>}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation followed by leftquoatation @ rightquoatation") (targetValue (name "?{concat(<!>,<@>)}")))
(example (utterance "lines with leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , and a character preceding leftquoatation # rightquoatation") (targetValue (name "?{and(and(<!>,<@>),<any>),<#>}")))
(example (utterance "lines not containing a number nor a character") (targetValue (name "?{and(<num>,<any>)}")))
(example (utterance "lines which have not having a character and not starting with a capital letter") (targetValue (name "?{and(<any>,startwith(<cap>))}")))
(example (utterance "lines with a lower case letter or leftquoatation ! rightquoatation , zero times or more") (targetValue (name "?{or(<!>,<low>)}")))
(example (utterance "lines containing a lower-case letter preceding a capital letter") (targetValue (name "?{<low>,<cap>}")))
(example (utterance "lines containing leftquoatation ! rightquoatation or any character , at least6instances") (targetValue (name "?{or(<any>,<!>)}")))
(example (utterance "lines ending with a character or a capital letter , 2 or more times in them") (targetValue (name "?{endwith(<any>),repeatatleast(<cap>,2)}")))
(example (utterance "lines which contain leftquoatation ! rightquoatation or a vowel , 3 or more times") (targetValue (name "?{or(<!>,<vow>)}")))
(example (utterance "lines with 6 characters or more before a lower-case letter") (targetValue (name "?{repeatatleast(<any>,6),<low>}")))
(example (utterance "lines beginning with leftquoatation ! rightquoatation 4 or more times") (targetValue (name "?{startwith(<!>)}")))
(example (utterance "the lines containing a vowel before leftquoatation ! rightquoatation , leftquoatation @ rightquoatation , or leftquoatation # rightquoatation") (targetValue (name "?{<vow>,and(and(<!>,<@>),<#>)}")))
(example (utterance "the lines starting with words that contain a number coming before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<num>,<!>)}")))
(example (utterance "lines containing leftquoatation ! rightquoatation followed by a lower-case letter that precedes a vowel") (targetValue (name "?{concat(<!>,<low>),<vow>}")))
(example (utterance "lines with words with letter , string leftquoatation ! rightquoatation , and a vowel") (targetValue (name "?{and(<let>,<!>),<vow>}")))
(example (utterance "lines with a vowel or 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{or(<vow>,<!>)}")))
(example (utterance "lines with a number or a capital letter before leftquoatation ! rightquoatation in them") (targetValue (name "?{or(<num>,<cap>),<!>}")))
(example (utterance "lines with a character followed by 3 or more of leftquoatation ! rightquoatation") (targetValue (name "?{repeatatleast(concat(<any>,<!>),3)}")))
(example (utterance "items with a beginning numeral or character .") (targetValue (name "?{or(<any>,<num>)}")))
(example (utterance "lines with words and string leftquoatation ! rightquoatation or ending in string leftquoatation @ rightquoatation") (targetValue (name "?{<!>,endwith(<@>)}")))
(example (utterance "lines with a number and a capital letter before leftquoatation ! rightquoatation in them") (targetValue (name "?{and(<num>,<cap>),<!>}")))
(example (utterance "lines with a character before leftquoatation ! rightquoatation") (targetValue (name "?{concat(<any>,<!>)}")))
(example (utterance "lines containing string leftquoatation ! rightquoatation followed by string leftquoatation @ rightquoatation , 3 or more times") (targetValue (name "?{repeatatleast(concat(<!>,<@>),3)}")))
(example (utterance "lines with leftquoatation ! rightquoatation before letter or vowel") (targetValue (name "?{<!>,or(<let>,<vow>)}")))
(example (utterance "lines ending with words with leftquoatation ! rightquoatation and leftquoatation @ rightquoatation , leftquoatation # rightquoatation , or leftquoatation $ rightquoatation") (targetValue (name "?{endwith(<!>),or(or(<@>,<#>),<$>)}")))
(example (utterance "lines ending in string leftquoatation ! rightquoatation and zero or more of string leftquoatation @ rightquoatation") (targetValue (name "?{endwith(<!>),star(<@>)}")))
(example (utterance "lines that contain a character or lowercase letter zero or more times") (targetValue (name "?{or(<any>,<low>)}")))
(example (utterance "lines containing words , leftquoatation ! rightquoatation , a vowe , or character") (targetValue (name "?{and(<!>,<vow>),<any>}")))
